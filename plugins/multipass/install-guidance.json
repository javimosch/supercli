{
  "plugin": "multipass",
  "binary": "multipass",
  "check": "which multipass",
  "install_steps": [
    "brew install multipass",
    "sudo snap install multipass",
    "Verify: multipass version",
    "supercli plugins install ./plugins/multipass --on-conflict replace --json"
  ],
  "note": "Official docs: https://multipass.run/docs. Available on macOS (brew), Linux (snap), and Windows. Supports launching multiple Ubuntu releases including 24.04 LTS and 22.04 LTS."
}
