{
  "plugin": "modsurfer",
  "binary": "modsurfer",
  "check": "which modsurfer",
  "install_steps": [
    "cargo install modsurfer",
    "Verify: modsurfer --version",
    "supercli plugins install ./plugins/modsurfer --on-conflict replace --json"
  ]
}