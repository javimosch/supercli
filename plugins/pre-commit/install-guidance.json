{
  "plugin": "pre-commit",
  "binary": "pre-commit",
  "check": "which pre-commit",
  "install_steps": [
    "pip install pre-commit",
    "Verify: pre-commit --version",
    "supercli plugins install ./plugins/pre-commit --on-conflict replace --json"
  ],
  "note": "Also available via brew: brew install pre-commit, or pipx: pipx install pre-commit. Requires a .pre-commit-config.yaml in the repo root. Supports over 3000+ hooks from the community at https://pre-commit.com/hooks.html."
}
