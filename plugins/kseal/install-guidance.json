{
  "plugin": "kseal",
  "binary": "kseal",
  "check": "which kseal",
  "install_steps": [
    "pip install kseal",
    "Verify: kseal --version"
  ],
  "note": "View and decode Kubernetes secrets including base64-encoded data and sealed secrets. Useful for debugging and inspecting cluster secrets."
}