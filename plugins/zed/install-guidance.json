{
  "plugin": "zed",
  "binary": "zed",
  "check": "which zed",
  "install_steps": ["brew install zed", "Verify: zed --version", "supercli plugins install ./plugins/zed --on-conflict replace --json"]
}
