{ "plugin": "circo", "binary": "circo", "check": "which circo", "install_steps": ["apt-get install circo 2>/dev/null || which circo", "Verify: circo --version", "supercli plugins install ./plugins/circo --on-conflict replace --json"] }
