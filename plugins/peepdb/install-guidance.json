{
  "plugin": "peepdb",
  "binary": "peepdb",
  "check": "which peepdb",
  "install_steps": [
    "pip install peepdb",
    "Verify: peepdb --version",
    "supercli plugins install ./plugins/peepdb --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
