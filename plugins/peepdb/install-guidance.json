{
  "plugin": "peepdb",
  "binary": "peepdb",
  "check": "which peepdb",
  "install_steps": [
    "pip install peepdb",
    "Verify: peepdb --version",
    "supercli plugins install ./plugins/peepdb --on-conflict replace --json"
  ],
  "note": "Requires mariadb development libraries. Install via: apt install libmariadb-dev or brew install mariadb."
}
