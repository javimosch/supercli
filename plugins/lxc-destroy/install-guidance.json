{
  "plugin": "lxc-destroy",
  "binary": "lxc-destroy",
  "check": "which lxc-destroy",
  "install_steps": [
    "apt-get install lxc-destroy 2>/dev/null || which lxc-destroy",
    "Verify: lxc-destroy --version",
    "supercli plugins install ./plugins/lxc-destroy --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}