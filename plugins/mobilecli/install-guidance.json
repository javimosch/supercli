{
  "plugin": "mobilecli",
  "binary": "mobilecli",
  "check": "which mobilecli",
  "install_steps": [
    "npm install -g mobilecli@latest",
    "Verify: mobilecli --version",
    "supercli plugins install ./plugins/mobilecli --on-conflict replace --json"
  ],
  "note": "Requires Android SDK with adb in PATH for Android support, and Xcode Command Line Tools for iOS simulator support on macOS. Also available via npx: npx mobilecli@latest."
}
