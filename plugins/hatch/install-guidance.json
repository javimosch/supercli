{
  "plugin": "hatch",
  "binary": "hatch",
  "check": "which hatch",
  "install_steps": [
    "pip install hatch",
    "Verify: hatch --version",
    "supercli plugins install ./plugins/hatch --on-conflict replace --json"
  ],
  "note": "Requires Python. Also available via: brew, conda."
}
