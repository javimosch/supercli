{
  "plugin": "shutl",
  "binary": "shutl",
  "check": "shutl --version",
  "install_steps": [
    "brew tap k15r/shutl",
    "brew install shutl",
    "Verify: shutl --version",
    "supercli plugins install ./plugins/shutl --on-conflict replace --json"
  ],
  "note": "Scripts are stored in ~/.shutl directory by default. Use metadata comments (#@description, #@arg, #@flag) in shell scripts to define command behavior. Enable completion with '. <(COMPLETE=bash shutl)' in .bashrc."
}