{ "plugin": "code", "binary": "code", "check": "which code", "install_steps": ["apt-get install code 2>/dev/null || which code", "Verify: code --version", "supercli plugins install ./plugins/code --on-conflict replace --json"] }
