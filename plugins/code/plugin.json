{
  "name": "code",
  "version": "0.1.0",
  "description": "CLI tool: code",
  "source": "https://packages.debian.org/code",
  "checks": [{ "type": "binary", "name": "code" }],
  "install_guidance": {
    "plugin": "code", "binary": "code", "check": "which code",
    "install_steps": ["apt-get install code 2>/dev/null || which code", "Verify: code --version", "supercli plugins install ./plugins/code --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "code", "resource": "_", "action": "_",
    "description": "Passthrough to code CLI",
    "adapter": "process", "adapterConfig": { "command": "code", "passthrough": true, "missingDependencyHelp": "Install code: apt-get install code" }, "args": []
  }]
}
