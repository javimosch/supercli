{
  "plugin": "grub-menulst2cfg",
  "binary": "grub-menulst2cfg",
  "check": "which grub-menulst2cfg",
  "install_steps": [
    "apt-get install grub-menulst2cfg 2>/dev/null || which grub-menulst2cfg",
    "Verify: grub-menulst2cfg --version",
    "supercli plugins install ./plugins/grub-menulst2cfg --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}