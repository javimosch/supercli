---
name: grub-menulst2cfg
description: Use this skill when the user wants to use grub-menulst2cfg, cli tool: grub-menulst2cfg.
---

# grub-menulst2cfg Plugin

CLI tool: grub-menulst2cfg.

## Commands
- `grub-menulst2cfg <resource> <action>` — Execute grub-menulst2cfg commands
- `grub-menulst2cfg self version` — Print grub-menulst2cfg version
- `grub-menulst2cfg _ _` — Passthrough to grub-menulst2cfg CLI

## Usage Examples
- "grub-menulst2cfg --help"
- "grub-menulst2cfg self version"

## Installation
```bash
apt-get install grub-menulst2cfg 2>/dev/null || which grub-menulst2cfg
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
