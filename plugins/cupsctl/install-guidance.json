{ "plugin": "cupsctl", "binary": "cupsctl", "check": "which cupsctl", "install_steps": ["apt-get install cups", "Verify: cupsctl --version", "supercli plugins install ./plugins/cupsctl --on-conflict replace --json"] }
