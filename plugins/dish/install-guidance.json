{
  "plugin": "dish",
  "binary": "dish",
  "check": "which dish",
  "install_steps": [
    "brew install dish",
    "Verify: dish --version",
    "supercli plugins install ./plugins/dish --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}