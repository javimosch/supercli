---
name: json-flatten
description: json-flatten — flatten nested JSON to dot-notation keys (e.g., {'user.name':'Alice'}). Handles arrays with index notation.
---
# json-flatten
json-flatten — flatten nested JSON to dot-notation keys (e.g., {'user.name':'Alice'}). Handles arrays with index notation.
