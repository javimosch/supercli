{
  "plugin": "ketch",
  "binary": "ketch",
  "check": "which ketch",
  "install_steps": ["brew install ketch", "Verify: ketch --version", "supercli plugins install ./plugins/ketch --on-conflict replace --json"]
}
