{
  "plugin": "ipcalc",
  "binary": "ipcalc",
  "check": "which ipcalc",
  "install_steps": [
    "brew install ipcalc",
    "Verify: ipcalc --version",
    "supercli plugins install ./plugins/ipcalc --on-conflict replace --json"
  ]
}
