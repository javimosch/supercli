{
  "plugin": "rdt",
  "binary": "rdt",
  "check": "which rdt",
  "install_steps": [
    "apt-get install rdt 2>/dev/null || which rdt",
    "Verify: rdt --version",
    "supercli plugins install ./plugins/rdt --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}