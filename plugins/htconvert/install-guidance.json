{
  "plugin": "htconvert",
  "binary": "htconvert",
  "check": "which htconvert",
  "install_steps": [
    "See https://github.com/lukechilds/htconvert",
    "Verify: htconvert --version",
    "supercli plugins install ./plugins/htconvert --on-conflict replace --json"
  ],
  "note": "See project repository"
}