{
  "plugin": "caire",
  "binary": "caire",
  "check": "which caire",
  "install_steps": [
    "go install github.com/esimov/caire/cmd/caire@latest",
    "Verify: caire --version",
    "supercli plugins install ./plugins/caire --on-conflict replace --json"
  ]
}