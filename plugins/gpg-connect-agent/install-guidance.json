{ "plugin": "gpg-connect-agent", "binary": "gpg-connect-agent", "check": "which gpg-connect-agent", "install_steps": ["apt-get install gnupg-agent", "Verify: gpg-connect-agent --version", "supercli plugins install ./plugins/gpg-connect-agent --on-conflict replace --json"] }
