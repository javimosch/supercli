{
  "plugin": "image-optimize",
  "binary": "image-optimize",
  "check": "which image-optimize",
  "install_steps": [
    "npm install -g image-optimize",
    "Verify: image-optimize --help",
    "supercli plugins install ./plugins/image-optimize --on-conflict replace --json"
  ],
  "note": "Image-optimize compresses images to reduce file size while maintaining quality. Supports optimizing single files or entire directories. Use -t to specify image type and -f to force overwrite."
}
