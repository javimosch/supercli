{
  "plugin": "jsawk",
  "binary": "jsawk",
  "check": "which jsawk",
  "install_steps": [
    "brew install jsawk",
    "Verify: jsawk --version",
    "supercli plugins install ./plugins/jsawk --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}