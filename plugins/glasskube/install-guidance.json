{
  "plugin": "glasskube",
  "binary": "glasskube",
  "check": "which glasskube",
  "install_steps": [
    "brew install glasskube",
    "Verify: glasskube version",
    "supercli plugins install ./plugins/glasskube --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
