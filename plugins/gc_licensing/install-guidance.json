{
  "plugin": "gc_licensing",
  "binary": "gc_licensing",
  "check": "which gc_licensing",
  "install_steps": ["pip install gc-licensing", "Verify: gc_licensing --version", "supercli plugins install ./plugins/gc_licensing --on-conflict replace --json"]
}
