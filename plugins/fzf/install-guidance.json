{
  "plugin": "fzf",
  "binary": "fzf",
  "check": "which fzf",
  "install_steps": [
    "brew install fzf",
    "Verify: fzf --version",
    "supercli plugins install ./plugins/fzf --on-conflict replace --json"
  ]
}