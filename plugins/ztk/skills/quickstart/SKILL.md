---
name: ztk
description: Use this skill when the user wants to use ztk for CLI proxy that reduces LLM token consumption by 78%+. Single Zig binary under 26.
---

# ztk Plugin

CLI proxy that reduces LLM token consumption by 78%+. Single Zig binary under 260KB. Zero dependencies.

## Commands

### General
- `ztk self version` — Print ztk version
- `ztk _ _` — Passthrough to ztk CLI

## Usage Examples
- "Run ztk"
- "Use ztk to CLI proxy that reduces LLM token consumption by 78%+. Single"

## Installation

```bash
pip install ztk
```

## Key Features
- CLI proxy that reduces LLM token consumption by 78%+. Single Zig binary under 260KB. Zero dependenci
- CLI tool, headless and scriptable
