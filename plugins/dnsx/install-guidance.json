{
  "plugin": "dnsx",
  "binary": "dnsx",
  "check": "which dnsx",
  "install_steps": [
    "brew install dnsx",
    "Verify: dnsx --version",
    "supercli plugins install ./plugins/dnsx --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/projectdiscovery/dnsx/cmd/dnsx@latest. Fast DNS toolkit for resolution, bruteforce, and wildcard filtering. Supports multiple record types and custom resolvers. Great for reconnaissance and security testing."
}
