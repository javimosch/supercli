---
name: seekdb
description: Use this skill when the user wants to use seekdb for The AI-native state store for agents. MySQL-compatible, embedded or server, hybr.
---

# seekdb Plugin

The AI-native state store for agents. MySQL-compatible, embedded or server, hybrid vector + full-text search, COW sandboxes (FORK/MERGE).

## Commands

### General
- `seekdb self version` — Print seekdb version
- `seekdb _ _` — Passthrough to seekdb CLI

## Usage Examples
- "Run seekdb"
- "Use seekdb to The AI-native state store for agents. MySQL-compatible, embe"

## Installation

```bash
pip install seekdb
```

## Key Features
- The AI-native state store for agents. MySQL-compatible, embedded or server, hybrid vector + full-tex
- CLI tool, headless and scriptable
