{
  "plugin": "seekdb",
  "binary": "seekdb",
  "check": "which seekdb",
  "install_steps": [
    "pip install seekdb",
    "Verify: seekdb --version",
    "supercli plugins install ./plugins/seekdb --on-conflict replace --json"
  ],
  "note": "Language: C++. Stars: 2611"
}