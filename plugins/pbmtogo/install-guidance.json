{
  "plugin": "pbmtogo",
  "binary": "pbmtogo",
  "check": "which pbmtogo",
  "install_steps": [
    "apt-get install pbmtogo 2>/dev/null || which pbmtogo",
    "Verify: pbmtogo --version",
    "supercli plugins install ./plugins/pbmtogo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}