{
  "name": "acpi",
  "version": "0.1.0",
  "description": "CLI tool: acpi",
  "source": "https://packages.debian.org/acpi",
  "checks": [{ "type": "binary", "name": "acpi" }],
  "install_guidance": {
    "plugin": "acpi", "binary": "acpi", "check": "which acpi",
    "install_steps": ["apt-get install acpi 2>/dev/null || which acpi", "Verify: acpi --version", "supercli plugins install ./plugins/acpi --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acpi", "resource": "_", "action": "_",
    "description": "Passthrough to acpi CLI",
    "adapter": "process", "adapterConfig": { "command": "acpi", "passthrough": true, "missingDependencyHelp": "Install acpi: apt-get install acpi" }, "args": []
  }]
}
