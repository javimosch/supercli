{ "plugin": "acpi", "binary": "acpi", "check": "which acpi", "install_steps": ["apt-get install acpi 2>/dev/null || which acpi", "Verify: acpi --version", "supercli plugins install ./plugins/acpi --on-conflict replace --json"] }
