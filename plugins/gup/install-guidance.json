{
  "plugin": "gup",
  "binary": "gup",
  "check": "which gup",
  "install_steps": [
    "brew install gup",
    "Verify: gup --version",
    "supercli plugins install ./plugins/gup --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}