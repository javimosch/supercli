{
  "plugin": "viewres",
  "binary": "viewres",
  "check": "which viewres",
  "install_steps": [
    "apt-get install viewres 2>/dev/null || which viewres",
    "Verify: viewres --version",
    "supercli plugins install ./plugins/viewres --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}