{
  "plugin": "postmap",
  "binary": "postmap",
  "check": "which postmap",
  "install_steps": [
    "# Install postmap",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postmap-linux-amd64 -o /usr/local/bin/postmap",
    "chmod +x /usr/local/bin/postmap",
    "# Or via package manager, see /usr/sbin/postmap"
  ]
}