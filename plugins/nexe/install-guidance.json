{
  "plugin": "nexe",
  "binary": "nexe",
  "check": "which nexe",
  "install_steps": [
    "npm install -g nexe",
    "Verify: nexe --version",
    "supercli plugins install ./plugins/nexe --on-conflict replace --json"
  ]
}