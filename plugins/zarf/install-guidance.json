{
  "plugin": "zarf",
  "binary": "zarf",
  "check": "which zarf",
  "install_steps": [
    "brew install zarf",
    "Verify: zarf version",
    "supercli plugins install ./plugins/zarf --on-conflict replace --json"
  ]
}
