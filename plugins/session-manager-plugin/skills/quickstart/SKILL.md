---
name: session-manager-plugin
description: Use this skill when the user wants to use session-manager-plugin, cli tool: session-manager-plugin.
---

# session-manager-plugin Plugin

CLI tool: session-manager-plugin.

## Commands
- `session-manager-plugin <resource> <action>` — Execute session-manager-plugin commands
- `session-manager-plugin self version` — Print session-manager-plugin version
- `session-manager-plugin _ _` — Passthrough to session-manager-plugin CLI

## Usage Examples
- "session-manager-plugin --help"
- "session-manager-plugin self version"

## Installation
```bash
apt-get install session-manager-plugin 2>/dev/null || which session-manager-plugin
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
