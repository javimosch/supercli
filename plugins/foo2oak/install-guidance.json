{
  "plugin": "foo2oak",
  "binary": "foo2oak",
  "check": "which foo2oak",
  "install_steps": [
    "apt-get install foo2oak 2>/dev/null || which foo2oak",
    "Verify: foo2oak --version",
    "supercli plugins install ./plugins/foo2oak --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}