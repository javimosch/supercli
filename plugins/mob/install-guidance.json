{
  "plugin": "mob",
  "binary": "mob",
  "check": "which mob",
  "install_steps": [
    "brew install mob",
    "Verify: mob --version",
    "supercli plugins install ./plugins/mob --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/remotemobprogramming/mob@latest. Optimized for pair and mob programming with smooth git handover. Uses WIP commits for seamless context switching between team members."
}
