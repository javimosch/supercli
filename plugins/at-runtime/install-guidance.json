{ "plugin": "at-runtime", "binary": "at-runtime", "check": "which at-runtime", "install_steps": ["apt-get install at-runtime 2>/dev/null || which at-runtime", "Verify: at-runtime --version", "supercli plugins install ./plugins/at-runtime --on-conflict replace --json"] }
