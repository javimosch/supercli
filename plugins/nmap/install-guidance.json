{
  "plugin": "nmap",
  "binary": "nmap",
  "check": "which nmap",
  "install_steps": [
    "brew install nmap",
    "Verify: nmap --version",
    "supercli plugins install ./plugins/nmap --on-conflict replace --json"
  ]
}
