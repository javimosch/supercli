{
  "plugin": "cron",
  "binary": "cron",
  "check": "which cron",
  "install_steps": [
    "go install github.com/lnquy/cron@latest",
    "Verify: cron --version",
    "supercli plugins install ./plugins/cron --on-conflict replace --json"
  ]
}