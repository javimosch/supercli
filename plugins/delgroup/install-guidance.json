{ "plugin": "delgroup", "binary": "delgroup", "check": "which delgroup", "install_steps": ["apt-get install delgroup 2>/dev/null || which delgroup", "Verify: delgroup --version", "supercli plugins install ./plugins/delgroup --on-conflict replace --json"] }
