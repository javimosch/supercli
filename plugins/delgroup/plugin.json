{
  "name": "delgroup",
  "version": "0.1.0",
  "description": "CLI tool: delgroup",
  "source": "https://packages.debian.org/delgroup",
  "checks": [{ "type": "binary", "name": "delgroup" }],
  "install_guidance": {
    "plugin": "delgroup", "binary": "delgroup", "check": "which delgroup",
    "install_steps": ["apt-get install delgroup 2>/dev/null || which delgroup", "Verify: delgroup --version", "supercli plugins install ./plugins/delgroup --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "delgroup", "resource": "_", "action": "_",
    "description": "Passthrough to delgroup CLI",
    "adapter": "process", "adapterConfig": { "command": "delgroup", "passthrough": true, "missingDependencyHelp": "Install delgroup: apt-get install delgroup" }, "args": []
  }]
}
