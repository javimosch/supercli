{ "plugin": "dm-reload", "binary": "dm-reload", "check": "which dm-reload", "install_steps": ["apt-get install dm-reload 2>/dev/null || which dm-reload", "Verify: dm-reload --version", "supercli plugins install ./plugins/dm-reload --on-conflict replace --json"] }
