{
  "name": "dm-reload",
  "version": "0.1.0",
  "description": "CLI tool: dm-reload",
  "source": "https://packages.debian.org/dm-reload",
  "checks": [{ "type": "binary", "name": "dm-reload" }],
  "install_guidance": {
    "plugin": "dm-reload", "binary": "dm-reload", "check": "which dm-reload",
    "install_steps": ["apt-get install dm-reload 2>/dev/null || which dm-reload", "Verify: dm-reload --version", "supercli plugins install ./plugins/dm-reload --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-reload", "resource": "_", "action": "_",
    "description": "Passthrough to dm-reload CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-reload", "passthrough": true, "missingDependencyHelp": "Install dm-reload: apt-get install dm-reload" }, "args": []
  }]
}
