{
  "plugin": "choose",
  "binary": "choose",
  "check": "which choose",
  "install_steps": [
    "brew install choose-rust",
    "Verify: choose --version",
    "supercli plugins install ./plugins/choose --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install choose. A human-friendly and fast alternative to cut and awk. Extract fields from text with intuitive syntax supporting negative indices, ranges, and custom separators. Great for shell pipelines."
}
