{
  "plugin": "matlab2tikz",
  "binary": "matlab2tikz",
  "check": "which matlab2tikz",
  "install_steps": [
    "brew install matlab2tikz",
    "Verify: matlab2tikz --version",
    "supercli plugins install ./plugins/matlab2tikz --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}