{
  "plugin": "systemd-socket-activate",
  "binary": "systemd-socket-activate",
  "check": "which systemd-socket-activate",
  "install_steps": [
    "apt-get install systemd-socket-activate 2>/dev/null || which systemd-socket-activate",
    "Verify: systemd-socket-activate --version",
    "supercli plugins install ./plugins/systemd-socket-activate --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}