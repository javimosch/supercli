{
  "plugin": "pandoc",
  "binary": "pandoc",
  "check": "which pandoc",
  "install_steps": ["brew install pandoc", "Verify: pandoc --version", "supercli plugins install ./plugins/pandoc --on-conflict replace --json"]
}
