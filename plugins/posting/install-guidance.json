{
  "plugin": "posting",
  "binary": "posting",
  "check": "which posting",
  "install_steps": [
    "pipx install posting",
    "Verify: posting --version"
  ],
  "note": "Also available via pip: pip install posting. Requires Python 3.11+. A keyboard-driven TUI HTTP client with collections, environments, and syntax highlighting."
}
