{
  "plugin": "netplan",
  "binary": "netplan",
  "check": "which netplan",
  "install_steps": [
    "apt-get install netplan 2>/dev/null || which netplan",
    "Verify: netplan --version",
    "supercli plugins install ./plugins/netplan --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}