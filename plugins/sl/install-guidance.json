{
  "plugin": "sl",
  "binary": "sl",
  "check": "which sl",
  "install_steps": [
    "apt-get install sl",
    "Verify: sl --version",
    "supercli plugins install ./plugins/sl --on-conflict replace --json"
  ]
}
