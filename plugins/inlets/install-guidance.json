{
  "plugin": "inlets",
  "binary": "inlets",
  "check": "which inlets",
  "install_steps": [
    "brew install inlets",
    "Verify: inlets --version",
    "supercli plugins install ./plugins/inlets --on-conflict replace --json"
  ]
}