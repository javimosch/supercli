{
  "plugin": "file-d",
  "binary": "file.d",
  "check": "which file.d",
  "install_steps": [
    "go install github.com/ozontech/file.d@latest",
    "Verify: file.d --version",
    "supercli plugins install ./plugins/file-d --on-conflict replace --json"
  ]
}