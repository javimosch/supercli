{
  "plugin": "ttysvr",
  "binary": "ttysvr",
  "check": "which ttysvr",
  "install_steps": ["cargo install ttysvr", "Verify: ttysvr --version", "supercli plugins install ./plugins/ttysvr --on-conflict replace --json"]
}
