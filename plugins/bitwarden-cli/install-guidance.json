{
  "plugin": "bitwarden-cli",
  "binary": "bw",
  "check": "which bw",
  "install_steps": [
    "npm install -g @bitwarden/cli",
    "Verify: bw --version",
    "supercli plugins install ./plugins/bitwarden-cli --on-conflict replace --json"
  ],
  "note": "Also available via: brew install bitwarden-cli, or direct download from https://bitwarden.com/download/ (CLI section). The binary is named 'bw' (not bitwarden). Requires a Bitwarden account. Set BW_SESSION after unlock for scripting."
}
