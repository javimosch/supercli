{
  "plugin": "protodep",
  "binary": "protodep",
  "check": "which protodep",
  "install_steps": [
    "go install github.com/stormcat24/protodep@latest",
    "Verify: protodep --version",
    "supercli plugins install ./plugins/protodep --on-conflict replace --json"
  ]
}