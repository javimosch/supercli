{
  "plugin": "umax_pp",
  "binary": "umax_pp",
  "check": "which umax_pp",
  "install_steps": [
    "apt-get install umax_pp 2>/dev/null || which umax_pp",
    "Verify: umax_pp --version",
    "supercli plugins install ./plugins/umax_pp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}