{
  "plugin": "xq",
  "binary": "xq",
  "check": "which xq",
  "install_steps": [
    "brew install xq",
    "Verify: xq --version",
    "supercli plugins install ./plugins/xq --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/sibprogrammer/xq@latest. Extracts and beautifies XML/HTML content using XPath queries. Perfect for parsing structured data and web scraping."
}
