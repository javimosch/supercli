{
  "plugin": "gotests",
  "binary": "gotests",
  "check": "which gotests",
  "install_steps": [
    "brew install gotests",
    "Verify: gotests --version",
    "supercli plugins install ./plugins/gotests --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}