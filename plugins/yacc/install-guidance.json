{
  "plugin": "yacc",
  "binary": "yacc",
  "check": "which yacc",
  "install_steps": [
    "apt-get install yacc 2>/dev/null || which yacc",
    "Verify: yacc --version",
    "supercli plugins install ./plugins/yacc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}