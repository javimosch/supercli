{
  "plugin": "csm",
  "binary": "csm",
  "check": "which csm",
  "install_steps": [
    "pip install csm",
    "Verify: csm --version",
    "supercli plugins install ./plugins/csm --on-conflict replace --json"
  ],
  "note": "Conversational speech model for natural-sounding context-aware speech generation."
}