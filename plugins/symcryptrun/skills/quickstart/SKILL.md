---
name: symcryptrun
description: Use this skill when the user wants to use symcryptrun, cli tool: symcryptrun.
---

# symcryptrun Plugin

CLI tool: symcryptrun.

## Commands
- `symcryptrun <resource> <action>` — Execute symcryptrun commands
- `symcryptrun self version` — Print symcryptrun version
- `symcryptrun _ _` — Passthrough to symcryptrun CLI

## Usage Examples
- "symcryptrun --help"
- "symcryptrun self version"

## Installation
```bash
apt-get install symcryptrun 2>/dev/null || which symcryptrun
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
