{
  "plugin": "barrelsby",
  "binary": "barrelsby",
  "check": "which barrelsby",
  "install_steps": [
    "npm install -g barrelsby",
    "Verify: barrelsby --version",
    "supercli plugins install ./plugins/barrelsby --on-conflict replace --json"
  ]
}