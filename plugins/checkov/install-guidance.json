{
  "plugin": "checkov",
  "binary": "checkov",
  "check": "which checkov",
  "install_steps": [
    "pip install checkov",
    "Verify: checkov --version",
    "supercli plugins install ./plugins/checkov --on-conflict replace --json"
  ]
}
