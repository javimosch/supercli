{
  "name": "dpkg-buildpackage",
  "version": "1.0.0",
  "description": "dpkg-buildpackage \u2014 dpkg-buildpackage \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-buildpackage"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-buildpackage",
    "binary": "dpkg-buildpackage",
    "check": "which dpkg-buildpackage",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-buildpackage",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-buildpackage \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-buildpackage",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}