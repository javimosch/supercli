{
  "plugin": "peep",
  "binary": "peep",
  "check": "which peep",
  "install_steps": [
    "cargo install peep",
    "Verify: peep --help",
    "supercli plugins install ./plugins/peep --on-conflict replace --json"
  ],
  "note": "peep provides a text viewer in a small pane within the terminal"
}
