{
  "plugin": "pepe",
  "binary": "pepe",
  "check": "which pepe",
  "install_steps": [
    "curl --proto '=https' --tlsv1.2 -sSf https://pepe.mhaimdat.com/install.sh | bash",
    "Verify: pepe --version",
    "supercli plugins install ./plugins/pepe --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew: brew tap omarmhaimdat/tap && brew install pepe, or build from source with cargo install --path ."
}
