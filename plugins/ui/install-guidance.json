{
  "plugin": "ui",
  "binary": "ui",
  "check": "which ui",
  "install_steps": [
    "pip install ui",
    "Verify: ui --version",
    "supercli plugins install ./plugins/ui --on-conflict replace --json"
  ],
  "note": "Language: TypeScript"
}