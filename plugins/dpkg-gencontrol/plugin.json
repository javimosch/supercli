{
  "name": "dpkg-gencontrol",
  "version": "1.0.0",
  "description": "dpkg-gencontrol \u2014 dpkg-gencontrol \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-gencontrol"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-gencontrol",
    "binary": "dpkg-gencontrol",
    "check": "which dpkg-gencontrol",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-gencontrol",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-gencontrol \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-gencontrol",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}