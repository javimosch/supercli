{
  "plugin": "uz",
  "binary": "uz",
  "check": "which uz",
  "install_steps": [
    "apt-get install uz 2>/dev/null || which uz",
    "Verify: uz --version",
    "supercli plugins install ./plugins/uz --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}