{
  "plugin": "halt",
  "binary": "halt",
  "check": "which halt",
  "install_steps": [
    "apt-get install halt 2>/dev/null || which halt",
    "Verify: halt --version",
    "supercli plugins install ./plugins/halt --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}