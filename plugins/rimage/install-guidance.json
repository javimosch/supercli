{
  "plugin": "rimage",
  "binary": "rimage",
  "check": "which rimage",
  "install_steps": [
    "cargo install rimage",
    "Verify: rimage --version",
    "supercli plugins install ./plugins/rimage --on-conflict replace --json"
  ],
  "note": "Also available via pre-built binaries from GitHub releases. Supports AVIF, JPEG, PNG, WebP, JPEG XL, QOI, and more formats."
}
