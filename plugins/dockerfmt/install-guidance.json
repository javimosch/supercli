{
  "plugin": "dockerfmt",
  "binary": "dockerfmt",
  "check": "which dockerfmt",
  "install_steps": [
    "go install github.com/reteps/dockerfmt@latest",
    "Verify: dockerfmt version",
    "supercli plugins install ./plugins/dockerfmt --on-conflict replace --json"
  ],
  "note": "Also available as a Docker image: docker run --rm -v $(pwd):/data reteps/dockerfmt Dockerfile. Pre-commit hook configuration is supported. See https://github.com/reteps/dockerfmt for details."
}
