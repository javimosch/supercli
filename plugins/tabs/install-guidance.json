{
  "plugin": "tabs",
  "binary": "tabs",
  "check": "which tabs",
  "install_steps": [
    "apt-get install tabs 2>/dev/null || which tabs",
    "Verify: tabs --version",
    "supercli plugins install ./plugins/tabs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}