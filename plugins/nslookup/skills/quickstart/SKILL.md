---
name: nslookup
description: Use this skill when the user wants to use the nslookup command-line tool.
---

# nslookup Plugin

DNS lookup tool for querying name servers

## Commands
- `nslookup self version` — Print version
- `nslookup _ _` — Passthrough to nslookup

## Installation
```bash
brew install bind
```
