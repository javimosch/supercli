{
  "name": "dhall-freeze",
  "version": "0.1.0",
  "description": "CLI tool: dhall-freeze",
  "source": "https://packages.debian.org/dhall-freeze",
  "checks": [{ "type": "binary", "name": "dhall-freeze" }],
  "install_guidance": {
    "plugin": "dhall-freeze", "binary": "dhall-freeze", "check": "which dhall-freeze",
    "install_steps": ["apt-get install dhall-freeze 2>/dev/null || which dhall-freeze", "Verify: dhall-freeze --version", "supercli plugins install ./plugins/dhall-freeze --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dhall-freeze", "resource": "_", "action": "_",
    "description": "Passthrough to dhall-freeze CLI",
    "adapter": "process", "adapterConfig": { "command": "dhall-freeze", "passthrough": true, "missingDependencyHelp": "Install dhall-freeze: apt-get install dhall-freeze" }, "args": []
  }]
}
