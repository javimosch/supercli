{ "plugin": "dhall-freeze", "binary": "dhall-freeze", "check": "which dhall-freeze", "install_steps": ["apt-get install dhall-freeze 2>/dev/null || which dhall-freeze", "Verify: dhall-freeze --version", "supercli plugins install ./plugins/dhall-freeze --on-conflict replace --json"] }
