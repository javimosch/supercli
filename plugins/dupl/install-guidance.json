{
  "plugin": "dupl",
  "binary": "dupl",
  "check": "which dupl",
  "install_steps": [
    "go install github.com/mibk/dupl@latest",
    "Verify: dupl --version",
    "supercli plugins install ./plugins/dupl --on-conflict replace --json"
  ]
}