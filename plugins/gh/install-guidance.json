{
  "plugin": "gh",
  "binary": "gh",
  "check": "which gh",
  "install_steps": [
    "brew install gh",
    "Verify: gh --version",
    "supercli plugins install ./plugins/gh --on-conflict replace --json"
  ]
}