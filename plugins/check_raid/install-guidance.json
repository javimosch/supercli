{ "plugin": "check_raid", "binary": "check_raid", "check": "which check_raid", "install_steps": ["apt-get install check_raid 2>/dev/null || which check_raid", "Verify: check_raid --version", "supercli plugins install ./plugins/check_raid --on-conflict replace --json"] }
