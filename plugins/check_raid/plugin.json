{
  "name": "check_raid",
  "version": "0.1.0",
  "description": "CLI tool: check_raid",
  "source": "https://packages.debian.org/check_raid",
  "checks": [{ "type": "binary", "name": "check_raid" }],
  "install_guidance": {
    "plugin": "check_raid", "binary": "check_raid", "check": "which check_raid",
    "install_steps": ["apt-get install check_raid 2>/dev/null || which check_raid", "Verify: check_raid --version", "supercli plugins install ./plugins/check_raid --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check_raid", "resource": "_", "action": "_",
    "description": "Passthrough to check_raid CLI",
    "adapter": "process", "adapterConfig": { "command": "check_raid", "passthrough": true, "missingDependencyHelp": "Install check_raid: apt-get install check_raid" }, "args": []
  }]
}
