{
  "plugin": "dasel",
  "binary": "dasel",
  "check": "which dasel",
  "install_steps": [
    "brew install dasel",
    "Verify: dasel --version",
    "supercli plugins install ./plugins/dasel --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/TomWright/dasel/cmd/dasel@latest. Supports JSON, TOML, YAML, XML, INI, HCL, CSV formats. Can convert between formats. Simple selector syntax for querying and updating data."
}
