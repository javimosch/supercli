{
    "plugin": "llm-cli",
    "binary": "llm-cli",
    "check": "which llm-cli",
    "install_steps": ["pip install llm", "which llm-cli", "Verify: llm-cli --version", "supercli plugins install ./plugins/llm_cli --on-conflict replace --json"]
  }
