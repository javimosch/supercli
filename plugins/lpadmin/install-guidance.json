{ "plugin": "lpadmin", "binary": "lpadmin", "check": "which lpadmin", "install_steps": ["apt-get install cups", "Verify: lpadmin --version", "supercli plugins install ./plugins/lpadmin --on-conflict replace --json"] }
