{
  "plugin": "soft-serve",
  "binary": "soft-serve",
  "check": "which soft-serve",
  "install_steps": ["brew install soft-serve", "Verify: soft-serve --version", "supercli plugins install ./plugins/soft-serve --on-conflict replace --json"]
}
