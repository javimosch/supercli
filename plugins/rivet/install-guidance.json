{
  "plugin": "rivet",
  "binary": "rivet",
  "check": "which rivet",
  "install_steps": [
    "pip install rivet",
    "Verify: rivet --version",
    "supercli plugins install ./plugins/rivet --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}