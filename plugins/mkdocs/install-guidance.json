{
  "plugin": "mkdocs",
  "binary": "mkdocs",
  "check": "which mkdocs",
  "install_steps": [
    "pip install mkdocs",
    "Verify: mkdocs --version",
    "supercli plugins install ./plugins/mkdocs --on-conflict replace --json"
  ]
}
