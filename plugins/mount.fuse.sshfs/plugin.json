{
  "name": "mount.fuse.sshfs",
  "version": "0.1.0",
  "description": "CLI tool: mount.fuse.sshfs",
  "source": "https://manpages.debian.org/mount.fuse.sshfs",
  "checks": [
    {
      "type": "binary",
      "name": "mount.fuse.sshfs"
    }
  ],
  "install_guidance": {
    "plugin": "mount.fuse.sshfs",
    "binary": "mount.fuse.sshfs",
    "check": "which mount.fuse.sshfs",
    "install_steps": [
      "apt-get install mount.fuse.sshfs 2>/dev/null || which mount.fuse.sshfs",
      "Verify: mount.fuse.sshfs --version",
      "supercli plugins install ./plugins/mount.fuse.sshfs --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "mount.fuse.sshfs",
      "resource": "self",
      "action": "version",
      "description": "Print mount.fuse.sshfs version",
      "adapter": "process",
      "adapterConfig": {
        "command": "mount.fuse.sshfs",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install mount.fuse.sshfs: apt-get install mount.fuse.sshfs 2>/dev/null || which mount.fuse.sshfs"
      },
      "args": []
    },
    {
      "namespace": "mount.fuse.sshfs",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to mount.fuse.sshfs CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "mount.fuse.sshfs",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install mount.fuse.sshfs: apt-get install mount.fuse.sshfs 2>/dev/null || which mount.fuse.sshfs"
      },
      "args": []
    }
  ]
}