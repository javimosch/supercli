---
name: mount.fuse.sshfs
description: Use this skill when the user wants to use mount.fuse.sshfs, cli tool: mount.fuse.sshfs.
---

# mount.fuse.sshfs Plugin

CLI tool: mount.fuse.sshfs.

## Commands
- `mount.fuse.sshfs <resource> <action>` — Execute mount.fuse.sshfs commands
- `mount.fuse.sshfs self version` — Print mount.fuse.sshfs version
- `mount.fuse.sshfs _ _` — Passthrough to mount.fuse.sshfs CLI

## Usage Examples
- "mount.fuse.sshfs --help"
- "mount.fuse.sshfs self version"

## Installation
```bash
apt-get install mount.fuse.sshfs 2>/dev/null || which mount.fuse.sshfs
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
