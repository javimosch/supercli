{
  "plugin": "mpif90",
  "binary": "mpif90",
  "check": "which mpif90",
  "install_steps": [
    "apt-get install mpif90 2>/dev/null || which mpif90",
    "Verify: mpif90 --version",
    "supercli plugins install ./plugins/mpif90 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}