---
name: ByeByeDPI
description: Use this skill when the user wants to use ByeByeDPI for Приложение локально запускает ByeDPI и перенаправляет весь трафик через него.
---

# ByeByeDPI Plugin

Приложение локально запускает ByeDPI и перенаправляет весь трафик через него

## Commands

### General
- `ByeByeDPI self version` — Print ByeByeDPI version
- `ByeByeDPI _ _` — Passthrough to byebyedpi CLI

## Usage Examples
- "Run ByeByeDPI"
- "Use byebyedpi to Приложение локально запускает ByeDPI и перенаправляет весь т"

## Installation

```bash
pip install byebyedpi
```

## Key Features
- Приложение локально запускает ByeDPI и перенаправляет весь трафик через него
- CLI tool, headless and scriptable
