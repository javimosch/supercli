{
  "name": "defmt",
  "version": "0.1.0",
  "description": "CLI tool: defmt",
  "source": "https://packages.debian.org/defmt",
  "checks": [{ "type": "binary", "name": "defmt" }],
  "install_guidance": {
    "plugin": "defmt", "binary": "defmt", "check": "which defmt",
    "install_steps": ["apt-get install defmt 2>/dev/null || which defmt", "Verify: defmt --version", "supercli plugins install ./plugins/defmt --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "defmt", "resource": "_", "action": "_",
    "description": "Passthrough to defmt CLI",
    "adapter": "process", "adapterConfig": { "command": "defmt", "passthrough": true, "missingDependencyHelp": "Install defmt: apt-get install defmt" }, "args": []
  }]
}
