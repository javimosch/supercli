{ "plugin": "defmt", "binary": "defmt", "check": "which defmt", "install_steps": ["apt-get install defmt 2>/dev/null || which defmt", "Verify: defmt --version", "supercli plugins install ./plugins/defmt --on-conflict replace --json"] }
