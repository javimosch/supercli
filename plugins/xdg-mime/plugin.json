{
  "name": "xdg-mime",
  "version": "0.1.0",
  "description": "CLI tool: xdg-mime",
  "source": "https://manpages.debian.org/xdg-mime",
  "checks": [
    {
      "type": "binary",
      "name": "xdg-mime"
    }
  ],
  "install_guidance": {
    "plugin": "xdg-mime",
    "binary": "xdg-mime",
    "check": "which xdg-mime",
    "install_steps": [
      "apt-get install xdg-mime 2>/dev/null || which xdg-mime",
      "Verify: xdg-mime --version",
      "supercli plugins install ./plugins/xdg-mime --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "xdg-mime",
      "resource": "self",
      "action": "version",
      "description": "Print xdg-mime version",
      "adapter": "process",
      "adapterConfig": {
        "command": "xdg-mime",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install xdg-mime: apt-get install xdg-mime 2>/dev/null || which xdg-mime"
      },
      "args": []
    },
    {
      "namespace": "xdg-mime",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to xdg-mime CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "xdg-mime",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install xdg-mime: apt-get install xdg-mime 2>/dev/null || which xdg-mime"
      },
      "args": []
    }
  ]
}