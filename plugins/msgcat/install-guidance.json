{
  "plugin": "msgcat",
  "binary": "msgcat",
  "check": "which msgcat",
  "install_steps": [
    "apt-get install msgcat 2>/dev/null || which msgcat",
    "Verify: msgcat --version",
    "supercli plugins install ./plugins/msgcat --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}