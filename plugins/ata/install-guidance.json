{ "plugin": "ata", "binary": "ata", "check": "which ata", "install_steps": ["apt-get install ata 2>/dev/null || which ata", "Verify: ata --version", "supercli plugins install ./plugins/ata --on-conflict replace --json"] }
