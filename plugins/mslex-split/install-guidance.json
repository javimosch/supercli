{
  "plugin": "mslex-split",
  "binary": "mslex-split",
  "check": "which mslex-split",
  "install_steps": [
    "apt-get install mslex-split 2>/dev/null || which mslex-split",
    "Verify: mslex-split --version",
    "supercli plugins install ./plugins/mslex-split --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}