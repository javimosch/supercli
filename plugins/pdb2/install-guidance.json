{
  "plugin": "pdb2",
  "binary": "pdb2",
  "check": "which pdb2",
  "install_steps": [
    "apt-get install pdb2 2>/dev/null || which pdb2",
    "Verify: pdb2 --version",
    "supercli plugins install ./plugins/pdb2 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}