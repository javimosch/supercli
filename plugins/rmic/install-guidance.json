{
  "plugin": "rmic",
  "binary": "rmic",
  "check": "which rmic",
  "install_steps": [
    "apt-get install rmic 2>/dev/null || which rmic",
    "Verify: rmic --version",
    "supercli plugins install ./plugins/rmic --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}