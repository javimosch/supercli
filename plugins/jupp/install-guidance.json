{
  "plugin": "jupp",
  "binary": "jupp",
  "check": "which jupp",
  "install_steps": ["brew install jupp", "Verify: jupp --version", "supercli plugins install ./plugins/jupp --on-conflict replace --json"]
}
