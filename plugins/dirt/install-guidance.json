{ "plugin": "dirt", "binary": "dirt", "check": "which dirt", "install_steps": ["apt-get install dirt 2>/dev/null || which dirt", "Verify: dirt --version", "supercli plugins install ./plugins/dirt --on-conflict replace --json"] }
