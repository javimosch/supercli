{
  "plugin": "ubuntu-report",
  "binary": "ubuntu-report",
  "check": "which ubuntu-report",
  "install_steps": [
    "apt-get install ubuntu-report 2>/dev/null || which ubuntu-report",
    "Verify: ubuntu-report --version",
    "supercli plugins install ./plugins/ubuntu-report --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}