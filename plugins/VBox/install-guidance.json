{
  "plugin": "VBox",
  "binary": "VBox",
  "check": "which VBox",
  "install_steps": [
    "apt-get install VBox 2>/dev/null || which VBox",
    "Verify: VBox --version",
    "supercli plugins install ./plugins/VBox --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}