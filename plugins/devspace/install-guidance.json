{
  "plugin": "devspace",
  "binary": "devspace",
  "check": "which devspace",
  "install_steps": [
    "brew install devspace",
    "Verify: devspace --version",
    "supercli plugins install ./plugins/devspace --on-conflict replace --json"
  ],
  "note": "Also available via binary download or npm. Requires kubectl and a Kubernetes cluster. Enables cloud-native development directly inside Kubernetes with hot reloading and debugging."
}
