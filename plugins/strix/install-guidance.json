{
  "plugin": "strix",
  "binary": "strix",
  "check": "which strix",
  "install_steps": [
    "pip install strix",
    "Verify: strix --version",
    "supercli plugins install ./plugins/strix --on-conflict replace --json"
  ],
  "note": "AI-powered security testing framework for finding and fixing app vulnerabilities."
}