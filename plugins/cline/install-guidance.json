{
  "plugin": "cline",
  "binary": "cline",
  "check": "which cline",
  "install_steps": [
    "brew install cline",
    "Verify: cline --version",
    "supercli plugins install ./plugins/cline --on-conflict replace --json"
  ]
}