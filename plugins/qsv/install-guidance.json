{
  "plugin": "qsv",
  "binary": "qsv",
  "check": "which qsv",
  "install_steps": [
    "cargo install qsv",
    "Verify: qsv --version",
    "supercli plugins install ./plugins/qsv --on-conflict replace --json"
  ],
  "note": "Also available via brew: brew install qsv. Pre-built binaries available on GitHub releases for Linux, macOS, and Windows."
}
