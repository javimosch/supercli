{
  "plugin": "mpc",
  "binary": "mpc",
  "check": "which mpc",
  "install_steps": [
    "brew install mpc",
    "Verify: mpc --version",
    "supercli plugins install ./plugins/mpc --on-conflict replace --json"
  ]
}
