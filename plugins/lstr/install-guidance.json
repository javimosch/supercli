{
  "plugin": "lstr",
  "binary": "lstr",
  "check": "which lstr",
  "install_steps": [
    "brew install lstr",
    "Verify: lstr --version",
    "supercli plugins install ./plugins/lstr --on-conflict replace --json"
  ],
  "note": "Also available via NetBSD pkgin (pkgin install lstr) or build from source: git clone https://github.com/bgreenwell/lstr.git && cd lstr && cargo install --path ."
}
