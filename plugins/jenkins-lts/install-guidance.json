{
  "plugin": "jenkins-lts",
  "binary": "jenkins-lts",
  "check": "which jenkins-lts",
  "install_steps": [
    "brew install jenkins-lts",
    "Verify: jenkins-lts --version",
    "supercli plugins install ./plugins/jenkins-lts --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}