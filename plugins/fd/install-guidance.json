{
  "plugin": "fd",
  "binary": "fd",
  "check": "which fd",
  "install_steps": [
    "cargo install fd-find",
    "Verify: fd --version",
    "supercli plugins install ./plugins/fd --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, pacman, scoop, winget, nix. ~13x faster than find."
}