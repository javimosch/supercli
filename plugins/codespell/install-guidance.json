{
  "plugin": "codespell",
  "binary": "codespell",
  "check": "which codespell",
  "install_steps": [
    "pip install codespell",
    "Verify: codespell --version",
    "supercli plugins install ./plugins/codespell --on-conflict replace --json"
  ],
  "note": "Requires Python. Also available via: brew, apt."
}
