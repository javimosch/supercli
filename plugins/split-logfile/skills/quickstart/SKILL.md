---
name: split-logfile
description: Use this skill when the user wants to use split-logfile, cli tool: split-logfile.
---

# split-logfile Plugin

CLI tool: split-logfile.

## Commands
- `split-logfile <resource> <action>` — Execute split-logfile commands
- `split-logfile self version` — Print split-logfile version
- `split-logfile _ _` — Passthrough to split-logfile CLI

## Usage Examples
- "split-logfile --help"
- "split-logfile self version"

## Installation
```bash
apt-get install split-logfile 2>/dev/null || which split-logfile
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
