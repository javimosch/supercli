{
  "plugin": "pdd",
  "binary": "pdd",
  "check": "which pdd",
  "install_steps": [
    "pip install pdd",
    "Verify: pdd --version",
    "supercli plugins install ./plugins/pdd --on-conflict replace --json"
  ]
}
