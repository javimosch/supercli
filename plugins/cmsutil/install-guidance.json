{ "plugin": "cmsutil", "binary": "cmsutil", "check": "which cmsutil", "install_steps": ["apt-get install cmsutil 2>/dev/null || which cmsutil", "Verify: cmsutil --version", "supercli plugins install ./plugins/cmsutil --on-conflict replace --json"] }
