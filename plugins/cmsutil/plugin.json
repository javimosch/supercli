{
  "name": "cmsutil",
  "version": "0.1.0",
  "description": "CLI tool: cmsutil",
  "source": "https://packages.debian.org/cmsutil",
  "checks": [{ "type": "binary", "name": "cmsutil" }],
  "install_guidance": {
    "plugin": "cmsutil", "binary": "cmsutil", "check": "which cmsutil",
    "install_steps": ["apt-get install cmsutil 2>/dev/null || which cmsutil", "Verify: cmsutil --version", "supercli plugins install ./plugins/cmsutil --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cmsutil", "resource": "_", "action": "_",
    "description": "Passthrough to cmsutil CLI",
    "adapter": "process", "adapterConfig": { "command": "cmsutil", "passthrough": true, "missingDependencyHelp": "Install cmsutil: apt-get install cmsutil" }, "args": []
  }]
}
