{
  "plugin": "zev",
  "binary": "zev",
  "check": "which zev",
  "install_steps": [
    "pipx install zev",
    "Verify: zev --version",
    "supercli plugins install ./plugins/zev --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
