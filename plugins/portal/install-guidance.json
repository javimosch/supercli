{
  "plugin": "portal",
  "binary": "portal",
  "check": "which portal",
  "install_steps": [
    "brew install portal",
    "Verify: portal --version",
    "supercli plugins install ./plugins/portal --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}