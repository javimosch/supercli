{
  "plugin": "postlock",
  "binary": "postlock",
  "check": "which postlock",
  "install_steps": [
    "# Install postlock",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postlock-linux-amd64 -o /usr/local/bin/postlock",
    "chmod +x /usr/local/bin/postlock",
    "# Or via package manager, see /usr/sbin/postlock"
  ]
}