{
  "plugin": "zig-book",
  "binary": "zig-book",
  "check": "which zig-book",
  "install_steps": [
    "pip install zig-book",
    "Verify: zig-book --version",
    "supercli plugins install ./plugins/zig-book --on-conflict replace --json"
  ],
  "note": "Language: Zig. Stars: 2590"
}