{
  "plugin": "lorder",
  "binary": "lorder",
  "check": "which lorder",
  "install_steps": [
    "apt-get install lorder 2>/dev/null || which lorder",
    "Verify: lorder --version",
    "supercli plugins install ./plugins/lorder --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}