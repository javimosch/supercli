{
  "plugin": "json_pp",
  "binary": "json_pp",
  "check": "which json_pp",
  "install_steps": [
    "apt install json-pp (Debian/Ubuntu)",
    "brew install json-pp (macOS)",
    "cpan JSON::PP"
  ],
  "note": "Part of the Perl JSON::PP module. Usually pre-installed on most systems."
}
