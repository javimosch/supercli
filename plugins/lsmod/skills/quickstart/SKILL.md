---
name: lsmod
description: Use this skill when the user wants to use lsmod, cli tool: lsmod.
---

# lsmod Plugin

CLI tool: lsmod.

## Commands
- `lsmod <resource> <action>` — Execute lsmod commands
- `lsmod self version` — Print lsmod version
- `lsmod _ _` — Passthrough to lsmod CLI

## Usage Examples
- "lsmod --help"
- "lsmod self version"

## Installation
```bash
apt-get install lsmod 2>/dev/null || which lsmod
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
