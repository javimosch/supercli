{
  "plugin": "lsmod",
  "binary": "lsmod",
  "check": "which lsmod",
  "install_steps": [
    "apt-get install lsmod 2>/dev/null || which lsmod",
    "Verify: lsmod --version",
    "supercli plugins install ./plugins/lsmod --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}