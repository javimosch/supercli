{
  "plugin": "sub2api",
  "binary": "sub2api",
  "check": "which sub2api",
  "install_steps": [
    "pip install sub2api",
    "Verify: sub2api --version",
    "supercli plugins install ./plugins/sub2api --on-conflict replace --json"
  ],
  "note": "Language: Go"
}