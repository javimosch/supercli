{
  "plugin": "vhs",
  "binary": "vhs",
  "check": "which vhs",
  "install_steps": [
    "brew install vhs",
    "Verify: vhs --version",
    "supercli plugins install ./plugins/vhs --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/charmbracelet/vhs@latest. Requires ffmpeg for video output. Tape files use simple declarative syntax. Can record to GIF, MP4, WebM formats."
}
