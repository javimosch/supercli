{
  "plugin": "vhs",
  "binary": "vhs",
  "check": "which vhs",
  "install_steps": [
    "brew install vhs",
    "Verify: vhs --version",
    "supercli plugins install ./plugins/vhs --on-conflict replace --json"
  ]
}
