{
  "plugin": "probe",
  "binary": "probe",
  "check": "which probe",
  "install_steps": [
    "npm install -g @probelabs/probe",
    "Verify: probe --version",
    "supercli plugins install ./plugins/probe --on-conflict replace --json"
  ],
  "note": "Also available via: curl -fsSL https://raw.githubusercontent.com/probelabs/probe/main/install.sh | bash, or cargo install from source. Supports macOS, Linux, Windows."
}