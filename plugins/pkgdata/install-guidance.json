{
  "plugin": "pkgdata",
  "binary": "pkgdata",
  "check": "which pkgdata",
  "install_steps": [
    "apt-get install pkgdata 2>/dev/null || which pkgdata",
    "Verify: pkgdata --version",
    "supercli plugins install ./plugins/pkgdata --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}