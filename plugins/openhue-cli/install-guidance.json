{
  "plugin": "openhue-cli",
  "binary": "openhue",
  "check": "which openhue",
  "install_steps": [
    "brew install openhue-cli",
    "Verify: openhue --version",
    "supercli plugins install ./plugins/openhue-cli --on-conflict replace --json"
  ]
}