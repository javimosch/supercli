{ "plugin": "apt-cdrom", "binary": "apt-cdrom", "check": "which apt-cdrom", "install_steps": ["apt-get install apt-cdrom 2>/dev/null || which apt-cdrom", "Verify: apt-cdrom --version", "supercli plugins install ./plugins/apt-cdrom --on-conflict replace --json"] }
