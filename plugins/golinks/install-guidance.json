{
  "plugin": "golinks",
  "binary": "golinks",
  "check": "which golinks",
  "install_steps": ["npm install -g golinks", "Verify: golinks --version", "supercli plugins install ./plugins/golinks --on-conflict replace --json"]
}
