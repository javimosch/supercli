{
  "plugin": "echo-rust",
  "binary": "echo-rust",
  "check": "which echo-rust",
  "install_steps": ["cargo install echo-rust", "Verify: echo-rust --version", "supercli plugins install ./plugins/echo-rust --on-conflict replace --json"]
}
