{
  "name": "aplay",
  "version": "0.1.0",
  "description": "CLI tool: aplay",
  "source": "https://packages.debian.org/aplay",
  "checks": [{ "type": "binary", "name": "aplay" }],
  "install_guidance": {
    "plugin": "aplay", "binary": "aplay", "check": "which aplay",
    "install_steps": ["apt-get install aplay 2>/dev/null || which aplay", "Verify: aplay --version", "supercli plugins install ./plugins/aplay --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "aplay", "resource": "_", "action": "_",
    "description": "Passthrough to aplay CLI",
    "adapter": "process", "adapterConfig": { "command": "aplay", "passthrough": true, "missingDependencyHelp": "Install aplay: apt-get install aplay" }, "args": []
  }]
}
