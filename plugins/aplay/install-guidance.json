{ "plugin": "aplay", "binary": "aplay", "check": "which aplay", "install_steps": ["apt-get install aplay 2>/dev/null || which aplay", "Verify: aplay --version", "supercli plugins install ./plugins/aplay --on-conflict replace --json"] }
