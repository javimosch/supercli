{ "plugin": "at-user", "binary": "at-user", "check": "which at-user", "install_steps": ["apt-get install at-user 2>/dev/null || which at-user", "Verify: at-user --version", "supercli plugins install ./plugins/at-user --on-conflict replace --json"] }
