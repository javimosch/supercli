{
  "plugin": "update-language",
  "binary": "update-language",
  "check": "which update-language",
  "install_steps": [
    "apt-get install update-language 2>/dev/null || which update-language",
    "Verify: update-language --version",
    "supercli plugins install ./plugins/update-language --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}