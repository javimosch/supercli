{
  "plugin": "libccd",
  "binary": "libccd",
  "check": "which libccd",
  "install_steps": [
    "brew install libccd",
    "Verify: libccd --version",
    "supercli plugins install ./plugins/libccd --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}