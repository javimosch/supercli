{
  "name": "dm-crypt-list",
  "version": "0.1.0",
  "description": "CLI tool: dm-crypt-list",
  "source": "https://packages.debian.org/dm-crypt-list",
  "checks": [{ "type": "binary", "name": "dm-crypt-list" }],
  "install_guidance": {
    "plugin": "dm-crypt-list", "binary": "dm-crypt-list", "check": "which dm-crypt-list",
    "install_steps": ["apt-get install dm-crypt-list 2>/dev/null || which dm-crypt-list", "Verify: dm-crypt-list --version", "supercli plugins install ./plugins/dm-crypt-list --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-crypt-list", "resource": "_", "action": "_",
    "description": "Passthrough to dm-crypt-list CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-crypt-list", "passthrough": true, "missingDependencyHelp": "Install dm-crypt-list: apt-get install dm-crypt-list" }, "args": []
  }]
}
