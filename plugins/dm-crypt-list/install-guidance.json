{ "plugin": "dm-crypt-list", "binary": "dm-crypt-list", "check": "which dm-crypt-list", "install_steps": ["apt-get install dm-crypt-list 2>/dev/null || which dm-crypt-list", "Verify: dm-crypt-list --version", "supercli plugins install ./plugins/dm-crypt-list --on-conflict replace --json"] }
