{
  "plugin": "lego",
  "binary": "lego",
  "check": "which lego",
  "install_steps": [
    "brew install lego",
    "curl -sSfL https://github.com/go-acme/lego/releases/latest/download/lego_linux_amd64.tar.gz | tar xz -C /usr/local/bin lego",
    "go install github.com/go-acme/lego/v4/cmd/lego@latest",
    "Verify: lego --version",
    "supercli plugins install ./plugins/lego --on-conflict replace --json"
  ],
  "note": "lego supports 200+ DNS providers via environment variables for DNS-01 challenges. Set LEGO_EMAIL and LEGO_PATH for common defaults. Official docs: https://go-acme.github.io/lego/"
}
