{
  "plugin": "rip",
  "binary": "rip",
  "check": "which rip",
  "install_steps": [
    "# Install rip",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/rip-linux-amd64 -o /usr/local/bin/rip",
    "chmod +x /usr/local/bin/rip",
    "# Or via package manager, see crates.io/crates/rip"
  ]
}