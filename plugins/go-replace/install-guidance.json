{
  "plugin": "go-replace",
  "binary": "go-replace",
  "check": "which go-replace",
  "install_steps": [
    "Download from https://github.com/webdevops/go-replace/releases",
    "Add to PATH",
    "Verify: go-replace --help",
    "supercli plugins install ./plugins/go-replace --on-conflict replace --json"
  ],
  "note": "Supports multiple replacement modes: replace (text replacement), line (replace entire lines), lineinfile (add line if not exists), and template (Go template mode). Can process multiple files concurrently."
}
