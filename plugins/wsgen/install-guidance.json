{
  "plugin": "wsgen",
  "binary": "wsgen",
  "check": "which wsgen",
  "install_steps": [
    "apt-get install wsgen 2>/dev/null || which wsgen",
    "Verify: wsgen --version",
    "supercli plugins install ./plugins/wsgen --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}