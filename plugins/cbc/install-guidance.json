{
  "plugin": "cbc",
  "binary": "cbc",
  "check": "which cbc",
  "install_steps": [
    "brew install cbc",
    "Verify: cbc --version",
    "supercli plugins install ./plugins/cbc --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}