{
  "plugin": "cf-tool",
  "binary": "cf",
  "check": "which cf",
  "install_steps": [
    "brew install cf-tool",
    "Verify: cf --version",
    "supercli plugins install ./plugins/cf-tool --on-conflict replace --json"
  ]
}