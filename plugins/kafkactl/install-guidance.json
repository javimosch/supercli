{
  "plugin": "kafkactl",
  "binary": "kafkactl",
  "check": "which kafkactl",
  "install_steps": [
    "brew install kafkactl",
    "Verify: kafkactl version",
    "supercli plugins install ./plugins/kafkactl --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/deviceinsight/kafkactl/cmd/kafkactl@latest. A command-line interface for Apache Kafka. Manage Kafka clusters, topics, consumers, and produce/consume messages from the CLI."
}
