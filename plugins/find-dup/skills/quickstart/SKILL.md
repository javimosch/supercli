---
name: find-dup
description: find-dup — find duplicate lines in text. Reads stdin, outputs JSON with duplicate counts and line numbers, sorted by frequency.
---
# find-dup
find-dup — find duplicate lines in text. Reads stdin, outputs JSON with duplicate counts and line numbers, sorted by frequency.
