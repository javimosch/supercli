{
  "plugin": "osmium",
  "binary": "osmium",
  "check": "which osmium",
  "install_steps": [
    "brew install osmium",
    "Verify: osmium --version",
    "supercli plugins install ./plugins/osmium --on-conflict replace --json"
  ]
}
