{
  "plugin": "gdk-pixbuf",
  "binary": "gdk-pixbuf",
  "check": "which gdk-pixbuf",
  "install_steps": [
    "brew install gdk-pixbuf",
    "Verify: gdk-pixbuf --version",
    "supercli plugins install ./plugins/gdk-pixbuf --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}