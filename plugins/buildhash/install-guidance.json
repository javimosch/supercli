{ "plugin": "buildhash", "binary": "buildhash", "check": "which buildhash", "install_steps": ["apt-get install buildhash 2>/dev/null || which buildhash", "Verify: buildhash --version", "supercli plugins install ./plugins/buildhash --on-conflict replace --json"] }
