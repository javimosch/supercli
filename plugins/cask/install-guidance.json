{
  "plugin": "cask",
  "binary": "cask",
  "check": "which cask",
  "install_steps": [
    "brew install cask",
    "Verify: cask --version",
    "supercli plugins install ./plugins/cask --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}