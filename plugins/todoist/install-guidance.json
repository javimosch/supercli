{
  "plugin": "todoist",
  "binary": "todoist",
  "check": "which todoist",
  "install_steps": [
    "brew install todoist",
    "Verify: todoist --version",
    "supercli plugins install ./plugins/todoist --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/sachaos/todoist@latest. Requires Todoist API token. Manage tasks, projects, and productivity from the command line. Great for CLI-based task management workflows."
}
