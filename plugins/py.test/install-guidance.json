{
  "plugin": "py.test",
  "binary": "py.test",
  "check": "which py.test",
  "install_steps": [
    "apt-get install py.test 2>/dev/null || which py.test",
    "Verify: py.test --version",
    "supercli plugins install ./plugins/py.test --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}