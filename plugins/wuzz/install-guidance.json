{
  "plugin": "wuzz",
  "binary": "wuzz",
  "check": "which wuzz",
  "install_steps": [
    "brew install wuzz",
    "Verify: wuzz --version",
    "supercli plugins install ./plugins/wuzz --on-conflict replace --json"
  ]
}