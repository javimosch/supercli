{
  "plugin": "slowql",
  "binary": "slowql",
  "check": "which slowql",
  "install_steps": [
    "pipx install slowql",
    "Verify: slowql --version",
    "supercli plugins install ./plugins/slowql --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
