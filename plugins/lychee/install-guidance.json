{
  "plugin": "lychee",
  "binary": "lychee",
  "check": "which lychee",
  "install_steps": [
    "cargo install lychee",
    "Verify: lychee --version",
    "supercli plugins install ./plugins/lychee --on-conflict replace --json"
  ]
}
