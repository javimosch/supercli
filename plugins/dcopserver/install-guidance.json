{ "plugin": "dcopserver", "binary": "dcopserver", "check": "which dcopserver", "install_steps": ["apt-get install dcopserver 2>/dev/null || which dcopserver", "Verify: dcopserver --version", "supercli plugins install ./plugins/dcopserver --on-conflict replace --json"] }
