---
name: mount.fuse3
description: Use this skill when the user wants to use mount.fuse3, cli tool: mount.fuse3.
---

# mount.fuse3 Plugin

CLI tool: mount.fuse3.

## Commands
- `mount.fuse3 <resource> <action>` — Execute mount.fuse3 commands
- `mount.fuse3 self version` — Print mount.fuse3 version
- `mount.fuse3 _ _` — Passthrough to mount.fuse3 CLI

## Usage Examples
- "mount.fuse3 --help"
- "mount.fuse3 self version"

## Installation
```bash
apt-get install mount.fuse3 2>/dev/null || which mount.fuse3
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
