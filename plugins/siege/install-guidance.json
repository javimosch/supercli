{
  "plugin": "siege",
  "binary": "siege",
  "check": "which siege",
  "install_steps": ["brew install siege", "Verify: siege --version", "supercli plugins install ./plugins/siege --on-conflict replace --json"]
}
