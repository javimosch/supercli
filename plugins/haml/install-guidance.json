{
  "plugin": "haml",
  "binary": "haml",
  "check": "which haml",
  "install_steps": [
    "gem install haml",
    "Verify: haml --version",
    "supercli plugins install ./plugins/haml --on-conflict replace --json"
  ]
}
