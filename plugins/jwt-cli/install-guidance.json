{
  "plugin": "jwt-cli",
  "binary": "jwt",
  "check": "which jwt",
  "install_steps": [
    "brew install mike-engel/tap/jwt-cli",
    "Verify: jwt --version",
    "supercli plugins install ./plugins/jwt-cli --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install jwt-cli. Super fast CLI tool to decode and encode JWTs. Supports multiple algorithms, flexible output formats, and pipe-friendly workflows. Great for debugging and generating JWT tokens."
}
