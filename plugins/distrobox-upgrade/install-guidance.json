{ "plugin": "distrobox-upgrade", "binary": "distrobox-upgrade", "check": "which distrobox-upgrade", "install_steps": ["apt-get install distrobox-upgrade 2>/dev/null || which distrobox-upgrade", "Verify: distrobox-upgrade --version", "supercli plugins install ./plugins/distrobox-upgrade --on-conflict replace --json"] }
