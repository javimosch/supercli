{
  "plugin": "csvtojson",
  "binary": "csvtojson",
  "check": "which csvtojson",
  "install_steps": [
    "npm install -g csvtojson",
    "Verify: csvtojson",
    "supercli plugins install ./plugins/csvtojson --on-conflict replace --json"
  ],
  "note": "Csvtojson converts CSV files to JSON. Supports custom delimiters, header options, and can process data from stdin/stdout for pipeline operations."
}
