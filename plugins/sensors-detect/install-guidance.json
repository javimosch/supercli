{
  "plugin": "sensors-detect",
  "binary": "sensors-detect",
  "check": "which sensors-detect",
  "install_steps": [
    "apt-get install sensors-detect 2>/dev/null || which sensors-detect",
    "Verify: sensors-detect --version",
    "supercli plugins install ./plugins/sensors-detect --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}