{ "plugin": "composite", "binary": "composite", "check": "which composite", "install_steps": ["apt-get install imagemagick", "Verify: composite --version", "supercli plugins install ./plugins/composite --on-conflict replace --json"] }
