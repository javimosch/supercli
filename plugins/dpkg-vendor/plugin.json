{
  "name": "dpkg-vendor",
  "version": "1.0.0",
  "description": "dpkg-vendor \u2014 dpkg-vendor \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-vendor"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-vendor",
    "binary": "dpkg-vendor",
    "check": "which dpkg-vendor",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-vendor",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-vendor \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-vendor",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}