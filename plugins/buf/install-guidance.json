{
  "plugin": "buf",
  "binary": "buf",
  "check": "which buf",
  "install_steps": ["brew install bufbuild/buf/buf", "Verify: buf --version", "supercli plugins install ./plugins/buf --on-conflict replace --json"]
}
