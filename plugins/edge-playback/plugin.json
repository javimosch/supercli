{
  "name": "edge-playback",
  "version": "1.0.0",
  "description": "edge-playback \u2014 CLI edge-playback \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=edge-playback+cli",
  "checks": [
    {
      "type": "binary",
      "name": "edge-playback"
    }
  ],
  "install_guidance": {
    "plugin": "edge-playback",
    "binary": "edge-playback",
    "check": "which edge-playback",
    "install_steps": [
      "Install via package manager: apt install edge-playback or brew install edge-playback"
    ]
  },
  "commands": [
    {
      "namespace": "edgeplayback",
      "resource": "run",
      "action": "exec",
      "description": "edge-playback \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "edge-playback",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install edge-playback via package manager"
      },
      "args": []
    }
  ]
}