{
  "plugin": "mixctl",
  "binary": "mixctl",
  "check": "which mixctl",
  "install_steps": ["git clone and install", "Verify: mixctl --version", "supercli plugins install ./plugins/mixctl --on-conflict replace --json"]
}
