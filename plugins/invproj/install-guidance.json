{
  "plugin": "invproj",
  "binary": "invproj",
  "check": "which invproj",
  "install_steps": [
    "apt-get install invproj 2>/dev/null || which invproj",
    "Verify: invproj --version",
    "supercli plugins install ./plugins/invproj --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}