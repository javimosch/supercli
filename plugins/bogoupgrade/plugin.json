{
  "name": "bogoupgrade",
  "version": "0.1.0",
  "description": "CLI tool: bogoupgrade",
  "source": "https://packages.debian.org/bogoupgrade",
  "checks": [{ "type": "binary", "name": "bogoupgrade" }],
  "install_guidance": {
    "plugin": "bogoupgrade", "binary": "bogoupgrade", "check": "which bogoupgrade",
    "install_steps": ["apt-get install bogoupgrade 2>/dev/null || which bogoupgrade", "Verify: bogoupgrade --version", "supercli plugins install ./plugins/bogoupgrade --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "bogoupgrade", "resource": "_", "action": "_",
    "description": "Passthrough to bogoupgrade CLI",
    "adapter": "process", "adapterConfig": { "command": "bogoupgrade", "passthrough": true, "missingDependencyHelp": "Install bogoupgrade: apt-get install bogoupgrade" }, "args": []
  }]
}
