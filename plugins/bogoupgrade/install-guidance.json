{ "plugin": "bogoupgrade", "binary": "bogoupgrade", "check": "which bogoupgrade", "install_steps": ["apt-get install bogoupgrade 2>/dev/null || which bogoupgrade", "Verify: bogoupgrade --version", "supercli plugins install ./plugins/bogoupgrade --on-conflict replace --json"] }
