{
  "plugin": "totem",
  "binary": "totem",
  "check": "which totem",
  "install_steps": [
    "apt-get install totem 2>/dev/null || which totem",
    "Verify: totem --version",
    "supercli plugins install ./plugins/totem --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}