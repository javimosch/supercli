{
  "plugin": "systemd-cgtop",
  "binary": "systemd-cgtop",
  "check": "which systemd-cgtop",
  "install_steps": [
    "apt-get install systemd-cgtop 2>/dev/null || which systemd-cgtop",
    "Verify: systemd-cgtop --version",
    "supercli plugins install ./plugins/systemd-cgtop --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}