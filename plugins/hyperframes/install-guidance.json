{
  "plugin": "hyperframes",
  "binary": "npx",
  "check": "npx --yes hyperframes --version 2>&1 | head -3",
  "install_steps": [
    "Skills auto-indexed via remote_repo. Search: sc skills search <query> --provider hyperframes",
    "CLI: sc hyperframes project init <name>  — scaffold a video project",
    "MCP: sc hyperframes self mcp  — register MCP server"
  ],
  "note": "Apache 2.0 license. Requires Node.js 22+."
}
