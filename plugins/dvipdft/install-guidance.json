{
  "plugin": "dvipdft",
  "binary": "dvipdft",
  "check": "which dvipdft",
  "install_steps": [
    "apt-get install dvipdft 2>/dev/null || which dvipdft",
    "Verify: dvipdft --version",
    "supercli plugins install ./plugins/dvipdft --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}