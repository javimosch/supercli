{ "plugin": "devstat", "binary": "devstat", "check": "which devstat", "install_steps": ["apt-get install devstat 2>/dev/null || which devstat", "Verify: devstat --version", "supercli plugins install ./plugins/devstat --on-conflict replace --json"] }
