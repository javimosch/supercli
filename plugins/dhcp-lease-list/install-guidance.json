{ "plugin": "dhcp-lease-list", "binary": "dhcp-lease-list", "check": "which dhcp-lease-list", "install_steps": ["apt-get install dhcp-lease-list 2>/dev/null || which dhcp-lease-list", "Verify: dhcp-lease-list --version", "supercli plugins install ./plugins/dhcp-lease-list --on-conflict replace --json"] }
