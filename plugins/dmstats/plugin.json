{
  "name": "dmstats",
  "version": "1.0.0",
  "description": "dmstats \u2014 CLI dmstats \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=dmstats+cli",
  "checks": [
    {
      "type": "binary",
      "name": "dmstats"
    }
  ],
  "install_guidance": {
    "plugin": "dmstats",
    "binary": "dmstats",
    "check": "which dmstats",
    "install_steps": [
      "Install via package manager: apt install dmstats or brew install dmstats"
    ]
  },
  "commands": [
    {
      "namespace": "dmstats",
      "resource": "run",
      "action": "exec",
      "description": "dmstats \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "dmstats",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install dmstats via package manager"
      },
      "args": []
    }
  ]
}