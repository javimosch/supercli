{
  "plugin": "m4",
  "binary": "m4",
  "check": "which m4",
  "install_steps": [
    "apt-get install m4 2>/dev/null || which m4",
    "Verify: m4 --version",
    "supercli plugins install ./plugins/m4 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}