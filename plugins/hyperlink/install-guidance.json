{
  "plugin": "hyperlink",
  "binary": "lychee",
  "check": "which lychee",
  "install_steps": ["cargo install lychee", "Verify: lychee --version", "supercli plugins install ./plugins/hyperlink --on-conflict replace --json"]
}
