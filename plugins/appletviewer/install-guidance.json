{ "plugin": "appletviewer", "binary": "appletviewer", "check": "which appletviewer", "install_steps": ["apt-get install appletviewer 2>/dev/null || which appletviewer", "Verify: appletviewer --version", "supercli plugins install ./plugins/appletviewer --on-conflict replace --json"] }
