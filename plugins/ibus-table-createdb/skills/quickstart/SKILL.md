---
name: ibus-table-createdb
description: Use this skill when the user wants to use ibus-table-createdb, cli tool: ibus-table-createdb.
---

# ibus-table-createdb Plugin

CLI tool: ibus-table-createdb.

## Commands
- `ibus-table-createdb <resource> <action>` — Execute ibus-table-createdb commands
- `ibus-table-createdb self version` — Print ibus-table-createdb version
- `ibus-table-createdb _ _` — Passthrough to ibus-table-createdb CLI

## Usage Examples
- "ibus-table-createdb --help"
- "ibus-table-createdb self version"

## Installation
```bash
apt-get install ibus-table-createdb 2>/dev/null || which ibus-table-createdb
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
