{
  "plugin": "gnomon",
  "binary": "gnomon",
  "check": "which gnomon",
  "install_steps": ["npm install -g gnomon", "Verify: gnomon --version", "supercli plugins install ./plugins/gnomon --on-conflict replace --json"]
}
