{
  "plugin": "update-smart-drivedb",
  "binary": "update-smart-drivedb",
  "check": "which update-smart-drivedb",
  "install_steps": [
    "apt-get install update-smart-drivedb 2>/dev/null || which update-smart-drivedb",
    "Verify: update-smart-drivedb --version",
    "supercli plugins install ./plugins/update-smart-drivedb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}