{
  "name": "corelist",
  "version": "1.0.0",
  "description": "corelist \u2014 corelist \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "corelist"
    }
  ],
  "install_guidance": {
    "plugin": "corelist",
    "binary": "corelist",
    "check": "which corelist",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "corelist",
      "resource": "run",
      "action": "exec",
      "description": "corelist \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "corelist",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}