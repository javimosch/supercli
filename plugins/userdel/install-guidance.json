{
  "plugin": "userdel",
  "binary": "userdel",
  "check": "which userdel",
  "install_steps": [
    "apt-get install userdel 2>/dev/null || which userdel",
    "Verify: userdel --version",
    "supercli plugins install ./plugins/userdel --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}