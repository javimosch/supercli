{
  "plugin": "minio",
  "binary": "mc",
  "check": "which mc",
  "install_steps": [
    "brew install minio/stable/mc",
    "Verify: mc --version",
    "supercli plugins install ./plugins/minio --on-conflict replace --json"
  ]
}