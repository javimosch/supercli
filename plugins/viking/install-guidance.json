{
  "plugin": "viking",
  "binary": "viking",
  "check": "which viking",
  "install_steps": [
    "go install github.com/d3witt/viking@latest",
    "Verify: viking config",
    "supercli plugins install ./plugins/viking --on-conflict replace --json"
  ],
  "note": "Pre-built binaries available at https://github.com/d3witt/viking/releases. Set VIKING_CONFIG_DIR environment variable for custom config directory. Uses SSH Agent if key is not specified. Supports parallel execution across multiple machines."
}
