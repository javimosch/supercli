{
  "plugin": "ggc",
  "binary": "ggc",
  "check": "which ggc",
  "install_steps": [
    "curl -sSL https://raw.githubusercontent.com/bmf-san/ggc/main/install.sh | bash",
    "Verify: ggc --version",
    "supercli plugins install ./plugins/ggc --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (brew install ggc) or Go install (go install github.com/bmf-san/ggc/v8@latest). Running ggc without arguments opens an interactive fuzzy picker TUI."
}
