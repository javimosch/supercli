{
  "name": "dbus-daemon",
  "version": "0.1.0",
  "description": "CLI tool: dbus-daemon",
  "source": "https://packages.debian.org/dbus-daemon",
  "checks": [{ "type": "binary", "name": "dbus-daemon" }],
  "install_guidance": {
    "plugin": "dbus-daemon", "binary": "dbus-daemon", "check": "which dbus-daemon",
    "install_steps": ["apt-get install dbus-daemon 2>/dev/null || which dbus-daemon", "Verify: dbus-daemon --version", "supercli plugins install ./plugins/dbus-daemon --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbus-daemon", "resource": "_", "action": "_",
    "description": "Passthrough to dbus-daemon CLI",
    "adapter": "process", "adapterConfig": { "command": "dbus-daemon", "passthrough": true, "missingDependencyHelp": "Install dbus-daemon: apt-get install dbus-daemon" }, "args": []
  }]
}
