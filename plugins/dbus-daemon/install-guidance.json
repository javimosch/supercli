{ "plugin": "dbus-daemon", "binary": "dbus-daemon", "check": "which dbus-daemon", "install_steps": ["apt-get install dbus-daemon 2>/dev/null || which dbus-daemon", "Verify: dbus-daemon --version", "supercli plugins install ./plugins/dbus-daemon --on-conflict replace --json"] }
