{
  "plugin": "mbw",
  "binary": "mbw",
  "check": "which mbw",
  "install_steps": [
    "brew install mbw",
    "Verify: mbw --version",
    "supercli plugins install ./plugins/mbw --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}