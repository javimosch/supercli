{
  "plugin": "agnix",
  "binary": "agnix",
  "check": "which agnix",
  "install_steps": [
    "npm install -g agnix",
    "Verify: agnix --version",
    "supercli plugins install ./plugins/agnix --on-conflict replace --json"
  ],
  "note": "Also available via: brew tap agent-sh/agnix && brew install agnix, cargo install agnix-cli, or download from GitHub releases."
}