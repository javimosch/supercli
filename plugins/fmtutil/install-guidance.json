{
  "plugin": "fmtutil",
  "binary": "fmtutil",
  "check": "which fmtutil",
  "install_steps": [
    "apt-get install fmtutil 2>/dev/null || which fmtutil",
    "Verify: fmtutil --version",
    "supercli plugins install ./plugins/fmtutil --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}