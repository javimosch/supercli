{ "plugin": "check-usb", "binary": "check-usb", "check": "which check-usb", "install_steps": ["apt-get install check-usb 2>/dev/null || which check-usb", "Verify: check-usb --version", "supercli plugins install ./plugins/check-usb --on-conflict replace --json"] }
