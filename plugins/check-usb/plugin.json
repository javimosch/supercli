{
  "name": "check-usb",
  "version": "0.1.0",
  "description": "CLI tool: check-usb",
  "source": "https://packages.debian.org/check-usb",
  "checks": [{ "type": "binary", "name": "check-usb" }],
  "install_guidance": {
    "plugin": "check-usb", "binary": "check-usb", "check": "which check-usb",
    "install_steps": ["apt-get install check-usb 2>/dev/null || which check-usb", "Verify: check-usb --version", "supercli plugins install ./plugins/check-usb --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check-usb", "resource": "_", "action": "_",
    "description": "Passthrough to check-usb CLI",
    "adapter": "process", "adapterConfig": { "command": "check-usb", "passthrough": true, "missingDependencyHelp": "Install check-usb: apt-get install check-usb" }, "args": []
  }]
}
