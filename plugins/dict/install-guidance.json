{
  "plugin": "dict",
  "binary": "dict",
  "check": "which dict",
  "install_steps": [
    "brew install dict",
    "Verify: dict --version",
    "supercli plugins install ./plugins/dict --on-conflict replace --json"
  ]
}
