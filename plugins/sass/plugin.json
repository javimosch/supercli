{
  "name": "sass",
  "version": "0.1.0",
  "description": "Sass — CSS extension language for stylesheet preprocessing",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "sass"
    }
  ],
  "install_guidance": {
    "plugin": "sass",
    "binary": "sass",
    "check": "which sass",
    "install_steps": [
      "npm install -g sass"
    ],
    "note": "Node.js tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "sass",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to sass",
      "adapter": "process",
      "adapterConfig": {
        "command": "sass",
        "passthrough": true,
        "missingDependencyHelp": "Install sass: npm install -g sass"
      },
      "args": []
    }
  ]
}
