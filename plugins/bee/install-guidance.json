{
  "plugin": "bee",
  "binary": "bee",
  "check": "which bee",
  "install_steps": [
    "brew install bee",
    "Verify: bee --version",
    "supercli plugins install ./plugins/bee --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}