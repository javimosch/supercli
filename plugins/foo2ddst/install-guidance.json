{
  "plugin": "foo2ddst",
  "binary": "foo2ddst",
  "check": "which foo2ddst",
  "install_steps": [
    "apt-get install foo2ddst 2>/dev/null || which foo2ddst",
    "Verify: foo2ddst --version",
    "supercli plugins install ./plugins/foo2ddst --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}