---
name: foo2ddst
description: Use this skill when the user wants to use foo2ddst, cli tool: foo2ddst.
---

# foo2ddst Plugin

CLI tool: foo2ddst.

## Commands
- `foo2ddst <resource> <action>` — Execute foo2ddst commands
- `foo2ddst self version` — Print foo2ddst version
- `foo2ddst _ _` — Passthrough to foo2ddst CLI

## Usage Examples
- "foo2ddst --help"
- "foo2ddst self version"

## Installation
```bash
apt-get install foo2ddst 2>/dev/null || which foo2ddst
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
