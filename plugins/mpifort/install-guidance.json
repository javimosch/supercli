{
  "plugin": "mpifort",
  "binary": "mpifort",
  "check": "which mpifort",
  "install_steps": [
    "apt-get install mpifort 2>/dev/null || which mpifort",
    "Verify: mpifort --version",
    "supercli plugins install ./plugins/mpifort --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}