{
  "plugin": "vlna",
  "binary": "vlna",
  "check": "which vlna",
  "install_steps": [
    "apt-get install vlna 2>/dev/null || which vlna",
    "Verify: vlna --version",
    "supercli plugins install ./plugins/vlna --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}