{
  "plugin": "openssl",
  "binary": "openssl",
  "check": "which openssl",
  "install_steps": [
    "brew install openssl",
    "Verify: openssl --version",
    "supercli plugins install ./plugins/openssl --on-conflict replace --json"
  ]
}
