{ "plugin": "an", "binary": "an", "check": "which an", "install_steps": ["apt-get install an 2>/dev/null || which an", "Verify: an --version", "supercli plugins install ./plugins/an --on-conflict replace --json"] }
