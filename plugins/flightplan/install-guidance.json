{
  "plugin": "flightplan",
  "binary": "flightplan",
  "check": "which flightplan",
  "install_steps": ["npm install -g flightplan", "Verify: flightplan --version", "supercli plugins install ./plugins/flightplan --on-conflict replace --json"]
}
