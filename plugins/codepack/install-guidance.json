{ "plugin": "codepack", "binary": "codepack", "check": "which codepack", "install_steps": ["npm install -g codepack", "Verify: codepack --version", "supercli plugins install ./plugins/codepack --on-conflict replace --json"], "note": "Optimized for LLM context windows. Extracts code with structure preservation for AI processing pipelines." }
