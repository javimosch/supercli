{
  "plugin": "wipe",
  "binary": "wipe",
  "check": "which wipe",
  "install_steps": [
    "brew install wipe",
    "Verify: wipe --version",
    "supercli plugins install ./plugins/wipe --on-conflict replace --json"
  ]
}
