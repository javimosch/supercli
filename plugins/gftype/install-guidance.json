{
  "plugin": "gftype",
  "binary": "gftype",
  "check": "which gftype",
  "install_steps": [
    "apt-get install gftype 2>/dev/null || which gftype",
    "Verify: gftype --version",
    "supercli plugins install ./plugins/gftype --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}