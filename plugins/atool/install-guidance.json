{
  "plugin": "atool",
  "binary": "atool",
  "check": "which atool",
  "install_steps": [
    "brew install atool",
    "Verify: atool --version",
    "supercli plugins install ./plugins/atool --on-conflict replace --json"
  ]
}
