{
  "plugin": "zip",
  "binary": "zip",
  "check": "which zip",
  "install_steps": [
    "brew install zip",
    "Verify: zip --version",
    "supercli plugins install ./plugins/zip --on-conflict replace --json"
  ]
}
