{
  "name": "most",
  "version": "0.1.0",
  "description": "Terminal pager with multiple windows",
  "source": "https://packages.debian.org/most",
  "checks": [{ "type": "binary", "name": "most" }],
  "install_guidance": {
    "plugin": "most", "binary": "most", "check": "which most",
    "install_steps": ["apt-get install most", "Verify: most --version", "supercli plugins install ./plugins/most --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "most", "resource": "_", "action": "_",
    "description": "Passthrough to most CLI",
    "adapter": "process", "adapterConfig": { "command": "most", "passthrough": true, "missingDependencyHelp": "Install most: apt-get install most" }, "args": []
  }]
}
