{ "plugin": "most", "binary": "most", "check": "which most", "install_steps": ["apt-get install most", "Verify: most --version", "supercli plugins install ./plugins/most --on-conflict replace --json"] }
