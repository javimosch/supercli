{
  "plugin": "ots",
  "binary": "ots",
  "check": "which ots",
  "install_steps": [
    "brew install ots",
    "Verify: ots --version",
    "supercli plugins install ./plugins/ots --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/sniptt-official/ots@latest. End-to-end encrypted one-time secret sharing. Uses AES-256-GCM encryption. Secrets can only be viewed once before being permanently deleted."
}
