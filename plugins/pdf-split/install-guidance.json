{
  "plugin": "pdf-split",
  "binary": "pdf-split",
  "check": "which pdf-split",
  "install_steps": ["pip install pdf-split", "Verify: pdf-split --version", "supercli plugins install ./plugins/pdf-split --on-conflict replace --json"]
}
