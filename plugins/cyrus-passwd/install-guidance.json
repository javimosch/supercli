{ "plugin": "cyrus-passwd", "binary": "cyrus-passwd", "check": "which cyrus-passwd", "install_steps": ["apt-get install cyrus-passwd 2>/dev/null || which cyrus-passwd", "Verify: cyrus-passwd --version", "supercli plugins install ./plugins/cyrus-passwd --on-conflict replace --json"] }
