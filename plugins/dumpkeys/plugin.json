{
  "name": "dumpkeys",
  "version": "1.0.0",
  "description": "Keyboard layout and key mapping utility for displaying keyboard information",
  "checks": [
    {
      "type": "binary",
      "name": "dumpkeys"
    }
  ],
  "install_guidance": {
    "plugin": "dumpkeys",
    "binary": "dumpkeys",
    "check": "which dumpkeys",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dumpkeys",
      "resource": "run",
      "action": "exec",
      "description": "Keyboard layout and key mapping utility for displaying keyboard information",
      "adapter": "process",
      "adapterConfig": {
        "command": "dumpkeys",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}