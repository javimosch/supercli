{
  "name": "dumpkeys",
  "version": "1.0.0",
  "description": "dumpkeys \u2014 dumpkeys \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dumpkeys"
    }
  ],
  "install_guidance": {
    "plugin": "dumpkeys",
    "binary": "dumpkeys",
    "check": "which dumpkeys",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dumpkeys",
      "resource": "run",
      "action": "exec",
      "description": "dumpkeys \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dumpkeys",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}