{
  "plugin": "dry",
  "binary": "dry",
  "check": "which dry",
  "install_steps": [
    "brew install dry",
    "Verify: dry --version",
    "supercli plugins install ./plugins/dry --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/moncho/dry@latest. A Docker manager for the terminal. Monitor and manage Docker containers, images, networks, and volumes. Requires Docker to be running."
}
