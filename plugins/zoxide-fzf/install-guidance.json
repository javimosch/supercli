{
  "plugin": "zoxide-fzf",
  "binary": "zoxide",
  "check": "which zoxide",
  "install_steps": ["cargo install zoxide", "Verify: zoxide --version", "supercli plugins install ./plugins/zoxide-fzf --on-conflict replace --json"]
}
