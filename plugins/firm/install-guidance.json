{
  "plugin": "firm",
  "binary": "firm",
  "check": "which firm",
  "install_steps": [
    "brew tap 42futures/firm && brew install firm",
    "Verify: firm --version",
    "supercli plugins install ./plugins/firm --on-conflict replace --json"
  ],
  "note": "Also available via GitHub Releases prebuilt binaries. Run 'firm init' in a workspace directory after installing."
}
