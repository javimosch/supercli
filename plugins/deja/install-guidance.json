{ "plugin": "deja", "binary": "deja", "check": "which deja", "install_steps": ["apt-get install deja 2>/dev/null || which deja", "Verify: deja --version", "supercli plugins install ./plugins/deja --on-conflict replace --json"] }
