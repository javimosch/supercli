{
  "name": "deja",
  "version": "0.1.0",
  "description": "CLI tool: deja",
  "source": "https://packages.debian.org/deja",
  "checks": [{ "type": "binary", "name": "deja" }],
  "install_guidance": {
    "plugin": "deja", "binary": "deja", "check": "which deja",
    "install_steps": ["apt-get install deja 2>/dev/null || which deja", "Verify: deja --version", "supercli plugins install ./plugins/deja --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "deja", "resource": "_", "action": "_",
    "description": "Passthrough to deja CLI",
    "adapter": "process", "adapterConfig": { "command": "deja", "passthrough": true, "missingDependencyHelp": "Install deja: apt-get install deja" }, "args": []
  }]
}
