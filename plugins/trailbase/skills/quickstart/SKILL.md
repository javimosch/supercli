---
name: trailbase
description: Use this skill when the user wants to use trailbase for An open, sub-millisecond, single-executable Firebase alternative with type-safe .
---

# trailbase Plugin

An open, sub-millisecond, single-executable Firebase alternative with type-safe APIs, built-in WebAssembly runtime, realtime subscriptions, auth, and admin UI built on Rust, SQLite & Wasmtime.

## Commands

### General
- `trailbase self version` — Print trailbase version
- `trailbase _ _` — Passthrough to trailbase CLI

## Usage Examples
- "Run trailbase"
- "Use trailbase to An open, sub-millisecond, single-executable Firebase alterna"

## Installation

```bash
pip install trailbase
```

## Key Features
- An open, sub-millisecond, single-executable Firebase alternative with type-safe APIs, built-in WebAs
- CLI tool, headless and scriptable
