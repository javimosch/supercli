{
  "plugin": "mdBook",
  "binary": "mdbook",
  "check": "which mdbook",
  "install_steps": ["cargo install mdbook", "Verify: mdbook --version", "supercli plugins install ./plugins/mdBook --on-conflict replace --json"]
}
