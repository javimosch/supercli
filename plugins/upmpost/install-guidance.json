{
  "plugin": "upmpost",
  "binary": "upmpost",
  "check": "which upmpost",
  "install_steps": [
    "apt-get install upmpost 2>/dev/null || which upmpost",
    "Verify: upmpost --version",
    "supercli plugins install ./plugins/upmpost --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}