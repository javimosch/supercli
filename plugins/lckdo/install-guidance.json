{
  "plugin": "lckdo",
  "binary": "lckdo",
  "check": "which lckdo",
  "install_steps": [
    "apt-get install lckdo 2>/dev/null || which lckdo",
    "Verify: lckdo --version",
    "supercli plugins install ./plugins/lckdo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}