{
  "plugin": "lomath",
  "binary": "lomath",
  "check": "which lomath",
  "install_steps": [
    "apt-get install lomath 2>/dev/null || which lomath",
    "Verify: lomath --version",
    "supercli plugins install ./plugins/lomath --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}