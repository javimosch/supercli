{
  "name": "ac",
  "version": "0.1.0",
  "description": "CLI tool: ac",
  "source": "https://packages.debian.org/ac",
  "checks": [{ "type": "binary", "name": "ac" }],
  "install_guidance": {
    "plugin": "ac", "binary": "ac", "check": "which ac",
    "install_steps": ["apt-get install ac 2>/dev/null || which ac", "Verify: ac --version", "supercli plugins install ./plugins/ac --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ac", "resource": "_", "action": "_",
    "description": "Passthrough to ac CLI",
    "adapter": "process", "adapterConfig": { "command": "ac", "passthrough": true, "missingDependencyHelp": "Install ac: apt-get install ac" }, "args": []
  }]
}
