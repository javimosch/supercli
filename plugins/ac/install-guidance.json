{ "plugin": "ac", "binary": "ac", "check": "which ac", "install_steps": ["apt-get install ac 2>/dev/null || which ac", "Verify: ac --version", "supercli plugins install ./plugins/ac --on-conflict replace --json"] }
