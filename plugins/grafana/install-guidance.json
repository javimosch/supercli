{
  "plugin": "grafana",
  "binary": "grafana",
  "check": "which grafana",
  "install_steps": [
    "# Install grafana",
    "# See grafana.com",
    "# apt: sudo apt-get install grafana",
    "# brew: brew install grafana",
    "# cargo: cargo install grafana",
    "# npm: npm install -g grafana"
  ]
}