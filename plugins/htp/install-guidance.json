{
  "plugin": "htp",
  "binary": "htp",
  "check": "which htp",
  "install_steps": [
    "cargo install htp",
    "Verify: htp --version",
    "supercli plugins install ./plugins/htp --on-conflict replace --json"
  ],
  "note": "Binary name: htp"
}
