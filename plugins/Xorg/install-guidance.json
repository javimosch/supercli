{
  "plugin": "Xorg",
  "binary": "Xorg",
  "check": "which Xorg",
  "install_steps": [
    "apt-get install Xorg 2>/dev/null || which Xorg",
    "Verify: Xorg --version",
    "supercli plugins install ./plugins/Xorg --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}