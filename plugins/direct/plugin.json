{
  "name": "direct",
  "version": "0.1.0",
  "description": "CLI tool: direct",
  "source": "https://packages.debian.org/direct",
  "checks": [{ "type": "binary", "name": "direct" }],
  "install_guidance": {
    "plugin": "direct", "binary": "direct", "check": "which direct",
    "install_steps": ["apt-get install direct 2>/dev/null || which direct", "Verify: direct --version", "supercli plugins install ./plugins/direct --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "direct", "resource": "_", "action": "_",
    "description": "Passthrough to direct CLI",
    "adapter": "process", "adapterConfig": { "command": "direct", "passthrough": true, "missingDependencyHelp": "Install direct: apt-get install direct" }, "args": []
  }]
}
