{ "plugin": "direct", "binary": "direct", "check": "which direct", "install_steps": ["apt-get install direct 2>/dev/null || which direct", "Verify: direct --version", "supercli plugins install ./plugins/direct --on-conflict replace --json"] }
