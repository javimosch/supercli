{
  "plugin": "wsdump",
  "binary": "wsdump",
  "check": "which wsdump",
  "install_steps": [
    "apt-get install wsdump 2>/dev/null || which wsdump",
    "Verify: wsdump --version",
    "supercli plugins install ./plugins/wsdump --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}