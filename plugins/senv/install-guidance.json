{
  "plugin": "senv",
  "binary": "senv",
  "check": "which senv",
  "install_steps": [
    "npm install -g senv",
    "Verify: senv --version"
  ],
  "note": "Encrypt and decrypt .env files to protect sensitive environment variables in version control."
}