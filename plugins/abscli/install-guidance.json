{
  "plugin": "abscli",
  "binary": "abscli",
  "check": "which abscli",
  "install_steps": ["pip install abscli", "Verify: abscli --version", "supercli plugins install ./plugins/abscli --on-conflict replace --json"]
}
