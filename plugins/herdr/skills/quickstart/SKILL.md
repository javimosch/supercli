---
name: herdr
description: Use this skill when the user wants to use herdr for agent multiplexer that lives in your terminal..
---

# herdr Plugin

agent multiplexer that lives in your terminal.

## Commands

### General
- `herdr self version` — Print herdr version
- `herdr _ _` — Passthrough to herdr CLI

## Usage Examples
- "Run herdr"
- "Use herdr to agent multiplexer that lives in your terminal."

## Installation

```bash
pip install herdr
```

## Key Features
- agent multiplexer that lives in your terminal.
- CLI tool, headless and scriptable
