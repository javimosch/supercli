{
  "plugin": "termshot",
  "binary": "termshot",
  "check": "which termshot",
  "install_steps": ["cargo install termshot", "Verify: termshot --version", "supercli plugins install ./plugins/termshot --on-conflict replace --json"]
}
