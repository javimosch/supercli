{ "plugin": "delaunay", "binary": "delaunay", "check": "which delaunay", "install_steps": ["apt-get install delaunay 2>/dev/null || which delaunay", "Verify: delaunay --version", "supercli plugins install ./plugins/delaunay --on-conflict replace --json"] }
