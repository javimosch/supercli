{
  "plugin": "rename-cli",
  "binary": "rename",
  "check": "which rename",
  "install_steps": [
    "npm install -g rename-cli",
    "Verify: rename --help",
    "supercli plugins install ./plugins/rename-cli --on-conflict replace --json"
  ],
  "note": "Rename-cli performs batch file renaming using search and replace patterns. Supports glob patterns for selecting files. Use dry-run mode (-d) to preview changes before applying."
}
