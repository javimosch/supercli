{
  "plugin": "pppd",
  "binary": "pppd",
  "check": "which pppd",
  "install_steps": [
    "apt-get install pppd 2>/dev/null || which pppd",
    "Verify: pppd --version",
    "supercli plugins install ./plugins/pppd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}