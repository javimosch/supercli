{
  "plugin": "ecs-deploy",
  "binary": "ecs-deploy",
  "check": "which ecs-deploy",
  "install_steps": [
    "pip install ecs-deploy",
    "Verify: ecs-deploy --version"
  ],
  "note": "Zero-downtime deployment tool for Amazon ECS services. Supports rolling deployments, blue/green, and canary strategies."
}