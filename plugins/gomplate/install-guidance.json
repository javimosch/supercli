{
  "plugin": "gomplate",
  "binary": "gomplate",
  "check": "which gomplate",
  "install_steps": [
    "brew install gomplate",
    "Verify: gomplate --version",
    "supercli plugins install ./plugins/gomplate --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/hairyhenderson/gomplate/v4/cmd/gomplate@latest. Flexible template rendering with Go templates. Supports local and remote datasources including Consul, Vault, HTTP, and more. Great for config generation and automation."
}
