{
  "plugin": "zgrep",
  "binary": "zgrep",
  "check": "which zgrep",
  "install_steps": ["system package", "Verify: zgrep --version", "supercli plugins install ./plugins/zgrep --on-conflict replace --json"]
}
