{
  "plugin": "phlare",
  "binary": "phlare",
  "check": "which phlare",
  "install_steps": ["brew install phlare", "Verify: phlare --version", "supercli plugins install ./plugins/phlare --on-conflict replace --json"]
}
