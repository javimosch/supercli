{
  "plugin": "pipet",
  "binary": "pipet",
  "check": "which pipet",
  "install_steps": [
    "pip install pipet",
    "Verify: pipet --version",
    "supercli plugins install ./plugins/pipet --on-conflict replace --json"
  ],
  "note": "Language: Go"
}