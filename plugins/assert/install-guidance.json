{ "plugin": "assert", "binary": "assert", "check": "which assert", "install_steps": ["apt-get install assert 2>/dev/null || which assert", "Verify: assert --version", "supercli plugins install ./plugins/assert --on-conflict replace --json"] }
