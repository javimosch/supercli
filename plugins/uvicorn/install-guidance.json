{
  "plugin": "uvicorn",
  "binary": "uvicorn",
  "check": "which uvicorn",
  "install_steps": [
    "pip install uvicorn",
    "Verify: uvicorn --version",
    "supercli plugins install ./plugins/uvicorn --on-conflict replace --json"
  ],
  "note": "Also available via: pip install 'uvicorn[standard]' for extra features (websockets, httptools). Commonly used with FastAPI: pip install fastapi uvicorn. Supports HTTP/1.1, WebSockets, and HTTP/2 (via h2). Production deployment recommended with Gunicorn + Uvicorn workers."
}
