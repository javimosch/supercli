{
  "plugin": "koji",
  "binary": "koji",
  "check": "which koji",
  "install_steps": [
    "brew install koji",
    "Verify: koji --version",
    "supercli plugins install ./plugins/koji --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}