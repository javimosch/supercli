{ "plugin": "crlutil", "binary": "crlutil", "check": "which crlutil", "install_steps": ["apt-get install crlutil 2>/dev/null || which crlutil", "Verify: crlutil --version", "supercli plugins install ./plugins/crlutil --on-conflict replace --json"] }
