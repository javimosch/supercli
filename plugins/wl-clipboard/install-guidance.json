{
  "plugin": "wl-clipboard",
  "binary": "wl-copy",
  "check": "which wl-copy",
  "install_steps": [
    "brew install wl-clipboard",
    "Verify: wl-copy --version",
    "supercli plugins install ./plugins/wl-clipboard --on-conflict replace --json"
  ]
}
