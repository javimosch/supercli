{
  "plugin": "has",
  "binary": "has",
  "check": "which has",
  "install_steps": [
    "brew install has",
    "Verify: has --version",
    "supercli plugins install ./plugins/has --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}