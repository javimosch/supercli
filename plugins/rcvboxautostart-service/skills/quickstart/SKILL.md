---
name: rcvboxautostart-service
description: Use this skill when the user wants to use rcvboxautostart-service, cli tool: rcvboxautostart-service.
---

# rcvboxautostart-service Plugin

CLI tool: rcvboxautostart-service.

## Commands
- `rcvboxautostart-service <resource> <action>` — Execute rcvboxautostart-service commands
- `rcvboxautostart-service self version` — Print rcvboxautostart-service version
- `rcvboxautostart-service _ _` — Passthrough to rcvboxautostart-service CLI

## Usage Examples
- "rcvboxautostart-service --help"
- "rcvboxautostart-service self version"

## Installation
```bash
apt-get install rcvboxautostart-service 2>/dev/null || which rcvboxautostart-service
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
