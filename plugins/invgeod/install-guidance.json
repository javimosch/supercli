{
  "plugin": "invgeod",
  "binary": "invgeod",
  "check": "which invgeod",
  "install_steps": [
    "apt-get install invgeod 2>/dev/null || which invgeod",
    "Verify: invgeod --version",
    "supercli plugins install ./plugins/invgeod --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}