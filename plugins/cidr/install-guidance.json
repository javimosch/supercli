{
  "plugin": "cidr",
  "binary": "cidr",
  "check": "which cidr",
  "install_steps": [
    "brew install cidr",
    "Verify: cidr --version",
    "supercli plugins install ./plugins/cidr --on-conflict replace --json"
  ]
}