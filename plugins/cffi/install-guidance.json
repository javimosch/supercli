{
  "plugin": "cffi",
  "binary": "cffi",
  "check": "which cffi",
  "install_steps": [
    "brew install cffi",
    "Verify: cffi --version",
    "supercli plugins install ./plugins/cffi --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}