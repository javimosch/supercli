{
  "plugin": "sar.sysstat",
  "binary": "sar.sysstat",
  "check": "which sar.sysstat",
  "install_steps": [
    "# Install sar.sysstat",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/sar.sysstat-linux-amd64 -o /usr/local/bin/sar.sysstat",
    "chmod +x /usr/local/bin/sar.sysstat",
    "# Or via package manager, see /usr/bin/sar.sysstat"
  ]
}