{
  "plugin": "javah",
  "binary": "javah",
  "check": "which javah",
  "install_steps": [
    "apt-get install javah 2>/dev/null || which javah",
    "Verify: javah --version",
    "supercli plugins install ./plugins/javah --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}