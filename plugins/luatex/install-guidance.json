{
  "plugin": "luatex",
  "binary": "luatex",
  "check": "which luatex",
  "install_steps": [
    "apt-get install luatex 2>/dev/null || which luatex",
    "Verify: luatex --version",
    "supercli plugins install ./plugins/luatex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}