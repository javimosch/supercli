{
  "plugin": "mptopdf",
  "binary": "mptopdf",
  "check": "which mptopdf",
  "install_steps": [
    "apt-get install mptopdf 2>/dev/null || which mptopdf",
    "Verify: mptopdf --version",
    "supercli plugins install ./plugins/mptopdf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}