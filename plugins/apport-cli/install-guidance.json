{ "plugin": "apport-cli", "binary": "apport-cli", "check": "which apport-cli", "install_steps": ["apt-get install apport-cli 2>/dev/null || which apport-cli", "Verify: apport-cli --version", "supercli plugins install ./plugins/apport-cli --on-conflict replace --json"] }
