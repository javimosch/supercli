{
  "plugin": "degit",
  "binary": "degit",
  "check": "which degit",
  "install_steps": [
    "npm install -g degit",
    "Verify: degit --version",
    "supercli plugins install ./plugins/degit --on-conflict replace --json"
  ]
}
