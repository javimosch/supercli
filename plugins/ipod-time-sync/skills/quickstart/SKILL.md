---
name: ipod-time-sync
description: Use this skill when the user wants to use ipod-time-sync, cli tool: ipod-time-sync.
---

# ipod-time-sync Plugin

CLI tool: ipod-time-sync.

## Commands
- `ipod-time-sync <resource> <action>` — Execute ipod-time-sync commands
- `ipod-time-sync self version` — Print ipod-time-sync version
- `ipod-time-sync _ _` — Passthrough to ipod-time-sync CLI

## Usage Examples
- "ipod-time-sync --help"
- "ipod-time-sync self version"

## Installation
```bash
apt-get install ipod-time-sync 2>/dev/null || which ipod-time-sync
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
