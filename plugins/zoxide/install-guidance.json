{
  "plugin": "zoxide",
  "binary": "zoxide",
  "check": "which zoxide",
  "install_steps": [
    "brew install zoxide",
    "Verify: zoxide --version",
    "supercli plugins install ./plugins/zoxide --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install zoxide. Requires shell integration (eval \"$(zoxide init bash|zsh|fish)\"). Integrates with fzf for fuzzy directory selection. Tracks directory visits based on frequency and recency."
}
