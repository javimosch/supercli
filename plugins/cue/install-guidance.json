{
  "plugin": "cue",
  "binary": "cue",
  "check": "which cue",
  "install_steps": [
    "brew install cue",
    "Verify: cue --version",
    "supercli plugins install ./plugins/cue --on-conflict replace --json"
  ]
}