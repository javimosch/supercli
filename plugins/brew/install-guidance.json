{
  "plugin": "brew",
  "binary": "brew",
  "check": "which brew",
  "install_steps": [
    "/bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\"",
    "Verify: brew --version",
    "supercli plugins install ./plugins/brew --on-conflict replace --json"
  ],
  "note": "Homebrew is the standard package manager for macOS. Also available on Linux via linuxbrew (https://docs.brew.sh/Homebrew-on-Linux)."
}
