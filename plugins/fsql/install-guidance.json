{
  "plugin": "fsql",
  "binary": "fsql",
  "check": "which fsql",
  "install_steps": [
    "brew install fsql",
    "Verify: fsql --version",
    "supercli plugins install ./plugins/fsql --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}