{
  "plugin": "wheel",
  "binary": "wheel",
  "check": "which wheel",
  "install_steps": [
    "apt-get install wheel 2>/dev/null || which wheel",
    "Verify: wheel --version",
    "supercli plugins install ./plugins/wheel --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}