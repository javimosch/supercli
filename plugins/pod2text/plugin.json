{
  "name": "pod2text",
  "version": "1.0.0",
  "description": "pod2text \u2014 pod2text \u2014 CLI utility",
  "source": "/usr/bin/pod2text",
  "checks": [
    {
      "type": "binary",
      "name": "pod2text"
    }
  ],
  "install_guidance": {
    "plugin": "pod2text",
    "binary": "pod2text",
    "check": "which pod2text",
    "install_steps": [
      "# Install pod2text:",
      "# apt: sudo apt-get install pod2text",
      "# brew: brew install pod2text",
      "# cargo: cargo install pod2text",
      "# npm: npm install -g pod2text",
      "# pip: pip install pod2text",
      "# or download from /usr/bin/pod2text"
    ]
  },
  "commands": [
    {
      "namespace": "pod2text",
      "resource": "pod2text",
      "action": "run",
      "description": "Run pod2text",
      "adapter": "process",
      "adapterConfig": {
        "command": "pod2text",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install pod2text from /usr/bin/pod2text"
      },
      "args": []
    }
  ]
}