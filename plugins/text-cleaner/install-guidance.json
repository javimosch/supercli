{
  "plugin": "text-cleaner",
  "binary": "text-cleaner",
  "check": "which text-cleaner",
  "install_steps": ["npm install -g text-cleaner", "Verify: text-cleaner --version", "supercli plugins install ./plugins/text-cleaner --on-conflict replace --json"]
}
