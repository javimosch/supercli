{
  "plugin": "emoji-list",
  "binary": "emoji-list",
  "check": "which emoji-list",
  "install_steps": ["script/binary", "Verify: emoji-list --version", "supercli plugins install ./plugins/emoji-list --on-conflict replace --json"]
}
