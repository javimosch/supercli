{
  "plugin": "iconv",
  "binary": "iconv",
  "check": "which iconv",
  "install_steps": [
    "Pre-installed on most Linux systems (part of glibc)",
    "macOS: built-in",
    "Verify: iconv --version",
    "supercli plugins install ./plugins/iconv --on-conflict replace --json"
  ]
}
