{
  "plugin": "yaml-convert",
  "binary": "yaml-convert",
  "check": "which yaml-convert",
  "install_steps": [
    "npm install -g yaml-convert",
    "Verify: yaml-convert --help",
    "supercli plugins install ./plugins/yaml-convert --on-conflict replace --json"
  ],
  "note": "Yaml-convert converts between YAML and JSON formats. Can output as plain JSON, YAML, or wrap as ES6/Node.js modules. Deprecated in favor of the yaml package but still functional."
}
