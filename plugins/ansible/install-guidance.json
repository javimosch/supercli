{
  "plugin": "ansible",
  "binary": "ansible",
  "check": "which ansible",
  "install_steps": [
    "pip install ansible",
    "Verify: ansible --version",
    "supercli plugins install ./plugins/ansible --on-conflict replace --json"
  ],
  "note": "Also available via: brew install ansible, apt install ansible, dnf install ansible, or pipx install ansible"
}
