{
  "plugin": "linkedin",
  "binary": "node",
  "check": "which node",
  "install_steps": [
    "npm install -g node",
    "Verify: node --version",
    "supercli plugins install ./plugins/linkedin --on-conflict replace --json"
  ]
}
