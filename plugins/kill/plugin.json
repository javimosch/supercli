{
  "name": "kill",
  "version": "0.1.0",
  "description": "kill — Terminate processes by PID",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "kill"
    }
  ],
  "install_guidance": {
    "plugin": "kill",
    "binary": "kill",
    "check": "which kill",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/kill --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "kill",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to kill CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "kill",
        "passthrough": true,
        "missingDependencyHelp": "Install kill: base system"
      },
      "args": []
    }
  ]
}
