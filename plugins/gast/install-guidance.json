{
  "plugin": "gast",
  "binary": "gast",
  "check": "which gast",
  "install_steps": [
    "apt-get install gast 2>/dev/null || which gast",
    "Verify: gast --version",
    "supercli plugins install ./plugins/gast --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}