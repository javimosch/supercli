{
  "plugin": "HEAD",
  "binary": "HEAD",
  "check": "which HEAD",
  "install_steps": [
    "apt-get install HEAD 2>/dev/null || which HEAD",
    "Verify: HEAD --version",
    "supercli plugins install ./plugins/HEAD --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}