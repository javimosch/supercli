{
  "plugin": "deeptools",
  "binary": "deepTools",
  "check": "which deepTools",
  "install_steps": [
    "# Install deeptools",
    "# See deeptools.readthedocs.io",
    "# apt: sudo apt-get install deeptools",
    "# brew: brew install deeptools",
    "# cargo: cargo install deeptools",
    "# npm: npm install -g deeptools"
  ]
}