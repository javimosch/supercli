{
  "plugin": "gws",
  "binary": "gws",
  "check": "which gws",
  "install_steps": [
    "npm install -g @googleworkspace/cli",
    "Verify: gws --version",
    "supercli plugins install ./plugins/gws --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew: brew install googleworkspace-cli. Or build from source: cargo install --git https://github.com/googleworkspace/cli --locked. Authenticate with: gws auth setup (interactive), gws auth login (OAuth), or use Service Account / export flow for headless/CI environments. Requires a Google Cloud project for OAuth credentials."
}
