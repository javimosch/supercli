{
  "plugin": "db-backup",
  "binary": "db-backup",
  "check": "which db-backup",
  "install_steps": ["go install github.com/omegion/db-backup@latest", "Verify: db-backup --version", "supercli plugins install ./plugins/db-backup --on-conflict replace --json"]
}
