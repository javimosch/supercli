{
  "plugin": "kubesess",
  "binary": "kubesess",
  "check": "which kubesess",
  "install_steps": [
    "brew install kubesess",
    "Verify: kubesess --version",
    "supercli plugins install ./plugins/kubesess --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}