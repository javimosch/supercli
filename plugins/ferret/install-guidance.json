{
  "plugin": "ferret",
  "binary": "ferret",
  "check": "which ferret",
  "install_steps": [
    "brew install ferret",
    "Verify: ferret --version",
    "supercli plugins install ./plugins/ferret --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/MontFerret/ferret/cmd/ferret@latest. Uses FQL (Ferret Query Language) for declarative web scraping. Supports dynamic content with headless Chrome. Can crawl multiple pages and extract structured data."
}
