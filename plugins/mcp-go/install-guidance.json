{
  "plugin": "mcp-go",
  "binary": "mcp-go",
  "check": "which mcp-go",
  "install_steps": [
    "pip install mcp-go",
    "Verify: mcp-go --version",
    "supercli plugins install ./plugins/mcp-go --on-conflict replace --json"
  ],
  "note": "Language: Go"
}