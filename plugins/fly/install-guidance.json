{
  "plugin": "fly",
  "binary": "fly",
  "check": "which fly",
  "install_steps": [
    "# Install fly",
    "# See concourse-ci.org",
    "# apt: sudo apt-get install fly",
    "# brew: brew install fly",
    "# cargo: cargo install fly",
    "# npm: npm install -g fly"
  ]
}