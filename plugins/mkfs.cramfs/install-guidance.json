{
  "plugin": "mkfs.cramfs",
  "binary": "mkfs.cramfs",
  "check": "which mkfs.cramfs",
  "install_steps": [
    "apt-get install mkfs.cramfs 2>/dev/null || which mkfs.cramfs",
    "Verify: mkfs.cramfs --version",
    "supercli plugins install ./plugins/mkfs.cramfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}