---
name: mount.fuse
description: Use this skill when the user wants to use mount.fuse, cli tool: mount.fuse.
---

# mount.fuse Plugin

CLI tool: mount.fuse.

## Commands
- `mount.fuse <resource> <action>` — Execute mount.fuse commands
- `mount.fuse self version` — Print mount.fuse version
- `mount.fuse _ _` — Passthrough to mount.fuse CLI

## Usage Examples
- "mount.fuse --help"
- "mount.fuse self version"

## Installation
```bash
apt-get install mount.fuse 2>/dev/null || which mount.fuse
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
