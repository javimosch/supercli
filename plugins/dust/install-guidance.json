{
  "plugin": "dust",
  "binary": "dust",
  "check": "which dust",
  "install_steps": [
    "brew install dust",
    "Verify: dust --version",
    "supercli plugins install ./plugins/dust --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install du-dust. A more intuitive version of du written in Rust. Displays disk usage with a visual tree view showing which directories are taking up space. Color-coded and graph bars for easy scanning."
}
