{
  "plugin": "partx",
  "binary": "partx",
  "check": "which partx",
  "install_steps": [
    "# Install partx",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/partx-linux-amd64 -o /usr/local/bin/partx",
    "chmod +x /usr/local/bin/partx",
    "# Or via package manager, see /usr/bin/partx"
  ]
}