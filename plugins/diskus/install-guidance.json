{
  "plugin": "diskus",
  "binary": "diskus",
  "check": "which diskus",
  "install_steps": [
    "cargo install diskus",
    "Verify: diskus --version",
    "supercli plugins install ./plugins/diskus --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, pacman, scoop, nix. Very simple tool - just computes directory size."
}