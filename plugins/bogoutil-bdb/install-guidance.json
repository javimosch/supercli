{ "plugin": "bogoutil-bdb", "binary": "bogoutil-bdb", "check": "which bogoutil-bdb", "install_steps": ["apt-get install bogoutil-bdb 2>/dev/null || which bogoutil-bdb", "Verify: bogoutil-bdb --version", "supercli plugins install ./plugins/bogoutil-bdb --on-conflict replace --json"] }
