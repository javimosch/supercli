{
  "plugin": "wpa_cli",
  "binary": "wpa_cli",
  "check": "which wpa_cli",
  "install_steps": [
    "apt-get install wpa_cli 2>/dev/null || which wpa_cli",
    "Verify: wpa_cli --version",
    "supercli plugins install ./plugins/wpa_cli --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}