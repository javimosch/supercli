{
  "name": "designer",
  "version": "0.1.0",
  "description": "CLI tool: designer",
  "source": "https://packages.debian.org/designer",
  "checks": [{ "type": "binary", "name": "designer" }],
  "install_guidance": {
    "plugin": "designer", "binary": "designer", "check": "which designer",
    "install_steps": ["apt-get install designer 2>/dev/null || which designer", "Verify: designer --version", "supercli plugins install ./plugins/designer --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "designer", "resource": "_", "action": "_",
    "description": "Passthrough to designer CLI",
    "adapter": "process", "adapterConfig": { "command": "designer", "passthrough": true, "missingDependencyHelp": "Install designer: apt-get install designer" }, "args": []
  }]
}
