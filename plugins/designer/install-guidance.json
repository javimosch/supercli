{ "plugin": "designer", "binary": "designer", "check": "which designer", "install_steps": ["apt-get install designer 2>/dev/null || which designer", "Verify: designer --version", "supercli plugins install ./plugins/designer --on-conflict replace --json"] }
