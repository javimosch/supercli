---
name: gzip
description: Use this skill when the user wants to use the gzip command-line tool.
---

# gzip Plugin

GNU file compression utility

## Commands
- `gzip self version` — Print version
- `gzip _ _` — Passthrough to gzip

## Installation
```bash
brew install gzip
```
