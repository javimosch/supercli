{
  "plugin": "gzip",
  "binary": "gzip",
  "check": "which gzip",
  "install_steps": [
    "brew install gzip",
    "Verify: gzip --version",
    "supercli plugins install ./plugins/gzip --on-conflict replace --json"
  ]
}
