{
  "plugin": "pdfopen",
  "binary": "pdfopen",
  "check": "which pdfopen",
  "install_steps": [
    "apt-get install pdfopen 2>/dev/null || which pdfopen",
    "Verify: pdfopen --version",
    "supercli plugins install ./plugins/pdfopen --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}