{
  "plugin": "we-get",
  "binary": "we-get",
  "check": "which we-get",
  "install_steps": [
    "pip install we-get",
    "Verify: we-get --version",
    "supercli plugins install ./plugins/we-get --on-conflict replace --json"
  ]
}