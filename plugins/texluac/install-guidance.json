{
  "plugin": "texluac",
  "binary": "texluac",
  "check": "which texluac",
  "install_steps": [
    "apt-get install texluac 2>/dev/null || which texluac",
    "Verify: texluac --version",
    "supercli plugins install ./plugins/texluac --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}