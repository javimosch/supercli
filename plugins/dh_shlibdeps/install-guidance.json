{ "plugin": "dh_shlibdeps", "binary": "dh_shlibdeps", "check": "which dh_shlibdeps", "install_steps": ["apt-get install dh_shlibdeps 2>/dev/null || which dh_shlibdeps", "Verify: dh_shlibdeps --version", "supercli plugins install ./plugins/dh_shlibdeps --on-conflict replace --json"] }
