{
  "plugin": "setpriv",
  "binary": "setpriv",
  "check": "which setpriv",
  "install_steps": [
    "apt-get install setpriv 2>/dev/null || which setpriv",
    "Verify: setpriv --version",
    "supercli plugins install ./plugins/setpriv --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}