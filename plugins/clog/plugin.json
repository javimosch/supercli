{
  "name": "clog",
  "version": "0.1.0",
  "description": "CLI tool: clog",
  "source": "https://packages.debian.org/clog",
  "checks": [{ "type": "binary", "name": "clog" }],
  "install_guidance": {
    "plugin": "clog", "binary": "clog", "check": "which clog",
    "install_steps": ["apt-get install clog 2>/dev/null || which clog", "Verify: clog --version", "supercli plugins install ./plugins/clog --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "clog", "resource": "_", "action": "_",
    "description": "Passthrough to clog CLI",
    "adapter": "process", "adapterConfig": { "command": "clog", "passthrough": true, "missingDependencyHelp": "Install clog: apt-get install clog" }, "args": []
  }]
}
