{ "plugin": "clog", "binary": "clog", "check": "which clog", "install_steps": ["apt-get install clog 2>/dev/null || which clog", "Verify: clog --version", "supercli plugins install ./plugins/clog --on-conflict replace --json"] }
