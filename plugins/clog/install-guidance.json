{
  "plugin": "clog",
  "binary": "clog",
  "check": "which clog",
  "install_steps": ["cargo install clog-cli", "Verify: clog --version", "supercli plugins install ./plugins/clog --on-conflict replace --json"]
}
