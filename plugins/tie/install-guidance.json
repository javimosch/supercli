{
  "plugin": "tie",
  "binary": "tie",
  "check": "which tie",
  "install_steps": [
    "apt-get install tie 2>/dev/null || which tie",
    "Verify: tie --version",
    "supercli plugins install ./plugins/tie --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}