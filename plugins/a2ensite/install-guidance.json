{ "plugin": "a2ensite", "binary": "a2ensite", "check": "which a2ensite", "install_steps": ["apt-get install a2ensite 2>/dev/null || which a2ensite", "Verify: a2ensite --version", "supercli plugins install ./plugins/a2ensite --on-conflict replace --json"] }
