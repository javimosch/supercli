{
  "name": "dbprint",
  "version": "0.1.0",
  "description": "CLI tool: dbprint",
  "source": "https://packages.debian.org/dbprint",
  "checks": [{ "type": "binary", "name": "dbprint" }],
  "install_guidance": {
    "plugin": "dbprint", "binary": "dbprint", "check": "which dbprint",
    "install_steps": ["apt-get install dbprint 2>/dev/null || which dbprint", "Verify: dbprint --version", "supercli plugins install ./plugins/dbprint --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbprint", "resource": "_", "action": "_",
    "description": "Passthrough to dbprint CLI",
    "adapter": "process", "adapterConfig": { "command": "dbprint", "passthrough": true, "missingDependencyHelp": "Install dbprint: apt-get install dbprint" }, "args": []
  }]
}
