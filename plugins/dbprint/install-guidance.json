{ "plugin": "dbprint", "binary": "dbprint", "check": "which dbprint", "install_steps": ["apt-get install dbprint 2>/dev/null || which dbprint", "Verify: dbprint --version", "supercli plugins install ./plugins/dbprint --on-conflict replace --json"] }
