{
  "plugin": "mongosh",
  "binary": "mongosh",
  "check": "which mongosh",
  "install_steps": [
    "brew install mongosh",
    "Verify: mongosh --version",
    "supercli plugins install ./plugins/mongosh --on-conflict replace --json"
  ]
}