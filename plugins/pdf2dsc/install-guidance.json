{
  "plugin": "pdf2dsc",
  "binary": "pdf2dsc",
  "check": "which pdf2dsc",
  "install_steps": [
    "apt-get install pdf2dsc 2>/dev/null || which pdf2dsc",
    "Verify: pdf2dsc --version",
    "supercli plugins install ./plugins/pdf2dsc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}