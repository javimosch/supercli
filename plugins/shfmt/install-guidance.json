{
  "plugin": "shfmt",
  "binary": "shfmt",
  "check": "which shfmt",
  "install_steps": [
    "go install mvdan.cc/sh/v3/cmd/shfmt@latest",
    "Verify: shfmt --version",
    "supercli plugins install ./plugins/shfmt --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, pacman, dnf, scoop, docker (mvdan/shfmt). Requires Go toolchain for go install."
}
