{
  "plugin": "gitu",
  "binary": "gitu",
  "check": "which gitu",
  "install_steps": [
    "cargo install gitu",
    "Verify: gitu --version",
    "supercli plugins install ./plugins/gitu --on-conflict replace --json"
  ],
  "note": "Alternatively: brew install gitu (macOS/Linux) or download pre-built binaries from GitHub releases. Configuration file at ~/.config/gitu/config.toml. Uses VISUAL, EDITOR, or GIT_EDITOR env vars (checked in order) to determine editor for commit messages."
}
