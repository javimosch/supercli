{
  "plugin": "google-maps-scraper",
  "binary": "node",
  "check": "node --version",
  "install_steps": [
    "sc lightpanda cli setup",
    "supercli plugins install ./plugins/google-maps-scraper --on-conflict replace --json"
  ],
  "note": "Requires Node.js and Lightpanda runtime. The scraper uses Lightpanda + puppeteer-core for lightweight headless browser automation."
}
