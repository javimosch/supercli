{
  "plugin": "stegify",
  "binary": "stegify",
  "check": "which stegify",
  "install_steps": [
    "brew install stegify",
    "Verify: stegify --version",
    "supercli plugins install ./plugins/stegify --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/DimitarPetrov/stegify@latest. Steganography tool for images using LSB encoding. Hide any file inside an image with minimal visual impact. Supports PNG, JPEG, and BMP carriers."
}
