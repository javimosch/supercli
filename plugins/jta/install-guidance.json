{
  "plugin": "jta",
  "binary": "jta",
  "check": "which jta",
  "install_steps": [
    "brew install hikanner/jta/jta",
    "Verify: jta --version",
    "supercli plugins install ./plugins/jta --on-conflict replace --json"
  ],
  "note": "Also available via: go install github.com/hikanner/jta/cmd/jta@latest"
}
