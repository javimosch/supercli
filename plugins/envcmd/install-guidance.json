{
  "plugin": "envcmd",
  "binary": "envcmd",
  "check": "which envcmd",
  "install_steps": ["npm install -g envcmd", "Verify: envcmd --version", "supercli plugins install ./plugins/envcmd --on-conflict replace --json"]
}
