{
  "plugin": "sem",
  "binary": "sem",
  "check": "which sem",
  "install_steps": [
    "brew install sem-cli",
    "Verify: sem --version",
    "supercli plugins install ./plugins/sem --on-conflict replace --json"
  ],
  "note": "Supports 23 languages via tree-sitter. Entity-level diff detects renames/moves, not just additions/deletions. Can replace git diff with sem setup. Also available as npm package @ataraxy-labs/sem."
}
