{
  "plugin": "espeak",
  "binary": "espeak",
  "check": "which espeak",
  "install_steps": [
    "brew install espeak",
    "Verify: espeak --version",
    "supercli plugins install ./plugins/espeak --on-conflict replace --json"
  ]
}
