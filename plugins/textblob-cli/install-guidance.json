{
  "plugin": "textblob-cli",
  "binary": "textblob-cli",
  "check": "which textblob-cli",
  "install_steps": ["pip install textblob", "Verify: textblob-cli --version", "supercli plugins install ./plugins/textblob-cli --on-conflict replace --json"]
}
