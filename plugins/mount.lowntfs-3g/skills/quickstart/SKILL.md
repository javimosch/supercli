---
name: mount.lowntfs-3g
description: Use this skill when the user wants to use mount.lowntfs-3g, cli tool: mount.lowntfs-3g.
---

# mount.lowntfs-3g Plugin

CLI tool: mount.lowntfs-3g.

## Commands
- `mount.lowntfs-3g <resource> <action>` — Execute mount.lowntfs-3g commands
- `mount.lowntfs-3g self version` — Print mount.lowntfs-3g version
- `mount.lowntfs-3g _ _` — Passthrough to mount.lowntfs-3g CLI

## Usage Examples
- "mount.lowntfs-3g --help"
- "mount.lowntfs-3g self version"

## Installation
```bash
apt-get install mount.lowntfs-3g 2>/dev/null || which mount.lowntfs-3g
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
