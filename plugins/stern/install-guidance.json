{
  "plugin": "stern",
  "binary": "stern",
  "check": "which stern",
  "install_steps": [
    "brew install stern",
    "Verify: stern --version",
    "supercli plugins install ./plugins/stern --on-conflict replace --json"
  ]
}