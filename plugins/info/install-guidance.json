{
  "plugin": "info",
  "binary": "info",
  "check": "which info",
  "install_steps": [
    "apt-get install info 2>/dev/null || which info",
    "Verify: info --version",
    "supercli plugins install ./plugins/info --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}