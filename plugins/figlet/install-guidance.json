{
  "plugin": "figlet",
  "binary": "figlet",
  "check": "which figlet",
  "install_steps": [
    "brew install figlet",
    "Verify: figlet --version",
    "supercli plugins install ./plugins/figlet --on-conflict replace --json"
  ]
}
