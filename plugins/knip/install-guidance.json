{
  "plugin": "knip",
  "binary": "knip",
  "check": "which knip",
  "install_steps": [
    "npm install -g knip",
    "Verify: knip --version",
    "supercli plugins install ./plugins/knip --on-conflict replace --json"
  ]
}
