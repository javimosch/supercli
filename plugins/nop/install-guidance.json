{
  "plugin": "nop",
  "binary": "nop",
  "check": "which nop",
  "install_steps": [
    "apt-get install nop 2>/dev/null || which nop",
    "Verify: nop --version",
    "supercli plugins install ./plugins/nop --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}