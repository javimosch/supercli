{ "plugin": "chat", "binary": "chat", "check": "which chat", "install_steps": ["apt-get install chat 2>/dev/null || which chat", "Verify: chat --version", "supercli plugins install ./plugins/chat --on-conflict replace --json"] }
