{
  "name": "chat",
  "version": "0.1.0",
  "description": "CLI tool: chat",
  "source": "https://packages.debian.org/chat",
  "checks": [{ "type": "binary", "name": "chat" }],
  "install_guidance": {
    "plugin": "chat", "binary": "chat", "check": "which chat",
    "install_steps": ["apt-get install chat 2>/dev/null || which chat", "Verify: chat --version", "supercli plugins install ./plugins/chat --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "chat", "resource": "_", "action": "_",
    "description": "Passthrough to chat CLI",
    "adapter": "process", "adapterConfig": { "command": "chat", "passthrough": true, "missingDependencyHelp": "Install chat: apt-get install chat" }, "args": []
  }]
}
