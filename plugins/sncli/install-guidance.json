{
  "plugin": "sncli",
  "binary": "sncli",
  "check": "which sncli",
  "install_steps": ["pip install sncli", "Verify: sncli --version", "supercli plugins install ./plugins/sncli --on-conflict replace --json"]
}
