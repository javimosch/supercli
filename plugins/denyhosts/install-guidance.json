{ "plugin": "denyhosts", "binary": "denyhosts", "check": "which denyhosts", "install_steps": ["apt-get install denyhosts 2>/dev/null || which denyhosts", "Verify: denyhosts --version", "supercli plugins install ./plugins/denyhosts --on-conflict replace --json"] }
