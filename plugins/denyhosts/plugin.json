{
  "name": "denyhosts",
  "version": "0.1.0",
  "description": "CLI tool: denyhosts",
  "source": "https://packages.debian.org/denyhosts",
  "checks": [{ "type": "binary", "name": "denyhosts" }],
  "install_guidance": {
    "plugin": "denyhosts", "binary": "denyhosts", "check": "which denyhosts",
    "install_steps": ["apt-get install denyhosts 2>/dev/null || which denyhosts", "Verify: denyhosts --version", "supercli plugins install ./plugins/denyhosts --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "denyhosts", "resource": "_", "action": "_",
    "description": "Passthrough to denyhosts CLI",
    "adapter": "process", "adapterConfig": { "command": "denyhosts", "passthrough": true, "missingDependencyHelp": "Install denyhosts: apt-get install denyhosts" }, "args": []
  }]
}
