{
  "name": "acyclic",
  "version": "0.1.0",
  "description": "CLI tool: acyclic",
  "source": "https://packages.debian.org/acyclic",
  "checks": [{ "type": "binary", "name": "acyclic" }],
  "install_guidance": {
    "plugin": "acyclic", "binary": "acyclic", "check": "which acyclic",
    "install_steps": ["apt-get install acyclic 2>/dev/null || which acyclic", "Verify: acyclic --version", "supercli plugins install ./plugins/acyclic --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acyclic", "resource": "_", "action": "_",
    "description": "Passthrough to acyclic CLI",
    "adapter": "process", "adapterConfig": { "command": "acyclic", "passthrough": true, "missingDependencyHelp": "Install acyclic: apt-get install acyclic" }, "args": []
  }]
}
