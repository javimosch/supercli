---
name: LLocalSearch
description: Use this skill when the user wants to use LLocalSearch for LLocalSearch is a completely locally running search aggregator using LLM Agents..
---

# LLocalSearch Plugin

LLocalSearch is a completely locally running search aggregator using LLM Agents. The user can ask a question and the system will use a chain of LLMs to find the answer. The user can see the progress of the agents and the final answer. No OpenAI or Google API keys are needed.

## Commands

### General
- `LLocalSearch self version` — Print LLocalSearch version
- `LLocalSearch _ _` — Passthrough to llocalsearch CLI

## Usage Examples
- "Run LLocalSearch"
- "Use llocalsearch to LLocalSearch is a completely locally running search aggregat"

## Installation

```bash
pip install llocalsearch
```

## Key Features
- LLocalSearch is a completely locally running search aggregator using LLM Agents. The user can ask a 
- CLI tool, headless and scriptable
