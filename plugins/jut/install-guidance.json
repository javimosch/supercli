{
  "plugin": "jut",
  "binary": "jut",
  "check": "which jut",
  "install_steps": [
    "pip install jut",
    "Verify: jut --version",
    "supercli plugins install ./plugins/jut --on-conflict replace --json"
  ]
}