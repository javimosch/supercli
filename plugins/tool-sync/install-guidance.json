{
  "plugin": "tool-sync",
  "binary": "tool-sync",
  "check": "which tool-sync",
  "install_steps": ["cargo install tool-sync", "Verify: tool-sync --version", "supercli plugins install ./plugins/tool-sync --on-conflict replace --json"]
}
