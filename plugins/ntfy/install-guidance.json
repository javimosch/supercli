{
  "plugin": "ntfy",
  "binary": "ntfy",
  "check": "which ntfy",
  "install_steps": [
    "# Install ntfy",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/ntfy-linux-amd64 -o /usr/local/bin/ntfy",
    "chmod +x /usr/local/bin/ntfy",
    "# Or via package manager, see ntfy.sh"
  ]
}