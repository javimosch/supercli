{ "plugin": "dh_strip", "binary": "dh_strip", "check": "which dh_strip", "install_steps": ["apt-get install dh_strip 2>/dev/null || which dh_strip", "Verify: dh_strip --version", "supercli plugins install ./plugins/dh_strip --on-conflict replace --json"] }
