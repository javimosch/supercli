{
  "plugin": "tirith",
  "binary": "tirith",
  "check": "which tirith",
  "install_steps": [
    "curl -fsSL https://raw.githubusercontent.com/sheeki03/tirith/main/install.sh | bash",
    "Verify: tirith doctor",
    "supercli plugins install ./plugins/tirith --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (macOS), Linux packages, Windows, and Nix. See https://github.com/sheeki03/tirith for platform-specific install instructions. Offline by default - no telemetry or phone-home behavior."
}
