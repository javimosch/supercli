{
  "plugin": "fastmod",
  "binary": "fastmod",
  "check": "which fastmod",
  "install_steps": [
    "brew install fastmod",
    "Verify: fastmod --version",
    "supercli plugins install ./plugins/fastmod --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install fastmod. A fast partial replacement for codemod. Supports regex-based search and replace across a codebase. Great for bulk refactoring and code migrations."
}
