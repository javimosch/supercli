{
  "plugin": "queries",
  "binary": "queries",
  "check": "which queries",
  "install_steps": [
    "# Install queries",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/queries-linux-amd64 -o /usr/local/bin/queries",
    "chmod +x /usr/local/bin/queries",
    "# Or via package manager, see crates.io/crates/queries"
  ]
}