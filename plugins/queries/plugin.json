{
  "name": "queries",
  "version": "1.0.0",
  "description": "queries \u2014 SQL query runner",
  "source": "crates.io/crates/queries",
  "checks": [
    {
      "type": "binary",
      "name": "queries"
    }
  ],
  "install_guidance": {
    "plugin": "queries",
    "binary": "queries",
    "check": "which queries",
    "install_steps": [
      "# Install queries:",
      "# apt: sudo apt-get install queries",
      "# brew: brew install queries",
      "# cargo: cargo install queries",
      "# npm: npm install -g queries",
      "# pip: pip install queries",
      "# or download from crates.io/crates/queries"
    ]
  },
  "commands": [
    {
      "namespace": "queries",
      "resource": "queries",
      "action": "run",
      "description": "Run queries",
      "adapter": "process",
      "adapterConfig": {
        "command": "queries",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install queries from crates.io/crates/queries"
      },
      "args": []
    }
  ]
}