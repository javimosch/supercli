{
  "plugin": "usb_printerid",
  "binary": "usb_printerid",
  "check": "which usb_printerid",
  "install_steps": [
    "apt-get install usb_printerid 2>/dev/null || which usb_printerid",
    "Verify: usb_printerid --version",
    "supercli plugins install ./plugins/usb_printerid --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}