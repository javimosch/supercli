{
  "name": "usb_printerid",
  "version": "0.1.0",
  "description": "CLI tool: usb_printerid",
  "source": "https://manpages.debian.org/usb_printerid",
  "checks": [
    {
      "type": "binary",
      "name": "usb_printerid"
    }
  ],
  "install_guidance": {
    "plugin": "usb_printerid",
    "binary": "usb_printerid",
    "check": "which usb_printerid",
    "install_steps": [
      "apt-get install usb_printerid 2>/dev/null || which usb_printerid",
      "Verify: usb_printerid --version",
      "supercli plugins install ./plugins/usb_printerid --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "usb_printerid",
      "resource": "self",
      "action": "version",
      "description": "Print usb_printerid version",
      "adapter": "process",
      "adapterConfig": {
        "command": "usb_printerid",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install usb_printerid: apt-get install usb_printerid 2>/dev/null || which usb_printerid"
      },
      "args": []
    },
    {
      "namespace": "usb_printerid",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to usb_printerid CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "usb_printerid",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install usb_printerid: apt-get install usb_printerid 2>/dev/null || which usb_printerid"
      },
      "args": []
    }
  ]
}