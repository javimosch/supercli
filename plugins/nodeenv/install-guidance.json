{
  "plugin": "nodeenv",
  "binary": "nodeenv",
  "check": "which nodeenv",
  "install_steps": [
    "apt-get install nodeenv 2>/dev/null || which nodeenv",
    "Verify: nodeenv --version",
    "supercli plugins install ./plugins/nodeenv --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}