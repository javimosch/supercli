{
  "plugin": "ilograph-cli",
  "binary": "ilograph-cli",
  "check": "which ilograph-cli",
  "install_steps": ["npm install -g ilograph", "Verify: ilograph-cli --version", "supercli plugins install ./plugins/ilograph-cli --on-conflict replace --json"]
}
