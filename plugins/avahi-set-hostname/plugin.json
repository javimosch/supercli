{
  "name": "avahi-set-hostname",
  "version": "0.1.0",
  "description": "Set mDNS hostname",
  "source": "https://packages.debian.org/avahi-set-hostname",
  "checks": [{ "type": "binary", "name": "avahi-set-hostname" }],
  "install_guidance": {
    "plugin": "avahi-set-hostname", "binary": "avahi-set-hostname", "check": "which avahi-set-hostname",
    "install_steps": ["apt-get install avahi-utils", "Verify: avahi-set-hostname --version", "supercli plugins install ./plugins/avahi-set-hostname --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "avahi-set-hostname", "resource": "_", "action": "_",
    "description": "Passthrough to avahi-set-hostname CLI",
    "adapter": "process", "adapterConfig": { "command": "avahi-set-hostname", "passthrough": true, "missingDependencyHelp": "Install avahi-set-hostname: apt-get install avahi-utils" }, "args": []
  }]
}
