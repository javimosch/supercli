{
  "name": "addpart",
  "version": "1.0.0",
  "description": "addpart \u2014 CLI addpart \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=addpart+cli",
  "checks": [
    {
      "type": "binary",
      "name": "addpart"
    }
  ],
  "install_guidance": {
    "plugin": "addpart",
    "binary": "addpart",
    "check": "which addpart",
    "install_steps": [
      "Install via package manager: apt install addpart or brew install addpart"
    ]
  },
  "commands": [
    {
      "namespace": "addpart",
      "resource": "run",
      "action": "exec",
      "description": "addpart \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "addpart",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install addpart via package manager"
      },
      "args": []
    }
  ]
}