{
  "name": "ca-legacy",
  "version": "0.1.0",
  "description": "CLI tool: ca-legacy",
  "source": "https://packages.debian.org/ca-legacy",
  "checks": [{ "type": "binary", "name": "ca-legacy" }],
  "install_guidance": {
    "plugin": "ca-legacy", "binary": "ca-legacy", "check": "which ca-legacy",
    "install_steps": ["apt-get install ca-legacy 2>/dev/null || which ca-legacy", "Verify: ca-legacy --version", "supercli plugins install ./plugins/ca-legacy --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ca-legacy", "resource": "_", "action": "_",
    "description": "Passthrough to ca-legacy CLI",
    "adapter": "process", "adapterConfig": { "command": "ca-legacy", "passthrough": true, "missingDependencyHelp": "Install ca-legacy: apt-get install ca-legacy" }, "args": []
  }]
}
