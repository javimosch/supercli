{ "plugin": "ca-legacy", "binary": "ca-legacy", "check": "which ca-legacy", "install_steps": ["apt-get install ca-legacy 2>/dev/null || which ca-legacy", "Verify: ca-legacy --version", "supercli plugins install ./plugins/ca-legacy --on-conflict replace --json"] }
