{
  "plugin": "slurp",
  "binary": "slurp",
  "check": "which slurp",
  "install_steps": [
    "brew install slurp",
    "Verify: slurp --version",
    "supercli plugins install ./plugins/slurp --on-conflict replace --json"
  ]
}
