{ "plugin": "dhcpd", "binary": "dhcpd", "check": "which dhcpd", "install_steps": ["apt-get install dhcpd 2>/dev/null || which dhcpd", "Verify: dhcpd --version", "supercli plugins install ./plugins/dhcpd --on-conflict replace --json"] }
