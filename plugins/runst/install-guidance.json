{
  "plugin": "runst",
  "binary": "runst",
  "check": "which runst",
  "install_steps": [
    "# Install runst",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/runst-linux-amd64 -o /usr/local/bin/runst",
    "chmod +x /usr/local/bin/runst",
    "# Or via package manager, see crates.io/crates/runst"
  ]
}