{
  "plugin": "kubent",
  "binary": "kubent",
  "check": "which kubent",
  "install_steps": [
    "brew install kubent",
    "sh -c \"$(curl -sSL https://git.io/install-kubent)\"",
    "kubent"
  ],
  "note": "kubent = kube-no-trouble. Requires access to a Kubernetes cluster via kubeconfig or in-cluster config. Supports config file via KUBENT_CONFIG environment variable. Default behavior scans all namespaces."
}
