{ "plugin": "timeshift", "binary": "timeshift", "check": "which timeshift", "install_steps": ["apt-get install timeshift", "Verify: timeshift --version", "supercli plugins install ./plugins/timeshift --on-conflict replace --json"] }
