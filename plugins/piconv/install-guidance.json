{
  "plugin": "piconv",
  "binary": "piconv",
  "check": "which piconv",
  "install_steps": [
    "apt-get install piconv 2>/dev/null || which piconv",
    "Verify: piconv --version",
    "supercli plugins install ./plugins/piconv --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}