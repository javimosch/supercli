{ "plugin": "dbaccess", "binary": "dbaccess", "check": "which dbaccess", "install_steps": ["apt-get install dbaccess 2>/dev/null || which dbaccess", "Verify: dbaccess --version", "supercli plugins install ./plugins/dbaccess --on-conflict replace --json"] }
