{
  "name": "dbaccess",
  "version": "0.1.0",
  "description": "CLI tool: dbaccess",
  "source": "https://packages.debian.org/dbaccess",
  "checks": [{ "type": "binary", "name": "dbaccess" }],
  "install_guidance": {
    "plugin": "dbaccess", "binary": "dbaccess", "check": "which dbaccess",
    "install_steps": ["apt-get install dbaccess 2>/dev/null || which dbaccess", "Verify: dbaccess --version", "supercli plugins install ./plugins/dbaccess --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbaccess", "resource": "_", "action": "_",
    "description": "Passthrough to dbaccess CLI",
    "adapter": "process", "adapterConfig": { "command": "dbaccess", "passthrough": true, "missingDependencyHelp": "Install dbaccess: apt-get install dbaccess" }, "args": []
  }]
}
