{
  "name": "diskscan",
  "version": "0.1.0",
  "description": "CLI tool: diskscan",
  "source": "https://packages.debian.org/diskscan",
  "checks": [{ "type": "binary", "name": "diskscan" }],
  "install_guidance": {
    "plugin": "diskscan", "binary": "diskscan", "check": "which diskscan",
    "install_steps": ["apt-get install diskscan 2>/dev/null || which diskscan", "Verify: diskscan --version", "supercli plugins install ./plugins/diskscan --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "diskscan", "resource": "_", "action": "_",
    "description": "Passthrough to diskscan CLI",
    "adapter": "process", "adapterConfig": { "command": "diskscan", "passthrough": true, "missingDependencyHelp": "Install diskscan: apt-get install diskscan" }, "args": []
  }]
}
