{
  "plugin": "vgmerge",
  "binary": "vgmerge",
  "check": "which vgmerge",
  "install_steps": [
    "apt-get install vgmerge 2>/dev/null || which vgmerge",
    "Verify: vgmerge --version",
    "supercli plugins install ./plugins/vgmerge --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}