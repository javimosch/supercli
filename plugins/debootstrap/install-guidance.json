{ "plugin": "debootstrap", "binary": "debootstrap", "check": "which debootstrap", "install_steps": ["apt-get install debootstrap 2>/dev/null || which debootstrap", "Verify: debootstrap --version", "supercli plugins install ./plugins/debootstrap --on-conflict replace --json"] }
