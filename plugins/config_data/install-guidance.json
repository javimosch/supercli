{ "plugin": "config_data", "binary": "config_data", "check": "which config_data", "install_steps": ["apt-get install config_data 2>/dev/null || which config_data", "Verify: config_data --version", "supercli plugins install ./plugins/config_data --on-conflict replace --json"] }
