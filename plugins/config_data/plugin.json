{
  "name": "config_data",
  "version": "0.1.0",
  "description": "CLI tool: config_data",
  "source": "https://packages.debian.org/config_data",
  "checks": [{ "type": "binary", "name": "config_data" }],
  "install_guidance": {
    "plugin": "config_data", "binary": "config_data", "check": "which config_data",
    "install_steps": ["apt-get install config_data 2>/dev/null || which config_data", "Verify: config_data --version", "supercli plugins install ./plugins/config_data --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "config_data", "resource": "_", "action": "_",
    "description": "Passthrough to config_data CLI",
    "adapter": "process", "adapterConfig": { "command": "config_data", "passthrough": true, "missingDependencyHelp": "Install config_data: apt-get install config_data" }, "args": []
  }]
}
