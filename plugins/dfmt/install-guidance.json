{
  "plugin": "dfmt",
  "binary": "dfmt",
  "check": "which dfmt",
  "install_steps": [
    "brew install dfmt",
    "Verify: dfmt --version",
    "supercli plugins install ./plugins/dfmt --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}