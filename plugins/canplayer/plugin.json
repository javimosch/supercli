{
  "name": "canplayer",
  "version": "0.1.0",
  "description": "CLI tool: canplayer",
  "source": "https://packages.debian.org/canplayer",
  "checks": [{ "type": "binary", "name": "canplayer" }],
  "install_guidance": {
    "plugin": "canplayer", "binary": "canplayer", "check": "which canplayer",
    "install_steps": ["apt-get install canplayer 2>/dev/null || which canplayer", "Verify: canplayer --version", "supercli plugins install ./plugins/canplayer --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "canplayer", "resource": "_", "action": "_",
    "description": "Passthrough to canplayer CLI",
    "adapter": "process", "adapterConfig": { "command": "canplayer", "passthrough": true, "missingDependencyHelp": "Install canplayer: apt-get install canplayer" }, "args": []
  }]
}
