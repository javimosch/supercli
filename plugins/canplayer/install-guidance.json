{ "plugin": "canplayer", "binary": "canplayer", "check": "which canplayer", "install_steps": ["apt-get install canplayer 2>/dev/null || which canplayer", "Verify: canplayer --version", "supercli plugins install ./plugins/canplayer --on-conflict replace --json"] }
