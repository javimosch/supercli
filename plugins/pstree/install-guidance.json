{
  "plugin": "pstree",
  "binary": "pstree",
  "check": "which pstree",
  "install_steps": [
    "# Install pstree",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pstree-linux-amd64 -o /usr/local/bin/pstree",
    "chmod +x /usr/local/bin/pstree",
    "# Or via package manager, see /usr/bin/pstree"
  ]
}