{
  "plugin": "oxlint",
  "binary": "oxlint",
  "check": "which oxlint",
  "install_steps": [
    "npm install -g oxlint",
    "Verify: oxlint --version",
    "supercli plugins install ./plugins/oxlint --on-conflict replace --json"
  ]
}
