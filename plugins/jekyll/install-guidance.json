{
  "plugin": "jekyll",
  "binary": "jekyll",
  "check": "which jekyll",
  "install_steps": [
    "brew install jekyll",
    "Verify: jekyll --version",
    "supercli plugins install ./plugins/jekyll --on-conflict replace --json"
  ]
}
