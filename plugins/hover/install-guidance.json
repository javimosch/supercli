{
  "plugin": "hover",
  "binary": "hover",
  "check": "which hover",
  "install_steps": [
    "brew install hover",
    "Verify: hover --version",
    "supercli plugins install ./plugins/hover --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/go-flutter-desktop/hover/cmd/hover@latest. A CLI tool to build and run Flutter desktop applications. Works with Go-flutter for desktop Flutter apps."
}
