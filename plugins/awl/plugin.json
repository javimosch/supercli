{
  "name": "awl",
  "version": "1.0.0",
  "description": "DNS lookup tool supporting UDP, TCP, TLS, HTTPS, DNSCrypt, and QUIC with JSON output",
  "commands": [
    {
      "name": "lookup",
      "description": "Perform DNS lookup with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-a",
      "description": "Lookup A records with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "A", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-aaaa",
      "description": "Lookup AAAA records with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "AAAA", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-mx",
      "description": "Lookup MX records with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "MX", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-txt",
      "description": "Lookup TXT records with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "TXT", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-ns",
      "description": "Lookup NS records with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "NS", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-https",
      "description": "DNS over HTTPS lookup with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "+https", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-tls",
      "description": "DNS over TLS lookup with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "+tls", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-quic",
      "description": "DNS over QUIC lookup with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "+quic", "+json"],
      "output_format": "json"
    },
    {
      "name": "lookup-dnscrypt",
      "description": "DNSCrypt lookup with JSON output",
      "binary": "awl",
      "args": ["{{.domain}}", "+dnscrypt", "+json"],
      "output_format": "json"
    }
  ]
}