{ "plugin": "digdag", "binary": "digdag", "check": "which digdag", "install_steps": ["apt-get install digdag 2>/dev/null || which digdag", "Verify: digdag --version", "supercli plugins install ./plugins/digdag --on-conflict replace --json"] }
