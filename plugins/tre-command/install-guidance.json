{
  "plugin": "tre-command",
  "binary": "tre",
  "check": "which tre",
  "install_steps": [
    "cargo install tre-command",
    "Verify: tre --version",
    "supercli plugins install ./plugins/tre-command --on-conflict replace --json"
  ]
}
