{
  "plugin": "mrd",
  "binary": "mrd",
  "check": "which mrd",
  "install_steps": [
    "apt-get install mrd 2>/dev/null || which mrd",
    "Verify: mrd --version",
    "supercli plugins install ./plugins/mrd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}