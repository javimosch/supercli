---
name: tbl
description: Use this skill when the user wants to use tbl, cli tool: tbl.
---

# tbl Plugin

CLI tool: tbl.

## Commands
- `tbl <resource> <action>` — Execute tbl commands
- `tbl self version` — Print tbl version
- `tbl _ _` — Passthrough to tbl CLI

## Usage Examples
- "tbl --help"
- "tbl self version"

## Installation
```bash
apt-get install tbl 2>/dev/null || which tbl
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
