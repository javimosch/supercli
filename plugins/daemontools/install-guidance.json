{
  "plugin": "daemontools",
  "binary": "daemontools",
  "check": "which daemontools",
  "install_steps": ["make && sudo make install", "Verify: daemontools --version", "supercli plugins install ./plugins/daemontools --on-conflict replace --json"]
}
