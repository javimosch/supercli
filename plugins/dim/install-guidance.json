{
  "plugin": "dim",
  "binary": "dim",
  "check": "which dim",
  "install_steps": [
    "npm install -g dim",
    "Verify: dim --version",
    "supercli plugins install ./plugins/dim --on-conflict replace --json"
  ]
}