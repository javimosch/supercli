{
  "plugin": "fab",
  "binary": "fab",
  "check": "which fab",
  "install_steps": [
    "# Install fab",
    "# See fabfile.org",
    "# apt: sudo apt-get install fab",
    "# brew: brew install fab",
    "# cargo: cargo install fab",
    "# npm: npm install -g fab"
  ]
}