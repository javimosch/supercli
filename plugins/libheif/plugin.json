{
  "name": "libheif",
  "version": "0.1.0",
  "description": "libheif — HEIC/HEIF image format tools",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "heif-enc"
    }
  ],
  "install_guidance": {
    "plugin": "libheif",
    "binary": "heif-enc",
    "check": "which heif-enc",
    "install_steps": [
      "brew install libheif"
    ],
    "note": "Utility tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "libheif",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to heif-enc CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "heif-enc",
        "passthrough": true,
        "missingDependencyHelp": "Install libheif: brew install libheif"
      },
      "args": []
    }
  ]
}
