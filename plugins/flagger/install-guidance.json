{
  "plugin": "flagger",
  "binary": "flagger",
  "check": "which flagger",
  "install_steps": ["brew install flagger", "Verify: flagger --version", "supercli plugins install ./plugins/flagger --on-conflict replace --json"]
}
