{
  "plugin": "weave",
  "binary": "weave",
  "check": "which weave",
  "install_steps": [
    "apt-get install weave 2>/dev/null || which weave",
    "Verify: weave --version",
    "supercli plugins install ./plugins/weave --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}