{
  "plugin": "saw",
  "binary": "saw",
  "check": "which saw",
  "install_steps": [
    "brew install saw",
    "Verify: saw --version",
    "supercli plugins install ./plugins/saw --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/TylerBrock/saw/cmd/saw@latest. A modern and intuitive CLI tool for AWS. Manage AWS resources with a simple, human-friendly command line interface. Simplifies AWS CLI usage."
}
