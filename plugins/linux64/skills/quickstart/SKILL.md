---
name: linux64
description: Use this skill when the user wants to use linux64, cli tool: linux64.
---

# linux64 Plugin

CLI tool: linux64.

## Commands
- `linux64 <resource> <action>` — Execute linux64 commands
- `linux64 self version` — Print linux64 version
- `linux64 _ _` — Passthrough to linux64 CLI

## Usage Examples
- "linux64 --help"
- "linux64 self version"

## Installation
```bash
apt-get install linux64 2>/dev/null || which linux64
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
