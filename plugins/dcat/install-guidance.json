{ "plugin": "dcat", "binary": "dcat", "check": "which dcat", "install_steps": ["apt-get install dcat 2>/dev/null || which dcat", "Verify: dcat --version", "supercli plugins install ./plugins/dcat --on-conflict replace --json"] }
