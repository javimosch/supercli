{
  "name": "dcat",
  "version": "0.1.0",
  "description": "CLI tool: dcat",
  "source": "https://packages.debian.org/dcat",
  "checks": [{ "type": "binary", "name": "dcat" }],
  "install_guidance": {
    "plugin": "dcat", "binary": "dcat", "check": "which dcat",
    "install_steps": ["apt-get install dcat 2>/dev/null || which dcat", "Verify: dcat --version", "supercli plugins install ./plugins/dcat --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dcat", "resource": "_", "action": "_",
    "description": "Passthrough to dcat CLI",
    "adapter": "process", "adapterConfig": { "command": "dcat", "passthrough": true, "missingDependencyHelp": "Install dcat: apt-get install dcat" }, "args": []
  }]
}
