{
  "plugin": "systemd",
  "binary": "systemd",
  "check": "which systemd",
  "install_steps": [
    "apt-get install systemd 2>/dev/null || which systemd",
    "Verify: systemd --version",
    "supercli plugins install ./plugins/systemd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}