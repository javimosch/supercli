{
  "plugin": "sssd",
  "binary": "sssd",
  "check": "which sssd",
  "install_steps": [
    "apt-get install sssd 2>/dev/null || which sssd",
    "Verify: sssd --version",
    "supercli plugins install ./plugins/sssd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}