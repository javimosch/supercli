{ "plugin": "arp-scan", "binary": "arp-scan", "check": "which arp-scan", "install_steps": ["apt-get install arp-scan", "Verify: arp-scan --version", "supercli plugins install ./plugins/arp-scan --on-conflict replace --json"] }
