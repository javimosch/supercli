{
  "plugin": "auto",
  "binary": "auto",
  "check": "which auto",
  "install_steps": ["npm install -g auto", "Verify: auto --version", "supercli plugins install ./plugins/auto --on-conflict replace --json"]
}
