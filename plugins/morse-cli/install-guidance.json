{
  "plugin": "morse-cli",
  "binary": "morse-cli",
  "check": "which morse-cli",
  "install_steps": ["pip install morse-cli", "Verify: morse-cli --version", "supercli plugins install ./plugins/morse-cli --on-conflict replace --json"]
}
