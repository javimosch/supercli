{
  "name": "dm-list",
  "version": "0.1.0",
  "description": "CLI tool: dm-list",
  "source": "https://packages.debian.org/dm-list",
  "checks": [{ "type": "binary", "name": "dm-list" }],
  "install_guidance": {
    "plugin": "dm-list", "binary": "dm-list", "check": "which dm-list",
    "install_steps": ["apt-get install dm-list 2>/dev/null || which dm-list", "Verify: dm-list --version", "supercli plugins install ./plugins/dm-list --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-list", "resource": "_", "action": "_",
    "description": "Passthrough to dm-list CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-list", "passthrough": true, "missingDependencyHelp": "Install dm-list: apt-get install dm-list" }, "args": []
  }]
}
