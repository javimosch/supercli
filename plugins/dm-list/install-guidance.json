{ "plugin": "dm-list", "binary": "dm-list", "check": "which dm-list", "install_steps": ["apt-get install dm-list 2>/dev/null || which dm-list", "Verify: dm-list --version", "supercli plugins install ./plugins/dm-list --on-conflict replace --json"] }
