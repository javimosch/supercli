{
  "plugin": "gifgen",
  "binary": "gifgen",
  "check": "which gifgen",
  "install_steps": ["go install github.com/gifgen@latest", "Verify: gifgen --version", "supercli plugins install ./plugins/gifgen --on-conflict replace --json"]
}
