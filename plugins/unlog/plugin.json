{
  "name": "unlog",
  "version": "1.0.0",
  "description": "Fast CLI tool for log analysis with JSON output and optional LLM analysis",
  "commands": [
    {
      "name": "analyze",
      "description": "Analyze log directory with JSON output",
      "binary": "unlog",
      "args": ["--format", "json", "{{.directory}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-file",
      "description": "Analyze single log file with JSON output",
      "binary": "unlog",
      "args": ["--format", "json", "{{.file}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-stdin",
      "description": "Analyze logs from stdin with JSON output",
      "binary": "unlog",
      "args": ["--format", "json", "-"],
      "output_format": "json"
    },
    {
      "name": "analyze-level",
      "description": "Analyze logs with minimum log level filter",
      "binary": "unlog",
      "args": ["--format", "json", "--level", "{{.level}}", "{{.directory}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-time-range",
      "description": "Analyze logs within time range",
      "binary": "unlog",
      "args": ["--format", "json", "--since", "{{.since}}", "--until", "{{.until}}", "{{.directory}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-ai",
      "description": "Analyze logs with AI-powered root cause analysis",
      "binary": "unlog",
      "args": ["--format", "json", "--ai-provider", "{{.provider}}", "{{.directory}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-verbose",
      "description": "Analyze logs with detailed output",
      "binary": "unlog",
      "args": ["--format", "json", "--verbose", "{{.directory}}"],
      "output_format": "json"
    },
    {
      "name": "analyze-markdown",
      "description": "Analyze logs and output as Markdown report",
      "binary": "unlog",
      "args": ["--format", "markdown", "{{.directory}}"],
      "output_format": "markdown"
    }
  ]
}