{
  "plugin": "gohash",
  "binary": "hasher",
  "check": "which hasher",
  "install_steps": [
    "go install github.com/martinlindhe/gohash/cmd/hasher@latest",
    "Verify: hasher --list-hashes",
    "supercli plugins install ./plugins/gohash --on-conflict replace --json"
  ],
  "note": "Includes multiple tools: hasher (calculate hashes), coder (encode/decode), findhash (search for plaintext). Supports many hash algorithms and encodings including MD5, SHA1, SHA256, base64, hex, and more."
}
