{
  "name": "dpkg-deb",
  "version": "1.0.0",
  "description": "dpkg-deb \u2014 dpkg-deb \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-deb"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-deb",
    "binary": "dpkg-deb",
    "check": "which dpkg-deb",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-deb",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-deb \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-deb",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}