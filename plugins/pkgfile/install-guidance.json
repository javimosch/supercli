{
  "plugin": "pkgfile",
  "binary": "pkgfile",
  "check": "which pkgfile",
  "install_steps": [
    "# Install pkgfile",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pkgfile-linux-amd64 -o /usr/local/bin/pkgfile",
    "chmod +x /usr/local/bin/pkgfile",
    "# Or via package manager, see github.com/falconindy/pkgfile"
  ]
}