{
  "plugin": "cloog",
  "binary": "cloog",
  "check": "which cloog",
  "install_steps": [
    "brew install cloog",
    "Verify: cloog --version",
    "supercli plugins install ./plugins/cloog --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}