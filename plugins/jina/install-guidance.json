{
  "plugin": "jina",
  "binary": "jina",
  "check": "jina --version",
  "install_steps": [
    "pip install jina-cli",
    "jina --version",
    "supercli plugins install ./plugins/jina --json"
  ],
  "note": "Jina CLI requires Python. Some commands may require Jina AI API authentication."
}
