{
  "plugin": "semtools",
  "binary": "semtools",
  "check": "which semtools",
  "install_steps": [
    "cargo install semtools",
    "Verify: semtools --version",
    "supercli plugins install ./plugins/semtools --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
