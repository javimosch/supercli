{
  "plugin": "rungs",
  "binary": "rungs",
  "check": "which rungs",
  "install_steps": [
    "apt-get install rungs 2>/dev/null || which rungs",
    "Verify: rungs --version",
    "supercli plugins install ./plugins/rungs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}