{
  "plugin": "pltotf",
  "binary": "pltotf",
  "check": "which pltotf",
  "install_steps": [
    "apt-get install pltotf 2>/dev/null || which pltotf",
    "Verify: pltotf --version",
    "supercli plugins install ./plugins/pltotf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}