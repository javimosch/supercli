{
  "plugin": "rumdl",
  "binary": "rumdl",
  "check": "which rumdl",
  "install_steps": [
    "cargo install rumdl",
    "Verify: rumdl --version",
    "supercli plugins install ./plugins/rumdl --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (brew install rumdl), npm (npm install -g rumdl), pip (pip install rumdl), uv (uv tool install rumdl), Nix, and pacman."
}
