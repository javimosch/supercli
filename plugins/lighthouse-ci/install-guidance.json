{
  "plugin": "lighthouse-ci",
  "binary": "lighthouse-ci",
  "check": "which lighthouse-ci",
  "install_steps": [
    "npm install -g @lhci/cli",
    "Verify: lighthouse-ci --version",
    "supercli plugins install ./plugins/lighthouse-ci --on-conflict replace --json"
  ]
}
