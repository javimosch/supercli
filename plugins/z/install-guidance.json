{
  "plugin": "z",
  "binary": "z",
  "check": "which z",
  "install_steps": [
    "brew install z",
    "Verify: z --version",
    "supercli plugins install ./plugins/z --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}