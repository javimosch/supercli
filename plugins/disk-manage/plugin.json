{
  "name": "disk-manage",
  "version": "0.1.0",
  "description": "CLI tool: disk-manage",
  "source": "https://packages.debian.org/disk-manage",
  "checks": [{ "type": "binary", "name": "disk-manage" }],
  "install_guidance": {
    "plugin": "disk-manage", "binary": "disk-manage", "check": "which disk-manage",
    "install_steps": ["apt-get install disk-manage 2>/dev/null || which disk-manage", "Verify: disk-manage --version", "supercli plugins install ./plugins/disk-manage --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "disk-manage", "resource": "_", "action": "_",
    "description": "Passthrough to disk-manage CLI",
    "adapter": "process", "adapterConfig": { "command": "disk-manage", "passthrough": true, "missingDependencyHelp": "Install disk-manage: apt-get install disk-manage" }, "args": []
  }]
}
