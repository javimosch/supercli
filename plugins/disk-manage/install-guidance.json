{ "plugin": "disk-manage", "binary": "disk-manage", "check": "which disk-manage", "install_steps": ["apt-get install disk-manage 2>/dev/null || which disk-manage", "Verify: disk-manage --version", "supercli plugins install ./plugins/disk-manage --on-conflict replace --json"] }
