{
  "plugin": "ftp",
  "binary": "ftp",
  "check": "which ftp",
  "install_steps": [
    "# Install ftp",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/ftp-linux-amd64 -o /usr/local/bin/ftp",
    "chmod +x /usr/local/bin/ftp",
    "# Or via package manager, see /usr/bin/ftp"
  ]
}