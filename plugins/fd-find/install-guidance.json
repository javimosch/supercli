{
  "plugin": "fd-find",
  "binary": "fd",
  "check": "which fd",
  "install_steps": [
    "brew install fd",
    "Verify: fd --version",
    "supercli plugins install ./plugins/fd-find --on-conflict replace --json"
  ]
}
