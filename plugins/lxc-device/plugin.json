{
  "name": "lxc-device",
  "version": "0.1.0",
  "description": "CLI tool: lxc-device",
  "source": "https://manpages.debian.org/lxc-device",
  "checks": [
    {
      "type": "binary",
      "name": "lxc-device"
    }
  ],
  "install_guidance": {
    "plugin": "lxc-device",
    "binary": "lxc-device",
    "check": "which lxc-device",
    "install_steps": [
      "apt-get install lxc-device 2>/dev/null || which lxc-device",
      "Verify: lxc-device --version",
      "supercli plugins install ./plugins/lxc-device --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "lxc-device",
      "resource": "self",
      "action": "version",
      "description": "Print lxc-device version",
      "adapter": "process",
      "adapterConfig": {
        "command": "lxc-device",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install lxc-device: apt-get install lxc-device 2>/dev/null || which lxc-device"
      },
      "args": []
    },
    {
      "namespace": "lxc-device",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to lxc-device CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "lxc-device",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install lxc-device: apt-get install lxc-device 2>/dev/null || which lxc-device"
      },
      "args": []
    }
  ]
}