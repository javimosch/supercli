{ "plugin": "cg_annotate", "binary": "cg_annotate", "check": "which cg_annotate", "install_steps": ["apt-get install cg_annotate 2>/dev/null || which cg_annotate", "Verify: cg_annotate --version", "supercli plugins install ./plugins/cg_annotate --on-conflict replace --json"] }
