{
  "plugin": "xclock",
  "binary": "xclock",
  "check": "which xclock",
  "install_steps": [
    "apt-get install xclock 2>/dev/null || which xclock",
    "Verify: xclock --version",
    "supercli plugins install ./plugins/xclock --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}