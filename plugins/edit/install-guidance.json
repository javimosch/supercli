{
  "plugin": "edit",
  "binary": "edit",
  "check": "which edit",
  "install_steps": [
    "cargo install edit",
    "Verify: edit --version",
    "supercli plugins install ./plugins/edit --on-conflict replace --json"
  ],
  "note": "Microsoft's terminal text editor with syntax highlighting and modern interface."
}
