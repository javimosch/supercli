{
  "plugin": "karpor",
  "binary": "karpor",
  "check": "which karpor",
  "install_steps": ["brew install karpor", "Verify: karpor --version", "supercli plugins install ./plugins/karpor --on-conflict replace --json"]
}
