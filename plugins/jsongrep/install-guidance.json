{
  "plugin": "jsongrep",
  "binary": "jg",
  "check": "which jg",
  "install_steps": [
    "cargo install jsongrep",
    "Verify: jg --version",
    "supercli plugins install ./plugins/jsongrep --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (brew install jsongrep), Winget (winget install jsongrep), and Scoop (scoop install jsongrep). Shell completions and man pages can be generated with jg generate."
}
