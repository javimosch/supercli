{ "plugin": "automake", "binary": "automake", "check": "which automake", "install_steps": ["apt-get install automake 2>/dev/null || which automake", "Verify: automake --version", "supercli plugins install ./plugins/automake --on-conflict replace --json"] }
