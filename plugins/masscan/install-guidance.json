{
  "plugin": "masscan",
  "binary": "masscan",
  "check": "which masscan",
  "install_steps": [
    "brew install masscan",
    "Verify: masscan --version",
    "supercli plugins install ./plugins/masscan --on-conflict replace --json"
  ]
}
