{
  "plugin": "gowall",
  "binary": "gowall",
  "check": "which gowall",
  "install_steps": [
    "brew install gowall",
    "Or download from https://github.com/Achno/gowall/releases",
    "Verify: gowall -v",
    "supercli plugins install ./plugins/gowall --on-conflict replace --json"
  ],
  "note": "Gowall supports .png, .jpeg, .jpg, .webp, and .avif formats. Can read from stdin and write to stdout for pipeline operations. Has optional terminal image preview feature."
}
