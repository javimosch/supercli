---
name: karakeep
description: Use this skill when the user wants to use karakeep for A self-hostable bookmark-everything app (links, notes and images) with AI-based .
---

# karakeep Plugin

A self-hostable bookmark-everything app (links, notes and images) with AI-based automatic tagging and full text search

## Commands

### General
- `karakeep self version` — Print karakeep version
- `karakeep _ _` — Passthrough to karakeep CLI

## Usage Examples
- "Run karakeep"
- "Use karakeep to A self-hostable bookmark-everything app (links, notes and im"

## Installation

```bash
pip install karakeep
```

## Key Features
- A self-hostable bookmark-everything app (links, notes and images) with AI-based automatic tagging an
- CLI tool, headless and scriptable
