---
name: textql
description: textql — execute SQL queries against structured text (CSV/TSV). No database needed, works directly on text files.
---
# textql
textql — execute SQL queries against structured text (CSV/TSV). No database needed, works directly on text files.
