{
  "plugin": "pnmtoplainpnm",
  "binary": "pnmtoplainpnm",
  "check": "which pnmtoplainpnm",
  "install_steps": [
    "apt-get install pnmtoplainpnm 2>/dev/null || which pnmtoplainpnm",
    "Verify: pnmtoplainpnm --version",
    "supercli plugins install ./plugins/pnmtoplainpnm --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}