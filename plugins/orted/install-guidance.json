{
  "plugin": "orted",
  "binary": "orted",
  "check": "which orted",
  "install_steps": [
    "apt-get install orted 2>/dev/null || which orted",
    "Verify: orted --version",
    "supercli plugins install ./plugins/orted --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}