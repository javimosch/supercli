{
  "plugin": "zfgrep",
  "binary": "zfgrep",
  "check": "which zfgrep",
  "install_steps": [
    "apt-get install zfgrep 2>/dev/null || which zfgrep",
    "Verify: zfgrep --version",
    "supercli plugins install ./plugins/zfgrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}