{
  "plugin": "agg",
  "binary": "agg",
  "check": "which agg",
  "install_steps": [
    "cargo install agg",
    "Verify: agg --version",
    "supercli plugins install ./plugins/agg --on-conflict replace --json"
  ]
}
