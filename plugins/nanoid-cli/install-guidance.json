{
  "plugin": "nanoid-cli",
  "binary": "nanoid",
  "check": "which nanoid",
  "install_steps": [
    "npm install -g nanoid-cli",
    "Verify: nanoid --version",
    "supercli plugins install ./plugins/nanoid-cli --on-conflict replace --json"
  ]
}
