{
  "name": "ssh-keyscan",
  "version": "0.1.0",
  "description": "ssh-keyscan — Collect SSH public keys",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "ssh-keyscan"
    }
  ],
  "install_guidance": {
    "plugin": "ssh-keyscan",
    "binary": "ssh-keyscan",
    "check": "which ssh-keyscan",
    "install_steps": [
      "base system"
    ],
    "note": "System utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "ssh-keyscan",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to ssh-keyscan",
      "adapter": "process",
      "adapterConfig": {
        "command": "ssh-keyscan",
        "passthrough": true,
        "missingDependencyHelp": "Install ssh-keyscan: base system"
      },
      "args": []
    }
  ]
}
