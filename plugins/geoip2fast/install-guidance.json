{
  "plugin": "geoip2fast",
  "binary": "geoip2fast",
  "check": "which geoip2fast",
  "install_steps": [
    "brew install geoip2fast",
    "Verify: geoip2fast --version",
    "supercli plugins install ./plugins/geoip2fast --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}