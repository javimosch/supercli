{
  "plugin": "dufs",
  "binary": "dufs",
  "check": "which dufs",
  "install_steps": [
    "brew install dufs",
    "Verify: dufs --version",
    "supercli plugins install ./plugins/dufs --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}