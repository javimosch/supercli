{
  "plugin": "dtsroll",
  "binary": "dtsroll",
  "check": "which dtsroll",
  "install_steps": [
    "brew install dtsroll",
    "Verify: dtsroll --version",
    "supercli plugins install ./plugins/dtsroll --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}