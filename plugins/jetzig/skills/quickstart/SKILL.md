---
name: jetzig
description: Use this skill when the user wants to use jetzig for Jetzig is a web framework written in Zig.
---

# jetzig Plugin

Jetzig is a web framework written in Zig

## Commands

### General
- `jetzig self version` — Print jetzig version
- `jetzig _ _` — Passthrough to jetzig CLI

## Usage Examples
- "Run jetzig"
- "Use jetzig to Jetzig is a web framework written in Zig"

## Installation

```bash
pip install jetzig
```

## Key Features
- Jetzig is a web framework written in Zig
- CLI tool, headless and scriptable
