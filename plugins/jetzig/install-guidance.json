{
  "plugin": "jetzig",
  "binary": "jetzig",
  "check": "which jetzig",
  "install_steps": [
    "pip install jetzig",
    "Verify: jetzig --version",
    "supercli plugins install ./plugins/jetzig --on-conflict replace --json"
  ],
  "note": "Language: Zig. Stars: 1472"
}