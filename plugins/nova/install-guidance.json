{
  "plugin": "nova",
  "binary": "nova",
  "check": "which nova",
  "install_steps": [
    "brew install FairwindsOps/tap/nova",
    "Verify: nova --version",
    "supercli plugins install ./plugins/nova --on-conflict replace --json"
  ]
}