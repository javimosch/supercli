{
  "plugin": "flex",
  "binary": "flex",
  "check": "which flex",
  "install_steps": [
    "# Install flex",
    "# See github.com/westes/flex",
    "# apt: sudo apt-get install flex",
    "# brew: brew install flex",
    "# cargo: cargo install flex",
    "# npm: npm install -g flex"
  ]
}