{
  "plugin": "rustnet",
  "binary": "rustnet",
  "check": "which rustnet",
  "install_steps": [
    "cargo install rustnet",
    "Verify: rustnet --version",
    "supercli plugins install ./plugins/rustnet --on-conflict replace --json"
  ],
  "note": "Cross-platform, sandboxed network monitoring with deep packet inspection."
}