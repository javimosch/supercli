{
  "plugin": "pm2",
  "binary": "pm2",
  "check": "which pm2",
  "install_steps": [
    "npm install -g pm2",
    "Verify: pm2 --version",
    "supercli plugins install ./plugins/pm2 --on-conflict replace --json"
  ],
  "note": "PM2 is a production process manager. Supports clustering, automatic restarts, log management, and monitoring. Use --json flag for machine-readable output. Requires pm2 save to persist process list across reboots."
}
