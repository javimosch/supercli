{
  "plugin": "autorestic",
  "binary": "autorestic",
  "check": "which autorestic",
  "install_steps": [
    "brew install autorestic",
    "Verify: autorestic --version",
    "supercli plugins install ./plugins/autorestic --on-conflict replace --json"
  ],
  "note": "Also available via npm: npm install -g autorestic. Requires restic to be installed. Simplifies restic backup configuration with YAML. Supports multiple locations and backends. Great for automated backup workflows."
}
