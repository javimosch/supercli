{
  "plugin": "oji",
  "binary": "oji",
  "check": "which oji",
  "install_steps": [
    "npm install -g oji",
    "Verify: oji --version",
    "supercli plugins install ./plugins/oji --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}