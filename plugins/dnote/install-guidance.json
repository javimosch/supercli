{
  "plugin": "dnote",
  "binary": "dnote",
  "check": "which dnote",
  "install_steps": [
    "brew install dnote",
    "Verify: dnote --version",
    "supercli plugins install ./plugins/dnote --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Simple command line notebook for programmers. Organize notes into books, search, and sync across devices. Great for building a terminal-based knowledge base."
}
