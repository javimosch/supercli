{
  "plugin": "hdoc",
  "binary": "hdoc",
  "check": "which hdoc",
  "install_steps": ["cmake -B build && cmake --build build", "Verify: hdoc --version", "supercli plugins install ./plugins/hdoc --on-conflict replace --json"]
}
