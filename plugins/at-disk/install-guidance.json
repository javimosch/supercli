{ "plugin": "at-disk", "binary": "at-disk", "check": "which at-disk", "install_steps": ["apt-get install at-disk 2>/dev/null || which at-disk", "Verify: at-disk --version", "supercli plugins install ./plugins/at-disk --on-conflict replace --json"] }
