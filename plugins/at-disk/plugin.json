{
  "name": "at-disk",
  "version": "0.1.0",
  "description": "CLI tool: at-disk",
  "source": "https://packages.debian.org/at-disk",
  "checks": [{ "type": "binary", "name": "at-disk" }],
  "install_guidance": {
    "plugin": "at-disk", "binary": "at-disk", "check": "which at-disk",
    "install_steps": ["apt-get install at-disk 2>/dev/null || which at-disk", "Verify: at-disk --version", "supercli plugins install ./plugins/at-disk --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "at-disk", "resource": "_", "action": "_",
    "description": "Passthrough to at-disk CLI",
    "adapter": "process", "adapterConfig": { "command": "at-disk", "passthrough": true, "missingDependencyHelp": "Install at-disk: apt-get install at-disk" }, "args": []
  }]
}
