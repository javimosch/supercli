{
  "plugin": "dnsping",
  "binary": "dnsping",
  "check": "which dnsping",
  "install_steps": ["go install fortio.org/dnsping@latest", "Verify: dnsping --version", "supercli plugins install ./plugins/dnsping --on-conflict replace --json"]
}
