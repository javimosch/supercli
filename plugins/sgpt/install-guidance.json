{
    "plugin": "sgpt",
    "binary": "sgpt",
    "check": "which sgpt",
    "install_steps": ["pip install shell-gpt", "which sgpt", "Verify: sgpt --version", "supercli plugins install ./plugins/sgpt --on-conflict replace --json"]
  }
