{
  "plugin": "qrcp",
  "binary": "qrcp",
  "check": "which qrcp",
  "install_steps": [
    "brew install qrcp",
    "Verify: qrcp --version",
    "supercli plugins install ./plugins/qrcp --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/claudiodangelis/qrcp@latest. Works over local WiFi. Both devices must be on same network. Supports HTTPS for secure transfer. Can compress files automatically."
}
