{
  "plugin": "font-stats",
  "binary": "font-stats",
  "check": "which font-stats",
  "install_steps": ["pip install font-stats", "Verify: font-stats --version", "supercli plugins install ./plugins/font-stats --on-conflict replace --json"]
}
