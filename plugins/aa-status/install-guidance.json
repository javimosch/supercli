{
  "plugin": "aa-status",
  "binary": "aa-status",
  "check": "which aa-status",
  "install_steps": [
    "apt-get install aa-status 2>/dev/null || which aa-status",
    "Verify: aa-status --version",
    "supercli plugins install ./plugins/aa-status --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}