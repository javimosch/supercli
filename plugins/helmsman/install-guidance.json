{
  "plugin": "helmsman",
  "binary": "helmsman",
  "check": "which helmsman",
  "install_steps": [
    "brew install helmsman",
    "Verify: helmsman --version",
    "supercli plugins install ./plugins/helmsman --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}