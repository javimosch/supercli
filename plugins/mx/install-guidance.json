{
  "plugin": "mx",
  "binary": "mx",
  "check": "which mx",
  "install_steps": [
    "brew install mx",
    "Verify: mx --version",
    "supercli plugins install ./plugins/mx --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}