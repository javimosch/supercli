{
  "plugin": "gitops-cli",
  "binary": "gitops",
  "check": "which gitops",
  "install_steps": ["brew install gitops", "Verify: gitops --version", "supercli plugins install ./plugins/gitops-cli --on-conflict replace --json"]
}
