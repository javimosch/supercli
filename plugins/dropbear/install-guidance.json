{ "plugin": "dropbear", "binary": "dropbear", "check": "which dropbear", "install_steps": ["apt-get install dropbear", "Verify: dropbear --version", "supercli plugins install ./plugins/dropbear --on-conflict replace --json"] }
