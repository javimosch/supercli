{
  "plugin": "gnupg",
  "binary": "gnupg",
  "check": "which gnupg",
  "install_steps": [
    "brew install gnupg",
    "Verify: gnupg --version",
    "supercli plugins install ./plugins/gnupg --on-conflict replace --json"
  ]
}
