{
  "plugin": "it2",
  "binary": "it2",
  "check": "which it2",
  "install_steps": [
    "pip install it2",
    "Verify: it2 --version",
    "supercli plugins install ./plugins/it2 --on-conflict replace --json"
  ]
}