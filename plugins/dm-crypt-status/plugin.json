{
  "name": "dm-crypt-status",
  "version": "0.1.0",
  "description": "CLI tool: dm-crypt-status",
  "source": "https://packages.debian.org/dm-crypt-status",
  "checks": [{ "type": "binary", "name": "dm-crypt-status" }],
  "install_guidance": {
    "plugin": "dm-crypt-status", "binary": "dm-crypt-status", "check": "which dm-crypt-status",
    "install_steps": ["apt-get install dm-crypt-status 2>/dev/null || which dm-crypt-status", "Verify: dm-crypt-status --version", "supercli plugins install ./plugins/dm-crypt-status --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-crypt-status", "resource": "_", "action": "_",
    "description": "Passthrough to dm-crypt-status CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-crypt-status", "passthrough": true, "missingDependencyHelp": "Install dm-crypt-status: apt-get install dm-crypt-status" }, "args": []
  }]
}
