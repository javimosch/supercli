{ "plugin": "dm-crypt-status", "binary": "dm-crypt-status", "check": "which dm-crypt-status", "install_steps": ["apt-get install dm-crypt-status 2>/dev/null || which dm-crypt-status", "Verify: dm-crypt-status --version", "supercli plugins install ./plugins/dm-crypt-status --on-conflict replace --json"] }
