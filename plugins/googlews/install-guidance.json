{
  "plugin": "googlews",
  "binary": "googlews",
  "check": "googlews version",
  "install_steps": [
    "go install github.com/googleworkspace/cli@latest"
  ],
  "note": "Requires Google OAuth2 credentials. Run `googlews auth login` to authenticate."
}
