{
  "plugin": "spotctl",
  "binary": "spotctl",
  "check": "which spotctl",
  "install_steps": [
    "go install github.com/owenthereal/spotctl@latest",
    "Verify: spotctl --version",
    "supercli plugins install ./plugins/spotctl --on-conflict replace --json"
  ]
}