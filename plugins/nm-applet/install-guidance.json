{
  "plugin": "nm-applet",
  "binary": "nm-applet",
  "check": "which nm-applet",
  "install_steps": [
    "apt-get install nm-applet 2>/dev/null || which nm-applet",
    "Verify: nm-applet --version",
    "supercli plugins install ./plugins/nm-applet --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}