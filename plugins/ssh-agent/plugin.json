{
  "name": "ssh-agent",
  "version": "0.1.0",
  "description": "ssh-agent — SSH authentication agent",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "ssh-agent"
    }
  ],
  "install_guidance": {
    "plugin": "ssh-agent",
    "binary": "ssh-agent",
    "check": "which ssh-agent",
    "install_steps": [
      "base system"
    ],
    "note": "System utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "ssh-agent",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to ssh-agent",
      "adapter": "process",
      "adapterConfig": {
        "command": "ssh-agent",
        "passthrough": true,
        "missingDependencyHelp": "Install ssh-agent: base system"
      },
      "args": []
    }
  ]
}
