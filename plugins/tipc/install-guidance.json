{
  "plugin": "tipc",
  "binary": "tipc",
  "check": "which tipc",
  "install_steps": [
    "apt-get install tipc 2>/dev/null || which tipc",
    "Verify: tipc --version",
    "supercli plugins install ./plugins/tipc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}