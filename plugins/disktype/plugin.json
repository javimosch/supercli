{
  "name": "disktype",
  "version": "0.1.0",
  "description": "CLI tool: disktype",
  "source": "https://packages.debian.org/disktype",
  "checks": [{ "type": "binary", "name": "disktype" }],
  "install_guidance": {
    "plugin": "disktype", "binary": "disktype", "check": "which disktype",
    "install_steps": ["apt-get install disktype 2>/dev/null || which disktype", "Verify: disktype --version", "supercli plugins install ./plugins/disktype --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "disktype", "resource": "_", "action": "_",
    "description": "Passthrough to disktype CLI",
    "adapter": "process", "adapterConfig": { "command": "disktype", "passthrough": true, "missingDependencyHelp": "Install disktype: apt-get install disktype" }, "args": []
  }]
}
