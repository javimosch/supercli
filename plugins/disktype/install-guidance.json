{ "plugin": "disktype", "binary": "disktype", "check": "which disktype", "install_steps": ["apt-get install disktype 2>/dev/null || which disktype", "Verify: disktype --version", "supercli plugins install ./plugins/disktype --on-conflict replace --json"] }
