{
  "plugin": "ssh",
  "binary": "ssh",
  "check": "which ssh",
  "install_steps": [
    "apt-get install openssh-client",
    "Verify: ssh -V",
    "supercli plugins install ./plugins/ssh --on-conflict replace --json"
  ]
}
