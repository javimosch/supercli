{
  "plugin": "mani",
  "binary": "mani",
  "check": "which mani",
  "install_steps": [
    "brew install mani",
    "Verify: mani --version",
    "supercli plugins install ./plugins/mani --on-conflict replace --json"
  ]
}