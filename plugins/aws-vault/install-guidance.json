{
  "plugin": "aws-vault",
  "binary": "aws-vault",
  "check": "which aws-vault",
  "install_steps": [
    "brew install aws-vault",
    "Verify: aws-vault --version",
    "supercli plugins install ./plugins/aws-vault --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Stores AWS credentials in OS-native keystores (macOS Keychain, Windows Credential Manager, Linux Secret Service). Requires AWS CLI for some operations."
}
