{
  "name": "rpm",
  "version": "0.1.0",
  "description": "RPM — Red Hat Package Manager for installing and managing packages",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "rpm"
    }
  ],
  "install_guidance": {
    "plugin": "rpm",
    "binary": "rpm",
    "check": "which rpm",
    "install_steps": [
      "brew install rpm"
    ],
    "note": "Utility tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "rpm",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to rpm",
      "adapter": "process",
      "adapterConfig": {
        "command": "rpm",
        "passthrough": true,
        "missingDependencyHelp": "Install rpm: brew install rpm"
      },
      "args": []
    }
  ]
}
