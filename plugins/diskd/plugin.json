{
  "name": "diskd",
  "version": "0.1.0",
  "description": "CLI tool: diskd",
  "source": "https://packages.debian.org/diskd",
  "checks": [{ "type": "binary", "name": "diskd" }],
  "install_guidance": {
    "plugin": "diskd", "binary": "diskd", "check": "which diskd",
    "install_steps": ["apt-get install diskd 2>/dev/null || which diskd", "Verify: diskd --version", "supercli plugins install ./plugins/diskd --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "diskd", "resource": "_", "action": "_",
    "description": "Passthrough to diskd CLI",
    "adapter": "process", "adapterConfig": { "command": "diskd", "passthrough": true, "missingDependencyHelp": "Install diskd: apt-get install diskd" }, "args": []
  }]
}
