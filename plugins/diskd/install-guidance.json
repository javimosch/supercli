{ "plugin": "diskd", "binary": "diskd", "check": "which diskd", "install_steps": ["apt-get install diskd 2>/dev/null || which diskd", "Verify: diskd --version", "supercli plugins install ./plugins/diskd --on-conflict replace --json"] }
