{
  "name": "debconf-escape",
  "version": "1.0.0",
  "description": "debconf-escape \u2014 debconf-escape \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "debconf-escape"
    }
  ],
  "install_guidance": {
    "plugin": "debconf-escape",
    "binary": "debconf-escape",
    "check": "which debconf-escape",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "debconf-escape",
      "resource": "run",
      "action": "exec",
      "description": "debconf-escape \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "debconf-escape",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}