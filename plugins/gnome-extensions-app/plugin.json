{
  "name": "gnome-extensions-app",
  "version": "0.1.0",
  "description": "CLI tool: gnome-extensions-app",
  "source": "https://manpages.debian.org/gnome-extensions-app",
  "checks": [
    {
      "type": "binary",
      "name": "gnome-extensions-app"
    }
  ],
  "install_guidance": {
    "plugin": "gnome-extensions-app",
    "binary": "gnome-extensions-app",
    "check": "which gnome-extensions-app",
    "install_steps": [
      "apt-get install gnome-extensions-app 2>/dev/null || which gnome-extensions-app",
      "Verify: gnome-extensions-app --version",
      "supercli plugins install ./plugins/gnome-extensions-app --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "gnome-extensions-app",
      "resource": "self",
      "action": "version",
      "description": "Print gnome-extensions-app version",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-extensions-app",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install gnome-extensions-app: apt-get install gnome-extensions-app 2>/dev/null || which gnome-extensions-app"
      },
      "args": []
    },
    {
      "namespace": "gnome-extensions-app",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to gnome-extensions-app CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-extensions-app",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install gnome-extensions-app: apt-get install gnome-extensions-app 2>/dev/null || which gnome-extensions-app"
      },
      "args": []
    }
  ]
}