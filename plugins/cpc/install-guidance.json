{
  "plugin": "cpc",
  "binary": "cpc",
  "check": "which cpc",
  "install_steps": [
    "cargo install cpc",
    "Verify: cpc --help",
    "supercli plugins install ./plugins/cpc --on-conflict replace --json"
  ],
  "note": "cpc provides unit-aware calculator functionality"
}
