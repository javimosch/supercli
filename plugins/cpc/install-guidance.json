{
  "plugin": "cpc",
  "binary": "cpc",
  "check": "which cpc",
  "install_steps": ["cargo install cpc", "Verify: cpc --version", "supercli plugins install ./plugins/cpc --on-conflict replace --json"]
}
