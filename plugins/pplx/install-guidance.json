{
  "plugin": "pplx",
  "binary": "pplx",
  "check": "which pplx",
  "install_steps": [
    "brew install pplx",
    "Verify: pplx --version",
    "supercli plugins install ./plugins/pplx --on-conflict replace --json"
  ]
}