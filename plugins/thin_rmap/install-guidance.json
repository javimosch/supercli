{
  "plugin": "thin_rmap",
  "binary": "thin_rmap",
  "check": "which thin_rmap",
  "install_steps": [
    "apt-get install thin_rmap 2>/dev/null || which thin_rmap",
    "Verify: thin_rmap --version",
    "supercli plugins install ./plugins/thin_rmap --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}