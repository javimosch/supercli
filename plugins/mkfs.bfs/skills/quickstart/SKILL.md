---
name: mkfs.bfs
description: Use this skill when the user wants to use mkfs.bfs, cli tool: mkfs.bfs.
---

# mkfs.bfs Plugin

CLI tool: mkfs.bfs.

## Commands
- `mkfs.bfs <resource> <action>` — Execute mkfs.bfs commands
- `mkfs.bfs self version` — Print mkfs.bfs version
- `mkfs.bfs _ _` — Passthrough to mkfs.bfs CLI

## Usage Examples
- "mkfs.bfs --help"
- "mkfs.bfs self version"

## Installation
```bash
apt-get install mkfs.bfs 2>/dev/null || which mkfs.bfs
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
