{
  "plugin": "mkfs.bfs",
  "binary": "mkfs.bfs",
  "check": "which mkfs.bfs",
  "install_steps": [
    "apt-get install mkfs.bfs 2>/dev/null || which mkfs.bfs",
    "Verify: mkfs.bfs --version",
    "supercli plugins install ./plugins/mkfs.bfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}