{
  "plugin": "xzegrep",
  "binary": "xzegrep",
  "check": "which xzegrep",
  "install_steps": [
    "apt-get install xzegrep 2>/dev/null || which xzegrep",
    "Verify: xzegrep --version",
    "supercli plugins install ./plugins/xzegrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}