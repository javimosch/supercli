{
  "plugin": "drawio-cli",
  "binary": "drawio-cli",
  "check": "which drawio-cli",
  "install_steps": ["npm install -g drawio-cli", "Verify: drawio-cli --version", "supercli plugins install ./plugins/drawio-cli --on-conflict replace --json"]
}
