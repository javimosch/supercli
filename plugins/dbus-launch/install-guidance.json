{ "plugin": "dbus-launch", "binary": "dbus-launch", "check": "which dbus-launch", "install_steps": ["apt-get install dbus-launch 2>/dev/null || which dbus-launch", "Verify: dbus-launch --version", "supercli plugins install ./plugins/dbus-launch --on-conflict replace --json"] }
