{
  "plugin": "lsns",
  "binary": "lsns",
  "check": "which lsns",
  "install_steps": [
    "apt-get install lsns 2>/dev/null || which lsns",
    "Verify: lsns --version",
    "supercli plugins install ./plugins/lsns --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}