{
  "plugin": "ntfstruncate",
  "binary": "ntfstruncate",
  "check": "which ntfstruncate",
  "install_steps": [
    "# Install ntfstruncate",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/ntfstruncate-linux-amd64 -o /usr/local/bin/ntfstruncate",
    "chmod +x /usr/local/bin/ntfstruncate",
    "# Or via package manager, see /usr/bin/ntfstruncate"
  ]
}