{
  "plugin": "localc",
  "binary": "localc",
  "check": "which localc",
  "install_steps": [
    "apt-get install localc 2>/dev/null || which localc",
    "Verify: localc --version",
    "supercli plugins install ./plugins/localc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}