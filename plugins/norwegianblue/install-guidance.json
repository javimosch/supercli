{ "plugin": "norwegianblue", "binary": "norwegianblue", "check": "which norwegianblue", "install_steps": ["pipx install norwegianblue", "Verify: norwegianblue --version", "supercli plugins install ./plugins/norwegianblue --on-conflict replace --json"], "note": "Uses the endoflife.date API (no auth required). Perfect for CI/CD pipelines to check if dependencies are approaching EOL." }
