{
  "plugin": "rarp",
  "binary": "rarp",
  "check": "which rarp",
  "install_steps": [
    "apt-get install rarp 2>/dev/null || which rarp",
    "Verify: rarp --version",
    "supercli plugins install ./plugins/rarp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}