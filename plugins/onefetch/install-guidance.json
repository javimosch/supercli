{
  "plugin": "onefetch",
  "binary": "onefetch",
  "check": "which onefetch",
  "install_steps": [
    "brew install onefetch",
    "Verify: onefetch --version",
    "supercli plugins install ./plugins/onefetch --on-conflict replace --json"
  ],
  "note": "Also available via cargo (cargo install onefetch), scoop, winget, or prebuilt binaries from GitHub Releases."
}
