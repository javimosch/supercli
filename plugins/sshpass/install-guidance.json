{
  "plugin": "sshpass",
  "binary": "sshpass",
  "check": "which sshpass",
  "install_steps": [
    "apt-get install sshpass",
    "Verify: sshpass -h",
    "supercli plugins install ./plugins/sshpass --on-conflict replace --json"
  ]
}
