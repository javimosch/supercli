{
  "plugin": "kapp",
  "binary": "kapp",
  "check": "which kapp",
  "install_steps": ["brew install kapp", "Verify: kapp --version", "supercli plugins install ./plugins/kapp --on-conflict replace --json"]
}
