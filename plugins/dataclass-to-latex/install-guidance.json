{ "plugin": "dataclass-to-latex", "binary": "dataclass-to-latex", "check": "which dataclass-to-latex", "install_steps": ["apt-get install dataclass-to-latex 2>/dev/null || which dataclass-to-latex", "Verify: dataclass-to-latex --version", "supercli plugins install ./plugins/dataclass-to-latex --on-conflict replace --json"] }
