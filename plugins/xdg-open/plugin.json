{
  "name": "xdg-open",
  "version": "0.1.0",
  "description": "CLI tool: xdg-open",
  "source": "https://manpages.debian.org/xdg-open",
  "checks": [
    {
      "type": "binary",
      "name": "xdg-open"
    }
  ],
  "install_guidance": {
    "plugin": "xdg-open",
    "binary": "xdg-open",
    "check": "which xdg-open",
    "install_steps": [
      "apt-get install xdg-open 2>/dev/null || which xdg-open",
      "Verify: xdg-open --version",
      "supercli plugins install ./plugins/xdg-open --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "xdg-open",
      "resource": "self",
      "action": "version",
      "description": "Print xdg-open version",
      "adapter": "process",
      "adapterConfig": {
        "command": "xdg-open",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install xdg-open: apt-get install xdg-open 2>/dev/null || which xdg-open"
      },
      "args": []
    },
    {
      "namespace": "xdg-open",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to xdg-open CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "xdg-open",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install xdg-open: apt-get install xdg-open 2>/dev/null || which xdg-open"
      },
      "args": []
    }
  ]
}