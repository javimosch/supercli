{ "plugin": "dm-crypt", "binary": "dm-crypt", "check": "which dm-crypt", "install_steps": ["apt-get install dm-crypt 2>/dev/null || which dm-crypt", "Verify: dm-crypt --version", "supercli plugins install ./plugins/dm-crypt --on-conflict replace --json"] }
