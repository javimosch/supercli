{
  "name": "dm-crypt",
  "version": "0.1.0",
  "description": "CLI tool: dm-crypt",
  "source": "https://packages.debian.org/dm-crypt",
  "checks": [{ "type": "binary", "name": "dm-crypt" }],
  "install_guidance": {
    "plugin": "dm-crypt", "binary": "dm-crypt", "check": "which dm-crypt",
    "install_steps": ["apt-get install dm-crypt 2>/dev/null || which dm-crypt", "Verify: dm-crypt --version", "supercli plugins install ./plugins/dm-crypt --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-crypt", "resource": "_", "action": "_",
    "description": "Passthrough to dm-crypt CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-crypt", "passthrough": true, "missingDependencyHelp": "Install dm-crypt: apt-get install dm-crypt" }, "args": []
  }]
}
