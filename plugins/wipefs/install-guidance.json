{
  "plugin": "wipefs",
  "binary": "wipefs",
  "check": "which wipefs",
  "install_steps": [
    "apt-get install util-linux",
    "Verify: wipefs --version",
    "supercli plugins install ./plugins/wipefs --on-conflict replace --json"
  ]
}
