{
  "plugin": "sloc-guard",
  "binary": "sloc-guard",
  "check": "sloc-guard --version",
  "install_steps": [
    "cargo install sloc-guard",
    "Verify: sloc-guard --version",
    "sloc-guard init --detect (for initial setup)",
    "supercli plugins install ./plugins/sloc-guard --on-conflict replace --json"
  ],
  "note": "Requires Rust toolchain. Creates .sloc-guard.toml config file. Default 600-line limit per file. Use --diff for CI integration, --baseline for grandfathering existing violations. Perfect for AI-assisted development to enforce code quality constraints."
}