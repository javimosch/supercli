{
  "plugin": "live-server",
  "binary": "live-server",
  "check": "which live-server",
  "install_steps": [
    "npm install -g live-server",
    "Verify: live-server --version",
    "supercli plugins install ./plugins/live-server --on-conflict replace --json"
  ]
}
