{ "plugin": "apturl", "binary": "apturl", "check": "which apturl", "install_steps": ["apt-get install apturl 2>/dev/null || which apturl", "Verify: apturl --version", "supercli plugins install ./plugins/apturl --on-conflict replace --json"] }
