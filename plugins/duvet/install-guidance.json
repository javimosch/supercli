{
  "plugin": "duvet",
  "binary": "duvet",
  "check": "which duvet",
  "install_steps": ["cargo install duvet", "Verify: duvet --version", "supercli plugins install ./plugins/duvet --on-conflict replace --json"]
}
