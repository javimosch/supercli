{
  "plugin": "terrascan",
  "binary": "terrascan",
  "check": "which terrascan",
  "install_steps": ["brew install terrascan", "Verify: terrascan --version", "supercli plugins install ./plugins/terrascan --on-conflict replace --json"]
}
