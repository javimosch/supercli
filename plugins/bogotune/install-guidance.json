{ "plugin": "bogotune", "binary": "bogotune", "check": "which bogotune", "install_steps": ["apt-get install bogotune 2>/dev/null || which bogotune", "Verify: bogotune --version", "supercli plugins install ./plugins/bogotune --on-conflict replace --json"] }
