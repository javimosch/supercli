{
  "plugin": "infracost",
  "binary": "infracost",
  "check": "which infracost",
  "install_steps": [
    "brew install infracost",
    "curl -fsSL https://raw.githubusercontent.com/infracost/infracost/master/scripts/install.sh | sh",
    "infracost register"
  ],
  "note": "Requires free API key via 'infracost register'. Supports Terraform, Terragrunt, and OpenTofu. Integrates with CI/CD via GitHub Actions, GitLab CI, Atlantis, and more."
}
