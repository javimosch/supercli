{
  "plugin": "infracost",
  "binary": "infracost",
  "check": "which infracost",
  "install_steps": ["brew install infracost", "Verify: infracost --version", "supercli plugins install ./plugins/infracost --on-conflict replace --json"]
}
