{
  "plugin": "pretext",
  "binary": "pretext",
  "check": "which pretext",
  "install_steps": [
    "npm install pretext",
    "Verify: pretext --version",
    "supercli plugins install ./plugins/pretext --on-conflict replace --json"
  ],
  "note": "High-performance text measurement and layout computation."
}
