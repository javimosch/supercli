{
  "plugin": "pretext",
  "binary": "pretext",
  "check": "which pretext",
  "install_steps": [
    "pip install pretext",
    "Verify: pretext --version",
    "supercli plugins install ./plugins/pretext --on-conflict replace --json"
  ],
  "note": "Language: TypeScript. Stars: 48108"
}