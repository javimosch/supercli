{
  "name": "ddrescuelog",
  "version": "0.1.0",
  "description": "CLI tool: ddrescuelog",
  "source": "https://packages.debian.org/ddrescuelog",
  "checks": [{ "type": "binary", "name": "ddrescuelog" }],
  "install_guidance": {
    "plugin": "ddrescuelog", "binary": "ddrescuelog", "check": "which ddrescuelog",
    "install_steps": ["apt-get install ddrescuelog 2>/dev/null || which ddrescuelog", "Verify: ddrescuelog --version", "supercli plugins install ./plugins/ddrescuelog --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ddrescuelog", "resource": "_", "action": "_",
    "description": "Passthrough to ddrescuelog CLI",
    "adapter": "process", "adapterConfig": { "command": "ddrescuelog", "passthrough": true, "missingDependencyHelp": "Install ddrescuelog: apt-get install ddrescuelog" }, "args": []
  }]
}
