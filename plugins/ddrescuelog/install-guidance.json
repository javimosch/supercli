{ "plugin": "ddrescuelog", "binary": "ddrescuelog", "check": "which ddrescuelog", "install_steps": ["apt-get install ddrescuelog 2>/dev/null || which ddrescuelog", "Verify: ddrescuelog --version", "supercli plugins install ./plugins/ddrescuelog --on-conflict replace --json"] }
