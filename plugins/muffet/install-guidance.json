{
  "plugin": "muffet",
  "binary": "muffet",
  "check": "which muffet",
  "install_steps": [
    "brew install muffet",
    "Verify: muffet --version",
    "supercli plugins install ./plugins/muffet --on-conflict replace --json"
  ]
}