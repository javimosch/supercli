{
  "plugin": "cosign",
  "binary": "cosign",
  "check": "which cosign",
  "install_steps": [
    "brew install cosign",
    "Verify: cosign --version",
    "supercli plugins install ./plugins/cosign --on-conflict replace --json"
  ]
}