{
  "plugin": "clip",
  "binary": "clip",
  "check": "which clip",
  "install_steps": [
    "brew install clip",
    "Verify: clip --version",
    "supercli plugins install ./plugins/clip --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}