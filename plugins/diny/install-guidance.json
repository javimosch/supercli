{
  "plugin": "diny",
  "binary": "diny",
  "check": "which diny",
  "install_steps": [
    "go install github.com/dinoDanic/diny@latest",
    "Verify: diny --version",
    "supercli plugins install ./plugins/diny --on-conflict replace --json"
  ],
  "note": "Requires git repository with staged changes"
}
