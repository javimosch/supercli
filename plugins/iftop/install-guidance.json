{
  "plugin": "iftop",
  "binary": "iftop",
  "check": "which iftop",
  "install_steps": [
    "brew install iftop",
    "Verify: iftop --version",
    "supercli plugins install ./plugins/iftop --on-conflict replace --json"
  ]
}
