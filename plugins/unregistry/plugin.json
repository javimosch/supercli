{
  "name": "unregistry",
  "version": "0.1.0",
  "description": "Push Docker images to remote hosts via SSH without a registry",
  "source": "https://github.com/psviderski/unregistry",
  "checks": [
    {
      "type": "binary",
      "name": "unregistry"
    }
  ],
  "install_guidance": {
    "plugin": "unregistry",
    "binary": "unregistry",
    "check": "which unregistry",
    "install_steps": [
      "go install github.com/psviderski/unregistry@latest",
      "Verify: unregistry --version",
      "supercli plugins install ./plugins/unregistry --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "unregistry",
      "resource": "self",
      "action": "version",
      "description": "Print unregistry version",
      "adapter": "process",
      "adapterConfig": {
        "command": "unregistry",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install unregistry: go install github.com/psviderski/unregistry@latest"
      },
      "args": []
    },
    {
      "namespace": "unregistry",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to unregistry CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "unregistry",
        "passthrough": true,
        "missingDependencyHelp": "Install unregistry: go install github.com/psviderski/unregistry@latest"
      },
      "args": []
    }
  ]
}
