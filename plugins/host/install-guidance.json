{
  "plugin": "host",
  "binary": "host",
  "check": "which host",
  "install_steps": [
    "brew install bind",
    "Verify: host --version",
    "supercli plugins install ./plugins/host --on-conflict replace --json"
  ]
}
