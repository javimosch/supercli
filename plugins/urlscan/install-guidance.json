{
  "plugin": "urlscan",
  "binary": "urlscan",
  "check": "which urlscan",
  "install_steps": [
    "pip install urlscan",
    "Verify: urlscan --version",
    "supercli plugins install ./plugins/urlscan --on-conflict replace --json"
  ]
}
