{
  "plugin": "git-revise",
  "binary": "git-revise",
  "check": "which git-revise",
  "install_steps": ["pip install git-revise", "Verify: git-revise --version", "supercli plugins install ./plugins/git-revise --on-conflict replace --json"]
}
