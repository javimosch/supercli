---
name: vite-plus
description: Use this skill when the user wants to use vite-plus for Vite+ is the unified toolchain and entry point for web development. It manages y.
---

# vite-plus Plugin

Vite+ is the unified toolchain and entry point for web development. It manages your runtime, package manager, and frontend toolchain in one place.

## Commands

### General
- `vite-plus self version` — Print vite-plus version
- `vite-plus _ _` — Passthrough to vite-plus CLI

## Usage Examples
- "Run vite-plus"
- "Use vite-plus to Vite+ is the unified toolchain and entry point for web devel"

## Installation

```bash
pip install vite-plus
```

## Key Features
- Vite+ is the unified toolchain and entry point for web development. It manages your runtime, package
- CLI tool, headless and scriptable
