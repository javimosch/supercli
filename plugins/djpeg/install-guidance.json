{ "plugin": "djpeg", "binary": "djpeg", "check": "which djpeg", "install_steps": ["apt-get install djpeg 2>/dev/null || which djpeg", "Verify: djpeg --version", "supercli plugins install ./plugins/djpeg --on-conflict replace --json"] }
