{
  "name": "mv",
  "version": "0.1.0",
  "description": "mv — Move or rename files",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "mv"
    }
  ],
  "install_guidance": {
    "plugin": "mv",
    "binary": "mv",
    "check": "which mv",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/mv --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "mv",
      "resource": "self",
      "action": "version",
      "description": "Print mv version",
      "adapter": "process",
      "adapterConfig": {
        "command": "mv",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install mv: base system"
      },
      "args": []
    },
    {
      "namespace": "mv",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to mv CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "mv",
        "passthrough": true,
        "missingDependencyHelp": "Install mv: base system"
      },
      "args": []
    }
  ]
}
