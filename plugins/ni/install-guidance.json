{
  "plugin": "ni",
  "binary": "ni",
  "check": "which ni",
  "install_steps": [
    "npm install -g @antfu/ni",
    "Verify: ni --version",
    "supercli plugins install ./plugins/ni --on-conflict replace --json"
  ]
}