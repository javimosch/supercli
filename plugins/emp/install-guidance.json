{
  "plugin": "emp",
  "binary": "emp",
  "check": "which emp",
  "install_steps": [
    "brew install emp",
    "Verify: emp --version",
    "supercli plugins install ./plugins/emp --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}