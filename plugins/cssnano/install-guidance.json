{
  "plugin": "cssnano",
  "binary": "cssnano",
  "check": "which cssnano",
  "install_steps": [
    "npm install -g cssnano",
    "Verify: cssnano --version",
    "supercli plugins install ./plugins/cssnano --on-conflict replace --json"
  ]
}
