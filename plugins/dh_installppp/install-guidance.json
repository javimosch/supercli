{ "plugin": "dh_installppp", "binary": "dh_installppp", "check": "which dh_installppp", "install_steps": ["apt-get install dh_installppp 2>/dev/null || which dh_installppp", "Verify: dh_installppp --version", "supercli plugins install ./plugins/dh_installppp --on-conflict replace --json"] }
