{
  "plugin": "task",
  "binary": "task",
  "check": "which task",
  "install_steps": [
    "brew install go-task/tap/go-task",
    "Verify: task --version",
    "supercli plugins install ./plugins/task --on-conflict replace --json"
  ]
}