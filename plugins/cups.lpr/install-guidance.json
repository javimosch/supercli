{
  "plugin": "cups.lpr",
  "binary": "cups.lpr",
  "check": "which cups.lpr",
  "install_steps": [
    "apt-get install cups.lpr 2>/dev/null || which cups.lpr",
    "Verify: cups.lpr --version",
    "supercli plugins install ./plugins/cups.lpr --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}