{
  "plugin": "update-leap",
  "binary": "update-leap",
  "check": "which update-leap",
  "install_steps": [
    "apt-get install update-leap 2>/dev/null || which update-leap",
    "Verify: update-leap --version",
    "supercli plugins install ./plugins/update-leap --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}