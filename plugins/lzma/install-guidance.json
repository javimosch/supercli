{
  "plugin": "lzma",
  "binary": "lzma",
  "check": "which lzma",
  "install_steps": [
    "apt-get install lzma 2>/dev/null || which lzma",
    "Verify: lzma --version",
    "supercli plugins install ./plugins/lzma --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}