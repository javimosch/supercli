{
  "plugin": "cava",
  "binary": "cava",
  "check": "which cava",
  "install_steps": [
    "brew install cava",
    "Verify: cava --version",
    "supercli plugins install ./plugins/cava --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}