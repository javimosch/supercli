{
  "plugin": "pdftops",
  "binary": "pdftops",
  "check": "which pdftops",
  "install_steps": [
    "apt-get install pdftops 2>/dev/null || which pdftops",
    "Verify: pdftops --version",
    "supercli plugins install ./plugins/pdftops --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}