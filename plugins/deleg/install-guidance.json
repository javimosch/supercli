{ "plugin": "deleg", "binary": "deleg", "check": "which deleg", "install_steps": ["apt-get install deleg 2>/dev/null || which deleg", "Verify: deleg --version", "supercli plugins install ./plugins/deleg --on-conflict replace --json"] }
