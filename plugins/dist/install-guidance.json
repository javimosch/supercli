{ "plugin": "dist", "binary": "dist", "check": "which dist", "install_steps": ["apt-get install dist 2>/dev/null || which dist", "Verify: dist --version", "supercli plugins install ./plugins/dist --on-conflict replace --json"] }
