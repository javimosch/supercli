{
  "name": "cupsd",
  "version": "0.1.0",
  "description": "CLI tool: cupsd",
  "source": "https://packages.debian.org/cupsd",
  "checks": [{ "type": "binary", "name": "cupsd" }],
  "install_guidance": {
    "plugin": "cupsd", "binary": "cupsd", "check": "which cupsd",
    "install_steps": ["apt-get install cupsd 2>/dev/null || which cupsd", "Verify: cupsd --version", "supercli plugins install ./plugins/cupsd --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cupsd", "resource": "_", "action": "_",
    "description": "Passthrough to cupsd CLI",
    "adapter": "process", "adapterConfig": { "command": "cupsd", "passthrough": true, "missingDependencyHelp": "Install cupsd: apt-get install cupsd" }, "args": []
  }]
}
