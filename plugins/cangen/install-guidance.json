{ "plugin": "cangen", "binary": "cangen", "check": "which cangen", "install_steps": ["apt-get install cangen 2>/dev/null || which cangen", "Verify: cangen --version", "supercli plugins install ./plugins/cangen --on-conflict replace --json"] }
