{
  "name": "cangen",
  "version": "0.1.0",
  "description": "CLI tool: cangen",
  "source": "https://packages.debian.org/cangen",
  "checks": [{ "type": "binary", "name": "cangen" }],
  "install_guidance": {
    "plugin": "cangen", "binary": "cangen", "check": "which cangen",
    "install_steps": ["apt-get install cangen 2>/dev/null || which cangen", "Verify: cangen --version", "supercli plugins install ./plugins/cangen --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cangen", "resource": "_", "action": "_",
    "description": "Passthrough to cangen CLI",
    "adapter": "process", "adapterConfig": { "command": "cangen", "passthrough": true, "missingDependencyHelp": "Install cangen: apt-get install cangen" }, "args": []
  }]
}
