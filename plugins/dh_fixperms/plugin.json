{
  "name": "dh_fixperms",
  "version": "0.1.0",
  "description": "CLI tool: dh_fixperms",
  "source": "https://packages.debian.org/dh_fixperms",
  "checks": [{ "type": "binary", "name": "dh_fixperms" }],
  "install_guidance": {
    "plugin": "dh_fixperms", "binary": "dh_fixperms", "check": "which dh_fixperms",
    "install_steps": ["apt-get install dh_fixperms 2>/dev/null || which dh_fixperms", "Verify: dh_fixperms --version", "supercli plugins install ./plugins/dh_fixperms --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_fixperms", "resource": "_", "action": "_",
    "description": "Passthrough to dh_fixperms CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_fixperms", "passthrough": true, "missingDependencyHelp": "Install dh_fixperms: apt-get install dh_fixperms" }, "args": []
  }]
}
