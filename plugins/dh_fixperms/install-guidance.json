{ "plugin": "dh_fixperms", "binary": "dh_fixperms", "check": "which dh_fixperms", "install_steps": ["apt-get install dh_fixperms 2>/dev/null || which dh_fixperms", "Verify: dh_fixperms --version", "supercli plugins install ./plugins/dh_fixperms --on-conflict replace --json"] }
