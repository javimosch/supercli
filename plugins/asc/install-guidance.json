{
  "plugin": "asc",
  "binary": "asc",
  "check": "which asc",
  "install_steps": [
    "brew install asc",
    "Verify: asc --version",
    "supercli plugins install ./plugins/asc --on-conflict replace --json"
  ],
  "note": "Also available via install script: curl -fsSL https://asccli.sh/install | bash. Windows users can download signed binaries from GitHub releases. API keys must be generated at https://appstoreconnect.apple.com/access/integrations/api. Use --bypass-keychain for CI environments."
}
