{
  "plugin": "bump2version",
  "binary": "bump2version",
  "check": "which bump2version",
  "install_steps": ["pip install bump2version", "Verify: bump2version --version", "supercli plugins install ./plugins/bump2version --on-conflict replace --json"]
}
