{
  "plugin": "teller",
  "binary": "teller",
  "check": "which teller",
  "install_steps": ["brew install teller", "Verify: teller --version", "supercli plugins install ./plugins/teller --on-conflict replace --json"]
}
