{ "plugin": "assistant", "binary": "assistant", "check": "which assistant", "install_steps": ["apt-get install assistant 2>/dev/null || which assistant", "Verify: assistant --version", "supercli plugins install ./plugins/assistant --on-conflict replace --json"] }
