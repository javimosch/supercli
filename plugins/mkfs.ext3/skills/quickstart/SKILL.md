---
name: mkfs.ext3
description: Use this skill when the user wants to use mkfs.ext3, cli tool: mkfs.ext3.
---

# mkfs.ext3 Plugin

CLI tool: mkfs.ext3.

## Commands
- `mkfs.ext3 <resource> <action>` — Execute mkfs.ext3 commands
- `mkfs.ext3 self version` — Print mkfs.ext3 version
- `mkfs.ext3 _ _` — Passthrough to mkfs.ext3 CLI

## Usage Examples
- "mkfs.ext3 --help"
- "mkfs.ext3 self version"

## Installation
```bash
apt-get install mkfs.ext3 2>/dev/null || which mkfs.ext3
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
