{
  "plugin": "mkfs.ext3",
  "binary": "mkfs.ext3",
  "check": "which mkfs.ext3",
  "install_steps": [
    "apt-get install mkfs.ext3 2>/dev/null || which mkfs.ext3",
    "Verify: mkfs.ext3 --version",
    "supercli plugins install ./plugins/mkfs.ext3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}