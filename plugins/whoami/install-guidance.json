{
  "plugin": "whoami",
  "binary": "whoami",
  "check": "which whoami",
  "install_steps": [
    "apt-get install whoami 2>/dev/null || which whoami",
    "Verify: whoami --version",
    "supercli plugins install ./plugins/whoami --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}