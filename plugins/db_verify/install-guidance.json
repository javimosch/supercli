{ "plugin": "db_verify", "binary": "db_verify", "check": "which db_verify", "install_steps": ["apt-get install db_verify 2>/dev/null || which db_verify", "Verify: db_verify --version", "supercli plugins install ./plugins/db_verify --on-conflict replace --json"] }
