{
  "plugin": "mmroff",
  "binary": "mmroff",
  "check": "which mmroff",
  "install_steps": [
    "apt-get install mmroff 2>/dev/null || which mmroff",
    "Verify: mmroff --version",
    "supercli plugins install ./plugins/mmroff --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}