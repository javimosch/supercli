{
  "name": "chromedriver",
  "version": "0.1.0",
  "description": "CLI tool: chromedriver",
  "source": "https://packages.debian.org/chromedriver",
  "checks": [{ "type": "binary", "name": "chromedriver" }],
  "install_guidance": {
    "plugin": "chromedriver", "binary": "chromedriver", "check": "which chromedriver",
    "install_steps": ["apt-get install chromedriver 2>/dev/null || which chromedriver", "Verify: chromedriver --version", "supercli plugins install ./plugins/chromedriver --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "chromedriver", "resource": "_", "action": "_",
    "description": "Passthrough to chromedriver CLI",
    "adapter": "process", "adapterConfig": { "command": "chromedriver", "passthrough": true, "missingDependencyHelp": "Install chromedriver: apt-get install chromedriver" }, "args": []
  }]
}
