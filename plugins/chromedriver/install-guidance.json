{ "plugin": "chromedriver", "binary": "chromedriver", "check": "which chromedriver", "install_steps": ["apt-get install chromedriver 2>/dev/null || which chromedriver", "Verify: chromedriver --version", "supercli plugins install ./plugins/chromedriver --on-conflict replace --json"] }
