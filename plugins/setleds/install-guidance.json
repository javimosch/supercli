{
  "plugin": "setleds",
  "binary": "setleds",
  "check": "which setleds",
  "install_steps": [
    "apt-get install setleds 2>/dev/null || which setleds",
    "Verify: setleds --version",
    "supercli plugins install ./plugins/setleds --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}