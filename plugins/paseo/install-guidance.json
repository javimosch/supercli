{
  "plugin": "paseo",
  "binary": "paseo",
  "check": "paseo --version 2>&1 | head -3",
  "install_steps": [
    "npm install -g @getpaseo/cli",
    "Daemon: sc paseo daemon start",
    "Run agent: sc paseo agent run --provider codex/gpt-5.4 \"implement feature\"",
    "Skills available via: sc skills search <query> --provider paseo"
  ],
  "note": "Proprietary license. Requires npm install. 500+ dependencies (~2 min install)."
}
