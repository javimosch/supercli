{
  "plugin": "tlrc",
  "binary": "tldr",
  "check": "which tldr || echo 'tldr (tlrc) not installed'",
  "install_steps": [
    "brew install tlrc",
    "Verify: tldr --version",
    "supercli plugins install ./plugins/tlrc --on-conflict replace --json"
  ],
  "note": "Also available via: cargo install tlrc --locked, or download binaries from https://github.com/tldr-pages/tlrc/releases"
}
