{ "plugin": "boxcli", "binary": "boxcli", "check": "which boxcli", "install_steps": ["apt-get install boxcli 2>/dev/null || which boxcli", "Verify: boxcli --version", "supercli plugins install ./plugins/boxcli --on-conflict replace --json"] }
