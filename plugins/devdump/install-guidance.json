{ "plugin": "devdump", "binary": "devdump", "check": "which devdump", "install_steps": ["apt-get install devdump 2>/dev/null || which devdump", "Verify: devdump --version", "supercli plugins install ./plugins/devdump --on-conflict replace --json"] }
