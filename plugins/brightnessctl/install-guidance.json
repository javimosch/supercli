{
  "plugin": "brightnessctl",
  "binary": "brightnessctl",
  "check": "which brightnessctl",
  "install_steps": [
    "brew install brightnessctl",
    "Verify: brightnessctl --version",
    "supercli plugins install ./plugins/brightnessctl --on-conflict replace --json"
  ]
}
