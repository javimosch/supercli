{
  "plugin": "gh-dash",
  "binary": "gh-dash",
  "check": "which gh-dash",
  "install_steps": ["gh extension install dlvhdr/gh-dash", "Verify: gh-dash --version", "supercli plugins install ./plugins/gh-dash --on-conflict replace --json"]
}
