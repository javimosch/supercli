{
  "plugin": "rook",
  "binary": "rook",
  "check": "which rook",
  "install_steps": ["brew install rook", "Verify: rook --version", "supercli plugins install ./plugins/rook --on-conflict replace --json"]
}
