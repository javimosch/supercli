{
  "plugin": "dee-contacts",
  "binary": "dee-contacts",
  "check": "which dee-contacts",
  "install_steps": ["cargo install dee-contacts", "Verify: dee-contacts --version", "supercli plugins install ./plugins/dee-contacts --on-conflict replace --json"]
}
