{
  "plugin": "detex",
  "binary": "detex",
  "check": "which detex",
  "install_steps": [
    "brew install detex",
    "Verify: detex --version",
    "supercli plugins install ./plugins/detex --on-conflict replace --json"
  ]
}
