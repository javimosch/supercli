{
  "name": "skipfish",
  "version": "1.0.0",
  "description": "skipfish \u2014 CLI CLI utility",
  "source": "https://github.com/search?q=skipfish+cli&type=repositories",
  "checks": [
    {
      "type": "binary",
      "name": "skipfish"
    }
  ],
  "install_guidance": {
    "plugin": "skipfish",
    "binary": "skipfish",
    "check": "which skipfish",
    "install_steps": [
      "Install skipfish via package manager: apt install skipfish or brew install skipfish"
    ]
  },
  "commands": [
    {
      "namespace": "skipfish",
      "resource": "run",
      "action": "exec",
      "description": "skipfish \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "skipfish",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install skipfish via your package manager"
      },
      "args": []
    }
  ]
}