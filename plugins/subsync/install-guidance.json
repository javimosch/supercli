{
  "plugin": "subsync",
  "binary": "subsync",
  "check": "which subsync",
  "install_steps": ["pip install subsync", "Verify: subsync --version", "supercli plugins install ./plugins/subsync --on-conflict replace --json"]
}
