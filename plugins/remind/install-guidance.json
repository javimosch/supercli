{
  "plugin": "remind",
  "binary": "remind",
  "check": "which remind",
  "install_steps": [
    "brew install remind",
    "Verify: remind --version",
    "supercli plugins install ./plugins/remind --on-conflict replace --json"
  ]
}
