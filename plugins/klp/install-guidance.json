{
  "plugin": "klp",
  "binary": "klp",
  "check": "which klp",
  "install_steps": [
    "pip install klp",
    "Verify: klp --version",
    "supercli plugins install ./plugins/klp --on-conflict replace --json"
  ]
}