{
  "plugin": "pw-metadata",
  "binary": "pw-metadata",
  "check": "which pw-metadata",
  "install_steps": [
    "apt-get install pw-metadata 2>/dev/null || which pw-metadata",
    "Verify: pw-metadata --version",
    "supercli plugins install ./plugins/pw-metadata --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}