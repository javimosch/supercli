{
  "name": "pw-metadata",
  "version": "0.1.0",
  "description": "CLI tool: pw-metadata",
  "source": "https://manpages.debian.org/pw-metadata",
  "checks": [
    {
      "type": "binary",
      "name": "pw-metadata"
    }
  ],
  "install_guidance": {
    "plugin": "pw-metadata",
    "binary": "pw-metadata",
    "check": "which pw-metadata",
    "install_steps": [
      "apt-get install pw-metadata 2>/dev/null || which pw-metadata",
      "Verify: pw-metadata --version",
      "supercli plugins install ./plugins/pw-metadata --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "pw-metadata",
      "resource": "self",
      "action": "version",
      "description": "Print pw-metadata version",
      "adapter": "process",
      "adapterConfig": {
        "command": "pw-metadata",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install pw-metadata: apt-get install pw-metadata 2>/dev/null || which pw-metadata"
      },
      "args": []
    },
    {
      "namespace": "pw-metadata",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to pw-metadata CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "pw-metadata",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install pw-metadata: apt-get install pw-metadata 2>/dev/null || which pw-metadata"
      },
      "args": []
    }
  ]
}