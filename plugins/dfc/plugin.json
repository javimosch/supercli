{
  "name": "dfc",
  "version": "0.1.0",
  "description": "CLI tool: dfc",
  "source": "https://packages.debian.org/dfc",
  "checks": [{ "type": "binary", "name": "dfc" }],
  "install_guidance": {
    "plugin": "dfc", "binary": "dfc", "check": "which dfc",
    "install_steps": ["apt-get install dfc 2>/dev/null || which dfc", "Verify: dfc --version", "supercli plugins install ./plugins/dfc --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dfc", "resource": "_", "action": "_",
    "description": "Passthrough to dfc CLI",
    "adapter": "process", "adapterConfig": { "command": "dfc", "passthrough": true, "missingDependencyHelp": "Install dfc: apt-get install dfc" }, "args": []
  }]
}
