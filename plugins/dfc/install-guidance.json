{ "plugin": "dfc", "binary": "dfc", "check": "which dfc", "install_steps": ["apt-get install dfc 2>/dev/null || which dfc", "Verify: dfc --version", "supercli plugins install ./plugins/dfc --on-conflict replace --json"] }
