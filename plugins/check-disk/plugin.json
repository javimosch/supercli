{
  "name": "check-disk",
  "version": "0.1.0",
  "description": "CLI tool: check-disk",
  "source": "https://packages.debian.org/check-disk",
  "checks": [{ "type": "binary", "name": "check-disk" }],
  "install_guidance": {
    "plugin": "check-disk", "binary": "check-disk", "check": "which check-disk",
    "install_steps": ["apt-get install check-disk 2>/dev/null || which check-disk", "Verify: check-disk --version", "supercli plugins install ./plugins/check-disk --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check-disk", "resource": "_", "action": "_",
    "description": "Passthrough to check-disk CLI",
    "adapter": "process", "adapterConfig": { "command": "check-disk", "passthrough": true, "missingDependencyHelp": "Install check-disk: apt-get install check-disk" }, "args": []
  }]
}
