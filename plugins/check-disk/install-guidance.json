{ "plugin": "check-disk", "binary": "check-disk", "check": "which check-disk", "install_steps": ["apt-get install check-disk 2>/dev/null || which check-disk", "Verify: check-disk --version", "supercli plugins install ./plugins/check-disk --on-conflict replace --json"] }
