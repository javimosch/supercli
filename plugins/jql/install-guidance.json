{
  "plugin": "jql",
  "binary": "jql",
  "check": "which jql",
  "install_steps": [
    "cargo install jql",
    "Verify: jql --version",
    "supercli plugins install ./plugins/jql --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, apk, pacman, nix, and more. Eats JSON in, outputs JSON back."
}