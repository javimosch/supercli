---
name: gvfs-mime
description: Use this skill when the user wants to use gvfs-mime, cli tool: gvfs-mime.
---

# gvfs-mime Plugin

CLI tool: gvfs-mime.

## Commands
- `gvfs-mime <resource> <action>` — Execute gvfs-mime commands
- `gvfs-mime self version` — Print gvfs-mime version
- `gvfs-mime _ _` — Passthrough to gvfs-mime CLI

## Usage Examples
- "gvfs-mime --help"
- "gvfs-mime self version"

## Installation
```bash
apt-get install gvfs-mime 2>/dev/null || which gvfs-mime
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
