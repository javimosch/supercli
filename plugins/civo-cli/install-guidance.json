{
  "plugin": "civo-cli",
  "binary": "civo",
  "check": "which civo",
  "install_steps": [
    "brew install civo",
    "Verify: civo --version",
    "supercli plugins install ./plugins/civo-cli --on-conflict replace --json"
  ]
}