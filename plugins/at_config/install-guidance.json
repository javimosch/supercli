{ "plugin": "at_config", "binary": "at_config", "check": "which at_config", "install_steps": ["apt-get install at_config 2>/dev/null || which at_config", "Verify: at_config --version", "supercli plugins install ./plugins/at_config --on-conflict replace --json"] }
