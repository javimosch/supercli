{
  "plugin": "loudness",
  "binary": "loudness",
  "check": "which loudness",
  "install_steps": [
    "cargo install loudness",
    "Verify: loudness --version",
    "supercli plugins install ./plugins/loudness --on-conflict replace --json"
  ]
}
