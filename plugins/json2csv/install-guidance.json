{
  "plugin": "json2csv",
  "binary": "json2csv",
  "check": "which json2csv",
  "install_steps": [
    "npm install -g @json2csv/cli",
    "Verify: json2csv --version",
    "supercli plugins install ./plugins/json2csv --on-conflict replace --json"
  ],
  "note": "Json2csv converts JSON to CSV with full RFC4180 compliance. Supports custom delimiters, field selection, and streaming for large files. Use -o for output file or pipe to stdout."
}
