{ "plugin": "canbusload", "binary": "canbusload", "check": "which canbusload", "install_steps": ["apt-get install canbusload 2>/dev/null || which canbusload", "Verify: canbusload --version", "supercli plugins install ./plugins/canbusload --on-conflict replace --json"] }
