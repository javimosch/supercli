---
name: qui
description: Use this skill when the user wants to use qui for A fast, single-binary qBittorrent web UI: manage multiple instances, automate to.
---

# qui Plugin

A fast, single-binary qBittorrent web UI: manage multiple instances, automate torrent workflows, and cross-seed across trackers.

## Commands

### General
- `qui self version` — Print qui version
- `qui _ _` — Passthrough to qui CLI

## Usage Examples
- "Run qui"
- "Use qui to A fast, single-binary qBittorrent web UI: manage multiple in"

## Installation

```bash
pip install qui
```

## Key Features
- A fast, single-binary qBittorrent web UI: manage multiple instances, automate torrent workflows, and
- CLI tool, headless and scriptable
