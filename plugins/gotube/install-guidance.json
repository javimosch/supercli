{
  "plugin": "gotube",
  "binary": "gotube",
  "check": "which gotube",
  "install_steps": [
    "go install github.com/marethyu/gotube@latest",
    "Verify: gotube --version",
    "supercli plugins install ./plugins/gotube --on-conflict replace --json"
  ]
}