{ "plugin": "cdt", "binary": "cdt", "check": "which cdt", "install_steps": ["apt-get install cdt 2>/dev/null || which cdt", "Verify: cdt --version", "supercli plugins install ./plugins/cdt --on-conflict replace --json"] }
