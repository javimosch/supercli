{
  "description": "Persistent memory for AI coding agents — save, search, and recall context across sessions from the command line. Zero external dependencies, SQLite-backed, Bun.js compiled binary. Supports projects, tags, full-text search, and JSON output. Inspired by agentmemory (rohitg00/agentmemory) but re-implemented as a headless CLI.",
  "tags": ["agentmemory", "memory", "ai", "persistence", "context", "cli"],
  "has_learn": true
}
