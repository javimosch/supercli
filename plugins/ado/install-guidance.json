{ "plugin": "ado", "binary": "ado", "check": "which ado", "install_steps": ["apt-get install ado 2>/dev/null || which ado", "Verify: ado --version", "supercli plugins install ./plugins/ado --on-conflict replace --json"] }
