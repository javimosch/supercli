{
  "plugin": "mkfs.fat",
  "binary": "mkfs.fat",
  "check": "which mkfs.fat",
  "install_steps": [
    "apt-get install mkfs.fat 2>/dev/null || which mkfs.fat",
    "Verify: mkfs.fat --version",
    "supercli plugins install ./plugins/mkfs.fat --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}