{
  "plugin": "croc",
  "binary": "croc",
  "check": "which croc",
  "install_steps": [
    "brew install croc",
    "Verify: croc --version",
    "supercli plugins install ./plugins/croc --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/schollz/croc/v9@latest. Simple, secure, and cross-platform file transfer tool. Uses end-to-end encryption via password-authenticated key exchange. Supports any two computers, even behind firewalls."
}
