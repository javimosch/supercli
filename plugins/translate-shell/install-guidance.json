{
  "plugin": "translate-shell",
  "binary": "trans",
  "check": "which trans",
  "install_steps": [
    "brew install translate-shell  # macOS",
    "sudo apt-get install translate-shell  # Debian/Ubuntu",
    "sudo dnf install translate-shell  # Fedora",
    "git clone https://github.com/soimort/translate-shell && cd translate-shell && make && sudo make install",
    "Verify: trans --version",
    "supercli plugins install ./plugins/translate-shell --on-conflict replace --json"
  ],
  "note": "Requires gawk (GNU Awk). Multiple translation engines: google (default), bing, yandex, apertium. Works entirely offline in interactive shell mode. All translation data comes from free public APIs."
}
