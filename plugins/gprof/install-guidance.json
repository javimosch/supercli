{
  "plugin": "gprof",
  "binary": "gprof",
  "check": "which gprof",
  "install_steps": [
    "# Install gprof",
    "# See gnu.org/software/binutils",
    "# apt: sudo apt-get install gprof",
    "# brew: brew install gprof",
    "# cargo: cargo install gprof",
    "# npm: npm install -g gprof"
  ]
}