{
  "plugin": "js-yaml",
  "binary": "js-yaml",
  "check": "which js-yaml",
  "install_steps": [
    "npm install -g js-yaml",
    "Verify: js-yaml --version",
    "supercli plugins install ./plugins/js-yaml --on-conflict replace --json"
  ],
  "note": "Implements YAML 1.2 specification. Very fast parsing and dumping. Can be used as a library or CLI tool. Supports custom schemas and types."
}
