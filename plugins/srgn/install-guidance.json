{
  "plugin": "srgn",
  "binary": "srgn",
  "check": "which srgn",
  "install_steps": [
    "cargo install srgn",
    "Verify: srgn --version",
    "supercli plugins install ./plugins/srgn --on-conflict replace --json"
  ]
}