{
  "plugin": "gh-cli",
  "binary": "gh",
  "check": "which gh",
  "install_steps": ["brew install gh", "Verify: gh --version", "supercli plugins install ./plugins/gh-cli --on-conflict replace --json"]
}
