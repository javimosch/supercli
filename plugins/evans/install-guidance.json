{
  "plugin": "evans",
  "binary": "evans",
  "check": "which evans",
  "install_steps": [
    "brew install evans",
    "Verify: evans --version",
    "supercli plugins install ./plugins/evans --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/ktr0731/evans@latest. Universal gRPC client with reflection support, streaming, and an interactive REPL. More expressive than curl for gRPC services."
}
