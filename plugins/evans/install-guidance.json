{
  "plugin": "evans",
  "binary": "evans",
  "check": "which evans",
  "install_steps": ["brew install evans", "Verify: evans --version", "supercli plugins install ./plugins/evans --on-conflict replace --json"]
}
