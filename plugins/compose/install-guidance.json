{ "plugin": "compose", "binary": "compose", "check": "which compose", "install_steps": ["apt-get install compose 2>/dev/null || which compose", "Verify: compose --version", "supercli plugins install ./plugins/compose --on-conflict replace --json"] }
