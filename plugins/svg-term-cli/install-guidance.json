{
  "plugin": "svg-term-cli",
  "binary": "svg-term",
  "check": "svg-term --version",
  "install_steps": [
    "npm install -g svg-term-cli"
  ],
  "note": "Requires Node.js and npm. Also requires asciinema for recording terminal sessions."
}
