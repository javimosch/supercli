{
  "description": "CLI for running YAML-defined data pipelines with JSON, JSONL, and CSV support. Includes built-in steps for filtering, normalizing, redacting, casting, sorting, validating, and converting data.",
  "tags": ["pipectl", "pipeline", "data", "etl", "yaml", "json", "csv", "go", "cli"],
  "has_learn": true
}
