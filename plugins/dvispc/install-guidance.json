{
  "plugin": "dvispc",
  "binary": "dvispc",
  "check": "which dvispc",
  "install_steps": [
    "apt-get install dvispc 2>/dev/null || which dvispc",
    "Verify: dvispc --version",
    "supercli plugins install ./plugins/dvispc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}