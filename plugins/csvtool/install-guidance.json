{
  "plugin": "csvtool",
  "binary": "csvtool",
  "check": "which csvtool",
  "install_steps": [
    "brew install csvtool",
    "Verify: csvtool --version",
    "supercli plugins install ./plugins/csvtool --on-conflict replace --json"
  ]
}
