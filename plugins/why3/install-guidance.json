{
  "plugin": "why3",
  "binary": "why3",
  "check": "which why3",
  "install_steps": [
    "brew install why3",
    "Verify: why3 --version",
    "supercli plugins install ./plugins/why3 --on-conflict replace --json"
  ]
}
