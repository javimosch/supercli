{
  "plugin": "bombardier",
  "binary": "bombardier",
  "check": "which bombardier",
  "install_steps": [
    "brew install bombardier",
    "Verify: bombardier --version",
    "supercli plugins install ./plugins/bombardier --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/codesenberg/bombardier/cmd/bombardier@latest. Supports HTTP/HTTPS, configurable concurrency, request rates, and duration. Provides detailed statistics and latency histograms."
}
