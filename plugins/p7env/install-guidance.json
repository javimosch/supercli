{
  "plugin": "p7env",
  "binary": "p7env",
  "check": "which p7env",
  "install_steps": [
    "apt-get install p7env 2>/dev/null || which p7env",
    "Verify: p7env --version",
    "supercli plugins install ./plugins/p7env --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}