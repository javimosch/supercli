{
  "plugin": "jsonlint",
  "binary": "jsonlint",
  "check": "which jsonlint",
  "install_steps": [
    "npm install -g jsonlint",
    "Verify: jsonlint --version",
    "supercli plugins install ./plugins/jsonlint --on-conflict replace --json"
  ],
  "note": "Pure JavaScript JSON implementation. Validates JSON syntax, can sort keys, and format JSON. Works with stdin or files."
}
