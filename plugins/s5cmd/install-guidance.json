{
  "plugin": "s5cmd",
  "binary": "s5cmd",
  "check": "which s5cmd",
  "install_steps": [
    "brew install s5cmd",
    "Verify: s5cmd --version",
    "supercli plugins install ./plugins/s5cmd --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/peak/s5cmd/cmd/s5cmd@latest. Parallel S3 operations for high performance. Supports AWS S3 and Google Cloud Storage. Can copy, sync, list, and delete files. Configurable concurrency."
}
