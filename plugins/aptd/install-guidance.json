{ "plugin": "aptd", "binary": "aptd", "check": "which aptd", "install_steps": ["apt-get install aptd 2>/dev/null || which aptd", "Verify: aptd --version", "supercli plugins install ./plugins/aptd --on-conflict replace --json"] }
