{
  "plugin": "yek",
  "binary": "yek",
  "check": "which yek",
  "install_steps": [
    "curl -fsSL https://azimi.me/yek.sh | bash",
    "Verify: yek --version",
    "supercli plugins install ./plugins/yek --on-conflict replace --json"
  ],
  "note": "Fastest alternative to repomix (230x faster). Prioritizes important files for LLM context. Supports glob patterns, multi-directory processing, and token-based limits."
}
