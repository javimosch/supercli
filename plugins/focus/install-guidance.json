{
  "plugin": "focus",
  "binary": "focus",
  "check": "which focus",
  "install_steps": [
    "apt-get install focus 2>/dev/null || which focus",
    "Verify: focus --version",
    "supercli plugins install ./plugins/focus --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}