{
  "plugin": "howdoi",
  "binary": "howdoi",
  "check": "which howdoi",
  "install_steps": [
    "pip install howdoi",
    "Or: brew install howdoi",
    "Verify: howdoi --version",
    "supercli plugins install ./plugins/howdoi --on-conflict replace --json"
  ],
  "note": "Available via pip, brew, and apt. Supports --json for machine-readable output. Uses Stack Overflow as primary source. Cache is stored in ~/.cache/howdoi."
}
