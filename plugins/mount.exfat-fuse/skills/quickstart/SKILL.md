---
name: mount.exfat-fuse
description: Use this skill when the user wants to use mount.exfat-fuse, cli tool: mount.exfat-fuse.
---

# mount.exfat-fuse Plugin

CLI tool: mount.exfat-fuse.

## Commands
- `mount.exfat-fuse <resource> <action>` — Execute mount.exfat-fuse commands
- `mount.exfat-fuse self version` — Print mount.exfat-fuse version
- `mount.exfat-fuse _ _` — Passthrough to mount.exfat-fuse CLI

## Usage Examples
- "mount.exfat-fuse --help"
- "mount.exfat-fuse self version"

## Installation
```bash
apt-get install mount.exfat-fuse 2>/dev/null || which mount.exfat-fuse
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
