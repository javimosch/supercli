{
  "plugin": "pure",
  "binary": "pure",
  "check": "which pure",
  "install_steps": ["npm install --global pure-prompt", "Verify: pure --version", "supercli plugins install ./plugins/pure --on-conflict replace --json"]
}
