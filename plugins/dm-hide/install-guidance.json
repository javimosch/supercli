{ "plugin": "dm-hide", "binary": "dm-hide", "check": "which dm-hide", "install_steps": ["apt-get install dm-hide 2>/dev/null || which dm-hide", "Verify: dm-hide --version", "supercli plugins install ./plugins/dm-hide --on-conflict replace --json"] }
