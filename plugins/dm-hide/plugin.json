{
  "name": "dm-hide",
  "version": "0.1.0",
  "description": "CLI tool: dm-hide",
  "source": "https://packages.debian.org/dm-hide",
  "checks": [{ "type": "binary", "name": "dm-hide" }],
  "install_guidance": {
    "plugin": "dm-hide", "binary": "dm-hide", "check": "which dm-hide",
    "install_steps": ["apt-get install dm-hide 2>/dev/null || which dm-hide", "Verify: dm-hide --version", "supercli plugins install ./plugins/dm-hide --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-hide", "resource": "_", "action": "_",
    "description": "Passthrough to dm-hide CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-hide", "passthrough": true, "missingDependencyHelp": "Install dm-hide: apt-get install dm-hide" }, "args": []
  }]
}
