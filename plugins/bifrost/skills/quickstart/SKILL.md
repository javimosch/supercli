---
name: bifrost
description: Use this skill when the user wants to use bifrost for Fastest enterprise AI gateway (50x faster than LiteLLM) with adaptive load balan.
---

# bifrost Plugin

Fastest enterprise AI gateway (50x faster than LiteLLM) with adaptive load balancer, cluster mode, guardrails, 1000+ models support & <100 µs overhead at 5k RPS.

## Commands

### General
- `bifrost self version` — Print bifrost version
- `bifrost _ _` — Passthrough to bifrost CLI

## Usage Examples
- "Run bifrost"
- "Use bifrost to Fastest enterprise AI gateway (50x faster than LiteLLM) with"

## Installation

```bash
pip install bifrost
```

## Key Features
- Fastest enterprise AI gateway (50x faster than LiteLLM) with adaptive load balancer, cluster mode, g
- CLI tool, headless and scriptable
