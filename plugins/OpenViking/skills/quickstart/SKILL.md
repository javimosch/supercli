---
name: OpenViking
description: Use this skill when the user wants to use OpenViking for OpenViking is an open-source context database designed specifically for AI Agent.
---

# OpenViking Plugin

OpenViking is an open-source context database designed specifically for AI Agents(such as openclaw). OpenViking unifies the management of context (memory, resources, and skills) that Agents need through a file system paradigm, enabling hierarchical context delivery and self-evolving.

## Commands

### General
- `OpenViking self version` — Print OpenViking version
- `OpenViking _ _` — Passthrough to openviking CLI

## Usage Examples
- "Run OpenViking"
- "Use openviking to OpenViking is an open-source context database designed speci"

## Installation

```bash
pip install openviking
```

## Key Features
- OpenViking is an open-source context database designed specifically for AI Agents(such as openclaw).
- CLI tool, headless and scriptable
