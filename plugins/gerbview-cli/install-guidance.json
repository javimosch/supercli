{
  "plugin": "gerbview-cli",
  "binary": "gerbview-cli",
  "check": "which gerbview-cli",
  "install_steps": ["cargo install gerbview-cli", "Verify: gerbview-cli --version", "supercli plugins install ./plugins/gerbview-cli --on-conflict replace --json"]
}
