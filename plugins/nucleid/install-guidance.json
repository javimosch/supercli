{
  "plugin": "nucleid",
  "binary": "nucleid",
  "check": "which nucleid",
  "install_steps": [
    "Check GitHub repository for installation instructions",
    "Verify: nucleid --version",
    "supercli plugins install ./plugins/nucleid --on-conflict replace --json"
  ],
  "note": "JavaScript language CLI tool - 4 GitHub stars"
}