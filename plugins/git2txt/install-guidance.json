{
  "plugin": "git2txt",
  "binary": "git2txt",
  "check": "which git2txt",
  "install_steps": [
    "npm install -g git2txt",
    "Verify: git2txt --version",
    "supercli plugins install ./plugins/git2txt --on-conflict replace --json"
  ],
  "note": "Git2txt downloads public GitHub repos and converts them to single text files. Automatically excludes binary files. Use --max-size flag to filter by file size."
}
