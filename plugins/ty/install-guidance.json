{
  "plugin": "ty",
  "binary": "ty",
  "check": "which ty",
  "install_steps": [
    "pip install ty",
    "Verify: ty --version",
    "supercli plugins install ./plugins/ty --on-conflict replace --json"
  ],
  "note": "Install via pip: pip install ty"
}
