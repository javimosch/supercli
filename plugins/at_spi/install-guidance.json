{ "plugin": "at_spi", "binary": "at_spi", "check": "which at_spi", "install_steps": ["apt-get install at_spi 2>/dev/null || which at_spi", "Verify: at_spi --version", "supercli plugins install ./plugins/at_spi --on-conflict replace --json"] }
