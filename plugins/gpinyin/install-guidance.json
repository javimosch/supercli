{
  "plugin": "gpinyin",
  "binary": "gpinyin",
  "check": "which gpinyin",
  "install_steps": [
    "apt-get install gpinyin 2>/dev/null || which gpinyin",
    "Verify: gpinyin --version",
    "supercli plugins install ./plugins/gpinyin --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}