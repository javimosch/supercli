{
  "plugin": "pamon",
  "binary": "pamon",
  "check": "which pamon",
  "install_steps": [
    "apt-get install pamon 2>/dev/null || which pamon",
    "Verify: pamon --version",
    "supercli plugins install ./plugins/pamon --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}