{
  "plugin": "wtx",
  "binary": "wtx",
  "check": "which wtx",
  "install_steps": [
    "cargo install wtx",
    "Verify: wtx --version",
    "supercli plugins install ./plugins/wtx --on-conflict replace --json"
  ]
}