{
  "plugin": "mdp",
  "binary": "mdp",
  "check": "which mdp",
  "install_steps": [
    "brew install mdp",
    "Verify: mdp --version",
    "supercli plugins install ./plugins/mdp --on-conflict replace --json"
  ]
}
