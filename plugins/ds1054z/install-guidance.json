{
  "plugin": "ds1054z",
  "binary": "ds1054z",
  "check": "which ds1054z",
  "install_steps": [
    "pip install ds1054z",
    "Verify: ds1054z --version",
    "supercli plugins install ./plugins/ds1054z --on-conflict replace --json"
  ]
}