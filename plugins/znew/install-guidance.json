{
  "plugin": "znew",
  "binary": "znew",
  "check": "which znew",
  "install_steps": [
    "apt-get install znew 2>/dev/null || which znew",
    "Verify: znew --version",
    "supercli plugins install ./plugins/znew --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}