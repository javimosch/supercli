{
  "plugin": "pageres-cli",
  "binary": "pageres",
  "check": "which pageres",
  "install_steps": [
    "npm install -g pageres-cli",
    "Verify: pageres --version",
    "supercli plugins install ./plugins/pageres-cli --on-conflict replace --json"
  ]
}
