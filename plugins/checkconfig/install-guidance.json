{ "plugin": "checkconfig", "binary": "checkconfig", "check": "which checkconfig", "install_steps": ["apt-get install checkconfig 2>/dev/null || which checkconfig", "Verify: checkconfig --version", "supercli plugins install ./plugins/checkconfig --on-conflict replace --json"] }
