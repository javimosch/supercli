{
  "plugin": "pbmtog3",
  "binary": "pbmtog3",
  "check": "which pbmtog3",
  "install_steps": [
    "apt-get install pbmtog3 2>/dev/null || which pbmtog3",
    "Verify: pbmtog3 --version",
    "supercli plugins install ./plugins/pbmtog3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}