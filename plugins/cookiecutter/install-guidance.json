{
  "plugin": "cookiecutter",
  "binary": "cookiecutter",
  "check": "which cookiecutter",
  "install_steps": [
    "brew install cookiecutter",
    "Verify: cookiecutter --version",
    "supercli plugins install ./plugins/cookiecutter --on-conflict replace --json"
  ],
  "note": "Also available via pip: pip install cookiecutter. Supports GitHub, GitLab, local filesystem, and zip URLs as template sources."
}
