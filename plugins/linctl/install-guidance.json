{
  "plugin": "linctl",
  "binary": "linctl",
  "check": "linctl --version",
  "install_steps": [
    "brew tap dorkitude/linctl",
    "brew install linctl",
    "Verify: linctl --version",
    "linctl auth (for initial API key setup)",
    "supercli plugins install ./plugins/linctl --on-conflict replace --json"
  ],
  "note": "Requires Linear API key. Default filters show items from last 6 months. Use --newer-than all_time for historical data. Agents should use --json flag on read operations."
}