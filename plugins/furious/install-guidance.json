{
  "plugin": "furious",
  "binary": "furious",
  "check": "which furious",
  "install_steps": [
    "go install github.com/liamg/furious@latest",
    "Verify: furious --version",
    "supercli plugins install ./plugins/furious --on-conflict replace --json"
  ],
  "note": "SYN scanning requires root/sudo privileges. Connect scan works for any user. Can identify device MAC addresses and manufacturers on local networks."
}
