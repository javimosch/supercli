{
  "plugin": "csv-tool",
  "binary": "csv-tool",
  "check": "which csv-tool",
  "install_steps": ["npm install -g csv-tool", "Verify: csv-tool --version", "supercli plugins install ./plugins/csv-tool --on-conflict replace --json"]
}
