{
  "name": "hostname",
  "version": "0.1.0",
  "description": "hostname — Show or set system hostname",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "hostname"
    }
  ],
  "install_guidance": {
    "plugin": "hostname",
    "binary": "hostname",
    "check": "which hostname",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/hostname --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "hostname",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to hostname CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "hostname",
        "passthrough": true,
        "missingDependencyHelp": "Install hostname: base system"
      },
      "args": []
    }
  ]
}
