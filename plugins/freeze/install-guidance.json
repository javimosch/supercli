{
  "plugin": "freeze",
  "binary": "freeze",
  "check": "which freeze",
  "install_steps": [
    "pip install freeze",
    "Verify: freeze --version",
    "supercli plugins install ./plugins/freeze --on-conflict replace --json"
  ],
  "note": "Language: Go. Stars: 4654"
}