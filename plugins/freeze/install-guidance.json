{
  "plugin": "freeze",
  "binary": "freeze",
  "check": "which freeze",
  "install_steps": ["brew install freeze", "Verify: freeze --version", "supercli plugins install ./plugins/freeze --on-conflict replace --json"]
}
