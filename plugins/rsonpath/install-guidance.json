{
  "plugin": "rsonpath",
  "binary": "rq",
  "check": "which rq",
  "install_steps": [
    "cargo install rsonpath",
    "Verify: rq --version",
    "supercli plugins install ./plugins/rsonpath --on-conflict replace --json"
  ],
  "note": "Binary is named rq, not rsonpath. SIMD-optimized JSONPath engine."
}
