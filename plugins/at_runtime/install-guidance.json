{ "plugin": "at_runtime", "binary": "at_runtime", "check": "which at_runtime", "install_steps": ["apt-get install at_runtime 2>/dev/null || which at_runtime", "Verify: at_runtime --version", "supercli plugins install ./plugins/at_runtime --on-conflict replace --json"] }
