{
  "plugin": "imagemagick",
  "binary": "convert",
  "check": "which convert",
  "install_steps": [
    "brew install imagemagick",
    "Verify: convert --version",
    "supercli plugins install ./plugins/imagemagick --on-conflict replace --json"
  ],
  "note": "ImageMagick provides multiple binaries: convert, identify, mogrify, montage, compare, composite, animate, display. All are non-interactive CLI tools. For ImageMagick 7+, use the unified 'magick' command. This plugin supports v6+ (convert/identify)."
}
