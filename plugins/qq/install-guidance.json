{ "plugin": "qq", "binary": "qq", "check": "which qq", "install_steps": ["brew install qq", "Verify: qq --version", "supercli plugins install ./plugins/qq --on-conflict replace --json"], "note": "Also available via go: go install github.com/JFryy/qq@latest. Converts between JSON, CSV, YAML, HCL, TOML, XML formats." }
