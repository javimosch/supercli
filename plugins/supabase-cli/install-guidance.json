{
  "plugin": "supabase-cli",
  "binary": "supabase",
  "check": "which supabase",
  "install_steps": [
    "brew install supabase/tap/supabase",
    "Verify: supabase --version",
    "supercli plugins install ./plugins/supabase-cli --on-conflict replace --json"
  ],
  "note": "Also available via npm: npm install -g supabase. Manage Supabase projects, databases, edge functions, and auth from the command line. Requires Docker for local development."
}
