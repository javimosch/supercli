{ "plugin": "colormgr", "binary": "colormgr", "check": "which colormgr", "install_steps": ["apt-get install colormgr 2>/dev/null || which colormgr", "Verify: colormgr --version", "supercli plugins install ./plugins/colormgr --on-conflict replace --json"] }
