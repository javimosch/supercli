{
  "name": "colormgr",
  "version": "0.1.0",
  "description": "CLI tool: colormgr",
  "source": "https://packages.debian.org/colormgr",
  "checks": [{ "type": "binary", "name": "colormgr" }],
  "install_guidance": {
    "plugin": "colormgr", "binary": "colormgr", "check": "which colormgr",
    "install_steps": ["apt-get install colormgr 2>/dev/null || which colormgr", "Verify: colormgr --version", "supercli plugins install ./plugins/colormgr --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "colormgr", "resource": "_", "action": "_",
    "description": "Passthrough to colormgr CLI",
    "adapter": "process", "adapterConfig": { "command": "colormgr", "passthrough": true, "missingDependencyHelp": "Install colormgr: apt-get install colormgr" }, "args": []
  }]
}
