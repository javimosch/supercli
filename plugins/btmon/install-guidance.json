{ "plugin": "btmon", "binary": "btmon", "check": "which btmon", "install_steps": ["apt-get install btmon 2>/dev/null || which btmon", "Verify: btmon --version", "supercli plugins install ./plugins/btmon --on-conflict replace --json"] }
