{ "plugin": "cansend", "binary": "cansend", "check": "which cansend", "install_steps": ["apt-get install cansend 2>/dev/null || which cansend", "Verify: cansend --version", "supercli plugins install ./plugins/cansend --on-conflict replace --json"] }
