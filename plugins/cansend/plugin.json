{
  "name": "cansend",
  "version": "0.1.0",
  "description": "CLI tool: cansend",
  "source": "https://packages.debian.org/cansend",
  "checks": [{ "type": "binary", "name": "cansend" }],
  "install_guidance": {
    "plugin": "cansend", "binary": "cansend", "check": "which cansend",
    "install_steps": ["apt-get install cansend 2>/dev/null || which cansend", "Verify: cansend --version", "supercli plugins install ./plugins/cansend --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cansend", "resource": "_", "action": "_",
    "description": "Passthrough to cansend CLI",
    "adapter": "process", "adapterConfig": { "command": "cansend", "passthrough": true, "missingDependencyHelp": "Install cansend: apt-get install cansend" }, "args": []
  }]
}
