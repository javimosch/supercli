{
  "plugin": "telemt",
  "binary": "telemt",
  "check": "which telemt",
  "install_steps": [
    "cargo install telemt",
    "Verify: telemt --version",
    "supercli plugins install ./plugins/telemt --on-conflict replace --json"
  ],
  "note": "Fast Rust-based MTProxy (MTProto) server for Telegram."
}