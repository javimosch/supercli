{
  "plugin": "splain",
  "binary": "splain",
  "check": "which splain",
  "install_steps": [
    "apt-get install splain 2>/dev/null || which splain",
    "Verify: splain --version",
    "supercli plugins install ./plugins/splain --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}