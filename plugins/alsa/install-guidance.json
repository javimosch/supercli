{ "plugin": "alsa", "binary": "alsa", "check": "which alsa", "install_steps": ["apt-get install alsa 2>/dev/null || which alsa", "Verify: alsa --version", "supercli plugins install ./plugins/alsa --on-conflict replace --json"] }
