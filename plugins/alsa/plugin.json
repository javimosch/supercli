{
  "name": "alsa",
  "version": "0.1.0",
  "description": "CLI tool: alsa",
  "source": "https://packages.debian.org/alsa",
  "checks": [{ "type": "binary", "name": "alsa" }],
  "install_guidance": {
    "plugin": "alsa", "binary": "alsa", "check": "which alsa",
    "install_steps": ["apt-get install alsa 2>/dev/null || which alsa", "Verify: alsa --version", "supercli plugins install ./plugins/alsa --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "alsa", "resource": "_", "action": "_",
    "description": "Passthrough to alsa CLI",
    "adapter": "process", "adapterConfig": { "command": "alsa", "passthrough": true, "missingDependencyHelp": "Install alsa: apt-get install alsa" }, "args": []
  }]
}
