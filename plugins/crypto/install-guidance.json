{ "plugin": "crypto", "binary": "crypto", "check": "which crypto", "install_steps": ["apt-get install crypto 2>/dev/null || which crypto", "Verify: crypto --version", "supercli plugins install ./plugins/crypto --on-conflict replace --json"] }
