{
  "plugin": "headson",
  "binary": "hson",
  "check": "which hson",
  "install_steps": [
    "cargo install headson",
    "Verify: hson --version",
    "supercli plugins install ./plugins/headson --on-conflict replace --json"
  ],
  "note": "The package is called headson, but the installed binary is hson. Also supports building from source (cargo build --release)."
}
