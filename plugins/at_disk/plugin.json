{
  "name": "at_disk",
  "version": "0.1.0",
  "description": "CLI tool: at_disk",
  "source": "https://packages.debian.org/at_disk",
  "checks": [{ "type": "binary", "name": "at_disk" }],
  "install_guidance": {
    "plugin": "at_disk", "binary": "at_disk", "check": "which at_disk",
    "install_steps": ["apt-get install at_disk 2>/dev/null || which at_disk", "Verify: at_disk --version", "supercli plugins install ./plugins/at_disk --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "at_disk", "resource": "_", "action": "_",
    "description": "Passthrough to at_disk CLI",
    "adapter": "process", "adapterConfig": { "command": "at_disk", "passthrough": true, "missingDependencyHelp": "Install at_disk: apt-get install at_disk" }, "args": []
  }]
}
