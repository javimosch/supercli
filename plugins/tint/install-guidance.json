{
  "plugin": "tint",
  "binary": "tint",
  "check": "which tint",
  "install_steps": [
    "go install github.com/ashish0kumar/tint@latest",
    "Verify: tint --version",
    "supercli plugins install ./plugins/tint --on-conflict replace --json"
  ],
  "note": "Also available via Nix, AUR, Void Linux, and pre-built binaries from GitHub releases. Supports JPEG and PNG formats."
}
