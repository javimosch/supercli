{
  "plugin": "rts",
  "binary": "rts",
  "check": "which rts",
  "install_steps": [
    "go install github.com/galeone/rts@latest",
    "Verify: rts --version",
    "supercli plugins install ./plugins/rts --on-conflict replace --json"
  ]
}