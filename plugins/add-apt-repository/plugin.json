{
  "name": "add-apt-repository",
  "version": "0.1.0",
  "description": "CLI tool: add-apt-repository",
  "source": "https://packages.debian.org/add-apt-repository",
  "checks": [{ "type": "binary", "name": "add-apt-repository" }],
  "install_guidance": {
    "plugin": "add-apt-repository", "binary": "add-apt-repository", "check": "which add-apt-repository",
    "install_steps": ["apt-get install add-apt-repository 2>/dev/null || which add-apt-repository", "Verify: add-apt-repository --version", "supercli plugins install ./plugins/add-apt-repository --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "add-apt-repository", "resource": "_", "action": "_",
    "description": "Passthrough to add-apt-repository CLI",
    "adapter": "process", "adapterConfig": { "command": "add-apt-repository", "passthrough": true, "missingDependencyHelp": "Install add-apt-repository: apt-get install add-apt-repository" }, "args": []
  }]
}
