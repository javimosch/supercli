{
  "plugin": "qrencode",
  "binary": "qrencode",
  "check": "which qrencode",
  "install_steps": [
    "brew install qrencode",
    "Verify: qrencode --version",
    "supercli plugins install ./plugins/qrencode --on-conflict replace --json"
  ],
  "note": "Also available via: apt install qrencode, dnf install qrencode. Output formats: PNG (default) and EPS. For SVG output, pipe through a converter. Supports Micro QR and structured append for larger data."
}
