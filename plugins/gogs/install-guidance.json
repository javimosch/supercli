{
  "plugin": "gogs",
  "binary": "gogs",
  "check": "which gogs",
  "install_steps": [
    "brew install gogs",
    "Verify: gogs --version",
    "supercli plugins install ./plugins/gogs --on-conflict replace --json"
  ]
}