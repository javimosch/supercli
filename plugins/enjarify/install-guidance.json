{
  "plugin": "enjarify",
  "binary": "enjarify",
  "check": "which enjarify",
  "install_steps": [
    "pip install enjarify",
    "Verify: enjarify --version",
    "supercli plugins install ./plugins/enjarify --on-conflict replace --json"
  ]
}
