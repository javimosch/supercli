{
  "plugin": "htmlq",
  "binary": "htmlq",
  "check": "which htmlq",
  "install_steps": ["cargo install htmlq", "Verify: htmlq --version", "supercli plugins install ./plugins/htmlq --on-conflict replace --json"]
}
