{
  "name": "dpkg-scanpackages",
  "version": "1.0.0",
  "description": "dpkg-scanpackages \u2014 dpkg-scanpackages \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-scanpackages"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-scanpackages",
    "binary": "dpkg-scanpackages",
    "check": "which dpkg-scanpackages",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-scanpackages",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-scanpackages \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-scanpackages",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}