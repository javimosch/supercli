{ "plugin": "check-nic", "binary": "check-nic", "check": "which check-nic", "install_steps": ["apt-get install check-nic 2>/dev/null || which check-nic", "Verify: check-nic --version", "supercli plugins install ./plugins/check-nic --on-conflict replace --json"] }
