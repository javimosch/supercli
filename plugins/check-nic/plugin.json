{
  "name": "check-nic",
  "version": "0.1.0",
  "description": "CLI tool: check-nic",
  "source": "https://packages.debian.org/check-nic",
  "checks": [{ "type": "binary", "name": "check-nic" }],
  "install_guidance": {
    "plugin": "check-nic", "binary": "check-nic", "check": "which check-nic",
    "install_steps": ["apt-get install check-nic 2>/dev/null || which check-nic", "Verify: check-nic --version", "supercli plugins install ./plugins/check-nic --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check-nic", "resource": "_", "action": "_",
    "description": "Passthrough to check-nic CLI",
    "adapter": "process", "adapterConfig": { "command": "check-nic", "passthrough": true, "missingDependencyHelp": "Install check-nic: apt-get install check-nic" }, "args": []
  }]
}
