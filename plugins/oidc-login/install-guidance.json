{
  "plugin": "oidc-login",
  "binary": "oidc-login",
  "check": "which oidc-login",
  "install_steps": ["brew install oidc-login", "Verify: oidc-login --version", "supercli plugins install ./plugins/oidc-login --on-conflict replace --json"]
}
