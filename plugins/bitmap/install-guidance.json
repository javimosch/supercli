{ "plugin": "bitmap", "binary": "bitmap", "check": "which bitmap", "install_steps": ["apt-get install bitmap 2>/dev/null || which bitmap", "Verify: bitmap --version", "supercli plugins install ./plugins/bitmap --on-conflict replace --json"] }
