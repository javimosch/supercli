{
  "name": "bitmap",
  "version": "0.1.0",
  "description": "CLI tool: bitmap",
  "source": "https://packages.debian.org/bitmap",
  "checks": [{ "type": "binary", "name": "bitmap" }],
  "install_guidance": {
    "plugin": "bitmap", "binary": "bitmap", "check": "which bitmap",
    "install_steps": ["apt-get install bitmap 2>/dev/null || which bitmap", "Verify: bitmap --version", "supercli plugins install ./plugins/bitmap --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "bitmap", "resource": "_", "action": "_",
    "description": "Passthrough to bitmap CLI",
    "adapter": "process", "adapterConfig": { "command": "bitmap", "passthrough": true, "missingDependencyHelp": "Install bitmap: apt-get install bitmap" }, "args": []
  }]
}
