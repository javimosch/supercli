{
  "plugin": "precat",
  "binary": "precat",
  "check": "which precat",
  "install_steps": [
    "apt-get install precat 2>/dev/null || which precat",
    "Verify: precat --version",
    "supercli plugins install ./plugins/precat --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}