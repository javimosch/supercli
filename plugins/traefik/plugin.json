{
  "name": "traefik",
  "version": "0.1.0",
  "description": "Traefik — cloud-native reverse proxy and load balancer",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "traefik"
    }
  ],
  "install_guidance": {
    "plugin": "traefik",
    "binary": "traefik",
    "check": "which traefik",
    "install_steps": [
      "brew install traefik"
    ],
    "note": "Infrastructure tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "traefik",
      "resource": "_",
      "action": "_",
      "description": "Passthrough",
      "adapter": "process",
      "adapterConfig": {
        "command": "traefik",
        "passthrough": true,
        "missingDependencyHelp": "Install traefik: brew install traefik"
      },
      "args": []
    }
  ]
}
