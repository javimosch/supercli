{
  "plugin": "update-initramfs",
  "binary": "update-initramfs",
  "check": "which update-initramfs",
  "install_steps": [
    "apt-get install update-initramfs 2>/dev/null || which update-initramfs",
    "Verify: update-initramfs --version",
    "supercli plugins install ./plugins/update-initramfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}