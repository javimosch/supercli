{
  "plugin": "pwgen",
  "binary": "pwgen",
  "check": "which pwgen",
  "install_steps": [
    "brew install pwgen",
    "Verify: pwgen --version",
    "supercli plugins install ./plugins/pwgen --on-conflict replace --json"
  ]
}
