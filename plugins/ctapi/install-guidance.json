{ "plugin": "ctapi", "binary": "ctapi", "check": "which ctapi", "install_steps": ["apt-get install ctapi 2>/dev/null || which ctapi", "Verify: ctapi --version", "supercli plugins install ./plugins/ctapi --on-conflict replace --json"] }
