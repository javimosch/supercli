{
  "plugin": "xml2",
  "binary": "xml2",
  "check": "which xml2",
  "install_steps": [
    "brew install xml2",
    "Verify: xml2 --version",
    "supercli plugins install ./plugins/xml2 --on-conflict replace --json"
  ]
}
