{
  "plugin": "sort-package-json",
  "binary": "sort-package-json",
  "check": "which sort-package-json",
  "install_steps": [
    "npm install -g sort-package-json",
    "Verify: sort-package-json --version"
  ],
  "note": "Optionally also install eslint-plugin-sort-package-json for IDE integration."
}
