{
  "plugin": "tokscale",
  "binary": "tokscale",
  "check": "which tokscale",
  "install_steps": [
    "bunx tokscale@latest",
    "Verify: tokscale --version",
    "supercli plugins install ./plugins/tokscale --on-conflict replace --json"
  ],
  "note": "Requires Node.js or Bun runtime. Native Rust core provides ~10x faster processing. Supports macOS, Linux, and Windows."
}