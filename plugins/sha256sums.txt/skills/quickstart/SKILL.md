---
name: sha256sums.txt
description: Use this skill when the user wants to use sha256sums.txt, cli tool: sha256sums.txt.
---

# sha256sums.txt Plugin

CLI tool: sha256sums.txt.

## Commands
- `sha256sums.txt <resource> <action>` — Execute sha256sums.txt commands
- `sha256sums.txt self version` — Print sha256sums.txt version
- `sha256sums.txt _ _` — Passthrough to sha256sums.txt CLI

## Usage Examples
- "sha256sums.txt --help"
- "sha256sums.txt self version"

## Installation
```bash
apt-get install sha256sums.txt 2>/dev/null || which sha256sums.txt
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
