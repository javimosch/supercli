{
  "plugin": "gwctl",
  "binary": "gwctl",
  "check": "which gwctl",
  "install_steps": [
    "brew install gwctl",
    "Verify: gwctl --version",
    "supercli plugins install ./plugins/gwctl --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}