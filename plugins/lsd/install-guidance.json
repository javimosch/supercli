{
  "plugin": "lsd",
  "binary": "lsd",
  "check": "which lsd",
  "install_steps": [
    "brew install lsd",
    "Verify: lsd --version",
    "supercli plugins install ./plugins/lsd --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install lsd. The next gen ls command with colors, icons, tree view, and git integration. Requires Nerd Font for icons. A modern replacement for ls with enhanced visual output."
}
