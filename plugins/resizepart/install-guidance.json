{
  "plugin": "resizepart",
  "binary": "resizepart",
  "check": "which resizepart",
  "install_steps": [
    "# Install resizepart",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/resizepart-linux-amd64 -o /usr/local/bin/resizepart",
    "chmod +x /usr/local/bin/resizepart",
    "# Or via package manager, see /usr/bin/resizepart"
  ]
}