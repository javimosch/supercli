{
  "plugin": "dev-cleaner",
  "binary": "dev-cleaner",
  "check": "which dev-cleaner",
  "install_steps": [
    "npm install -g dev-cleaner",
    "Verify: dev-cleaner --version",
    "supercli plugins install ./plugins/dev-cleaner --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
