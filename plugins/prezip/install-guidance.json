{
  "plugin": "prezip",
  "binary": "prezip",
  "check": "which prezip",
  "install_steps": [
    "apt-get install prezip 2>/dev/null || which prezip",
    "Verify: prezip --version",
    "supercli plugins install ./plugins/prezip --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}