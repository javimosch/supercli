{ "plugin": "cbor2", "binary": "cbor2", "check": "which cbor2", "install_steps": ["apt-get install cbor2 2>/dev/null || which cbor2", "Verify: cbor2 --version", "supercli plugins install ./plugins/cbor2 --on-conflict replace --json"] }
