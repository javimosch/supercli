{
  "plugin": "gnome-terminal.real",
  "binary": "gnome-terminal.real",
  "check": "which gnome-terminal.real",
  "install_steps": [
    "apt-get install gnome-terminal.real 2>/dev/null || which gnome-terminal.real",
    "Verify: gnome-terminal.real --version",
    "supercli plugins install ./plugins/gnome-terminal.real --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}