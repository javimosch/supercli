{
  "plugin": "rsync",
  "binary": "rsync",
  "check": "which rsync",
  "install_steps": [
    "brew install rsync",
    "Verify: rsync --version",
    "supercli plugins install ./plugins/rsync --on-conflict replace --json"
  ]
}
