{
  "plugin": "circus",
  "binary": "circus",
  "check": "which circus",
  "install_steps": ["pip install circus", "Verify: circus --version", "supercli plugins install ./plugins/circus --on-conflict replace --json"]
}
