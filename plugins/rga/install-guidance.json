{
  "plugin": "rga",
  "binary": "rga",
  "check": "which rga",
  "install_steps": [
    "brew install ripgrep-all",
    "Verify: rga --version",
    "supercli plugins install ./plugins/rga --on-conflict replace --json"
  ],
  "note": "Also available via: cargo install ripgrep_all, or download pre-built binaries from https://github.com/phiresky/ripgrep-all/releases. Requires pandoc for DOCX/ODT support and poppler for PDF support. Adapters: pdf (poppler), docx (pandoc), odt (pandoc), epub (pandoc), mobi (pandoc)."
}
