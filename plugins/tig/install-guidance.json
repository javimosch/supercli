{
  "plugin": "tig",
  "binary": "tig",
  "check": "which tig",
  "install_steps": [
    "brew install tig",
    "Verify: tig --version",
    "supercli plugins install ./plugins/tig --on-conflict replace --json"
  ],
  "note": "Also available via apt: sudo apt install tig, pacman: sudo pacman -S tig, or build from source from https://github.com/jonas/tig. Supports keybindings, custom color schemes, and git-config-level customization."
}
