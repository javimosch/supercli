---
name: javap
description: Use this skill when the user wants to use javap, cli tool: javap.
---

# javap Plugin

CLI tool: javap.

## Commands
- `javap <resource> <action>` — Execute javap commands
- `javap self version` — Print javap version
- `javap _ _` — Passthrough to javap CLI

## Usage Examples
- "javap --help"
- "javap self version"

## Installation
```bash
apt-get install javap 2>/dev/null || which javap
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
