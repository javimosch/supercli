{
  "plugin": "javap",
  "binary": "javap",
  "check": "which javap",
  "install_steps": [
    "apt-get install javap 2>/dev/null || which javap",
    "Verify: javap --version",
    "supercli plugins install ./plugins/javap --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}