{
  "plugin": "monty",
  "binary": "monty",
  "check": "which monty",
  "install_steps": [
    "brew install monty",
    "Verify: monty --version",
    "supercli plugins install ./plugins/monty --on-conflict replace --json"
  ]
}