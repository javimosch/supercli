{
  "plugin": "dbmate",
  "binary": "dbmate",
  "check": "which dbmate",
  "install_steps": ["brew install dbmate", "Verify: dbmate --version", "supercli plugins install ./plugins/dbmate --on-conflict replace --json"]
}
