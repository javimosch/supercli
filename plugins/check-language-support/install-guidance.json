{ "plugin": "check-language-support", "binary": "check-language-support", "check": "which check-language-support", "install_steps": ["apt-get install check-language-support 2>/dev/null || which check-language-support", "Verify: check-language-support --version", "supercli plugins install ./plugins/check-language-support --on-conflict replace --json"] }
