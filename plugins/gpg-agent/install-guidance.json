{
  "plugin": "gpg-agent",
  "binary": "gpg-agent",
  "check": "which gpg-agent",
  "install_steps": [
    "brew install gnupg",
    "Verify: gpg-agent --version",
    "supercli plugins install ./plugins/gpg-agent --on-conflict replace --json"
  ]
}
