{
  "plugin": "nss-pp",
  "binary": "nss-pp",
  "check": "which nss-pp",
  "install_steps": [
    "apt-get install nss-pp 2>/dev/null || which nss-pp",
    "Verify: nss-pp --version",
    "supercli plugins install ./plugins/nss-pp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}