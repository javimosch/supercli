{
  "name": "ampaths",
  "version": "0.1.0",
  "description": "CLI tool: ampaths",
  "source": "https://packages.debian.org/ampaths",
  "checks": [{ "type": "binary", "name": "ampaths" }],
  "install_guidance": {
    "plugin": "ampaths", "binary": "ampaths", "check": "which ampaths",
    "install_steps": ["apt-get install ampaths 2>/dev/null || which ampaths", "Verify: ampaths --version", "supercli plugins install ./plugins/ampaths --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ampaths", "resource": "_", "action": "_",
    "description": "Passthrough to ampaths CLI",
    "adapter": "process", "adapterConfig": { "command": "ampaths", "passthrough": true, "missingDependencyHelp": "Install ampaths: apt-get install ampaths" }, "args": []
  }]
}
