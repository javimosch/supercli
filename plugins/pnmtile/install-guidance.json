{
  "plugin": "pnmtile",
  "binary": "pnmtile",
  "check": "which pnmtile",
  "install_steps": [
    "apt-get install pnmtile 2>/dev/null || which pnmtile",
    "Verify: pnmtile --version",
    "supercli plugins install ./plugins/pnmtile --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}