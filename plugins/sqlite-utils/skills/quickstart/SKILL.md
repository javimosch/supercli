---
name: sqlite-utils
description: sqlite-utils — CLI utility for manipulating SQLite databases. Create, query, insert JSON/CSV, build full-text search indexes.
---
# sqlite-utils
sqlite-utils — CLI utility for manipulating SQLite databases. Create, query, insert JSON/CSV, build full-text search indexes.
