{
  "name": "ccx",
  "version": "0.1.0",
  "description": "CLI tool: ccx",
  "source": "https://packages.debian.org/ccx",
  "checks": [{ "type": "binary", "name": "ccx" }],
  "install_guidance": {
    "plugin": "ccx", "binary": "ccx", "check": "which ccx",
    "install_steps": ["apt-get install ccx 2>/dev/null || which ccx", "Verify: ccx --version", "supercli plugins install ./plugins/ccx --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ccx", "resource": "_", "action": "_",
    "description": "Passthrough to ccx CLI",
    "adapter": "process", "adapterConfig": { "command": "ccx", "passthrough": true, "missingDependencyHelp": "Install ccx: apt-get install ccx" }, "args": []
  }]
}
