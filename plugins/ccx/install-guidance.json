{ "plugin": "ccx", "binary": "ccx", "check": "which ccx", "install_steps": ["apt-get install ccx 2>/dev/null || which ccx", "Verify: ccx --version", "supercli plugins install ./plugins/ccx --on-conflict replace --json"] }
