{
  "plugin": "kustomize",
  "binary": "kustomize",
  "check": "which kustomize",
  "install_steps": [
    "brew install kustomize",
    "Verify: kustomize version",
    "supercli plugins install ./plugins/kustomize --on-conflict replace --json"
  ],
  "note": "Also available via: apt, pacman, scoop, or download from GitHub releases. Built into kubectl v1.14+."
}
