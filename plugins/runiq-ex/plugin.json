{
  "name": "runiq-ex",
  "version": "0.1.0",
  "description": "Extended runiq with additional deduplication features",
  "source": "https://github.com/whitfin/runiq",
  "checks": [{ "type": "binary", "name": "runiq" }],
  "install_guidance": {
    "plugin": "runiq-ex",
    "binary": "runiq",
    "check": "which runiq",
    "install_steps": ["cargo install runiq", "Verify: runiq --version", "supercli plugins install ./plugins/runiq-ex --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "runiq-ex",
    "resource": "_",
    "action": "_",
    "description": "Passthrough to runiq CLI",
    "adapter": "process",
    "adapterConfig": {
      "command": "runiq",
      "passthrough": true,
      "missingDependencyHelp": "Install runiq-ex: cargo install runiq"
    },
    "args": []
  }]
}
