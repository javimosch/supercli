{ "plugin": "boot", "binary": "boot", "check": "which boot", "install_steps": ["apt-get install boot", "Verify: boot --version", "supercli plugins install ./plugins/boot --on-conflict replace --json"] }
