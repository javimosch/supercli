{
  "plugin": "clib",
  "binary": "clib",
  "check": "which clib",
  "install_steps": [
    "brew install clib",
    "Verify: clib --version",
    "supercli plugins install ./plugins/clib --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}