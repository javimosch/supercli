{
  "plugin": "committed",
  "binary": "committed",
  "check": "which committed",
  "install_steps": [
    "brew install committed",
    "Verify: committed --version",
    "supercli plugins install ./plugins/committed --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}