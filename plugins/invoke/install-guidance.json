{
  "plugin": "invoke",
  "binary": "invoke",
  "check": "which invoke",
  "install_steps": [
    "# Install invoke",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/invoke-linux-amd64 -o /usr/local/bin/invoke",
    "chmod +x /usr/local/bin/invoke",
    "# Or via package manager, see pyinvoke.org"
  ]
}