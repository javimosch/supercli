{
  "plugin": "dee-gas",
  "binary": "dee-gas",
  "check": "which dee-gas",
  "install_steps": ["cargo install dee-gas", "Verify: dee-gas --version", "supercli plugins install ./plugins/dee-gas --on-conflict replace --json"]
}
