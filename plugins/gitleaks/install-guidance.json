{
  "plugin": "gitleaks",
  "binary": "gitleaks",
  "check": "which gitleaks",
  "install_steps": [
    "brew install gitleaks",
    "Verify: gitleaks --version",
    "supercli plugins install ./plugins/gitleaks --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/gitleaks/gitleaks/v8/cmd/gitleaks@latest. Supports GitHub Actions and pre-commit hooks. Can scan git history or individual files. Baseline feature allows ignoring known secrets."
}
