{
  "plugin": "chroma",
  "binary": "chroma",
  "check": "which chroma",
  "install_steps": [
    "go install github.com/alecthomas/chroma/cmd/chroma@latest",
    "Verify: chroma --version",
    "supercli plugins install ./plugins/chroma --on-conflict replace --json"
  ]
}