{ "plugin": "decrement", "binary": "decrement", "check": "which decrement", "install_steps": ["apt-get install decrement 2>/dev/null || which decrement", "Verify: decrement --version", "supercli plugins install ./plugins/decrement --on-conflict replace --json"] }
