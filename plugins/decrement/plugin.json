{
  "name": "decrement",
  "version": "0.1.0",
  "description": "CLI tool: decrement",
  "source": "https://packages.debian.org/decrement",
  "checks": [{ "type": "binary", "name": "decrement" }],
  "install_guidance": {
    "plugin": "decrement", "binary": "decrement", "check": "which decrement",
    "install_steps": ["apt-get install decrement 2>/dev/null || which decrement", "Verify: decrement --version", "supercli plugins install ./plugins/decrement --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "decrement", "resource": "_", "action": "_",
    "description": "Passthrough to decrement CLI",
    "adapter": "process", "adapterConfig": { "command": "decrement", "passthrough": true, "missingDependencyHelp": "Install decrement: apt-get install decrement" }, "args": []
  }]
}
