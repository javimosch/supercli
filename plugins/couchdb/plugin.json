{
  "name": "couchdb",
  "version": "0.1.0",
  "description": "CouchDB — Apache CouchDB NoSQL document database server",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "couchdb"
    }
  ],
  "install_guidance": {
    "plugin": "couchdb",
    "binary": "couchdb",
    "check": "which couchdb",
    "install_steps": [
      "brew install couchdb"
    ],
    "note": "Infrastructure tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "couchdb",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to couchdb CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "couchdb",
        "passthrough": true,
        "missingDependencyHelp": "Install couchdb: brew install couchdb"
      },
      "args": []
    }
  ]
}
