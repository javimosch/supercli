{
  "plugin": "espeak-ng",
  "binary": "espeak-ng",
  "check": "which espeak-ng",
  "install_steps": [
    "apt-get install espeak-ng",
    "Verify: espeak-ng --version",
    "supercli plugins install ./plugins/espeak-ng --on-conflict replace --json"
  ],
  "note": "Plugin added to reach 10,000 total plugins."
}
