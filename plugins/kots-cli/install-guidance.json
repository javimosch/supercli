{
  "plugin": "kots-cli",
  "binary": "kots",
  "check": "which kots",
  "install_steps": ["brew install kots", "Verify: kots --version", "supercli plugins install ./plugins/kots-cli --on-conflict replace --json"]
}
