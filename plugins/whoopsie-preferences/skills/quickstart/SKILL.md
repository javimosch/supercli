---
name: whoopsie-preferences
description: Use this skill when the user wants to use whoopsie-preferences, cli tool: whoopsie-preferences.
---

# whoopsie-preferences Plugin

CLI tool: whoopsie-preferences.

## Commands
- `whoopsie-preferences <resource> <action>` — Execute whoopsie-preferences commands
- `whoopsie-preferences self version` — Print whoopsie-preferences version
- `whoopsie-preferences _ _` — Passthrough to whoopsie-preferences CLI

## Usage Examples
- "whoopsie-preferences --help"
- "whoopsie-preferences self version"

## Installation
```bash
apt-get install whoopsie-preferences 2>/dev/null || which whoopsie-preferences
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
