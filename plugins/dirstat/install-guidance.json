{ "plugin": "dirstat", "binary": "dirstat", "check": "which dirstat", "install_steps": ["apt-get install dirstat 2>/dev/null || which dirstat", "Verify: dirstat --version", "supercli plugins install ./plugins/dirstat --on-conflict replace --json"] }
