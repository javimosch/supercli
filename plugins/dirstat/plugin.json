{
  "name": "dirstat",
  "version": "0.1.0",
  "description": "CLI tool: dirstat",
  "source": "https://packages.debian.org/dirstat",
  "checks": [{ "type": "binary", "name": "dirstat" }],
  "install_guidance": {
    "plugin": "dirstat", "binary": "dirstat", "check": "which dirstat",
    "install_steps": ["apt-get install dirstat 2>/dev/null || which dirstat", "Verify: dirstat --version", "supercli plugins install ./plugins/dirstat --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dirstat", "resource": "_", "action": "_",
    "description": "Passthrough to dirstat CLI",
    "adapter": "process", "adapterConfig": { "command": "dirstat", "passthrough": true, "missingDependencyHelp": "Install dirstat: apt-get install dirstat" }, "args": []
  }]
}
