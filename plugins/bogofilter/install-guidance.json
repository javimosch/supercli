{ "plugin": "bogofilter", "binary": "bogofilter", "check": "which bogofilter", "install_steps": ["apt-get install bogofilter 2>/dev/null || which bogofilter", "Verify: bogofilter --version", "supercli plugins install ./plugins/bogofilter --on-conflict replace --json"] }
