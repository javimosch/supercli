{
  "plugin": "recon",
  "binary": "recon",
  "check": "which recon",
  "install_steps": [
    "cargo install recon-cli",
    "Verify: recon --version",
    "supercli plugins install ./plugins/recon --on-conflict replace --json"
  ],
  "note": "Crate is published as recon-cli but the installed binary is recon. Homebrew also available: brew tap codedeviate/cli && brew install recon."
}
