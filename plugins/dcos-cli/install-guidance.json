{
  "plugin": "dcos-cli",
  "binary": "dcos",
  "check": "which dcos",
  "install_steps": [
    "brew install dcos-cli",
    "Verify: dcos --version",
    "supercli plugins install ./plugins/dcos-cli --on-conflict replace --json"
  ]
}