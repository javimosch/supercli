{
  "name": "dj",
  "version": "0.1.0",
  "description": "CLI tool: dj",
  "source": "https://packages.debian.org/dj",
  "checks": [{ "type": "binary", "name": "dj" }],
  "install_guidance": {
    "plugin": "dj", "binary": "dj", "check": "which dj",
    "install_steps": ["apt-get install dj 2>/dev/null || which dj", "Verify: dj --version", "supercli plugins install ./plugins/dj --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dj", "resource": "_", "action": "_",
    "description": "Passthrough to dj CLI",
    "adapter": "process", "adapterConfig": { "command": "dj", "passthrough": true, "missingDependencyHelp": "Install dj: apt-get install dj" }, "args": []
  }]
}
