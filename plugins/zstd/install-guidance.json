{
  "plugin": "zstd",
  "binary": "zstd",
  "check": "which zstd",
  "install_steps": [
    "brew install zstd",
    "Verify: zstd --version",
    "supercli plugins install ./plugins/zstd --on-conflict replace --json"
  ]
}
