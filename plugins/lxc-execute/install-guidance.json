{
  "plugin": "lxc-execute",
  "binary": "lxc-execute",
  "check": "which lxc-execute",
  "install_steps": [
    "apt-get install lxc-execute 2>/dev/null || which lxc-execute",
    "Verify: lxc-execute --version",
    "supercli plugins install ./plugins/lxc-execute --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}