{
  "plugin": "foo2zjs",
  "binary": "foo2zjs",
  "check": "which foo2zjs",
  "install_steps": [
    "apt-get install foo2zjs 2>/dev/null || which foo2zjs",
    "Verify: foo2zjs --version",
    "supercli plugins install ./plugins/foo2zjs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}