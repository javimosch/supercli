{
  "plugin": "nat",
  "binary": "nat",
  "check": "which nat",
  "install_steps": [
    "brew install nat",
    "Verify: nat --version",
    "supercli plugins install ./plugins/nat --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install natls. A beautifully formatted file and directory listing tool with icons, colors, and git integration. A modern replacement for ls with enhanced visuals."
}
