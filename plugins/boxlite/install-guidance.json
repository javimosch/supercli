{
  "plugin": "boxlite",
  "binary": "boxlite",
  "check": "which boxlite",
  "install_steps": [
    "brew install boxlite",
    "Verify: boxlite --version",
    "supercli plugins install ./plugins/boxlite --on-conflict replace --json"
  ]
}