{
  "plugin": "bandwhich",
  "binary": "bandwhich",
  "check": "which bandwhich",
  "install_steps": [
    "brew install bandwhich",
    "Verify: bandwhich --version",
    "supercli plugins install ./plugins/bandwhich --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install bandwhich. Terminal bandwidth utilization tool displaying network usage by process, connection, and remote host. May require root/admin privileges for packet capture on some systems."
}
