{
  "plugin": "carthage",
  "binary": "carthage",
  "check": "which carthage",
  "install_steps": [
    "brew install carthage",
    "Verify: carthage --version",
    "supercli plugins install ./plugins/carthage --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}