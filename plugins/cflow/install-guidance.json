{ "plugin": "cflow", "binary": "cflow", "check": "which cflow", "install_steps": ["apt-get install cflow 2>/dev/null || which cflow", "Verify: cflow --version", "supercli plugins install ./plugins/cflow --on-conflict replace --json"] }
