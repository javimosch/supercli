{
  "plugin": "dot",
  "binary": "dot",
  "check": "which dot",
  "install_steps": [
    "# Install dot",
    "# See graphviz.org",
    "# apt: sudo apt-get install dot",
    "# brew: brew install dot",
    "# cargo: cargo install dot",
    "# npm: npm install -g dot"
  ]
}