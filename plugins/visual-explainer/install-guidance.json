{
  "plugin": "visual-explainer",
  "binary": "visual-explainer",
  "check": "which visual-explainer",
  "install_steps": [
    "brew install visual-explainer",
    "Verify: visual-explainer --version",
    "supercli plugins install ./plugins/visual-explainer --on-conflict replace --json"
  ]
}