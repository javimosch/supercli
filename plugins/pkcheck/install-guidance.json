{
  "plugin": "pkcheck",
  "binary": "pkcheck",
  "check": "which pkcheck",
  "install_steps": [
    "apt-get install pkcheck 2>/dev/null || which pkcheck",
    "Verify: pkcheck --version",
    "supercli plugins install ./plugins/pkcheck --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}