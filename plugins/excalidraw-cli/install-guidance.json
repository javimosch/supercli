{
  "plugin": "excalidraw-cli",
  "binary": "excalidraw-cli",
  "check": "which excalidraw-cli",
  "install_steps": [
    "npm install -g @swiftlysingh/excalidraw-cli",
    "Verify: excalidraw-cli --version",
    "supercli plugins install ./plugins/excalidraw-cli --on-conflict replace --json"
  ],
  "note": "Requires Node >=20.19.0. Also installable via Homebrew: brew tap swiftlysingh/tap && brew install excalidraw-cli. The CLI uses a text-based DSL to create flowcharts — perfect for AI agents to generate diagrams programmatically."
}
