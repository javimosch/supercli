{
  "plugin": "spacer",
  "binary": "spacer",
  "check": "which spacer",
  "install_steps": [
    "brew install spacer",
    "Or: cargo install spacer",
    "Verify: spacer --version",
    "supercli plugins install ./plugins/spacer --on-conflict replace --json"
  ],
  "note": "Spacer monitors STDOUT and inserts a blank line after output stops for a specified duration. Use |& instead of | to monitor both STDERR and STDOUT."
}
