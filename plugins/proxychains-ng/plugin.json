{
  "name": "proxychains-ng",
  "version": "0.1.0",
  "description": "Tool to redirect TCP connections through proxy servers",
  "source": "shell",
  "checks": [{ "type": "binary", "name": "proxychains-ng" }],
  "install_guidance": {
    "plugin": "proxychains-ng",
    "binary": "proxychains-ng",
    "check": "which proxychains-ng",
    "install_steps": ["./configure && make && sudo make install", "Verify: proxychains-ng --version", "supercli plugins install ./plugins/proxychains-ng --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "proxychains-ng",
    "resource": "_",
    "action": "_",
    "description": "Passthrough to proxychains-ng CLI",
    "adapter": "process",
    "adapterConfig": {
      "command": "proxychains-ng",
      "passthrough": true,
      "missingDependencyHelp": "Install proxychains-ng: ./configure && make && sudo make install"
    },
    "args": []
  }]
}
