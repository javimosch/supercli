{
  "plugin": "ghtkn",
  "binary": "ghtkn",
  "check": "ghtkn --help",
  "install_steps": [
    "go install github.com/suzuki-shunsuke/ghtkn@latest",
    "Verify: ghtkn --help",
    "supercli plugins install ./plugins/ghtkn --on-conflict replace --json"
  ],
  "note": "Requires GitHub App with Device Flow enabled. Only Client ID needed (not secret). Tokens stored in OS keyring."
}