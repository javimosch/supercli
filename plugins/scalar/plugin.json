{
  "name": "scalar",
  "version": "1.0.0",
  "description": "scalar \u2014 scalar \u2014 CLI utility",
  "source": "/usr/bin/scalar",
  "checks": [
    {
      "type": "binary",
      "name": "scalar"
    }
  ],
  "install_guidance": {
    "plugin": "scalar",
    "binary": "scalar",
    "check": "which scalar",
    "install_steps": [
      "# Install scalar:",
      "# apt: sudo apt-get install scalar",
      "# brew: brew install scalar",
      "# cargo: cargo install scalar",
      "# npm: npm install -g scalar",
      "# pip: pip install scalar",
      "# or download from /usr/bin/scalar"
    ]
  },
  "commands": [
    {
      "namespace": "scalar",
      "resource": "scalar",
      "action": "run",
      "description": "Run scalar",
      "adapter": "process",
      "adapterConfig": {
        "command": "scalar",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install scalar from /usr/bin/scalar"
      },
      "args": []
    }
  ]
}