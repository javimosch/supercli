{
  "plugin": "scalar",
  "binary": "scalar",
  "check": "which scalar",
  "install_steps": [
    "# Install scalar",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/scalar-linux-amd64 -o /usr/local/bin/scalar",
    "chmod +x /usr/local/bin/scalar",
    "# Or via package manager, see /usr/bin/scalar"
  ]
}