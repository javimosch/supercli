---
name: bingrep
description: Use this skill when the user wants to use bingrep, cross-platform binary grep.
---

# bingrep Plugin

Cross-platform binary grep.

## Commands
- `bingrep <resource> <action>` — Execute bingrep commands
- `bingrep self version` — Print bingrep version
- `bingrep _ _` — Passthrough to bingrep CLI

## Usage Examples
- "bingrep --help"
- "bingrep self version"

## Installation
```bash
cargo install bingrep
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- rust based
