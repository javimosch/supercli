{
  "plugin": "lab",
  "binary": "lab",
  "check": "which lab",
  "install_steps": [
    "brew install lab",
    "Verify: lab --version",
    "supercli plugins install ./plugins/lab --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/zaquestion/lab/cmd/lab@latest. A hub-like CLI for GitLab. Requires GitLab access token. Supports repositories, issues, merge requests, and pipelines."
}
