{
  "plugin": "hd",
  "binary": "hd",
  "check": "which hd",
  "install_steps": [
    "apt-get install hd 2>/dev/null || which hd",
    "Verify: hd --version",
    "supercli plugins install ./plugins/hd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}