{
  "plugin": "ipmaddr",
  "binary": "ipmaddr",
  "check": "which ipmaddr",
  "install_steps": [
    "apt-get install ipmaddr 2>/dev/null || which ipmaddr",
    "Verify: ipmaddr --version",
    "supercli plugins install ./plugins/ipmaddr --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}