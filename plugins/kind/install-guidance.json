{
  "plugin": "kind",
  "binary": "kind",
  "check": "which kind",
  "install_steps": [
    "brew install kind",
    "Verify: kind --version",
    "supercli plugins install ./plugins/kind --on-conflict replace --json"
  ]
}
