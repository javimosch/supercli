{
  "plugin": "dee-stash",
  "binary": "dee-stash",
  "check": "which dee-stash",
  "install_steps": ["cargo install dee-stash", "Verify: dee-stash --version", "supercli plugins install ./plugins/dee-stash --on-conflict replace --json"]
}
