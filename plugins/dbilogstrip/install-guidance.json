{ "plugin": "dbilogstrip", "binary": "dbilogstrip", "check": "which dbilogstrip", "install_steps": ["apt-get install dbilogstrip 2>/dev/null || which dbilogstrip", "Verify: dbilogstrip --version", "supercli plugins install ./plugins/dbilogstrip --on-conflict replace --json"] }
