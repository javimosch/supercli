{
  "plugin": "fasd",
  "binary": "fasd",
  "check": "which fasd",
  "install_steps": [
    "brew install fasd (macOS)",
    "apt-get install fasd (Debian/Ubuntu)",
    "sudo dnf install fasd (Fedora)",
    "sudo pacman -S fasd (Arch Linux)",
    "Verify: fasd --version"
  ],
  "note": "fasd is a shell integration tool. After installation, add 'eval \"$(fasd --init auto)\"' to your shell config (.bashrc, .zshrc, etc.) to enable the 'z' and 'f' aliases."
}
