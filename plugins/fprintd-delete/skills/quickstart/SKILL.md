---
name: fprintd-delete
description: Use this skill when the user wants to use fprintd-delete, cli tool: fprintd-delete.
---

# fprintd-delete Plugin

CLI tool: fprintd-delete.

## Commands
- `fprintd-delete <resource> <action>` — Execute fprintd-delete commands
- `fprintd-delete self version` — Print fprintd-delete version
- `fprintd-delete _ _` — Passthrough to fprintd-delete CLI

## Usage Examples
- "fprintd-delete --help"
- "fprintd-delete self version"

## Installation
```bash
apt-get install fprintd-delete 2>/dev/null || which fprintd-delete
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
