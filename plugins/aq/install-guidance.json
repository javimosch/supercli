{
  "plugin": "aq",
  "binary": "aq",
  "check": "which aq",
  "install_steps": [
    "pip install aq",
    "Verify: aq --version",
    "supercli plugins install ./plugins/aq --on-conflict replace --json"
  ]
}