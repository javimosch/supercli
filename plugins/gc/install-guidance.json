{
  "plugin": "gc",
  "binary": "gc",
  "check": "which gc",
  "install_steps": [
    "apt-get install gc 2>/dev/null || which gc",
    "Verify: gc --version",
    "supercli plugins install ./plugins/gc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}