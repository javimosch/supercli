{
  "plugin": "aspect-cli",
  "binary": "aspect",
  "check": "which aspect",
  "install_steps": [
    "cargo install aspect-cli",
    "Verify: aspect --help",
    "supercli plugins install ./plugins/aspect-cli --on-conflict replace --json"
  ],
  "note": "aspect-cli provides Bazel CLI improvements"
}
