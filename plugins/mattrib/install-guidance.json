{
  "plugin": "mattrib",
  "binary": "mattrib",
  "check": "which mattrib",
  "install_steps": [
    "apt-get install mattrib 2>/dev/null || which mattrib",
    "Verify: mattrib --version",
    "supercli plugins install ./plugins/mattrib --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}