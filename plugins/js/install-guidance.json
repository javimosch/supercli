{
  "plugin": "js",
  "binary": "js",
  "check": "which js",
  "install_steps": [
    "apt-get install js 2>/dev/null || which js",
    "Verify: js --version",
    "supercli plugins install ./plugins/js --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}