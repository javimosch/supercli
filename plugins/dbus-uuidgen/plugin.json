{
  "name": "dbus-uuidgen",
  "version": "1.0.0",
  "description": "dbus-uuidgen \u2014 dbus-uuidgen \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dbus-uuidgen"
    }
  ],
  "install_guidance": {
    "plugin": "dbus-uuidgen",
    "binary": "dbus-uuidgen",
    "check": "which dbus-uuidgen",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dbus-uuidgen",
      "resource": "run",
      "action": "exec",
      "description": "dbus-uuidgen \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dbus-uuidgen",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}