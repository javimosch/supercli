{
  "plugin": "fkill",
  "binary": "fkill",
  "check": "fkill --version",
  "install_steps": [
    "npm install --global fkill-cli"
  ],
  "note": "Requires Node.js. Works on macOS, Linux, and Windows."
}
