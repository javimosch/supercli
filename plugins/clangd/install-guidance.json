{ "plugin": "clangd", "binary": "clangd", "check": "which clangd", "install_steps": ["apt-get install clangd 2>/dev/null || which clangd", "Verify: clangd --version", "supercli plugins install ./plugins/clangd --on-conflict replace --json"] }
