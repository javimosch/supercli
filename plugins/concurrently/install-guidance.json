{
  "plugin": "concurrently",
  "binary": "concurrently",
  "check": "which concurrently",
  "install_steps": [
    "npm install -g concurrently",
    "Verify: concurrently --version",
    "supercli plugins install ./plugins/concurrently --on-conflict replace --json"
  ],
  "note": "concurrently runs multiple commands concurrently with prefixed output for easy tracking"
}
