{
  "plugin": "apiark",
  "binary": "apiark",
  "check": "which apiark",
  "install_steps": ["git clone and install", "Verify: apiark --version", "supercli plugins install ./plugins/apiark --on-conflict replace --json"]
}
