{
  "name": "ramda-cli",
  "version": "0.1.0",
  "description": "ramda-cli — functional-style JSON/data processing pipeline using Ramda.js on the command line",
  "source": "https://github.com/raine/ramda-cli",
  "checks": [
    { "type": "binary", "name": "ramda" }
  ],
  "commands": [
    {
      "namespace": "ramda-cli",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to ramda CLI — process JSON/CSV/TSV data with Ramda functional expressions",
      "adapter": "process",
      "adapterConfig": {
        "command": "ramda",
        "passthrough": true,
        "missingDependencyHelp": "Install ramda-cli: npm install -g ramda-cli"
      },
      "args": []
    }
  ]
}
