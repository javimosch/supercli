{
  "plugin": "tg-cli",
  "binary": "tg",
  "check": "which tg",
  "install_steps": [
    "uv tool install kabi-tg-cli",
    "Verify: tg --version",
    "supercli plugins install ./plugins/tg-cli --on-conflict replace --json"
  ],
  "note": "Requires Telegram API credentials. Set TG_API_ID and TG_API_HASH env vars, or uses Telegram Desktop default (api_id=2040). Requires Python 3.10+ and uv/pipx."
}
