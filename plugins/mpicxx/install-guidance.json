{
  "plugin": "mpicxx",
  "binary": "mpicxx",
  "check": "which mpicxx",
  "install_steps": [
    "apt-get install mpicxx 2>/dev/null || which mpicxx",
    "Verify: mpicxx --version",
    "supercli plugins install ./plugins/mpicxx --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}