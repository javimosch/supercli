{
  "plugin": "autopilot",
  "binary": "autopilot",
  "check": "which autopilot",
  "install_steps": ["npm install -g autopilot-cli", "Verify: autopilot --version", "supercli plugins install ./plugins/autopilot --on-conflict replace --json"]
}
