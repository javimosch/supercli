{
  "plugin": "dtctl",
  "binary": "dtctl",
  "check": "which dtctl",
  "install_steps": [
    "go install github.com/dynatrace-oss/dtctl@latest",
    "Verify: dtctl --version",
    "supercli plugins install ./plugins/dtctl --on-conflict replace --json"
  ]
}