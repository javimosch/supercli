{
  "plugin": "gcov-8",
  "binary": "gcov-8",
  "check": "which gcov-8",
  "install_steps": [
    "apt-get install gcov-8 2>/dev/null || which gcov-8",
    "Verify: gcov-8 --version",
    "supercli plugins install ./plugins/gcov-8 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}