{
  "plugin": "gron",
  "binary": "gron",
  "check": "which gron",
  "install_steps": [
    "brew install gron",
    "Verify: gron --version",
    "supercli plugins install ./plugins/gron --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/tomnomnom/gron@latest. Can read from files, URLs, or stdin. Converts JSON to path=value format for easy grep/sed/awk processing. Can convert back to JSON with --ungron."
}
