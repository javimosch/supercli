{
  "plugin": "gron",
  "binary": "gron",
  "check": "which gron",
  "install_steps": ["brew install gron", "Verify: gron --version", "supercli plugins install ./plugins/gron --on-conflict replace --json"]
}
