{
  "plugin": "gcloud",
  "binary": "gcloud",
  "check": "which gcloud",
  "install_steps": [
    "brew install gcloud",
    "Verify: gcloud --version",
    "supercli plugins install ./plugins/gcloud --on-conflict replace --json"
  ]
}