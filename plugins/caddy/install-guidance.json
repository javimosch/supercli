{
  "plugin": "caddy",
  "binary": "caddy",
  "check": "which caddy",
  "install_steps": [
    "brew install caddy",
    "Verify: caddy --version",
    "supercli plugins install ./plugins/caddy --on-conflict replace --json"
  ]
}