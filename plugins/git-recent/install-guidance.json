{
  "plugin": "git-recent",
  "binary": "git-recent",
  "check": "which git-recent",
  "install_steps": ["npm install -g git-recent", "Verify: git-recent --version", "supercli plugins install ./plugins/git-recent --on-conflict replace --json"]
}
