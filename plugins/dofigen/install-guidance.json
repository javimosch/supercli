{
  "plugin": "dofigen",
  "binary": "dofigen",
  "check": "which dofigen",
  "install_steps": [
    "cargo install dofigen",
    "Verify: dofigen --version",
    "supercli plugins install ./plugins/dofigen --on-conflict replace --json"
  ],
  "note": "Also available via npm: npm install -g dofigen, or direct download from GitHub releases."
}
