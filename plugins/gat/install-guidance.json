{
  "plugin": "gat",
  "binary": "gat",
  "check": "which gat",
  "install_steps": [
    "brew install gat",
    "Verify: gat --version",
    "supercli plugins install ./plugins/gat --on-conflict replace --json"
  ]
}