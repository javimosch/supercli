{
  "plugin": "spectral",
  "binary": "spectral",
  "check": "which spectral",
  "install_steps": [
    "npm install -g @stoplight/spectral-cli",
    "Verify: spectral --version",
    "supercli plugins install ./plugins/spectral --on-conflict replace --json"
  ]
}
