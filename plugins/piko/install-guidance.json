{
  "plugin": "piko",
  "binary": "piko",
  "check": "which piko",
  "install_steps": [
    "pip install piko",
    "Verify: piko --version",
    "supercli plugins install ./plugins/piko --on-conflict replace --json"
  ],
  "note": "Language: Java. Stars: 3845"
}