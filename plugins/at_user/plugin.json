{
  "name": "at_user",
  "version": "0.1.0",
  "description": "CLI tool: at_user",
  "source": "https://packages.debian.org/at_user",
  "checks": [{ "type": "binary", "name": "at_user" }],
  "install_guidance": {
    "plugin": "at_user", "binary": "at_user", "check": "which at_user",
    "install_steps": ["apt-get install at_user 2>/dev/null || which at_user", "Verify: at_user --version", "supercli plugins install ./plugins/at_user --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "at_user", "resource": "_", "action": "_",
    "description": "Passthrough to at_user CLI",
    "adapter": "process", "adapterConfig": { "command": "at_user", "passthrough": true, "missingDependencyHelp": "Install at_user: apt-get install at_user" }, "args": []
  }]
}
