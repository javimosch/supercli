{
  "plugin": "venom",
  "binary": "venom",
  "check": "which venom",
  "install_steps": [
    "brew install venom",
    "Verify: venom --version",
    "supercli plugins install ./plugins/venom --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/ovh/venom/cmd/venom@latest. Runs integration tests with multiple executors including script, HTTP, web, imap, and more. Supports assertions and variable substitution. Great for CI/CD pipelines."
}
