{
  "name": "dpkg-name",
  "version": "1.0.0",
  "description": "dpkg-name \u2014 dpkg-name \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-name"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-name",
    "binary": "dpkg-name",
    "check": "which dpkg-name",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-name",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-name \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-name",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}