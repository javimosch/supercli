{
  "plugin": "jqn",
  "binary": "jqn",
  "check": "which jqn",
  "install_steps": ["npm install -g jqn", "Verify: jqn --version", "supercli plugins install ./plugins/jqn --on-conflict replace --json"]
}
