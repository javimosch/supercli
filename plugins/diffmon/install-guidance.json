{ "plugin": "diffmon", "binary": "diffmon", "check": "which diffmon", "install_steps": ["apt-get install diffmon 2>/dev/null || which diffmon", "Verify: diffmon --version", "supercli plugins install ./plugins/diffmon --on-conflict replace --json"] }
