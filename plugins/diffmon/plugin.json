{
  "name": "diffmon",
  "version": "0.1.0",
  "description": "CLI tool: diffmon",
  "source": "https://packages.debian.org/diffmon",
  "checks": [{ "type": "binary", "name": "diffmon" }],
  "install_guidance": {
    "plugin": "diffmon", "binary": "diffmon", "check": "which diffmon",
    "install_steps": ["apt-get install diffmon 2>/dev/null || which diffmon", "Verify: diffmon --version", "supercli plugins install ./plugins/diffmon --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "diffmon", "resource": "_", "action": "_",
    "description": "Passthrough to diffmon CLI",
    "adapter": "process", "adapterConfig": { "command": "diffmon", "passthrough": true, "missingDependencyHelp": "Install diffmon: apt-get install diffmon" }, "args": []
  }]
}
