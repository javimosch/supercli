{
  "plugin": "zq",
  "binary": "zq",
  "check": "which zq",
  "install_steps": [
    "brew install zed",
    "Verify: zq --version",
    "supercli plugins install ./plugins/zq --on-conflict replace --json"
  ]
}