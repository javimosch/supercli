{
  "plugin": "stripe",
  "binary": "stripe",
  "check": "which stripe",
  "install_steps": [
    "brew install stripe",
    "Verify: stripe --version",
    "supercli plugins install ./plugins/stripe --on-conflict replace --json"
  ]
}