{
  "plugin": "lue",
  "binary": "lue",
  "check": "which lue",
  "install_steps": ["cargo install lue", "Verify: lue --version", "supercli plugins install ./plugins/lue --on-conflict replace --json"]
}
