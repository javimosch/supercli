{
  "name": "g++",
  "version": "0.1.0",
  "description": "G++ — GNU C++ compiler for compiling C++ programs",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "g++"
    }
  ],
  "install_guidance": {
    "plugin": "g++",
    "binary": "g++",
    "check": "which g++",
    "install_steps": [
      "brew install gcc",
      "supercli plugins install ./plugins/g++ --on-conflict replace --json"
    ],
    "note": "Development tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "g++",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to g++ CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "g++",
        "passthrough": true,
        "missingDependencyHelp": "Install g++: brew install gcc"
      },
      "args": []
    }
  ]
}
