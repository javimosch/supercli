{ "plugin": "cpuinfo", "binary": "cpuinfo", "check": "which cpuinfo", "install_steps": ["apt-get install cpuinfo 2>/dev/null || which cpuinfo", "Verify: cpuinfo --version", "supercli plugins install ./plugins/cpuinfo --on-conflict replace --json"] }
