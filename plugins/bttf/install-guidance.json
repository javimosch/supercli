{
  "plugin": "bttf",
  "binary": "bttf",
  "check": "which bttf",
  "install_steps": [
    "brew install bttf",
    "Verify: bttf --version",
    "supercli plugins install ./plugins/bttf --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}