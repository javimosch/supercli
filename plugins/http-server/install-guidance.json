{
  "plugin": "http-server",
  "binary": "http-server",
  "check": "which http-server",
  "install_steps": [
    "npm install --global http-server",
    "Verify: http-server --version",
    "supercli plugins install ./plugins/http-server --on-conflict replace --json"
  ],
  "note": "Http-server is a simple static HTTP server. Defaults to port 8080. Supports HTTPS, CORS, gzip/brotli compression, caching control, and custom headers."
}
