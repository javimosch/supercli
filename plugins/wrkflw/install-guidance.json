{
  "plugin": "wrkflw",
  "binary": "wrkflw",
  "check": "which wrkflw",
  "install_steps": [
    "cargo install wrkflw",
    "Verify: wrkflw --version",
    "supercli plugins install ./plugins/wrkflw --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (brew install wrkflw). Requires Docker or Podman for local workflow execution."
}
