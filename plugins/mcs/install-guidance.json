{ "plugin": "mcs", "binary": "mcs", "check": "which mcs", "install_steps": ["apt-get install mono-mcs", "Verify: mcs --version", "supercli plugins install ./plugins/mcs --on-conflict replace --json"] }
