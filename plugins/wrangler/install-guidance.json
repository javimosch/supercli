{
  "plugin": "wrangler",
  "binary": "wrangler",
  "check": "which wrangler",
  "install_steps": [
    "npm install -g wrangler",
    "Verify: wrangler --version",
    "Authenticate: wrangler login",
    "Alternative: export CLOUDFLARE_API_TOKEN=...",
    "supercli plugins install ./plugins/wrangler --on-conflict replace --json"
  ],
  "note": "Also available via: npx wrangler. Requires a Cloudflare account (free tier available for Workers). Use --json flag for structured output on supported commands."
}
