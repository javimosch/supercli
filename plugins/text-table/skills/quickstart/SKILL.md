---
name: text-table
description: text-table — format pipe-delimited text into aligned ASCII tables. Reads stdin, outputs table with borders.
---
# text-table
text-table — format pipe-delimited text into aligned ASCII tables. Reads stdin, outputs table with borders.
