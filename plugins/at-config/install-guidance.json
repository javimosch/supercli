{ "plugin": "at-config", "binary": "at-config", "check": "which at-config", "install_steps": ["apt-get install at-config 2>/dev/null || which at-config", "Verify: at-config --version", "supercli plugins install ./plugins/at-config --on-conflict replace --json"] }
