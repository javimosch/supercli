{ "plugin": "ccc", "binary": "ccc", "check": "which ccc", "install_steps": ["apt-get install ccc 2>/dev/null || which ccc", "Verify: ccc --version", "supercli plugins install ./plugins/ccc --on-conflict replace --json"] }
