{
  "name": "ccc",
  "version": "0.1.0",
  "description": "CLI tool: ccc",
  "source": "https://packages.debian.org/ccc",
  "checks": [{ "type": "binary", "name": "ccc" }],
  "install_guidance": {
    "plugin": "ccc", "binary": "ccc", "check": "which ccc",
    "install_steps": ["apt-get install ccc 2>/dev/null || which ccc", "Verify: ccc --version", "supercli plugins install ./plugins/ccc --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ccc", "resource": "_", "action": "_",
    "description": "Passthrough to ccc CLI",
    "adapter": "process", "adapterConfig": { "command": "ccc", "passthrough": true, "missingDependencyHelp": "Install ccc: apt-get install ccc" }, "args": []
  }]
}
