{ "plugin": "dumpcap", "binary": "dumpcap", "check": "which dumpcap", "install_steps": ["apt-get install wireshark-common", "Verify: dumpcap --version", "supercli plugins install ./plugins/dumpcap --on-conflict replace --json"] }
