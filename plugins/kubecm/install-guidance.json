{
  "plugin": "kubecm",
  "binary": "kubecm",
  "check": "which kubecm",
  "install_steps": [
    "brew install kubecm",
    "Verify: kubecm version",
    "supercli plugins install ./plugins/kubecm --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/sunny0826/kubecm/cmd/kubecm@latest. A Kubeconfig manager for multiple Kubernetes clusters. Switch contexts, merge kubeconfigs, and manage K8s configurations easily."
}
