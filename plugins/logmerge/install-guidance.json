{
  "plugin": "logmerge",
  "binary": "logmerge",
  "check": "which logmerge",
  "install_steps": [
    "cargo install logmerge",
    "Verify: logmerge --version",
    "supercli plugins install ./plugins/logmerge --on-conflict replace --json"
  ]
}
