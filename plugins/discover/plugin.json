{
  "name": "discover",
  "version": "0.1.0",
  "description": "CLI tool: discover",
  "source": "https://packages.debian.org/discover",
  "checks": [{ "type": "binary", "name": "discover" }],
  "install_guidance": {
    "plugin": "discover", "binary": "discover", "check": "which discover",
    "install_steps": ["apt-get install discover 2>/dev/null || which discover", "Verify: discover --version", "supercli plugins install ./plugins/discover --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "discover", "resource": "_", "action": "_",
    "description": "Passthrough to discover CLI",
    "adapter": "process", "adapterConfig": { "command": "discover", "passthrough": true, "missingDependencyHelp": "Install discover: apt-get install discover" }, "args": []
  }]
}
