{ "plugin": "discover", "binary": "discover", "check": "which discover", "install_steps": ["apt-get install discover 2>/dev/null || which discover", "Verify: discover --version", "supercli plugins install ./plugins/discover --on-conflict replace --json"] }
