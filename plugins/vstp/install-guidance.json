{
  "plugin": "vstp",
  "binary": "vstp",
  "check": "which vstp",
  "install_steps": [
    "apt-get install vstp 2>/dev/null || which vstp",
    "Verify: vstp --version",
    "supercli plugins install ./plugins/vstp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}