{
  "plugin": "beyla",
  "binary": "beyla",
  "check": "which beyla",
  "install_steps": ["brew install beyla", "Verify: beyla --version", "supercli plugins install ./plugins/beyla --on-conflict replace --json"]
}
