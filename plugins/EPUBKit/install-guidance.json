{
  "plugin": "EPUBKit",
  "binary": "EPUBKit",
  "check": "which EPUBKit",
  "install_steps": ["swift build -c release", "Verify: EPUBKit --version", "supercli plugins install ./plugins/EPUBKit --on-conflict replace --json"]
}
