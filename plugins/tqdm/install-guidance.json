{
  "plugin": "tqdm",
  "binary": "tqdm",
  "check": "which tqdm",
  "install_steps": [
    "pip install tqdm",
    "Verify: tqdm --version",
    "supercli plugins install ./plugins/tqdm --on-conflict replace --json"
  ],
  "note": "Also available via conda: conda install -c conda-forge tqdm. The CLI mode pipes stdin through a progress bar: cat bigfile | tqdm --bytes | process. Over 100M downloads/month. Supports nested progress bars, custom bar formats, manual updates, and integration with pandas, IPython/Jupyter, and keras."
}
