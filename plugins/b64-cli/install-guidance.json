{
  "plugin": "b64-cli",
  "binary": "b64",
  "check": "which b64",
  "install_steps": [
    "npm install -g b64-cli",
    "Verify: b64 --help",
    "supercli plugins install ./plugins/b64-cli --on-conflict replace --json"
  ],
  "note": "B64-cli provides simple base64 encoding and decoding for files. Use -d flag for decoding. Can work with stdin/stdout for pipeline operations."
}
