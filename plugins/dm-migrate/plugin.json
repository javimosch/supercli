{
  "name": "dm-migrate",
  "version": "0.1.0",
  "description": "CLI tool: dm-migrate",
  "source": "https://packages.debian.org/dm-migrate",
  "checks": [{ "type": "binary", "name": "dm-migrate" }],
  "install_guidance": {
    "plugin": "dm-migrate", "binary": "dm-migrate", "check": "which dm-migrate",
    "install_steps": ["apt-get install dm-migrate 2>/dev/null || which dm-migrate", "Verify: dm-migrate --version", "supercli plugins install ./plugins/dm-migrate --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-migrate", "resource": "_", "action": "_",
    "description": "Passthrough to dm-migrate CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-migrate", "passthrough": true, "missingDependencyHelp": "Install dm-migrate: apt-get install dm-migrate" }, "args": []
  }]
}
