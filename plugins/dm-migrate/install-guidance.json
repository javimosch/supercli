{ "plugin": "dm-migrate", "binary": "dm-migrate", "check": "which dm-migrate", "install_steps": ["apt-get install dm-migrate 2>/dev/null || which dm-migrate", "Verify: dm-migrate --version", "supercli plugins install ./plugins/dm-migrate --on-conflict replace --json"] }
