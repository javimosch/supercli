{
  "name": "acpidbg",
  "version": "0.1.0",
  "description": "CLI tool: acpidbg",
  "source": "https://packages.debian.org/acpidbg",
  "checks": [{ "type": "binary", "name": "acpidbg" }],
  "install_guidance": {
    "plugin": "acpidbg", "binary": "acpidbg", "check": "which acpidbg",
    "install_steps": ["apt-get install acpidbg 2>/dev/null || which acpidbg", "Verify: acpidbg --version", "supercli plugins install ./plugins/acpidbg --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acpidbg", "resource": "_", "action": "_",
    "description": "Passthrough to acpidbg CLI",
    "adapter": "process", "adapterConfig": { "command": "acpidbg", "passthrough": true, "missingDependencyHelp": "Install acpidbg: apt-get install acpidbg" }, "args": []
  }]
}
