{
  "plugin": "firmware",
  "binary": "firmware",
  "check": "which firmware",
  "install_steps": [
    "pip install firmware",
    "Verify: firmware --version",
    "supercli plugins install ./plugins/firmware --on-conflict replace --json"
  ],
  "note": "Language: C++. Stars: 5804"
}