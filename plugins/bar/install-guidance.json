{
  "plugin": "bar",
  "binary": "bar",
  "check": "which bar",
  "install_steps": ["sudo apt install bar", "Verify: bar --version", "supercli plugins install ./plugins/bar --on-conflict replace --json"]
}
