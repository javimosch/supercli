{
  "plugin": "unxz",
  "binary": "unxz",
  "check": "which unxz",
  "install_steps": [
    "apt-get install unxz 2>/dev/null || which unxz",
    "Verify: unxz --version",
    "supercli plugins install ./plugins/unxz --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}