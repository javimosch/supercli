{
  "plugin": "treefmt",
  "binary": "treefmt",
  "check": "which treefmt",
  "install_steps": [
    "brew install treefmt",
    "Verify: treefmt --version",
    "supercli plugins install ./plugins/treefmt --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/numtide/treefmt/cmd/treefmt@latest. Runs multiple formatters on a codebase with a single command. Supports many formatters including black, prettier, gofmt, and more. Configurable via treefmt.toml."
}
