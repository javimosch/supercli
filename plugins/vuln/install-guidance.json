{ "plugin": "vuln", "binary": "vuln", "check": "which vuln", "install_steps": ["apt-get install vuln", "Verify: vuln --version", "supercli plugins install ./plugins/vuln --on-conflict replace --json"] }
