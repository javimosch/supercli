{
  "plugin": "psnup",
  "binary": "psnup",
  "check": "which psnup",
  "install_steps": [
    "apt-get install psnup 2>/dev/null || which psnup",
    "Verify: psnup --version",
    "supercli plugins install ./plugins/psnup --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}