{
  "plugin": "bolt.new",
  "binary": "bolt.new",
  "check": "which bolt.new",
  "install_steps": [
    "pip install bolt.new",
    "Verify: bolt.new --version",
    "supercli plugins install ./plugins/bolt.new --on-conflict replace --json"
  ],
  "note": "Language: TypeScript. Stars: 16401"
}