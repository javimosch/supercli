{
  "plugin": "gdk-pixbuf-csource",
  "binary": "gdk-pixbuf-csource",
  "check": "which gdk-pixbuf-csource",
  "install_steps": [
    "apt-get install gdk-pixbuf-csource 2>/dev/null || which gdk-pixbuf-csource",
    "Verify: gdk-pixbuf-csource --version",
    "supercli plugins install ./plugins/gdk-pixbuf-csource --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}