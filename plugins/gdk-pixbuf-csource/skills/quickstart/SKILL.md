---
name: gdk-pixbuf-csource
description: Use this skill when the user wants to use gdk-pixbuf-csource, cli tool: gdk-pixbuf-csource.
---

# gdk-pixbuf-csource Plugin

CLI tool: gdk-pixbuf-csource.

## Commands
- `gdk-pixbuf-csource <resource> <action>` — Execute gdk-pixbuf-csource commands
- `gdk-pixbuf-csource self version` — Print gdk-pixbuf-csource version
- `gdk-pixbuf-csource _ _` — Passthrough to gdk-pixbuf-csource CLI

## Usage Examples
- "gdk-pixbuf-csource --help"
- "gdk-pixbuf-csource self version"

## Installation
```bash
apt-get install gdk-pixbuf-csource 2>/dev/null || which gdk-pixbuf-csource
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
