{
  "plugin": "rbw",
  "binary": "rbw",
  "check": "which rbw",
  "install_steps": ["cargo install rbw", "Verify: rbw --version", "supercli plugins install ./plugins/rbw --on-conflict replace --json"]
}
