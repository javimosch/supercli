{ "plugin": "agent", "binary": "agent", "check": "which agent", "install_steps": ["apt-get install agent 2>/dev/null || which agent", "Verify: agent --version", "supercli plugins install ./plugins/agent --on-conflict replace --json"] }
