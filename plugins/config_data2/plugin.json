{
  "name": "config_data2",
  "version": "0.1.0",
  "description": "CLI tool: config_data2",
  "source": "https://packages.debian.org/config_data2",
  "checks": [{ "type": "binary", "name": "config_data2" }],
  "install_guidance": {
    "plugin": "config_data2", "binary": "config_data2", "check": "which config_data2",
    "install_steps": ["apt-get install config_data2 2>/dev/null || which config_data2", "Verify: config_data2 --version", "supercli plugins install ./plugins/config_data2 --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "config_data2", "resource": "_", "action": "_",
    "description": "Passthrough to config_data2 CLI",
    "adapter": "process", "adapterConfig": { "command": "config_data2", "passthrough": true, "missingDependencyHelp": "Install config_data2: apt-get install config_data2" }, "args": []
  }]
}
