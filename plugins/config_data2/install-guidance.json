{ "plugin": "config_data2", "binary": "config_data2", "check": "which config_data2", "install_steps": ["apt-get install config_data2 2>/dev/null || which config_data2", "Verify: config_data2 --version", "supercli plugins install ./plugins/config_data2 --on-conflict replace --json"] }
