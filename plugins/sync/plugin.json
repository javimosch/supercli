{
  "name": "sync",
  "version": "0.1.0",
  "description": "sync — flush filesystem buffers to disk for data integrity",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "sync"
    }
  ],
  "install_guidance": {
    "plugin": "sync",
    "binary": "sync",
    "check": "which sync",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/sync --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "sync",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to sync CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "sync",
        "passthrough": true,
        "missingDependencyHelp": "Install sync: base system"
      },
      "args": []
    }
  ]
}
