{ "plugin": "autoconf", "binary": "autoconf", "check": "which autoconf", "install_steps": ["apt-get install autoconf 2>/dev/null || which autoconf", "Verify: autoconf --version", "supercli plugins install ./plugins/autoconf --on-conflict replace --json"] }
