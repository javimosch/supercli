{ "plugin": "axfer", "binary": "axfer", "check": "which axfer", "install_steps": ["apt-get install axfer 2>/dev/null || which axfer", "Verify: axfer --version", "supercli plugins install ./plugins/axfer --on-conflict replace --json"] }
