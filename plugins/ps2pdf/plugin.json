{
  "name": "ps2pdf",
  "version": "0.1.0",
  "description": "ps2pdf — PostScript to PDF converter",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "ps2pdf"
    }
  ],
  "install_guidance": {
    "plugin": "ps2pdf",
    "binary": "ps2pdf",
    "check": "which ps2pdf",
    "install_steps": [
      "brew install ghostscript"
    ],
    "note": "Utility tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "ps2pdf",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to ps2pdf",
      "adapter": "process",
      "adapterConfig": {
        "command": "ps2pdf",
        "passthrough": true,
        "missingDependencyHelp": "Install ps2pdf: brew install ghostscript"
      },
      "args": []
    }
  ]
}
