{
  "plugin": "mkfs",
  "binary": "mkfs",
  "check": "which mkfs",
  "install_steps": [
    "apt-get install mkfs 2>/dev/null || which mkfs",
    "Verify: mkfs --version",
    "supercli plugins install ./plugins/mkfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}