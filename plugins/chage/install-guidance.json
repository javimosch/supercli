{ "plugin": "chage", "binary": "chage", "check": "which chage", "install_steps": ["apt-get install chage 2>/dev/null || which chage", "Verify: chage --version", "supercli plugins install ./plugins/chage --on-conflict replace --json"] }
