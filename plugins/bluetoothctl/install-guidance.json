{ "plugin": "bluetoothctl", "binary": "bluetoothctl", "check": "which bluetoothctl", "install_steps": ["apt-get install bluetoothctl 2>/dev/null || which bluetoothctl", "Verify: bluetoothctl --version", "supercli plugins install ./plugins/bluetoothctl --on-conflict replace --json"] }
