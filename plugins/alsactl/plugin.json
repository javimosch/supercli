{
  "name": "alsactl",
  "version": "0.1.0",
  "description": "CLI tool: alsactl",
  "source": "https://packages.debian.org/alsactl",
  "checks": [{ "type": "binary", "name": "alsactl" }],
  "install_guidance": {
    "plugin": "alsactl", "binary": "alsactl", "check": "which alsactl",
    "install_steps": ["apt-get install alsactl 2>/dev/null || which alsactl", "Verify: alsactl --version", "supercli plugins install ./plugins/alsactl --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "alsactl", "resource": "_", "action": "_",
    "description": "Passthrough to alsactl CLI",
    "adapter": "process", "adapterConfig": { "command": "alsactl", "passthrough": true, "missingDependencyHelp": "Install alsactl: apt-get install alsactl" }, "args": []
  }]
}
