{ "plugin": "alsactl", "binary": "alsactl", "check": "which alsactl", "install_steps": ["apt-get install alsactl 2>/dev/null || which alsactl", "Verify: alsactl --version", "supercli plugins install ./plugins/alsactl --on-conflict replace --json"] }
