---
name: siege-2
description: siege — HTTP load testing CLI. Concurrent HTTP/HTTPS benchmark with configurable users and duration.
---
# siege-2
siege — HTTP load testing CLI. Concurrent HTTP/HTTPS benchmark with configurable users and duration.
