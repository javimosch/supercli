{ "plugin": "createdb", "binary": "createdb", "check": "which createdb", "install_steps": ["apt-get install createdb 2>/dev/null || which createdb", "Verify: createdb --version", "supercli plugins install ./plugins/createdb --on-conflict replace --json"] }
