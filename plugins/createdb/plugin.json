{
  "name": "createdb",
  "version": "0.1.0",
  "description": "CLI tool: createdb",
  "source": "https://packages.debian.org/createdb",
  "checks": [{ "type": "binary", "name": "createdb" }],
  "install_guidance": {
    "plugin": "createdb", "binary": "createdb", "check": "which createdb",
    "install_steps": ["apt-get install createdb 2>/dev/null || which createdb", "Verify: createdb --version", "supercli plugins install ./plugins/createdb --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "createdb", "resource": "_", "action": "_",
    "description": "Passthrough to createdb CLI",
    "adapter": "process", "adapterConfig": { "command": "createdb", "passthrough": true, "missingDependencyHelp": "Install createdb: apt-get install createdb" }, "args": []
  }]
}
