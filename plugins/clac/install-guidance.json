{
  "plugin": "clac",
  "binary": "clac",
  "check": "which clac",
  "install_steps": [
    "brew install clac",
    "Verify: clac --version",
    "supercli plugins install ./plugins/clac --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}