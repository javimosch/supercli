{
  "plugin": "maim",
  "binary": "maim",
  "check": "which maim",
  "install_steps": [
    "brew install maim",
    "Verify: maim --version",
    "supercli plugins install ./plugins/maim --on-conflict replace --json"
  ]
}
