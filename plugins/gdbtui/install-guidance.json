{
  "plugin": "gdbtui",
  "binary": "gdbtui",
  "check": "which gdbtui",
  "install_steps": [
    "apt-get install gdbtui 2>/dev/null || which gdbtui",
    "Verify: gdbtui --version",
    "supercli plugins install ./plugins/gdbtui --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}