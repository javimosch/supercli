{
  "plugin": "env-manager",
  "binary": "env-manager",
  "check": "which env-manager",
  "install_steps": [
    "npm install -g env-manager",
    "Verify: env-manager --version",
    "supercli plugins install ./plugins/env-manager --on-conflict replace --json"
  ],
  "note": "Manages .env files for different environments (development, testing, production). Supports switching environments, validating .env files, and checking for unused variables."
}
