{
  "plugin": "ledger",
  "binary": "ledger",
  "check": "which ledger",
  "install_steps": [
    "brew install ledger",
    "apt install ledger",
    "Verify: ledger --version",
    "supercli plugins install ./plugins/ledger --on-conflict replace --json"
  ],
  "note": "Available via brew, apt, dnf, pacman, and from source at https://ledger-cli.org. Requires a plain-text journal file — set $LEDGER_FILE env var or pass -f <file> to each command."
}
