{
  "plugin": "dig",
  "binary": "dig",
  "check": "which dig",
  "install_steps": [
    "brew install bind",
    "Verify: dig --version",
    "supercli plugins install ./plugins/dig --on-conflict replace --json"
  ],
  "note": "Also available via apt install dnsutils."
}