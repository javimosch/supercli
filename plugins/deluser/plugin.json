{
  "name": "deluser",
  "version": "0.1.0",
  "description": "CLI tool: deluser",
  "source": "https://packages.debian.org/deluser",
  "checks": [{ "type": "binary", "name": "deluser" }],
  "install_guidance": {
    "plugin": "deluser", "binary": "deluser", "check": "which deluser",
    "install_steps": ["apt-get install deluser 2>/dev/null || which deluser", "Verify: deluser --version", "supercli plugins install ./plugins/deluser --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "deluser", "resource": "_", "action": "_",
    "description": "Passthrough to deluser CLI",
    "adapter": "process", "adapterConfig": { "command": "deluser", "passthrough": true, "missingDependencyHelp": "Install deluser: apt-get install deluser" }, "args": []
  }]
}
