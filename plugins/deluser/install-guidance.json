{ "plugin": "deluser", "binary": "deluser", "check": "which deluser", "install_steps": ["apt-get install deluser 2>/dev/null || which deluser", "Verify: deluser --version", "supercli plugins install ./plugins/deluser --on-conflict replace --json"] }
