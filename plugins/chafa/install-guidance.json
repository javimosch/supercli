{
  "plugin": "chafa",
  "binary": "chafa",
  "check": "which chafa",
  "install_steps": [
    "brew install chafa",
    "Verify: chafa --version",
    "supercli plugins install ./plugins/chafa --on-conflict replace --json"
  ]
}
