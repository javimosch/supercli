{
  "plugin": "lxcfs",
  "binary": "lxcfs",
  "check": "which lxcfs",
  "install_steps": [
    "apt-get install lxcfs 2>/dev/null || which lxcfs",
    "Verify: lxcfs --version",
    "supercli plugins install ./plugins/lxcfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}