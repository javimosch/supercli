{
  "plugin": "ascii-chart",
  "binary": "ascii-chart",
  "check": "which ascii-chart",
  "install_steps": ["npm install -g ascii-chart", "Verify: ascii-chart --version", "supercli plugins install ./plugins/ascii-chart --on-conflict replace --json"]
}
