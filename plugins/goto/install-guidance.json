{
  "plugin": "goto",
  "binary": "goto",
  "check": "which goto",
  "install_steps": [
    "brew install goto",
    "Verify: goto --version",
    "supercli plugins install ./plugins/goto --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}