---
name: nerdctl
description: nerdctl — Docker-compatible CLI for containerd. Same interface as docker but for containerd.
---
# nerdctl
nerdctl — Docker-compatible CLI for containerd. Same interface as docker but for containerd.
