{
  "plugin": "thin_ls",
  "binary": "thin_ls",
  "check": "which thin_ls",
  "install_steps": [
    "apt-get install thin_ls 2>/dev/null || which thin_ls",
    "Verify: thin_ls --version",
    "supercli plugins install ./plugins/thin_ls --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}