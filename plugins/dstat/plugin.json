{
  "name": "dstat",
  "version": "0.1.0",
  "description": "dstat — Versatile resource statistics",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "dstat"
    }
  ],
  "install_guidance": {
    "plugin": "dstat",
    "binary": "dstat",
    "check": "which dstat",
    "install_steps": [
      "brew install dstat"
    ],
    "note": "System utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "dstat",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to dstat",
      "adapter": "process",
      "adapterConfig": {
        "command": "dstat",
        "passthrough": true,
        "missingDependencyHelp": "Install dstat: brew install dstat"
      },
      "args": []
    }
  ]
}
