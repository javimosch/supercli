{
  "plugin": "visidata",
  "binary": "vd",
  "check": "which vd",
  "install_steps": [
    "pip install visidata",
    "Or: brew install visidata",
    "Verify: vd --version",
    "supercli plugins install ./plugins/visidata --on-conflict replace --json"
  ],
  "note": "Binary is `vd` (also available as `visidata`). Install via pip, brew, or apt. Supports 40+ file formats including CSV, TSV, JSON, XLSX, SQLite, Parquet, HTML, and more."
}
