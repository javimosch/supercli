---
name: lsinitramfs
description: Use this skill when the user wants to use lsinitramfs, cli tool: lsinitramfs.
---

# lsinitramfs Plugin

CLI tool: lsinitramfs.

## Commands
- `lsinitramfs <resource> <action>` — Execute lsinitramfs commands
- `lsinitramfs self version` — Print lsinitramfs version
- `lsinitramfs _ _` — Passthrough to lsinitramfs CLI

## Usage Examples
- "lsinitramfs --help"
- "lsinitramfs self version"

## Installation
```bash
apt-get install lsinitramfs 2>/dev/null || which lsinitramfs
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
