{
  "plugin": "duf",
  "binary": "duf",
  "check": "which duf",
  "install_steps": [
    "brew install duf",
    "Verify: duf --version",
    "supercli plugins install ./plugins/duf --on-conflict replace --json"
  ]
}