{ "plugin": "cyrus-admin", "binary": "cyrus-admin", "check": "which cyrus-admin", "install_steps": ["apt-get install cyrus-admin 2>/dev/null || which cyrus-admin", "Verify: cyrus-admin --version", "supercli plugins install ./plugins/cyrus-admin --on-conflict replace --json"] }
