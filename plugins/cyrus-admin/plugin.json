{
  "name": "cyrus-admin",
  "version": "0.1.0",
  "description": "CLI tool: cyrus-admin",
  "source": "https://packages.debian.org/cyrus-admin",
  "checks": [{ "type": "binary", "name": "cyrus-admin" }],
  "install_guidance": {
    "plugin": "cyrus-admin", "binary": "cyrus-admin", "check": "which cyrus-admin",
    "install_steps": ["apt-get install cyrus-admin 2>/dev/null || which cyrus-admin", "Verify: cyrus-admin --version", "supercli plugins install ./plugins/cyrus-admin --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cyrus-admin", "resource": "_", "action": "_",
    "description": "Passthrough to cyrus-admin CLI",
    "adapter": "process", "adapterConfig": { "command": "cyrus-admin", "passthrough": true, "missingDependencyHelp": "Install cyrus-admin: apt-get install cyrus-admin" }, "args": []
  }]
}
