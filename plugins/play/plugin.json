{
  "name": "play",
  "version": "0.1.0",
  "description": "Audio playback tool",
  "source": "https://packages.debian.org/play",
  "checks": [{ "type": "binary", "name": "play" }],
  "install_guidance": {
    "plugin": "play", "binary": "play", "check": "which play",
    "install_steps": ["apt-get install sox", "Verify: play --version", "supercli plugins install ./plugins/play --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "play", "resource": "_", "action": "_",
    "description": "Passthrough to play CLI",
    "adapter": "process", "adapterConfig": { "command": "play", "passthrough": true, "missingDependencyHelp": "Install play: apt-get install sox" }, "args": []
  }]
}
