{ "plugin": "distrobox-create", "binary": "distrobox-create", "check": "which distrobox-create", "install_steps": ["apt-get install distrobox-create 2>/dev/null || which distrobox-create", "Verify: distrobox-create --version", "supercli plugins install ./plugins/distrobox-create --on-conflict replace --json"] }
