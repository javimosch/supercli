{
  "plugin": "svgbob",
  "binary": "svgbob_cli",
  "check": "which svgbob_cli",
  "install_steps": [
    "cargo install svgbob_cli",
    "Verify: svgbob_cli --version",
    "supercli plugins install ./plugins/svgbob --on-conflict replace --json"
  ],
  "note": "Binary is named svgbob_cli (crate contains only the CLI wrapper; svgbob is the library). Also available as an mdbook preprocessor: mdbook-svgbob."
}
