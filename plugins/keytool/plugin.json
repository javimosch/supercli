{
  "name": "keytool",
  "version": "0.1.0",
  "description": "Java keystore management",
  "source": "https://packages.debian.org/keytool",
  "checks": [{ "type": "binary", "name": "keytool" }],
  "install_guidance": {
    "plugin": "keytool", "binary": "keytool", "check": "which keytool",
    "install_steps": ["apt-get install default-jdk", "Verify: keytool --version", "supercli plugins install ./plugins/keytool --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "keytool", "resource": "_", "action": "_",
    "description": "Passthrough to keytool CLI",
    "adapter": "process", "adapterConfig": { "command": "keytool", "passthrough": true, "missingDependencyHelp": "Install keytool: apt-get install default-jdk" }, "args": []
  }]
}
