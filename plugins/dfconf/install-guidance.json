{ "plugin": "dfconf", "binary": "dfconf", "check": "which dfconf", "install_steps": ["apt-get install dfconf 2>/dev/null || which dfconf", "Verify: dfconf --version", "supercli plugins install ./plugins/dfconf --on-conflict replace --json"] }
