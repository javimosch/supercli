---
name: xmllint
description: Use this skill when the user wants to use xmllint, cli tool: xmllint.
---

# xmllint Plugin

CLI tool: xmllint.

## Commands
- `xmllint <resource> <action>` — Execute xmllint commands
- `xmllint self version` — Print xmllint version
- `xmllint _ _` — Passthrough to xmllint CLI

## Usage Examples
- "xmllint --help"
- "xmllint self version"

## Installation
```bash
apt-get install xmllint 2>/dev/null || which xmllint
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
