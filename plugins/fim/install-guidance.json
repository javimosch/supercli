{
  "plugin": "fim",
  "binary": "fim",
  "check": "which fim",
  "install_steps": ["sudo apt install fim", "Verify: fim --version", "supercli plugins install ./plugins/fim --on-conflict replace --json"]
}
