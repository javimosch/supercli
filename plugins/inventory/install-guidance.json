{
  "plugin": "inventory",
  "binary": "inventory",
  "check": "which inventory",
  "install_steps": [
    "See https://github.com/mothdotmonster/inventory",
    "Verify: inventory --version",
    "supercli plugins install ./plugins/inventory --on-conflict replace --json"
  ],
  "note": "See project repository"
}