{
  "plugin": "ward",
  "binary": "ward",
  "check": "which ward",
  "install_steps": [
    "go install github.com/eljakani/ward@latest",
    "Verify: ward --version",
    "supercli plugins install ./plugins/ward --on-conflict replace --json"
  ],
  "note": "Also supports building from source (git clone + make build + make install). Run 'ward init' after installing to generate default security rules in ~/.ward/."
}
