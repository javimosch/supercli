{
  "name": "dh_link",
  "version": "0.1.0",
  "description": "CLI tool: dh_link",
  "source": "https://packages.debian.org/dh_link",
  "checks": [{ "type": "binary", "name": "dh_link" }],
  "install_guidance": {
    "plugin": "dh_link", "binary": "dh_link", "check": "which dh_link",
    "install_steps": ["apt-get install dh_link 2>/dev/null || which dh_link", "Verify: dh_link --version", "supercli plugins install ./plugins/dh_link --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_link", "resource": "_", "action": "_",
    "description": "Passthrough to dh_link CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_link", "passthrough": true, "missingDependencyHelp": "Install dh_link: apt-get install dh_link" }, "args": []
  }]
}
