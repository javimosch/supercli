{
  "plugin": "gitk",
  "binary": "gitk",
  "check": "which gitk",
  "install_steps": [
    "apt-get install gitk 2>/dev/null || which gitk",
    "Verify: gitk --version",
    "supercli plugins install ./plugins/gitk --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}