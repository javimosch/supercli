{
  "name": "dpkg-trigger",
  "version": "1.0.0",
  "description": "dpkg-trigger \u2014 dpkg-trigger \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-trigger"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-trigger",
    "binary": "dpkg-trigger",
    "check": "which dpkg-trigger",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-trigger",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-trigger \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-trigger",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}