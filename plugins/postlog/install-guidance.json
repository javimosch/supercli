{
  "plugin": "postlog",
  "binary": "postlog",
  "check": "which postlog",
  "install_steps": [
    "# Install postlog",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postlog-linux-amd64 -o /usr/local/bin/postlog",
    "chmod +x /usr/local/bin/postlog",
    "# Or via package manager, see /usr/sbin/postlog"
  ]
}