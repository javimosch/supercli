{
  "plugin": "bit",
  "binary": "bit",
  "check": "which bit",
  "install_steps": [
    "curl -fsSL https://raw.githubusercontent.com/superstarryeyes/bit/main/install.sh | bash",
    "Verify: bit --version",
    "supercli plugins install ./plugins/bit --on-conflict replace --json"
  ],
  "note": "Also available via manual download from GitHub Releases or build from source (go build -o bit ./cmd/bit)."
}
