{
  "name": "dpkg-query",
  "version": "1.0.0",
  "description": "dpkg-query \u2014 dpkg-query \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-query"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-query",
    "binary": "dpkg-query",
    "check": "which dpkg-query",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-query",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-query \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-query",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}