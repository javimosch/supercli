---
name: agent-os
description: Use this skill when the user wants to use agent-os for Agent OS is a system for injecting your codebase standards and writing better sp.
---

# agent-os Plugin

Agent OS is a system for injecting your codebase standards and writing better specs for spec-driven development.

## Commands

### General
- `agent-os self version` — Print agent-os version
- `agent-os _ _` — Passthrough to agent-os CLI

## Usage Examples
- "Run agent-os"
- "Use agent-os to Agent OS is a system for injecting your codebase standards a"

## Installation

```bash
pip install agent-os
```

## Key Features
- Agent OS is a system for injecting your codebase standards and writing better specs for spec-driven 
- CLI tool, headless and scriptable
