{
  "plugin": "gsf",
  "binary": "gsf",
  "check": "which gsf",
  "install_steps": [
    "apt-get install gsf 2>/dev/null || which gsf",
    "Verify: gsf --version",
    "supercli plugins install ./plugins/gsf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}