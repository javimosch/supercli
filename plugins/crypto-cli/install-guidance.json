{
  "plugin": "crypto-cli",
  "binary": "crypto-cli",
  "check": "which crypto-cli",
  "install_steps": ["npm install -g crypto-cli", "Verify: crypto-cli --version", "supercli plugins install ./plugins/crypto-cli --on-conflict replace --json"]
}
