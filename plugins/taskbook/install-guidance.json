{
  "plugin": "taskbook",
  "binary": "tb",
  "check": "which tb",
  "install_steps": [
    "npm install -g taskbook",
    "Verify: tb --version",
    "supercli plugins install ./plugins/taskbook --on-conflict replace --json"
  ]
}