{
  "plugin": "gap",
  "binary": "gap",
  "check": "which gap",
  "install_steps": [
    "brew install gap",
    "Verify: gap --version",
    "supercli plugins install ./plugins/gap --on-conflict replace --json"
  ]
}
