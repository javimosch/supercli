{
  "name": "pass",
  "version": "1.0.0",
  "description": "pass \u2014 Password store",
  "source": "passwordstore.org",
  "checks": [
    {
      "type": "binary",
      "name": "pass"
    }
  ],
  "install_guidance": {
    "plugin": "pass",
    "binary": "pass",
    "check": "which pass",
    "install_steps": [
      "# Install pass:",
      "# apt: sudo apt-get install pass",
      "# brew: brew install pass",
      "# cargo: cargo install pass",
      "# npm: npm install -g pass",
      "# pip: pip install pass",
      "# or download from passwordstore.org"
    ]
  },
  "commands": [
    {
      "namespace": "pass",
      "resource": "pass",
      "action": "run",
      "description": "Run pass",
      "adapter": "process",
      "adapterConfig": {
        "command": "pass",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install pass from passwordstore.org"
      },
      "args": []
    }
  ]
}