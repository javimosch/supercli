{
  "plugin": "pass",
  "binary": "pass",
  "check": "which pass",
  "install_steps": [
    "# Install pass",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pass-linux-amd64 -o /usr/local/bin/pass",
    "chmod +x /usr/local/bin/pass",
    "# Or via package manager, see passwordstore.org"
  ]
}