{
  "plugin": "pkexec",
  "binary": "pkexec",
  "check": "which pkexec",
  "install_steps": [
    "apt-get install pkexec 2>/dev/null || which pkexec",
    "Verify: pkexec --version",
    "supercli plugins install ./plugins/pkexec --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}