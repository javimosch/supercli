{
  "plugin": "dvc",
  "binary": "dvc",
  "check": "which dvc",
  "install_steps": ["pip install dvc", "Verify: dvc --version", "supercli plugins install ./plugins/dvc --on-conflict replace --json"]
}
