{
  "plugin": "wksctl",
  "binary": "wksctl",
  "check": "which wksctl",
  "install_steps": ["brew install wksctl", "Verify: wksctl --version", "supercli plugins install ./plugins/wksctl --on-conflict replace --json"]
}
