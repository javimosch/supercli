{
  "plugin": "rgx",
  "binary": "rgx",
  "check": "which rgx",
  "install_steps": [
    "cargo install rgx",
    "Verify: rgx --help",
    "supercli plugins install ./plugins/rgx --on-conflict replace --json"
  ],
  "note": "rgx provides regex101-like functionality in the terminal"
}
