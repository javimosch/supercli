{
  "plugin": "percol",
  "binary": "percol",
  "check": "which percol",
  "install_steps": [
    "# Install percol",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/percol-linux-amd64 -o /usr/local/bin/percol",
    "chmod +x /usr/local/bin/percol",
    "# Or via package manager, see github.com/mooz/percol"
  ]
}