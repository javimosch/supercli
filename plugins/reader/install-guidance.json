{
  "plugin": "reader",
  "binary": "reader",
  "check": "which reader",
  "install_steps": [
    "go install github.com/mrusme/reader@latest",
    "Verify: reader --version",
    "supercli plugins install ./plugins/reader --on-conflict replace --json"
  ]
}