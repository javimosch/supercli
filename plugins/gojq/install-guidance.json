{
  "plugin": "gojq",
  "binary": "gojq",
  "check": "which gojq",
  "install_steps": [
    "brew install gojq",
    "Verify: gojq --version",
    "supercli plugins install ./plugins/gojq --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/itchyny/gojq/cmd/gojq@latest. Pure Go implementation with no C dependencies. Compatible with jq query syntax."
}
