{
  "description": "Fast, modern traceroute with per-hop stats, ASN/geo lookup, ECMP detection, and MPLS label parsing. Supports headless JSON, CSV, and text report output.",
  "tags": ["ttl", "traceroute", "network", "icmp", "tcp", "latency", "rust", "cli"],
  "has_learn": true
}
