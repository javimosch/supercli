{
  "plugin": "ttl",
  "binary": "ttl",
  "check": "which ttl",
  "install_steps": [
    "brew install lance0/tap/ttl",
    "Verify: ttl --version",
    "supercli plugins install ./plugins/ttl --on-conflict replace --json"
  ],
  "note": "Linux: Add capability once with 'sudo setcap cap_net_raw+ep $(which ttl)' to run without sudo. Also available via cargo (cargo install ttl), AUR (yay -S ttl-bin), Gentoo (emerge net-analyzer/ttl), or prebuilt binaries from GitHub Releases."
}
