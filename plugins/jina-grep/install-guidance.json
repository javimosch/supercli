{
  "plugin": "jina-grep",
  "binary": "jina-grep",
  "check": "which jina-grep",
  "install_steps": [
    "pip install jina-grep-cli",
    "Verify: jina-grep --version",
    "supercli plugins install ./plugins/jina-grep --on-conflict replace --json"
  ],
  "note": "Requires Python. On Apple Silicon, uses MLX for accelerated embeddings. On other platforms, uses standard Python execution."
}
