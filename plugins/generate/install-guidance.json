{
  "plugin": "generate",
  "binary": "gen",
  "check": "which gen",
  "install_steps": [
    "npm install -g generate",
    "Verify: gen --version",
    "supercli plugins install ./plugins/generate --on-conflict replace --json"
  ]
}