{
  "plugin": "hstr",
  "binary": "hstr",
  "check": "which hstr",
  "install_steps": [
    "brew install hstr",
    "sudo apt install hstr",
    "sudo dnf install hstr",
    "sudo pacman -S hstr",
    "curl -sSL https://github.com/dvorka-oss/hstr/releases/latest/download/hstr_Linux_x86_64.tar.gz | tar xz -C /usr/local/bin",
    "Verify: hstr --version",
    "supercli plugins install ./plugins/hstr --on-conflict replace --json"
  ],
  "note": "hstr integrates with bash and zsh via Ctrl-r binding. After installing, run 'hstr --show-bash-configuration >> ~/.bashrc' (or zsh equivalent) to enable the history suggest box. Official docs: https://github.com/dvorka-oss/hstr"
}
