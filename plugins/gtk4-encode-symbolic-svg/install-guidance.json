{
  "plugin": "gtk4-encode-symbolic-svg",
  "binary": "gtk4-encode-symbolic-svg",
  "check": "which gtk4-encode-symbolic-svg",
  "install_steps": [
    "apt-get install gtk4-encode-symbolic-svg 2>/dev/null || which gtk4-encode-symbolic-svg",
    "Verify: gtk4-encode-symbolic-svg --version",
    "supercli plugins install ./plugins/gtk4-encode-symbolic-svg --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}