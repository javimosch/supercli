{
  "plugin": "oxvg",
  "binary": "oxvg",
  "check": "which oxvg",
  "install_steps": [
    "cargo install oxvg",
    "Verify: oxvg --version",
    "supercli plugins install ./plugins/oxvg --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
