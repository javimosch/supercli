{
  "name": "arch",
  "version": "0.1.0",
  "description": "arch — Print machine architecture",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "arch"
    }
  ],
  "install_guidance": {
    "plugin": "arch",
    "binary": "arch",
    "check": "which arch",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/arch --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "arch",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to arch CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "arch",
        "passthrough": true,
        "missingDependencyHelp": "Install arch: base system"
      },
      "args": []
    }
  ]
}
