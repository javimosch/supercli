{
  "plugin": "hdt",
  "binary": "hdt",
  "check": "which hdt",
  "install_steps": [
    "brew install hdt",
    "Verify: hdt --version",
    "supercli plugins install ./plugins/hdt --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}