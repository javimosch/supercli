{
  "plugin": "prettier",
  "binary": "prettier",
  "check": "which prettier",
  "install_steps": [
    "npm install -g prettier",
    "Verify: prettier --version",
    "supercli plugins install ./plugins/prettier --on-conflict replace --json"
  ],
  "note": "Prettier is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules. Supports --check flag to verify formatting without modifying files."
}
