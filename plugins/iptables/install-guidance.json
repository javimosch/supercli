{
  "plugin": "iptables",
  "binary": "iptables",
  "check": "which iptables",
  "install_steps": [
    "# Install iptables",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/iptables-linux-amd64 -o /usr/local/bin/iptables",
    "chmod +x /usr/local/bin/iptables",
    "# Or via package manager, see netfilter.org"
  ]
}