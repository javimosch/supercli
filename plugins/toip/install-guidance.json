{
  "plugin": "toip",
  "binary": "toip",
  "check": "which toip",
  "install_steps": [
    "cargo install toip",
    "Verify: toip --version",
    "supercli plugins install ./plugins/toip --on-conflict replace --json"
  ],
  "note": "Binary name: toip"
}
