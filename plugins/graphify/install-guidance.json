{
  "plugin": "graphify",
  "binary": "graphify",
  "check": "which graphify",
  "install_steps": [
    "pip install graphifyy",
    "Verify: graphify --version",
    "graphify install",
    "supercli plugins install ./plugins/graphify --on-conflict replace --json"
  ],
  "note": "PyPI package is graphifyy (double-y), but the CLI binary is graphify. After installing, run 'graphify install' to set up the skill for your AI assistant."
}
