{
  "plugin": "tcpulse",
  "binary": "tcpulse",
  "check": "which tcpulse",
  "install_steps": ["go install github.com/tcpulse@latest", "Verify: tcpulse --version", "supercli plugins install ./plugins/tcpulse --on-conflict replace --json"]
}
