{
  "plugin": "dee-habit",
  "binary": "dee-habit",
  "check": "which dee-habit",
  "install_steps": ["cargo install dee-habit", "Verify: dee-habit --version", "supercli plugins install ./plugins/dee-habit --on-conflict replace --json"]
}
