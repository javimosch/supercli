{
  "plugin": "esptool",
  "binary": "esptool",
  "check": "which esptool",
  "install_steps": [
    "brew install esptool",
    "Verify: esptool --version",
    "supercli plugins install ./plugins/esptool --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}