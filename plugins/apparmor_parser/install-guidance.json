{ "plugin": "apparmor_parser", "binary": "apparmor_parser", "check": "which apparmor_parser", "install_steps": ["apt-get install apparmor_parser 2>/dev/null || which apparmor_parser", "Verify: apparmor_parser --version", "supercli plugins install ./plugins/apparmor_parser --on-conflict replace --json"] }
