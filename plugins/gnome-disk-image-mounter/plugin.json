{
  "name": "gnome-disk-image-mounter",
  "version": "0.1.0",
  "description": "CLI tool: gnome-disk-image-mounter",
  "source": "https://manpages.debian.org/gnome-disk-image-mounter",
  "checks": [
    {
      "type": "binary",
      "name": "gnome-disk-image-mounter"
    }
  ],
  "install_guidance": {
    "plugin": "gnome-disk-image-mounter",
    "binary": "gnome-disk-image-mounter",
    "check": "which gnome-disk-image-mounter",
    "install_steps": [
      "apt-get install gnome-disk-image-mounter 2>/dev/null || which gnome-disk-image-mounter",
      "Verify: gnome-disk-image-mounter --version",
      "supercli plugins install ./plugins/gnome-disk-image-mounter --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "gnome-disk-image-mounter",
      "resource": "self",
      "action": "version",
      "description": "Print gnome-disk-image-mounter version",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-disk-image-mounter",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install gnome-disk-image-mounter: apt-get install gnome-disk-image-mounter 2>/dev/null || which gnome-disk-image-mounter"
      },
      "args": []
    },
    {
      "namespace": "gnome-disk-image-mounter",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to gnome-disk-image-mounter CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-disk-image-mounter",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install gnome-disk-image-mounter: apt-get install gnome-disk-image-mounter 2>/dev/null || which gnome-disk-image-mounter"
      },
      "args": []
    }
  ]
}