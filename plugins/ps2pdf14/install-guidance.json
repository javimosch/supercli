{
  "plugin": "ps2pdf14",
  "binary": "ps2pdf14",
  "check": "which ps2pdf14",
  "install_steps": [
    "apt-get install ps2pdf14 2>/dev/null || which ps2pdf14",
    "Verify: ps2pdf14 --version",
    "supercli plugins install ./plugins/ps2pdf14 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}