{
  "name": "ps2pdf14",
  "version": "0.1.0",
  "description": "CLI tool: ps2pdf14",
  "source": "https://manpages.debian.org/ps2pdf14",
  "checks": [
    {
      "type": "binary",
      "name": "ps2pdf14"
    }
  ],
  "install_guidance": {
    "plugin": "ps2pdf14",
    "binary": "ps2pdf14",
    "check": "which ps2pdf14",
    "install_steps": [
      "apt-get install ps2pdf14 2>/dev/null || which ps2pdf14",
      "Verify: ps2pdf14 --version",
      "supercli plugins install ./plugins/ps2pdf14 --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "ps2pdf14",
      "resource": "self",
      "action": "version",
      "description": "Print ps2pdf14 version",
      "adapter": "process",
      "adapterConfig": {
        "command": "ps2pdf14",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install ps2pdf14: apt-get install ps2pdf14 2>/dev/null || which ps2pdf14"
      },
      "args": []
    },
    {
      "namespace": "ps2pdf14",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to ps2pdf14 CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "ps2pdf14",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install ps2pdf14: apt-get install ps2pdf14 2>/dev/null || which ps2pdf14"
      },
      "args": []
    }
  ]
}