{ "plugin": "check_cert", "binary": "check_cert", "check": "which check_cert", "install_steps": ["apt-get install check_cert 2>/dev/null || which check_cert", "Verify: check_cert --version", "supercli plugins install ./plugins/check_cert --on-conflict replace --json"] }
