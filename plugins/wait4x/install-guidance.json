{
  "plugin": "wait4x",
  "binary": "wait4x",
  "check": "which wait4x",
  "install_steps": [
    "brew install wait4x",
    "Verify: wait4x --version",
    "supercli plugins install ./plugins/wait4x --on-conflict replace --json"
  ],
  "note": "Wait for services to be ready. Ideal for CI/CD, containers, and local development."
}