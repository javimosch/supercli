{ "plugin": "div", "binary": "div", "check": "which div", "install_steps": ["apt-get install div 2>/dev/null || which div", "Verify: div --version", "supercli plugins install ./plugins/div --on-conflict replace --json"] }
