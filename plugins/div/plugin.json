{
  "name": "div",
  "version": "0.1.0",
  "description": "CLI tool: div",
  "source": "https://packages.debian.org/div",
  "checks": [{ "type": "binary", "name": "div" }],
  "install_guidance": {
    "plugin": "div", "binary": "div", "check": "which div",
    "install_steps": ["apt-get install div 2>/dev/null || which div", "Verify: div --version", "supercli plugins install ./plugins/div --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "div", "resource": "_", "action": "_",
    "description": "Passthrough to div CLI",
    "adapter": "process", "adapterConfig": { "command": "div", "passthrough": true, "missingDependencyHelp": "Install div: apt-get install div" }, "args": []
  }]
}
