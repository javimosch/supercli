{ "plugin": "cryptsetup-reencrypt", "binary": "cryptsetup-reencrypt", "check": "which cryptsetup-reencrypt", "install_steps": ["apt-get install cryptsetup-reencrypt 2>/dev/null || which cryptsetup-reencrypt", "Verify: cryptsetup-reencrypt --version", "supercli plugins install ./plugins/cryptsetup-reencrypt --on-conflict replace --json"] }
