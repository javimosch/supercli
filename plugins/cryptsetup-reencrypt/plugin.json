{
  "name": "cryptsetup-reencrypt",
  "version": "0.1.0",
  "description": "CLI tool: cryptsetup-reencrypt",
  "source": "https://packages.debian.org/cryptsetup-reencrypt",
  "checks": [{ "type": "binary", "name": "cryptsetup-reencrypt" }],
  "install_guidance": {
    "plugin": "cryptsetup-reencrypt", "binary": "cryptsetup-reencrypt", "check": "which cryptsetup-reencrypt",
    "install_steps": ["apt-get install cryptsetup-reencrypt 2>/dev/null || which cryptsetup-reencrypt", "Verify: cryptsetup-reencrypt --version", "supercli plugins install ./plugins/cryptsetup-reencrypt --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cryptsetup-reencrypt", "resource": "_", "action": "_",
    "description": "Passthrough to cryptsetup-reencrypt CLI",
    "adapter": "process", "adapterConfig": { "command": "cryptsetup-reencrypt", "passthrough": true, "missingDependencyHelp": "Install cryptsetup-reencrypt: apt-get install cryptsetup-reencrypt" }, "args": []
  }]
}
