{
  "plugin": "pon",
  "binary": "pon",
  "check": "which pon",
  "install_steps": [
    "apt-get install pon 2>/dev/null || which pon",
    "Verify: pon --version",
    "supercli plugins install ./plugins/pon --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}