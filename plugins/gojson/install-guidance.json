{
  "plugin": "gojson",
  "binary": "gojson",
  "check": "which gojson",
  "install_steps": [
    "brew install gojson",
    "Verify: gojson --version",
    "supercli plugins install ./plugins/gojson --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/ChimeraCoder/gojson@latest. Converts JSON to Go struct definitions automatically. Handles nested structures and arrays. Perfect for API integration and data serialization."
}
