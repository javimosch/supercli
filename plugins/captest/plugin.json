{
  "name": "captest",
  "version": "0.1.0",
  "description": "CLI tool: captest",
  "source": "https://packages.debian.org/captest",
  "checks": [{ "type": "binary", "name": "captest" }],
  "install_guidance": {
    "plugin": "captest", "binary": "captest", "check": "which captest",
    "install_steps": ["apt-get install captest 2>/dev/null || which captest", "Verify: captest --version", "supercli plugins install ./plugins/captest --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "captest", "resource": "_", "action": "_",
    "description": "Passthrough to captest CLI",
    "adapter": "process", "adapterConfig": { "command": "captest", "passthrough": true, "missingDependencyHelp": "Install captest: apt-get install captest" }, "args": []
  }]
}
