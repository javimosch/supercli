{
  "name": "squid",
  "version": "0.1.0",
  "description": "Squid — caching and forwarding web proxy for HTTP traffic",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "squid"
    }
  ],
  "install_guidance": {
    "plugin": "squid",
    "binary": "squid",
    "check": "which squid",
    "install_steps": [
      "brew install squid"
    ],
    "note": "Infrastructure tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "squid",
      "resource": "_",
      "action": "_",
      "description": "Passthrough",
      "adapter": "process",
      "adapterConfig": {
        "command": "squid",
        "passthrough": true,
        "missingDependencyHelp": "Install squid: brew install squid"
      },
      "args": []
    }
  ]
}
