---
name: skopeo
description: Use this skill when the user wants to work with remote container image registries without requiring a daemon.
---

# Skopeo Plugin

Work with remote container image registries without requiring a daemon.

## Commands

### Operations
- `skopeo image copy` — copy image via skopeo
- `skopeo image inspect` — inspect image via skopeo
- `skopeo image list-tags` — list-tags image via skopeo

## Usage Examples
- "skopeo --help"
- "skopeo <args>"

## Installation

```bash
brew install skopeo
```

## Examples

```bash
skopeo --version
skopeo --help
```

## Key Features
- containers\n- registry\n- images
