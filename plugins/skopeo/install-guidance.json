{
  "plugin": "skopeo",
  "binary": "skopeo",
  "check": "which skopeo",
  "install_steps": [
    "brew install skopeo",
    "Verify: skopeo --version",
    "supercli plugins install ./plugins/skopeo --on-conflict replace --json"
  ]
}