{
  "plugin": "pet",
  "binary": "pet",
  "check": "which pet",
  "install_steps": [
    "brew install pet",
    "Verify: pet --version",
    "Configure editor: pet configure",
    "supercli plugins install ./plugins/pet --on-conflict replace --json"
  ],
  "note": "Also available as Go binary from GitHub Releases: https://github.com/knqyf263/pet/releases. Snippets stored in ~/.config/pet/config.toml. Supports optional sync via Gist or GitLab Snippets."
}
