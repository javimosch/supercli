{
  "plugin": "vals",
  "binary": "vals",
  "check": "which vals",
  "install_steps": ["brew install vals", "Verify: vals --version", "supercli plugins install ./plugins/vals --on-conflict replace --json"]
}
