{
  "plugin": "timer",
  "binary": "timer",
  "check": "which timer",
  "install_steps": [
    "cargo install timer",
    "Verify: timer --version"
  ],
  "note": "Install timer for Simple countdown terminal alarm in Rust"
}
