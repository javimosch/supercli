{
  "plugin": "bazel-diff",
  "binary": "bazel-diff",
  "check": "which bazel-diff",
  "install_steps": [
    "brew install bazel-diff",
    "Verify: bazel-diff --version",
    "supercli plugins install ./plugins/bazel-diff --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}