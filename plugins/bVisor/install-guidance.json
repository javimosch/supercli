{
  "plugin": "bVisor",
  "binary": "bvisor",
  "check": "which bvisor",
  "install_steps": [
    "pip install bvisor",
    "Verify: bvisor --version",
    "supercli plugins install ./plugins/bVisor --on-conflict replace --json"
  ],
  "note": "Language: Zig. Stars: 188"
}