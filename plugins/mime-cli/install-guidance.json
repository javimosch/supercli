{
  "plugin": "mime-cli",
  "binary": "mime",
  "check": "which mime",
  "install_steps": [
    "npm install -g mime-cli",
    "Verify: mime --help",
    "supercli plugins install ./plugins/mime-cli --on-conflict replace --json"
  ],
  "note": "Mime-cli detects MIME types based on file extensions. Simple and fast utility for determining content types. Useful for file processing and web development."
}
