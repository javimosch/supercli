{
  "plugin": "gtk-launch",
  "binary": "gtk-launch",
  "check": "which gtk-launch",
  "install_steps": [
    "apt-get install gtk-launch 2>/dev/null || which gtk-launch",
    "Verify: gtk-launch --version",
    "supercli plugins install ./plugins/gtk-launch --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}