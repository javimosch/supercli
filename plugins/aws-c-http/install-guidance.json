{
  "plugin": "aws-c-http",
  "binary": "aws-c-http",
  "check": "which aws-c-http",
  "install_steps": [
    "brew install aws-c-http",
    "Verify: aws-c-http --version",
    "supercli plugins install ./plugins/aws-c-http --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}