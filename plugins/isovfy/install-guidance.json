{
  "plugin": "isovfy",
  "binary": "isovfy",
  "check": "which isovfy",
  "install_steps": [
    "apt-get install isovfy 2>/dev/null || which isovfy",
    "Verify: isovfy --version",
    "supercli plugins install ./plugins/isovfy --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}