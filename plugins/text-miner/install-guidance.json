{
  "plugin": "text-miner",
  "binary": "text-miner",
  "check": "which text-miner",
  "install_steps": ["npm install -g text-miner", "Verify: text-miner --version", "supercli plugins install ./plugins/text-miner --on-conflict replace --json"]
}
