{
  "plugin": "progress",
  "binary": "progress",
  "check": "which progress",
  "install_steps": [
    "# Install progress",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/progress-linux-amd64 -o /usr/local/bin/progress",
    "chmod +x /usr/local/bin/progress",
    "# Or via package manager, see github.com/Xfennec/progress"
  ]
}