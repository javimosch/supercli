{
  "plugin": "progress",
  "binary": "progress",
  "check": "which progress",
  "install_steps": [
    "brew install progress",
    "Or: apt-get install progress (Debian/Ubuntu)",
    "Or: dnf install progress (Fedora)",
    "Or: cargo install progress (Rust)",
    "Verify: progress --version",
    "supercli plugins install ./plugins/progress --on-conflict replace --json"
  ],
  "note": "A simple C tool that looks for coreutils commands (cp, mv, dd, tar, cat, etc.) currently running on your system and shows the percentage of data that has been copied. Previously known as 'cv'. Source: https://github.com/Xfennec/progress (8k+ stars)."
}
