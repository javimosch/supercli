{
  "plugin": "progress",
  "binary": "progress",
  "check": "which progress",
  "install_steps": [
    "sudo apt-get install progress",
    "brew install progress"
  ],
  "note": "Linux / macOS. Shows progress of coreutils commands."
}
