{
  "plugin": "btop",
  "binary": "btop",
  "check": "which btop",
  "install_steps": [
    "brew install btop",
    "Verify: btop --version",
    "supercli plugins install ./plugins/btop --on-conflict replace --json"
  ]
}