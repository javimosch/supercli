{
  "plugin": "zoom",
  "binary": "zoom",
  "check": "which zoom",
  "install_steps": [
    "apt-get install zoom 2>/dev/null || which zoom",
    "Verify: zoom --version",
    "supercli plugins install ./plugins/zoom --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}