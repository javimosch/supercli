{ "plugin": "dizion", "binary": "dizion", "check": "which dizion", "install_steps": ["apt-get install dizion 2>/dev/null || which dizion", "Verify: dizion --version", "supercli plugins install ./plugins/dizion --on-conflict replace --json"] }
