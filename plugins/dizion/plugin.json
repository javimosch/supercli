{
  "name": "dizion",
  "version": "0.1.0",
  "description": "CLI tool: dizion",
  "source": "https://packages.debian.org/dizion",
  "checks": [{ "type": "binary", "name": "dizion" }],
  "install_guidance": {
    "plugin": "dizion", "binary": "dizion", "check": "which dizion",
    "install_steps": ["apt-get install dizion 2>/dev/null || which dizion", "Verify: dizion --version", "supercli plugins install ./plugins/dizion --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dizion", "resource": "_", "action": "_",
    "description": "Passthrough to dizion CLI",
    "adapter": "process", "adapterConfig": { "command": "dizion", "passthrough": true, "missingDependencyHelp": "Install dizion: apt-get install dizion" }, "args": []
  }]
}
