{
  "plugin": "json-cli",
  "binary": "json",
  "check": "which json",
  "install_steps": [
    "npm install -g json",
    "Verify: json --version",
    "supercli plugins install ./plugins/json-cli --on-conflict replace --json"
  ],
  "note": "The json CLI tool provides powerful JSON manipulation capabilities including key extraction, value extraction, path filtering, and more. Works with stdin/stdout for pipeline operations."
}
