{
  "plugin": "huniq",
  "binary": "huniq",
  "check": "which huniq",
  "install_steps": [
    "cargo install huniq",
    "Verify: huniq --version",
    "supercli plugins install ./plugins/huniq --on-conflict replace --json"
  ]
}