{
  "plugin": "linkding-cli",
  "binary": "linkding-cli",
  "check": "which linkding-cli",
  "install_steps": ["pip install linkding-cli", "Verify: linkding-cli --version", "supercli plugins install ./plugins/linkding-cli --on-conflict replace --json"]
}
