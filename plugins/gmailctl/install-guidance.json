{
  "plugin": "gmailctl",
  "binary": "gmailctl",
  "check": "which gmailctl",
  "install_steps": [
    "brew install gmailctl",
    "Verify: gmailctl --version",
    "supercli plugins install ./plugins/gmailctl --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/mbrt/gmailctl@latest. Declarative Gmail filter management as code. Version control your email automation with a simple DSL."
}
