{
  "plugin": "todo.txt-cli",
  "binary": "todo",
  "check": "which todo",
  "install_steps": [
    "brew install todo-txt",
    "Verify: todo --version",
    "supercli plugins install ./plugins/todo.txt-cli --on-conflict replace --json"
  ],
  "note": "Install via brew: brew install todo-txt"
}
