{ "plugin": "code-server", "binary": "code-server", "check": "which code-server", "install_steps": ["apt-get install code-server 2>/dev/null || which code-server", "Verify: code-server --version", "supercli plugins install ./plugins/code-server --on-conflict replace --json"] }
