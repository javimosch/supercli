{
  "plugin": "cconv",
  "binary": "cconv",
  "check": "which cconv",
  "install_steps": [
    "brew install cconv",
    "Verify: cconv --version",
    "supercli plugins install ./plugins/cconv --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}