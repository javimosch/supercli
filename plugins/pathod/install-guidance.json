{
  "plugin": "pathod",
  "binary": "pathod",
  "check": "which pathod",
  "install_steps": [
    "apt-get install pathod 2>/dev/null || which pathod",
    "Verify: pathod --version",
    "supercli plugins install ./plugins/pathod --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}