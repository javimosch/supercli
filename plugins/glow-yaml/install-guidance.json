{
  "plugin": "glow-yaml",
  "binary": "glow",
  "check": "which glow",
  "install_steps": ["go install github.com/charmbracelet/glow@latest", "Verify: glow --version", "supercli plugins install ./plugins/glow-yaml --on-conflict replace --json"]
}
