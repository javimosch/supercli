{
  "plugin": "mkcert",
  "binary": "mkcert",
  "check": "which mkcert",
  "install_steps": [
    "brew install mkcert",
    "Verify: mkcert --version",
    "supercli plugins install ./plugins/mkcert --on-conflict replace --json"
  ]
}