{ "plugin": "bwrap", "binary": "bwrap", "check": "which bwrap", "install_steps": ["apt-get install bwrap 2>/dev/null || which bwrap", "Verify: bwrap --version", "supercli plugins install ./plugins/bwrap --on-conflict replace --json"] }
