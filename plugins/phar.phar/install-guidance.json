{
  "plugin": "phar.phar",
  "binary": "phar.phar",
  "check": "which phar.phar",
  "install_steps": [
    "apt-get install phar.phar 2>/dev/null || which phar.phar",
    "Verify: phar.phar --version",
    "supercli plugins install ./plugins/phar.phar --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}