{
  "name": "dh_installemacsen",
  "version": "0.1.0",
  "description": "CLI tool: dh_installemacsen",
  "source": "https://packages.debian.org/dh_installemacsen",
  "checks": [{ "type": "binary", "name": "dh_installemacsen" }],
  "install_guidance": {
    "plugin": "dh_installemacsen", "binary": "dh_installemacsen", "check": "which dh_installemacsen",
    "install_steps": ["apt-get install dh_installemacsen 2>/dev/null || which dh_installemacsen", "Verify: dh_installemacsen --version", "supercli plugins install ./plugins/dh_installemacsen --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_installemacsen", "resource": "_", "action": "_",
    "description": "Passthrough to dh_installemacsen CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_installemacsen", "passthrough": true, "missingDependencyHelp": "Install dh_installemacsen: apt-get install dh_installemacsen" }, "args": []
  }]
}
