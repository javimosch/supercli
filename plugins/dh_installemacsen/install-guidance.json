{ "plugin": "dh_installemacsen", "binary": "dh_installemacsen", "check": "which dh_installemacsen", "install_steps": ["apt-get install dh_installemacsen 2>/dev/null || which dh_installemacsen", "Verify: dh_installemacsen --version", "supercli plugins install ./plugins/dh_installemacsen --on-conflict replace --json"] }
