{
  "name": "ata_id",
  "version": "0.1.0",
  "description": "CLI tool: ata_id",
  "source": "https://packages.debian.org/ata_id",
  "checks": [{ "type": "binary", "name": "ata_id" }],
  "install_guidance": {
    "plugin": "ata_id", "binary": "ata_id", "check": "which ata_id",
    "install_steps": ["apt-get install ata_id 2>/dev/null || which ata_id", "Verify: ata_id --version", "supercli plugins install ./plugins/ata_id --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ata_id", "resource": "_", "action": "_",
    "description": "Passthrough to ata_id CLI",
    "adapter": "process", "adapterConfig": { "command": "ata_id", "passthrough": true, "missingDependencyHelp": "Install ata_id: apt-get install ata_id" }, "args": []
  }]
}
