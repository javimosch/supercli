{ "plugin": "ata_id", "binary": "ata_id", "check": "which ata_id", "install_steps": ["apt-get install ata_id 2>/dev/null || which ata_id", "Verify: ata_id --version", "supercli plugins install ./plugins/ata_id --on-conflict replace --json"] }
