{
  "name": "dh_testdir",
  "version": "0.1.0",
  "description": "CLI tool: dh_testdir",
  "source": "https://packages.debian.org/dh_testdir",
  "checks": [{ "type": "binary", "name": "dh_testdir" }],
  "install_guidance": {
    "plugin": "dh_testdir", "binary": "dh_testdir", "check": "which dh_testdir",
    "install_steps": ["apt-get install dh_testdir 2>/dev/null || which dh_testdir", "Verify: dh_testdir --version", "supercli plugins install ./plugins/dh_testdir --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_testdir", "resource": "_", "action": "_",
    "description": "Passthrough to dh_testdir CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_testdir", "passthrough": true, "missingDependencyHelp": "Install dh_testdir: apt-get install dh_testdir" }, "args": []
  }]
}
