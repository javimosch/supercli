{ "plugin": "dh_testdir", "binary": "dh_testdir", "check": "which dh_testdir", "install_steps": ["apt-get install dh_testdir 2>/dev/null || which dh_testdir", "Verify: dh_testdir --version", "supercli plugins install ./plugins/dh_testdir --on-conflict replace --json"] }
