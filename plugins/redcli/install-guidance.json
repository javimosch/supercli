{
  "plugin": "redcli",
  "binary": "redcli",
  "check": "which redcli",
  "install_steps": ["go install github.com/redcli@latest", "Verify: redcli --version", "supercli plugins install ./plugins/redcli --on-conflict replace --json"]
}
