{ "plugin": "appres", "binary": "appres", "check": "which appres", "install_steps": ["apt-get install appres 2>/dev/null || which appres", "Verify: appres --version", "supercli plugins install ./plugins/appres --on-conflict replace --json"] }
