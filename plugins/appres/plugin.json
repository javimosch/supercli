{
  "name": "appres",
  "version": "0.1.0",
  "description": "CLI tool: appres",
  "source": "https://packages.debian.org/appres",
  "checks": [{ "type": "binary", "name": "appres" }],
  "install_guidance": {
    "plugin": "appres", "binary": "appres", "check": "which appres",
    "install_steps": ["apt-get install appres 2>/dev/null || which appres", "Verify: appres --version", "supercli plugins install ./plugins/appres --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "appres", "resource": "_", "action": "_",
    "description": "Passthrough to appres CLI",
    "adapter": "process", "adapterConfig": { "command": "appres", "passthrough": true, "missingDependencyHelp": "Install appres: apt-get install appres" }, "args": []
  }]
}
