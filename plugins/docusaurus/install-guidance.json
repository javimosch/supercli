{
  "plugin": "docusaurus",
  "binary": "docusaurus",
  "check": "which docusaurus",
  "install_steps": [
    "npm install -g docusaurus",
    "Verify: docusaurus --version",
    "supercli plugins install ./plugins/docusaurus --on-conflict replace --json"
  ]
}
