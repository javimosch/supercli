{
  "plugin": "pixterm",
  "binary": "pixterm",
  "check": "which pixterm",
  "install_steps": [
    "brew install pixterm",
    "Verify: pixterm --version",
    "supercli plugins install ./plugins/pixterm --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/eliukblau/pixterm/cmd/pixterm@latest. A terminal image viewer using ANSI escape codes. Display images directly in the terminal with TrueColor support. Great for image previews in SSH sessions."
}
