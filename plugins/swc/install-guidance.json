{
  "plugin": "swc",
  "binary": "swc",
  "check": "which swc",
  "install_steps": [
    "npm install -g @swc/cli",
    "Verify: swc --version",
    "supercli plugins install ./plugins/swc --on-conflict replace --json"
  ]
}
