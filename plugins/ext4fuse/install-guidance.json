{
  "plugin": "ext4fuse",
  "binary": "ext4fuse",
  "check": "which ext4fuse",
  "install_steps": [
    "brew install ext4fuse",
    "Verify: ext4fuse --version",
    "supercli plugins install ./plugins/ext4fuse --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}