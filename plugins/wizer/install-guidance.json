{
  "plugin": "wizer",
  "binary": "wizer",
  "check": "which wizer",
  "install_steps": [
    "cargo install wizer",
    "Verify: wizer --version",
    "supercli plugins install ./plugins/wizer --on-conflict replace --json"
  ],
  "note": "Binary name: wizer"
}
