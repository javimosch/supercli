{
  "plugin": "ouch",
  "binary": "ouch",
  "check": "which ouch",
  "install_steps": [
    "cargo install ouch",
    "Verify: ouch --version",
    "supercli plugins install ./plugins/ouch --on-conflict replace --json"
  ],
  "note": "Also available via: brew, pacman, scoop. Supports 15+ formats including zip, tar, 7z, gz, zst, xz, lz4, rar."
}