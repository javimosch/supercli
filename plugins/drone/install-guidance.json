{
  "plugin": "drone",
  "binary": "drone",
  "check": "which drone",
  "install_steps": [
    "brew install drone",
    "Verify: drone --version",
    "supercli plugins install ./plugins/drone --on-conflict replace --json"
  ]
}