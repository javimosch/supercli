{
  "plugin": "just-cli",
  "binary": "just",
  "check": "which just",
  "install_steps": [
    "brew install just",
    "Verify: just --version",
    "supercli plugins install ./plugins/just-cli --on-conflict replace --json"
  ]
}
