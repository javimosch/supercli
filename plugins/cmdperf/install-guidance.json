{
  "plugin": "cmdperf",
  "binary": "cmdperf",
  "check": "which cmdperf",
  "install_steps": ["cargo install cmdperf", "Verify: cmdperf --version", "supercli plugins install ./plugins/cmdperf --on-conflict replace --json"]
}
