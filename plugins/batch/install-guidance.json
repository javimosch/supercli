{ "plugin": "batch", "binary": "batch", "check": "which batch", "install_steps": ["apt-get install at", "Verify: batch --version", "supercli plugins install ./plugins/batch --on-conflict replace --json"] }
