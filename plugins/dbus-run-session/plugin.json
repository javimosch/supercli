{
  "name": "dbus-run-session",
  "version": "1.0.0",
  "description": "dbus-run-session \u2014 dbus-run-session \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dbus-run-session"
    }
  ],
  "install_guidance": {
    "plugin": "dbus-run-session",
    "binary": "dbus-run-session",
    "check": "which dbus-run-session",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dbus-run-session",
      "resource": "run",
      "action": "exec",
      "description": "dbus-run-session \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dbus-run-session",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}