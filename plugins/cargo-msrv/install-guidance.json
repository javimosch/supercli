{
  "plugin": "cargo-msrv",
  "binary": "cargo-msrv",
  "check": "cargo msrv --version",
  "install_steps": [
    "cargo install cargo-msrv",
    "Verify: cargo msrv --version",
    "supercli plugins install ./plugins/cargo-msrv --on-conflict replace --json"
  ],
  "note": "Cargo subcommand to determine the Minimum Supported Rust Version (MSRV). Great for verifying Rust version compatibility for crates and projects. Supports searching, verifying, and listing MSRV."
}
