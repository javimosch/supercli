{
  "plugin": "witr",
  "binary": "witr",
  "check": "which witr",
  "install_steps": [
    "brew install witr",
    "Verify: witr --version",
    "supercli plugins install ./plugins/witr --on-conflict replace --json"
  ],
  "note": "Also installable via: go install github.com/pranshuparmar/witr/cmd/witr@latest, npm install -g @pranshuparmar/witr, or curl from GitHub releases. Supports --json output. Some features may require sudo."
}
