{
  "plugin": "onerepo",
  "binary": "onerepo",
  "check": "which onerepo",
  "install_steps": [
    "npm install -g onerepo",
    "Verify: onerepo --version",
    "supercli plugins install ./plugins/onerepo --on-conflict replace --json"
  ]
}