{
  "plugin": "fastfetch",
  "binary": "fastfetch",
  "check": "which fastfetch",
  "install_steps": [
    "brew install fastfetch",
    "Verify: fastfetch --version",
    "supercli plugins install ./plugins/fastfetch --on-conflict replace --json"
  ],
  "note": "Also available via: apt, pacman, dnf, scoop, winget, choco, pkg. Pre-built binaries on GitHub releases."
}
