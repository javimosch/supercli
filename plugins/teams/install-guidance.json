{
  "plugin": "teams",
  "binary": "teams",
  "check": "which teams",
  "install_steps": [
    "apt-get install teams 2>/dev/null || which teams",
    "Verify: teams --version",
    "supercli plugins install ./plugins/teams --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}