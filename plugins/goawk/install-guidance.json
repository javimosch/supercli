{
  "plugin": "goawk",
  "binary": "goawk",
  "check": "which goawk",
  "install_steps": [
    "brew install goawk",
    "Verify: goawk --version",
    "supercli plugins install ./plugins/goawk --on-conflict replace --json"
  ]
}