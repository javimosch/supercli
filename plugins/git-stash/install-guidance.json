{
  "plugin": "git-stash",
  "binary": "git",
  "check": "which git",
  "install_steps": [
    "Install git: https://git-scm.com",
    "macOS: brew install git",
    "Linux: apt install git / dnf install git",
    "Verify: git --version"
  ],
  "note": "Requires git. Works in any git repository."
}
