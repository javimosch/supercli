{
  "plugin": "kubelogin",
  "binary": "kubelogin",
  "check": "which kubelogin",
  "install_steps": ["brew install kubelogin", "Verify: kubelogin --version", "supercli plugins install ./plugins/kubelogin --on-conflict replace --json"]
}
