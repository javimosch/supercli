{
  "plugin": "demumble",
  "binary": "demumble",
  "check": "which demumble",
  "install_steps": [
    "brew install demumble",
    "Verify: demumble --version",
    "supercli plugins install ./plugins/demumble --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}