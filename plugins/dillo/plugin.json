{
  "name": "dillo",
  "version": "0.1.0",
  "description": "CLI tool: dillo",
  "source": "https://packages.debian.org/dillo",
  "checks": [{ "type": "binary", "name": "dillo" }],
  "install_guidance": {
    "plugin": "dillo", "binary": "dillo", "check": "which dillo",
    "install_steps": ["apt-get install dillo 2>/dev/null || which dillo", "Verify: dillo --version", "supercli plugins install ./plugins/dillo --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dillo", "resource": "_", "action": "_",
    "description": "Passthrough to dillo CLI",
    "adapter": "process", "adapterConfig": { "command": "dillo", "passthrough": true, "missingDependencyHelp": "Install dillo: apt-get install dillo" }, "args": []
  }]
}
