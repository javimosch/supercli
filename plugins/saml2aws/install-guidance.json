{
  "plugin": "saml2aws",
  "binary": "saml2aws",
  "check": "which saml2aws",
  "install_steps": ["brew install saml2aws", "Verify: saml2aws --version", "supercli plugins install ./plugins/saml2aws --on-conflict replace --json"]
}
