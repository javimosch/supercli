{
  "plugin": "servers",
  "binary": "servers",
  "check": "which servers",
  "install_steps": [
    "pip install servers",
    "Verify: servers --version",
    "supercli plugins install ./plugins/servers --on-conflict replace --json"
  ],
  "note": "Language: TypeScript. Stars: 86684"
}