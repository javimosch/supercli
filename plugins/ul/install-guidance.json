{
  "plugin": "ul",
  "binary": "ul",
  "check": "which ul",
  "install_steps": [
    "apt-get install ul 2>/dev/null || which ul",
    "Verify: ul --version",
    "supercli plugins install ./plugins/ul --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}