{ "plugin": "dbiproxy", "binary": "dbiproxy", "check": "which dbiproxy", "install_steps": ["apt-get install dbiproxy 2>/dev/null || which dbiproxy", "Verify: dbiproxy --version", "supercli plugins install ./plugins/dbiproxy --on-conflict replace --json"] }
