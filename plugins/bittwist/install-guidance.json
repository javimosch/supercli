{ "plugin": "bittwist", "binary": "bittwist", "check": "which bittwist", "install_steps": ["apt-get install bittwist 2>/dev/null || which bittwist", "Verify: bittwist --version", "supercli plugins install ./plugins/bittwist --on-conflict replace --json"] }
