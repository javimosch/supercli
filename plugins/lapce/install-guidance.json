{
  "plugin": "lapce",
  "binary": "lapce",
  "check": "which lapce",
  "install_steps": ["brew install lapce", "Verify: lapce --version", "supercli plugins install ./plugins/lapce --on-conflict replace --json"]
}
