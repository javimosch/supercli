{
  "plugin": "flask",
  "binary": "flask",
  "check": "which flask",
  "install_steps": [
    "apt-get install flask 2>/dev/null || which flask",
    "Verify: flask --version",
    "supercli plugins install ./plugins/flask --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}