{
  "plugin": "gvgen",
  "binary": "gvgen",
  "check": "which gvgen",
  "install_steps": [
    "apt-get install gvgen 2>/dev/null || which gvgen",
    "Verify: gvgen --version",
    "supercli plugins install ./plugins/gvgen --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}