---
name: lupine
description: Use this skill when the user wants to use lupine for LUPINE is a GPU over IP bridge allowing GPUs on remote machines to be attached t.
---

# lupine Plugin

LUPINE is a GPU over IP bridge allowing GPUs on remote machines to be attached to CPU-only machines.

## Commands

### General
- `lupine self version` — Print lupine version
- `lupine _ _` — Passthrough to lupine CLI

## Usage Examples
- "Run lupine"
- "Use lupine to LUPINE is a GPU over IP bridge allowing GPUs on remote machi"

## Installation

```bash
pip install lupine
```

## Key Features
- LUPINE is a GPU over IP bridge allowing GPUs on remote machines to be attached to CPU-only machines.
- CLI tool, headless and scriptable
