---
name: jd
description: jd — JSON diff CLI. Compute semantic diffs between JSON files, not just text lines.
---
# jd
jd — JSON diff CLI. Compute semantic diffs between JSON files, not just text lines.
