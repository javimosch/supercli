{
  "plugin": "parsort",
  "binary": "parsort",
  "check": "which parsort",
  "install_steps": [
    "apt-get install parsort 2>/dev/null || which parsort",
    "Verify: parsort --version",
    "supercli plugins install ./plugins/parsort --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}