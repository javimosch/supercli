{
  "plugin": "opcode",
  "binary": "opcode",
  "check": "which opcode",
  "install_steps": [
    "apt-get install opcode 2>/dev/null || which opcode",
    "Verify: opcode --version",
    "supercli plugins install ./plugins/opcode --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}