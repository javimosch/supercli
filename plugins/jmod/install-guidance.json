{
  "plugin": "jmod",
  "binary": "jmod",
  "check": "which jmod",
  "install_steps": [
    "apt-get install jmod 2>/dev/null || which jmod",
    "Verify: jmod --version",
    "supercli plugins install ./plugins/jmod --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}