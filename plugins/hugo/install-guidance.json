{
  "plugin": "hugo",
  "binary": "hugo",
  "check": "which hugo",
  "install_steps": [
    "brew install hugo",
    "Verify: hugo --version",
    "supercli plugins install ./plugins/hugo --on-conflict replace --json"
  ]
}
