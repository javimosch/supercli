{
  "plugin": "just-lsp",
  "binary": "just-lsp",
  "check": "which just-lsp",
  "install_steps": [
    "brew install just-lsp",
    "Verify: just-lsp --version",
    "supercli plugins install ./plugins/just-lsp --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}