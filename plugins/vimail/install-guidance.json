{
  "plugin": "vimail",
  "binary": "vimail",
  "check": "which vimail",
  "install_steps": ["go install github.com/gausejakub/vimail@latest", "Verify: vimail --version", "supercli plugins install ./plugins/vimail --on-conflict replace --json"]
}
