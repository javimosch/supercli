{
  "plugin": "kumo",
  "binary": "kumo",
  "check": "which kumo",
  "install_steps": [
    "brew install kumo",
    "Verify: kumo --version",
    "supercli plugins install ./plugins/kumo --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}