{
  "name": "db_upgrade",
  "version": "0.1.0",
  "description": "CLI tool: db_upgrade",
  "source": "https://packages.debian.org/db_upgrade",
  "checks": [{ "type": "binary", "name": "db_upgrade" }],
  "install_guidance": {
    "plugin": "db_upgrade", "binary": "db_upgrade", "check": "which db_upgrade",
    "install_steps": ["apt-get install db_upgrade 2>/dev/null || which db_upgrade", "Verify: db_upgrade --version", "supercli plugins install ./plugins/db_upgrade --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_upgrade", "resource": "_", "action": "_",
    "description": "Passthrough to db_upgrade CLI",
    "adapter": "process", "adapterConfig": { "command": "db_upgrade", "passthrough": true, "missingDependencyHelp": "Install db_upgrade: apt-get install db_upgrade" }, "args": []
  }]
}
