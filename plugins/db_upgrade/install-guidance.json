{ "plugin": "db_upgrade", "binary": "db_upgrade", "check": "which db_upgrade", "install_steps": ["apt-get install db_upgrade 2>/dev/null || which db_upgrade", "Verify: db_upgrade --version", "supercli plugins install ./plugins/db_upgrade --on-conflict replace --json"] }
