{
  "name": "dhall-yaml",
  "version": "0.1.0",
  "description": "CLI tool: dhall-yaml",
  "source": "https://packages.debian.org/dhall-yaml",
  "checks": [{ "type": "binary", "name": "dhall-yaml" }],
  "install_guidance": {
    "plugin": "dhall-yaml", "binary": "dhall-yaml", "check": "which dhall-yaml",
    "install_steps": ["apt-get install dhall-yaml 2>/dev/null || which dhall-yaml", "Verify: dhall-yaml --version", "supercli plugins install ./plugins/dhall-yaml --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dhall-yaml", "resource": "_", "action": "_",
    "description": "Passthrough to dhall-yaml CLI",
    "adapter": "process", "adapterConfig": { "command": "dhall-yaml", "passthrough": true, "missingDependencyHelp": "Install dhall-yaml: apt-get install dhall-yaml" }, "args": []
  }]
}
