{
  "plugin": "tock",
  "binary": "tock",
  "check": "which tock",
  "install_steps": [
    "brew install kriuchkov/tap/tock",
    "Verify: tock --version",
    "supercli plugins install ./plugins/tock --on-conflict replace --json"
  ],
  "note": "Also available via go install (go install github.com/kriuchkov/tock/cmd/tock@latest), install script (curl -sS https://raw.githubusercontent.com/kriuchkov/tock/master/install.sh | sh), or prebuilt binaries from GitHub Releases."
}
