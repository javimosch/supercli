{
  "plugin": "ares",
  "binary": "ares",
  "check": "which ares",
  "install_steps": [
    "brew install ares",
    "Verify: ares --version",
    "supercli plugins install ./plugins/ares --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}