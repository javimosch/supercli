{
  "plugin": "flomo",
  "binary": "flomo",
  "check": "which flomo",
  "install_steps": [
    "pip install flomo",
    "Verify: flomo --version",
    "supercli plugins install ./plugins/flomo --on-conflict replace --json"
  ]
}
