{
  "plugin": "prlimit",
  "binary": "prlimit",
  "check": "which prlimit",
  "install_steps": [
    "apt-get install prlimit 2>/dev/null || which prlimit",
    "Verify: prlimit --version",
    "supercli plugins install ./plugins/prlimit --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}