{
  "plugin": "poetry2setup",
  "binary": "poetry2setup",
  "check": "which poetry2setup",
  "install_steps": ["pip install poetry2setup", "Verify: poetry2setup --version", "supercli plugins install ./plugins/poetry2setup --on-conflict replace --json"]
}
