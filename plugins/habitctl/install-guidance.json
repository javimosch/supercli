{
  "plugin": "habitctl",
  "binary": "habitctl",
  "check": "which habitctl",
  "install_steps": [
    "See https://github.com/blinry/habitctl",
    "Verify: habitctl --version",
    "supercli plugins install ./plugins/habitctl --on-conflict replace --json"
  ],
  "note": "See project repository"
}