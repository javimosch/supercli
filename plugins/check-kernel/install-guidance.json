{ "plugin": "check-kernel", "binary": "check-kernel", "check": "which check-kernel", "install_steps": ["apt-get install check-kernel 2>/dev/null || which check-kernel", "Verify: check-kernel --version", "supercli plugins install ./plugins/check-kernel --on-conflict replace --json"] }
