{
  "plugin": "pnmscale",
  "binary": "pnmscale",
  "check": "which pnmscale",
  "install_steps": [
    "apt-get install pnmscale 2>/dev/null || which pnmscale",
    "Verify: pnmscale --version",
    "supercli plugins install ./plugins/pnmscale --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}