{
  "plugin": "tag-stats",
  "binary": "tag-stats",
  "check": "which tag-stats",
  "install_steps": ["cargo install tag-stats", "Verify: tag-stats --version", "supercli plugins install ./plugins/tag-stats --on-conflict replace --json"]
}
