{
  "plugin": "load-tester",
  "binary": "load-tester",
  "check": "which load-tester",
  "install_steps": ["go install github.com/load-tester/cli@latest", "Verify: load-tester --version", "supercli plugins install ./plugins/load-tester --on-conflict replace --json"]
}
