{
  "plugin": "gren",
  "binary": "gren",
  "check": "which gren",
  "install_steps": ["npm install -g github-release-notes", "Verify: gren --version", "supercli plugins install ./plugins/gren --on-conflict replace --json"]
}
