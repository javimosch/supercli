{
  "name": "dkg",
  "version": "0.1.0",
  "description": "CLI tool: dkg",
  "source": "https://packages.debian.org/dkg",
  "checks": [{ "type": "binary", "name": "dkg" }],
  "install_guidance": {
    "plugin": "dkg", "binary": "dkg", "check": "which dkg",
    "install_steps": ["apt-get install dkg 2>/dev/null || which dkg", "Verify: dkg --version", "supercli plugins install ./plugins/dkg --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dkg", "resource": "_", "action": "_",
    "description": "Passthrough to dkg CLI",
    "adapter": "process", "adapterConfig": { "command": "dkg", "passthrough": true, "missingDependencyHelp": "Install dkg: apt-get install dkg" }, "args": []
  }]
}
