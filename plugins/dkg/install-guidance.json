{ "plugin": "dkg", "binary": "dkg", "check": "which dkg", "install_steps": ["apt-get install dkg 2>/dev/null || which dkg", "Verify: dkg --version", "supercli plugins install ./plugins/dkg --on-conflict replace --json"] }
