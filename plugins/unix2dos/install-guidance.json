{
  "plugin": "unix2dos",
  "binary": "unix2dos",
  "check": "which unix2dos",
  "install_steps": [
    "brew install dos2unix",
    "Verify: unix2dos --version",
    "supercli plugins install ./plugins/unix2dos --on-conflict replace --json"
  ]
}
