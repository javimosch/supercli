{
  "plugin": "gotun",
  "binary": "gotun",
  "check": "which gotun",
  "install_steps": [
    "go install github.com/Sesame2/gotun@latest",
    "Verify: gotun --version",
    "supercli plugins install ./plugins/gotun --on-conflict replace --json"
  ]
}