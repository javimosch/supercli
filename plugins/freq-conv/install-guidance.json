{
  "plugin": "freq-conv",
  "binary": "freq-conv",
  "check": "which freq-conv",
  "install_steps": ["go install github.com/freq-conv@latest", "Verify: freq-conv --version", "supercli plugins install ./plugins/freq-conv --on-conflict replace --json"]
}
