{ "plugin": "cups", "binary": "cups", "check": "which cups", "install_steps": ["apt-get install cups 2>/dev/null || which cups", "Verify: cups --version", "supercli plugins install ./plugins/cups --on-conflict replace --json"] }
