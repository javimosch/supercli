{
  "plugin": "delta",
  "binary": "delta",
  "check": "which delta",
  "install_steps": [
    "brew install git-delta",
    "Verify: delta --version",
    "supercli plugins install ./plugins/delta --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install git-delta. A syntax-highlighting pager for git and diff output. Configured as git core.pager for beautiful diffs. Supports side-by-side comparison, line numbers, and many themes. Great for improving git diff readability."
}
