---
name: helix-db
description: Use this skill when the user wants to use helix-db for HelixDB is an open-source graph-vector database built from scratch in Rust..
---

# helix-db Plugin

HelixDB is an open-source graph-vector database built from scratch in Rust.

## Commands

### General
- `helix-db self version` — Print helix-db version
- `helix-db _ _` — Passthrough to helix-db CLI

## Usage Examples
- "Run helix-db"
- "Use helix-db to HelixDB is an open-source graph-vector database built from s"

## Installation

```bash
pip install helix-db
```

## Key Features
- HelixDB is an open-source graph-vector database built from scratch in Rust.
- CLI tool, headless and scriptable
