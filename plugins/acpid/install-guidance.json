{ "plugin": "acpid", "binary": "acpid", "check": "which acpid", "install_steps": ["apt-get install acpid 2>/dev/null || which acpid", "Verify: acpid --version", "supercli plugins install ./plugins/acpid --on-conflict replace --json"] }
