{
  "name": "acpid",
  "version": "0.1.0",
  "description": "CLI tool: acpid",
  "source": "https://packages.debian.org/acpid",
  "checks": [{ "type": "binary", "name": "acpid" }],
  "install_guidance": {
    "plugin": "acpid", "binary": "acpid", "check": "which acpid",
    "install_steps": ["apt-get install acpid 2>/dev/null || which acpid", "Verify: acpid --version", "supercli plugins install ./plugins/acpid --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acpid", "resource": "_", "action": "_",
    "description": "Passthrough to acpid CLI",
    "adapter": "process", "adapterConfig": { "command": "acpid", "passthrough": true, "missingDependencyHelp": "Install acpid: apt-get install acpid" }, "args": []
  }]
}
