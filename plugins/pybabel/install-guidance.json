{
  "plugin": "pybabel",
  "binary": "pybabel",
  "check": "which pybabel",
  "install_steps": [
    "apt-get install pybabel 2>/dev/null || which pybabel",
    "Verify: pybabel --version",
    "supercli plugins install ./plugins/pybabel --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}