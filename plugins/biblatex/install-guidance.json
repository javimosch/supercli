{
  "plugin": "biblatex",
  "binary": "biblatex",
  "check": "which biblatex",
  "install_steps": [
    "brew install biblatex",
    "Verify: biblatex --version",
    "supercli plugins install ./plugins/biblatex --on-conflict replace --json"
  ]
}
