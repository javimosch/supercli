{
  "plugin": "crane",
  "binary": "crane",
  "check": "which crane",
  "install_steps": [
    "brew install crane",
    "Verify: crane --version",
    "supercli plugins install ./plugins/crane --on-conflict replace --json"
  ]
}