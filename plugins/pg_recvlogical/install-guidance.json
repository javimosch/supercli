{
  "plugin": "pg_recvlogical",
  "binary": "pg_recvlogical",
  "check": "which pg_recvlogical",
  "install_steps": [
    "apt-get install pg_recvlogical 2>/dev/null || which pg_recvlogical",
    "Verify: pg_recvlogical --version",
    "supercli plugins install ./plugins/pg_recvlogical --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}