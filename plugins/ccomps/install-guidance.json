{ "plugin": "ccomps", "binary": "ccomps", "check": "which ccomps", "install_steps": ["apt-get install ccomps 2>/dev/null || which ccomps", "Verify: ccomps --version", "supercli plugins install ./plugins/ccomps --on-conflict replace --json"] }
