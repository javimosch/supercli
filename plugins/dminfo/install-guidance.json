{ "plugin": "dminfo", "binary": "dminfo", "check": "which dminfo", "install_steps": ["apt-get install dminfo 2>/dev/null || which dminfo", "Verify: dminfo --version", "supercli plugins install ./plugins/dminfo --on-conflict replace --json"] }
