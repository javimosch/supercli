{
  "name": "dminfo",
  "version": "0.1.0",
  "description": "CLI tool: dminfo",
  "source": "https://packages.debian.org/dminfo",
  "checks": [{ "type": "binary", "name": "dminfo" }],
  "install_guidance": {
    "plugin": "dminfo", "binary": "dminfo", "check": "which dminfo",
    "install_steps": ["apt-get install dminfo 2>/dev/null || which dminfo", "Verify: dminfo --version", "supercli plugins install ./plugins/dminfo --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dminfo", "resource": "_", "action": "_",
    "description": "Passthrough to dminfo CLI",
    "adapter": "process", "adapterConfig": { "command": "dminfo", "passthrough": true, "missingDependencyHelp": "Install dminfo: apt-get install dminfo" }, "args": []
  }]
}
