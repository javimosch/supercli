{
  "plugin": "tflint",
  "binary": "tflint",
  "check": "which tflint",
  "install_steps": ["brew install tflint", "Verify: tflint --version", "supercli plugins install ./plugins/tflint --on-conflict replace --json"]
}
