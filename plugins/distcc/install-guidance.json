{ "plugin": "distcc", "binary": "distcc", "check": "which distcc", "install_steps": ["apt-get install distcc 2>/dev/null || which distcc", "Verify: distcc --version", "supercli plugins install ./plugins/distcc --on-conflict replace --json"] }
