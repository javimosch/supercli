{
  "plugin": "gifview",
  "binary": "gifview",
  "check": "which gifview",
  "install_steps": [
    "apt-get install gifview 2>/dev/null || which gifview",
    "Verify: gifview --version",
    "supercli plugins install ./plugins/gifview --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}