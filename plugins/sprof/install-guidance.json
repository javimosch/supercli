{
  "plugin": "sprof",
  "binary": "sprof",
  "check": "which sprof",
  "install_steps": [
    "apt-get install sprof 2>/dev/null || which sprof",
    "Verify: sprof --version",
    "supercli plugins install ./plugins/sprof --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}