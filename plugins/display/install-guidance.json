{ "plugin": "display", "binary": "display", "check": "which display", "install_steps": ["apt-get install display 2>/dev/null || which display", "Verify: display --version", "supercli plugins install ./plugins/display --on-conflict replace --json"] }
