{
  "name": "display",
  "version": "0.1.0",
  "description": "CLI tool: display",
  "source": "https://packages.debian.org/display",
  "checks": [{ "type": "binary", "name": "display" }],
  "install_guidance": {
    "plugin": "display", "binary": "display", "check": "which display",
    "install_steps": ["apt-get install display 2>/dev/null || which display", "Verify: display --version", "supercli plugins install ./plugins/display --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "display", "resource": "_", "action": "_",
    "description": "Passthrough to display CLI",
    "adapter": "process", "adapterConfig": { "command": "display", "passthrough": true, "missingDependencyHelp": "Install display: apt-get install display" }, "args": []
  }]
}
