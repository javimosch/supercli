{
  "plugin": "alex",
  "binary": "alex",
  "check": "which alex",
  "install_steps": [
    "npm install -g alex",
    "Verify: alex --version",
    "supercli plugins install ./plugins/alex --on-conflict replace --json"
  ]
}
