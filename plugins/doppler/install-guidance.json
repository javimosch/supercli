{
  "plugin": "doppler",
  "binary": "doppler",
  "check": "which doppler",
  "install_steps": [
    "brew install dopplerhq/cli/doppler",
    "Verify: doppler --version",
    "supercli plugins install ./plugins/doppler --on-conflict replace --json"
  ],
  "note": "Also available via: npm install -g @dopplerhq/cli or curl -sL https://cli.doppler.com/install.sh | sh. Authenticate with: doppler login."
}
