---
name: git-fame
description: Git repository contributor statistics. Shows lines added/deleted per author with optional JSON output.
---
# git-fame Plugin
Git repository contributor statistics. Shows lines added/deleted per author with optional JSON output.
## Commands
- `git self version` — Print version
- `git _ _` — Passthrough to git-fame
## Installation
```bash
brew install git-fame
```
