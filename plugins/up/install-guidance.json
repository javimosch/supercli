{
  "plugin": "up",
  "binary": "up",
  "check": "which up",
  "install_steps": ["curl https://up.apex.sh/install ", "Verify: up --version", "supercli plugins install ./plugins/up --on-conflict replace --json"]
}
