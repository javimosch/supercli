{
  "plugin": "klog",
  "binary": "klog",
  "check": "which klog",
  "install_steps": [
    "brew install klog",
    "Verify: klog --version",
    "supercli plugins install ./plugins/klog --on-conflict replace --json"
  ]
}