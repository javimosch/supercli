{
  "plugin": "ruff",
  "binary": "ruff",
  "check": "which ruff",
  "install_steps": [
    "pip install ruff",
    "Verify: ruff --version",
    "supercli plugins install ./plugins/ruff --on-conflict replace --json"
  ],
  "note": "Also available via: cargo, brew, conda. Extremely fast Rust-based linter/formatter."
}
