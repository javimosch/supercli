{
  "plugin": "flite",
  "binary": "flite",
  "check": "which flite",
  "install_steps": ["sudo apt install flite", "Verify: flite --version", "supercli plugins install ./plugins/flite --on-conflict replace --json"]
}
