{
  "plugin": "dcp",
  "binary": "dcp",
  "check": "which dcp",
  "install_steps": [
    "brew install dcp",
    "Verify: dcp --version",
    "supercli plugins install ./plugins/dcp --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}