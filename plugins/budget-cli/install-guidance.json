{
  "plugin": "budget-cli",
  "binary": "budget-cli",
  "check": "which budget-cli",
  "install_steps": ["go install github.com/budget-cli@latest", "Verify: budget-cli --version", "supercli plugins install ./plugins/budget-cli --on-conflict replace --json"]
}
