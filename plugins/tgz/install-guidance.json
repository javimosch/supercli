{
  "plugin": "tgz",
  "binary": "tgz",
  "check": "which tgz",
  "install_steps": [
    "apt-get install tgz 2>/dev/null || which tgz",
    "Verify: tgz --version",
    "supercli plugins install ./plugins/tgz --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}