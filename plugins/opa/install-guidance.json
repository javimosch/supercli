{
  "plugin": "opa",
  "binary": "opa",
  "check": "which opa",
  "install_steps": [
    "brew install opa",
    "Verify: opa --version",
    "supercli plugins install ./plugins/opa --on-conflict replace --json"
  ]
}