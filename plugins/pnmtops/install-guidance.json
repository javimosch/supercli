{
  "plugin": "pnmtops",
  "binary": "pnmtops",
  "check": "which pnmtops",
  "install_steps": [
    "apt-get install pnmtops 2>/dev/null || which pnmtops",
    "Verify: pnmtops --version",
    "supercli plugins install ./plugins/pnmtops --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}