---
name: dockerd-rootless-setuptool.sh
description: Use this skill when the user wants to use dockerd-rootless-setuptool.sh, cli tool: dockerd-rootless-setuptool.sh.
---

# dockerd-rootless-setuptool.sh Plugin

CLI tool: dockerd-rootless-setuptool.sh.

## Commands
- `dockerd-rootless-setuptool.sh <resource> <action>` — Execute dockerd-rootless-setuptool.sh commands
- `dockerd-rootless-setuptool.sh self version` — Print dockerd-rootless-setuptool.sh version
- `dockerd-rootless-setuptool.sh _ _` — Passthrough to dockerd-rootless-setuptool.sh CLI

## Usage Examples
- "dockerd-rootless-setuptool.sh --help"
- "dockerd-rootless-setuptool.sh self version"

## Installation
```bash
apt-get install dockerd-rootless-setuptool.sh 2>/dev/null || which dockerd-rootless-setuptool.sh
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
