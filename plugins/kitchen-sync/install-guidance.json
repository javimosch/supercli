{
  "plugin": "kitchen-sync",
  "binary": "kitchen-sync",
  "check": "which kitchen-sync",
  "install_steps": [
    "brew install kitchen-sync",
    "Verify: kitchen-sync --version",
    "supercli plugins install ./plugins/kitchen-sync --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}