{ "plugin": "bottlerocket", "binary": "bottlerocket", "check": "which bottlerocket", "install_steps": ["apt-get install bottlerocket 2>/dev/null || which bottlerocket", "Verify: bottlerocket --version", "supercli plugins install ./plugins/bottlerocket --on-conflict replace --json"] }
