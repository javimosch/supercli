{
  "plugin": "pdb3",
  "binary": "pdb3",
  "check": "which pdb3",
  "install_steps": [
    "apt-get install pdb3 2>/dev/null || which pdb3",
    "Verify: pdb3 --version",
    "supercli plugins install ./plugins/pdb3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}