{
  "plugin": "valkey",
  "binary": "valkey",
  "check": "which valkey",
  "install_steps": [
    "pip install valkey",
    "Verify: valkey --version",
    "supercli plugins install ./plugins/valkey --on-conflict replace --json"
  ],
  "note": "Language: C. Stars: 26015"
}