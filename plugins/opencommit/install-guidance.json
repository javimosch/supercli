{
  "plugin": "opencommit",
  "binary": "oco",
  "check": "which oco",
  "install_steps": [
    "npm install -g opencommit",
    "Verify: oco --version",
    "Set up API key: oco config set OCO_OPENAI_API_KEY=<your-key>",
    "supercli plugins install ./plugins/opencommit --on-conflict replace --json"
  ],
  "note": "Requires an OpenAI (or other provider) API key. Default model: gpt-4o. Supports custom providers (OCO_PROVIDER). Configure with `oco config` commands."
}
