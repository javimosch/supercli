{
  "plugin": "john",
  "binary": "john",
  "check": "which john",
  "install_steps": [
    "brew install john",
    "Verify: john --version",
    "supercli plugins install ./plugins/john --on-conflict replace --json"
  ]
}
