{
  "plugin": "mail-deduplicate",
  "binary": "mail-deduplicate",
  "check": "which mail-deduplicate",
  "install_steps": [
    "pip install mail-deduplicate",
    "Verify: mail-deduplicate --version",
    "supercli plugins install ./plugins/mail-deduplicate --on-conflict replace --json"
  ]
}