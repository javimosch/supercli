{
  "plugin": "wg",
  "binary": "wg",
  "check": "which wg",
  "install_steps": [
    "apt-get install wg 2>/dev/null || which wg",
    "Verify: wg --version",
    "supercli plugins install ./plugins/wg --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}