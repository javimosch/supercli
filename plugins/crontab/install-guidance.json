{
  "plugin": "crontab",
  "binary": "crontab",
  "check": "which crontab",
  "install_steps": [
    "macOS: pre-installed with cron (brew install cron for newer versions)",
    "Debian/Ubuntu: apt-get install cron",
    "Verify: crontab -l"
  ],
  "note": "Pre-installed on most Unix systems."
}
