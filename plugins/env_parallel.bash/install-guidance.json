{
  "plugin": "env_parallel.bash",
  "binary": "env_parallel.bash",
  "check": "which env_parallel.bash",
  "install_steps": [
    "apt-get install env_parallel.bash 2>/dev/null || which env_parallel.bash",
    "Verify: env_parallel.bash --version",
    "supercli plugins install ./plugins/env_parallel.bash --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}