{
  "plugin": "wildq",
  "binary": "wildq",
  "check": "which wildq",
  "install_steps": [
    "pip install wildq",
    "Verify: wildq --version",
    "supercli plugins install ./plugins/wildq --on-conflict replace --json"
  ],
  "note": "Query and transform structured data files with jq-like syntax."
}
