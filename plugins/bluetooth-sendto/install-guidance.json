{ "plugin": "bluetooth-sendto", "binary": "bluetooth-sendto", "check": "which bluetooth-sendto", "install_steps": ["apt-get install bluetooth-sendto 2>/dev/null || which bluetooth-sendto", "Verify: bluetooth-sendto --version", "supercli plugins install ./plugins/bluetooth-sendto --on-conflict replace --json"] }
