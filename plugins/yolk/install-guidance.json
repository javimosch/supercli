{
  "plugin": "yolk",
  "binary": "yolk",
  "check": "which yolk",
  "install_steps": ["git clone && make install", "Verify: yolk --version", "supercli plugins install ./plugins/yolk --on-conflict replace --json"]
}
