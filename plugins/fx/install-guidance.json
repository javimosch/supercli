{
  "plugin": "fx",
  "binary": "fx",
  "check": "which fx",
  "install_steps": [
    "brew install fx",
    "Verify: fx --version",
    "supercli plugins install ./plugins/fx --on-conflict replace --json"
  ],
  "note": "Also available via npm: npm install -g fx. Supports JSON viewing, filtering, and transformation with a powerful query language. Can process JSON from stdin or files."
}
