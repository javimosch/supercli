{
  "plugin": "netcat",
  "binary": "netcat",
  "check": "which netcat",
  "install_steps": [
    "brew install netcat",
    "Verify: netcat --version",
    "supercli plugins install ./plugins/netcat --on-conflict replace --json"
  ]
}
