{
  "plugin": "line",
  "binary": "line",
  "check": "which line",
  "install_steps": ["cargo install line-sequencer", "Verify: line --version", "supercli plugins install ./plugins/line --on-conflict replace --json"]
}
