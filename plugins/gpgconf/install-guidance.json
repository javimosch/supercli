{ "plugin": "gpgconf", "binary": "gpgconf", "check": "which gpgconf", "install_steps": ["apt-get install gnupg", "Verify: gpgconf --version", "supercli plugins install ./plugins/gpgconf --on-conflict replace --json"] }
