{ "plugin": "am", "binary": "am", "check": "which am", "install_steps": ["apt-get install am 2>/dev/null || which am", "Verify: am --version", "supercli plugins install ./plugins/am --on-conflict replace --json"] }
