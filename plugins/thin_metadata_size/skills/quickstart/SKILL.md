---
name: thin_metadata_size
description: Use this skill when the user wants to use thin_metadata_size, cli tool: thin_metadata_size.
---

# thin_metadata_size Plugin

CLI tool: thin_metadata_size.

## Commands
- `thin_metadata_size <resource> <action>` — Execute thin_metadata_size commands
- `thin_metadata_size self version` — Print thin_metadata_size version
- `thin_metadata_size _ _` — Passthrough to thin_metadata_size CLI

## Usage Examples
- "thin_metadata_size --help"
- "thin_metadata_size self version"

## Installation
```bash
apt-get install thin_metadata_size 2>/dev/null || which thin_metadata_size
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
