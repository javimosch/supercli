{
  "plugin": "pic",
  "binary": "pic",
  "check": "which pic",
  "install_steps": [
    "apt-get install pic 2>/dev/null || which pic",
    "Verify: pic --version",
    "supercli plugins install ./plugins/pic --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}