{
  "plugin": "habit-cli",
  "binary": "habit-cli",
  "check": "which habit-cli",
  "install_steps": ["cargo install habit-cli", "Verify: habit-cli --version", "supercli plugins install ./plugins/habit-cli --on-conflict replace --json"]
}
