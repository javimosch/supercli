{
  "plugin": "shasum",
  "binary": "shasum",
  "check": "which shasum",
  "install_steps": [
    "brew install shasum",
    "Verify: shasum --version",
    "supercli plugins install ./plugins/shasum --on-conflict replace --json"
  ]
}
