{ "plugin": "dh_perl", "binary": "dh_perl", "check": "which dh_perl", "install_steps": ["apt-get install dh_perl 2>/dev/null || which dh_perl", "Verify: dh_perl --version", "supercli plugins install ./plugins/dh_perl --on-conflict replace --json"] }
