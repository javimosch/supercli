{
  "plugin": "l2ping",
  "binary": "l2ping",
  "check": "which l2ping",
  "install_steps": [
    "apt-get install l2ping 2>/dev/null || which l2ping",
    "Verify: l2ping --version",
    "supercli plugins install ./plugins/l2ping --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}