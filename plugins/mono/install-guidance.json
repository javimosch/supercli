{ "plugin": "mono", "binary": "mono", "check": "which mono", "install_steps": ["apt-get install mono-complete", "Verify: mono --version", "supercli plugins install ./plugins/mono --on-conflict replace --json"] }
