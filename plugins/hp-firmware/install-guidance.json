{
  "plugin": "hp-firmware",
  "binary": "hp-firmware",
  "check": "which hp-firmware",
  "install_steps": [
    "apt-get install hp-firmware 2>/dev/null || which hp-firmware",
    "Verify: hp-firmware --version",
    "supercli plugins install ./plugins/hp-firmware --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}