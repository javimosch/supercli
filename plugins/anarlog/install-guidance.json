{
  "plugin": "anarlog",
  "binary": "anarlog",
  "check": "which anarlog",
  "install_steps": [
    "pip install anarlog",
    "Verify: anarlog --version",
    "supercli plugins install ./plugins/anarlog --on-conflict replace --json"
  ],
  "note": "Language: Rust. Stars: 8570"
}