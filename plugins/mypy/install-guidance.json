{
  "plugin": "mypy",
  "binary": "mypy",
  "check": "which mypy",
  "install_steps": [
    "pip install mypy",
    "Verify: mypy --version",
    "supercli plugins install ./plugins/mypy --on-conflict replace --json"
  ],
  "note": "Requires Python."
}
