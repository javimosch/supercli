{
  "plugin": "watchtower",
  "binary": "watchtower",
  "check": "which watchtower",
  "install_steps": [
    "docker pull containrrr/watchtower",
    "go install github.com/containrrr/watchtower@latest",
    "brew install watchtower (macOS)",
    "Verify: watchtower --version"
  ],
  "note": "For Docker usage: docker run -d --name watchtower -v /var/run/docker.sock:/var/run/docker.sock containrrr/watchtower"
}
