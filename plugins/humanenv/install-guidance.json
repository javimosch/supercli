{
  "plugin": "humanenv",
  "binary": "humanenv",
  "check": "which humanenv",
  "install_steps": [
    "npm install -g humanenv",
    "Verify: humanenv",
    "supercli plugins install ./plugins/humanenv --on-conflict replace --json"
  ],
  "note": "Local mode (recommended for agents): Run `supercli humanenv local init` first. Remote mode requires human to run `humanenv server` and create a project in the admin UI."
}
