{
  "plugin": "k0s",
  "binary": "k0s",
  "check": "which k0s",
  "install_steps": [
    "curl -sSLf https",
    "Verify: k0s --version",
    "supercli plugins install ./plugins/k0s --on-conflict replace --json"
  ]
}
