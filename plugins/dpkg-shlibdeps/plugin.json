{
  "name": "dpkg-shlibdeps",
  "version": "1.0.0",
  "description": "dpkg-shlibdeps \u2014 dpkg-shlibdeps \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-shlibdeps"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-shlibdeps",
    "binary": "dpkg-shlibdeps",
    "check": "which dpkg-shlibdeps",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-shlibdeps",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-shlibdeps \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-shlibdeps",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}