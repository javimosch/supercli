{
  "plugin": "taskwarrior",
  "binary": "task",
  "check": "which task",
  "install_steps": [
    "brew install task",
    "Or: apt install taskwarrior",
    "Verify: task version",
    "supercli plugins install ./plugins/taskwarrior --on-conflict replace --json"
  ],
  "note": "Taskwarrior stores tasks in a local database. Supports projects, tags, priorities, due dates, dependencies, and recurring tasks. Has an active ecosystem of extensions."
}
