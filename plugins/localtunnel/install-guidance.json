{
  "plugin": "localtunnel",
  "binary": "localtunnel",
  "check": "which localtunnel",
  "install_steps": [
    "npm install -g localtunnel",
    "Verify: localtunnel --version",
    "supercli plugins install ./plugins/localtunnel --on-conflict replace --json"
  ]
}
