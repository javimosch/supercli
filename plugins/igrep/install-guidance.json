{
  "plugin": "igrep",
  "binary": "igrep",
  "check": "which igrep",
  "install_steps": [
    "brew install igrep",
    "Verify: igrep --version",
    "supercli plugins install ./plugins/igrep --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}