{
  "plugin": "sparrow-wifi",
  "binary": "sparrow-wifi",
  "check": "which sparrow-wifi",
  "install_steps": ["git clone and install", "Verify: sparrow-wifi --version", "supercli plugins install ./plugins/sparrow-wifi --on-conflict replace --json"]
}
