{ "plugin": "distrobox-export", "binary": "distrobox-export", "check": "which distrobox-export", "install_steps": ["apt-get install distrobox-export 2>/dev/null || which distrobox-export", "Verify: distrobox-export --version", "supercli plugins install ./plugins/distrobox-export --on-conflict replace --json"] }
