{
  "plugin": "cups.lpc",
  "binary": "cups.lpc",
  "check": "which cups.lpc",
  "install_steps": [
    "apt-get install cups.lpc 2>/dev/null || which cups.lpc",
    "Verify: cups.lpc --version",
    "supercli plugins install ./plugins/cups.lpc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}