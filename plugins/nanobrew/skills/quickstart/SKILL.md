---
name: nanobrew
description: Use this skill when the user wants to use nanobrew for The fastest macOS package manager. Written in Zig. 3ms warm installs..
---

# nanobrew Plugin

The fastest macOS package manager. Written in Zig. 3ms warm installs.

## Commands

### General
- `nanobrew self version` — Print nanobrew version
- `nanobrew _ _` — Passthrough to nanobrew CLI

## Usage Examples
- "Run nanobrew"
- "Use nanobrew to The fastest macOS package manager. Written in Zig. 3ms warm "

## Installation

```bash
pip install nanobrew
```

## Key Features
- The fastest macOS package manager. Written in Zig. 3ms warm installs.
- CLI tool, headless and scriptable
