{
  "plugin": "duckdb",
  "binary": "duckdb",
  "check": "which duckdb",
  "install_steps": [
    "brew install duckdb",
    "Verify: duckdb --version",
    "supercli plugins install ./plugins/duckdb --on-conflict replace --json"
  ]
}
