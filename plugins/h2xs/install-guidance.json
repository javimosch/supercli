{
  "plugin": "h2xs",
  "binary": "h2xs",
  "check": "which h2xs",
  "install_steps": [
    "apt-get install h2xs 2>/dev/null || which h2xs",
    "Verify: h2xs --version",
    "supercli plugins install ./plugins/h2xs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}