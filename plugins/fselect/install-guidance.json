{
  "plugin": "fselect",
  "binary": "fselect",
  "check": "which fselect",
  "install_steps": [
    "cargo install fselect",
    "Verify: fselect --version",
    "supercli plugins install ./plugins/fselect --on-conflict replace --json"
  ],
  "note": "Also available via: brew install fselect, apt install (Debian), aur (Arch), winget, chocolatey, scoop, or pre-built binaries from GitHub releases."
}