{
  "plugin": "rvc-cli",
  "binary": "rvc-cli",
  "check": "which rvc-cli",
  "install_steps": [
    "pip install rvc-cli",
    "Verify: rvc-cli --version",
    "supercli plugins install ./plugins/rvc-cli --on-conflict replace --json"
  ]
}