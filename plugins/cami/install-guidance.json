{
  "plugin": "cami",
  "binary": "cami",
  "check": "which cami",
  "install_steps": [
    "npm install -g cami",
    "Verify: cami --version",
    "supercli plugins install ./plugins/cami --on-conflict replace --json"
  ]
}