{
  "name": "dbus-send",
  "version": "1.0.0",
  "description": "dbus-send \u2014 dbus-send \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dbus-send"
    }
  ],
  "install_guidance": {
    "plugin": "dbus-send",
    "binary": "dbus-send",
    "check": "which dbus-send",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dbus-send",
      "resource": "run",
      "action": "exec",
      "description": "dbus-send \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dbus-send",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}