{ "plugin": "case", "binary": "case", "check": "which case", "install_steps": ["apt-get install case 2>/dev/null || which case", "Verify: case --version", "supercli plugins install ./plugins/case --on-conflict replace --json"] }
