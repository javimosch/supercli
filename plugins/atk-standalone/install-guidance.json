{ "plugin": "atk-standalone", "binary": "atk-standalone", "check": "which atk-standalone", "install_steps": ["apt-get install atk-standalone 2>/dev/null || which atk-standalone", "Verify: atk-standalone --version", "supercli plugins install ./plugins/atk-standalone --on-conflict replace --json"] }
