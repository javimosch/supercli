{
  "name": "atk-standalone",
  "version": "0.1.0",
  "description": "CLI tool: atk-standalone",
  "source": "https://packages.debian.org/atk-standalone",
  "checks": [{ "type": "binary", "name": "atk-standalone" }],
  "install_guidance": {
    "plugin": "atk-standalone", "binary": "atk-standalone", "check": "which atk-standalone",
    "install_steps": ["apt-get install atk-standalone 2>/dev/null || which atk-standalone", "Verify: atk-standalone --version", "supercli plugins install ./plugins/atk-standalone --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "atk-standalone", "resource": "_", "action": "_",
    "description": "Passthrough to atk-standalone CLI",
    "adapter": "process", "adapterConfig": { "command": "atk-standalone", "passthrough": true, "missingDependencyHelp": "Install atk-standalone: apt-get install atk-standalone" }, "args": []
  }]
}
