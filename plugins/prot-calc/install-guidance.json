{
  "plugin": "prot-calc",
  "binary": "prot-calc",
  "check": "which prot-calc",
  "install_steps": ["pip install prot-calc", "Verify: prot-calc --version", "supercli plugins install ./plugins/prot-calc --on-conflict replace --json"]
}
