{
  "plugin": "ips",
  "binary": "ips",
  "check": "which ips",
  "install_steps": [
    "go install github.com/sjzar/ips@latest",
    "Verify: ips --version",
    "supercli plugins install ./plugins/ips --on-conflict replace --json"
  ]
}