{
  "plugin": "dhall-toml",
  "binary": "dhall-toml",
  "check": "which dhall-toml",
  "install_steps": [
    "brew install dhall-toml",
    "Verify: dhall-toml --version",
    "supercli plugins install ./plugins/dhall-toml --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}