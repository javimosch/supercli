{
  "plugin": "pktogf",
  "binary": "pktogf",
  "check": "which pktogf",
  "install_steps": [
    "apt-get install pktogf 2>/dev/null || which pktogf",
    "Verify: pktogf --version",
    "supercli plugins install ./plugins/pktogf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}