{
  "plugin": "cmus",
  "binary": "cmus",
  "check": "which cmus",
  "install_steps": [
    "# Install cmus",
    "# See cmus.github.io",
    "# apt: sudo apt-get install cmus",
    "# brew: brew install cmus",
    "# cargo: cargo install cmus",
    "# npm: npm install -g cmus"
  ]
}