{
  "plugin": "airi",
  "binary": "airi",
  "check": "which airi",
  "install_steps": [
    "pip install airi",
    "Verify: airi --version",
    "supercli plugins install ./plugins/airi --on-conflict replace --json"
  ],
  "note": "Language: TypeScript"
}