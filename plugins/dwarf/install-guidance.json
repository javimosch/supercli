{
  "plugin": "dwarf",
  "binary": "dwarf",
  "check": "which dwarf",
  "install_steps": [
    "brew install dwarf",
    "Verify: dwarf --version",
    "supercli plugins install ./plugins/dwarf --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}