{
  "plugin": "chglog",
  "binary": "chglog",
  "check": "which chglog",
  "install_steps": ["go install github.com/goreleaser/chglog/cmd/chglog@latest", "Verify: chglog --version", "supercli plugins install ./plugins/chglog --on-conflict replace --json"]
}
