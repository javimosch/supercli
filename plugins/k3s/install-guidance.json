{
  "plugin": "k3s",
  "binary": "k3s",
  "check": "which k3s",
  "install_steps": [
    "curl -sfL https",
    "Verify: k3s --version",
    "supercli plugins install ./plugins/k3s --on-conflict replace --json"
  ]
}
