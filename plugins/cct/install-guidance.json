{ "plugin": "cct", "binary": "cct", "check": "which cct", "install_steps": ["apt-get install cct 2>/dev/null || which cct", "Verify: cct --version", "supercli plugins install ./plugins/cct --on-conflict replace --json"] }
