{
  "plugin": "postdrop",
  "binary": "postdrop",
  "check": "which postdrop",
  "install_steps": [
    "# Install postdrop",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postdrop-linux-amd64 -o /usr/local/bin/postdrop",
    "chmod +x /usr/local/bin/postdrop",
    "# Or via package manager, see /usr/sbin/postdrop"
  ]
}