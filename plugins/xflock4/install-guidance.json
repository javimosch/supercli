{
  "plugin": "xflock4",
  "binary": "xflock4",
  "check": "which xflock4",
  "install_steps": [
    "apt-get install xflock4 2>/dev/null || which xflock4",
    "Verify: xflock4 --version",
    "supercli plugins install ./plugins/xflock4 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}