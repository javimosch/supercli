#!/bin/bash
# act workflow run wrapper
# Args: event job dryrun
cd /home/jarancibia/ai/supercli
EVENT="${1:-push}"
JOB="$2"
DRYRUN="$3"

CMD="act $EVENT"

[ -n "$JOB" ] && CMD="$CMD -j $JOB"
[ "$DRYRUN" = "true" ] && CMD="$CMD -n"

eval $CMD