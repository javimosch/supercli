{
  "plugin": "docuum",
  "binary": "docuum",
  "check": "which docuum",
  "install_steps": ["brew install docuum", "Verify: docuum --version", "supercli plugins install ./plugins/docuum --on-conflict replace --json"]
}
