---
name: newuidmap
description: Use this skill when the user wants to use newuidmap, cli tool: newuidmap.
---

# newuidmap Plugin

CLI tool: newuidmap.

## Commands
- `newuidmap <resource> <action>` — Execute newuidmap commands
- `newuidmap self version` — Print newuidmap version
- `newuidmap _ _` — Passthrough to newuidmap CLI

## Usage Examples
- "newuidmap --help"
- "newuidmap self version"

## Installation
```bash
apt-get install newuidmap 2>/dev/null || which newuidmap
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
