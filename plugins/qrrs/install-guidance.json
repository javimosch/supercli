{
  "plugin": "qrrs",
  "binary": "qrrs",
  "check": "which qrrs",
  "install_steps": [
    "cargo install qrrs",
    "Verify: qrrs --version",
    "supercli plugins install ./plugins/qrrs --on-conflict replace --json"
  ]
}