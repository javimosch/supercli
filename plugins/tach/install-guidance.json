{
  "plugin": "tach",
  "binary": "tach",
  "check": "which tach",
  "install_steps": [
    "pip install tach",
    "Verify: tach --version",
    "supercli plugins install ./plugins/tach --on-conflict replace --json"
  ],
  "note": "Also available via pipx: pipx install tach. Python tool to enforce modular design boundaries and manage dependencies between packages. Define and visualize dependency graphs between modules."
}
