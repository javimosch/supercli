{
  "plugin": "genact",
  "binary": "genact",
  "check": "which genact",
  "install_steps": [
    "brew install genact",
    "Verify: genact --version",
    "supercli plugins install ./plugins/genact --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install genact. WebAssembly version available for browser use. Multiple activity modules available. Customizable speed and duration. Perfect for screenshots and demos."
}
