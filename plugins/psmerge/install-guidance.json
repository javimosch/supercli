{
  "plugin": "psmerge",
  "binary": "psmerge",
  "check": "which psmerge",
  "install_steps": [
    "apt-get install psmerge 2>/dev/null || which psmerge",
    "Verify: psmerge --version",
    "supercli plugins install ./plugins/psmerge --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}