{ "plugin": "dh_prep", "binary": "dh_prep", "check": "which dh_prep", "install_steps": ["apt-get install dh_prep 2>/dev/null || which dh_prep", "Verify: dh_prep --version", "supercli plugins install ./plugins/dh_prep --on-conflict replace --json"] }
