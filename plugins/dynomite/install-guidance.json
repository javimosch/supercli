{
  "plugin": "dynomite",
  "binary": "dynomite",
  "check": "which dynomite",
  "install_steps": [
    "brew install dynomite",
    "Verify: dynomite --version",
    "supercli plugins install ./plugins/dynomite --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}