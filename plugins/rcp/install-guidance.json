{
  "plugin": "rcp",
  "binary": "rcp",
  "check": "which rcp",
  "install_steps": [
    "apt-get install rcp 2>/dev/null || which rcp",
    "Verify: rcp --version",
    "supercli plugins install ./plugins/rcp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}