{
  "plugin": "systemd-cryptenroll",
  "binary": "systemd-cryptenroll",
  "check": "which systemd-cryptenroll",
  "install_steps": [
    "apt-get install systemd-cryptenroll 2>/dev/null || which systemd-cryptenroll",
    "Verify: systemd-cryptenroll --version",
    "supercli plugins install ./plugins/systemd-cryptenroll --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}