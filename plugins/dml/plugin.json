{
  "name": "dml",
  "version": "0.1.0",
  "description": "CLI tool: dml",
  "source": "https://packages.debian.org/dml",
  "checks": [{ "type": "binary", "name": "dml" }],
  "install_guidance": {
    "plugin": "dml", "binary": "dml", "check": "which dml",
    "install_steps": ["apt-get install dml 2>/dev/null || which dml", "Verify: dml --version", "supercli plugins install ./plugins/dml --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dml", "resource": "_", "action": "_",
    "description": "Passthrough to dml CLI",
    "adapter": "process", "adapterConfig": { "command": "dml", "passthrough": true, "missingDependencyHelp": "Install dml: apt-get install dml" }, "args": []
  }]
}
