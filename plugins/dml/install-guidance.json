{ "plugin": "dml", "binary": "dml", "check": "which dml", "install_steps": ["apt-get install dml 2>/dev/null || which dml", "Verify: dml --version", "supercli plugins install ./plugins/dml --on-conflict replace --json"] }
