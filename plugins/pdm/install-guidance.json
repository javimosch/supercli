{
  "plugin": "pdm",
  "binary": "pdm",
  "check": "which pdm",
  "install_steps": [
    "pip install pdm",
    "Verify: pdm --version",
    "supercli plugins install ./plugins/pdm --on-conflict replace --json"
  ],
  "note": "Requires Python 3.8+. Also available via: brew, cargo, npm."
}
