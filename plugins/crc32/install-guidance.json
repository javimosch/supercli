{ "plugin": "crc32", "binary": "crc32", "check": "which crc32", "install_steps": ["apt-get install crc32 2>/dev/null || which crc32", "Verify: crc32 --version", "supercli plugins install ./plugins/crc32 --on-conflict replace --json"] }
