{
  "plugin": "preflight",
  "binary": "preflight",
  "check": "which preflight",
  "install_steps": ["go install github.com/preflight@latest", "Verify: preflight --version", "supercli plugins install ./plugins/preflight --on-conflict replace --json"]
}
