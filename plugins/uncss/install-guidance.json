{
  "plugin": "uncss",
  "binary": "uncss",
  "check": "which uncss",
  "install_steps": [
    "npm install -g uncss",
    "Verify: uncss --version",
    "supercli plugins install ./plugins/uncss --on-conflict replace --json"
  ]
}
