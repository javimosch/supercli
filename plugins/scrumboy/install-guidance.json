{
  "plugin": "scrumboy",
  "binary": "docker",
  "check": "docker --version && curl --version",
  "install_steps": [
    "sc scrumboy self start  — starts Docker container on :8080",
    "sc scrumboy self mcp    — registers MCP server for agent use",
    "Open http://localhost:8080 in browser"
  ],
  "note": "Requires Docker. Scrumboy runs as a container with SQLite persistence in ~/.scrumboy/data/. The MCP endpoint lets AI agents create/read/update/delete boards and todos alongside humans."
}
