{
  "plugin": "csv2tsv",
  "binary": "csv2tsv",
  "check": "which csv2tsv",
  "install_steps": [
    "brew install csv2tsv",
    "Verify: csv2tsv --version",
    "supercli plugins install ./plugins/csv2tsv --on-conflict replace --json"
  ]
}
