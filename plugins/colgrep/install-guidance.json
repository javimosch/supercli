{
  "plugin": "colgrep",
  "binary": "colgrep",
  "check": "which colgrep",
  "install_steps": [
    "brew install lightonai/tap/colgrep",
    "Verify: colgrep --version",
    "supercli plugins install ./plugins/colgrep --on-conflict replace --json"
  ],
  "note": "Also available via shell installer: curl --proto '=https' --tlsv1.2 -LsSf https://github.com/lightonai/next-plaid/releases/latest/download/colgrep-installer.sh | sh. A single Rust binary with ONNX Runtime baked in — no external dependencies."
}
