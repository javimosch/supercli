{
  "plugin": "lzfgrep",
  "binary": "lzfgrep",
  "check": "which lzfgrep",
  "install_steps": [
    "apt-get install lzfgrep 2>/dev/null || which lzfgrep",
    "Verify: lzfgrep --version",
    "supercli plugins install ./plugins/lzfgrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}