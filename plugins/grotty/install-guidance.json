{
  "plugin": "grotty",
  "binary": "grotty",
  "check": "which grotty",
  "install_steps": [
    "apt-get install grotty 2>/dev/null || which grotty",
    "Verify: grotty --version",
    "supercli plugins install ./plugins/grotty --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}