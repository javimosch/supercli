{
  "name": "cpufreq-set",
  "version": "0.1.0",
  "description": "CLI tool: cpufreq-set",
  "source": "https://packages.debian.org/cpufreq-set",
  "checks": [{ "type": "binary", "name": "cpufreq-set" }],
  "install_guidance": {
    "plugin": "cpufreq-set", "binary": "cpufreq-set", "check": "which cpufreq-set",
    "install_steps": ["apt-get install cpufreq-set 2>/dev/null || which cpufreq-set", "Verify: cpufreq-set --version", "supercli plugins install ./plugins/cpufreq-set --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cpufreq-set", "resource": "_", "action": "_",
    "description": "Passthrough to cpufreq-set CLI",
    "adapter": "process", "adapterConfig": { "command": "cpufreq-set", "passthrough": true, "missingDependencyHelp": "Install cpufreq-set: apt-get install cpufreq-set" }, "args": []
  }]
}
