{
  "plugin": "lzless",
  "binary": "lzless",
  "check": "which lzless",
  "install_steps": [
    "apt-get install lzless 2>/dev/null || which lzless",
    "Verify: lzless --version",
    "supercli plugins install ./plugins/lzless --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}