{
  "plugin": "lockenv",
  "binary": "lockenv",
  "check": "which lockenv",
  "install_steps": [
    "brew install illarion/tap/lockenv",
    "Verify: lockenv --version",
    "supercli plugins install ./plugins/lockenv --on-conflict replace --json"
  ],
  "note": "lockenv uses AES-256-GCM encryption with PBKDF2 key derivation. Passwords are not stored - you must remember them. Supports macOS Keychain, GNOME/KDE Keyring, and Windows Credential Manager for optional password caching."
}