{
  "plugin": "skim",
  "binary": "sk",
  "check": "which sk",
  "install_steps": [
    "cargo install skim",
    "Or: brew install skim",
    "Or: apt install skim",
    "Or: pacman -S skim",
    "Or: scoop install skim",
    "Or download binary from https://github.com/lotabout/skim/releases",
    "Verify: sk --version",
    "supercli plugins install ./plugins/skim --on-conflict replace --json"
  ],
  "note": "Binary name is 'sk'. Written in Rust by lotabout. Also installable via: dnf install skim, emerge skim."
}
