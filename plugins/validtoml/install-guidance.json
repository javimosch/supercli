{
  "plugin": "validtoml",
  "binary": "validtoml",
  "check": "which validtoml",
  "install_steps": ["pip install validtoml", "Verify: validtoml --version", "supercli plugins install ./plugins/validtoml --on-conflict replace --json"]
}
