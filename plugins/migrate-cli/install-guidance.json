{
  "plugin": "migrate-cli",
  "binary": "migrate-cli",
  "check": "which migrate-cli",
  "install_steps": ["composer require byjg/migration-cli", "Verify: migrate-cli --version", "supercli plugins install ./plugins/migrate-cli --on-conflict replace --json"]
}
