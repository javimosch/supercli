{
  "plugin": "gitql",
  "binary": "gitql",
  "check": "which gitql",
  "install_steps": [
    "brew install gitql",
    "Verify: gitql --version",
    "supercli plugins install ./plugins/gitql --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}