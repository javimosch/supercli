{
  "name": "dpkg-architecture",
  "version": "1.0.0",
  "description": "dpkg-architecture \u2014 dpkg-architecture \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-architecture"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-architecture",
    "binary": "dpkg-architecture",
    "check": "which dpkg-architecture",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-architecture",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-architecture \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-architecture",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}