{
  "plugin": "mount.ntfs",
  "binary": "mount.ntfs",
  "check": "which mount.ntfs",
  "install_steps": [
    "apt-get install mount.ntfs 2>/dev/null || which mount.ntfs",
    "Verify: mount.ntfs --version",
    "supercli plugins install ./plugins/mount.ntfs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}