{
  "plugin": "mailflare",
  "binary": "mailflare",
  "check": "which mailflare",
  "install_steps": ["git clone && npm install", "Verify: mailflare --version", "supercli plugins install ./plugins/mailflare --on-conflict replace --json"]
}
