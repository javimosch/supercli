{ "plugin": "blockdev", "binary": "blockdev", "check": "which blockdev", "install_steps": ["apt-get install blockdev 2>/dev/null || which blockdev", "Verify: blockdev --version", "supercli plugins install ./plugins/blockdev --on-conflict replace --json"] }
