{
  "name": "blockdev",
  "version": "0.1.0",
  "description": "CLI tool: blockdev",
  "source": "https://packages.debian.org/blockdev",
  "checks": [{ "type": "binary", "name": "blockdev" }],
  "install_guidance": {
    "plugin": "blockdev", "binary": "blockdev", "check": "which blockdev",
    "install_steps": ["apt-get install blockdev 2>/dev/null || which blockdev", "Verify: blockdev --version", "supercli plugins install ./plugins/blockdev --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "blockdev", "resource": "_", "action": "_",
    "description": "Passthrough to blockdev CLI",
    "adapter": "process", "adapterConfig": { "command": "blockdev", "passthrough": true, "missingDependencyHelp": "Install blockdev: apt-get install blockdev" }, "args": []
  }]
}
