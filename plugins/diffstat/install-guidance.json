{
  "plugin": "diffstat",
  "binary": "diffstat",
  "check": "which diffstat",
  "install_steps": [
    "brew install diffstat",
    "Verify: diffstat --version",
    "supercli plugins install ./plugins/diffstat --on-conflict replace --json"
  ]
}
