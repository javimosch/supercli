{
  "plugin": "tracee",
  "binary": "tracee",
  "check": "which tracee",
  "install_steps": ["brew install tracee", "Verify: tracee --version", "supercli plugins install ./plugins/tracee --on-conflict replace --json"]
}
