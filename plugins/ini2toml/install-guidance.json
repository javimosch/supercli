{
  "plugin": "ini2toml",
  "binary": "ini2toml",
  "check": "which ini2toml",
  "install_steps": ["pipx install ini2toml", "Verify: ini2toml --version", "supercli plugins install ./plugins/ini2toml --on-conflict replace --json"]
}
