{ "plugin": "dm-flush", "binary": "dm-flush", "check": "which dm-flush", "install_steps": ["apt-get install dm-flush 2>/dev/null || which dm-flush", "Verify: dm-flush --version", "supercli plugins install ./plugins/dm-flush --on-conflict replace --json"] }
