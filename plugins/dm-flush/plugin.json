{
  "name": "dm-flush",
  "version": "0.1.0",
  "description": "CLI tool: dm-flush",
  "source": "https://packages.debian.org/dm-flush",
  "checks": [{ "type": "binary", "name": "dm-flush" }],
  "install_guidance": {
    "plugin": "dm-flush", "binary": "dm-flush", "check": "which dm-flush",
    "install_steps": ["apt-get install dm-flush 2>/dev/null || which dm-flush", "Verify: dm-flush --version", "supercli plugins install ./plugins/dm-flush --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-flush", "resource": "_", "action": "_",
    "description": "Passthrough to dm-flush CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-flush", "passthrough": true, "missingDependencyHelp": "Install dm-flush: apt-get install dm-flush" }, "args": []
  }]
}
