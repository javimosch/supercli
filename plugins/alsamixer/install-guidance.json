{ "plugin": "alsamixer", "binary": "alsamixer", "check": "which alsamixer", "install_steps": ["apt-get install alsamixer 2>/dev/null || which alsamixer", "Verify: alsamixer --version", "supercli plugins install ./plugins/alsamixer --on-conflict replace --json"] }
