{
  "name": "alsamixer",
  "version": "0.1.0",
  "description": "CLI tool: alsamixer",
  "source": "https://packages.debian.org/alsamixer",
  "checks": [{ "type": "binary", "name": "alsamixer" }],
  "install_guidance": {
    "plugin": "alsamixer", "binary": "alsamixer", "check": "which alsamixer",
    "install_steps": ["apt-get install alsamixer 2>/dev/null || which alsamixer", "Verify: alsamixer --version", "supercli plugins install ./plugins/alsamixer --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "alsamixer", "resource": "_", "action": "_",
    "description": "Passthrough to alsamixer CLI",
    "adapter": "process", "adapterConfig": { "command": "alsamixer", "passthrough": true, "missingDependencyHelp": "Install alsamixer: apt-get install alsamixer" }, "args": []
  }]
}
