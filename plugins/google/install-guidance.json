{
  "plugin": "google",
  "binary": "google",
  "check": "which google",
  "install_steps": [
    "apt-get install google 2>/dev/null || which google",
    "Verify: google --version",
    "supercli plugins install ./plugins/google --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}