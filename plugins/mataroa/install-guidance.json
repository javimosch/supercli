{
  "plugin": "mataroa",
  "binary": "mataroa",
  "check": "which mataroa",
  "install_steps": ["pip install mataroa", "Verify: mataroa --version", "supercli plugins install ./plugins/mataroa --on-conflict replace --json"]
}
