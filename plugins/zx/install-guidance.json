{
  "plugin": "zx",
  "binary": "zx",
  "check": "which zx",
  "install_steps": [
    "npm install -g zx",
    "Verify: zx --version",
    "supercli plugins install ./plugins/zx --on-conflict replace --json"
  ]
}