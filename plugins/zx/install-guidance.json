{
  "plugin": "zx",
  "binary": "zx",
  "check": "which zx",
  "install_steps": [
    "npm install -g zx",
    "Verify: zx --version",
    "supercli plugins install ./plugins/zx --on-conflict replace --json"
  ],
  "note": "Requires Node.js 12.17.0+. Also works with Bun, Deno, and GraalVM Node.js. Supports both CJS and ESM modules in JS or TS."
}
