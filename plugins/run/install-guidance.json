{
  "plugin": "run",
  "binary": "run",
  "check": "which run",
  "install_steps": [
    "cargo install run",
    "Verify: run --version",
    "supercli plugins install ./plugins/run --on-conflict replace --json"
  ]
}