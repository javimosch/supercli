{
  "plugin": "kbdinfo",
  "binary": "kbdinfo",
  "check": "which kbdinfo",
  "install_steps": [
    "apt-get install kbdinfo 2>/dev/null || which kbdinfo",
    "Verify: kbdinfo --version",
    "supercli plugins install ./plugins/kbdinfo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}