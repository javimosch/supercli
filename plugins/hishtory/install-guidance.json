{
  "plugin": "hishtory",
  "binary": "hishtory",
  "check": "which hishtory",
  "install_steps": [
    "brew install hishtory",
    "Verify: hishtory --version",
    "supercli plugins install ./plugins/hishtory --on-conflict replace --json"
  ],
  "note": "Also available via curl install. Contextual shell history with search and sync across devices. Records working directory, exit code, duration, and git context. Great for remembering and finding past commands."
}
