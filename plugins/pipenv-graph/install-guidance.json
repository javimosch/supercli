{
  "plugin": "pipenv-graph",
  "binary": "pipenv-graph",
  "check": "which pipenv-graph",
  "install_steps": ["pip install pipenv-graph", "Verify: pipenv-graph --version", "supercli plugins install ./plugins/pipenv-graph --on-conflict replace --json"]
}
