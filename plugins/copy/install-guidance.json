{ "plugin": "copy", "binary": "copy", "check": "which copy", "install_steps": ["apt-get install copy 2>/dev/null || which copy", "Verify: copy --version", "supercli plugins install ./plugins/copy --on-conflict replace --json"] }
