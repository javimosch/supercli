{
  "name": "copy",
  "version": "0.1.0",
  "description": "CLI tool: copy",
  "source": "https://packages.debian.org/copy",
  "checks": [{ "type": "binary", "name": "copy" }],
  "install_guidance": {
    "plugin": "copy", "binary": "copy", "check": "which copy",
    "install_steps": ["apt-get install copy 2>/dev/null || which copy", "Verify: copy --version", "supercli plugins install ./plugins/copy --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "copy", "resource": "_", "action": "_",
    "description": "Passthrough to copy CLI",
    "adapter": "process", "adapterConfig": { "command": "copy", "passthrough": true, "missingDependencyHelp": "Install copy: apt-get install copy" }, "args": []
  }]
}
