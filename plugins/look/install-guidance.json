{
  "plugin": "look",
  "binary": "look",
  "check": "which look",
  "install_steps": [
    "apt-get install util-linux",
    "Verify: look --version",
    "supercli plugins install ./plugins/look --on-conflict replace --json"
  ]
}
