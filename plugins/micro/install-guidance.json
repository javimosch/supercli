{
  "plugin": "micro",
  "binary": "micro",
  "check": "which micro",
  "install_steps": [
    "brew install micro",
    "Verify: micro --version",
    "supercli plugins install ./plugins/micro --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/zyedidia/micro/cmd/micro@latest. A modern and intuitive terminal-based text editor. Simple, easy-to-use with mouse support and customizable keybindings. Great for terminal editing."
}
