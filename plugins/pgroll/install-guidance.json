{
  "plugin": "pgroll",
  "binary": "pgroll",
  "check": "which pgroll",
  "install_steps": [
    "pip install pgroll",
    "Verify: pgroll --version",
    "supercli plugins install ./plugins/pgroll --on-conflict replace --json"
  ],
  "note": "Language: Go"
}