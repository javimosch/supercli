{
  "plugin": "pgroll",
  "binary": "pgroll",
  "check": "which pgroll",
  "install_steps": ["brew install pgroll", "Verify: pgroll --version", "supercli plugins install ./plugins/pgroll --on-conflict replace --json"]
}
