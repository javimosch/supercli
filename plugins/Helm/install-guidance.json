{
  "plugin": "Helm",
  "binary": "Helm",
  "check": "which Helm",
  "install_steps": [
    "brew install Helm",
    "Verify: Helm --version",
    "supercli plugins install ./plugins/Helm --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}