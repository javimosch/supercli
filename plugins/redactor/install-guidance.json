{
  "plugin": "redactor",
  "binary": "redactor",
  "check": "which redactor",
  "install_steps": ["go install github.com/ypcrts/redactor@latest", "Verify: redactor --version", "supercli plugins install ./plugins/redactor --on-conflict replace --json"]
}
