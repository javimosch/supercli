{
  "plugin": "merge-yaml-cli",
  "binary": "merge-yaml",
  "check": "which merge-yaml",
  "install_steps": [
    "npm install -g merge-yaml-cli",
    "Verify: merge-yaml --version",
    "supercli plugins install ./plugins/merge-yaml-cli --on-conflict replace --json"
  ],
  "note": "Uses glob patterns to specify input files. Merges YAML files together with the last file taking precedence for conflicting keys. Great for configuration management."
}
