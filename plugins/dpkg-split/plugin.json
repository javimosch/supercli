{
  "name": "dpkg-split",
  "version": "1.0.0",
  "description": "dpkg-split \u2014 dpkg-split \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-split"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-split",
    "binary": "dpkg-split",
    "check": "which dpkg-split",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-split",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-split \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-split",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}