{
  "plugin": "clang-format",
  "binary": "clang-format",
  "check": "which clang-format",
  "install_steps": [
    "# Install clang-format",
    "# See clang.llvm.org",
    "# apt: sudo apt-get install clang-format",
    "# brew: brew install clang-format",
    "# cargo: cargo install clang-format",
    "# npm: npm install -g clang-format"
  ]
}