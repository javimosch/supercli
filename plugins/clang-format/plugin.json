{
  "name": "clang-format",
  "version": "1.0.0",
  "description": "clang-format \u2014 C/C++ code formatter",
  "source": "clang.llvm.org",
  "checks": [
    {
      "type": "binary",
      "name": "clang-format"
    }
  ],
  "install_guidance": {
    "plugin": "clang-format",
    "binary": "clang-format",
    "check": "which clang-format",
    "install_steps": [
      "# Install clang-format",
      "# See clang.llvm.org",
      "# apt: sudo apt-get install clang-format",
      "# brew: brew install clang-format",
      "# cargo: cargo install clang-format",
      "# npm: npm install -g clang-format",
      "# pip: pip install clang-format"
    ]
  },
  "commands": [
    {
      "namespace": "clang",
      "resource": "format",
      "action": "run",
      "description": "Run clang-format",
      "adapter": "process",
      "adapterConfig": {
        "command": "clang-format",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install clang-format from clang.llvm.org"
      },
      "args": []
    }
  ]
}