{
  "plugin": "glib-compile-resources",
  "binary": "glib-compile-resources",
  "check": "which glib-compile-resources",
  "install_steps": [
    "apt-get install glib-compile-resources 2>/dev/null || which glib-compile-resources",
    "Verify: glib-compile-resources --version",
    "supercli plugins install ./plugins/glib-compile-resources --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}