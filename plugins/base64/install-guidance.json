{
  "plugin": "base64",
  "binary": "base64",
  "check": "which base64",
  "install_steps": [
    "brew install coreutils",
    "Verify: base64 --version",
    "supercli plugins install ./plugins/base64 --on-conflict replace --json"
  ]
}
