{
  "plugin": "mydoro",
  "binary": "mydoro",
  "check": "which mydoro",
  "install_steps": ["cargo install mydoro", "Verify: mydoro --version", "supercli plugins install ./plugins/mydoro --on-conflict replace --json"]
}
