{
  "plugin": "mise",
  "binary": "mise",
  "check": "which mise",
  "install_steps": [
    "brew install mise",
    "Verify: mise --version",
    "supercli plugins install ./plugins/mise --on-conflict replace --json"
  ]
}
