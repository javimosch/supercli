{
  "plugin": "snipt",
  "binary": "snipt",
  "check": "which snipt",
  "install_steps": [
    "cargo install snipt",
    "Verify: snipt --version",
    "supercli plugins install ./plugins/snipt --on-conflict replace --json"
  ],
  "note": "Expand and manage text snippets for increased productivity."
}
