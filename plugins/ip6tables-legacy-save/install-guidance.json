{
  "plugin": "ip6tables-legacy-save",
  "binary": "ip6tables-legacy-save",
  "check": "which ip6tables-legacy-save",
  "install_steps": [
    "# Install ip6tables-legacy-save",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/ip6tables-legacy-save-linux-amd64 -o /usr/local/bin/ip6tables-legacy-save",
    "chmod +x /usr/local/bin/ip6tables-legacy-save",
    "# Or via package manager, see /usr/sbin/ip6tables-legacy-save"
  ]
}