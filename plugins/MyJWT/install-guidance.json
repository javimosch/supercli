{
  "plugin": "MyJWT",
  "binary": "myjwt",
  "check": "which myjwt",
  "install_steps": [
    "pip install myjwt",
    "Verify: myjwt --version",
    "supercli plugins install ./plugins/MyJWT --on-conflict replace --json"
  ]
}