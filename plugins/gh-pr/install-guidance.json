{
  "plugin": "gh-pr",
  "binary": "gh",
  "check": "which gh",
  "install_steps": [
    "Install GitHub CLI: https://cli.github.com",
    "macOS: brew install gh",
    "Linux: apt install gh / dnf install gh",
    "Verify: gh --version",
    "Authenticate: gh auth login"
  ],
  "note": "Requires GitHub CLI (gh) and authentication. Run 'gh auth login' to authenticate."
}
