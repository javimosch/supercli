{
  "plugin": "mbt",
  "binary": "mbt",
  "check": "which mbt",
  "install_steps": [
    "go install github.com/mbtproject/mbt@latest",
    "Verify: mbt --version",
    "supercli plugins install ./plugins/mbt --on-conflict replace --json"
  ]
}