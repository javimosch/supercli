{
  "plugin": "enchant-2",
  "binary": "enchant-2",
  "check": "which enchant-2",
  "install_steps": [
    "apt-get install enchant-2 2>/dev/null || which enchant-2",
    "Verify: enchant-2 --version",
    "supercli plugins install ./plugins/enchant-2 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}