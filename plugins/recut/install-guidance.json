{
  "plugin": "recut",
  "binary": "recut",
  "check": "which recut",
  "install_steps": [
    "cargo install recut",
    "Verify: recut --version",
    "supercli plugins install ./plugins/recut --on-conflict replace --json"
  ]
}
