---
name: json-paths
description: json-paths — extract all paths from a JSON document. Lists every key path in dot-notation and array-index notation.
---
# json-paths
json-paths — extract all paths from a JSON document. Lists every key path in dot-notation and array-index notation.
