{
  "name": "dm-clear",
  "version": "0.1.0",
  "description": "CLI tool: dm-clear",
  "source": "https://packages.debian.org/dm-clear",
  "checks": [{ "type": "binary", "name": "dm-clear" }],
  "install_guidance": {
    "plugin": "dm-clear", "binary": "dm-clear", "check": "which dm-clear",
    "install_steps": ["apt-get install dm-clear 2>/dev/null || which dm-clear", "Verify: dm-clear --version", "supercli plugins install ./plugins/dm-clear --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-clear", "resource": "_", "action": "_",
    "description": "Passthrough to dm-clear CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-clear", "passthrough": true, "missingDependencyHelp": "Install dm-clear: apt-get install dm-clear" }, "args": []
  }]
}
