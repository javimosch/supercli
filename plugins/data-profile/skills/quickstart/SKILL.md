---
name: data-profile
description: data-profile — profile CSV and JSONL/NDJSON data files. Reports schema, column types, null counts, numeric stats, unique values, sample rows.
---
# data-profile
data-profile — profile CSV and JSONL/NDJSON data files. Reports schema, column types, null counts, numeric stats, unique values, sample rows.
