---
name: bgrep
description: bgrep — binary grep CLI. Search binary files for byte patterns. Like grep but for binary data.
---
# bgrep
bgrep — binary grep CLI. Search binary files for byte patterns. Like grep but for binary data.
