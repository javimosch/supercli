{
  "plugin": "kysely-ctl",
  "binary": "kysely-ctl",
  "check": "which kysely-ctl",
  "install_steps": [
    "npm install -g kysely-ctl",
    "Verify: kysely --version",
    "supercli plugins install ./plugins/kysely-ctl --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
