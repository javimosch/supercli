{
  "plugin": "pager",
  "binary": "pager",
  "check": "which pager",
  "install_steps": [
    "# Install pager",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pager-linux-amd64 -o /usr/local/bin/pager",
    "chmod +x /usr/local/bin/pager",
    "# Or via package manager, see /usr/bin/pager"
  ]
}