{ "plugin": "apachectl", "binary": "apachectl", "check": "which apachectl", "install_steps": ["apt-get install apachectl 2>/dev/null || which apachectl", "Verify: apachectl --version", "supercli plugins install ./plugins/apachectl --on-conflict replace --json"] }
