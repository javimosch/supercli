{
  "plugin": "mmcli",
  "binary": "mmcli",
  "check": "which mmcli",
  "install_steps": [
    "apt-get install mmcli 2>/dev/null || which mmcli",
    "Verify: mmcli --version",
    "supercli plugins install ./plugins/mmcli --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}