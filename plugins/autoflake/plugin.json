{
  "name": "autoflake",
  "version": "0.1.0",
  "description": "autoflake — Remove unused Python imports",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "autoflake"
    }
  ],
  "install_guidance": {
    "plugin": "autoflake",
    "binary": "autoflake",
    "check": "which autoflake",
    "install_steps": [
      "brew install autoflake"
    ],
    "note": "Python tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "autoflake",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to autoflake",
      "adapter": "process",
      "adapterConfig": {
        "command": "autoflake",
        "passthrough": true,
        "missingDependencyHelp": "Install autoflake: brew install autoflake"
      },
      "args": []
    }
  ]
}
