{
  "plugin": "kics",
  "binary": "kics",
  "check": "which kics",
  "install_steps": ["brew install kics", "Verify: kics --version", "supercli plugins install ./plugins/kics --on-conflict replace --json"]
}
