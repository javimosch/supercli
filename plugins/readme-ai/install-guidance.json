{
  "plugin": "readme-ai",
  "binary": "readmeai",
  "check": "which readmeai",
  "install_steps": [
    "pip install -U readmeai",
    "or: pipx install readmeai",
    "or: uv tool install readmeai",
    "or: docker pull zeroxeli/readme-ai:latest",
    "Set OPENAI_API_KEY or ANTHROPIC_API_KEY or GOOGLE_API_KEY for AI modes",
    "Use --api offline for no API key required",
    "Verify: readmeai --help"
  ],
  "note": "Requires Python 3.9+. Supports offline mode (no API key needed). For AI modes, set the appropriate API key as environment variable."
}
