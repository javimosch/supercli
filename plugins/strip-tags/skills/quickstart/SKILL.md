---
name: strip-tags
description: Strip HTML tags from text for LLM processing
---
# strip-tags Plugin
Strip HTML tags from text for LLM processing

## Usage
- `strip-tags _ _ <args>` — Run strip-tags with any arguments
