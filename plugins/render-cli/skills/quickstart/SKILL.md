---
name: render-cli
description: Use this skill when the user wants to use the render command-line tool.
---

# render-cli Plugin

Render CLI for cloud application deployment

## Commands
- `render-cli self version` — Print version
- `render-cli _ _` — Passthrough to render

## Installation
```bash
brew install render-cli
```
