{
  "plugin": "reg",
  "binary": "reg",
  "check": "which reg",
  "install_steps": [
    "brew install reg",
    "Verify: reg --version",
    "supercli plugins install ./plugins/reg --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/genuinetools/reg@latest. Docker registry CLI that doesn't require pulling images. List repositories, inspect manifests, and analyze images directly from the registry."
}
