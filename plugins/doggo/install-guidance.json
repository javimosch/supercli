{
  "plugin": "doggo",
  "binary": "doggo",
  "check": "which doggo",
  "install_steps": [
    "brew install doggo",
    "Verify: doggo --version",
    "supercli plugins install ./plugins/doggo --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/mr-karan/doggo/cmd/doggo@latest. Supports DNS over HTTPS (DoH) and DNS over TLS (DoT). Colorized output and JSON format available."
}
