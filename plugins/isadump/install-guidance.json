{
  "plugin": "isadump",
  "binary": "isadump",
  "check": "which isadump",
  "install_steps": [
    "apt-get install isadump 2>/dev/null || which isadump",
    "Verify: isadump --version",
    "supercli plugins install ./plugins/isadump --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}