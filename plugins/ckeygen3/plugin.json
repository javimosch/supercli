{
  "name": "ckeygen3",
  "version": "0.1.0",
  "description": "CLI tool: ckeygen3",
  "source": "https://packages.debian.org/ckeygen3",
  "checks": [{ "type": "binary", "name": "ckeygen3" }],
  "install_guidance": {
    "plugin": "ckeygen3", "binary": "ckeygen3", "check": "which ckeygen3",
    "install_steps": ["apt-get install ckeygen3 2>/dev/null || which ckeygen3", "Verify: ckeygen3 --version", "supercli plugins install ./plugins/ckeygen3 --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ckeygen3", "resource": "_", "action": "_",
    "description": "Passthrough to ckeygen3 CLI",
    "adapter": "process", "adapterConfig": { "command": "ckeygen3", "passthrough": true, "missingDependencyHelp": "Install ckeygen3: apt-get install ckeygen3" }, "args": []
  }]
}
