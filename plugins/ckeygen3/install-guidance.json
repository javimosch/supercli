{ "plugin": "ckeygen3", "binary": "ckeygen3", "check": "which ckeygen3", "install_steps": ["apt-get install ckeygen3 2>/dev/null || which ckeygen3", "Verify: ckeygen3 --version", "supercli plugins install ./plugins/ckeygen3 --on-conflict replace --json"] }
