{
  "plugin": "textstat",
  "binary": "textstat",
  "check": "which textstat",
  "install_steps": ["pip install textstat", "Verify: textstat --version", "supercli plugins install ./plugins/textstat --on-conflict replace --json"]
}
