{
  "plugin": "hash-buster",
  "binary": "hash-buster",
  "check": "which hash-buster",
  "install_steps": ["pip install hash-buster", "Verify: hash-buster --version", "supercli plugins install ./plugins/hash-buster --on-conflict replace --json"]
}
