{
  "plugin": "rename.ul",
  "binary": "rename.ul",
  "check": "which rename.ul",
  "install_steps": [
    "# Install rename.ul",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/rename.ul-linux-amd64 -o /usr/local/bin/rename.ul",
    "chmod +x /usr/local/bin/rename.ul",
    "# Or via package manager, see /usr/bin/rename.ul"
  ]
}