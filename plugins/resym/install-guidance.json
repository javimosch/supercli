{
  "plugin": "resym",
  "binary": "resym",
  "check": "which resym",
  "install_steps": [
    "cargo install resym",
    "Verify: resym --version",
    "supercli plugins install ./plugins/resym --on-conflict replace --json"
  ]
}