{
  "plugin": "explodeview",
  "binary": "explodeview",
  "check": "which explodeview",
  "install_steps": ["npm install -g explodeview", "Verify: explodeview --version", "supercli plugins install ./plugins/explodeview --on-conflict replace --json"]
}
