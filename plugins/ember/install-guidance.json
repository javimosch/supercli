{
  "plugin": "ember",
  "binary": "ember",
  "check": "which ember",
  "install_steps": [
    "# Install ember",
    "# See emberjs.com",
    "# apt: sudo apt-get install ember",
    "# brew: brew install ember",
    "# cargo: cargo install ember",
    "# npm: npm install -g ember"
  ]
}