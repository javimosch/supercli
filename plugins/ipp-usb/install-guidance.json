{
  "plugin": "ipp-usb",
  "binary": "ipp-usb",
  "check": "which ipp-usb",
  "install_steps": [
    "apt-get install ipp-usb 2>/dev/null || which ipp-usb",
    "Verify: ipp-usb --version",
    "supercli plugins install ./plugins/ipp-usb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}