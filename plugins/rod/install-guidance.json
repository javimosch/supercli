{
  "plugin": "rod",
  "binary": "rod",
  "check": "which rod",
  "install_steps": [
    "go install github.com/go-rod/rod@latest",
    "Verify: rod --version",
    "supercli plugins install ./plugins/rod --on-conflict replace --json"
  ]
}
