{
  "plugin": "pycld3",
  "binary": "pycld3",
  "check": "which pycld3",
  "install_steps": ["pip install pycld3", "Verify: pycld3 --version", "supercli plugins install ./plugins/pycld3 --on-conflict replace --json"]
}
