{
  "plugin": "pamdice",
  "binary": "pamdice",
  "check": "which pamdice",
  "install_steps": [
    "apt-get install pamdice 2>/dev/null || which pamdice",
    "Verify: pamdice --version",
    "supercli plugins install ./plugins/pamdice --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}