{
  "plugin": "czg",
  "binary": "czg",
  "check": "which czg",
  "install_steps": [
    "npm install -g czg",
    "Or: brew install czg",
    "Or: cargo install czg",
    "Verify: czg --version",
    "supercli plugins install ./plugins/czg --on-conflict replace --json"
  ],
  "note": "czg is the standalone CLI for cz-git. It provides an interactive prompt for creating Conventional Commits. Customize behavior with a .czrc or czg config file. Supports emoji commits (--mode emoji), multiple languages, and integration with Git hooks."
}
