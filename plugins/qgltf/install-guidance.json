{
  "plugin": "qgltf",
  "binary": "qgltf",
  "check": "which qgltf",
  "install_steps": [
    "apt-get install qgltf 2>/dev/null || which qgltf",
    "Verify: qgltf --version",
    "supercli plugins install ./plugins/qgltf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}