{
  "plugin": "xcutsel",
  "binary": "xcutsel",
  "check": "which xcutsel",
  "install_steps": [
    "apt-get install xcutsel 2>/dev/null || which xcutsel",
    "Verify: xcutsel --version",
    "supercli plugins install ./plugins/xcutsel --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}