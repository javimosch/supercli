{
  "plugin": "mcomp",
  "binary": "mcomp",
  "check": "which mcomp",
  "install_steps": [
    "apt-get install mcomp 2>/dev/null || which mcomp",
    "Verify: mcomp --version",
    "supercli plugins install ./plugins/mcomp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}