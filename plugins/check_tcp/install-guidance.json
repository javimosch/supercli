{ "plugin": "check_tcp", "binary": "check_tcp", "check": "which check_tcp", "install_steps": ["apt-get install check_tcp 2>/dev/null || which check_tcp", "Verify: check_tcp --version", "supercli plugins install ./plugins/check_tcp --on-conflict replace --json"] }
