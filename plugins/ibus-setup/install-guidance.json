{
  "plugin": "ibus-setup",
  "binary": "ibus-setup",
  "check": "which ibus-setup",
  "install_steps": [
    "apt-get install ibus-setup 2>/dev/null || which ibus-setup",
    "Verify: ibus-setup --version",
    "supercli plugins install ./plugins/ibus-setup --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}