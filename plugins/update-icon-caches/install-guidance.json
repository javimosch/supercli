{
  "plugin": "update-icon-caches",
  "binary": "update-icon-caches",
  "check": "which update-icon-caches",
  "install_steps": [
    "apt-get install update-icon-caches 2>/dev/null || which update-icon-caches",
    "Verify: update-icon-caches --version",
    "supercli plugins install ./plugins/update-icon-caches --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}