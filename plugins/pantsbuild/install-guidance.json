{
  "plugin": "pantsbuild",
  "binary": "pantsbuild",
  "check": "which pantsbuild",
  "install_steps": ["curl install", "Verify: pantsbuild --version", "supercli plugins install ./plugins/pantsbuild --on-conflict replace --json"]
}
