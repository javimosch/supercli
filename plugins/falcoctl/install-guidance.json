{
  "plugin": "falcoctl",
  "binary": "falcoctl",
  "check": "which falcoctl",
  "install_steps": ["brew install falcoctl", "Verify: falcoctl --version", "supercli plugins install ./plugins/falcoctl --on-conflict replace --json"]
}
