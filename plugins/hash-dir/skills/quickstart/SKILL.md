---
name: hash-dir
description: hash-dir — compute SHA256 hashes for all files in a directory tree. Outputs JSON array.
---
# hash-dir
hash-dir — compute SHA256 hashes for all files in a directory tree. Outputs JSON array.
