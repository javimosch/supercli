{
  "plugin": "lsof",
  "binary": "lsof",
  "check": "which lsof",
  "install_steps": [
    "brew install lsof",
    "Verify: lsof --version",
    "supercli plugins install ./plugins/lsof --on-conflict replace --json"
  ]
}
