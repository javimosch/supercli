{
  "plugin": "eptex",
  "binary": "eptex",
  "check": "which eptex",
  "install_steps": [
    "apt-get install eptex 2>/dev/null || which eptex",
    "Verify: eptex --version",
    "supercli plugins install ./plugins/eptex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}