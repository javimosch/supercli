---
name: qtap
description: Use this skill when the user wants to use qtap for Qtap: An eBPF agent that captures pre-encrypted network traffic, providing rich .
---

# qtap Plugin

Qtap: An eBPF agent that captures pre-encrypted network traffic, providing rich context about egress connections and their originating processes.

## Commands

### General
- `qtap self version` — Print qtap version
- `qtap _ _` — Passthrough to qtap CLI

## Usage Examples
- "Run qtap"
- "Use qtap to Qtap: An eBPF agent that captures pre-encrypted network traf"

## Installation

```bash
pip install qtap
```

## Key Features
- Qtap: An eBPF agent that captures pre-encrypted network traffic, providing rich context about egress
- CLI tool, headless and scriptable
