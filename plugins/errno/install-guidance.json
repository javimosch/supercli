{
  "plugin": "errno",
  "binary": "errno",
  "check": "which errno",
  "install_steps": [
    "apt-get install errno 2>/dev/null || which errno",
    "Verify: errno --version",
    "supercli plugins install ./plugins/errno --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}