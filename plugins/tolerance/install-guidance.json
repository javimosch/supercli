{
  "plugin": "tolerance",
  "binary": "tolaria",
  "check": "which tolaria",
  "install_steps": [
    "npm install -g tolaria",
    "Verify: tolaria --version",
    "supercli plugins install ./plugins/tolerance --on-conflict replace --json"
  ],
  "note": "Markdown knowledge base manager. Binary is tolaria."
}