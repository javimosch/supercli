{
  "name": "dm-del",
  "version": "0.1.0",
  "description": "CLI tool: dm-del",
  "source": "https://packages.debian.org/dm-del",
  "checks": [{ "type": "binary", "name": "dm-del" }],
  "install_guidance": {
    "plugin": "dm-del", "binary": "dm-del", "check": "which dm-del",
    "install_steps": ["apt-get install dm-del 2>/dev/null || which dm-del", "Verify: dm-del --version", "supercli plugins install ./plugins/dm-del --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-del", "resource": "_", "action": "_",
    "description": "Passthrough to dm-del CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-del", "passthrough": true, "missingDependencyHelp": "Install dm-del: apt-get install dm-del" }, "args": []
  }]
}
