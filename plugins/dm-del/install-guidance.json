{ "plugin": "dm-del", "binary": "dm-del", "check": "which dm-del", "install_steps": ["apt-get install dm-del 2>/dev/null || which dm-del", "Verify: dm-del --version", "supercli plugins install ./plugins/dm-del --on-conflict replace --json"] }
