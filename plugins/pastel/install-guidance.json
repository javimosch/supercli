{
  "plugin": "pastel",
  "binary": "pastel",
  "check": "which pastel",
  "install_steps": [
    "brew install pastel",
    "Verify: pastel --version",
    "supercli plugins install ./plugins/pastel --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install pastel. Available on all major platforms via package managers (apt, pacman, brew, snap, scoop, winget). Supports various color formats and conversions."
}
