{
  "plugin": "buku",
  "binary": "buku",
  "check": "which buku",
  "install_steps": [
    "pip install buku",
    "Or: brew install buku",
    "Verify: buku --version",
    "supercli plugins install ./plugins/buku --on-conflict replace --json"
  ],
  "note": "Buku stores bookmarks in a portable SQLite database. Auto-fetches titles, tags, and descriptions from URLs. Supports import/export from HTML, XBEL, Markdown, and RSS/Atom."
}
