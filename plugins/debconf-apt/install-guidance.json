{ "plugin": "debconf-apt", "binary": "debconf-apt", "check": "which debconf-apt", "install_steps": ["apt-get install debconf-apt 2>/dev/null || which debconf-apt", "Verify: debconf-apt --version", "supercli plugins install ./plugins/debconf-apt --on-conflict replace --json"] }
