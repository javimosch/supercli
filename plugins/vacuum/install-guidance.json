{
  "plugin": "vacuum",
  "binary": "vacuum",
  "check": "which vacuum",
  "install_steps": [
    "brew install vacuum",
    "Verify: vacuum --version",
    "supercli plugins install ./plugins/vacuum --on-conflict replace --json"
  ]
}
