{
  "name": "dinit",
  "version": "0.1.0",
  "description": "CLI tool: dinit",
  "source": "https://packages.debian.org/dinit",
  "checks": [{ "type": "binary", "name": "dinit" }],
  "install_guidance": {
    "plugin": "dinit", "binary": "dinit", "check": "which dinit",
    "install_steps": ["apt-get install dinit 2>/dev/null || which dinit", "Verify: dinit --version", "supercli plugins install ./plugins/dinit --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dinit", "resource": "_", "action": "_",
    "description": "Passthrough to dinit CLI",
    "adapter": "process", "adapterConfig": { "command": "dinit", "passthrough": true, "missingDependencyHelp": "Install dinit: apt-get install dinit" }, "args": []
  }]
}
