{ "plugin": "dinit", "binary": "dinit", "check": "which dinit", "install_steps": ["apt-get install dinit 2>/dev/null || which dinit", "Verify: dinit --version", "supercli plugins install ./plugins/dinit --on-conflict replace --json"] }
