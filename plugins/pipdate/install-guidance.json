{
  "plugin": "pipdate",
  "binary": "pipdate",
  "check": "which pipdate",
  "install_steps": ["pip install pipdate", "Verify: pipdate --version", "supercli plugins install ./plugins/pipdate --on-conflict replace --json"]
}
