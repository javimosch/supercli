{
  "plugin": "lookbib",
  "binary": "lookbib",
  "check": "which lookbib",
  "install_steps": [
    "apt-get install lookbib 2>/dev/null || which lookbib",
    "Verify: lookbib --version",
    "supercli plugins install ./plugins/lookbib --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}