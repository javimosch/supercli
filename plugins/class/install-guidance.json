{ "plugin": "class", "binary": "class", "check": "which class", "install_steps": ["apt-get install class 2>/dev/null || which class", "Verify: class --version", "supercli plugins install ./plugins/class --on-conflict replace --json"] }
