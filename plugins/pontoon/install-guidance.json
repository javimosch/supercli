{
  "plugin": "pontoon",
  "binary": "pontoon",
  "check": "which pontoon",
  "install_steps": [
    "# Install pontoon",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pontoon-linux-amd64 -o /usr/local/bin/pontoon",
    "chmod +x /usr/local/bin/pontoon",
    "# Or via package manager, see github.com/inthem/pontoon"
  ]
}