{
  "plugin": "smoke",
  "binary": "smoke",
  "check": "which smoke",
  "install_steps": ["go install github.com/skx/smoke@latest", "Verify: smoke --version", "supercli plugins install ./plugins/smoke --on-conflict replace --json"]
}
