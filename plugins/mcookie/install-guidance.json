{
  "plugin": "mcookie",
  "binary": "mcookie",
  "check": "which mcookie",
  "install_steps": [
    "apt-get install mcookie 2>/dev/null || which mcookie",
    "Verify: mcookie --version",
    "supercli plugins install ./plugins/mcookie --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}