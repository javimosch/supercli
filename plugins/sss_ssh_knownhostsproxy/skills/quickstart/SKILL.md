---
name: sss_ssh_knownhostsproxy
description: Use this skill when the user wants to use sss_ssh_knownhostsproxy, cli tool: sss_ssh_knownhostsproxy.
---

# sss_ssh_knownhostsproxy Plugin

CLI tool: sss_ssh_knownhostsproxy.

## Commands
- `sss_ssh_knownhostsproxy <resource> <action>` — Execute sss_ssh_knownhostsproxy commands
- `sss_ssh_knownhostsproxy self version` — Print sss_ssh_knownhostsproxy version
- `sss_ssh_knownhostsproxy _ _` — Passthrough to sss_ssh_knownhostsproxy CLI

## Usage Examples
- "sss_ssh_knownhostsproxy --help"
- "sss_ssh_knownhostsproxy self version"

## Installation
```bash
apt-get install sss_ssh_knownhostsproxy 2>/dev/null || which sss_ssh_knownhostsproxy
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
