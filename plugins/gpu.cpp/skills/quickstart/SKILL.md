---
name: gpu.cpp
description: Use this skill when the user wants to use gpu.cpp for A lightweight library for portable low-level GPU computation using WebGPU. .
---

# gpu.cpp Plugin

A lightweight library for portable low-level GPU computation using WebGPU. 

## Commands

### General
- `gpu.cpp self version` — Print gpu.cpp version
- `gpu.cpp _ _` — Passthrough to gpu.cpp CLI

## Usage Examples
- "Run gpu.cpp"
- "Use gpu.cpp to A lightweight library for portable low-level GPU computation"

## Installation

```bash
pip install gpu.cpp
```

## Key Features
- A lightweight library for portable low-level GPU computation using WebGPU. 
- CLI tool, headless and scriptable
