{
  "plugin": "storm",
  "binary": "storm",
  "check": "which storm",
  "install_steps": [
    "pip install storm",
    "Verify: storm --version",
    "supercli plugins install ./plugins/storm --on-conflict replace --json"
  ],
  "note": "Language: Python"
}