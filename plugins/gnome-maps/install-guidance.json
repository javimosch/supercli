{
  "plugin": "gnome-maps",
  "binary": "gnome-maps",
  "check": "which gnome-maps",
  "install_steps": [
    "apt-get install gnome-maps 2>/dev/null || which gnome-maps",
    "Verify: gnome-maps --version",
    "supercli plugins install ./plugins/gnome-maps --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}