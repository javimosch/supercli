{
  "name": "dpkg-realpath",
  "version": "1.0.0",
  "description": "dpkg-realpath \u2014 dpkg-realpath \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-realpath"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-realpath",
    "binary": "dpkg-realpath",
    "check": "which dpkg-realpath",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-realpath",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-realpath \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-realpath",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}