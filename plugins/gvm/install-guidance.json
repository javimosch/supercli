{
  "plugin": "gvm",
  "binary": "gvm",
  "check": "which gvm",
  "install_steps": [
    "Download the appropriate binary from GitHub Releases (https://github.com/toodofun/gvm/releases)",
    "Place it in your PATH and make it executable",
    "Verify: gvm --version",
    "supercli plugins install ./plugins/gvm --on-conflict replace --json"
  ],
  "note": "Also supports building from source: git clone https://github.com/toodofun/gvm.git && cd gvm && make build. The binary from releases is named gvm."
}
