{
  "plugin": "eureka",
  "binary": "eureka",
  "check": "which eureka",
  "install_steps": [
    "cargo install eureka",
    "Verify: eureka --version",
    "supercli plugins install ./plugins/eureka --on-conflict replace --json"
  ]
}