{
  "plugin": "gparted",
  "binary": "gparted",
  "check": "which gparted",
  "install_steps": [
    "apt-get install gparted 2>/dev/null || which gparted",
    "Verify: gparted --version",
    "supercli plugins install ./plugins/gparted --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}