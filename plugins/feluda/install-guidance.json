{
  "plugin": "feluda",
  "binary": "feluda",
  "check": "which feluda",
  "install_steps": [
    "cargo install feluda",
    "Verify: feluda --version",
    "supercli plugins install ./plugins/feluda --on-conflict replace --json"
  ],
  "note": "Also available via: brew install feluda, apt install (DEB), rpm install (RPM), Arch AUR (paru -S feluda), or download from GitHub releases."
}