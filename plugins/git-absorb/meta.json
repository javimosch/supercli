{
  "description": "git-absorb — auto-create fixup! commits for your staged changes. Scans your working copy, finds which commits each staged diff belongs to, and writes a fixup! commit for each. Works with any Git repository.",
  "tags": ["git-absorb", "git", "rebase", "fixup", "commit", "rust"],
  "has_learn": true
}
