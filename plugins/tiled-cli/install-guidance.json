{
  "plugin": "tiled-cli",
  "binary": "tiled-cli",
  "check": "which tiled-cli",
  "install_steps": ["go install github.com/tiled-cli@latest", "Verify: tiled-cli --version", "supercli plugins install ./plugins/tiled-cli --on-conflict replace --json"]
}
