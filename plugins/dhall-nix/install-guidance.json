{ "plugin": "dhall-nix", "binary": "dhall-nix", "check": "which dhall-nix", "install_steps": ["apt-get install dhall-nix 2>/dev/null || which dhall-nix", "Verify: dhall-nix --version", "supercli plugins install ./plugins/dhall-nix --on-conflict replace --json"] }
