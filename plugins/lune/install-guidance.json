{
  "plugin": "lune",
  "binary": "lune",
  "check": "which lune",
  "install_steps": [
    "cargo install lune",
    "Verify: lune --version",
    "supercli plugins install ./plugins/lune --on-conflict replace --json"
  ]
}