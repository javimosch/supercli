{ "plugin": "annoyindex", "binary": "annoyindex", "check": "which annoyindex", "install_steps": ["apt-get install annoyindex 2>/dev/null || which annoyindex", "Verify: annoyindex --version", "supercli plugins install ./plugins/annoyindex --on-conflict replace --json"] }
