{
  "plugin": "bloop",
  "binary": "bloop",
  "check": "which bloop",
  "install_steps": ["brew install bloop", "Verify: bloop --version", "supercli plugins install ./plugins/bloop --on-conflict replace --json"]
}
