{
  "plugin": "opl2ofm",
  "binary": "opl2ofm",
  "check": "which opl2ofm",
  "install_steps": [
    "apt-get install opl2ofm 2>/dev/null || which opl2ofm",
    "Verify: opl2ofm --version",
    "supercli plugins install ./plugins/opl2ofm --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}