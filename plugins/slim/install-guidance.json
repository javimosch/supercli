{
  "plugin": "slim",
  "binary": "slim",
  "check": "which slim || echo 'slim not installed'",
  "install_steps": [
    "brew install docker-slim",
    "Verify: slim version",
    "supercli plugins install ./plugins/slim --on-conflict replace --json"
  ],
  "note": "Linux: curl -sL https://raw.githubusercontent.com/slimtoolkit/slim/master/scripts/install-slim.sh | sudo -E bash - . Requires Docker."
}
