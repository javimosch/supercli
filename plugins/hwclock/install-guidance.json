{
  "plugin": "hwclock",
  "binary": "hwclock",
  "check": "which hwclock",
  "install_steps": [
    "apt-get install hwclock 2>/dev/null || which hwclock",
    "Verify: hwclock --version",
    "supercli plugins install ./plugins/hwclock --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}