{
  "plugin": "fzy-find",
  "binary": "fzy-find",
  "check": "which fzy-find",
  "install_steps": ["make install", "Verify: fzy-find --version", "supercli plugins install ./plugins/fzy-find --on-conflict replace --json"]
}
