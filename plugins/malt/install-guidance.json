{
  "plugin": "malt",
  "binary": "malt",
  "check": "which malt",
  "install_steps": [
    "curl -fsSL https://raw.githubusercontent.com/indaco/malt/main/scripts/install.sh | bash",
    "Verify: malt --version",
    "supercli plugins install ./plugins/malt --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
