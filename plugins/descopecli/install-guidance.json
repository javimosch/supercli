{
  "plugin": "descopecli",
  "binary": "descope",
  "check": "which descope",
  "install_steps": [
    "sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys e8365d8513142909 && ...",
    "Verify: descope --version",
    "supercli plugins install ./plugins/descopecli --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
