{
  "plugin": "devbox",
  "binary": "devbox",
  "check": "which devbox",
  "install_steps": [
    "curl -fsSL https://get.jetify.com/devbox | bash",
    "Verify: devbox version",
    "supercli plugins install ./plugins/devbox --on-conflict replace --json"
  ],
  "note": "Also available via: brew install jetify-com/devbox/devbox. See https://www.jetify.com/docs/devbox/ for usage docs."
}
