{
  "plugin": "magic-g",
  "binary": "magic-g",
  "check": "which magic-g",
  "install_steps": [
    "apt-get install magic-g 2>/dev/null || which magic-g",
    "Verify: magic-g --version",
    "supercli plugins install ./plugins/magic-g --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}