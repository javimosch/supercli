{
  "plugin": "nail",
  "binary": "nail",
  "check": "which nail",
  "install_steps": [
    "cargo install nail-parquet",
    "Verify: nail --version",
    "supercli plugins install ./plugins/nail --on-conflict replace --json"
  ],
  "note": "Also available via Nix: nix shell nixpkgs#nail-parquet. Build from source: git clone https://github.com/Vitruves/nail-parquet && cd nail-parquet && cargo build --release && sudo cp target/release/nail /usr/local/bin/"
}
