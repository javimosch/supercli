---
name: traceroute.db
description: Use this skill when the user wants to use traceroute.db, cli tool: traceroute.db.
---

# traceroute.db Plugin

CLI tool: traceroute.db.

## Commands
- `traceroute.db <resource> <action>` — Execute traceroute.db commands
- `traceroute.db self version` — Print traceroute.db version
- `traceroute.db _ _` — Passthrough to traceroute.db CLI

## Usage Examples
- "traceroute.db --help"
- "traceroute.db self version"

## Installation
```bash
apt-get install traceroute.db 2>/dev/null || which traceroute.db
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
