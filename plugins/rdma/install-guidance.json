{
  "plugin": "rdma",
  "binary": "rdma",
  "check": "which rdma",
  "install_steps": [
    "apt-get install rdma 2>/dev/null || which rdma",
    "Verify: rdma --version",
    "supercli plugins install ./plugins/rdma --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}