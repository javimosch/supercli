{
  "plugin": "zmx",
  "binary": "zmx",
  "check": "which zmx",
  "install_steps": [
    "cargo install zmx",
    "Verify: zmx --version",
    "supercli plugins install ./plugins/zmx --on-conflict replace --json"
  ],
  "note": "Install via cargo: cargo install zmx"
}
