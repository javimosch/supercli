---
name: zig-aio
description: Use this skill when the user wants to use zig-aio for io_uring like asynchronous API and coroutine powered IO tasks for zig.
---

# zig-aio Plugin

io_uring like asynchronous API and coroutine powered IO tasks for zig

## Commands

### General
- `zig-aio self version` — Print zig-aio version
- `zig-aio _ _` — Passthrough to zig-aio CLI

## Usage Examples
- "Run zig-aio"
- "Use zig-aio to io_uring like asynchronous API and coroutine powered IO task"

## Installation

```bash
pip install zig-aio
```

## Key Features
- io_uring like asynchronous API and coroutine powered IO tasks for zig
- CLI tool, headless and scriptable
