{
  "plugin": "standard",
  "binary": "standard",
  "check": "which standard",
  "install_steps": [
    "npm install -g standard",
    "Verify: standard --version",
    "supercli plugins install ./plugins/standard --on-conflict replace --json"
  ],
  "note": "Standard is a zero-config linter and formatter for JavaScript. Use --fix to auto-fix problems. Configurable via standard.rc or package.json. Supports TypeScript with @standard/ts."
}
