{ "plugin": "check_ntp_peer", "binary": "check_ntp_peer", "check": "which check_ntp_peer", "install_steps": ["apt-get install check_ntp_peer 2>/dev/null || which check_ntp_peer", "Verify: check_ntp_peer --version", "supercli plugins install ./plugins/check_ntp_peer --on-conflict replace --json"] }
