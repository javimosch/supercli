{
  "plugin": "s-tui",
  "binary": "s-tui",
  "check": "which s-tui",
  "install_steps": [
    "pip install s-tui --user",
    "Verify: s-tui --version",
    "supercli plugins install ./plugins/s-tui --on-conflict replace --json"
  ],
  "note": "Also available via apt: apt install s-tui. Terminal-based CPU stress test and monitoring utility with graphical display of CPU temperature, frequency, power, and utilization."
}
