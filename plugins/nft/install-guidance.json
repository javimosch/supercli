{
  "plugin": "nft",
  "binary": "nft",
  "check": "which nft",
  "install_steps": [
    "apt-get install nft 2>/dev/null || which nft",
    "Verify: nft --version",
    "supercli plugins install ./plugins/nft --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}