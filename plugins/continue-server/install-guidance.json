{
  "plugin": "continue-server",
  "binary": "continue",
  "check": "which continue",
  "install_steps": ["npm install -g @continuedev/continue", "Verify: continue --version", "supercli plugins install ./plugins/continue-server --on-conflict replace --json"]
}
