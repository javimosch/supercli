{
  "plugin": "httpie",
  "binary": "http",
  "check": "which http",
  "install_steps": [
    "brew install httpie",
    "Verify: http --version",
    "supercli plugins install ./plugins/httpie --on-conflict replace --json"
  ],
  "note": "Also available via pip: pip install httpie. Pre-built packages available at https://httpie.io/download. Supports JSON, forms, file uploads, authentication, and custom headers out of the box."
}
