{
  "plugin": "httpie",
  "binary": "httpie",
  "check": "which httpie",
  "install_steps": [
    "brew install httpie",
    "Verify: httpie --version",
    "supercli plugins install ./plugins/httpie --on-conflict replace --json"
  ]
}