{
  "plugin": "xfr",
  "binary": "xfr",
  "check": "which xfr",
  "install_steps": [
    "cargo install xfr",
    "Verify: xfr --version",
    "supercli plugins install ./plugins/xfr --on-conflict replace --json"
  ],
  "note": "Also available via: brew install lance0/tap/xfr, eget lance0/xfr, yay -S xfr-bin (Arch), or download from GitHub releases. No authentication required for basic use."
}