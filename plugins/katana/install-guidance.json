{
  "plugin": "katana",
  "binary": "katana",
  "check": "which katana",
  "install_steps": [
    "brew install katana",
    "Verify: katana -version",
    "supercli plugins install ./plugins/katana --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/projectdiscovery/katana/cmd/katana@latest. Supports standard and headless crawling modes. JavaScript crawling available for SPAs. Configurable scope, rate limiting, and concurrency."
}
