{
  "plugin": "em-keyboard",
  "binary": "em",
  "check": "which em",
  "install_steps": [
    "pip install em-keyboard",
    "Verify: em --version",
    "supercli plugins install ./plugins/em-keyboard --on-conflict replace --json"
  ]
}
