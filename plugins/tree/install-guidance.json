{
  "plugin": "tree",
  "binary": "tree",
  "check": "which tree",
  "install_steps": [
    "brew install tree",
    "Verify: tree --version",
    "supercli plugins install ./plugins/tree --on-conflict replace --json"
  ]
}
