{
  "plugin": "pw-cat",
  "binary": "pw-cat",
  "check": "which pw-cat",
  "install_steps": [
    "apt-get install pw-cat 2>/dev/null || which pw-cat",
    "Verify: pw-cat --version",
    "supercli plugins install ./plugins/pw-cat --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}