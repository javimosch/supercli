{
  "plugin": "ntpq",
  "binary": "ntpq",
  "check": "which ntpq",
  "install_steps": [
    "apt-get install ntpq 2>/dev/null || which ntpq",
    "Verify: ntpq --version",
    "supercli plugins install ./plugins/ntpq --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}