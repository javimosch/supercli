{
  "name": "gnome-session",
  "version": "0.1.0",
  "description": "CLI tool: gnome-session",
  "source": "https://manpages.debian.org/gnome-session",
  "checks": [
    {
      "type": "binary",
      "name": "gnome-session"
    }
  ],
  "install_guidance": {
    "plugin": "gnome-session",
    "binary": "gnome-session",
    "check": "which gnome-session",
    "install_steps": [
      "apt-get install gnome-session 2>/dev/null || which gnome-session",
      "Verify: gnome-session --version",
      "supercli plugins install ./plugins/gnome-session --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "gnome-session",
      "resource": "self",
      "action": "version",
      "description": "Print gnome-session version",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-session",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install gnome-session: apt-get install gnome-session 2>/dev/null || which gnome-session"
      },
      "args": []
    },
    {
      "namespace": "gnome-session",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to gnome-session CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "gnome-session",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install gnome-session: apt-get install gnome-session 2>/dev/null || which gnome-session"
      },
      "args": []
    }
  ]
}