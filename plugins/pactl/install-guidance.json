{
  "plugin": "pactl",
  "binary": "pactl",
  "check": "which pactl",
  "install_steps": [
    "apt-get install pactl 2>/dev/null || which pactl",
    "Verify: pactl --version",
    "supercli plugins install ./plugins/pactl --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}