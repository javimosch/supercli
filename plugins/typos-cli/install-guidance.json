{
  "plugin": "typos-cli",
  "binary": "typos",
  "check": "which typos",
  "install_steps": [
    "brew install typos-cli",
    "Verify: typos --version",
    "supercli plugins install ./plugins/typos-cli --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install typos-cli. Pre-built binaries at https://github.com/crate-ci/typos/releases. Designed for CI pipelines with minimal false positives."
}
