{
  "name": "a2enmod",
  "version": "0.1.0",
  "description": "CLI tool: a2enmod",
  "source": "https://packages.debian.org/a2enmod",
  "checks": [{ "type": "binary", "name": "a2enmod" }],
  "install_guidance": {
    "plugin": "a2enmod", "binary": "a2enmod", "check": "which a2enmod",
    "install_steps": ["apt-get install a2enmod 2>/dev/null || which a2enmod", "Verify: a2enmod --version", "supercli plugins install ./plugins/a2enmod --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "a2enmod", "resource": "_", "action": "_",
    "description": "Passthrough to a2enmod CLI",
    "adapter": "process", "adapterConfig": { "command": "a2enmod", "passthrough": true, "missingDependencyHelp": "Install a2enmod: apt-get install a2enmod" }, "args": []
  }]
}
