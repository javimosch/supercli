{
  "plugin": "python3.11-config",
  "binary": "python3.11-config",
  "check": "which python3.11-config",
  "install_steps": [
    "apt-get install python3.11-config 2>/dev/null || which python3.11-config",
    "Verify: python3.11-config --version",
    "supercli plugins install ./plugins/python3.11-config --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}