{ "plugin": "derb", "binary": "derb", "check": "which derb", "install_steps": ["apt-get install derb 2>/dev/null || which derb", "Verify: derb --version", "supercli plugins install ./plugins/derb --on-conflict replace --json"] }
