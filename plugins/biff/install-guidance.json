{
  "plugin": "biff",
  "binary": "biff",
  "check": "which biff",
  "install_steps": [
    "cargo install biff",
    "Verify: biff --version",
    "supercli plugins install ./plugins/biff --on-conflict replace --json"
  ],
  "note": "Also available via GitHub Releases binaries (https://github.com/BurntSushi/biff/releases). For locale support, set BIFF_LOCALE and use a release binary or build with the locale feature enabled."
}
