{
  "plugin": "downtimed",
  "binary": "downtimed",
  "check": "which downtimed",
  "install_steps": [
    "# Install downtimed",
    "# See github.com/ir0nstone/downtimed",
    "# apt: sudo apt-get install downtimed",
    "# brew: brew install downtimed",
    "# cargo: cargo install downtimed",
    "# npm: npm install -g downtimed"
  ]
}