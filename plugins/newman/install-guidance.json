{
  "plugin": "newman",
  "binary": "newman",
  "check": "which newman",
  "install_steps": [
    "npm install -g newman",
    "Verify: newman --version",
    "supercli plugins install ./plugins/newman --on-conflict replace --json"
  ]
}
