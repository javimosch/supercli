{
  "plugin": "bats",
  "binary": "bats",
  "check": "bats --version",
  "install_steps": [
    "npm install -g bats",
    "bats --version"
  ],
  "note": "Also available via brew: brew install bats-core. On Debian/Ubuntu: apt install bats. See https://bats-core.readthedocs.io for documentation."
}
