{
  "plugin": "pnmcrop",
  "binary": "pnmcrop",
  "check": "which pnmcrop",
  "install_steps": [
    "apt-get install pnmcrop 2>/dev/null || which pnmcrop",
    "Verify: pnmcrop --version",
    "supercli plugins install ./plugins/pnmcrop --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}