{
  "plugin": "silo",
  "binary": "silo",
  "check": "which silo",
  "install_steps": [
    "curl -fsSL https://setup.silo.rs | sh",
    "Verify: silo ip",
    "supercli plugins install ./plugins/silo --on-conflict replace --json"
  ],
  "note": "Works with git worktrees, supports make, just, overmind, turbo"
}
