{
  "plugin": "ifnames",
  "binary": "ifnames",
  "check": "which ifnames",
  "install_steps": [
    "apt-get install ifnames 2>/dev/null || which ifnames",
    "Verify: ifnames --version",
    "supercli plugins install ./plugins/ifnames --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}