{
  "plugin": "zcmp",
  "binary": "zcmp",
  "check": "which zcmp",
  "install_steps": [
    "apt-get install zcmp 2>/dev/null || which zcmp",
    "Verify: zcmp --version",
    "supercli plugins install ./plugins/zcmp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}