{ "plugin": "cb", "binary": "cb", "check": "which cb", "install_steps": ["apt-get install cb 2>/dev/null || which cb", "Verify: cb --version", "supercli plugins install ./plugins/cb --on-conflict replace --json"] }
