{
  "name": "cb",
  "version": "0.1.0",
  "description": "CLI tool: cb",
  "source": "https://packages.debian.org/cb",
  "checks": [{ "type": "binary", "name": "cb" }],
  "install_guidance": {
    "plugin": "cb", "binary": "cb", "check": "which cb",
    "install_steps": ["apt-get install cb 2>/dev/null || which cb", "Verify: cb --version", "supercli plugins install ./plugins/cb --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cb", "resource": "_", "action": "_",
    "description": "Passthrough to cb CLI",
    "adapter": "process", "adapterConfig": { "command": "cb", "passthrough": true, "missingDependencyHelp": "Install cb: apt-get install cb" }, "args": []
  }]
}
