{
  "plugin": "warp",
  "binary": "warp",
  "check": "which warp",
  "install_steps": [
    "See https://github.com/spolu/warp",
    "Verify: warp --version",
    "supercli plugins install ./plugins/warp --on-conflict replace --json"
  ],
  "note": "See project repository"
}