{
  "plugin": "mcat",
  "binary": "mcat",
  "check": "which mcat",
  "install_steps": [
    "apt-get install mcat 2>/dev/null || which mcat",
    "Verify: mcat --version",
    "supercli plugins install ./plugins/mcat --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}