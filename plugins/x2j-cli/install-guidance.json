{
  "plugin": "x2j-cli",
  "binary": "x2j",
  "check": "which x2j",
  "install_steps": [
    "npm install --global x2j-cli",
    "Verify: x2j --help",
    "supercli plugins install ./plugins/x2j-cli --on-conflict replace --json"
  ],
  "note": "X2j-cli installs both 'x2j' and 'xml2json' executables. Reads XML from stdin and outputs formatted JSON to stdout. Perfect for pipeline operations."
}
