{
  "plugin": "faker-cli",
  "binary": "faker-cli",
  "check": "which faker-cli",
  "install_steps": [
    "npm install -g faker-cli",
    "Verify: faker-cli --version",
    "supercli plugins install ./plugins/faker-cli --on-conflict replace --json"
  ],
  "note": "Uses Faker.js v4 under the hood. Archived but fully functional. For locale support, use --locale flag (e.g., --locale de)."
}
