{
  "plugin": "lsipc",
  "binary": "lsipc",
  "check": "which lsipc",
  "install_steps": [
    "apt-get install lsipc 2>/dev/null || which lsipc",
    "Verify: lsipc --version",
    "supercli plugins install ./plugins/lsipc --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}