{
  "plugin": "ppdi",
  "binary": "ppdi",
  "check": "which ppdi",
  "install_steps": [
    "apt-get install ppdi 2>/dev/null || which ppdi",
    "Verify: ppdi --version",
    "supercli plugins install ./plugins/ppdi --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}