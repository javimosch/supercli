{
  "plugin": "noti",
  "binary": "noti",
  "check": "which noti",
  "install_steps": [
    "brew install noti",
    "Verify: noti --version",
    "supercli plugins install ./plugins/noti --on-conflict replace --json"
  ]
}
