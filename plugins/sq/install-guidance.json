{
  "plugin": "sq",
  "binary": "sq",
  "check": "which sq",
  "install_steps": [
    "go install github.com/neilotoole/sq@latest",
    "Verify: sq version",
    "supercli plugins install ./plugins/sq --on-conflict replace --json"
  ],
  "note": "Also available via brew: brew install sq. Query structured data (CSV, JSON, Excel, Parquet, Postgres, MySQL, SQL Server) with SQL or jq syntax. Cross-source joins are supported."
}
