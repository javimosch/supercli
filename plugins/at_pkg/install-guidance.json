{ "plugin": "at_pkg", "binary": "at_pkg", "check": "which at_pkg", "install_steps": ["apt-get install at_pkg 2>/dev/null || which at_pkg", "Verify: at_pkg --version", "supercli plugins install ./plugins/at_pkg --on-conflict replace --json"] }
