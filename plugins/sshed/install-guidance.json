{
  "plugin": "sshed",
  "binary": "sshed",
  "check": "which sshed",
  "install_steps": [
    "go install github.com/trntv/sshed@latest",
    "Verify: sshed --version",
    "supercli plugins install ./plugins/sshed --on-conflict replace --json"
  ]
}