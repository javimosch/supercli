{
  "plugin": "slack-cli",
  "binary": "slack",
  "check": "which slack",
  "install_steps": [
    "brew install slack",
    "Verify: slack --version",
    "supercli plugins install ./plugins/slack-cli --on-conflict replace --json"
  ]
}