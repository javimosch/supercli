---
name: mdcat
description: mdcat — Markdown renderer for the terminal. Renders Markdown files with syntax highlighting, tables, links. Cat for markdown.
---
# mdcat
mdcat — Markdown renderer for the terminal. Renders Markdown files with syntax highlighting, tables, links. Cat for markdown.
