{
  "plugin": "rsaperf",
  "binary": "rsaperf",
  "check": "which rsaperf",
  "install_steps": [
    "apt-get install rsaperf 2>/dev/null || which rsaperf",
    "Verify: rsaperf --version",
    "supercli plugins install ./plugins/rsaperf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}