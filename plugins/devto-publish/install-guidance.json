{
  "plugin": "devto-publish",
  "binary": "devto-publish",
  "check": "which devto-publish",
  "install_steps": [
    "brew install devto-publish",
    "Verify: devto-publish --version",
    "supercli plugins install ./plugins/devto-publish --on-conflict replace --json"
  ]
}