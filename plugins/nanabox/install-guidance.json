{
  "plugin": "nanabox",
  "binary": "nanobox",
  "check": "which nanobox",
  "install_steps": [
    "brew install nanobox",
    "Verify: nanobox --version",
    "supercli plugins install ./plugins/nanabox --on-conflict replace --json"
  ]
}