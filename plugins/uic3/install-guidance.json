{
  "plugin": "uic3",
  "binary": "uic3",
  "check": "which uic3",
  "install_steps": [
    "apt-get install uic3 2>/dev/null || which uic3",
    "Verify: uic3 --version",
    "supercli plugins install ./plugins/uic3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}