{ "plugin": "apt-config", "binary": "apt-config", "check": "which apt-config", "install_steps": ["apt-get install apt-config 2>/dev/null || which apt-config", "Verify: apt-config --version", "supercli plugins install ./plugins/apt-config --on-conflict replace --json"] }
