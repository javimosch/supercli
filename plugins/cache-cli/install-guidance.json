{
  "plugin": "cache-cli",
  "binary": "cache-cli",
  "check": "which cache-cli",
  "install_steps": ["go install github.com/cache-cli@latest", "Verify: cache-cli --version", "supercli plugins install ./plugins/cache-cli --on-conflict replace --json"]
}
