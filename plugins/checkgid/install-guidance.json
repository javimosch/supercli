{ "plugin": "checkgid", "binary": "checkgid", "check": "which checkgid", "install_steps": ["apt-get install checkgid 2>/dev/null || which checkgid", "Verify: checkgid --version", "supercli plugins install ./plugins/checkgid --on-conflict replace --json"] }
