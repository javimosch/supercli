{
  "name": "checkgid",
  "version": "0.1.0",
  "description": "CLI tool: checkgid",
  "source": "https://packages.debian.org/checkgid",
  "checks": [{ "type": "binary", "name": "checkgid" }],
  "install_guidance": {
    "plugin": "checkgid", "binary": "checkgid", "check": "which checkgid",
    "install_steps": ["apt-get install checkgid 2>/dev/null || which checkgid", "Verify: checkgid --version", "supercli plugins install ./plugins/checkgid --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "checkgid", "resource": "_", "action": "_",
    "description": "Passthrough to checkgid CLI",
    "adapter": "process", "adapterConfig": { "command": "checkgid", "passthrough": true, "missingDependencyHelp": "Install checkgid: apt-get install checkgid" }, "args": []
  }]
}
