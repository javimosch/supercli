{
  "plugin": "tsdown",
  "binary": "tsdown",
  "check": "which tsdown",
  "install_steps": ["npm install -g tsdown", "Verify: tsdown --version", "supercli plugins install ./plugins/tsdown --on-conflict replace --json"]
}
