{
  "plugin": "getfacl",
  "binary": "getfacl",
  "check": "which getfacl",
  "install_steps": [
    "# Install getfacl",
    "# See savannah.nongnu.org",
    "# apt: sudo apt-get install getfacl",
    "# brew: brew install getfacl",
    "# cargo: cargo install getfacl",
    "# npm: npm install -g getfacl"
  ]
}