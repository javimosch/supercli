{
  "plugin": "eutp",
  "binary": "eutp",
  "check": "which eutp",
  "install_steps": [
    "apt-get install eutp 2>/dev/null || which eutp",
    "Verify: eutp --version",
    "supercli plugins install ./plugins/eutp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}