{
  "name": "frps",
  "version": "1.0.0",
  "description": "frps \u2014 frps \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "frps"
    }
  ],
  "install_guidance": {
    "plugin": "frps",
    "binary": "frps",
    "check": "which frps",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "frps",
      "resource": "run",
      "action": "exec",
      "description": "frps \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "frps",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}