{
  "plugin": "hp-probe",
  "binary": "hp-probe",
  "check": "which hp-probe",
  "install_steps": [
    "apt-get install hp-probe 2>/dev/null || which hp-probe",
    "Verify: hp-probe --version",
    "supercli plugins install ./plugins/hp-probe --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}