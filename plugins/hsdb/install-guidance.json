{
  "plugin": "hsdb",
  "binary": "hsdb",
  "check": "which hsdb",
  "install_steps": [
    "apt-get install hsdb 2>/dev/null || which hsdb",
    "Verify: hsdb --version",
    "supercli plugins install ./plugins/hsdb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}