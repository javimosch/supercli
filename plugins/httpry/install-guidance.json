{
  "plugin": "httpry",
  "binary": "httpry",
  "check": "which httpry",
  "install_steps": [
    "brew install httpry",
    "Verify: httpry --version",
    "supercli plugins install ./plugins/httpry --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}