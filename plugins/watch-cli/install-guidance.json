{
  "plugin": "watch-cli",
  "binary": "watch",
  "check": "which watch",
  "install_steps": [
    "npm install -g watch-cli",
    "Verify: watch --version",
    "supercli plugins install ./plugins/watch-cli --on-conflict replace --json"
  ],
  "note": "Designed for npm workflows but can execute any command. Supports glob patterns, multiple patterns, and environment variables. Great for development and build processes."
}
