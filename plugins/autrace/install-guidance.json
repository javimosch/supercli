{ "plugin": "autrace", "binary": "autrace", "check": "which autrace", "install_steps": ["apt-get install autrace 2>/dev/null || which autrace", "Verify: autrace --version", "supercli plugins install ./plugins/autrace --on-conflict replace --json"] }
