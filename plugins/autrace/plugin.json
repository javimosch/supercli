{
  "name": "autrace",
  "version": "0.1.0",
  "description": "CLI tool: autrace",
  "source": "https://packages.debian.org/autrace",
  "checks": [{ "type": "binary", "name": "autrace" }],
  "install_guidance": {
    "plugin": "autrace", "binary": "autrace", "check": "which autrace",
    "install_steps": ["apt-get install autrace 2>/dev/null || which autrace", "Verify: autrace --version", "supercli plugins install ./plugins/autrace --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "autrace", "resource": "_", "action": "_",
    "description": "Passthrough to autrace CLI",
    "adapter": "process", "adapterConfig": { "command": "autrace", "passthrough": true, "missingDependencyHelp": "Install autrace: apt-get install autrace" }, "args": []
  }]
}
