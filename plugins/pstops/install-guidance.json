{
  "plugin": "pstops",
  "binary": "pstops",
  "check": "which pstops",
  "install_steps": [
    "apt-get install pstops 2>/dev/null || which pstops",
    "Verify: pstops --version",
    "supercli plugins install ./plugins/pstops --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}