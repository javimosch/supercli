{
  "plugin": "qo",
  "binary": "qo",
  "check": "which qo",
  "install_steps": [
    "brew install kiki-ki/tap/qo",
    "Verify: qo --version",
    "supercli plugins install ./plugins/qo --on-conflict replace --json"
  ],
  "note": "Also available via shell install script (curl -sfL https://raw.githubusercontent.com/kiki-ki/go-qo/main/install.sh | sh) or building from source."
}
