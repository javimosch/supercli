{
  "plugin": "aider",
  "binary": "aider",
  "check": "which aider",
  "install_steps": [
    "brew install aider",
    "Verify: aider --version",
    "supercli plugins install ./plugins/aider --on-conflict replace --json"
  ]
}