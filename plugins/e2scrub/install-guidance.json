{
  "plugin": "e2scrub",
  "binary": "e2scrub",
  "check": "which e2scrub",
  "install_steps": [
    "apt-get install e2scrub 2>/dev/null || which e2scrub",
    "Verify: e2scrub --version",
    "supercli plugins install ./plugins/e2scrub --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}