{ "plugin": "cgget", "binary": "cgget", "check": "which cgget", "install_steps": ["apt-get install cgget 2>/dev/null || which cgget", "Verify: cgget --version", "supercli plugins install ./plugins/cgget --on-conflict replace --json"] }
