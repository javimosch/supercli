{
  "name": "prom2json",
  "version": "1.0.0",
  "description": "Convert Prometheus metrics to JSON format for parsing and processing",
  "commands": [
    {
      "name": "convert",
      "description": "Convert Prometheus metrics URL to JSON",
      "binary": "prom2json",
      "args": ["{{.url}}"],
      "output_format": "json"
    },
    {
      "name": "convert-file",
      "description": "Convert Prometheus metrics file to JSON",
      "binary": "prom2json",
      "args": ["{{.file}}"],
      "output_format": "json"
    },
    {
      "name": "convert-stdin",
      "description": "Convert Prometheus metrics from stdin to JSON",
      "binary": "prom2json",
      "args": [],
      "output_format": "json"
    },
    {
      "name": "convert-tls",
      "description": "Convert HTTPS metrics with TLS client authentication",
      "binary": "prom2json",
      "args": ["--cert", "{{.cert}}", "--key", "{{.key}}", "{{.url}}"],
      "output_format": "json"
    },
    {
      "name": "convert-insecure",
      "description": "Convert HTTPS metrics without TLS validation (testing only)",
      "binary": "prom2json",
      "args": ["--accept-invalid-cert", "{{.url}}"],
      "output_format": "json"
    },
    {
      "name": "convert-utf8",
      "description": "Convert metrics with UTF-8 escaping scheme",
      "binary": "prom2json",
      "args": ["--escaping", "allow-utf-8", "{{.url}}"],
      "output_format": "json"
    }
  ]
}