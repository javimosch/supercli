{
  "plugin": "roll",
  "binary": "roll",
  "check": "which roll",
  "install_steps": ["brew install roll", "Verify: roll --version", "supercli plugins install ./plugins/roll --on-conflict replace --json"]
}
