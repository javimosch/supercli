{
  "plugin": "resume",
  "binary": "resume",
  "check": "resume --version",
  "install_steps": [
    "npm install -g resume-cli"
  ],
  "note": "Requires Node.js and npm. Installs globally as 'resume'."
}