{
  "plugin": "tftopl",
  "binary": "tftopl",
  "check": "which tftopl",
  "install_steps": [
    "apt-get install tftopl 2>/dev/null || which tftopl",
    "Verify: tftopl --version",
    "supercli plugins install ./plugins/tftopl --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}