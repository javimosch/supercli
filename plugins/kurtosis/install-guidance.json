{
  "plugin": "kurtosis",
  "binary": "kurtosis",
  "check": "which kurtosis",
  "install_steps": ["brew install kurtosis-tech/tap/kurtosis-cli", "Verify: kurtosis --version", "supercli plugins install ./plugins/kurtosis --on-conflict replace --json"]
}
