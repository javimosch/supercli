{
  "name": "pyi-grab_version",
  "version": "0.1.0",
  "description": "CLI tool: pyi-grab_version",
  "source": "https://manpages.debian.org/pyi-grab_version",
  "checks": [
    {
      "type": "binary",
      "name": "pyi-grab_version"
    }
  ],
  "install_guidance": {
    "plugin": "pyi-grab_version",
    "binary": "pyi-grab_version",
    "check": "which pyi-grab_version",
    "install_steps": [
      "apt-get install pyi-grab_version 2>/dev/null || which pyi-grab_version",
      "Verify: pyi-grab_version --version",
      "supercli plugins install ./plugins/pyi-grab_version --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "pyi-grab_version",
      "resource": "self",
      "action": "version",
      "description": "Print pyi-grab_version version",
      "adapter": "process",
      "adapterConfig": {
        "command": "pyi-grab_version",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install pyi-grab_version: apt-get install pyi-grab_version 2>/dev/null || which pyi-grab_version"
      },
      "args": []
    },
    {
      "namespace": "pyi-grab_version",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to pyi-grab_version CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "pyi-grab_version",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install pyi-grab_version: apt-get install pyi-grab_version 2>/dev/null || which pyi-grab_version"
      },
      "args": []
    }
  ]
}