{
  "plugin": "sapling",
  "binary": "sapling",
  "check": "which sapling",
  "install_steps": ["brew install sapling", "Verify: sapling --version", "supercli plugins install ./plugins/sapling --on-conflict replace --json"]
}
