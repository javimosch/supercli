{
  "plugin": "marisa",
  "binary": "marisa",
  "check": "which marisa",
  "install_steps": [
    "brew install marisa",
    "Verify: marisa --version",
    "supercli plugins install ./plugins/marisa --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}