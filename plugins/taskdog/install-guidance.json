{
  "plugin": "taskdog",
  "binary": "taskdog",
  "check": "which taskdog",
  "install_steps": [
    "pip install taskdog",
    "Verify: taskdog --version",
    "supercli plugins install ./plugins/taskdog --on-conflict replace --json"
  ]
}