{
  "plugin": "gobackup",
  "binary": "gobackup",
  "check": "which gobackup",
  "install_steps": [
    "curl -sSL https://gobackup.github.io/install | sh",
    "Verify: gobackup --version",
    "Configure: ~/.gobackup/gobackup.yml",
    "supercli plugins install ./plugins/gobackup --on-conflict replace --json"
  ],
  "note": "Requires config at ~/.gobackup/gobackup.yml. Supports MySQL, PostgreSQL, Redis, MongoDB, SQLite, and many cloud storages (S3, GCS, Azure, B2, R2, etc.)"
}