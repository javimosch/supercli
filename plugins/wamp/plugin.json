{
  "name": "wamp",
  "version": "0.1.0",
  "description": "CLI tool: wamp",
  "source": "https://manpages.debian.org/wamp",
  "checks": [
    {
      "type": "binary",
      "name": "wamp"
    }
  ],
  "install_guidance": {
    "plugin": "wamp",
    "binary": "wamp",
    "check": "which wamp",
    "install_steps": [
      "apt-get install wamp 2>/dev/null || which wamp",
      "Verify: wamp --version",
      "supercli plugins install ./plugins/wamp --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "wamp",
      "resource": "self",
      "action": "version",
      "description": "Print wamp version",
      "adapter": "process",
      "adapterConfig": {
        "command": "wamp",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install wamp: apt-get install wamp 2>/dev/null || which wamp"
      },
      "args": []
    },
    {
      "namespace": "wamp",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to wamp CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "wamp",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install wamp: apt-get install wamp 2>/dev/null || which wamp"
      },
      "args": []
    }
  ]
}