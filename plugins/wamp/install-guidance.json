{
  "plugin": "wamp",
  "binary": "wamp",
  "check": "which wamp",
  "install_steps": [
    "apt-get install wamp 2>/dev/null || which wamp",
    "Verify: wamp --version",
    "supercli plugins install ./plugins/wamp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}