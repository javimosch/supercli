{
  "plugin": "gtf",
  "binary": "gtf",
  "check": "which gtf",
  "install_steps": [
    "apt-get install gtf 2>/dev/null || which gtf",
    "Verify: gtf --version",
    "supercli plugins install ./plugins/gtf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}