{
  "plugin": "select-default-wordlist",
  "binary": "select-default-wordlist",
  "check": "which select-default-wordlist",
  "install_steps": [
    "apt-get install select-default-wordlist 2>/dev/null || which select-default-wordlist",
    "Verify: select-default-wordlist --version",
    "supercli plugins install ./plugins/select-default-wordlist --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}