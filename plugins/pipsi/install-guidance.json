{
  "plugin": "pipsi",
  "binary": "pipsi",
  "check": "which pipsi",
  "install_steps": ["pip install pipsi", "Verify: pipsi --version", "supercli plugins install ./plugins/pipsi --on-conflict replace --json"]
}
