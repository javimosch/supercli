{
  "plugin": "click",
  "binary": "click",
  "check": "which click",
  "install_steps": [
    "brew install click",
    "Verify: click --version",
    "supercli plugins install ./plugins/click --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install click. Interactive Kubernetes CLI that provides a more user-friendly interface for kubectl operations. Features tab completion, context switching, and simplified commands."
}
