{
  "plugin": "markdown-pdf",
  "binary": "markdown-pdf",
  "check": "which markdown-pdf",
  "install_steps": [
    "npm install -g markdown-pdf",
    "Verify: markdown-pdf --version",
    "supercli plugins install ./plugins/markdown-pdf --on-conflict replace --json"
  ]
}
