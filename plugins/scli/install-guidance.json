{
  "plugin": "scli",
  "binary": "scli",
  "check": "which scli",
  "install_steps": [
    "apt-get install scli 2>/dev/null || which scli",
    "Verify: scli --version",
    "supercli plugins install ./plugins/scli --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}