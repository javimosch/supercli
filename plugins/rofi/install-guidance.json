{
  "plugin": "rofi",
  "binary": "rofi",
  "check": "which rofi",
  "install_steps": [
    "# Install rofi",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/rofi-linux-amd64 -o /usr/local/bin/rofi",
    "chmod +x /usr/local/bin/rofi",
    "# Or via package manager, see github.com/davatorium/rofi"
  ]
}