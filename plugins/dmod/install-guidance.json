{ "plugin": "dmod", "binary": "dmod", "check": "which dmod", "install_steps": ["apt-get install dmod 2>/dev/null || which dmod", "Verify: dmod --version", "supercli plugins install ./plugins/dmod --on-conflict replace --json"] }
