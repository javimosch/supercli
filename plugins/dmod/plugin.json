{
  "name": "dmod",
  "version": "0.1.0",
  "description": "CLI tool: dmod",
  "source": "https://packages.debian.org/dmod",
  "checks": [{ "type": "binary", "name": "dmod" }],
  "install_guidance": {
    "plugin": "dmod", "binary": "dmod", "check": "which dmod",
    "install_steps": ["apt-get install dmod 2>/dev/null || which dmod", "Verify: dmod --version", "supercli plugins install ./plugins/dmod --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dmod", "resource": "_", "action": "_",
    "description": "Passthrough to dmod CLI",
    "adapter": "process", "adapterConfig": { "command": "dmod", "passthrough": true, "missingDependencyHelp": "Install dmod: apt-get install dmod" }, "args": []
  }]
}
