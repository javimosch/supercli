{
  "plugin": "go-passbolt-cli",
  "binary": "passbolt",
  "check": "which passbolt",
  "install_steps": [
    "go install github.com/passbolt/go-passbolt-cli@latest",
    "Verify: passbolt --version",
    "supercli plugins install ./plugins/go-passbolt-cli --on-conflict replace --json"
  ]
}