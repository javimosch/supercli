{
  "plugin": "systemd-hwdb",
  "binary": "systemd-hwdb",
  "check": "which systemd-hwdb",
  "install_steps": [
    "apt-get install systemd-hwdb 2>/dev/null || which systemd-hwdb",
    "Verify: systemd-hwdb --version",
    "supercli plugins install ./plugins/systemd-hwdb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}