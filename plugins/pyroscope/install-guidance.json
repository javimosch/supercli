{
  "plugin": "pyroscope",
  "binary": "pyroscope",
  "check": "which pyroscope",
  "install_steps": ["brew install pyroscope", "Verify: pyroscope --version", "supercli plugins install ./plugins/pyroscope --on-conflict replace --json"]
}
