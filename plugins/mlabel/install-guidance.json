{
  "plugin": "mlabel",
  "binary": "mlabel",
  "check": "which mlabel",
  "install_steps": [
    "apt-get install mlabel 2>/dev/null || which mlabel",
    "Verify: mlabel --version",
    "supercli plugins install ./plugins/mlabel --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}