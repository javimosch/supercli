{
  "plugin": "presenterm",
  "binary": "presenterm",
  "check": "which presenterm",
  "install_steps": [
    "cargo install presenterm",
    "Verify: presenterm --version",
    "supercli plugins install ./plugins/presenterm --on-conflict replace --json"
  ]
}
