{
  "plugin": "clang-tidy",
  "binary": "clang-tidy",
  "check": "which clang-tidy",
  "install_steps": [
    "# Install clang-tidy",
    "# See clang.llvm.org",
    "# apt: sudo apt-get install clang-tidy",
    "# brew: brew install clang-tidy",
    "# cargo: cargo install clang-tidy",
    "# npm: npm install -g clang-tidy"
  ]
}