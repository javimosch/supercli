{
  "name": "disk-detect",
  "version": "0.1.0",
  "description": "CLI tool: disk-detect",
  "source": "https://packages.debian.org/disk-detect",
  "checks": [{ "type": "binary", "name": "disk-detect" }],
  "install_guidance": {
    "plugin": "disk-detect", "binary": "disk-detect", "check": "which disk-detect",
    "install_steps": ["apt-get install disk-detect 2>/dev/null || which disk-detect", "Verify: disk-detect --version", "supercli plugins install ./plugins/disk-detect --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "disk-detect", "resource": "_", "action": "_",
    "description": "Passthrough to disk-detect CLI",
    "adapter": "process", "adapterConfig": { "command": "disk-detect", "passthrough": true, "missingDependencyHelp": "Install disk-detect: apt-get install disk-detect" }, "args": []
  }]
}
