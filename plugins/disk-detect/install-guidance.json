{ "plugin": "disk-detect", "binary": "disk-detect", "check": "which disk-detect", "install_steps": ["apt-get install disk-detect 2>/dev/null || which disk-detect", "Verify: disk-detect --version", "supercli plugins install ./plugins/disk-detect --on-conflict replace --json"] }
