{
  "plugin": "faq",
  "binary": "faq",
  "check": "which faq",
  "install_steps": [
    "brew install jzelinskie/faq/faq",
    "Verify: faq --version",
    "supercli plugins install ./plugins/faq --on-conflict replace --json"
  ]
}