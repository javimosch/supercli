{
  "plugin": "links",
  "binary": "links",
  "check": "which links",
  "install_steps": [
    "brew install links",
    "Verify: links --version",
    "supercli plugins install ./plugins/links --on-conflict replace --json"
  ]
}
