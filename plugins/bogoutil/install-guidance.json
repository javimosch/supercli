{ "plugin": "bogoutil", "binary": "bogoutil", "check": "which bogoutil", "install_steps": ["apt-get install bogoutil 2>/dev/null || which bogoutil", "Verify: bogoutil --version", "supercli plugins install ./plugins/bogoutil --on-conflict replace --json"] }
