{
  "plugin": "burn",
  "binary": "burn",
  "check": "which burn",
  "install_steps": [
    "go install github.com/helm/burn@latest",
    "Verify: burn --version",
    "supercli plugins install ./plugins/burn --on-conflict replace --json"
  ],
  "note": "Requires Go toolchain and kubectl configured"
}
