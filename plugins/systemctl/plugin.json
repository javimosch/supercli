{
  "name": "systemctl",
  "version": "1.0.0",
  "description": "systemctl \u2014 CLI CLI utility",
  "source": "https://github.com/search?q=systemctl+cli&type=repositories",
  "checks": [
    {
      "type": "binary",
      "name": "systemctl"
    }
  ],
  "install_guidance": {
    "plugin": "systemctl",
    "binary": "systemctl",
    "check": "which systemctl",
    "install_steps": [
      "Install systemctl via package manager: apt install systemctl or brew install systemctl"
    ]
  },
  "commands": [
    {
      "namespace": "systemctl",
      "resource": "run",
      "action": "exec",
      "description": "systemctl \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "systemctl",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install systemctl via your package manager"
      },
      "args": []
    }
  ]
}