{
  "name": "amixer",
  "version": "0.1.0",
  "description": "CLI tool: amixer",
  "source": "https://packages.debian.org/amixer",
  "checks": [{ "type": "binary", "name": "amixer" }],
  "install_guidance": {
    "plugin": "amixer", "binary": "amixer", "check": "which amixer",
    "install_steps": ["apt-get install amixer 2>/dev/null || which amixer", "Verify: amixer --version", "supercli plugins install ./plugins/amixer --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "amixer", "resource": "_", "action": "_",
    "description": "Passthrough to amixer CLI",
    "adapter": "process", "adapterConfig": { "command": "amixer", "passthrough": true, "missingDependencyHelp": "Install amixer: apt-get install amixer" }, "args": []
  }]
}
