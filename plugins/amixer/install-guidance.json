{ "plugin": "amixer", "binary": "amixer", "check": "which amixer", "install_steps": ["apt-get install amixer 2>/dev/null || which amixer", "Verify: amixer --version", "supercli plugins install ./plugins/amixer --on-conflict replace --json"] }
