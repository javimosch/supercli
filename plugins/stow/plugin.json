{
  "name": "stow",
  "version": "0.1.0",
  "description": "GNU Stow — symlink farm manager. Manage dotfiles and package installations via symbolic links.",
  "source": "https://www.gnu.org/software/stow/",
  "checks": [
    {
      "type": "binary",
      "name": "stow"
    }
  ],
  "install_guidance": {
    "plugin": "stow",
    "binary": "stow",
    "check": "which stow",
    "install_steps": [
      "brew install stow",
      "sudo apt install stow",
      "sudo pacman -S stow"
    ],
    "note": "GNU Stow — a symlink farm manager. Also available via: dnf install stow, emerge stow, pkg install stow, xbps-install stow."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "stow",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to stow CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "stow",
        "passthrough": true,
        "missingDependencyHelp": "Install stow: brew install stow"
      },
      "args": []
    }
  ]
}
