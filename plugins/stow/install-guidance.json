{
  "plugin": "stow",
  "binary": "stow",
  "check": "which stow",
  "install_steps": [
    "brew install stow",
    "sudo apt install stow",
    "sudo pacman -S stow",
    "Verify: stow --version",
    "supercli plugins install ./plugins/stow --on-conflict replace --json"
  ],
  "note": "GNU Stow is a symlink farm manager. It manages packages of software and their associated config files from a central directory. Extremely popular for dotfile management."
}
