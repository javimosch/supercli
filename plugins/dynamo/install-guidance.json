{
  "plugin": "dynamo",
  "binary": "dynamo",
  "check": "which dynamo",
  "install_steps": [
    "pip install dynamo",
    "Verify: dynamo --version",
    "supercli plugins install ./plugins/dynamo --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}