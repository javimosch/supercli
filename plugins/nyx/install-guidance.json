{
  "install": "cargo install nyx-scanner",
  "verify": "nyx --version",
  "notes": [
    "Requires Rust toolchain (cargo)",
    "Binary name is 'nyx' (installed as nyx-scanner via cargo)",
    "Includes browser UI for interactive analysis (nyx serve)",
    "Supports SARIF output for CI/CD integration",
    "Engine profiles: ast (fastest), balanced (default), deep (most thorough)"
  ]
}