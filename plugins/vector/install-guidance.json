{
  "plugin": "vector",
  "binary": "vector",
  "check": "which vector",
  "install_steps": ["brew install vector", "Verify: vector --version", "supercli plugins install ./plugins/vector --on-conflict replace --json"]
}
