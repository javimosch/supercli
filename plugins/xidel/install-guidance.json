{
  "plugin": "xidel",
  "binary": "xidel",
  "check": "which xidel",
  "install_steps": [
    "Download from http://www.videlibri.de/xidel.html",
    "chmod +x xidel",
    "sudo mv xidel /usr/local/bin/",
    "Verify: xidel --version",
    "supercli plugins install ./plugins/xidel --on-conflict replace --json"
  ],
  "note": "Standalone statically-linked binary — no runtime dependencies. Supports CSS selectors, XPath 3.0, XQuery 3.0, and JSONiq for extracting data from HTML, XML, and JSON."
}
