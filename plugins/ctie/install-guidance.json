{ "plugin": "ctie", "binary": "ctie", "check": "which ctie", "install_steps": ["apt-get install ctie 2>/dev/null || which ctie", "Verify: ctie --version", "supercli plugins install ./plugins/ctie --on-conflict replace --json"] }
