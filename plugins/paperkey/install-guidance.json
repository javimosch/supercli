{ "plugin": "paperkey", "binary": "paperkey", "check": "which paperkey", "install_steps": ["apt-get install paperkey", "Verify: paperkey --version", "supercli plugins install ./plugins/paperkey --on-conflict replace --json"] }
