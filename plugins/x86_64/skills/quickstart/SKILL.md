---
name: x86_64
description: Use this skill when the user wants to use x86_64, cli tool: x86_64.
---

# x86_64 Plugin

CLI tool: x86_64.

## Commands
- `x86_64 <resource> <action>` — Execute x86_64 commands
- `x86_64 self version` — Print x86_64 version
- `x86_64 _ _` — Passthrough to x86_64 CLI

## Usage Examples
- "x86_64 --help"
- "x86_64 self version"

## Installation
```bash
apt-get install x86_64 2>/dev/null || which x86_64
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
