{
  "plugin": "x86_64",
  "binary": "x86_64",
  "check": "which x86_64",
  "install_steps": [
    "apt-get install x86_64 2>/dev/null || which x86_64",
    "Verify: x86_64 --version",
    "supercli plugins install ./plugins/x86_64 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}