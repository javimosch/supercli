{
  "plugin": "ipfs-deploy",
  "binary": "ipfs-deploy",
  "check": "which ipfs-deploy",
  "install_steps": [
    "See https://github.com/agentofuser/ipfs-deploy",
    "Verify: ipfs-deploy --version",
    "supercli plugins install ./plugins/ipfs-deploy --on-conflict replace --json"
  ],
  "note": "See project repository"
}