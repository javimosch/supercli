{ "plugin": "d1", "binary": "d1", "check": "which d1", "install_steps": ["apt-get install d1 2>/dev/null || which d1", "Verify: d1 --version", "supercli plugins install ./plugins/d1 --on-conflict replace --json"] }
