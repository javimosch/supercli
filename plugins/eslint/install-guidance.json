{
  "plugin": "eslint",
  "binary": "eslint",
  "check": "which eslint",
  "install_steps": [
    "npm install -g eslint",
    "Verify: eslint --version",
    "supercli plugins install ./plugins/eslint --on-conflict replace --json"
  ],
  "note": "ESLint is a highly configurable linter for JavaScript and TypeScript. Use --fix to auto-fix problems. Use --format json for machine-readable output. Requires .eslintrc configuration file."
}
