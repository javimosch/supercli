{
  "plugin": "isoprod",
  "binary": "isoprod",
  "check": "which isoprod",
  "install_steps": [
    "apt-get install isoprod 2>/dev/null || which isoprod",
    "Verify: isoprod --version",
    "supercli plugins install ./plugins/isoprod --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}