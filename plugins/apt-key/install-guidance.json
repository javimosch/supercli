{ "plugin": "apt-key", "binary": "apt-key", "check": "which apt-key", "install_steps": ["apt-get install apt-key 2>/dev/null || which apt-key", "Verify: apt-key --version", "supercli plugins install ./plugins/apt-key --on-conflict replace --json"] }
