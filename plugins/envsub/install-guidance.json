{
  "plugin": "envsub",
  "binary": "envsub",
  "check": "which envsub",
  "install_steps": ["npm install -g envsub", "Verify: envsub --version", "supercli plugins install ./plugins/envsub --on-conflict replace --json"]
}
