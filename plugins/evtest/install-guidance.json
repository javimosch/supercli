{
  "plugin": "evtest",
  "binary": "evtest",
  "check": "which evtest",
  "install_steps": [
    "# Install evtest",
    "# See github.com/freedesktop/evtest",
    "# apt: sudo apt-get install evtest",
    "# brew: brew install evtest",
    "# cargo: cargo install evtest",
    "# npm: npm install -g evtest"
  ]
}