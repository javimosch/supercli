{
  "plugin": "rage",
  "binary": "rage",
  "check": "which rage",
  "install_steps": [
    "cargo install rage",
    "Verify: rage --version",
    "supercli plugins install ./plugins/rage --on-conflict replace --json"
  ],
  "note": "Requires Rust 1.74+. Also supports: brew install rage, apk add rage, pacman -S rage-encryption, or pre-built binaries from GitHub releases."
}