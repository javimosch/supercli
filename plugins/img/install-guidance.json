{
  "plugin": "img",
  "binary": "img",
  "check": "which img",
  "install_steps": [
    "brew install img",
    "Verify: img --version",
    "supercli plugins install ./plugins/img --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/genuinetools/img@latest. Builds container images without Docker daemon or root privileges. Uses user namespaces for isolation. Great for CI/CD and unprivileged environments."
}
