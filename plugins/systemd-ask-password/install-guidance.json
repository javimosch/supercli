{
  "plugin": "systemd-ask-password",
  "binary": "systemd-ask-password",
  "check": "which systemd-ask-password",
  "install_steps": [
    "apt-get install systemd-ask-password 2>/dev/null || which systemd-ask-password",
    "Verify: systemd-ask-password --version",
    "supercli plugins install ./plugins/systemd-ask-password --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}