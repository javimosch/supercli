{
  "plugin": "teip",
  "binary": "teip",
  "check": "which teip",
  "install_steps": [
    "cargo install teip",
    "Verify: teip --version",
    "supercli plugins install ./plugins/teip --on-conflict replace --json"
  ]
}