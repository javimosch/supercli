{ "plugin": "chkconfig", "binary": "chkconfig", "check": "which chkconfig", "install_steps": ["apt-get install chkconfig 2>/dev/null || which chkconfig", "Verify: chkconfig --version", "supercli plugins install ./plugins/chkconfig --on-conflict replace --json"] }
