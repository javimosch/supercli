---
name: see
description: Use this skill when the user wants to use see, cli tool: see.
---

# see Plugin

CLI tool: see.

## Commands
- `see <resource> <action>` — Execute see commands
- `see self version` — Print see version
- `see _ _` — Passthrough to see CLI

## Usage Examples
- "see --help"
- "see self version"

## Installation
```bash
apt-get install see 2>/dev/null || which see
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
