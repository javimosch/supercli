{
  "plugin": "ddgr",
  "binary": "ddgr",
  "check": "which ddgr",
  "install_steps": [
    "pip install ddgr",
    "Verify: ddgr --version",
    "supercli plugins install ./plugins/ddgr --on-conflict replace --json"
  ],
  "note": "Also available via apt (sudo apt-get install ddgr) and brew (brew install ddgr). Uses DuckDuckGo's instant answer API — no API key or registration needed. Respects privacy: no tracking."
}
