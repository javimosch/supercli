{
  "plugin": "hasura-cli",
  "binary": "hasura-cli",
  "check": "which hasura-cli",
  "install_steps": [
    "brew install hasura-cli",
    "Verify: hasura-cli --version",
    "supercli plugins install ./plugins/hasura-cli --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}