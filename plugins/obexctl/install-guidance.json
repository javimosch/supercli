{
  "plugin": "obexctl",
  "binary": "obexctl",
  "check": "which obexctl",
  "install_steps": [
    "apt-get install obexctl 2>/dev/null || which obexctl",
    "Verify: obexctl --version",
    "supercli plugins install ./plugins/obexctl --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}