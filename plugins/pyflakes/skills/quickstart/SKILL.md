---
name: pyflakes
description: Use this skill when the user wants to use pyflakes, cli tool: pyflakes.
---

# pyflakes Plugin

CLI tool: pyflakes.

## Commands
- `pyflakes <resource> <action>` — Execute pyflakes commands
- `pyflakes self version` — Print pyflakes version
- `pyflakes _ _` — Passthrough to pyflakes CLI

## Usage Examples
- "pyflakes --help"
- "pyflakes self version"

## Installation
```bash
apt-get install pyflakes 2>/dev/null || which pyflakes
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
