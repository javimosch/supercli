{
  "plugin": "filesize-cli",
  "binary": "filesize",
  "check": "which filesize",
  "install_steps": [
    "npm install -g filesize-cli",
    "Verify: filesize --help",
    "supercli plugins install ./plugins/filesize-cli --on-conflict replace --json"
  ],
  "note": "Filesize-cli calculates file sizes and can output in human-readable format (KB, MB, GB) or raw bytes. Simple utility for checking file sizes from the command line."
}
