{
  "plugin": "cromwell",
  "binary": "cromwell",
  "check": "which cromwell",
  "install_steps": [
    "brew install cromwell",
    "Verify: cromwell --version",
    "supercli plugins install ./plugins/cromwell --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}