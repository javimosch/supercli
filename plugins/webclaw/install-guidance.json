{
  "plugin": "webclaw",
  "binary": "webclaw",
  "check": "which webclaw",
  "install_steps": [
    "brew tap 0xMassi/webclaw && brew install webclaw",
    "Verify: webclaw --version",
    "supercli plugins install ./plugins/webclaw --on-conflict replace --json"
  ],
  "note": "Also available via prebuilt binaries from GitHub Releases, cargo (cargo install --git https://github.com/0xMassi/webclaw.git webclaw-cli), or Docker (docker run --rm ghcr.io/0xmassi/webclaw)."
}
