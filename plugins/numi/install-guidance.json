{
  "plugin": "numi",
  "binary": "numi",
  "check": "which numi",
  "install_steps": [
    "npm install -g numi",
    "Verify: numi --version",
    "supercli plugins install ./plugins/numi --on-conflict replace --json"
  ]
}
