{
  "plugin": "foxguard",
  "binary": "foxguard",
  "check": "which foxguard",
  "install_steps": [
    "curl -fsSL https://foxguard.dev/install.sh | sh",
    "Verify: foxguard --version",
    "supercli plugins install ./plugins/foxguard --on-conflict replace --json"
  ],
  "note": "Also available via npm (npx foxguard), crates.io (cargo install foxguard), or as a VS Code extension for inline scanning on save."
}
