{
  "plugin": "slc",
  "binary": "slc",
  "check": "which slc",
  "install_steps": ["go install github.com/sstadick/slc@latest", "Verify: slc --version", "supercli plugins install ./plugins/slc --on-conflict replace --json"]
}
