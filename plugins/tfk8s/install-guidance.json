{
  "plugin": "tfk8s",
  "binary": "tfk8s",
  "check": "which tfk8s",
  "install_steps": [
    "go install github.com/jrhouston/tfk8s@latest",
    "Verify: tfk8s --version",
    "supercli plugins install ./plugins/tfk8s --on-conflict replace --json"
  ]
}