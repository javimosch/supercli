{
  "name": "curl-config",
  "version": "0.1.0",
  "description": "CLI tool: curl-config",
  "source": "https://packages.debian.org/curl-config",
  "checks": [{ "type": "binary", "name": "curl-config" }],
  "install_guidance": {
    "plugin": "curl-config", "binary": "curl-config", "check": "which curl-config",
    "install_steps": ["apt-get install curl-config 2>/dev/null || which curl-config", "Verify: curl-config --version", "supercli plugins install ./plugins/curl-config --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "curl-config", "resource": "_", "action": "_",
    "description": "Passthrough to curl-config CLI",
    "adapter": "process", "adapterConfig": { "command": "curl-config", "passthrough": true, "missingDependencyHelp": "Install curl-config: apt-get install curl-config" }, "args": []
  }]
}
