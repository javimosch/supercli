{ "plugin": "curl-config", "binary": "curl-config", "check": "which curl-config", "install_steps": ["apt-get install curl-config 2>/dev/null || which curl-config", "Verify: curl-config --version", "supercli plugins install ./plugins/curl-config --on-conflict replace --json"] }
