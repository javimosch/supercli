{
  "plugin": "uipath",
  "binary": "uipath",
  "check": "which uipath",
  "install_steps": [
    "brew install uipath",
    "Verify: uipath --version",
    "supercli plugins install ./plugins/uipath --on-conflict replace --json"
  ]
}