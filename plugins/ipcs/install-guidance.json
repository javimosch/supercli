{
  "plugin": "ipcs",
  "binary": "ipcs",
  "check": "which ipcs",
  "install_steps": [
    "# Install ipcs",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/ipcs-linux-amd64 -o /usr/local/bin/ipcs",
    "chmod +x /usr/local/bin/ipcs",
    "# Or via package manager, see /usr/bin/ipcs"
  ]
}