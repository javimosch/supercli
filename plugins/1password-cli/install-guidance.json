{
  "plugin": "1password-cli",
  "binary": "op",
  "check": "which op",
  "install_steps": [
    "brew install 1password-cli",
    "Verify: op --version",
    "supercli plugins install ./plugins/1password-cli --on-conflict replace --json"
  ],
  "note": "Requires a 1Password account and the 1Password desktop app (or op vault access through biometric unlock). Authenticate with: op account add --address <signin>.1password.com --email <email>"
}
