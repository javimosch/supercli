{
  "name": "dm-mount",
  "version": "0.1.0",
  "description": "CLI tool: dm-mount",
  "source": "https://packages.debian.org/dm-mount",
  "checks": [{ "type": "binary", "name": "dm-mount" }],
  "install_guidance": {
    "plugin": "dm-mount", "binary": "dm-mount", "check": "which dm-mount",
    "install_steps": ["apt-get install dm-mount 2>/dev/null || which dm-mount", "Verify: dm-mount --version", "supercli plugins install ./plugins/dm-mount --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dm-mount", "resource": "_", "action": "_",
    "description": "Passthrough to dm-mount CLI",
    "adapter": "process", "adapterConfig": { "command": "dm-mount", "passthrough": true, "missingDependencyHelp": "Install dm-mount: apt-get install dm-mount" }, "args": []
  }]
}
