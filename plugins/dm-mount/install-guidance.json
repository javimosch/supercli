{ "plugin": "dm-mount", "binary": "dm-mount", "check": "which dm-mount", "install_steps": ["apt-get install dm-mount 2>/dev/null || which dm-mount", "Verify: dm-mount --version", "supercli plugins install ./plugins/dm-mount --on-conflict replace --json"] }
