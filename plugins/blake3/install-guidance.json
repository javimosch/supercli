{
  "plugin": "blake3",
  "binary": "b3sum",
  "check": "which b3sum",
  "install_steps": [
    "cargo install b3sum",
    "Verify: b3sum --version",
    "supercli plugins install ./plugins/blake3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}