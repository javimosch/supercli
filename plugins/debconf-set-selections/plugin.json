{
  "name": "debconf-set-selections",
  "version": "1.0.0",
  "description": "debconf-set-selections \u2014 debconf-set-selections \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "debconf-set-selections"
    }
  ],
  "install_guidance": {
    "plugin": "debconf-set-selections",
    "binary": "debconf-set-selections",
    "check": "which debconf-set-selections",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "debconf-set-selectio",
      "resource": "run",
      "action": "exec",
      "description": "debconf-set-selections \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "debconf-set-selections",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}