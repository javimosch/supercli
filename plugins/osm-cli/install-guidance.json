{
  "plugin": "osm-cli",
  "binary": "osm-cli",
  "check": "which osm-cli",
  "install_steps": ["cargo install osm-cli", "Verify: osm-cli --version", "supercli plugins install ./plugins/osm-cli --on-conflict replace --json"]
}
