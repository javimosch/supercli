{
  "plugin": "ffuf",
  "binary": "ffuf",
  "check": "which ffuf",
  "install_steps": [
    "brew install ffuf",
    "Verify: ffuf --version",
    "supercli plugins install ./plugins/ffuf --on-conflict replace --json"
  ]
}
