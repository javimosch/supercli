{
  "plugin": "gdiffmk",
  "binary": "gdiffmk",
  "check": "which gdiffmk",
  "install_steps": [
    "apt-get install gdiffmk 2>/dev/null || which gdiffmk",
    "Verify: gdiffmk --version",
    "supercli plugins install ./plugins/gdiffmk --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}