{
  "plugin": "extension-cli",
  "binary": "extension",
  "check": "which extension",
  "install_steps": [
    "npm install -g extension-cli",
    "Verify: extension --version",
    "supercli plugins install ./plugins/extension-cli --on-conflict replace --json"
  ]
}