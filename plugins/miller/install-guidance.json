{
  "plugin": "miller",
  "binary": "mlr",
  "check": "which mlr",
  "install_steps": [
    "brew install miller",
    "Verify: mlr --version",
    "supercli plugins install ./plugins/miller --on-conflict replace --json"
  ]
}