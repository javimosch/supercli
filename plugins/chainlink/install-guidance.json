{
  "plugin": "chainlink",
  "binary": "chainlink",
  "check": "which chainlink",
  "install_steps": [
    "cargo install chainlink-tracker",
    "Verify: chainlink --version",
    "supercli plugins install ./plugins/chainlink --on-conflict replace --json"
  ],
  "note": "Requires Rust toolchain for installation. Binary downloads via GitHub releases may also be available. Stores data in .chainlink/issues.db (SQLite) in your project directory."
}
