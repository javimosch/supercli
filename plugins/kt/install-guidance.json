{
  "plugin": "kt",
  "binary": "kt",
  "check": "which kt",
  "install_steps": [
    "brew install kt",
    "Verify: kt --version",
    "supercli plugins install ./plugins/kt --on-conflict replace --json"
  ]
}