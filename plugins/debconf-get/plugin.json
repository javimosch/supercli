{
  "name": "debconf-get",
  "version": "0.1.0",
  "description": "CLI tool: debconf-get",
  "source": "https://packages.debian.org/debconf-get",
  "checks": [{ "type": "binary", "name": "debconf-get" }],
  "install_guidance": {
    "plugin": "debconf-get", "binary": "debconf-get", "check": "which debconf-get",
    "install_steps": ["apt-get install debconf-get 2>/dev/null || which debconf-get", "Verify: debconf-get --version", "supercli plugins install ./plugins/debconf-get --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "debconf-get", "resource": "_", "action": "_",
    "description": "Passthrough to debconf-get CLI",
    "adapter": "process", "adapterConfig": { "command": "debconf-get", "passthrough": true, "missingDependencyHelp": "Install debconf-get: apt-get install debconf-get" }, "args": []
  }]
}
