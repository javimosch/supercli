---
name: unreal-mcp
description: Use this skill when the user wants to use unreal-mcp for Enable AI assistant clients like Cursor, Windsurf and Claude Desktop to control .
---

# unreal-mcp Plugin

Enable AI assistant clients like Cursor, Windsurf and Claude Desktop to control Unreal Engine through natural language using the Model Context Protocol (MCP).

## Commands

### General
- `unreal-mcp self version` — Print unreal-mcp version
- `unreal-mcp _ _` — Passthrough to unreal-mcp CLI

## Usage Examples
- "Run unreal-mcp"
- "Use unreal-mcp to Enable AI assistant clients like Cursor, Windsurf and Claude"

## Installation

```bash
pip install unreal-mcp
```

## Key Features
- Enable AI assistant clients like Cursor, Windsurf and Claude Desktop to control Unreal Engine throug
- CLI tool, headless and scriptable
