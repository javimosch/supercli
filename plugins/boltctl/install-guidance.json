{ "plugin": "boltctl", "binary": "boltctl", "check": "which boltctl", "install_steps": ["apt-get install boltctl 2>/dev/null || which boltctl", "Verify: boltctl --version", "supercli plugins install ./plugins/boltctl --on-conflict replace --json"] }
