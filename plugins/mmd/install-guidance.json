{
  "plugin": "mmd",
  "binary": "mmd",
  "check": "which mmd",
  "install_steps": [
    "apt-get install mmd 2>/dev/null || which mmd",
    "Verify: mmd --version",
    "supercli plugins install ./plugins/mmd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}