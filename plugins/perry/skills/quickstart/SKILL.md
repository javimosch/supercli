---
name: perry
description: Use this skill when the user wants to use perry for A native TypeScript compiler written in Rust. Compiles TypeScript directly to ex.
---

# perry Plugin

A native TypeScript compiler written in Rust. Compiles TypeScript directly to executables using SWC and LLVM.

## Commands

### General
- `perry self version` — Print perry version
- `perry _ _` — Passthrough to perry CLI

## Usage Examples
- "Run perry"
- "Use perry to A native TypeScript compiler written in Rust. Compiles TypeS"

## Installation

```bash
pip install perry
```

## Key Features
- A native TypeScript compiler written in Rust. Compiles TypeScript directly to executables using SWC 
- CLI tool, headless and scriptable
