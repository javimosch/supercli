{
  "plugin": "perry",
  "binary": "perry",
  "check": "which perry",
  "install_steps": [
    "cargo install perry-typescript",
    "Verify: perry --version",
    "supercli plugins install ./plugins/perry --on-conflict replace --json"
  ],
  "note": "TypeScript compiler reimplemented in Rust for speed. Install via: cargo install perry-typescript"
}
