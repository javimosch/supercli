{
  "plugin": "dive",
  "binary": "dive",
  "check": "which dive",
  "install_steps": ["brew install dive", "Verify: dive --version", "supercli plugins install ./plugins/dive --on-conflict replace --json"]
}
