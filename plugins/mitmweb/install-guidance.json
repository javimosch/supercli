{
  "plugin": "mitmweb",
  "binary": "mitmweb",
  "check": "which mitmweb",
  "install_steps": [
    "apt-get install mitmweb 2>/dev/null || which mitmweb",
    "Verify: mitmweb --version",
    "supercli plugins install ./plugins/mitmweb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}