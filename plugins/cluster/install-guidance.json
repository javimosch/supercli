{ "plugin": "cluster", "binary": "cluster", "check": "which cluster", "install_steps": ["apt-get install cluster 2>/dev/null || which cluster", "Verify: cluster --version", "supercli plugins install ./plugins/cluster --on-conflict replace --json"] }
