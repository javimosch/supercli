{
  "name": "cluster",
  "version": "0.1.0",
  "description": "CLI tool: cluster",
  "source": "https://packages.debian.org/cluster",
  "checks": [{ "type": "binary", "name": "cluster" }],
  "install_guidance": {
    "plugin": "cluster", "binary": "cluster", "check": "which cluster",
    "install_steps": ["apt-get install cluster 2>/dev/null || which cluster", "Verify: cluster --version", "supercli plugins install ./plugins/cluster --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cluster", "resource": "_", "action": "_",
    "description": "Passthrough to cluster CLI",
    "adapter": "process", "adapterConfig": { "command": "cluster", "passthrough": true, "missingDependencyHelp": "Install cluster: apt-get install cluster" }, "args": []
  }]
}
