{
  "plugin": "hoconvert",
  "binary": "hoconvert",
  "check": "which hoconvert",
  "install_steps": [
    "cargo install hoconvert",
    "Verify: hoconvert --version"
  ],
  "note": "Converts HOCON (Human-Optimized Config Object Notation) files to JSON, YAML, or TOML. HOCON is used by Akka, Play Framework, and other JVM tools."
}