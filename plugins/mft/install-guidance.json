{
  "plugin": "mft",
  "binary": "mft",
  "check": "which mft",
  "install_steps": [
    "apt-get install mft 2>/dev/null || which mft",
    "Verify: mft --version",
    "supercli plugins install ./plugins/mft --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}