{
  "plugin": "dash-licenses",
  "binary": "dash-licenses",
  "check": "which dash-licenses",
  "install_steps": ["mvn clean package", "Verify: dash-licenses --version", "supercli plugins install ./plugins/dash-licenses --on-conflict replace --json"]
}
