{
  "plugin": "abtop",
  "binary": "abtop",
  "check": "which abtop",
  "install_steps": [
    "cargo install abtop",
    "Verify: abtop --version",
    "supercli plugins install ./plugins/abtop --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
