{
  "plugin": "parse.f2fs",
  "binary": "parse.f2fs",
  "check": "which parse.f2fs",
  "install_steps": [
    "apt-get install parse.f2fs 2>/dev/null || which parse.f2fs",
    "Verify: parse.f2fs --version",
    "supercli plugins install ./plugins/parse.f2fs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}