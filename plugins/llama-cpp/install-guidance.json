{
  "plugin": "llama-cpp",
  "binary": "llama-cpp",
  "check": "which llama-cpp",
  "install_steps": ["brew install llama.cpp", "Verify: llama-cpp --version", "supercli plugins install ./plugins/llama-cpp --on-conflict replace --json"]
}
