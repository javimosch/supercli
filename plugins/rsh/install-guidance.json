{
  "plugin": "rsh",
  "binary": "rsh",
  "check": "which rsh",
  "install_steps": [
    "apt-get install rsh 2>/dev/null || which rsh",
    "Verify: rsh --version",
    "supercli plugins install ./plugins/rsh --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}