{ "plugin": "ctangle", "binary": "ctangle", "check": "which ctangle", "install_steps": ["apt-get install ctangle 2>/dev/null || which ctangle", "Verify: ctangle --version", "supercli plugins install ./plugins/ctangle --on-conflict replace --json"] }
