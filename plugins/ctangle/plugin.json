{
  "name": "ctangle",
  "version": "0.1.0",
  "description": "CLI tool: ctangle",
  "source": "https://packages.debian.org/ctangle",
  "checks": [{ "type": "binary", "name": "ctangle" }],
  "install_guidance": {
    "plugin": "ctangle", "binary": "ctangle", "check": "which ctangle",
    "install_steps": ["apt-get install ctangle 2>/dev/null || which ctangle", "Verify: ctangle --version", "supercli plugins install ./plugins/ctangle --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ctangle", "resource": "_", "action": "_",
    "description": "Passthrough to ctangle CLI",
    "adapter": "process", "adapterConfig": { "command": "ctangle", "passthrough": true, "missingDependencyHelp": "Install ctangle: apt-get install ctangle" }, "args": []
  }]
}
