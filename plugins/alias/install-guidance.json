{ "plugin": "alias", "binary": "alias", "check": "which alias", "install_steps": ["apt-get install alias 2>/dev/null || which alias", "Verify: alias --version", "supercli plugins install ./plugins/alias --on-conflict replace --json"] }
