{
  "plugin": "iredis",
  "binary": "iredis",
  "check": "which iredis",
  "install_steps": [
    "pip install iredis",
    "Or: brew install iredis",
    "Verify: iredis --version",
    "supercli plugins install ./plugins/iredis --on-conflict replace --json"
  ],
  "note": "Available via pip and brew. Supports Redis URL, DSN from iredisrc config, TLS, SSH tunnels, and NAT maps for clusters. Use --raw for machine-friendly output."
}
