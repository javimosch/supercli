---
name: git-worktree-runner
description: Use this skill when the user wants to use git-worktree-runner for Bash-based Git worktree manager with editor and AI tool integration. Automates p.
---

# git-worktree-runner Plugin

Bash-based Git worktree manager with editor and AI tool integration. Automates per-branch worktree creation, configuration copying, dependency installation, and workspace setup for efficient parallel development.

## Commands

### General
- `git-worktree-runner self version` — Print git-worktree-runner version
- `git-worktree-runner _ _` — Passthrough to git-worktree-runner CLI

## Usage Examples
- "Run git-worktree-runner"
- "Use git-worktree-runner to Bash-based Git worktree manager with editor and AI tool inte"

## Installation

```bash
pip install git-worktree-runner
```

## Key Features
- Bash-based Git worktree manager with editor and AI tool integration. Automates per-branch worktree c
- CLI tool, headless and scriptable
