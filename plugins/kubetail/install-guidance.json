{
  "plugin": "kubetail",
  "binary": "kubetail",
  "check": "which kubetail",
  "install_steps": ["brew install kubetail", "Verify: kubetail --version", "supercli plugins install ./plugins/kubetail --on-conflict replace --json"]
}
