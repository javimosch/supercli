{
  "plugin": "mysql",
  "binary": "mysql",
  "check": "which mysql",
  "install_steps": [
    "brew install mysql",
    "Verify: mysql --version",
    "supercli plugins install ./plugins/mysql --on-conflict replace --json"
  ]
}