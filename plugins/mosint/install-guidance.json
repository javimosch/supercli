{
  "plugin": "mosint",
  "binary": "mosint",
  "check": "which mosint",
  "install_steps": [
    "go install -v github.com/alpkeskin/mosint/v3/cmd/mosint@latest",
    "Ensure $GOPATH/bin is in your PATH",
    "Create config file: touch ~/.mosint.yaml",
    "Add API keys: breach_directory_api_key, emailrep_api_key, hunter_api_key, intelx_api_key, haveibeenpwned_api_key",
    "Verify: mosint -h"
  ],
  "note": "Requires Go 1.21+. API keys must be configured in ~/.mosint.yaml for data enrichment. Without keys, basic DNS/domain lookups still work."
}
