{
  "plugin": "gnu-time",
  "binary": "gnu-time",
  "check": "which gnu-time",
  "install_steps": [
    "brew install gnu-time",
    "Verify: gnu-time --version",
    "supercli plugins install ./plugins/gnu-time --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}