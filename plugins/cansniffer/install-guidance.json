{ "plugin": "cansniffer", "binary": "cansniffer", "check": "which cansniffer", "install_steps": ["apt-get install cansniffer 2>/dev/null || which cansniffer", "Verify: cansniffer --version", "supercli plugins install ./plugins/cansniffer --on-conflict replace --json"] }
