{
  "plugin": "service",
  "binary": "service",
  "check": "which service",
  "install_steps": [
    "apt-get install service 2>/dev/null || which service",
    "Verify: service --version",
    "supercli plugins install ./plugins/service --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}