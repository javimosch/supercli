{
  "plugin": "e2scrub_all",
  "binary": "e2scrub_all",
  "check": "which e2scrub_all",
  "install_steps": [
    "apt-get install e2scrub_all 2>/dev/null || which e2scrub_all",
    "Verify: e2scrub_all --version",
    "supercli plugins install ./plugins/e2scrub_all --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}