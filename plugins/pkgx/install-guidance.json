{
  "plugin": "pkgx",
  "binary": "pkgx",
  "check": "which pkgx",
  "install_steps": [
    "curl -fsSL https://pkgx.sh | sh",
    "Verify: pkgx --version",
    "supercli plugins install ./plugins/pkgx --on-conflict replace --json"
  ],
  "note": "Also available via: brew install pkgx or npm install -g pkgx. pkgx provides ephemeral package environments — use `pkgx <package>@<version>` to run tools without global installs, and `pkgx dev` to start a shell with packages."
}
