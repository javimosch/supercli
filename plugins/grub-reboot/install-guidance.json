{
  "plugin": "grub-reboot",
  "binary": "grub-reboot",
  "check": "which grub-reboot",
  "install_steps": [
    "apt-get install grub-reboot 2>/dev/null || which grub-reboot",
    "Verify: grub-reboot --version",
    "supercli plugins install ./plugins/grub-reboot --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}