{
  "plugin": "pwdx",
  "binary": "pwdx",
  "check": "which pwdx",
  "install_steps": [
    "apt-get install pwdx 2>/dev/null || which pwdx",
    "Verify: pwdx --version",
    "supercli plugins install ./plugins/pwdx --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}