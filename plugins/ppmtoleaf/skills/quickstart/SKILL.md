---
name: ppmtoleaf
description: Use this skill when the user wants to use ppmtoleaf, cli tool: ppmtoleaf.
---

# ppmtoleaf Plugin

CLI tool: ppmtoleaf.

## Commands
- `ppmtoleaf <resource> <action>` — Execute ppmtoleaf commands
- `ppmtoleaf self version` — Print ppmtoleaf version
- `ppmtoleaf _ _` — Passthrough to ppmtoleaf CLI

## Usage Examples
- "ppmtoleaf --help"
- "ppmtoleaf self version"

## Installation
```bash
apt-get install ppmtoleaf 2>/dev/null || which ppmtoleaf
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
