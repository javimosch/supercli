{
  "plugin": "which.debianutils",
  "binary": "which.debianutils",
  "check": "which which.debianutils",
  "install_steps": [
    "apt-get install which.debianutils 2>/dev/null || which which.debianutils",
    "Verify: which.debianutils --version",
    "supercli plugins install ./plugins/which.debianutils --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}