{
  "plugin": "brotli",
  "binary": "brotli",
  "check": "which brotli",
  "install_steps": [
    "brew install brotli",
    "Verify: brotli --version",
    "supercli plugins install ./plugins/brotli --on-conflict replace --json"
  ]
}
