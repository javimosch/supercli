{
  "plugin": "kool",
  "binary": "kool",
  "check": "which kool",
  "install_steps": ["brew install kool", "Verify: kool --version", "supercli plugins install ./plugins/kool --on-conflict replace --json"]
}
