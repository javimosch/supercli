{
  "plugin": "unicode_start",
  "binary": "unicode_start",
  "check": "which unicode_start",
  "install_steps": [
    "apt-get install unicode_start 2>/dev/null || which unicode_start",
    "Verify: unicode_start --version",
    "supercli plugins install ./plugins/unicode_start --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}