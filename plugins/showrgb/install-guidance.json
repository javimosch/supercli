{
  "plugin": "showrgb",
  "binary": "showrgb",
  "check": "which showrgb",
  "install_steps": [
    "apt-get install showrgb 2>/dev/null || which showrgb",
    "Verify: showrgb --version",
    "supercli plugins install ./plugins/showrgb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}