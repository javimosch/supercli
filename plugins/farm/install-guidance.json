{
  "plugin": "farm",
  "binary": "farm",
  "check": "which farm",
  "install_steps": ["npm install -g @farmfe/cli", "Verify: farm --version", "supercli plugins install ./plugins/farm --on-conflict replace --json"]
}
