{
  "plugin": "clever",
  "binary": "clever",
  "check": "which clever",
  "install_steps": [
    "brew install clever",
    "Verify: clever --version",
    "supercli plugins install ./plugins/clever --on-conflict replace --json"
  ]
}