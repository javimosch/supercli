{
  "plugin": "pvmove",
  "binary": "pvmove",
  "check": "which pvmove",
  "install_steps": [
    "apt-get install pvmove 2>/dev/null || which pvmove",
    "Verify: pvmove --version",
    "supercli plugins install ./plugins/pvmove --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}