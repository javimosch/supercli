{
  "plugin": "ngrok",
  "binary": "ngrok",
  "check": "which ngrok",
  "install_steps": [
    "brew install ngrok",
    "Verify: ngrok --version",
    "supercli plugins install ./plugins/ngrok --on-conflict replace --json"
  ]
}
