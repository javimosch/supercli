---
name: gpgparsemail
description: Use this skill when the user wants to use gpgparsemail, cli tool: gpgparsemail.
---

# gpgparsemail Plugin

CLI tool: gpgparsemail.

## Commands
- `gpgparsemail <resource> <action>` — Execute gpgparsemail commands
- `gpgparsemail self version` — Print gpgparsemail version
- `gpgparsemail _ _` — Passthrough to gpgparsemail CLI

## Usage Examples
- "gpgparsemail --help"
- "gpgparsemail self version"

## Installation
```bash
apt-get install gpgparsemail 2>/dev/null || which gpgparsemail
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
