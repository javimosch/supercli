{
  "plugin": "apicrafter",
  "binary": "apicrafter",
  "check": "which apicrafter",
  "install_steps": ["npm install -g apicrafter", "Verify: apicrafter --version", "supercli plugins install ./plugins/apicrafter --on-conflict replace --json"]
}
