{
  "plugin": "union",
  "binary": "union",
  "check": "which union",
  "install_steps": [
    "pip install union",
    "Verify: union --version",
    "supercli plugins install ./plugins/union --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}