{
  "plugin": "rls",
  "binary": "rls",
  "check": "which rls",
  "install_steps": [
    "apt-get install rls 2>/dev/null || which rls",
    "Verify: rls --version",
    "supercli plugins install ./plugins/rls --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}