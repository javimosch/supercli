{
  "plugin": "arduino-cli",
  "binary": "arduino-cli",
  "check": "which arduino-cli",
  "install_steps": [
    "brew install arduino-cli",
    "Verify: arduino-cli version",
    "supercli plugins install ./plugins/arduino-cli --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Command line tool for managing Arduino boards, compiling sketches, and uploading to hardware. Supports all Arduino-compatible boards."
}
