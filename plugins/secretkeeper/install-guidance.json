{
  "plugin": "secretkeeper",
  "binary": "secretkeeper",
  "check": "which secretkeeper",
  "install_steps": [
    "cargo install secretkeeper",
    "Verify: secretkeeper --version"
  ],
  "note": "Protect and manage secrets within text content. Useful for embedding sensitive data safely in documents and configurations."
}