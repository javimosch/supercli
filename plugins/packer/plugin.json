{
  "name": "packer",
  "version": "0.1.0",
  "description": "Packer — HashiCorp image builder for creating machine images",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "packer"
    }
  ],
  "install_guidance": {
    "plugin": "packer",
    "binary": "packer",
    "check": "which packer",
    "install_steps": [
      "brew install packer"
    ],
    "note": "Development tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "packer",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to packer CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "packer",
        "passthrough": true,
        "missingDependencyHelp": "Install packer: brew install packer"
      },
      "args": []
    }
  ]
}
