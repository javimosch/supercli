{
  "plugin": "sentry-cli",
  "binary": "sentry-cli",
  "check": "which sentry-cli",
  "install_steps": [
    "brew install sentry-cli",
    "Verify: sentry-cli --version",
    "supercli plugins install ./plugins/sentry-cli --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/getsentry/sentry-cli/cmd/sentry-cli@latest. Official Sentry CLI tool. Manage Sentry projects, releases, upload source maps, and debug files. Requires Sentry authentication token."
}
