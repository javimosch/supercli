{
  "plugin": "postqueue",
  "binary": "postqueue",
  "check": "which postqueue",
  "install_steps": [
    "# Install postqueue",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postqueue-linux-amd64 -o /usr/local/bin/postqueue",
    "chmod +x /usr/local/bin/postqueue",
    "# Or via package manager, see /usr/sbin/postqueue"
  ]
}