{
  "plugin": "wego",
  "binary": "wego",
  "check": "which wego",
  "install_steps": [
    "brew install wego",
    "Verify: wego --version",
    "supercli plugins install ./plugins/wego --on-conflict replace --json"
  ]
}
