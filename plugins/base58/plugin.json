{
  "name": "base58",
  "version": "0.1.0",
  "description": "CLI tool: base58",
  "source": "https://packages.debian.org/base58",
  "checks": [{ "type": "binary", "name": "base58" }],
  "install_guidance": {
    "plugin": "base58", "binary": "base58", "check": "which base58",
    "install_steps": ["apt-get install base58 2>/dev/null || which base58", "Verify: base58 --version", "supercli plugins install ./plugins/base58 --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "base58", "resource": "_", "action": "_",
    "description": "Passthrough to base58 CLI",
    "adapter": "process", "adapterConfig": { "command": "base58", "passthrough": true, "missingDependencyHelp": "Install base58: apt-get install base58" }, "args": []
  }]
}
