{ "plugin": "base58", "binary": "base58", "check": "which base58", "install_steps": ["apt-get install base58 2>/dev/null || which base58", "Verify: base58 --version", "supercli plugins install ./plugins/base58 --on-conflict replace --json"] }
