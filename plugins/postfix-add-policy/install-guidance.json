{
  "plugin": "postfix-add-policy",
  "binary": "postfix-add-policy",
  "check": "which postfix-add-policy",
  "install_steps": [
    "# Install postfix-add-policy",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postfix-add-policy-linux-amd64 -o /usr/local/bin/postfix-add-policy",
    "chmod +x /usr/local/bin/postfix-add-policy",
    "# Or via package manager, see /usr/sbin/postfix-add-policy"
  ]
}