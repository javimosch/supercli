{
  "plugin": "kpt",
  "binary": "kpt",
  "check": "which kpt",
  "install_steps": [
    "brew install kpt",
    "Verify: kpt version",
    "supercli plugins install ./plugins/kpt --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Google's tool for managing Kubernetes packages. Package, customize, and apply Kubernetes configurations. Uses Kptfile for package metadata."
}
