{
  "plugin": "etex",
  "binary": "etex",
  "check": "which etex",
  "install_steps": [
    "apt-get install etex 2>/dev/null || which etex",
    "Verify: etex --version",
    "supercli plugins install ./plugins/etex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}