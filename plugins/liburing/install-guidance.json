{
  "plugin": "liburing",
  "binary": "liburing",
  "check": "which liburing",
  "install_steps": [
    "brew install liburing",
    "Verify: liburing --version",
    "supercli plugins install ./plugins/liburing --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}