{
  "plugin": "readelf",
  "binary": "readelf",
  "check": "which readelf",
  "install_steps": [
    "apt install binutils (Debian/Ubuntu)",
    "yum install binutils (RHEL/Fedora)",
    "Pre-installed on most Linux distributions",
    "supercli plugins install ./plugins/readelf --on-conflict replace --json"
  ],
  "note": "Part of GNU Binutils, pre-installed on most Linux systems. Essential tool for examining ELF binaries and shared libraries."
}
