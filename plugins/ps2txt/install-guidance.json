{
  "plugin": "ps2txt",
  "binary": "ps2txt",
  "check": "which ps2txt",
  "install_steps": [
    "apt-get install ps2txt 2>/dev/null || which ps2txt",
    "Verify: ps2txt --version",
    "supercli plugins install ./plugins/ps2txt --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}