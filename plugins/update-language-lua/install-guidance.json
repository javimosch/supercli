{
  "plugin": "update-language-lua",
  "binary": "update-language-lua",
  "check": "which update-language-lua",
  "install_steps": [
    "apt-get install update-language-lua 2>/dev/null || which update-language-lua",
    "Verify: update-language-lua --version",
    "supercli plugins install ./plugins/update-language-lua --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}