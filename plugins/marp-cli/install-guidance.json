{
  "plugin": "marp-cli",
  "binary": "marp",
  "check": "which marp",
  "install_steps": [
    "npm install -g @marp-team/marp-cli",
    "Verify: marp --version",
    "supercli plugins install ./plugins/marp-cli --on-conflict replace --json"
  ],
  "note": "Create presentations from Markdown using Marp framework."
}
