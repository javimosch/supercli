{
  "plugin": "hp-pkservice",
  "binary": "hp-pkservice",
  "check": "which hp-pkservice",
  "install_steps": [
    "apt-get install hp-pkservice 2>/dev/null || which hp-pkservice",
    "Verify: hp-pkservice --version",
    "supercli plugins install ./plugins/hp-pkservice --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}