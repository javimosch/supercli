{
  "plugin": "codecount",
  "binary": "codecount",
  "check": "which codecount",
  "install_steps": ["pip install codecount", "Verify: codecount --version", "supercli plugins install ./plugins/codecount --on-conflict replace --json"]
}
