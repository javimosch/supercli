{
  "plugin": "journalctl",
  "binary": "journalctl",
  "check": "which journalctl",
  "install_steps": [
    "journalctl is included with systemd — install systemd if needed",
    "apt install systemd",
    "Verify: journalctl --version"
  ],
  "note": "journalctl is part of systemd and is available on most modern Linux distributions. No separate installation is needed if systemd is present."
}
