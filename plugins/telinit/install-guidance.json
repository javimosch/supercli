{
  "plugin": "telinit",
  "binary": "telinit",
  "check": "which telinit",
  "install_steps": [
    "apt-get install telinit 2>/dev/null || which telinit",
    "Verify: telinit --version",
    "supercli plugins install ./plugins/telinit --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}