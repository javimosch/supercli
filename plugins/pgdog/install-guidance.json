{
  "plugin": "pgdog",
  "binary": "pgdog",
  "check": "which pgdog",
  "install_steps": [
    "pip install pgdog",
    "Verify: pgdog --version",
    "supercli plugins install ./plugins/pgdog --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}