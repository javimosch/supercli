{
  "plugin": "openpolicy",
  "binary": "openpolicy",
  "check": "which openpolicy",
  "install_steps": [
    "npm install -g openpolicy",
    "Verify: openpolicy --version",
    "supercli plugins install ./plugins/openpolicy --on-conflict replace --json"
  ]
}