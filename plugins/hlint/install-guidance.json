{
  "plugin": "hlint",
  "binary": "hlint",
  "check": "which hlint",
  "install_steps": [
    "# Install hlint",
    "# See github.com/ndmitchell/hlint",
    "# apt: sudo apt-get install hlint",
    "# brew: brew install hlint",
    "# cargo: cargo install hlint",
    "# npm: npm install -g hlint"
  ]
}