{
  "plugin": "padsp",
  "binary": "padsp",
  "check": "which padsp",
  "install_steps": [
    "apt-get install padsp 2>/dev/null || which padsp",
    "Verify: padsp --version",
    "supercli plugins install ./plugins/padsp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}