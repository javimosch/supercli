{
  "plugin": "update-notifier",
  "binary": "update-notifier",
  "check": "which update-notifier",
  "install_steps": [
    "apt-get install update-notifier 2>/dev/null || which update-notifier",
    "Verify: update-notifier --version",
    "supercli plugins install ./plugins/update-notifier --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}