{
  "plugin": "git-semver",
  "binary": "git-semver",
  "check": "which git-semver",
  "install_steps": [
    "go install github.com/git-semver/git-semver@latest",
    "Verify: git-semver --version",
    "supercli plugins install ./plugins/git-semver --on-conflict replace --json"
  ],
  "note": "Binary name: git-semver"
}
