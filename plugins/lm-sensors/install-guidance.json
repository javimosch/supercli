{
  "plugin": "lm-sensors",
  "binary": "sensors",
  "check": "which sensors",
  "install_steps": [
    "brew install lm-sensors",
    "Verify: sensors --version",
    "supercli plugins install ./plugins/lm-sensors --on-conflict replace --json"
  ]
}
