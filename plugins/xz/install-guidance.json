{
  "plugin": "xz",
  "binary": "xz",
  "check": "which xz",
  "install_steps": [
    "brew install xz",
    "Verify: xz --version",
    "supercli plugins install ./plugins/xz --on-conflict replace --json"
  ]
}
