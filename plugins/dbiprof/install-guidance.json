{ "plugin": "dbiprof", "binary": "dbiprof", "check": "which dbiprof", "install_steps": ["apt-get install dbiprof 2>/dev/null || which dbiprof", "Verify: dbiprof --version", "supercli plugins install ./plugins/dbiprof --on-conflict replace --json"] }
