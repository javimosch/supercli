{
  "name": "dbiprof",
  "version": "0.1.0",
  "description": "CLI tool: dbiprof",
  "source": "https://packages.debian.org/dbiprof",
  "checks": [{ "type": "binary", "name": "dbiprof" }],
  "install_guidance": {
    "plugin": "dbiprof", "binary": "dbiprof", "check": "which dbiprof",
    "install_steps": ["apt-get install dbiprof 2>/dev/null || which dbiprof", "Verify: dbiprof --version", "supercli plugins install ./plugins/dbiprof --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbiprof", "resource": "_", "action": "_",
    "description": "Passthrough to dbiprof CLI",
    "adapter": "process", "adapterConfig": { "command": "dbiprof", "passthrough": true, "missingDependencyHelp": "Install dbiprof: apt-get install dbiprof" }, "args": []
  }]
}
