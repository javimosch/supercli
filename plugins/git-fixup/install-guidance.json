{
  "plugin": "git-fixup",
  "binary": "git-fixup",
  "check": "which git-fixup",
  "install_steps": [
    "brew install git-fixup",
    "Verify: git-fixup --version",
    "supercli plugins install ./plugins/git-fixup --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}