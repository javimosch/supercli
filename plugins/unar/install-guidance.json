{
  "plugin": "unar",
  "binary": "unar",
  "check": "which unar",
  "install_steps": [
    "brew install unar",
    "Verify: unar --version",
    "supercli plugins install ./plugins/unar --on-conflict replace --json"
  ],
  "note": "Pre-built binaries available at https://theunarchiver.com/command-line. Also available via most Linux package managers (apt, dnf, etc.). Supports password-protected archives and extracting specific files."
}
