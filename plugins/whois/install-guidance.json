{
  "plugin": "whois",
  "binary": "whois",
  "check": "which whois",
  "install_steps": [
    "brew install whois",
    "Verify: whois --version",
    "supercli plugins install ./plugins/whois --on-conflict replace --json"
  ]
}
