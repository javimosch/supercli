{
  "name": "domainname",
  "version": "1.0.0",
  "description": "domainname \u2014 domainname \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "domainname"
    }
  ],
  "install_guidance": {
    "plugin": "domainname",
    "binary": "domainname",
    "check": "which domainname",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "domainname",
      "resource": "run",
      "action": "exec",
      "description": "domainname \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "domainname",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}