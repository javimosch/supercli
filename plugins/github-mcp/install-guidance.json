{
  "plugin": "github-mcp",
  "binary": "npx",
  "check": "npx --yes @github/github-mcp-server --help 2>&1 | head -3",
  "install_steps": [
    "Register MCP: sc github-mcp self mcp",
    "Requires npx (comes with Node.js/npm)",
    "Set GITHUB_TOKEN env var in ~/.supercli/mcp.json or your shell"
  ],
  "note": "Official GitHub MCP server. MIT license."
}
