{
  "plugin": "netop",
  "binary": "netop",
  "check": "which netop",
  "install_steps": [
    "cargo install netop",
    "Verify: netop --version",
    "supercli plugins install ./plugins/netop --on-conflict replace --json"
  ]
}