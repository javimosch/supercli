{
  "plugin": "ghi",
  "binary": "ghi",
  "check": "which ghi",
  "install_steps": [
    "brew install ghi",
    "Verify: ghi --version",
    "supercli plugins install ./plugins/ghi --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}