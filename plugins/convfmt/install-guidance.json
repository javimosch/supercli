{
  "plugin": "convfmt",
  "binary": "convfmt",
  "check": "which convfmt",
  "install_steps": [
    "cargo install convfmt",
    "Verify: convfmt --version"
  ],
  "note": "Converts data between JSON, YAML, TOML and other structured data formats. Useful for config migration and format standardization."
}