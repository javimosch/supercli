{
  "name": "npx",
  "version": "0.1.0",
  "description": "npx — Node.js package runner for executing npm packages",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "npx"
    }
  ],
  "install_guidance": {
    "plugin": "npx",
    "binary": "npx",
    "check": "which npx",
    "install_steps": [
      "brew install node"
    ],
    "note": "Development tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "npx",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to npx CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "npx",
        "passthrough": true,
        "missingDependencyHelp": "Install npx: brew install node"
      },
      "args": []
    }
  ]
}
