{
  "plugin": "git-xargs",
  "binary": "git-xargs",
  "check": "which git-xargs",
  "install_steps": [
    "brew install git-xargs",
    "Verify: git-xargs --version",
    "supercli plugins install ./plugins/git-xargs --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/gruntwork-io/git-xargs/cmd/git-xargs@latest. Run Git commands across multiple repositories. Execute Git operations, create branches, and make commits across many repos at once. Great for bulk operations."
}
