{
  "plugin": "gemini",
  "binary": "gemini",
  "check": "which gemini",
  "install_steps": [
    "brew install gemini",
    "Verify: gemini --version",
    "supercli plugins install ./plugins/gemini --on-conflict replace --json"
  ]
}