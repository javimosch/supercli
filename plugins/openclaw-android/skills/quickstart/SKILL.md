---
name: openclaw-android
description: Use this skill when the user wants to use openclaw-android for Run OpenClaw on Android with a single command — no proot, no Linux.
---

# openclaw-android Plugin

Run OpenClaw on Android with a single command — no proot, no Linux

## Commands

### General
- `openclaw-android self version` — Print openclaw-android version
- `openclaw-android _ _` — Passthrough to openclaw-android CLI

## Usage Examples
- "Run openclaw-android"
- "Use openclaw-android to Run OpenClaw on Android with a single command — no proot, no"

## Installation

```bash
pip install openclaw-android
```

## Key Features
- Run OpenClaw on Android with a single command — no proot, no Linux
- CLI tool, headless and scriptable
