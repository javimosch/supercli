{
  "plugin": "litt",
  "binary": "litt",
  "check": "which litt",
  "install_steps": [
    "pip install litt",
    "Verify: litt --version",
    "supercli plugins install ./plugins/litt --on-conflict replace --json"
  ]
}
