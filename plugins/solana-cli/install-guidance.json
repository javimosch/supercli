{
  "plugin": "solana-cli",
  "binary": "solana",
  "check": "which solana",
  "install_steps": [
    "sh -c """,
    "Verify: solana --version",
    "supercli plugins install ./plugins/solana-cli --on-conflict replace --json"
  ]
}
