{
  "plugin": "driftctl",
  "binary": "driftctl",
  "check": "which driftctl",
  "install_steps": [
    "brew install driftctl",
    "Verify: driftctl --version",
    "supercli plugins install ./plugins/driftctl --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}