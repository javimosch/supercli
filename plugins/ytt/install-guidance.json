{
  "plugin": "ytt",
  "binary": "ytt",
  "check": "which ytt",
  "install_steps": [
    "brew install ytt",
    "Verify: ytt --version",
    "supercli plugins install ./plugins/ytt --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Part of the Carvel suite. Works on YAML structure instead of text for accurate templating. Supports overlays for patching configurations. Great for Kubernetes configurations."
}
