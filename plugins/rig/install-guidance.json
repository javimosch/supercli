{
  "plugin": "rig",
  "binary": "rig",
  "check": "which rig",
  "install_steps": [
    "brew install rig",
    "Verify: rig --version",
    "supercli plugins install ./plugins/rig --on-conflict replace --json"
  ]
}
