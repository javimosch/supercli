{
  "plugin": "tombi",
  "binary": "tombi",
  "check": "which tombi",
  "install_steps": [
    "brew install tombi",
    "Verify: tombi --version",
    "supercli plugins install ./plugins/tombi --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install tombi. TOML formatter, linter, and language server. Validates and formats TOML configuration files. Includes language server support for editor integration."
}
