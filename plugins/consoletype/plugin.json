{
  "name": "consoletype",
  "version": "0.1.0",
  "description": "CLI tool: consoletype",
  "source": "https://packages.debian.org/consoletype",
  "checks": [{ "type": "binary", "name": "consoletype" }],
  "install_guidance": {
    "plugin": "consoletype", "binary": "consoletype", "check": "which consoletype",
    "install_steps": ["apt-get install consoletype 2>/dev/null || which consoletype", "Verify: consoletype --version", "supercli plugins install ./plugins/consoletype --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "consoletype", "resource": "_", "action": "_",
    "description": "Passthrough to consoletype CLI",
    "adapter": "process", "adapterConfig": { "command": "consoletype", "passthrough": true, "missingDependencyHelp": "Install consoletype: apt-get install consoletype" }, "args": []
  }]
}
