{ "plugin": "consoletype", "binary": "consoletype", "check": "which consoletype", "install_steps": ["apt-get install consoletype 2>/dev/null || which consoletype", "Verify: consoletype --version", "supercli plugins install ./plugins/consoletype --on-conflict replace --json"] }
