{"plugin":"kubeless","binary":"kubeless","check":"which $BINARY","install_steps":["curl -LO https://github.com/kubeless/kubeless/releases/latest/download/kubeless_linux-amd64.zip && unzip kubeless_linux-amd64.zip && sudo mv kubeless /usr/local/bin/ && rm kubeless_linux-amd64.zip"]}
