{
  "plugin": "ctest",
  "binary": "ctest",
  "check": "which ctest",
  "install_steps": [
    "apt-get install ctest 2>/dev/null || which ctest",
    "Verify: ctest --version",
    "supercli plugins install ./plugins/ctest --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}