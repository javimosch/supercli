{
  "plugin": "qdbus",
  "binary": "qdbus",
  "check": "which qdbus",
  "install_steps": [
    "apt-get install qdbus 2>/dev/null || which qdbus",
    "Verify: qdbus --version",
    "supercli plugins install ./plugins/qdbus --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}