{
  "plugin": "git-hub",
  "binary": "git-hub",
  "check": "which git-hub",
  "install_steps": [
    "pip install git-hub",
    "Verify: git-hub --version",
    "supercli plugins install ./plugins/git-hub --on-conflict replace --json"
  ]
}