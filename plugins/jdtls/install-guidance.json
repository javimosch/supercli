{
  "plugin": "jdtls",
  "binary": "jdtls",
  "check": "which jdtls",
  "install_steps": [
    "brew install jdtls",
    "Verify: jdtls --version",
    "supercli plugins install ./plugins/jdtls --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}