{
  "name": "grub-bios-setup",
  "version": "0.1.0",
  "description": "CLI tool: grub-bios-setup",
  "source": "https://manpages.debian.org/grub-bios-setup",
  "checks": [
    {
      "type": "binary",
      "name": "grub-bios-setup"
    }
  ],
  "install_guidance": {
    "plugin": "grub-bios-setup",
    "binary": "grub-bios-setup",
    "check": "which grub-bios-setup",
    "install_steps": [
      "apt-get install grub-bios-setup 2>/dev/null || which grub-bios-setup",
      "Verify: grub-bios-setup --version",
      "supercli plugins install ./plugins/grub-bios-setup --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-bios-setup",
      "resource": "self",
      "action": "version",
      "description": "Print grub-bios-setup version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-bios-setup",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-bios-setup: apt-get install grub-bios-setup 2>/dev/null || which grub-bios-setup"
      },
      "args": []
    },
    {
      "namespace": "grub-bios-setup",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-bios-setup CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-bios-setup",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-bios-setup: apt-get install grub-bios-setup 2>/dev/null || which grub-bios-setup"
      },
      "args": []
    }
  ]
}