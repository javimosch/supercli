{
  "plugin": "zegrep",
  "binary": "zegrep",
  "check": "which zegrep",
  "install_steps": [
    "apt-get install zegrep 2>/dev/null || which zegrep",
    "Verify: zegrep --version",
    "supercli plugins install ./plugins/zegrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}