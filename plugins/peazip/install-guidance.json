{
  "plugin": "peazip",
  "binary": "peazip",
  "check": "which peazip",
  "install_steps": [
    "brew install peazip",
    "Verify: peazip --version",
    "supercli plugins install ./plugins/peazip --on-conflict replace --json"
  ]
}
