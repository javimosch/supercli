{
  "plugin": "velero",
  "binary": "velero",
  "check": "which velero",
  "install_steps": ["brew install velero", "Verify: velero --version", "supercli plugins install ./plugins/velero --on-conflict replace --json"]
}
