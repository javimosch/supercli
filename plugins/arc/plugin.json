{
  "name": "arc",
  "version": "0.1.0",
  "description": "CLI tool: arc",
  "source": "https://packages.debian.org/arc",
  "checks": [{ "type": "binary", "name": "arc" }],
  "install_guidance": {
    "plugin": "arc", "binary": "arc", "check": "which arc",
    "install_steps": ["apt-get install arc 2>/dev/null || which arc", "Verify: arc --version", "supercli plugins install ./plugins/arc --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "arc", "resource": "_", "action": "_",
    "description": "Passthrough to arc CLI",
    "adapter": "process", "adapterConfig": { "command": "arc", "passthrough": true, "missingDependencyHelp": "Install arc: apt-get install arc" }, "args": []
  }]
}
