{ "plugin": "arc", "binary": "arc", "check": "which arc", "install_steps": ["apt-get install arc 2>/dev/null || which arc", "Verify: arc --version", "supercli plugins install ./plugins/arc --on-conflict replace --json"] }
