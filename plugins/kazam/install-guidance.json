{
  "plugin": "kazam",
  "binary": "kazam",
  "check": "which kazam",
  "install_steps": [
    "apt-get install kazam 2>/dev/null || which kazam",
    "Verify: kazam --version",
    "supercli plugins install ./plugins/kazam --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}