{
  "plugin": "sc",
  "binary": "sc",
  "check": "which sc",
  "install_steps": [
    "# Install sc",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/sc-linux-amd64 -o /usr/local/bin/sc",
    "chmod +x /usr/local/bin/sc",
    "# Or via package manager, see /usr/bin/sc"
  ]
}