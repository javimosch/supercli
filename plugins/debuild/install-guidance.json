{ "plugin": "debuild", "binary": "debuild", "check": "which debuild", "install_steps": ["apt-get install debuild 2>/dev/null || which debuild", "Verify: debuild --version", "supercli plugins install ./plugins/debuild --on-conflict replace --json"] }
