{
  "name": "debuild",
  "version": "0.1.0",
  "description": "CLI tool: debuild",
  "source": "https://packages.debian.org/debuild",
  "checks": [{ "type": "binary", "name": "debuild" }],
  "install_guidance": {
    "plugin": "debuild", "binary": "debuild", "check": "which debuild",
    "install_steps": ["apt-get install debuild 2>/dev/null || which debuild", "Verify: debuild --version", "supercli plugins install ./plugins/debuild --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "debuild", "resource": "_", "action": "_",
    "description": "Passthrough to debuild CLI",
    "adapter": "process", "adapterConfig": { "command": "debuild", "passthrough": true, "missingDependencyHelp": "Install debuild: apt-get install debuild" }, "args": []
  }]
}
