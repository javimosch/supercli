{
  "plugin": "cpx",
  "binary": "cpx",
  "check": "which cpx",
  "install_steps": [
    "npm install -g cpx",
    "Verify: cpx --version",
    "supercli plugins install ./plugins/cpx --on-conflict replace --json"
  ]
}