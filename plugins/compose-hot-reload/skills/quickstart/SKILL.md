---
name: compose-hot-reload
description: Use this skill when the user wants to use compose-hot-reload for Compose Hot Reload: Make changes to your UI code in a Compose Multiplatform appl.
---

# compose-hot-reload Plugin

Compose Hot Reload: Make changes to your UI code in a Compose Multiplatform application, and see the results in real time. No restarts required. Compose Hot Reload runs your application on the JetBrains Runtime and intelligently reloads your code whenever it is changed.

## Commands

### General
- `compose-hot-reload self version` — Print compose-hot-reload version
- `compose-hot-reload _ _` — Passthrough to compose-hot-reload CLI

## Usage Examples
- "Run compose-hot-reload"
- "Use compose-hot-reload to Compose Hot Reload: Make changes to your UI code in a Compos"

## Installation

```bash
pip install compose-hot-reload
```

## Key Features
- Compose Hot Reload: Make changes to your UI code in a Compose Multiplatform application, and see the
- CLI tool, headless and scriptable
