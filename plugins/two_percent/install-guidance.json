{
  "plugin": "two_percent",
  "binary": "two_percent",
  "check": "which two_percent",
  "install_steps": ["cargo install two_percent", "Verify: two_percent --version", "supercli plugins install ./plugins/two_percent --on-conflict replace --json"]
}
