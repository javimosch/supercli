{
  "plugin": "mdbook-katex",
  "binary": "mdbook-katex",
  "check": "which mdbook-katex",
  "install_steps": ["cargo install mdbook-katex", "Verify: mdbook-katex --version", "supercli plugins install ./plugins/mdbook-katex --on-conflict replace --json"]
}
