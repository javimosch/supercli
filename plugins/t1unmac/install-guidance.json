{
  "plugin": "t1unmac",
  "binary": "t1unmac",
  "check": "which t1unmac",
  "install_steps": [
    "apt-get install t1unmac 2>/dev/null || which t1unmac",
    "Verify: t1unmac --version",
    "supercli plugins install ./plugins/t1unmac --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}