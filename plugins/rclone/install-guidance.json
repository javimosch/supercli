{
  "plugin": "rclone",
  "binary": "rclone",
  "check": "which rclone",
  "install_steps": [
    "curl https://rclone.org/install.sh | sudo bash",
    "Or: brew install rclone",
    "Verify: rclone version",
    "supercli plugins install ./plugins/rclone --on-conflict replace --json"
  ],
  "note": "Rclone supports 70+ storage providers. Configure remotes with 'rclone config'. Use 'rclone sync' to make destination identical to source."
}
