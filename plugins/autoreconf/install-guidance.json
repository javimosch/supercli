{ "plugin": "autoreconf", "binary": "autoreconf", "check": "which autoreconf", "install_steps": ["apt-get install autoreconf 2>/dev/null || which autoreconf", "Verify: autoreconf --version", "supercli plugins install ./plugins/autoreconf --on-conflict replace --json"] }
