{ "plugin": "dm-log", "binary": "dm-log", "check": "which dm-log", "install_steps": ["apt-get install dm-log 2>/dev/null || which dm-log", "Verify: dm-log --version", "supercli plugins install ./plugins/dm-log --on-conflict replace --json"] }
