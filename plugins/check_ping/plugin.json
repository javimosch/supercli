{
  "name": "check_ping",
  "version": "0.1.0",
  "description": "CLI tool: check_ping",
  "source": "https://packages.debian.org/check_ping",
  "checks": [{ "type": "binary", "name": "check_ping" }],
  "install_guidance": {
    "plugin": "check_ping", "binary": "check_ping", "check": "which check_ping",
    "install_steps": ["apt-get install check_ping 2>/dev/null || which check_ping", "Verify: check_ping --version", "supercli plugins install ./plugins/check_ping --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check_ping", "resource": "_", "action": "_",
    "description": "Passthrough to check_ping CLI",
    "adapter": "process", "adapterConfig": { "command": "check_ping", "passthrough": true, "missingDependencyHelp": "Install check_ping: apt-get install check_ping" }, "args": []
  }]
}
