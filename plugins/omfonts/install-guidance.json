{
  "plugin": "omfonts",
  "binary": "omfonts",
  "check": "which omfonts",
  "install_steps": [
    "apt-get install omfonts 2>/dev/null || which omfonts",
    "Verify: omfonts --version",
    "supercli plugins install ./plugins/omfonts --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}