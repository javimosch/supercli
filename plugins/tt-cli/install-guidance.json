{
  "plugin": "tt-cli",
  "binary": "tt-cli",
  "check": "which tt-cli",
  "install_steps": ["go install github.com/tt-cli@latest", "Verify: tt-cli --version", "supercli plugins install ./plugins/tt-cli --on-conflict replace --json"]
}
