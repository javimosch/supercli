{ "plugin": "bijiben", "binary": "bijiben", "check": "which bijiben", "install_steps": ["apt-get install bijiben 2>/dev/null || which bijiben", "Verify: bijiben --version", "supercli plugins install ./plugins/bijiben --on-conflict replace --json"] }
