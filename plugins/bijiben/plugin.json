{
  "name": "bijiben",
  "version": "0.1.0",
  "description": "CLI tool: bijiben",
  "source": "https://packages.debian.org/bijiben",
  "checks": [{ "type": "binary", "name": "bijiben" }],
  "install_guidance": {
    "plugin": "bijiben", "binary": "bijiben", "check": "which bijiben",
    "install_steps": ["apt-get install bijiben 2>/dev/null || which bijiben", "Verify: bijiben --version", "supercli plugins install ./plugins/bijiben --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "bijiben", "resource": "_", "action": "_",
    "description": "Passthrough to bijiben CLI",
    "adapter": "process", "adapterConfig": { "command": "bijiben", "passthrough": true, "missingDependencyHelp": "Install bijiben: apt-get install bijiben" }, "args": []
  }]
}
