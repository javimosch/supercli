{
  "plugin": "resend",
  "binary": "resend",
  "check": "which resend",
  "install_steps": [
    "brew install resend",
    "Verify: resend --version",
    "supercli plugins install ./plugins/resend --on-conflict replace --json"
  ]
}