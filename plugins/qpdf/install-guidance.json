{
  "plugin": "qpdf",
  "binary": "qpdf",
  "check": "which qpdf",
  "install_steps": [
    "brew install qpdf",
    "Verify: qpdf --version",
    "supercli plugins install ./plugins/qpdf --on-conflict replace --json"
  ],
  "note": "Also available via: apt install qpdf, dnf install qpdf. Uses --json flag for machine-readable PDF metadata. Supports password-protected PDFs (up to 256-bit AES encryption)."
}
