{
  "name": "charmap",
  "version": "0.1.0",
  "description": "CLI tool: charmap",
  "source": "https://packages.debian.org/charmap",
  "checks": [{ "type": "binary", "name": "charmap" }],
  "install_guidance": {
    "plugin": "charmap", "binary": "charmap", "check": "which charmap",
    "install_steps": ["apt-get install charmap 2>/dev/null || which charmap", "Verify: charmap --version", "supercli plugins install ./plugins/charmap --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "charmap", "resource": "_", "action": "_",
    "description": "Passthrough to charmap CLI",
    "adapter": "process", "adapterConfig": { "command": "charmap", "passthrough": true, "missingDependencyHelp": "Install charmap: apt-get install charmap" }, "args": []
  }]
}
