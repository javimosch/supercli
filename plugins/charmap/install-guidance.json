{ "plugin": "charmap", "binary": "charmap", "check": "which charmap", "install_steps": ["apt-get install charmap 2>/dev/null || which charmap", "Verify: charmap --version", "supercli plugins install ./plugins/charmap --on-conflict replace --json"] }
