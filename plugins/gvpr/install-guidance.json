{
  "plugin": "gvpr",
  "binary": "gvpr",
  "check": "which gvpr",
  "install_steps": [
    "apt-get install gvpr 2>/dev/null || which gvpr",
    "Verify: gvpr --version",
    "supercli plugins install ./plugins/gvpr --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}