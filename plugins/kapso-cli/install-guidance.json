{
  "plugin": "kapso-cli",
  "binary": "kapso",
  "check": "which kapso",
  "install_steps": [
    "npm i -g @kapso/cli",
    "Verify: kapso --version",
    "supercli plugins install ./plugins/kapso-cli --on-conflict replace --json"
  ],
  "note": "Requires KAPSO_API_KEY env var. Get key at https://kapso.ai"
}
