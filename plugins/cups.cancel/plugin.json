{
  "name": "cups.cancel",
  "version": "0.1.0",
  "description": "CLI tool: cups.cancel",
  "source": "https://manpages.debian.org/cups.cancel",
  "checks": [
    {
      "type": "binary",
      "name": "cups.cancel"
    }
  ],
  "install_guidance": {
    "plugin": "cups.cancel",
    "binary": "cups.cancel",
    "check": "which cups.cancel",
    "install_steps": [
      "apt-get install cups.cancel 2>/dev/null || which cups.cancel",
      "Verify: cups.cancel --version",
      "supercli plugins install ./plugins/cups.cancel --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "cups.cancel",
      "resource": "self",
      "action": "version",
      "description": "Print cups.cancel version",
      "adapter": "process",
      "adapterConfig": {
        "command": "cups.cancel",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install cups.cancel: apt-get install cups.cancel 2>/dev/null || which cups.cancel"
      },
      "args": []
    },
    {
      "namespace": "cups.cancel",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to cups.cancel CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "cups.cancel",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install cups.cancel: apt-get install cups.cancel 2>/dev/null || which cups.cancel"
      },
      "args": []
    }
  ]
}