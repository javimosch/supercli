{
  "plugin": "reaver",
  "binary": "reaver",
  "check": "which reaver",
  "install_steps": [
    "brew install reaver",
    "Verify: reaver --version",
    "supercli plugins install ./plugins/reaver --on-conflict replace --json"
  ]
}
