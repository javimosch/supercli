{
  "plugin": "terraformer",
  "binary": "terraformer",
  "check": "which terraformer",
  "install_steps": [
    "brew install terraformer",
    "Verify: terraformer --version",
    "supercli plugins install ./plugins/terraformer --on-conflict replace --json"
  ],
  "note": "Also available via: apt, scoop, or download from GitHub releases. Requires provider credentials (AWS keys, GCP service account, etc.)."
}
