{
  "plugin": "sd",
  "binary": "sd",
  "check": "which sd",
  "install_steps": [
    "cargo install sd",
    "Verify: sd --version",
    "supercli plugins install ./plugins/sd --on-conflict replace --json"
  ],
  "note": "sed alternative with intuitive syntax. Uses JS/Python regex syntax. Supports cross-line matching with -A flag."
}