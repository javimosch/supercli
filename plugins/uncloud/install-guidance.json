{
  "plugin": "uncloud",
  "binary": "uncloud",
  "check": "which uncloud",
  "install_steps": [
    "pip install uncloud",
    "Verify: uncloud --version",
    "supercli plugins install ./plugins/uncloud --on-conflict replace --json"
  ],
  "note": "Language: Go"
}