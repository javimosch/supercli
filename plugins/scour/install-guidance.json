{
  "plugin": "scour",
  "binary": "scour",
  "check": "which scour",
  "install_steps": [
    "pip install scour",
    "Verify: scour --version",
    "supercli plugins install ./plugins/scour --on-conflict replace --json"
  ]
}
