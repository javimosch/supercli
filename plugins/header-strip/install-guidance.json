{
  "plugin": "header-strip",
  "binary": "header-strip",
  "check": "which header-strip",
  "install_steps": ["pip install header-strip", "Verify: header-strip --version", "supercli plugins install ./plugins/header-strip --on-conflict replace --json"]
}
