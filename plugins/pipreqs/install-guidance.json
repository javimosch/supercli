{
  "plugin": "pipreqs",
  "binary": "pipreqs",
  "check": "which pipreqs",
  "install_steps": ["pip install pipreqs", "Verify: pipreqs --version", "supercli plugins install ./plugins/pipreqs --on-conflict replace --json"]
}
