{
  "plugin": "kube-ps1",
  "binary": "kube-ps1",
  "check": "which kube-ps1",
  "install_steps": ["brew install kube-ps1", "Verify: kube-ps1 --version", "supercli plugins install ./plugins/kube-ps1 --on-conflict replace --json"]
}
