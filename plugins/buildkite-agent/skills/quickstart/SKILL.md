---
name: buildkite-agent
description: buildkite-agent — Buildkite CI agent. Run build jobs, upload artifacts, interact with Buildkite pipelines.
---
# buildkite-agent
buildkite-agent — Buildkite CI agent. Run build jobs, upload artifacts, interact with Buildkite pipelines.
