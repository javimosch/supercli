{
  "plugin": "pg_format",
  "binary": "pg_format",
  "check": "which pg_format",
  "install_steps": ["npm install -g pg-formatter", "Verify: pg_format --version", "supercli plugins install ./plugins/pg_format --on-conflict replace --json"]
}
