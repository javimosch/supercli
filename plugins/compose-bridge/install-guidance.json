{ "plugin": "compose-bridge", "binary": "compose-bridge", "check": "which compose-bridge", "install_steps": ["apt-get install compose-bridge 2>/dev/null || which compose-bridge", "Verify: compose-bridge --version", "supercli plugins install ./plugins/compose-bridge --on-conflict replace --json"] }
