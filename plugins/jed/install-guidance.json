{
  "plugin": "jed",
  "binary": "jed",
  "check": "which jed",
  "install_steps": [
    "brew install jed",
    "Verify: jed --version",
    "supercli plugins install ./plugins/jed --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}