{ "plugin": "animate", "binary": "animate", "check": "which animate", "install_steps": ["apt-get install animate 2>/dev/null || which animate", "Verify: animate --version", "supercli plugins install ./plugins/animate --on-conflict replace --json"] }
