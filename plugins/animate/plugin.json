{
  "name": "animate",
  "version": "0.1.0",
  "description": "CLI tool: animate",
  "source": "https://packages.debian.org/animate",
  "checks": [{ "type": "binary", "name": "animate" }],
  "install_guidance": {
    "plugin": "animate", "binary": "animate", "check": "which animate",
    "install_steps": ["apt-get install animate 2>/dev/null || which animate", "Verify: animate --version", "supercli plugins install ./plugins/animate --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "animate", "resource": "_", "action": "_",
    "description": "Passthrough to animate CLI",
    "adapter": "process", "adapterConfig": { "command": "animate", "passthrough": true, "missingDependencyHelp": "Install animate: apt-get install animate" }, "args": []
  }]
}
