{
  "plugin": "mf",
  "binary": "mf",
  "check": "which mf",
  "install_steps": [
    "apt-get install mf 2>/dev/null || which mf",
    "Verify: mf --version",
    "supercli plugins install ./plugins/mf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}