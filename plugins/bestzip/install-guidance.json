{
  "plugin": "bestzip",
  "binary": "bestzip",
  "check": "which bestzip",
  "install_steps": [
    "npm install -g bestzip",
    "Verify: bestzip --version",
    "supercli plugins install ./plugins/bestzip --on-conflict replace --json"
  ],
  "note": "Bestzip creates zip archives. Uses the system zip command if available for better performance, otherwise uses a pure Node.js implementation. Works with files and directories."
}
