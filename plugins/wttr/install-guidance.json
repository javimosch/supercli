{
  "plugin": "wttr",
  "binary": "wttr",
  "check": "which wttr",
  "install_steps": [
    "curl -s wttr.in > /dev/null && echo 'curl wttr.in for weather'",
    "Verify: wttr --version",
    "supercli plugins install ./plugins/wttr --on-conflict replace --json"
  ]
}
