{
  "plugin": "aws-nuke",
  "binary": "aws-nuke",
  "check": "which aws-nuke",
  "install_steps": [
    "brew install aws-nuke",
    "Verify: aws-nuke --version",
    "supercli plugins install ./plugins/aws-nuke --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Destructive tool that removes all resources from an AWS account. Always use --dry-run first. Requires AWS credentials. Useful for cleaning up test/sandbox accounts."
}
