{
  "plugin": "dstask",
  "binary": "dstask",
  "check": "which dstask",
  "install_steps": [
    "brew install dstask",
    "Verify: dstask --version",
    "supercli plugins install ./plugins/dstask --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/naggie/dstask/cmd/dstask@latest. A personal command-line todo manager with git-backed storage. Supports tags, contexts, priorities, and project organization."
}
