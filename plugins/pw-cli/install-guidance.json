{
  "plugin": "pw-cli",
  "binary": "pw-cli",
  "check": "which pw-cli",
  "install_steps": [
    "apt-get install pw-cli 2>/dev/null || which pw-cli",
    "Verify: pw-cli --version",
    "supercli plugins install ./plugins/pw-cli --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}