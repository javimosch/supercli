{
  "plugin": "ovh-cli",
  "binary": "ovh",
  "check": "which ovh",
  "install_steps": [
    "npm install -g @ovh-api/cli",
    "Verify: ovh --version",
    "supercli plugins install ./plugins/ovh-cli --on-conflict replace --json"
  ]
}
