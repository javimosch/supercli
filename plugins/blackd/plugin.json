{
  "name": "blackd",
  "version": "0.1.0",
  "description": "CLI tool: blackd",
  "source": "https://packages.debian.org/blackd",
  "checks": [{ "type": "binary", "name": "blackd" }],
  "install_guidance": {
    "plugin": "blackd", "binary": "blackd", "check": "which blackd",
    "install_steps": ["apt-get install blackd 2>/dev/null || which blackd", "Verify: blackd --version", "supercli plugins install ./plugins/blackd --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "blackd", "resource": "_", "action": "_",
    "description": "Passthrough to blackd CLI",
    "adapter": "process", "adapterConfig": { "command": "blackd", "passthrough": true, "missingDependencyHelp": "Install blackd: apt-get install blackd" }, "args": []
  }]
}
