{ "plugin": "blackd", "binary": "blackd", "check": "which blackd", "install_steps": ["apt-get install blackd 2>/dev/null || which blackd", "Verify: blackd --version", "supercli plugins install ./plugins/blackd --on-conflict replace --json"] }
