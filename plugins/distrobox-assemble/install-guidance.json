{ "plugin": "distrobox-assemble", "binary": "distrobox-assemble", "check": "which distrobox-assemble", "install_steps": ["apt-get install distrobox-assemble 2>/dev/null || which distrobox-assemble", "Verify: distrobox-assemble --version", "supercli plugins install ./plugins/distrobox-assemble --on-conflict replace --json"] }
