---
name: sshfs
description: Mount remote directories over SSH as local filesystems using FUSE.
---
# sshfs Plugin
Mount remote directories over SSH as local filesystems using FUSE.
## Commands
- `sshfs self version` — Print version
- `sshfs _ _` — Passthrough
