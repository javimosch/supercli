{
  "plugin": "junie",
  "binary": "junie",
  "check": "which junie",
  "install_steps": [
    "apt-get install junie 2>/dev/null || which junie",
    "Verify: junie --version",
    "supercli plugins install ./plugins/junie --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}