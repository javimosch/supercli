{ "plugin": "cg_merge", "binary": "cg_merge", "check": "which cg_merge", "install_steps": ["apt-get install cg_merge 2>/dev/null || which cg_merge", "Verify: cg_merge --version", "supercli plugins install ./plugins/cg_merge --on-conflict replace --json"] }
