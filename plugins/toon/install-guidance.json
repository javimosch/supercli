{
  "plugin": "toon",
  "binary": "toon",
  "check": "which toon",
  "install_steps": [
    "npm install -g toon",
    "Verify: toon --version",
    "supercli plugins install ./plugins/toon --on-conflict replace --json"
  ],
  "note": "Compact token-oriented alternative to JSON for data serialization."
}
