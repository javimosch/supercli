{
  "plugin": "ts",
  "binary": "ts",
  "check": "which ts",
  "install_steps": [
    "apt-get install ts 2>/dev/null || which ts",
    "Verify: ts --version",
    "supercli plugins install ./plugins/ts --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}