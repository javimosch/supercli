{
  "plugin": "fallow",
  "binary": "fallow",
  "check": "which fallow",
  "install_steps": [
    "cargo install fallow-cli",
    "Verify: fallow --version",
    "supercli plugins install ./plugins/fallow --on-conflict replace --json"
  ],
  "note": "Also available via: npx fallow, npm install -g fallow, or brew install fallow. No authentication required. Sub-second analysis."
}