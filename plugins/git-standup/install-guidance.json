{
  "plugin": "git-standup",
  "binary": "git-standup",
  "check": "which git-standup",
  "install_steps": ["npm install -g git-standup", "Verify: git-standup --version", "supercli plugins install ./plugins/git-standup --on-conflict replace --json"]
}
