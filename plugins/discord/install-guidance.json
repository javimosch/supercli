{ "plugin": "discord", "binary": "discord", "check": "which discord", "install_steps": ["apt-get install discord 2>/dev/null || which discord", "Verify: discord --version", "supercli plugins install ./plugins/discord --on-conflict replace --json"] }
