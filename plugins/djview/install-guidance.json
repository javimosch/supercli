{ "plugin": "djview", "binary": "djview", "check": "which djview", "install_steps": ["apt-get install djview 2>/dev/null || which djview", "Verify: djview --version", "supercli plugins install ./plugins/djview --on-conflict replace --json"] }
