{
  "plugin": "toilet",
  "binary": "toilet",
  "check": "which toilet",
  "install_steps": [
    "brew install toilet",
    "Verify: toilet --version",
    "supercli plugins install ./plugins/toilet --on-conflict replace --json"
  ]
}
