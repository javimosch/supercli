{
  "plugin": "zls",
  "binary": "zls",
  "check": "which zls",
  "install_steps": ["zig build -Doptimize=ReleaseSafe", "Verify: zls --version", "supercli plugins install ./plugins/zls --on-conflict replace --json"]
}
