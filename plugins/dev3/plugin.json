{
  "name": "dev3",
  "version": "0.1.0",
  "description": "CLI tool: dev3",
  "source": "https://packages.debian.org/dev3",
  "checks": [{ "type": "binary", "name": "dev3" }],
  "install_guidance": {
    "plugin": "dev3", "binary": "dev3", "check": "which dev3",
    "install_steps": ["apt-get install dev3 2>/dev/null || which dev3", "Verify: dev3 --version", "supercli plugins install ./plugins/dev3 --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dev3", "resource": "_", "action": "_",
    "description": "Passthrough to dev3 CLI",
    "adapter": "process", "adapterConfig": { "command": "dev3", "passthrough": true, "missingDependencyHelp": "Install dev3: apt-get install dev3" }, "args": []
  }]
}
