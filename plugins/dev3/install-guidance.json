{ "plugin": "dev3", "binary": "dev3", "check": "which dev3", "install_steps": ["apt-get install dev3 2>/dev/null || which dev3", "Verify: dev3 --version", "supercli plugins install ./plugins/dev3 --on-conflict replace --json"] }
