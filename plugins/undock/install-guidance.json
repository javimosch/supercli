{
  "plugin": "undock",
  "binary": "undock",
  "check": "which undock",
  "install_steps": [
    "brew install undock",
    "Verify: undock --version",
    "supercli plugins install ./plugins/undock --on-conflict replace --json"
  ]
}