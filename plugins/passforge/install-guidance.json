{
  "plugin": "passforge",
  "binary": "passforge",
  "check": "which passforge",
  "install_steps": ["cargo install passforge", "Verify: passforge --version", "supercli plugins install ./plugins/passforge --on-conflict replace --json"]
}
