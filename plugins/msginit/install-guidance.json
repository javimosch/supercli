{
  "plugin": "msginit",
  "binary": "msginit",
  "check": "which msginit",
  "install_steps": [
    "apt-get install msginit 2>/dev/null || which msginit",
    "Verify: msginit --version",
    "supercli plugins install ./plugins/msginit --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}