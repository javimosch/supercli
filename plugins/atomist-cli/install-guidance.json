{
  "plugin": "atomist-cli",
  "binary": "atomist-cli",
  "check": "which atomist-cli",
  "install_steps": [
    "brew install atomist-cli",
    "Verify: atomist-cli --version",
    "supercli plugins install ./plugins/atomist-cli --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}