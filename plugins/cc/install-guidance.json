{ "plugin": "cc", "binary": "cc", "check": "which cc", "install_steps": ["apt-get install cc 2>/dev/null || which cc", "Verify: cc --version", "supercli plugins install ./plugins/cc --on-conflict replace --json"] }
