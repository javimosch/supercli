{
  "plugin": "tomlq",
  "binary": "tomlq",
  "check": "which tomlq",
  "install_steps": ["pip install tomlq", "Verify: tomlq --version", "supercli plugins install ./plugins/tomlq --on-conflict replace --json"]
}
