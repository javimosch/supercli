{
  "plugin": "lev",
  "binary": "lev",
  "check": "which lev",
  "install_steps": [
    "npm install -g lev",
    "Verify: lev --version",
    "supercli plugins install ./plugins/lev --on-conflict replace --json"
  ]
}