{
  "plugin": "phar",
  "binary": "phar",
  "check": "which phar",
  "install_steps": [
    "apt-get install phar 2>/dev/null || which phar",
    "Verify: phar --version",
    "supercli plugins install ./plugins/phar --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}