{
  "plugin": "kondo",
  "binary": "kondo",
  "check": "which kondo",
  "install_steps": [
    "brew install kondo",
    "Verify: kondo --version",
    "supercli plugins install ./plugins/kondo --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install kondo. Supports Rust, Node.js, Python, and more. Identifies and deletes build artifacts, caches, and dependencies to free up disk space."
}
