{
  "plugin": "hex",
  "binary": "hex",
  "check": "which hex",
  "install_steps": [
    "brew install hex",
    "Verify: hex --version",
    "supercli plugins install ./plugins/hex --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}