{
  "plugin": "h2",
  "binary": "h2",
  "check": "which h2",
  "install_steps": [
    "# Install h2",
    "# See github.com/h2/h2",
    "# apt: sudo apt-get install h2",
    "# brew: brew install h2",
    "# cargo: cargo install h2",
    "# npm: npm install -g h2"
  ]
}