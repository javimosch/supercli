---
name: gdk-pixbuf-thumbnailer
description: Use this skill when the user wants to use gdk-pixbuf-thumbnailer, cli tool: gdk-pixbuf-thumbnailer.
---

# gdk-pixbuf-thumbnailer Plugin

CLI tool: gdk-pixbuf-thumbnailer.

## Commands
- `gdk-pixbuf-thumbnailer <resource> <action>` — Execute gdk-pixbuf-thumbnailer commands
- `gdk-pixbuf-thumbnailer self version` — Print gdk-pixbuf-thumbnailer version
- `gdk-pixbuf-thumbnailer _ _` — Passthrough to gdk-pixbuf-thumbnailer CLI

## Usage Examples
- "gdk-pixbuf-thumbnailer --help"
- "gdk-pixbuf-thumbnailer self version"

## Installation
```bash
apt-get install gdk-pixbuf-thumbnailer 2>/dev/null || which gdk-pixbuf-thumbnailer
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
