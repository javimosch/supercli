{
  "plugin": "libdill",
  "binary": "libdill",
  "check": "which libdill",
  "install_steps": [
    "brew install libdill",
    "Verify: libdill --version",
    "supercli plugins install ./plugins/libdill --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}