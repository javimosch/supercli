{
  "plugin": "Jmp",
  "binary": "Jmp",
  "check": "which Jmp",
  "install_steps": [
    "See https://github.com/gholmes829/Jmp",
    "Verify: Jmp --version",
    "supercli plugins install ./plugins/Jmp --on-conflict replace --json"
  ],
  "note": "See project repository"
}