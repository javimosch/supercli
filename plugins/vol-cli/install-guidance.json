{
  "plugin": "vol-cli",
  "binary": "vol-cli",
  "check": "which vol-cli",
  "install_steps": ["git clone and install", "Verify: vol-cli --version", "supercli plugins install ./plugins/vol-cli --on-conflict replace --json"]
}
