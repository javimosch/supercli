{
  "name": "dpkg-parsechangelog",
  "version": "1.0.0",
  "description": "dpkg-parsechangelog \u2014 dpkg-parsechangelog \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-parsechangelog"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-parsechangelog",
    "binary": "dpkg-parsechangelog",
    "check": "which dpkg-parsechangelog",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-parsechangelog",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-parsechangelog \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-parsechangelog",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}