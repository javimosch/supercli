{
  "name": "cm",
  "version": "0.1.0",
  "description": "CLI tool: cm",
  "source": "https://packages.debian.org/cm",
  "checks": [{ "type": "binary", "name": "cm" }],
  "install_guidance": {
    "plugin": "cm", "binary": "cm", "check": "which cm",
    "install_steps": ["apt-get install cm 2>/dev/null || which cm", "Verify: cm --version", "supercli plugins install ./plugins/cm --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cm", "resource": "_", "action": "_",
    "description": "Passthrough to cm CLI",
    "adapter": "process", "adapterConfig": { "command": "cm", "passthrough": true, "missingDependencyHelp": "Install cm: apt-get install cm" }, "args": []
  }]
}
