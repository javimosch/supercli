{ "plugin": "cm", "binary": "cm", "check": "which cm", "install_steps": ["apt-get install cm 2>/dev/null || which cm", "Verify: cm --version", "supercli plugins install ./plugins/cm --on-conflict replace --json"] }
