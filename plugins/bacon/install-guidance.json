{
  "plugin": "bacon",
  "binary": "bacon",
  "check": "which bacon",
  "install_steps": [
    "cargo install bacon --locked",
    "Or: brew install bacon",
    "Verify: bacon --version",
    "supercli plugins install ./plugins/bacon --on-conflict replace --json"
  ],
  "note": "Bacon watches your Rust project and runs checks (clippy, rustc, test) on file changes. Default behavior runs cargo check. Use 'bacon --init' to generate a config file, then 'bacon clippy' or 'bacon test' for specific jobs. Requires Rust toolchain (rustup)."
}
