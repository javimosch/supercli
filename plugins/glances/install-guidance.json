{
  "plugin": "glances",
  "binary": "glances",
  "check": "which glances",
  "install_steps": [
    "pip install glances",
    "Or: brew install glances",
    "Verify: glances --version",
    "supercli plugins install ./plugins/glances --on-conflict replace --json"
  ],
  "note": "Glances supports headless mode with --stdout, --stdout-json, and --stdout-csv flags for automated monitoring and scripting."
}
