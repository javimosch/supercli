{
  "name": "magic-unalias",
  "version": "0.1.0",
  "description": "CLI tool: magic-unalias",
  "source": "https://manpages.debian.org/magic-unalias",
  "checks": [
    {
      "type": "binary",
      "name": "magic-unalias"
    }
  ],
  "install_guidance": {
    "plugin": "magic-unalias",
    "binary": "magic-unalias",
    "check": "which magic-unalias",
    "install_steps": [
      "apt-get install magic-unalias 2>/dev/null || which magic-unalias",
      "Verify: magic-unalias --version",
      "supercli plugins install ./plugins/magic-unalias --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "magic-unalias",
      "resource": "self",
      "action": "version",
      "description": "Print magic-unalias version",
      "adapter": "process",
      "adapterConfig": {
        "command": "magic-unalias",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install magic-unalias: apt-get install magic-unalias 2>/dev/null || which magic-unalias"
      },
      "args": []
    },
    {
      "namespace": "magic-unalias",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to magic-unalias CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "magic-unalias",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install magic-unalias: apt-get install magic-unalias 2>/dev/null || which magic-unalias"
      },
      "args": []
    }
  ]
}