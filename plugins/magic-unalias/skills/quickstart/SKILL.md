---
name: magic-unalias
description: Use this skill when the user wants to use magic-unalias, cli tool: magic-unalias.
---

# magic-unalias Plugin

CLI tool: magic-unalias.

## Commands
- `magic-unalias <resource> <action>` — Execute magic-unalias commands
- `magic-unalias self version` — Print magic-unalias version
- `magic-unalias _ _` — Passthrough to magic-unalias CLI

## Usage Examples
- "magic-unalias --help"
- "magic-unalias self version"

## Installation
```bash
apt-get install magic-unalias 2>/dev/null || which magic-unalias
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
