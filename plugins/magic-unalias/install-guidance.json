{
  "plugin": "magic-unalias",
  "binary": "magic-unalias",
  "check": "which magic-unalias",
  "install_steps": [
    "apt-get install magic-unalias 2>/dev/null || which magic-unalias",
    "Verify: magic-unalias --version",
    "supercli plugins install ./plugins/magic-unalias --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}