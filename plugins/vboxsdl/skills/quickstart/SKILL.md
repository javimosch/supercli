---
name: vboxsdl
description: Use this skill when the user wants to use vboxsdl, cli tool: vboxsdl.
---

# vboxsdl Plugin

CLI tool: vboxsdl.

## Commands
- `vboxsdl <resource> <action>` — Execute vboxsdl commands
- `vboxsdl self version` — Print vboxsdl version
- `vboxsdl _ _` — Passthrough to vboxsdl CLI

## Usage Examples
- "vboxsdl --help"
- "vboxsdl self version"

## Installation
```bash
apt-get install vboxsdl 2>/dev/null || which vboxsdl
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
