{
  "plugin": "bane",
  "binary": "bane",
  "check": "which bane",
  "install_steps": [
    "brew install bane",
    "Verify: bane --version",
    "supercli plugins install ./plugins/bane --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/genuinetools/bane@latest. Generates AppArmor security profiles for Docker containers automatically. Analyzes container behavior and creates appropriate security profiles. Great for container security hardening."
}
