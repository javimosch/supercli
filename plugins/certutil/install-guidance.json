{ "plugin": "certutil", "binary": "certutil", "check": "which certutil", "install_steps": ["apt-get install certutil 2>/dev/null || which certutil", "Verify: certutil --version", "supercli plugins install ./plugins/certutil --on-conflict replace --json"] }
