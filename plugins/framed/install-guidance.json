{
  "plugin": "framed",
  "binary": "framed",
  "check": "which framed",
  "install_steps": [
    "go install github.com/mactat/framed@latest",
    "Verify: framed --version",
    "supercli plugins install ./plugins/framed --on-conflict replace --json"
  ]
}