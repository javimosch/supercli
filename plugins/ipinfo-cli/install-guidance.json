{
  "plugin": "ipinfo-cli",
  "binary": "ipinfo",
  "check": "which ipinfo",
  "install_steps": [
    "brew install ipinfo-cli",
    "Verify: ipinfo --version",
    "supercli plugins install ./plugins/ipinfo-cli --on-conflict replace --json"
  ]
}