{ "plugin": "apache2", "binary": "apache2", "check": "which apache2", "install_steps": ["apt-get install apache2 2>/dev/null || which apache2", "Verify: apache2 --version", "supercli plugins install ./plugins/apache2 --on-conflict replace --json"] }
