{
  "name": "check-memory",
  "version": "0.1.0",
  "description": "CLI tool: check-memory",
  "source": "https://packages.debian.org/check-memory",
  "checks": [{ "type": "binary", "name": "check-memory" }],
  "install_guidance": {
    "plugin": "check-memory", "binary": "check-memory", "check": "which check-memory",
    "install_steps": ["apt-get install check-memory 2>/dev/null || which check-memory", "Verify: check-memory --version", "supercli plugins install ./plugins/check-memory --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check-memory", "resource": "_", "action": "_",
    "description": "Passthrough to check-memory CLI",
    "adapter": "process", "adapterConfig": { "command": "check-memory", "passthrough": true, "missingDependencyHelp": "Install check-memory: apt-get install check-memory" }, "args": []
  }]
}
