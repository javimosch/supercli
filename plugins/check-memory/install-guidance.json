{ "plugin": "check-memory", "binary": "check-memory", "check": "which check-memory", "install_steps": ["apt-get install check-memory 2>/dev/null || which check-memory", "Verify: check-memory --version", "supercli plugins install ./plugins/check-memory --on-conflict replace --json"] }
