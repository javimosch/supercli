{ "plugin": "dcop", "binary": "dcop", "check": "which dcop", "install_steps": ["apt-get install dcop 2>/dev/null || which dcop", "Verify: dcop --version", "supercli plugins install ./plugins/dcop --on-conflict replace --json"] }
