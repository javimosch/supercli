{
  "plugin": "northflank-cli",
  "binary": "northflank",
  "check": "which northflank",
  "install_steps": [
    "brew install northflank",
    "Verify: northflank --version",
    "supercli plugins install ./plugins/northflank-cli --on-conflict replace --json"
  ]
}
