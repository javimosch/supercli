---
name: tls.zig
description: Use this skill when the user wants to use tls.zig for TLS 1.3/1.2  client and TLS 1.3 server in Zig.
---

# tls.zig Plugin

TLS 1.3/1.2  client and TLS 1.3 server in Zig

## Commands

### General
- `tls.zig self version` — Print tls.zig version
- `tls.zig _ _` — Passthrough to tls.zig CLI

## Usage Examples
- "Run tls.zig"
- "Use tls.zig to TLS 1.3/1.2  client and TLS 1.3 server in Zig"

## Installation

```bash
pip install tls.zig
```

## Key Features
- TLS 1.3/1.2  client and TLS 1.3 server in Zig
- CLI tool, headless and scriptable
