{
  "plugin": "ppmhist",
  "binary": "ppmhist",
  "check": "which ppmhist",
  "install_steps": [
    "apt-get install ppmhist 2>/dev/null || which ppmhist",
    "Verify: ppmhist --version",
    "supercli plugins install ./plugins/ppmhist --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}