{ "plugin": "dbadmin", "binary": "dbadmin", "check": "which dbadmin", "install_steps": ["apt-get install dbadmin 2>/dev/null || which dbadmin", "Verify: dbadmin --version", "supercli plugins install ./plugins/dbadmin --on-conflict replace --json"] }
