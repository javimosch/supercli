{ "plugin": "allec2", "binary": "allec2", "check": "which allec2", "install_steps": ["apt-get install allec2 2>/dev/null || which allec2", "Verify: allec2 --version", "supercli plugins install ./plugins/allec2 --on-conflict replace --json"] }
