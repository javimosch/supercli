{
  "plugin": "lxc-checkpoint",
  "binary": "lxc-checkpoint",
  "check": "which lxc-checkpoint",
  "install_steps": [
    "apt-get install lxc-checkpoint 2>/dev/null || which lxc-checkpoint",
    "Verify: lxc-checkpoint --version",
    "supercli plugins install ./plugins/lxc-checkpoint --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}