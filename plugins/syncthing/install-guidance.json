{
  "plugin": "syncthing",
  "binary": "syncthing",
  "check": "which syncthing",
  "install_steps": [
    "brew install syncthing",
    "Verify: syncthing --version",
    "supercli plugins install ./plugins/syncthing --on-conflict replace --json"
  ]
}