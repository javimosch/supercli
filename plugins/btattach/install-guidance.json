{ "plugin": "btattach", "binary": "btattach", "check": "which btattach", "install_steps": ["apt-get install btattach 2>/dev/null || which btattach", "Verify: btattach --version", "supercli plugins install ./plugins/btattach --on-conflict replace --json"] }
