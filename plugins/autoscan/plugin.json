{
  "name": "autoscan",
  "version": "0.1.0",
  "description": "CLI tool: autoscan",
  "source": "https://packages.debian.org/autoscan",
  "checks": [{ "type": "binary", "name": "autoscan" }],
  "install_guidance": {
    "plugin": "autoscan", "binary": "autoscan", "check": "which autoscan",
    "install_steps": ["apt-get install autoscan 2>/dev/null || which autoscan", "Verify: autoscan --version", "supercli plugins install ./plugins/autoscan --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "autoscan", "resource": "_", "action": "_",
    "description": "Passthrough to autoscan CLI",
    "adapter": "process", "adapterConfig": { "command": "autoscan", "passthrough": true, "missingDependencyHelp": "Install autoscan: apt-get install autoscan" }, "args": []
  }]
}
