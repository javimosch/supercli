{
  "name": "foo2slx-wrapper",
  "version": "0.1.0",
  "description": "CLI tool: foo2slx-wrapper",
  "source": "https://manpages.debian.org/foo2slx-wrapper",
  "checks": [
    {
      "type": "binary",
      "name": "foo2slx-wrapper"
    }
  ],
  "install_guidance": {
    "plugin": "foo2slx-wrapper",
    "binary": "foo2slx-wrapper",
    "check": "which foo2slx-wrapper",
    "install_steps": [
      "apt-get install foo2slx-wrapper 2>/dev/null || which foo2slx-wrapper",
      "Verify: foo2slx-wrapper --version",
      "supercli plugins install ./plugins/foo2slx-wrapper --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "foo2slx-wrapper",
      "resource": "self",
      "action": "version",
      "description": "Print foo2slx-wrapper version",
      "adapter": "process",
      "adapterConfig": {
        "command": "foo2slx-wrapper",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install foo2slx-wrapper: apt-get install foo2slx-wrapper 2>/dev/null || which foo2slx-wrapper"
      },
      "args": []
    },
    {
      "namespace": "foo2slx-wrapper",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to foo2slx-wrapper CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "foo2slx-wrapper",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install foo2slx-wrapper: apt-get install foo2slx-wrapper 2>/dev/null || which foo2slx-wrapper"
      },
      "args": []
    }
  ]
}