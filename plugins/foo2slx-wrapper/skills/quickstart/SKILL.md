---
name: foo2slx-wrapper
description: Use this skill when the user wants to use foo2slx-wrapper, cli tool: foo2slx-wrapper.
---

# foo2slx-wrapper Plugin

CLI tool: foo2slx-wrapper.

## Commands
- `foo2slx-wrapper <resource> <action>` — Execute foo2slx-wrapper commands
- `foo2slx-wrapper self version` — Print foo2slx-wrapper version
- `foo2slx-wrapper _ _` — Passthrough to foo2slx-wrapper CLI

## Usage Examples
- "foo2slx-wrapper --help"
- "foo2slx-wrapper self version"

## Installation
```bash
apt-get install foo2slx-wrapper 2>/dev/null || which foo2slx-wrapper
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
