{
  "name": "sent",
  "version": "0.1.0",
  "description": "Plain-text presentation tool using X11 to render slides",
  "source": "shell",
  "checks": [{ "type": "binary", "name": "sent" }],
  "install_guidance": {
    "plugin": "sent",
    "binary": "sent",
    "check": "which sent",
    "install_steps": ["sudo apt install sent", "Verify: sent --version", "supercli plugins install ./plugins/sent --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "sent",
    "resource": "_",
    "action": "_",
    "description": "Passthrough to sent CLI",
    "adapter": "process",
    "adapterConfig": {
      "command": "sent",
      "passthrough": true,
      "missingDependencyHelp": "Install sent: sudo apt install sent"
    },
    "args": []
  }]
}
