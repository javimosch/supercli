{
  "plugin": "update-inetd",
  "binary": "update-inetd",
  "check": "which update-inetd",
  "install_steps": [
    "apt-get install update-inetd 2>/dev/null || which update-inetd",
    "Verify: update-inetd --version",
    "supercli plugins install ./plugins/update-inetd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}