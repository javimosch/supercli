{
  "plugin": "s",
  "binary": "s",
  "check": "which s",
  "install_steps": [
    "brew install s",
    "Verify: s --version",
    "supercli plugins install ./plugins/s --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}