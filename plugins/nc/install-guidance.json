{
  "plugin": "nc",
  "binary": "nc",
  "check": "which nc",
  "install_steps": [
    "apt-get install netcat-openbsd",
    "Verify: nc -h",
    "supercli plugins install ./plugins/nc --on-conflict replace --json"
  ]
}
