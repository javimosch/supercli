{
  "name": "upx",
  "version": "0.1.0",
  "description": "upx — Ultimate Packer for eXecutables. Compress executables by 50-70% with no runtime cost.",
  "source": "https://github.com/upx/upx",
  "checks": [
    { "type": "binary", "name": "upx" }
  ],
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "install_guidance": {
    "plugin": "upx",
    "binary": "upx",
    "check": "which upx",
    "install_steps": ["apt install upx"],
    "note": "Also available via: pkg install upx, brew install upx, pacman -S upx. Compresses PE/ELF/Mach-O executables."
  },
  "commands": [
    {
      "namespace": "upx",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to upx CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "upx",
        "passthrough": true,
        "missingDependencyHelp": "Install upx: apt install upx"
      },
      "args": []
    }
  ]
}
