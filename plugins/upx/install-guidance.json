{
  "plugin": "upx",
  "binary": "upx",
  "check": "which upx",
  "install_steps": [
    "apt install upx",
    "Verify: upx --version",
    "supercli plugins install ./plugins/upx --on-conflict replace --json"
  ],
  "note": "Also available via: brew install upx, pkg install upx, pacman -S upx. Compresses PE/ELF/Mach-O executables with no runtime decompression cost."
}
