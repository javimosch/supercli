{
  "plugin": "az",
  "binary": "az",
  "check": "which az",
  "install_steps": [
    "brew install az",
    "Verify: az --version",
    "supercli plugins install ./plugins/az --on-conflict replace --json"
  ]
}