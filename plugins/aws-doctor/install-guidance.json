{
  "plugin": "aws-doctor",
  "binary": "aws-doctor",
  "check": "aws-doctor version",
  "install_steps": [
    "brew install elC0mpa/homebrew-tap/aws-doctor"
  ],
  "note": "Requires AWS credentials configured via AWS CLI (aws configure)"
}
