{
  "plugin": "xword-dl",
  "binary": "xword-dl",
  "check": "which xword-dl",
  "install_steps": [
    "pip install xword-dl",
    "Verify: xword-dl --version",
    "supercli plugins install ./plugins/xword-dl --on-conflict replace --json"
  ]
}