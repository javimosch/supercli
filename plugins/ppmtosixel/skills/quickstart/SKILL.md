---
name: ppmtosixel
description: Use this skill when the user wants to use ppmtosixel, cli tool: ppmtosixel.
---

# ppmtosixel Plugin

CLI tool: ppmtosixel.

## Commands
- `ppmtosixel <resource> <action>` — Execute ppmtosixel commands
- `ppmtosixel self version` — Print ppmtosixel version
- `ppmtosixel _ _` — Passthrough to ppmtosixel CLI

## Usage Examples
- "ppmtosixel --help"
- "ppmtosixel self version"

## Installation
```bash
apt-get install ppmtosixel 2>/dev/null || which ppmtosixel
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
