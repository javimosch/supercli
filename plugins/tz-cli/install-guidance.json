{
  "plugin": "tz-cli",
  "binary": "tz-cli",
  "check": "which tz-cli",
  "install_steps": ["npm install -g tz-cli", "Verify: tz-cli --version", "supercli plugins install ./plugins/tz-cli --on-conflict replace --json"]
}
