{
  "plugin": "lxc-copy",
  "binary": "lxc-copy",
  "check": "which lxc-copy",
  "install_steps": [
    "apt-get install lxc-copy 2>/dev/null || which lxc-copy",
    "Verify: lxc-copy --version",
    "supercli plugins install ./plugins/lxc-copy --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}