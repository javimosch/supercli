{
  "plugin": "lumen",
  "binary": "lumen",
  "check": "which lumen",
  "install_steps": [
    "brew install jnsahaj/lumen/lumen",
    "Verify: lumen --version",
    "supercli plugins install ./plugins/lumen --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install lumen. The diff command works without AI configuration. For AI features (explain, draft, operate), run 'lumen configure' to set up your AI provider and API key. Optional dependencies: fzf for lumen explain --list, mdcat for pretty output formatting."
}
