{
  "plugin": "diffoscope",
  "binary": "diffoscope",
  "check": "which diffoscope",
  "install_steps": [
    "pip install diffoscope",
    "Verify: diffoscope --version",
    "supercli plugins install ./plugins/diffoscope --on-conflict replace --json"
  ],
  "note": "Also available via: brew install diffoscope, apt install diffoscope, or Docker: docker run --rm -t -w $(pwd) -v $(pwd):$(pwd):ro registry.salsa.debian.org/reproducible-builds/diffoscope"
}
