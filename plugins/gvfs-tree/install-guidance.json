{
  "plugin": "gvfs-tree",
  "binary": "gvfs-tree",
  "check": "which gvfs-tree",
  "install_steps": [
    "apt-get install gvfs-tree 2>/dev/null || which gvfs-tree",
    "Verify: gvfs-tree --version",
    "supercli plugins install ./plugins/gvfs-tree --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}