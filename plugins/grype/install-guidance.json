{
  "plugin": "grype",
  "binary": "grype",
  "check": "which grype",
  "install_steps": [
    "brew install grype",
    "Verify: grype --version",
    "supercli plugins install ./plugins/grype --on-conflict replace --json"
  ]
}