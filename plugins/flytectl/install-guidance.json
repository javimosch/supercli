{
  "plugin": "flytectl",
  "binary": "flytectl",
  "check": "which flytectl",
  "install_steps": ["brew install flytectl", "Verify: flytectl --version", "supercli plugins install ./plugins/flytectl --on-conflict replace --json"]
}
