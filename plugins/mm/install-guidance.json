{
  "plugin": "mm",
  "binary": "mm",
  "check": "which mm",
  "install_steps": [
    "pip install mm-ctx",
    "Verify: mm --version",
    "supercli plugins install ./plugins/mm --on-conflict replace --json"
  ],
  "note": "Also installable via: uv pip install mm-ctx, or run without installing: uvx --from mm-ctx mm --help. Optional extras: mm-ctx[mlx] (Apple Silicon audio), mm-ctx[gpu] (Linux/Windows GPU audio). Shell installer: curl -LsSf https://vlm-run.github.io/mm/install/install.sh | sh"
}
