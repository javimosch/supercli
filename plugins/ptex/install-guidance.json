{
  "plugin": "ptex",
  "binary": "ptex",
  "check": "which ptex",
  "install_steps": [
    "apt-get install ptex 2>/dev/null || which ptex",
    "Verify: ptex --version",
    "supercli plugins install ./plugins/ptex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}