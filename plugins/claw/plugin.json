{
  "name": "claw",
  "version": "0.1.0",
  "description": "CLI tool: claw",
  "source": "https://packages.debian.org/claw",
  "checks": [{ "type": "binary", "name": "claw" }],
  "install_guidance": {
    "plugin": "claw", "binary": "claw", "check": "which claw",
    "install_steps": ["apt-get install claw 2>/dev/null || which claw", "Verify: claw --version", "supercli plugins install ./plugins/claw --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "claw", "resource": "_", "action": "_",
    "description": "Passthrough to claw CLI",
    "adapter": "process", "adapterConfig": { "command": "claw", "passthrough": true, "missingDependencyHelp": "Install claw: apt-get install claw" }, "args": []
  }]
}
