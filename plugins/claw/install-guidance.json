{ "plugin": "claw", "binary": "claw", "check": "which claw", "install_steps": ["apt-get install claw 2>/dev/null || which claw", "Verify: claw --version", "supercli plugins install ./plugins/claw --on-conflict replace --json"] }
