{ "plugin": "at_colors", "binary": "at_colors", "check": "which at_colors", "install_steps": ["apt-get install at_colors 2>/dev/null || which at_colors", "Verify: at_colors --version", "supercli plugins install ./plugins/at_colors --on-conflict replace --json"] }
