{
  "plugin": "texhash",
  "binary": "texhash",
  "check": "which texhash",
  "install_steps": [
    "apt-get install texhash 2>/dev/null || which texhash",
    "Verify: texhash --version",
    "supercli plugins install ./plugins/texhash --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}