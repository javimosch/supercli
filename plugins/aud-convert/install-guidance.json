{
  "plugin": "aud-convert",
  "binary": "aud-convert",
  "check": "which aud-convert",
  "install_steps": ["pip install aud-convert", "Verify: aud-convert --version", "supercli plugins install ./plugins/aud-convert --on-conflict replace --json"]
}
