{
  "plugin": "topgrade",
  "binary": "topgrade",
  "check": "which topgrade",
  "install_steps": [
    "brew install topgrade",
    "Verify: topgrade --version",
    "supercli plugins install ./plugins/topgrade --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install topgrade. Upgrades system packages, Homebrew, npm, cargo, pip, and more. Cross-platform support for Linux, macOS, Windows. Configurable with custom steps."
}
