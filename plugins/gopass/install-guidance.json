{
  "plugin": "gopass",
  "binary": "gopass",
  "check": "which gopass",
  "install_steps": [
    "brew install gopass",
    "Verify: gopass --version",
    "Initialize: gopass init (creates local password store with GPG)",
    "Optionally setup git remote: gopass git remote add origin git@github.com:user/passwords.git",
    "supercli plugins install ./plugins/gopass --on-conflict replace --json"
  ],
  "note": "GPG key required for initialization. For automation, use gopass with '--yes' flag to skip confirmations. Store is at ~/.local/share/gopass by default. Supports age encryption as alternative to GPG."
}
