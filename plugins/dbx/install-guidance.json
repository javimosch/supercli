{
  "plugin": "dbx",
  "binary": "dbx",
  "check": "which dbx",
  "install_steps": [
    "cargo install dbx",
    "Verify: dbx --version",
    "supercli plugins install ./plugins/dbx --on-conflict replace --json"
  ],
  "note": "Fast CLI database client supporting MySQL, PostgreSQL, and SQLite."
}
