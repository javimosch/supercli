{ "plugin": "db_checkpoint", "binary": "db_checkpoint", "check": "which db_checkpoint", "install_steps": ["apt-get install db_checkpoint 2>/dev/null || which db_checkpoint", "Verify: db_checkpoint --version", "supercli plugins install ./plugins/db_checkpoint --on-conflict replace --json"] }
