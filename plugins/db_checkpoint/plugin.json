{
  "name": "db_checkpoint",
  "version": "0.1.0",
  "description": "CLI tool: db_checkpoint",
  "source": "https://packages.debian.org/db_checkpoint",
  "checks": [{ "type": "binary", "name": "db_checkpoint" }],
  "install_guidance": {
    "plugin": "db_checkpoint", "binary": "db_checkpoint", "check": "which db_checkpoint",
    "install_steps": ["apt-get install db_checkpoint 2>/dev/null || which db_checkpoint", "Verify: db_checkpoint --version", "supercli plugins install ./plugins/db_checkpoint --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_checkpoint", "resource": "_", "action": "_",
    "description": "Passthrough to db_checkpoint CLI",
    "adapter": "process", "adapterConfig": { "command": "db_checkpoint", "passthrough": true, "missingDependencyHelp": "Install db_checkpoint: apt-get install db_checkpoint" }, "args": []
  }]
}
