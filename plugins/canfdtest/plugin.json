{
  "name": "canfdtest",
  "version": "0.1.0",
  "description": "CLI tool: canfdtest",
  "source": "https://packages.debian.org/canfdtest",
  "checks": [{ "type": "binary", "name": "canfdtest" }],
  "install_guidance": {
    "plugin": "canfdtest", "binary": "canfdtest", "check": "which canfdtest",
    "install_steps": ["apt-get install canfdtest 2>/dev/null || which canfdtest", "Verify: canfdtest --version", "supercli plugins install ./plugins/canfdtest --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "canfdtest", "resource": "_", "action": "_",
    "description": "Passthrough to canfdtest CLI",
    "adapter": "process", "adapterConfig": { "command": "canfdtest", "passthrough": true, "missingDependencyHelp": "Install canfdtest: apt-get install canfdtest" }, "args": []
  }]
}
