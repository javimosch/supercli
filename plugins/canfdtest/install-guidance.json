{ "plugin": "canfdtest", "binary": "canfdtest", "check": "which canfdtest", "install_steps": ["apt-get install canfdtest 2>/dev/null || which canfdtest", "Verify: canfdtest --version", "supercli plugins install ./plugins/canfdtest --on-conflict replace --json"] }
