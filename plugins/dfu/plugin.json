{
  "name": "dfu",
  "version": "0.1.0",
  "description": "CLI tool: dfu",
  "source": "https://packages.debian.org/dfu",
  "checks": [{ "type": "binary", "name": "dfu" }],
  "install_guidance": {
    "plugin": "dfu", "binary": "dfu", "check": "which dfu",
    "install_steps": ["apt-get install dfu 2>/dev/null || which dfu", "Verify: dfu --version", "supercli plugins install ./plugins/dfu --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dfu", "resource": "_", "action": "_",
    "description": "Passthrough to dfu CLI",
    "adapter": "process", "adapterConfig": { "command": "dfu", "passthrough": true, "missingDependencyHelp": "Install dfu: apt-get install dfu" }, "args": []
  }]
}
