{ "plugin": "dfu", "binary": "dfu", "check": "which dfu", "install_steps": ["apt-get install dfu 2>/dev/null || which dfu", "Verify: dfu --version", "supercli plugins install ./plugins/dfu --on-conflict replace --json"] }
