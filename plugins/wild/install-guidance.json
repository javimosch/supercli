{
  "plugin": "wild",
  "binary": "wild",
  "check": "which wild",
  "install_steps": [
    "pip install wild",
    "Verify: wild --version",
    "supercli plugins install ./plugins/wild --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}