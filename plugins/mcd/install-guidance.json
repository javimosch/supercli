{
  "plugin": "mcd",
  "binary": "mcd",
  "check": "which mcd",
  "install_steps": [
    "apt-get install mcd 2>/dev/null || which mcd",
    "Verify: mcd --version",
    "supercli plugins install ./plugins/mcd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}