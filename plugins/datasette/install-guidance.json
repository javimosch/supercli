{
    "plugin": "datasette",
    "binary": "datasette",
    "check": "which datasette",
    "install_steps": ["pip install datasette", "which datasette", "Verify: datasette --version", "supercli plugins install ./plugins/datasette --on-conflict replace --json"]
  }
