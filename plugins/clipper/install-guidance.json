{
  "plugin": "clipper",
  "binary": "clipper",
  "check": "which clipper",
  "install_steps": ["go install github.com/wincent/clipper@latest", "Verify: clipper --version", "supercli plugins install ./plugins/clipper --on-conflict replace --json"]
}
