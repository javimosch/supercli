{
  "plugin": "devmoji",
  "binary": "devmoji",
  "check": "which devmoji",
  "install_steps": [
    "npm install -g devmoji",
    "Verify: devmoji --version",
    "supercli plugins install ./plugins/devmoji --on-conflict replace --json"
  ]
}
