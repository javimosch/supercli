{
  "plugin": "dockerc",
  "binary": "dockerc",
  "check": "which dockerc",
  "install_steps": [
    "pip install dockerc",
    "Verify: dockerc --version",
    "supercli plugins install ./plugins/dockerc --on-conflict replace --json"
  ],
  "note": "Language: Zig. Stars: 4901"
}