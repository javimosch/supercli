{
  "name": "faillock",
  "version": "1.0.0",
  "description": "faillock \u2014 CLI faillock \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=faillock+cli",
  "checks": [
    {
      "type": "binary",
      "name": "faillock"
    }
  ],
  "install_guidance": {
    "plugin": "faillock",
    "binary": "faillock",
    "check": "which faillock",
    "install_steps": [
      "Install via package manager: apt install faillock or brew install faillock"
    ]
  },
  "commands": [
    {
      "namespace": "faillock",
      "resource": "run",
      "action": "exec",
      "description": "faillock \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "faillock",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install faillock via package manager"
      },
      "args": []
    }
  ]
}