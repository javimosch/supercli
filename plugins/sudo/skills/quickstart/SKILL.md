---
name: sudo
description: Use this skill when the user wants to use sudo for It's sudo, for Windows.
---

# sudo Plugin

It's sudo, for Windows

## Commands

### General
- `sudo self version` — Print sudo version
- `sudo _ _` — Passthrough to sudo CLI

## Usage Examples
- "Run sudo"
- "Use sudo to It's sudo, for Windows"

## Installation

```bash
pip install sudo
```

## Key Features
- It's sudo, for Windows
- CLI tool, headless and scriptable
