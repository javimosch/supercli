{
  "plugin": "banner",
  "binary": "banner",
  "check": "which banner",
  "install_steps": [
    "brew install banner",
    "Verify: banner --version",
    "supercli plugins install ./plugins/banner --on-conflict replace --json"
  ]
}
