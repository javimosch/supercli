{
  "plugin": "hurl",
  "binary": "hurl",
  "check": "which hurl",
  "install_steps": ["brew install hurl", "Verify: hurl --version", "supercli plugins install ./plugins/hurl --on-conflict replace --json"]
}
