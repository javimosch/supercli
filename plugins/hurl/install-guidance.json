{
  "plugin": "hurl",
  "binary": "hurl",
  "check": "which hurl",
  "install_steps": [
    "brew install hurl",
    "Verify: hurl --version",
    "supercli plugins install ./plugins/hurl --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install hurl. Powered by curl under the hood. Supports GET, POST, PUT, DELETE, and more. Can generate HTML, JSON, JUnit, and TAP reports. Hurl files use plain text format for requests and assertions."
}
