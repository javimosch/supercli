{
  "plugin": "mmdr",
  "binary": "mmdr",
  "check": "which mmdr",
  "install_steps": [
    "cargo install mermaid-rs-renderer",
    "Verify: mmdr --version",
    "supercli plugins install ./plugins/mmdr --on-conflict replace --json"
  ],
  "note": "Also available via: brew (brew install mmdr), scoop, AUR (yay -S mmdr-bin). Binary name is 'mmdr'."
}