{
  "plugin": "update-fmtutil",
  "binary": "update-fmtutil",
  "check": "which update-fmtutil",
  "install_steps": [
    "apt-get install update-fmtutil 2>/dev/null || which update-fmtutil",
    "Verify: update-fmtutil --version",
    "supercli plugins install ./plugins/update-fmtutil --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}