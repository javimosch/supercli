{
  "plugin": "dpp",
  "binary": "dpp",
  "check": "which dpp",
  "install_steps": [
    "brew install dpp",
    "Verify: dpp --version",
    "supercli plugins install ./plugins/dpp --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}