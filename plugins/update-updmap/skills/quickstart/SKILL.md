---
name: update-updmap
description: Use this skill when the user wants to use update-updmap, cli tool: update-updmap.
---

# update-updmap Plugin

CLI tool: update-updmap.

## Commands
- `update-updmap <resource> <action>` — Execute update-updmap commands
- `update-updmap self version` — Print update-updmap version
- `update-updmap _ _` — Passthrough to update-updmap CLI

## Usage Examples
- "update-updmap --help"
- "update-updmap self version"

## Installation
```bash
apt-get install update-updmap 2>/dev/null || which update-updmap
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
