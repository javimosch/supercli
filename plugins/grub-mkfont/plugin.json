{
  "name": "grub-mkfont",
  "version": "0.1.0",
  "description": "CLI tool: grub-mkfont",
  "source": "https://manpages.debian.org/grub-mkfont",
  "checks": [
    {
      "type": "binary",
      "name": "grub-mkfont"
    }
  ],
  "install_guidance": {
    "plugin": "grub-mkfont",
    "binary": "grub-mkfont",
    "check": "which grub-mkfont",
    "install_steps": [
      "apt-get install grub-mkfont 2>/dev/null || which grub-mkfont",
      "Verify: grub-mkfont --version",
      "supercli plugins install ./plugins/grub-mkfont --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-mkfont",
      "resource": "self",
      "action": "version",
      "description": "Print grub-mkfont version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkfont",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-mkfont: apt-get install grub-mkfont 2>/dev/null || which grub-mkfont"
      },
      "args": []
    },
    {
      "namespace": "grub-mkfont",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-mkfont CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkfont",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-mkfont: apt-get install grub-mkfont 2>/dev/null || which grub-mkfont"
      },
      "args": []
    }
  ]
}