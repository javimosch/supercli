{
  "plugin": "foo2lava-wrapper",
  "binary": "foo2lava-wrapper",
  "check": "which foo2lava-wrapper",
  "install_steps": [
    "apt-get install foo2lava-wrapper 2>/dev/null || which foo2lava-wrapper",
    "Verify: foo2lava-wrapper --version",
    "supercli plugins install ./plugins/foo2lava-wrapper --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}