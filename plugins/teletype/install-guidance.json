{
  "plugin": "teletype",
  "binary": "teletype",
  "check": "which teletype",
  "install_steps": [
    "npm install -g teletype",
    "Verify: teletype --version",
    "supercli plugins install ./plugins/teletype --on-conflict replace --json"
  ]
}