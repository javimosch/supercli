{
  "plugin": "wikit",
  "binary": "wikit",
  "check": "which wikit",
  "install_steps": [
    "npm install -g wikit",
    "Verify: wikit --version",
    "supercli plugins install ./plugins/wikit --on-conflict replace --json"
  ]
}