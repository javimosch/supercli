{
  "plugin": "timg",
  "binary": "timg",
  "check": "which timg",
  "install_steps": [
    "brew install timg",
    "Verify: timg --version",
    "supercli plugins install ./plugins/timg --on-conflict replace --json"
  ]
}
