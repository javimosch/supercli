{
  "plugin": "hexdiff",
  "binary": "hexdiff",
  "check": "which hexdiff",
  "install_steps": [
    "which hexdiff",
    "Verify: hexdiff --version",
    "supercli plugins install ./plugins/hexdiff --on-conflict replace --json"
  ]
}
