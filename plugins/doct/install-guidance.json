{
  "plugin": "doct",
  "binary": "doct",
  "check": "which doct",
  "install_steps": [
    "# Install doct",
    "# See github.com/doct/doct",
    "# apt: sudo apt-get install doct",
    "# brew: brew install doct",
    "# cargo: cargo install doct",
    "# npm: npm install -g doct"
  ]
}