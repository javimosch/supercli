{ "plugin": "apt-mark", "binary": "apt-mark", "check": "which apt-mark", "install_steps": ["apt-get install apt-mark 2>/dev/null || which apt-mark", "Verify: apt-mark --version", "supercli plugins install ./plugins/apt-mark --on-conflict replace --json"] }
