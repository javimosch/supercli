{
  "plugin": "pslog",
  "binary": "pslog",
  "check": "which pslog",
  "install_steps": [
    "# Install pslog",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pslog-linux-amd64 -o /usr/local/bin/pslog",
    "chmod +x /usr/local/bin/pslog",
    "# Or via package manager, see /usr/bin/pslog"
  ]
}