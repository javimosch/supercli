{
  "plugin": "pslog",
  "binary": "pslog",
  "check": "which pslog",
  "install_steps": [
    "apt-get install pslog 2>/dev/null || which pslog",
    "Verify: pslog --version",
    "supercli plugins install ./plugins/pslog --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}