{ "plugin": "check-pci", "binary": "check-pci", "check": "which check-pci", "install_steps": ["apt-get install check-pci 2>/dev/null || which check-pci", "Verify: check-pci --version", "supercli plugins install ./plugins/check-pci --on-conflict replace --json"] }
