{
  "plugin": "grex",
  "binary": "grex",
  "check": "which grex",
  "install_steps": [
    "brew install grex",
    "Verify: grex --version",
    "supercli plugins install ./plugins/grex --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install grex. Can receive input from Unix pipelines. Supports Python bindings as a library. Converts test cases to regular expressions with various character class options."
}
