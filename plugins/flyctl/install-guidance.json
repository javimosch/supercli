{
  "plugin": "flyctl",
  "binary": "flyctl",
  "check": "which flyctl",
  "install_steps": [
    "brew install flyctl",
    "Verify: flyctl --version",
    "supercli plugins install ./plugins/flyctl --on-conflict replace --json"
  ]
}
