{
  "plugin": "dsq",
  "binary": "dsq",
  "check": "which dsq",
  "install_steps": [
    "brew install dsq",
    "Verify: dsq --version",
    "supercli plugins install ./plugins/dsq --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/multiprocessio/dsq@latest. Query JSON, CSV, Parquet, and other data files with SQL. No database needed. Supports JOINs, aggregations, and more."
}
