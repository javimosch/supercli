{
  "plugin": "pbibtex",
  "binary": "pbibtex",
  "check": "which pbibtex",
  "install_steps": [
    "apt-get install pbibtex 2>/dev/null || which pbibtex",
    "Verify: pbibtex --version",
    "supercli plugins install ./plugins/pbibtex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}