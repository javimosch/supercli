{
  "plugin": "kompose",
  "binary": "kompose",
  "check": "which kompose",
  "install_steps": [
    "curl -L https://github.com/kubernetes/kompose/releases/latest/download/kompose-linux-amd64 -o /usr/local/bin/kompose",
    "chmod +x /usr/local/bin/kompose",
    "Verify: kompose version",
    "supercli plugins install ./plugins/kompose --on-conflict replace --json"
  ],
  "note": "Also available via: brew install kompose, or 'go install github.com/kubernetes/kompose@latest', or download from GitHub releases (supports Linux, macOS, Windows). Part of the Kubernetes ecosystem (kubernetes.io)."
}
