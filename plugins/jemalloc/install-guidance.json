{
  "plugin": "jemalloc",
  "binary": "jemalloc",
  "check": "which jemalloc",
  "install_steps": [
    "brew install jemalloc",
    "Verify: jemalloc --version",
    "supercli plugins install ./plugins/jemalloc --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}