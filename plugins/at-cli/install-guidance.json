{ "plugin": "at-cli", "binary": "at-cli", "check": "which at-cli", "install_steps": ["apt-get install at-cli 2>/dev/null || which at-cli", "Verify: at-cli --version", "supercli plugins install ./plugins/at-cli --on-conflict replace --json"] }
