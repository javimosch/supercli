{
  "plugin": "spank",
  "binary": "spank",
  "check": "which spank",
  "install_steps": [
    "pip install spank",
    "Verify: spank --version",
    "supercli plugins install ./plugins/spank --on-conflict replace --json"
  ],
  "note": "Language: Go"
}