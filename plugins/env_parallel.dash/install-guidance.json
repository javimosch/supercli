{
  "plugin": "env_parallel.dash",
  "binary": "env_parallel.dash",
  "check": "which env_parallel.dash",
  "install_steps": [
    "apt-get install env_parallel.dash 2>/dev/null || which env_parallel.dash",
    "Verify: env_parallel.dash --version",
    "supercli plugins install ./plugins/env_parallel.dash --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}