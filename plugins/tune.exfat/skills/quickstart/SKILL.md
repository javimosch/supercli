---
name: tune.exfat
description: Use this skill when the user wants to use tune.exfat, cli tool: tune.exfat.
---

# tune.exfat Plugin

CLI tool: tune.exfat.

## Commands
- `tune.exfat <resource> <action>` — Execute tune.exfat commands
- `tune.exfat self version` — Print tune.exfat version
- `tune.exfat _ _` — Passthrough to tune.exfat CLI

## Usage Examples
- "tune.exfat --help"
- "tune.exfat self version"

## Installation
```bash
apt-get install tune.exfat 2>/dev/null || which tune.exfat
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
