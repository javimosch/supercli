{
  "plugin": "sshd",
  "binary": "sshd",
  "check": "which sshd",
  "install_steps": [
    "apt-get install sshd 2>/dev/null || which sshd",
    "Verify: sshd --version",
    "supercli plugins install ./plugins/sshd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}