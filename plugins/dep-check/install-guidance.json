{
  "plugin": "dep-check",
  "binary": "dep-check",
  "check": "which dep-check",
  "install_steps": ["npm install -g dep-check", "Verify: dep-check --version", "supercli plugins install ./plugins/dep-check --on-conflict replace --json"]
}
