{
  "plugin": "jump",
  "binary": "jump",
  "check": "which jump",
  "install_steps": [
    "brew install jump",
    "Verify: jump --version",
    "supercli plugins install ./plugins/jump --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}