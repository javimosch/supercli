{
  "plugin": "cargo-audit",
  "binary": "cargo-audit",
  "check": "which cargo-audit",
  "install_steps": [
    "cargo install cargo-audit",
    "Verify: cargo audit --version",
    "supercli plugins install ./plugins/cargo-audit --on-conflict replace --json"
  ],
  "note": "Run via: cargo audit. Requires Rust toolchain."
}
