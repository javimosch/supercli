{
  "plugin": "glslang",
  "binary": "glslang",
  "check": "which glslang",
  "install_steps": [
    "brew install glslang",
    "Verify: glslang --version",
    "supercli plugins install ./plugins/glslang --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}