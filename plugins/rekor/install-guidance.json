{ "plugin": "rekor", "binary": "rekor", "check": "which rekor", "install_steps": ["apt-get install rekor", "Verify: rekor --version", "supercli plugins install ./plugins/rekor --on-conflict replace --json"] }
