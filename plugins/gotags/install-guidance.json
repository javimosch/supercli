{
  "plugin": "gotags",
  "binary": "gotags",
  "check": "which gotags",
  "install_steps": [
    "brew install gotags",
    "Verify: gotags --version",
    "supercli plugins install ./plugins/gotags --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}