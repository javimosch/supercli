{
  "plugin": "sqlcmd",
  "binary": "sqlcmd",
  "check": "which sqlcmd",
  "install_steps": [
    "npm install -g sqlcmd",
    "Verify: sqlcmd --version"
  ],
  "note": "Also available via: brew install sqlcmd (macOS), or download from Microsoft. The npm package is the go-sqlcmd CLI."
}
