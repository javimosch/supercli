{
  "plugin": "jinfo",
  "binary": "jinfo",
  "check": "which jinfo",
  "install_steps": [
    "apt-get install jinfo 2>/dev/null || which jinfo",
    "Verify: jinfo --version",
    "supercli plugins install ./plugins/jinfo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}