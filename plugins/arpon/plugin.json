{
  "name": "arpon",
  "version": "0.1.0",
  "description": "CLI tool: arpon",
  "source": "https://packages.debian.org/arpon",
  "checks": [{ "type": "binary", "name": "arpon" }],
  "install_guidance": {
    "plugin": "arpon", "binary": "arpon", "check": "which arpon",
    "install_steps": ["apt-get install arpon 2>/dev/null || which arpon", "Verify: arpon --version", "supercli plugins install ./plugins/arpon --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "arpon", "resource": "_", "action": "_",
    "description": "Passthrough to arpon CLI",
    "adapter": "process", "adapterConfig": { "command": "arpon", "passthrough": true, "missingDependencyHelp": "Install arpon: apt-get install arpon" }, "args": []
  }]
}
