{ "plugin": "arpon", "binary": "arpon", "check": "which arpon", "install_steps": ["apt-get install arpon 2>/dev/null || which arpon", "Verify: arpon --version", "supercli plugins install ./plugins/arpon --on-conflict replace --json"] }
