{
  "plugin": "qemu-io",
  "binary": "qemu-io",
  "check": "which qemu-io",
  "install_steps": [
    "apt-get install qemu-io 2>/dev/null || which qemu-io",
    "Verify: qemu-io --version",
    "supercli plugins install ./plugins/qemu-io --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}