{
  "plugin": "pctl",
  "binary": "pctl",
  "check": "which pctl",
  "install_steps": ["brew install pctl", "Verify: pctl --version", "supercli plugins install ./plugins/pctl --on-conflict replace --json"]
}
