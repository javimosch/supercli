{ "plugin": "dhall-to-nix", "binary": "dhall-to-nix", "check": "which dhall-to-nix", "install_steps": ["apt-get install dhall-to-nix 2>/dev/null || which dhall-to-nix", "Verify: dhall-to-nix --version", "supercli plugins install ./plugins/dhall-to-nix --on-conflict replace --json"] }
