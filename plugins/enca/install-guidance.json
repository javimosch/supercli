{
  "plugin": "enca",
  "binary": "enca",
  "check": "which enca",
  "install_steps": [
    "apt-get install enca",
    "Verify: enca --version",
    "supercli plugins install ./plugins/enca --on-conflict replace --json"
  ]
}
