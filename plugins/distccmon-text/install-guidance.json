{ "plugin": "distccmon-text", "binary": "distccmon-text", "check": "which distccmon-text", "install_steps": ["apt-get install distccmon-text 2>/dev/null || which distccmon-text", "Verify: distccmon-text --version", "supercli plugins install ./plugins/distccmon-text --on-conflict replace --json"] }
