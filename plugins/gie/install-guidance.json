{
  "plugin": "gie",
  "binary": "gie",
  "check": "which gie",
  "install_steps": [
    "apt-get install gie 2>/dev/null || which gie",
    "Verify: gie --version",
    "supercli plugins install ./plugins/gie --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}