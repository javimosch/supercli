{ "plugin": "cups-config", "binary": "cups-config", "check": "which cups-config", "install_steps": ["apt-get install cups-config 2>/dev/null || which cups-config", "Verify: cups-config --version", "supercli plugins install ./plugins/cups-config --on-conflict replace --json"] }
