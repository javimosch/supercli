{
  "name": "check_journal",
  "version": "0.1.0",
  "description": "CLI tool: check_journal",
  "source": "https://packages.debian.org/check_journal",
  "checks": [{ "type": "binary", "name": "check_journal" }],
  "install_guidance": {
    "plugin": "check_journal", "binary": "check_journal", "check": "which check_journal",
    "install_steps": ["apt-get install check_journal 2>/dev/null || which check_journal", "Verify: check_journal --version", "supercli plugins install ./plugins/check_journal --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check_journal", "resource": "_", "action": "_",
    "description": "Passthrough to check_journal CLI",
    "adapter": "process", "adapterConfig": { "command": "check_journal", "passthrough": true, "missingDependencyHelp": "Install check_journal: apt-get install check_journal" }, "args": []
  }]
}
