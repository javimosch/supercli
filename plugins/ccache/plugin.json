{
  "name": "ccache",
  "version": "0.1.0",
  "description": "CLI tool: ccache",
  "source": "https://packages.debian.org/ccache",
  "checks": [{ "type": "binary", "name": "ccache" }],
  "install_guidance": {
    "plugin": "ccache", "binary": "ccache", "check": "which ccache",
    "install_steps": ["apt-get install ccache 2>/dev/null || which ccache", "Verify: ccache --version", "supercli plugins install ./plugins/ccache --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ccache", "resource": "_", "action": "_",
    "description": "Passthrough to ccache CLI",
    "adapter": "process", "adapterConfig": { "command": "ccache", "passthrough": true, "missingDependencyHelp": "Install ccache: apt-get install ccache" }, "args": []
  }]
}
