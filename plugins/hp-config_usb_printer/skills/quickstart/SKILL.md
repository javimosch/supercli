---
name: hp-config_usb_printer
description: Use this skill when the user wants to use hp-config_usb_printer, cli tool: hp-config_usb_printer.
---

# hp-config_usb_printer Plugin

CLI tool: hp-config_usb_printer.

## Commands
- `hp-config_usb_printer <resource> <action>` — Execute hp-config_usb_printer commands
- `hp-config_usb_printer self version` — Print hp-config_usb_printer version
- `hp-config_usb_printer _ _` — Passthrough to hp-config_usb_printer CLI

## Usage Examples
- "hp-config_usb_printer --help"
- "hp-config_usb_printer self version"

## Installation
```bash
apt-get install hp-config_usb_printer 2>/dev/null || which hp-config_usb_printer
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
