{
  "name": "hp-config_usb_printer",
  "version": "0.1.0",
  "description": "CLI tool: hp-config_usb_printer",
  "source": "https://manpages.debian.org/hp-config_usb_printer",
  "checks": [
    {
      "type": "binary",
      "name": "hp-config_usb_printer"
    }
  ],
  "install_guidance": {
    "plugin": "hp-config_usb_printer",
    "binary": "hp-config_usb_printer",
    "check": "which hp-config_usb_printer",
    "install_steps": [
      "apt-get install hp-config_usb_printer 2>/dev/null || which hp-config_usb_printer",
      "Verify: hp-config_usb_printer --version",
      "supercli plugins install ./plugins/hp-config_usb_printer --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "hp-config_usb_printer",
      "resource": "self",
      "action": "version",
      "description": "Print hp-config_usb_printer version",
      "adapter": "process",
      "adapterConfig": {
        "command": "hp-config_usb_printer",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install hp-config_usb_printer: apt-get install hp-config_usb_printer 2>/dev/null || which hp-config_usb_printer"
      },
      "args": []
    },
    {
      "namespace": "hp-config_usb_printer",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to hp-config_usb_printer CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "hp-config_usb_printer",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install hp-config_usb_printer: apt-get install hp-config_usb_printer 2>/dev/null || which hp-config_usb_printer"
      },
      "args": []
    }
  ]
}