{ "plugin": "dh_compress", "binary": "dh_compress", "check": "which dh_compress", "install_steps": ["apt-get install dh_compress 2>/dev/null || which dh_compress", "Verify: dh_compress --version", "supercli plugins install ./plugins/dh_compress --on-conflict replace --json"] }
