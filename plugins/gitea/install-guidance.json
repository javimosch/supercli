{
  "plugin": "gitea",
  "binary": "gitea",
  "check": "which gitea",
  "install_steps": [
    "brew install gitea",
    "Verify: gitea --version",
    "supercli plugins install ./plugins/gitea --on-conflict replace --json"
  ]
}