{
  "name": "dbus-cleanup-sockets",
  "version": "1.0.0",
  "description": "dbus-cleanup-sockets \u2014 dbus-cleanup-sockets \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dbus-cleanup-sockets"
    }
  ],
  "install_guidance": {
    "plugin": "dbus-cleanup-sockets",
    "binary": "dbus-cleanup-sockets",
    "check": "which dbus-cleanup-sockets",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dbus-cleanup-sockets",
      "resource": "run",
      "action": "exec",
      "description": "dbus-cleanup-sockets \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dbus-cleanup-sockets",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}