{
  "plugin": "IRCClient",
  "binary": "IRCClient",
  "check": "which IRCClient",
  "install_steps": ["g++ IRCClient.cpp -o ircclient", "Verify: IRCClient --version", "supercli plugins install ./plugins/IRCClient --on-conflict replace --json"]
}
