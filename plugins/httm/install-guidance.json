{
  "plugin": "httm",
  "binary": "httm",
  "check": "which httm",
  "install_steps": [
    "brew install httm",
    "Verify: httm --version",
    "supercli plugins install ./plugins/httm --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install httm. File-level Time Machine-like tool for ZFS and btrfs. Browse, compare, and restore file snapshots directly from the terminal. Requires ZFS or btrfs filesystem."
}
