{
  "plugin": "lxc-ls",
  "binary": "lxc-ls",
  "check": "which lxc-ls",
  "install_steps": [
    "apt-get install lxc-ls 2>/dev/null || which lxc-ls",
    "Verify: lxc-ls --version",
    "supercli plugins install ./plugins/lxc-ls --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}