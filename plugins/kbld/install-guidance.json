{
  "plugin": "kbld",
  "binary": "kbld",
  "check": "which kbld",
  "install_steps": ["brew install kbld", "Verify: kbld --version", "supercli plugins install ./plugins/kbld --on-conflict replace --json"]
}
