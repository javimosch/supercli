{
  "plugin": "gtester",
  "binary": "gtester",
  "check": "which gtester",
  "install_steps": [
    "apt-get install gtester 2>/dev/null || which gtester",
    "Verify: gtester --version",
    "supercli plugins install ./plugins/gtester --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}