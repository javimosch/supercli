{
  "plugin": "rem",
  "binary": "rem",
  "check": "which rem",
  "install_steps": [
    "brew install rem",
    "Verify: rem --version",
    "supercli plugins install ./plugins/rem --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}