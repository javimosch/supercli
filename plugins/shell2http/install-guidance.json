{
  "plugin": "shell2http",
  "binary": "shell2http",
  "check": "which shell2http",
  "install_steps": [
    "brew install shell2http",
    "Verify: shell2http --version",
    "supercli plugins install ./plugins/shell2http --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/msoap/shell2http@latest. Expose shell commands as HTTP endpoints. Great for creating quick APIs, webhooks, and integrating shell scripts with HTTP-based services."
}
