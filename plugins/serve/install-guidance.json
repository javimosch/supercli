{
  "plugin": "serve",
  "binary": "serve",
  "check": "which serve",
  "install_steps": [
    "npm install -g serve",
    "Verify: serve --version",
    "supercli plugins install ./plugins/serve --on-conflict replace --json"
  ],
  "note": "Serve is a production-ready static file server from Vercel. Defaults to port 3000. Supports SPA mode with --single flag, directory listing, and CORS."
}
