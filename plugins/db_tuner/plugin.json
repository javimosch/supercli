{
  "name": "db_tuner",
  "version": "0.1.0",
  "description": "CLI tool: db_tuner",
  "source": "https://packages.debian.org/db_tuner",
  "checks": [{ "type": "binary", "name": "db_tuner" }],
  "install_guidance": {
    "plugin": "db_tuner", "binary": "db_tuner", "check": "which db_tuner",
    "install_steps": ["apt-get install db_tuner 2>/dev/null || which db_tuner", "Verify: db_tuner --version", "supercli plugins install ./plugins/db_tuner --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_tuner", "resource": "_", "action": "_",
    "description": "Passthrough to db_tuner CLI",
    "adapter": "process", "adapterConfig": { "command": "db_tuner", "passthrough": true, "missingDependencyHelp": "Install db_tuner: apt-get install db_tuner" }, "args": []
  }]
}
