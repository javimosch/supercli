{ "plugin": "db_tuner", "binary": "db_tuner", "check": "which db_tuner", "install_steps": ["apt-get install db_tuner 2>/dev/null || which db_tuner", "Verify: db_tuner --version", "supercli plugins install ./plugins/db_tuner --on-conflict replace --json"] }
