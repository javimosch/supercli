{
  "plugin": "progressline",
  "binary": "progressline",
  "check": "which progressline",
  "install_steps": ["pip install progressline", "Verify: progressline --version", "supercli plugins install ./plugins/progressline --on-conflict replace --json"]
}
