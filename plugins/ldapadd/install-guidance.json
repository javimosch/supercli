{
  "plugin": "ldapadd",
  "binary": "ldapadd",
  "check": "which ldapadd",
  "install_steps": [
    "apt-get install ldapadd 2>/dev/null || which ldapadd",
    "Verify: ldapadd --version",
    "supercli plugins install ./plugins/ldapadd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}