{ "plugin": "montage", "binary": "montage", "check": "which montage", "install_steps": ["apt-get install imagemagick", "Verify: montage --version", "supercli plugins install ./plugins/montage --on-conflict replace --json"] }
