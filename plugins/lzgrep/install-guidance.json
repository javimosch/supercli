{
  "plugin": "lzgrep",
  "binary": "lzgrep",
  "check": "which lzgrep",
  "install_steps": [
    "apt-get install lzgrep 2>/dev/null || which lzgrep",
    "Verify: lzgrep --version",
    "supercli plugins install ./plugins/lzgrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}