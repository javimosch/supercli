{
  "plugin": "changeset",
  "binary": "changeset",
  "check": "which changeset",
  "install_steps": ["npm install -g @changesets/cli", "Verify: changeset --version", "supercli plugins install ./plugins/changeset --on-conflict replace --json"]
}
