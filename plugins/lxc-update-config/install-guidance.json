{
  "plugin": "lxc-update-config",
  "binary": "lxc-update-config",
  "check": "which lxc-update-config",
  "install_steps": [
    "apt-get install lxc-update-config 2>/dev/null || which lxc-update-config",
    "Verify: lxc-update-config --version",
    "supercli plugins install ./plugins/lxc-update-config --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}