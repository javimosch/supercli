{
  "plugin": "pnmsplit",
  "binary": "pnmsplit",
  "check": "which pnmsplit",
  "install_steps": [
    "apt-get install pnmsplit 2>/dev/null || which pnmsplit",
    "Verify: pnmsplit --version",
    "supercli plugins install ./plugins/pnmsplit --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}