{
  "plugin": "icu4c",
  "binary": "icu4c",
  "check": "which icu4c",
  "install_steps": [
    "brew install icu4c",
    "Verify: icu4c --version",
    "supercli plugins install ./plugins/icu4c --on-conflict replace --json"
  ]
}
