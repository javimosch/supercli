{
  "plugin": "hp-query",
  "binary": "hp-query",
  "check": "which hp-query",
  "install_steps": [
    "apt-get install hp-query 2>/dev/null || which hp-query",
    "Verify: hp-query --version",
    "supercli plugins install ./plugins/hp-query --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}