{
  "plugin": "e2fsck",
  "binary": "e2fsck",
  "check": "which e2fsck",
  "install_steps": [
    "apt-get install e2fsck 2>/dev/null || which e2fsck",
    "Verify: e2fsck --version",
    "supercli plugins install ./plugins/e2fsck --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}