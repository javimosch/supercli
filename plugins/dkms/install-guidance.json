{ "plugin": "dkms", "binary": "dkms", "check": "which dkms", "install_steps": ["apt-get install dkms 2>/dev/null || which dkms", "Verify: dkms --version", "supercli plugins install ./plugins/dkms --on-conflict replace --json"] }
