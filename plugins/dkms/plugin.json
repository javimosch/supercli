{
  "name": "dkms",
  "version": "0.1.0",
  "description": "CLI tool: dkms",
  "source": "https://packages.debian.org/dkms",
  "checks": [{ "type": "binary", "name": "dkms" }],
  "install_guidance": {
    "plugin": "dkms", "binary": "dkms", "check": "which dkms",
    "install_steps": ["apt-get install dkms 2>/dev/null || which dkms", "Verify: dkms --version", "supercli plugins install ./plugins/dkms --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dkms", "resource": "_", "action": "_",
    "description": "Passthrough to dkms CLI",
    "adapter": "process", "adapterConfig": { "command": "dkms", "passthrough": true, "missingDependencyHelp": "Install dkms: apt-get install dkms" }, "args": []
  }]
}
