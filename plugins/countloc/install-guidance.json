{
  "plugin": "countloc",
  "binary": "countloc",
  "check": "which countloc",
  "install_steps": ["cargo install countloc", "Verify: countloc --version", "supercli plugins install ./plugins/countloc --on-conflict replace --json"]
}
