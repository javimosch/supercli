---
name: SlipNet
description: Use this skill when the user wants to use SlipNet for Android VPN client with DNS tunneling (DNSTT, NoizDNS & Slipstream), NaiveProxy,.
---

# SlipNet Plugin

Android VPN client with DNS tunneling (DNSTT, NoizDNS & Slipstream), NaiveProxy, SSH, Tor, and DoH support — featuring a built-in DNS scanner.

## Commands

### General
- `SlipNet self version` — Print SlipNet version
- `SlipNet _ _` — Passthrough to slipnet CLI

## Usage Examples
- "Run SlipNet"
- "Use slipnet to Android VPN client with DNS tunneling (DNSTT, NoizDNS & Slip"

## Installation

```bash
pip install slipnet
```

## Key Features
- Android VPN client with DNS tunneling (DNSTT, NoizDNS & Slipstream), NaiveProxy, SSH, Tor, and DoH s
- CLI tool, headless and scriptable
