{
  "plugin": "git-flow",
  "binary": "git-flow",
  "check": "which git-flow",
  "install_steps": [
    "brew install git-flow",
    "Verify: git-flow --version",
    "supercli plugins install ./plugins/git-flow --on-conflict replace --json"
  ]
}
