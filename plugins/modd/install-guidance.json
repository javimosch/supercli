{
  "plugin": "modd",
  "binary": "modd",
  "check": "which modd",
  "install_steps": [
    "brew install modd",
    "Verify: modd --version",
    "supercli plugins install ./plugins/modd --on-conflict replace --json"
  ]
}