{
  "plugin": "marked",
  "binary": "marked",
  "check": "which marked",
  "install_steps": [
    "npm install -g marked",
    "Verify: marked --version",
    "supercli plugins install ./plugins/marked --on-conflict replace --json"
  ],
  "note": "Marked is a fast markdown parser and compiler. Supports CommonMark and GitHub Flavored Markdown. Can read from stdin or file, output to stdout or file. Use -o for output file."
}
