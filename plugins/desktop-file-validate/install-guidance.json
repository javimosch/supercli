{ "plugin": "desktop-file-validate", "binary": "desktop-file-validate", "check": "which desktop-file-validate", "install_steps": ["apt-get install desktop-file-validate 2>/dev/null || which desktop-file-validate", "Verify: desktop-file-validate --version", "supercli plugins install ./plugins/desktop-file-validate --on-conflict replace --json"] }
