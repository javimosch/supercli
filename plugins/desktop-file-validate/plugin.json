{
  "name": "desktop-file-validate",
  "version": "0.1.0",
  "description": "CLI tool: desktop-file-validate",
  "source": "https://packages.debian.org/desktop-file-validate",
  "checks": [{ "type": "binary", "name": "desktop-file-validate" }],
  "install_guidance": {
    "plugin": "desktop-file-validate", "binary": "desktop-file-validate", "check": "which desktop-file-validate",
    "install_steps": ["apt-get install desktop-file-validate 2>/dev/null || which desktop-file-validate", "Verify: desktop-file-validate --version", "supercli plugins install ./plugins/desktop-file-validate --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "desktop-file-validate", "resource": "_", "action": "_",
    "description": "Passthrough to desktop-file-validate CLI",
    "adapter": "process", "adapterConfig": { "command": "desktop-file-validate", "passthrough": true, "missingDependencyHelp": "Install desktop-file-validate: apt-get install desktop-file-validate" }, "args": []
  }]
}
