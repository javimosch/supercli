{
  "plugin": "pulumi",
  "binary": "pulumi",
  "check": "which pulumi",
  "install_steps": [
    "brew install pulumi",
    "Verify: pulumi --version",
    "supercli plugins install ./plugins/pulumi --on-conflict replace --json"
  ]
}