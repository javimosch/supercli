{
  "plugin": "pasuspender",
  "binary": "pasuspender",
  "check": "which pasuspender",
  "install_steps": [
    "apt-get install pasuspender 2>/dev/null || which pasuspender",
    "Verify: pasuspender --version",
    "supercli plugins install ./plugins/pasuspender --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}