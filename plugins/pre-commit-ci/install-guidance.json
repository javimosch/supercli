{
  "plugin": "pre-commit-ci",
  "binary": "pre-commit",
  "check": "which pre-commit",
  "install_steps": ["pip install pre-commit", "Verify: pre-commit --version", "supercli plugins install ./plugins/pre-commit-ci --on-conflict replace --json"]
}
