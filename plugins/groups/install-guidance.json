{
  "plugin": "groups",
  "binary": "groups",
  "check": "which groups",
  "install_steps": [
    "apt-get install groups 2>/dev/null || which groups",
    "Verify: groups --version",
    "supercli plugins install ./plugins/groups --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}