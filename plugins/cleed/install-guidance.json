{
  "plugin": "cleed",
  "binary": "cleed",
  "check": "which cleed",
  "install_steps": [
    "go install github.com/radulucut/cleed@latest",
    "Verify: cleed --version",
    "supercli plugins install ./plugins/cleed --on-conflict replace --json"
  ]
}