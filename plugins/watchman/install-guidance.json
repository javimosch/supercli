{
  "plugin": "watchman",
  "binary": "watchman",
  "check": "which watchman",
  "install_steps": [
    "brew install watchman",
    "Or: apt-get install watchman (Linux)",
    "Verify: watchman --version",
    "supercli plugins install ./plugins/watchman --on-conflict replace --json"
  ],
  "note": "Watchman watches files and records changes, and can trigger actions when files change. Used extensively by React Native, Buck, and other large build systems. Requires the watchman daemon to be running."
}
