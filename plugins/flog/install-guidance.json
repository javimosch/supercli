{
  "plugin": "flog",
  "binary": "flog",
  "check": "which flog",
  "install_steps": [
    "See https://github.com/mingrammer/flog",
    "Verify: flog --version",
    "supercli plugins install ./plugins/flog --on-conflict replace --json"
  ],
  "note": "See project repository"
}