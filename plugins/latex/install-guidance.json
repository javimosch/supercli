{
  "plugin": "latex",
  "binary": "latex",
  "check": "which latex",
  "install_steps": [
    "apt-get install latex 2>/dev/null || which latex",
    "Verify: latex --version",
    "supercli plugins install ./plugins/latex --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}