{
  "plugin": "lxc-attach",
  "binary": "lxc-attach",
  "check": "which lxc-attach",
  "install_steps": [
    "apt-get install lxc-attach 2>/dev/null || which lxc-attach",
    "Verify: lxc-attach --version",
    "supercli plugins install ./plugins/lxc-attach --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}