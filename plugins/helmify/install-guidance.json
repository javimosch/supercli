{
  "plugin": "helmify",
  "binary": "helmify",
  "check": "which helmify",
  "install_steps": [
    "brew install helmify",
    "Verify: helmify --version",
    "supercli plugins install ./plugins/helmify --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}