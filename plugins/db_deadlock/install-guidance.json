{ "plugin": "db_deadlock", "binary": "db_deadlock", "check": "which db_deadlock", "install_steps": ["apt-get install db_deadlock 2>/dev/null || which db_deadlock", "Verify: db_deadlock --version", "supercli plugins install ./plugins/db_deadlock --on-conflict replace --json"] }
