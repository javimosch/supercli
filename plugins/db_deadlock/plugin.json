{
  "name": "db_deadlock",
  "version": "0.1.0",
  "description": "CLI tool: db_deadlock",
  "source": "https://packages.debian.org/db_deadlock",
  "checks": [{ "type": "binary", "name": "db_deadlock" }],
  "install_guidance": {
    "plugin": "db_deadlock", "binary": "db_deadlock", "check": "which db_deadlock",
    "install_steps": ["apt-get install db_deadlock 2>/dev/null || which db_deadlock", "Verify: db_deadlock --version", "supercli plugins install ./plugins/db_deadlock --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_deadlock", "resource": "_", "action": "_",
    "description": "Passthrough to db_deadlock CLI",
    "adapter": "process", "adapterConfig": { "command": "db_deadlock", "passthrough": true, "missingDependencyHelp": "Install db_deadlock: apt-get install db_deadlock" }, "args": []
  }]
}
