{ "plugin": "devprobe", "binary": "devprobe", "check": "which devprobe", "install_steps": ["apt-get install devprobe 2>/dev/null || which devprobe", "Verify: devprobe --version", "supercli plugins install ./plugins/devprobe --on-conflict replace --json"] }
