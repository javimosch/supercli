{
  "plugin": "flux-check",
  "binary": "flux-check",
  "check": "which flux-check",
  "install_steps": ["brew install flux-check", "Verify: flux-check --version", "supercli plugins install ./plugins/flux-check --on-conflict replace --json"]
}
