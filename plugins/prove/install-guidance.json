{
  "plugin": "prove",
  "binary": "prove",
  "check": "which prove",
  "install_steps": [
    "# Install prove",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/prove-linux-amd64 -o /usr/local/bin/prove",
    "chmod +x /usr/local/bin/prove",
    "# Or via package manager, see /usr/bin/prove"
  ]
}