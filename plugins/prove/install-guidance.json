{
  "plugin": "prove",
  "binary": "prove",
  "check": "which prove",
  "install_steps": [
    "apt-get install prove 2>/dev/null || which prove",
    "Verify: prove --version",
    "supercli plugins install ./plugins/prove --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}