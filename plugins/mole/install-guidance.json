{
  "plugin": "mole",
  "binary": "mole",
  "check": "which mole",
  "install_steps": [
    "brew install mole",
    "Verify: mole --version",
    "supercli plugins install ./plugins/mole --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/davrodpin/mole@latest. Creates SSH tunnels with automatic reconnection and configuration file support. Supports local, remote, and dynamic port forwarding."
}
