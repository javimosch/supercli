{ "plugin": "agent-list", "binary": "agent-list", "check": "which agent-list", "install_steps": ["apt-get install agent-list 2>/dev/null || which agent-list", "Verify: agent-list --version", "supercli plugins install ./plugins/agent-list --on-conflict replace --json"] }
