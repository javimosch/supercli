{
  "plugin": "roff2x",
  "binary": "roff2x",
  "check": "which roff2x",
  "install_steps": [
    "apt-get install roff2x 2>/dev/null || which roff2x",
    "Verify: roff2x --version",
    "supercli plugins install ./plugins/roff2x --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}