{
  "plugin": "just-command",
  "binary": "just",
  "check": "which just",
  "install_steps": [
    "cargo install just",
    "Verify: just --version",
    "supercli plugins install ./plugins/just-command --on-conflict replace --json"
  ],
  "note": "Binary name: just"
}
