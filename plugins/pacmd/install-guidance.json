{
  "plugin": "pacmd",
  "binary": "pacmd",
  "check": "which pacmd",
  "install_steps": [
    "apt-get install pacmd 2>/dev/null || which pacmd",
    "Verify: pacmd --version",
    "supercli plugins install ./plugins/pacmd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}