{
  "plugin": "Xvfb",
  "binary": "Xvfb",
  "check": "which Xvfb",
  "install_steps": [
    "apt-get install Xvfb 2>/dev/null || which Xvfb",
    "Verify: Xvfb --version",
    "supercli plugins install ./plugins/Xvfb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}