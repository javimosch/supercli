{
  "name": "rec",
  "version": "0.1.0",
  "description": "Audio recording tool",
  "source": "https://packages.debian.org/rec",
  "checks": [{ "type": "binary", "name": "rec" }],
  "install_guidance": {
    "plugin": "rec", "binary": "rec", "check": "which rec",
    "install_steps": ["apt-get install sox", "Verify: rec --version", "supercli plugins install ./plugins/rec --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "rec", "resource": "_", "action": "_",
    "description": "Passthrough to rec CLI",
    "adapter": "process", "adapterConfig": { "command": "rec", "passthrough": true, "missingDependencyHelp": "Install rec: apt-get install sox" }, "args": []
  }]
}
