{
  "plugin": "update-mime",
  "binary": "update-mime",
  "check": "which update-mime",
  "install_steps": [
    "apt-get install update-mime 2>/dev/null || which update-mime",
    "Verify: update-mime --version",
    "supercli plugins install ./plugins/update-mime --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}