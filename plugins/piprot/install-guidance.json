{
  "plugin": "piprot",
  "binary": "piprot",
  "check": "which piprot",
  "install_steps": ["pip install piprot", "Verify: piprot --version", "supercli plugins install ./plugins/piprot --on-conflict replace --json"]
}
