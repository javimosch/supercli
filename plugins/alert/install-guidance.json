{
  "plugin": "alert",
  "binary": "alert",
  "check": "which alert",
  "install_steps": [
    "npm install -g alert",
    "Verify: alert --version",
    "supercli plugins install ./plugins/alert --on-conflict replace --json"
  ]
}
