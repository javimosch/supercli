{
  "name": "cdrdao",
  "version": "0.1.0",
  "description": "CLI tool: cdrdao",
  "source": "https://packages.debian.org/cdrdao",
  "checks": [{ "type": "binary", "name": "cdrdao" }],
  "install_guidance": {
    "plugin": "cdrdao", "binary": "cdrdao", "check": "which cdrdao",
    "install_steps": ["apt-get install cdrdao 2>/dev/null || which cdrdao", "Verify: cdrdao --version", "supercli plugins install ./plugins/cdrdao --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cdrdao", "resource": "_", "action": "_",
    "description": "Passthrough to cdrdao CLI",
    "adapter": "process", "adapterConfig": { "command": "cdrdao", "passthrough": true, "missingDependencyHelp": "Install cdrdao: apt-get install cdrdao" }, "args": []
  }]
}
