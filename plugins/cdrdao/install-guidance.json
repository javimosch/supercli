{ "plugin": "cdrdao", "binary": "cdrdao", "check": "which cdrdao", "install_steps": ["apt-get install cdrdao 2>/dev/null || which cdrdao", "Verify: cdrdao --version", "supercli plugins install ./plugins/cdrdao --on-conflict replace --json"] }
