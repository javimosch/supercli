{
  "plugin": "repo-radar-cli",
  "binary": "repo-radar",
  "check": "which repo-radar",
  "install_steps": [
    "cargo install repo-radar-cli",
    "Verify: repo-radar --version",
    "supercli plugins install ./plugins/repo-radar-cli --on-conflict replace --json"
  ],
  "note": "Binary name: repo-radar"
}
