{
  "plugin": "kube-linter",
  "binary": "kube-linter",
  "check": "which kube-linter",
  "install_steps": [
    "brew install kube-linter",
    "Verify: kube-linter --version",
    "supercli plugins install ./plugins/kube-linter --on-conflict replace --json"
  ]
}
