{
  "plugin": "gpg",
  "binary": "gpg",
  "check": "which gpg",
  "install_steps": [
    "brew install gpg",
    "Verify: gpg --version",
    "supercli plugins install ./plugins/gpg --on-conflict replace --json"
  ]
}
