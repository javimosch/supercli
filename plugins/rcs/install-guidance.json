{
  "plugin": "rcs",
  "binary": "rcs",
  "check": "which rcs",
  "install_steps": [
    "apt-get install rcs 2>/dev/null || which rcs",
    "Verify: rcs --version",
    "supercli plugins install ./plugins/rcs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}