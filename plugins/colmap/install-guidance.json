{
  "plugin": "colmap",
  "binary": "colmap",
  "check": "which colmap",
  "install_steps": [
    "brew install colmap",
    "Verify: colmap --version",
    "supercli plugins install ./plugins/colmap --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}