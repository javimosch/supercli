{
  "plugin": "slogin",
  "binary": "slogin",
  "check": "which slogin",
  "install_steps": [
    "apt-get install slogin 2>/dev/null || which slogin",
    "Verify: slogin --version",
    "supercli plugins install ./plugins/slogin --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}