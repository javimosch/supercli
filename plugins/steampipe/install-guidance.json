{
  "plugin": "steampipe",
  "binary": "steampipe",
  "check": "which steampipe",
  "install_steps": [
    "brew install turbot/tap/steampipe",
    "Verify: steampipe --version",
    "supercli plugins install ./plugins/steampipe --on-conflict replace --json"
  ],
  "note": "Also available via: curl -fsSL https://steampipe.io/install/steampipe.sh | sh. Install plugins: steampipe plugin install aws (or gcp, azure, github, etc.). Authenticate via your cloud provider's standard credentials."
}
