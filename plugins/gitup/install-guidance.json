{
  "plugin": "gitup",
  "binary": "gitup",
  "check": "which gitup",
  "install_steps": [
    "brew install gitup",
    "Verify: gitup --version",
    "supercli plugins install ./plugins/gitup --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}