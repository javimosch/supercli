{
  "plugin": "dicer",
  "binary": "dicer",
  "check": "which dicer",
  "install_steps": ["curl -sSL url/dicer -o /usr/local/bin/dicer", "Verify: dicer --version", "supercli plugins install ./plugins/dicer --on-conflict replace --json"]
}
