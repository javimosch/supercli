{ "plugin": "clusterdb", "binary": "clusterdb", "check": "which clusterdb", "install_steps": ["apt-get install clusterdb 2>/dev/null || which clusterdb", "Verify: clusterdb --version", "supercli plugins install ./plugins/clusterdb --on-conflict replace --json"] }
