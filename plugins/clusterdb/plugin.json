{
  "name": "clusterdb",
  "version": "0.1.0",
  "description": "CLI tool: clusterdb",
  "source": "https://packages.debian.org/clusterdb",
  "checks": [{ "type": "binary", "name": "clusterdb" }],
  "install_guidance": {
    "plugin": "clusterdb", "binary": "clusterdb", "check": "which clusterdb",
    "install_steps": ["apt-get install clusterdb 2>/dev/null || which clusterdb", "Verify: clusterdb --version", "supercli plugins install ./plugins/clusterdb --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "clusterdb", "resource": "_", "action": "_",
    "description": "Passthrough to clusterdb CLI",
    "adapter": "process", "adapterConfig": { "command": "clusterdb", "passthrough": true, "missingDependencyHelp": "Install clusterdb: apt-get install clusterdb" }, "args": []
  }]
}
