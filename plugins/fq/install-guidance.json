{
  "plugin": "fq",
  "binary": "fq",
  "check": "which fq",
  "install_steps": [
    "brew install fq",
    "Verify: fq --version",
    "supercli plugins install ./plugins/fq --on-conflict replace --json"
  ],
  "note": "Also available via pre-built binaries. Supports many binary formats (pcap, mp4, png, etc.). Uses jq-like query syntax. Can decode, transform, and re-encode binary data. REPL mode available for interactive exploration."
}
