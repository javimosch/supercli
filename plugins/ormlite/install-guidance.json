{
  "plugin": "ormlite",
  "binary": "ormlite",
  "check": "which ormlite",
  "install_steps": ["cargo install ormlite-cli", "Verify: ormlite --version", "supercli plugins install ./plugins/ormlite --on-conflict replace --json"]
}
