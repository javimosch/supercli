{
  "plugin": "coq",
  "binary": "coq",
  "check": "which coq",
  "install_steps": [
    "brew install coq",
    "Verify: coq --version",
    "supercli plugins install ./plugins/coq --on-conflict replace --json"
  ]
}
