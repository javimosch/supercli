{
  "plugin": "x-terminal-emulator",
  "binary": "x-terminal-emulator",
  "check": "which x-terminal-emulator",
  "install_steps": [
    "apt-get install x-terminal-emulator 2>/dev/null || which x-terminal-emulator",
    "Verify: x-terminal-emulator --version",
    "supercli plugins install ./plugins/x-terminal-emulator --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}