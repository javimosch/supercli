{
  "plugin": "postsuper",
  "binary": "postsuper",
  "check": "which postsuper",
  "install_steps": [
    "# Install postsuper",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/postsuper-linux-amd64 -o /usr/local/bin/postsuper",
    "chmod +x /usr/local/bin/postsuper",
    "# Or via package manager, see /usr/sbin/postsuper"
  ]
}