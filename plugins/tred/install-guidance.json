{
  "plugin": "tred",
  "binary": "tred",
  "check": "which tred",
  "install_steps": [
    "apt-get install tred 2>/dev/null || which tred",
    "Verify: tred --version",
    "supercli plugins install ./plugins/tred --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}