{
  "name": "chem",
  "version": "0.1.0",
  "description": "CLI tool: chem",
  "source": "https://packages.debian.org/chem",
  "checks": [{ "type": "binary", "name": "chem" }],
  "install_guidance": {
    "plugin": "chem", "binary": "chem", "check": "which chem",
    "install_steps": ["apt-get install chem 2>/dev/null || which chem", "Verify: chem --version", "supercli plugins install ./plugins/chem --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "chem", "resource": "_", "action": "_",
    "description": "Passthrough to chem CLI",
    "adapter": "process", "adapterConfig": { "command": "chem", "passthrough": true, "missingDependencyHelp": "Install chem: apt-get install chem" }, "args": []
  }]
}
