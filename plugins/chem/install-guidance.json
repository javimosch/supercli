{ "plugin": "chem", "binary": "chem", "check": "which chem", "install_steps": ["apt-get install chem 2>/dev/null || which chem", "Verify: chem --version", "supercli plugins install ./plugins/chem --on-conflict replace --json"] }
