{
  "plugin": "downloader-cli",
  "binary": "dw",
  "check": "which dw",
  "install_steps": [
    "pip install downloader-cli",
    "Verify: dw --help",
    "supercli plugins install ./plugins/downloader-cli --on-conflict replace --json"
  ],
  "note": "downloader-cli provides simple downloading with progressbar"
}
