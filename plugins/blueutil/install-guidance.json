{
  "plugin": "blueutil",
  "binary": "blueutil",
  "check": "which blueutil",
  "install_steps": [
    "brew install blueutil",
    "Verify: blueutil --version",
    "supercli plugins install ./plugins/blueutil --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}