{
  "plugin": "krep",
  "binary": "krep",
  "check": "which krep",
  "install_steps": [
    "brew install krep",
    "Verify: krep --version",
    "supercli plugins install ./plugins/krep --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
