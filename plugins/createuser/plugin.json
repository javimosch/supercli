{
  "name": "createuser",
  "version": "0.1.0",
  "description": "CLI tool: createuser",
  "source": "https://packages.debian.org/createuser",
  "checks": [{ "type": "binary", "name": "createuser" }],
  "install_guidance": {
    "plugin": "createuser", "binary": "createuser", "check": "which createuser",
    "install_steps": ["apt-get install createuser 2>/dev/null || which createuser", "Verify: createuser --version", "supercli plugins install ./plugins/createuser --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "createuser", "resource": "_", "action": "_",
    "description": "Passthrough to createuser CLI",
    "adapter": "process", "adapterConfig": { "command": "createuser", "passthrough": true, "missingDependencyHelp": "Install createuser: apt-get install createuser" }, "args": []
  }]
}
