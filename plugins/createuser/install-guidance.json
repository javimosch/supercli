{ "plugin": "createuser", "binary": "createuser", "check": "which createuser", "install_steps": ["apt-get install createuser 2>/dev/null || which createuser", "Verify: createuser --version", "supercli plugins install ./plugins/createuser --on-conflict replace --json"] }
