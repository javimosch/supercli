{
  "plugin": "gping",
  "binary": "gping",
  "check": "which gping",
  "install_steps": [
    "brew install gping",
    "Verify: gping --version",
    "supercli plugins install ./plugins/gping --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install gping. Visual ping with real-time graph. Can ping multiple hosts simultaneously. Supports IPv4 and IPv6. Customizable interval and timeout settings."
}
