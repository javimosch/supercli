{
  "plugin": "l2test",
  "binary": "l2test",
  "check": "which l2test",
  "install_steps": [
    "apt-get install l2test 2>/dev/null || which l2test",
    "Verify: l2test --version",
    "supercli plugins install ./plugins/l2test --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}