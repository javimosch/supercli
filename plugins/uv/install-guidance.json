{
  "plugin": "uv",
  "binary": "uv",
  "check": "which uv",
  "install_steps": [
    "brew install uv",
    "Verify: uv --version",
    "supercli plugins install ./plugins/uv --on-conflict replace --json"
  ]
}