{ "plugin": "cabc", "binary": "cabc", "check": "which cabc", "install_steps": ["apt-get install cabc 2>/dev/null || which cabc", "Verify: cabc --version", "supercli plugins install ./plugins/cabc --on-conflict replace --json"] }
