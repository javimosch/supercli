{
  "plugin": "fast-agent",
  "binary": "fast-agent",
  "check": "fast-agent --version",
  "install_steps": [
    "uv tool install -U fast-agent-mcp",
    "fast-agent --version",
    "supercli plugins install ./plugins/fast-agent --json"
  ],
  "note": "fast-agent is excellent for coding agents, evaluation workflows, and supports MCP (Model Context Protocol)."
}
