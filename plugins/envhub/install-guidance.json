{
  "plugin": "envhub",
  "binary": "envhub",
  "check": "which envhub",
  "install_steps": ["git clone && python3 setup.py", "Verify: envhub --version", "supercli plugins install ./plugins/envhub --on-conflict replace --json"]
}
