{ "plugin": "busctl", "binary": "busctl", "check": "which busctl", "install_steps": ["apt-get install busctl 2>/dev/null || which busctl", "Verify: busctl --version", "supercli plugins install ./plugins/busctl --on-conflict replace --json"] }
