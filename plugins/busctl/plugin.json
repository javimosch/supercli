{
  "name": "busctl",
  "version": "0.1.0",
  "description": "CLI tool: busctl",
  "source": "https://packages.debian.org/busctl",
  "checks": [{ "type": "binary", "name": "busctl" }],
  "install_guidance": {
    "plugin": "busctl", "binary": "busctl", "check": "which busctl",
    "install_steps": ["apt-get install busctl 2>/dev/null || which busctl", "Verify: busctl --version", "supercli plugins install ./plugins/busctl --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "busctl", "resource": "_", "action": "_",
    "description": "Passthrough to busctl CLI",
    "adapter": "process", "adapterConfig": { "command": "busctl", "passthrough": true, "missingDependencyHelp": "Install busctl: apt-get install busctl" }, "args": []
  }]
}
