{ "plugin": "allec2png", "binary": "allec2png", "check": "which allec2png", "install_steps": ["apt-get install allec2png 2>/dev/null || which allec2png", "Verify: allec2png --version", "supercli plugins install ./plugins/allec2png --on-conflict replace --json"] }
