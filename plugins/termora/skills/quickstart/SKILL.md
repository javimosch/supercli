---
name: termora
description: Use this skill when the user wants to use termora for Termora is a terminal emulator and SSH client for Windows, macOS and Linux..
---

# termora Plugin

Termora is a terminal emulator and SSH client for Windows, macOS and Linux.

## Commands

### General
- `termora self version` — Print termora version
- `termora _ _` — Passthrough to termora CLI

## Usage Examples
- "Run termora"
- "Use termora to Termora is a terminal emulator and SSH client for Windows, m"

## Installation

```bash
pip install termora
```

## Key Features
- Termora is a terminal emulator and SSH client for Windows, macOS and Linux.
- CLI tool, headless and scriptable
