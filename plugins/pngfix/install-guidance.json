{
  "plugin": "pngfix",
  "binary": "pngfix",
  "check": "which pngfix",
  "install_steps": [
    "apt-get install pngfix 2>/dev/null || which pngfix",
    "Verify: pngfix --version",
    "supercli plugins install ./plugins/pngfix --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}