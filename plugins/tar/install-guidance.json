{
  "plugin": "tar",
  "binary": "tar",
  "check": "which tar",
  "install_steps": [
    "brew install gnu-tar",
    "Verify: tar --version",
    "supercli plugins install ./plugins/tar --on-conflict replace --json"
  ]
}
