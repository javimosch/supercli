{
  "name": "db_hotbackup",
  "version": "0.1.0",
  "description": "CLI tool: db_hotbackup",
  "source": "https://packages.debian.org/db_hotbackup",
  "checks": [{ "type": "binary", "name": "db_hotbackup" }],
  "install_guidance": {
    "plugin": "db_hotbackup", "binary": "db_hotbackup", "check": "which db_hotbackup",
    "install_steps": ["apt-get install db_hotbackup 2>/dev/null || which db_hotbackup", "Verify: db_hotbackup --version", "supercli plugins install ./plugins/db_hotbackup --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_hotbackup", "resource": "_", "action": "_",
    "description": "Passthrough to db_hotbackup CLI",
    "adapter": "process", "adapterConfig": { "command": "db_hotbackup", "passthrough": true, "missingDependencyHelp": "Install db_hotbackup: apt-get install db_hotbackup" }, "args": []
  }]
}
