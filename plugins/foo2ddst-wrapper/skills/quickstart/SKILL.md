---
name: foo2ddst-wrapper
description: Use this skill when the user wants to use foo2ddst-wrapper, cli tool: foo2ddst-wrapper.
---

# foo2ddst-wrapper Plugin

CLI tool: foo2ddst-wrapper.

## Commands
- `foo2ddst-wrapper <resource> <action>` — Execute foo2ddst-wrapper commands
- `foo2ddst-wrapper self version` — Print foo2ddst-wrapper version
- `foo2ddst-wrapper _ _` — Passthrough to foo2ddst-wrapper CLI

## Usage Examples
- "foo2ddst-wrapper --help"
- "foo2ddst-wrapper self version"

## Installation
```bash
apt-get install foo2ddst-wrapper 2>/dev/null || which foo2ddst-wrapper
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
