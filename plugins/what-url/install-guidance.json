{
  "plugin": "what-url",
  "binary": "what-url",
  "check": "which what-url",
  "install_steps": ["pip install what-url", "Verify: what-url --version", "supercli plugins install ./plugins/what-url --on-conflict replace --json"]
}
