{
  "plugin": "litestream",
  "binary": "litestream",
  "check": "which litestream",
  "install_steps": ["go install github.com/benbjohnson/litestream/cmd/litestream@latest", "Verify: litestream --version", "supercli plugins install ./plugins/litestream --on-conflict replace --json"]
}
