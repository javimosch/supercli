{ "plugin": "brctl", "binary": "brctl", "check": "which brctl", "install_steps": ["apt-get install brctl 2>/dev/null || which brctl", "Verify: brctl --version", "supercli plugins install ./plugins/brctl --on-conflict replace --json"] }
