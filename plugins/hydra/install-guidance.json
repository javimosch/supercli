{
  "plugin": "hydra",
  "binary": "hydra",
  "check": "which hydra",
  "install_steps": [
    "brew install hydra",
    "Verify: hydra --version",
    "supercli plugins install ./plugins/hydra --on-conflict replace --json"
  ]
}
