{
  "plugin": "xsubpp",
  "binary": "xsubpp",
  "check": "which xsubpp",
  "install_steps": [
    "apt-get install xsubpp 2>/dev/null || which xsubpp",
    "Verify: xsubpp --version",
    "supercli plugins install ./plugins/xsubpp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}