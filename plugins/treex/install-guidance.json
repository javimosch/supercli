{
  "plugin": "treex",
  "binary": "treex",
  "check": "which treex",
  "install_steps": [
    "go install github.com/shiquda/treex@latest",
    "Verify: treex --version",
    "supercli plugins install ./plugins/treex --on-conflict replace --json"
  ],
  "note": "Also available via scoop on Windows (scoop bucket add extras && scoop install treex) or download pre-built binaries from GitHub releases."
}
