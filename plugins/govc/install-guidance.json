{
  "plugin": "govc",
  "binary": "govc",
  "check": "which govc",
  "install_steps": [
    "brew install govc",
    "Verify: govc --version",
    "supercli plugins install ./plugins/govc --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}