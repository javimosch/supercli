{
  "plugin": "cspell",
  "binary": "cspell",
  "check": "which cspell",
  "install_steps": [
    "npm install -g cspell",
    "Verify: cspell --version",
    "supercli plugins install ./plugins/cspell --on-conflict replace --json"
  ],
  "note": "Also available via: brew install cspell (macOS), or yarn global add cspell. Supports 40+ programming and markup languages with automatic identifier parsing."
}
