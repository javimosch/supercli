{
  "name": "check-db",
  "version": "0.1.0",
  "description": "CLI tool: check-db",
  "source": "https://packages.debian.org/check-db",
  "checks": [{ "type": "binary", "name": "check-db" }],
  "install_guidance": {
    "plugin": "check-db", "binary": "check-db", "check": "which check-db",
    "install_steps": ["apt-get install check-db 2>/dev/null || which check-db", "Verify: check-db --version", "supercli plugins install ./plugins/check-db --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check-db", "resource": "_", "action": "_",
    "description": "Passthrough to check-db CLI",
    "adapter": "process", "adapterConfig": { "command": "check-db", "passthrough": true, "missingDependencyHelp": "Install check-db: apt-get install check-db" }, "args": []
  }]
}
