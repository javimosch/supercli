{ "plugin": "check-db", "binary": "check-db", "check": "which check-db", "install_steps": ["apt-get install check-db 2>/dev/null || which check-db", "Verify: check-db --version", "supercli plugins install ./plugins/check-db --on-conflict replace --json"] }
