{ "plugin": "dcopclient", "binary": "dcopclient", "check": "which dcopclient", "install_steps": ["apt-get install dcopclient 2>/dev/null || which dcopclient", "Verify: dcopclient --version", "supercli plugins install ./plugins/dcopclient --on-conflict replace --json"] }
