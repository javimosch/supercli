{
  "plugin": "dirb",
  "binary": "dirb",
  "check": "which dirb",
  "install_steps": [
    "brew install dirb",
    "Verify: dirb --version",
    "supercli plugins install ./plugins/dirb --on-conflict replace --json"
  ]
}
