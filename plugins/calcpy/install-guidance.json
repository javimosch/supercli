{
  "plugin": "calcpy",
  "binary": "calcpy",
  "check": "which calcpy",
  "install_steps": [
    "pip install calcpy",
    "Verify: calcpy --version",
    "supercli plugins install ./plugins/calcpy --on-conflict replace --json"
  ]
}