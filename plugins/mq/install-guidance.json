{
  "plugin": "mq",
  "binary": "mq",
  "check": "which mq",
  "install_steps": [
    "curl -sSL https://mqlang.org/install.sh | bash",
    "Verify: mq --version",
    "supercli plugins install ./plugins/mq --on-conflict replace --json"
  ],
  "note": "Also available via: cargo install mq-run, brew install mq, or download binaries from GitHub releases. Requires no authentication."
}