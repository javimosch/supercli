{
  "plugin": "nroff",
  "binary": "nroff",
  "check": "which nroff",
  "install_steps": [
    "apt-get install nroff 2>/dev/null || which nroff",
    "Verify: nroff --version",
    "supercli plugins install ./plugins/nroff --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}