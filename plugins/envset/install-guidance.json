{
  "plugin": "envset",
  "binary": "envset",
  "check": "which envset",
  "install_steps": [
    "go install github.com/envset/envset@latest",
    "Verify: envset --version",
    "supercli plugins install ./plugins/envset --on-conflict replace --json"
  ]
}
