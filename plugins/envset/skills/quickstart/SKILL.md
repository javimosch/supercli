---
name: envset
description: Use this skill when the user wants to use envset — Environment variable manager CLI — load, validate, and export env vars as JSON.
---

# envset Plugin

Environment variable manager CLI — load, validate, and export env vars as JSON

## Commands
- `envset self version` — Print version
- `envset _ _` — Passthrough to envset

## Installation
```bash
go install github.com/envset/envset@latest
```
