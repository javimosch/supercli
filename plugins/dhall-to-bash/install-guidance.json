{ "plugin": "dhall-to-bash", "binary": "dhall-to-bash", "check": "which dhall-to-bash", "install_steps": ["apt-get install dhall-to-bash 2>/dev/null || which dhall-to-bash", "Verify: dhall-to-bash --version", "supercli plugins install ./plugins/dhall-to-bash --on-conflict replace --json"] }
