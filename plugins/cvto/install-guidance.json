{
  "plugin": "cvto",
  "binary": "cvto",
  "check": "which cvto",
  "install_steps": ["cargo install cvto", "Verify: cvto --version", "supercli plugins install ./plugins/cvto --on-conflict replace --json"]
}
