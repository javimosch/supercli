{
  "plugin": "git-shot",
  "binary": "git-shot",
  "check": "which git-shot",
  "install_steps": ["npm install -g git-shot", "Verify: git-shot --version", "supercli plugins install ./plugins/git-shot --on-conflict replace --json"]
}
