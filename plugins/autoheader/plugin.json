{
  "name": "autoheader",
  "version": "0.1.0",
  "description": "CLI tool: autoheader",
  "source": "https://packages.debian.org/autoheader",
  "checks": [{ "type": "binary", "name": "autoheader" }],
  "install_guidance": {
    "plugin": "autoheader", "binary": "autoheader", "check": "which autoheader",
    "install_steps": ["apt-get install autoheader 2>/dev/null || which autoheader", "Verify: autoheader --version", "supercli plugins install ./plugins/autoheader --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "autoheader", "resource": "_", "action": "_",
    "description": "Passthrough to autoheader CLI",
    "adapter": "process", "adapterConfig": { "command": "autoheader", "passthrough": true, "missingDependencyHelp": "Install autoheader: apt-get install autoheader" }, "args": []
  }]
}
