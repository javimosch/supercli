{ "plugin": "autoheader", "binary": "autoheader", "check": "which autoheader", "install_steps": ["apt-get install autoheader 2>/dev/null || which autoheader", "Verify: autoheader --version", "supercli plugins install ./plugins/autoheader --on-conflict replace --json"] }
