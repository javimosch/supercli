{
  "plugin": "dnsenum",
  "binary": "dnsenum",
  "check": "which dnsenum",
  "install_steps": [
    "brew install dnsenum",
    "Verify: dnsenum --version",
    "supercli plugins install ./plugins/dnsenum --on-conflict replace --json"
  ]
}
