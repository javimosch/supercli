---
name: era_invalidate
description: Use this skill when the user wants to use era_invalidate, cli tool: era_invalidate.
---

# era_invalidate Plugin

CLI tool: era_invalidate.

## Commands
- `era_invalidate <resource> <action>` — Execute era_invalidate commands
- `era_invalidate self version` — Print era_invalidate version
- `era_invalidate _ _` — Passthrough to era_invalidate CLI

## Usage Examples
- "era_invalidate --help"
- "era_invalidate self version"

## Installation
```bash
apt-get install era_invalidate 2>/dev/null || which era_invalidate
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
