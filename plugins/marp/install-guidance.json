{
  "plugin": "marp",
  "binary": "marp",
  "check": "which marp",
  "install_steps": [
    "brew install marp",
    "Verify: marp --version",
    "supercli plugins install ./plugins/marp --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}