{
  "plugin": "deadbranch",
  "binary": "deadbranch",
  "check": "which deadbranch",
  "install_steps": [
    "cargo install deadbranch",
    "Verify: deadbranch --version",
    "supercli plugins install ./plugins/deadbranch --on-conflict replace --json"
  ],
  "note": "Also available via Homebrew (brew install deadbranch), npm/npx (npx deadbranch), or the quick install script: curl -fsSL https://raw.githubusercontent.com/armgabrielyan/deadbranch/main/install.sh | bash"
}
