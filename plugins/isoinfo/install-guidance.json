{
  "plugin": "isoinfo",
  "binary": "isoinfo",
  "check": "which isoinfo",
  "install_steps": [
    "apt-get install isoinfo 2>/dev/null || which isoinfo",
    "Verify: isoinfo --version",
    "supercli plugins install ./plugins/isoinfo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}