{
  "plugin": "pamfile",
  "binary": "pamfile",
  "check": "which pamfile",
  "install_steps": [
    "apt-get install pamfile 2>/dev/null || which pamfile",
    "Verify: pamfile --version",
    "supercli plugins install ./plugins/pamfile --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}