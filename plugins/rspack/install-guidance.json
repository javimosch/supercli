{
  "plugin": "rspack",
  "binary": "rspack",
  "check": "which rspack",
  "install_steps": ["npm install -g @rspack/cli", "Verify: rspack --version", "supercli plugins install ./plugins/rspack --on-conflict replace --json"]
}
