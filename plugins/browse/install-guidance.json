{ "plugin": "browse", "binary": "browse", "check": "which browse", "install_steps": ["apt-get install browse 2>/dev/null || which browse", "Verify: browse --version", "supercli plugins install ./plugins/browse --on-conflict replace --json"] }
