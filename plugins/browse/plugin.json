{
  "name": "browse",
  "version": "0.1.0",
  "description": "CLI tool: browse",
  "source": "https://packages.debian.org/browse",
  "checks": [{ "type": "binary", "name": "browse" }],
  "install_guidance": {
    "plugin": "browse", "binary": "browse", "check": "which browse",
    "install_steps": ["apt-get install browse 2>/dev/null || which browse", "Verify: browse --version", "supercli plugins install ./plugins/browse --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "browse", "resource": "_", "action": "_",
    "description": "Passthrough to browse CLI",
    "adapter": "process", "adapterConfig": { "command": "browse", "passthrough": true, "missingDependencyHelp": "Install browse: apt-get install browse" }, "args": []
  }]
}
