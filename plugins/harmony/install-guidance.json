{
  "plugin": "harmony",
  "binary": "harmony",
  "check": "which harmony",
  "install_steps": [
    "cargo install harmony",
    "Verify: harmony --version",
    "supercli plugins install ./plugins/harmony --on-conflict replace --json"
  ],
  "note": "Render and format structured CLI responses."
}
