{
  "name": "bf_copy",
  "version": "0.1.0",
  "description": "CLI tool: bf_copy",
  "source": "https://packages.debian.org/bf_copy",
  "checks": [{ "type": "binary", "name": "bf_copy" }],
  "install_guidance": {
    "plugin": "bf_copy", "binary": "bf_copy", "check": "which bf_copy",
    "install_steps": ["apt-get install bf_copy 2>/dev/null || which bf_copy", "Verify: bf_copy --version", "supercli plugins install ./plugins/bf_copy --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "bf_copy", "resource": "_", "action": "_",
    "description": "Passthrough to bf_copy CLI",
    "adapter": "process", "adapterConfig": { "command": "bf_copy", "passthrough": true, "missingDependencyHelp": "Install bf_copy: apt-get install bf_copy" }, "args": []
  }]
}
