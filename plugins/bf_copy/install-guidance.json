{ "plugin": "bf_copy", "binary": "bf_copy", "check": "which bf_copy", "install_steps": ["apt-get install bf_copy 2>/dev/null || which bf_copy", "Verify: bf_copy --version", "supercli plugins install ./plugins/bf_copy --on-conflict replace --json"] }
