{
  "plugin": "hashsigs-rs",
  "binary": "hashsigs-rs",
  "check": "which hashsigs-rs",
  "install_steps": [
    "cargo install hashsigs-rs",
    "Verify: hashsigs-rs --version",
    "supercli plugins install ./plugins/hashsigs-rs --on-conflict replace --json"
  ],
  "note": "Post-quantum secure hash-based signature scheme implementation."
}
