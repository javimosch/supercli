{
  "name": "adb",
  "version": "0.1.0",
  "description": "CLI tool: adb",
  "source": "https://packages.debian.org/adb",
  "checks": [{ "type": "binary", "name": "adb" }],
  "install_guidance": {
    "plugin": "adb", "binary": "adb", "check": "which adb",
    "install_steps": ["apt-get install adb 2>/dev/null || which adb", "Verify: adb --version", "supercli plugins install ./plugins/adb --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "adb", "resource": "_", "action": "_",
    "description": "Passthrough to adb CLI",
    "adapter": "process", "adapterConfig": { "command": "adb", "passthrough": true, "missingDependencyHelp": "Install adb: apt-get install adb" }, "args": []
  }]
}
