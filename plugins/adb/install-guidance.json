{ "plugin": "adb", "binary": "adb", "check": "which adb", "install_steps": ["apt-get install adb 2>/dev/null || which adb", "Verify: adb --version", "supercli plugins install ./plugins/adb --on-conflict replace --json"] }
