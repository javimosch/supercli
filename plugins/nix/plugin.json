{
  "name": "nix",
  "version": "0.1.0",
  "description": "Nix — purely functional package manager for reproducible builds",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "nix"
    }
  ],
  "install_guidance": {
    "plugin": "nix",
    "binary": "nix",
    "check": "which nix",
    "install_steps": [
      "brew install nix"
    ],
    "note": "Utility tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "nix",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to nix",
      "adapter": "process",
      "adapterConfig": {
        "command": "nix",
        "passthrough": true,
        "missingDependencyHelp": "Install nix: brew install nix"
      },
      "args": []
    }
  ]
}
