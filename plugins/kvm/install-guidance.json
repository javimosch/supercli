{
  "plugin": "kvm",
  "binary": "kvm",
  "check": "which kvm",
  "install_steps": [
    "apt-get install kvm 2>/dev/null || which kvm",
    "Verify: kvm --version",
    "supercli plugins install ./plugins/kvm --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}