{ "plugin": "apg", "binary": "apg", "check": "which apg", "install_steps": ["apt-get install apg 2>/dev/null || which apg", "Verify: apg --version", "supercli plugins install ./plugins/apg --on-conflict replace --json"] }
