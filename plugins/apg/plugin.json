{
  "name": "apg",
  "version": "0.1.0",
  "description": "CLI tool: apg",
  "source": "https://packages.debian.org/apg",
  "checks": [{ "type": "binary", "name": "apg" }],
  "install_guidance": {
    "plugin": "apg", "binary": "apg", "check": "which apg",
    "install_steps": ["apt-get install apg 2>/dev/null || which apg", "Verify: apg --version", "supercli plugins install ./plugins/apg --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "apg", "resource": "_", "action": "_",
    "description": "Passthrough to apg CLI",
    "adapter": "process", "adapterConfig": { "command": "apg", "passthrough": true, "missingDependencyHelp": "Install apg: apt-get install apg" }, "args": []
  }]
}
