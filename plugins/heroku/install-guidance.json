{
  "plugin": "heroku",
  "binary": "heroku",
  "check": "which heroku",
  "install_steps": [
    "brew install heroku",
    "Verify: heroku --version",
    "supercli plugins install ./plugins/heroku --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}