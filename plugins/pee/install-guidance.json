{
  "plugin": "pee",
  "binary": "pee",
  "check": "which pee",
  "install_steps": [
    "apt-get install pee 2>/dev/null || which pee",
    "Verify: pee --version",
    "supercli plugins install ./plugins/pee --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}