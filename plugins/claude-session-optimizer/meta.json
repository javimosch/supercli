{
  "version": "1.0.0",
  "description": "claude-session-optimizer — Compress oversized Claude Code session files (JSONL). Removes thinking blocks, file-history-snapshots, truncates tool results. Recovers disk space and prevents 75MB+ crash. Zero dependencies (Python stdlib only).",
  "tags": ["claude", "session", "optimizer", "compression", "ai", "claude-code", "developer-tools", "python"],
  "has_learn": true
}
