{
  "plugin": "im-launch",
  "binary": "im-launch",
  "check": "which im-launch",
  "install_steps": [
    "apt-get install im-launch 2>/dev/null || which im-launch",
    "Verify: im-launch --version",
    "supercli plugins install ./plugins/im-launch --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}