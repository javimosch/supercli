{
  "name": "dload",
  "version": "0.1.0",
  "description": "CLI tool: dload",
  "source": "https://packages.debian.org/dload",
  "checks": [{ "type": "binary", "name": "dload" }],
  "install_guidance": {
    "plugin": "dload", "binary": "dload", "check": "which dload",
    "install_steps": ["apt-get install dload 2>/dev/null || which dload", "Verify: dload --version", "supercli plugins install ./plugins/dload --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dload", "resource": "_", "action": "_",
    "description": "Passthrough to dload CLI",
    "adapter": "process", "adapterConfig": { "command": "dload", "passthrough": true, "missingDependencyHelp": "Install dload: apt-get install dload" }, "args": []
  }]
}
