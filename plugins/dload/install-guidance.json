{ "plugin": "dload", "binary": "dload", "check": "which dload", "install_steps": ["apt-get install dload 2>/dev/null || which dload", "Verify: dload --version", "supercli plugins install ./plugins/dload --on-conflict replace --json"] }
