{
  "plugin": "zigmod",
  "binary": "zigmod",
  "check": "which zigmod",
  "install_steps": ["zig build-exe src/main.zig", "Verify: zigmod --version", "supercli plugins install ./plugins/zigmod --on-conflict replace --json"]
}
