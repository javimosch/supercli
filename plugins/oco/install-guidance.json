{
  "plugin": "oco",
  "binary": "oco",
  "check": "which oco",
  "install_steps": [
    "apt-get install oco 2>/dev/null || which oco",
    "Verify: oco --version",
    "supercli plugins install ./plugins/oco --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}