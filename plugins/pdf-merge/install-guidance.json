{
  "plugin": "pdf-merge",
  "binary": "pdf-merge",
  "check": "which pdf-merge",
  "install_steps": ["pip install pdf-merge", "Verify: pdf-merge --version", "supercli plugins install ./plugins/pdf-merge --on-conflict replace --json"]
}
