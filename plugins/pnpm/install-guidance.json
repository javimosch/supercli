{
  "plugin": "pnpm",
  "binary": "pnpm",
  "check": "which pnpm",
  "install_steps": [
    "brew install pnpm",
    "Verify: pnpm --version",
    "supercli plugins install ./plugins/pnpm --on-conflict replace --json"
  ]
}