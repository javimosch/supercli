---
name: gdk-pixbuf-pixdata
description: Use this skill when the user wants to use gdk-pixbuf-pixdata, cli tool: gdk-pixbuf-pixdata.
---

# gdk-pixbuf-pixdata Plugin

CLI tool: gdk-pixbuf-pixdata.

## Commands
- `gdk-pixbuf-pixdata <resource> <action>` — Execute gdk-pixbuf-pixdata commands
- `gdk-pixbuf-pixdata self version` — Print gdk-pixbuf-pixdata version
- `gdk-pixbuf-pixdata _ _` — Passthrough to gdk-pixbuf-pixdata CLI

## Usage Examples
- "gdk-pixbuf-pixdata --help"
- "gdk-pixbuf-pixdata self version"

## Installation
```bash
apt-get install gdk-pixbuf-pixdata 2>/dev/null || which gdk-pixbuf-pixdata
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
