{
  "plugin": "snm",
  "binary": "snm",
  "check": "which snm",
  "install_steps": [
    "cargo install snm",
    "Verify: snm --version",
    "supercli plugins install ./plugins/snm --on-conflict replace --json"
  ]
}