{
  "plugin": "glow",
  "binary": "glow",
  "check": "which glow",
  "install_steps": [
    "brew install glow",
    "Verify: glow --version",
    "supercli plugins install ./plugins/glow --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/charmbracelet/glow@latest. Supports TUI mode with 'glow -a'. Configuration file at ~/.config/glow/glow.yml. Can be used as pager for git and other tools."
}
