{
  "plugin": "blogr",
  "binary": "blogr",
  "check": "which blogr",
  "install_steps": [
    "cargo install blogr-cli",
    "Verify: blogr --version",
    "supercli plugins install ./plugins/blogr --on-conflict replace --json"
  ],
  "note": "Requires Rust 1.70+. Also supports building from source (git clone + cargo install --path blogr-cli). Deploy command requires GITHUB_TOKEN environment variable."
}
