{ "plugin": "acpi_listen", "binary": "acpi_listen", "check": "which acpi_listen", "install_steps": ["apt-get install acpi_listen 2>/dev/null || which acpi_listen", "Verify: acpi_listen --version", "supercli plugins install ./plugins/acpi_listen --on-conflict replace --json"] }
