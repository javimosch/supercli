{
  "name": "acpi_listen",
  "version": "0.1.0",
  "description": "CLI tool: acpi_listen",
  "source": "https://packages.debian.org/acpi_listen",
  "checks": [{ "type": "binary", "name": "acpi_listen" }],
  "install_guidance": {
    "plugin": "acpi_listen", "binary": "acpi_listen", "check": "which acpi_listen",
    "install_steps": ["apt-get install acpi_listen 2>/dev/null || which acpi_listen", "Verify: acpi_listen --version", "supercli plugins install ./plugins/acpi_listen --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acpi_listen", "resource": "_", "action": "_",
    "description": "Passthrough to acpi_listen CLI",
    "adapter": "process", "adapterConfig": { "command": "acpi_listen", "passthrough": true, "missingDependencyHelp": "Install acpi_listen: apt-get install acpi_listen" }, "args": []
  }]
}
