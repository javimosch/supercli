{
  "name": "dijkstra",
  "version": "0.1.0",
  "description": "CLI tool: dijkstra",
  "source": "https://packages.debian.org/dijkstra",
  "checks": [{ "type": "binary", "name": "dijkstra" }],
  "install_guidance": {
    "plugin": "dijkstra", "binary": "dijkstra", "check": "which dijkstra",
    "install_steps": ["apt-get install dijkstra 2>/dev/null || which dijkstra", "Verify: dijkstra --version", "supercli plugins install ./plugins/dijkstra --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dijkstra", "resource": "_", "action": "_",
    "description": "Passthrough to dijkstra CLI",
    "adapter": "process", "adapterConfig": { "command": "dijkstra", "passthrough": true, "missingDependencyHelp": "Install dijkstra: apt-get install dijkstra" }, "args": []
  }]
}
