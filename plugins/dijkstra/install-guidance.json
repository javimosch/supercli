{ "plugin": "dijkstra", "binary": "dijkstra", "check": "which dijkstra", "install_steps": ["apt-get install dijkstra 2>/dev/null || which dijkstra", "Verify: dijkstra --version", "supercli plugins install ./plugins/dijkstra --on-conflict replace --json"] }
