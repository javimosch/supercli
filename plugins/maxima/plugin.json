{
  "name": "maxima",
  "version": "0.1.0",
  "description": "maxima — Computer algebra system",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "maxima"
    }
  ],
  "install_guidance": {
    "plugin": "maxima",
    "binary": "maxima",
    "check": "which maxima",
    "install_steps": [
      "brew install maxima"
    ],
    "note": "Utility tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "maxima",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to maxima CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "maxima",
        "passthrough": true,
        "missingDependencyHelp": "Install maxima: brew install maxima"
      },
      "args": []
    }
  ]
}
