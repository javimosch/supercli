{
  "plugin": "dee-timer",
  "binary": "dee-timer",
  "check": "which dee-timer",
  "install_steps": ["cargo install dee-timer", "Verify: dee-timer --version", "supercli plugins install ./plugins/dee-timer --on-conflict replace --json"]
}
