{
  "plugin": "nmcli",
  "binary": "nmcli",
  "check": "which nmcli",
  "install_steps": [
    "apt-get install nmcli 2>/dev/null || which nmcli",
    "Verify: nmcli --version",
    "supercli plugins install ./plugins/nmcli --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}