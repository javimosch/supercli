{
  "plugin": "goenv",
  "binary": "goenv",
  "check": "which goenv",
  "install_steps": [
    "brew install goenv",
    "Verify: goenv --version",
    "supercli plugins install ./plugins/goenv --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}