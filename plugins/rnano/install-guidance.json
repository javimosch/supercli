{
  "plugin": "rnano",
  "binary": "rnano",
  "check": "which rnano",
  "install_steps": [
    "# Install rnano",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/rnano-linux-amd64 -o /usr/local/bin/rnano",
    "chmod +x /usr/local/bin/rnano",
    "# Or via package manager, see /usr/bin/rnano"
  ]
}