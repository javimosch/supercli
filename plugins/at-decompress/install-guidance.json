{ "plugin": "at-decompress", "binary": "at-decompress", "check": "which at-decompress", "install_steps": ["apt-get install at-decompress 2>/dev/null || which at-decompress", "Verify: at-decompress --version", "supercli plugins install ./plugins/at-decompress --on-conflict replace --json"] }
