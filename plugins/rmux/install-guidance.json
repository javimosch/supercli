{
  "plugin": "rmux",
  "binary": "rmux",
  "check": "which rmux",
  "install_steps": ["cargo install rmux", "Verify: rmux --version", "supercli plugins install ./plugins/rmux --on-conflict replace --json"]
}
