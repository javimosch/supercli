{
  "plugin": "when",
  "binary": "when",
  "check": "which when",
  "install_steps": [
    "pip install when",
    "Verify: when --version",
    "supercli plugins install ./plugins/when --on-conflict replace --json"
  ]
}
