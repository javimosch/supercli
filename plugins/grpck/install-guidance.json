{
  "plugin": "grpck",
  "binary": "grpck",
  "check": "which grpck",
  "install_steps": [
    "apt-get install grpck 2>/dev/null || which grpck",
    "Verify: grpck --version",
    "supercli plugins install ./plugins/grpck --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}