{
  "plugin": "mgrep",
  "binary": "mgrep",
  "check": "which mgrep",
  "install_steps": [
    "npm install -g mgrep",
    "Verify: mgrep --version",
    "supercli plugins install ./plugins/mgrep --on-conflict replace --json"
  ],
  "note": "Semantic code search tool that understands context and meaning."
}
