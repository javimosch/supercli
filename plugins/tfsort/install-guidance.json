{
  "plugin": "tfsort",
  "binary": "tfsort",
  "check": "which tfsort",
  "install_steps": [
    "brew install tfsort",
    "Verify: tfsort --version",
    "supercli plugins install ./plugins/tfsort --on-conflict replace --json"
  ]
}