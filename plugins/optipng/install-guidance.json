{
  "plugin": "optipng",
  "binary": "optipng",
  "check": "which optipng",
  "install_steps": [
    "brew install optipng",
    "Verify: optipng --version",
    "supercli plugins install ./plugins/optipng --on-conflict replace --json"
  ]
}
