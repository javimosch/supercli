{
  "plugin": "symbex",
  "binary": "symbex",
  "check": "which symbex",
  "install_steps": ["pip install symbex", "Verify: symbex --version", "supercli plugins install ./plugins/symbex --on-conflict replace --json"]
}
