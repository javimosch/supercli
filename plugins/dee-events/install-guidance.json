{
  "plugin": "dee-events",
  "binary": "dee-events",
  "check": "which dee-events",
  "install_steps": ["cargo install dee-events", "Verify: dee-events --version", "supercli plugins install ./plugins/dee-events --on-conflict replace --json"]
}
