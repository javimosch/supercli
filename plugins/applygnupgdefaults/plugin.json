{
  "name": "applygnupgdefaults",
  "version": "1.0.0",
  "description": "applygnupgdefaults \u2014 CLI applygnupgdefaults \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=applygnupgdefaults+cli",
  "checks": [
    {
      "type": "binary",
      "name": "applygnupgdefaults"
    }
  ],
  "install_guidance": {
    "plugin": "applygnupgdefaults",
    "binary": "applygnupgdefaults",
    "check": "which applygnupgdefaults",
    "install_steps": [
      "Install via package manager: apt install applygnupgdefaults or brew install applygnupgdefaults"
    ]
  },
  "commands": [
    {
      "namespace": "applygnupgdefaults",
      "resource": "run",
      "action": "exec",
      "description": "applygnupgdefaults \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "applygnupgdefaults",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install applygnupgdefaults via package manager"
      },
      "args": []
    }
  ]
}