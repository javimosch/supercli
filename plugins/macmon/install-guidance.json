{
  "plugin": "macmon",
  "binary": "macmon",
  "check": "which macmon",
  "install_steps": [
    "brew install macmon",
    "Verify: macmon --version",
    "supercli plugins install ./plugins/macmon --on-conflict replace --json"
  ],
  "note": "Also available via: MacPorts (sudo port install macmon), Cargo (cargo install macmon), Nix (nix-env -i macmon). Runs without sudo."
}
