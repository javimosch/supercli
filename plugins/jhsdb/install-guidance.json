{
  "plugin": "jhsdb",
  "binary": "jhsdb",
  "check": "which jhsdb",
  "install_steps": [
    "apt-get install jhsdb 2>/dev/null || which jhsdb",
    "Verify: jhsdb --version",
    "supercli plugins install ./plugins/jhsdb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}