{
  "plugin": "ctop",
  "binary": "ctop",
  "check": "which ctop",
  "install_steps": [
    "brew install ctop",
    "Verify: ctop --version",
    "supercli plugins install ./plugins/ctop --on-conflict replace --json"
  ]
}