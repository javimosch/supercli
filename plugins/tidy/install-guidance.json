{
  "plugin": "tidy",
  "binary": "tidy",
  "check": "which tidy",
  "install_steps": [
    "brew install tidy-html5",
    "Verify: tidy --version",
    "supercli plugins install ./plugins/tidy --on-conflict replace --json"
  ],
  "note": "Also available via apt: apt-get install tidy. HTML Tidy is a classic web development tool for cleaning up HTML markup and enforcing consistent coding style."
}
