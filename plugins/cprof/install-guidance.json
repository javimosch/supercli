{ "plugin": "cprof", "binary": "cprof", "check": "which cprof", "install_steps": ["apt-get install cprof 2>/dev/null || which cprof", "Verify: cprof --version", "supercli plugins install ./plugins/cprof --on-conflict replace --json"] }
