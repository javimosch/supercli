{
  "plugin": "ua",
  "binary": "ua",
  "check": "which ua",
  "install_steps": [
    "apt-get install ua 2>/dev/null || which ua",
    "Verify: ua --version",
    "supercli plugins install ./plugins/ua --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}