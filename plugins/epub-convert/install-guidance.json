{
  "plugin": "epub-convert",
  "binary": "epub-convert",
  "check": "which epub-convert",
  "install_steps": ["pip install epub-convert", "Verify: epub-convert --version", "supercli plugins install ./plugins/epub-convert --on-conflict replace --json"]
}
