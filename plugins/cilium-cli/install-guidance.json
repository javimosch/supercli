{
  "plugin": "cilium-cli",
  "binary": "cilium-cli",
  "check": "which cilium-cli",
  "install_steps": [
    "brew install cilium-cli",
    "Verify: cilium-cli --version",
    "supercli plugins install ./plugins/cilium-cli --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}