{
  "name": "combine",
  "version": "0.1.0",
  "description": "CLI tool: combine",
  "source": "https://packages.debian.org/combine",
  "checks": [{ "type": "binary", "name": "combine" }],
  "install_guidance": {
    "plugin": "combine", "binary": "combine", "check": "which combine",
    "install_steps": ["apt-get install combine 2>/dev/null || which combine", "Verify: combine --version", "supercli plugins install ./plugins/combine --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "combine", "resource": "_", "action": "_",
    "description": "Passthrough to combine CLI",
    "adapter": "process", "adapterConfig": { "command": "combine", "passthrough": true, "missingDependencyHelp": "Install combine: apt-get install combine" }, "args": []
  }]
}
