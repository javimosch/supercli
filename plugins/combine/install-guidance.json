{ "plugin": "combine", "binary": "combine", "check": "which combine", "install_steps": ["apt-get install combine 2>/dev/null || which combine", "Verify: combine --version", "supercli plugins install ./plugins/combine --on-conflict replace --json"] }
