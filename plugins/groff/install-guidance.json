{
  "plugin": "groff",
  "binary": "groff",
  "check": "which groff",
  "install_steps": [
    "apt-get install groff 2>/dev/null || which groff",
    "Verify: groff --version",
    "supercli plugins install ./plugins/groff --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}