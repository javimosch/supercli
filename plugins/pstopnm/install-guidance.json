{
  "plugin": "pstopnm",
  "binary": "pstopnm",
  "check": "which pstopnm",
  "install_steps": [
    "apt-get install pstopnm 2>/dev/null || which pstopnm",
    "Verify: pstopnm --version",
    "supercli plugins install ./plugins/pstopnm --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}