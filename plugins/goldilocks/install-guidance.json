{
  "plugin": "goldilocks",
  "binary": "goldilocks",
  "check": "which goldilocks",
  "install_steps": ["brew install goldilocks", "Verify: goldilocks --version", "supercli plugins install ./plugins/goldilocks --on-conflict replace --json"]
}
