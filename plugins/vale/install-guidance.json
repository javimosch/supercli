{
  "plugin": "vale",
  "binary": "vale",
  "check": "which vale",
  "install_steps": [
    "brew install vale",
    "Verify: vale --version",
    "supercli plugins install ./plugins/vale --on-conflict replace --json"
  ]
}
