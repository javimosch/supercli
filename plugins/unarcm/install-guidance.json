{
  "plugin": "unarcm",
  "binary": "unarcm",
  "check": "which unarcm",
  "install_steps": [
    "brew install unar",
    "Verify: unarcm --version",
    "supercli plugins install ./plugins/unarcm --on-conflict replace --json"
  ]
}
