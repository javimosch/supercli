{
  "plugin": "plow",
  "binary": "plow",
  "check": "which plow",
  "install_steps": [
    "brew install plow",
    "Verify: plow --version",
    "supercli plugins install ./plugins/plow --on-conflict replace --json"
  ]
}