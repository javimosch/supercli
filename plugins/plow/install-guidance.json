{
  "plugin": "plow",
  "binary": "plow",
  "check": "which plow",
  "install_steps": ["go install github.com/six-ddc/plow@latest", "Verify: plow --version", "supercli plugins install ./plugins/plow --on-conflict replace --json"]
}
