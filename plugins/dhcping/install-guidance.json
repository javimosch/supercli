{
  "plugin": "dhcping",
  "binary": "dhcping",
  "check": "which dhcping",
  "install_steps": [
    "# Install dhcping",
    "# See github.com/amishmm/dhcping",
    "# apt: sudo apt-get install dhcping",
    "# brew: brew install dhcping",
    "# cargo: cargo install dhcping",
    "# npm: npm install -g dhcping"
  ]
}