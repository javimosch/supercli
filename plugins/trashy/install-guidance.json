{
  "plugin": "trashy",
  "binary": "trashy",
  "check": "which trashy",
  "install_steps": [
    "cargo install trashy",
    "Verify: trashy --version",
    "supercli plugins install ./plugins/trashy --on-conflict replace --json"
  ],
  "note": "Also available via: AUR, Nix. Arguments for restore/empty are interpreted as regex by default."
}
