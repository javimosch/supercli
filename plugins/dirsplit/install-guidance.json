{ "plugin": "dirsplit", "binary": "dirsplit", "check": "which dirsplit", "install_steps": ["apt-get install dirsplit 2>/dev/null || which dirsplit", "Verify: dirsplit --version", "supercli plugins install ./plugins/dirsplit --on-conflict replace --json"] }
