{
  "plugin": "quien",
  "binary": "quien",
  "check": "which quien",
  "install_steps": [
    "brew tap retlehs/tap",
    "brew install retlehs/tap/quien",
    "Verify: quien --version",
    "supercli plugins install ./plugins/quien --on-conflict replace --json"
  ],
  "note": "Also available via: Go (go install github.com/retlehs/quien@latest), Ubuntu/Debian (curl -fsSL https://apt.quien.dev/install.sh | sudo sh), Arch Linux (yay -S quien)"
}
