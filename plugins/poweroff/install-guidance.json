{
  "plugin": "poweroff",
  "binary": "poweroff",
  "check": "which poweroff",
  "install_steps": [
    "# Install poweroff",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/poweroff-linux-amd64 -o /usr/local/bin/poweroff",
    "chmod +x /usr/local/bin/poweroff",
    "# Or via package manager, see /usr/sbin/poweroff"
  ]
}