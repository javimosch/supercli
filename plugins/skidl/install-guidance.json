{
  "plugin": "skidl",
  "binary": "skidl",
  "check": "which skidl",
  "install_steps": ["pip install skidl", "Verify: skidl --version", "supercli plugins install ./plugins/skidl --on-conflict replace --json"]
}
