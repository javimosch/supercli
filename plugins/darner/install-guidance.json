{
  "plugin": "darner",
  "binary": "darner",
  "check": "which darner",
  "install_steps": ["cmake . && make", "Verify: darner --version", "supercli plugins install ./plugins/darner --on-conflict replace --json"]
}
