{
  "plugin": "routel",
  "binary": "routel",
  "check": "which routel",
  "install_steps": [
    "# Install routel",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/routel-linux-amd64 -o /usr/local/bin/routel",
    "chmod +x /usr/local/bin/routel",
    "# Or via package manager, see /usr/bin/routel"
  ]
}