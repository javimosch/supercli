{
  "plugin": "ChatTTS",
  "binary": "chattts",
  "check": "which chattts",
  "install_steps": [
    "pip install chattts",
    "Verify: chattts --version",
    "supercli plugins install ./plugins/ChatTTS --on-conflict replace --json"
  ],
  "note": "Language: Python"
}