{
  "plugin": "nodemon",
  "binary": "nodemon",
  "check": "which nodemon",
  "install_steps": [
    "npm install -g nodemon",
    "Verify: nodemon --version",
    "supercli plugins install ./plugins/nodemon --on-conflict replace --json"
  ],
  "note": "Nodemon watches for file changes and automatically restarts your Node.js application. Use --watch to specify files/directories to watch. Use --ignore to exclude files. Configurable via nodemon.json."
}
