---
name: Valdi
description: Use this skill when the user wants to use Valdi for Valdi is a cross-platform UI framework that delivers native performance without .
---

# Valdi Plugin

Valdi is a cross-platform UI framework that delivers native performance without sacrificing developer velocity.

## Commands

### General
- `Valdi self version` — Print Valdi version
- `Valdi _ _` — Passthrough to valdi CLI

## Usage Examples
- "Run Valdi"
- "Use valdi to Valdi is a cross-platform UI framework that delivers native "

## Installation

```bash
pip install valdi
```

## Key Features
- Valdi is a cross-platform UI framework that delivers native performance without sacrificing develope
- CLI tool, headless and scriptable
