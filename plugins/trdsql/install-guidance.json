{
  "plugin": "trdsql",
  "binary": "trdsql",
  "check": "which trdsql",
  "install_steps": [
    "brew install trdsql",
    "Verify: trdsql --version",
    "supercli plugins install ./plugins/trdsql --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/noborus/trdsql@latest. Execute SQL on CSV, JSON, LTSV, TBLN, and other text files. Supports JOINs, aggregations, and more without a database."
}
