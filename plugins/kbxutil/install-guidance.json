{
  "plugin": "kbxutil",
  "binary": "kbxutil",
  "check": "which kbxutil",
  "install_steps": [
    "apt-get install kbxutil 2>/dev/null || which kbxutil",
    "Verify: kbxutil --version",
    "supercli plugins install ./plugins/kbxutil --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}