{
  "plugin": "ctlptl",
  "binary": "ctlptl",
  "check": "which ctlptl",
  "install_steps": [
    "brew install ctlptl",
    "Verify: ctlptl --version",
    "supercli plugins install ./plugins/ctlptl --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}