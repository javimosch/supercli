{
  "plugin": "kopia",
  "binary": "kopia",
  "check": "which kopia",
  "install_steps": [
    "brew install kopia",
    "Verify: kopia --version",
    "supercli plugins install ./plugins/kopia --on-conflict replace --json"
  ]
}