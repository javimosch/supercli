{
  "plugin": "ssh-proxy",
  "binary": "ssh-proxy",
  "check": "which ssh-proxy",
  "install_steps": [
    "go install github.com/ssh-proxy/ssh-proxy@latest",
    "Verify: ssh-proxy --version",
    "supercli plugins install ./plugins/ssh-proxy --on-conflict replace --json"
  ]
}
