{
  "plugin": "blackbox",
  "binary": "blackbox",
  "check": "which blackbox",
  "install_steps": ["brew install blackbox", "Verify: blackbox --version", "supercli plugins install ./plugins/blackbox --on-conflict replace --json"]
}
