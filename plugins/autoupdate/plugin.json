{
  "name": "autoupdate",
  "version": "0.1.0",
  "description": "CLI tool: autoupdate",
  "source": "https://packages.debian.org/autoupdate",
  "checks": [{ "type": "binary", "name": "autoupdate" }],
  "install_guidance": {
    "plugin": "autoupdate", "binary": "autoupdate", "check": "which autoupdate",
    "install_steps": ["apt-get install autoupdate 2>/dev/null || which autoupdate", "Verify: autoupdate --version", "supercli plugins install ./plugins/autoupdate --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "autoupdate", "resource": "_", "action": "_",
    "description": "Passthrough to autoupdate CLI",
    "adapter": "process", "adapterConfig": { "command": "autoupdate", "passthrough": true, "missingDependencyHelp": "Install autoupdate: apt-get install autoupdate" }, "args": []
  }]
}
