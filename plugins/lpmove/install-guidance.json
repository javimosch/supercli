{
  "plugin": "lpmove",
  "binary": "lpmove",
  "check": "which lpmove",
  "install_steps": [
    "apt-get install lpmove 2>/dev/null || which lpmove",
    "Verify: lpmove --version",
    "supercli plugins install ./plugins/lpmove --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}