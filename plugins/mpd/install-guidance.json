{
  "plugin": "mpd",
  "binary": "mpd",
  "check": "which mpd",
  "install_steps": [
    "brew install mpd",
    "Verify: mpd --version",
    "supercli plugins install ./plugins/mpd --on-conflict replace --json"
  ]
}
