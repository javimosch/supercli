---
name: swift-build
description: Use this skill when the user wants to use swift-build for A high-level build system based on llbuild, used by Xcode, Swift Playground, and.
---

# swift-build Plugin

A high-level build system based on llbuild, used by Xcode, Swift Playground, and the Swift Package Manager

## Commands

### General
- `swift-build self version` — Print swift-build version
- `swift-build _ _` — Passthrough to swift-build CLI

## Usage Examples
- "Run swift-build"
- "Use swift-build to A high-level build system based on llbuild, used by Xcode, S"

## Installation

```bash
pip install swift-build
```

## Key Features
- A high-level build system based on llbuild, used by Xcode, Swift Playground, and the Swift Package M
- CLI tool, headless and scriptable
