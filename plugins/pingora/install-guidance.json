{
  "plugin": "pingora",
  "binary": "pingora",
  "check": "which pingora",
  "install_steps": [
    "pip install pingora",
    "Verify: pingora --version",
    "supercli plugins install ./plugins/pingora --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}