{
  "plugin": "cups.lpinfo",
  "binary": "cups.lpinfo",
  "check": "which cups.lpinfo",
  "install_steps": [
    "apt-get install cups.lpinfo 2>/dev/null || which cups.lpinfo",
    "Verify: cups.lpinfo --version",
    "supercli plugins install ./plugins/cups.lpinfo --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}