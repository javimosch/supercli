{
  "plugin": "gtk4-launch",
  "binary": "gtk4-launch",
  "check": "which gtk4-launch",
  "install_steps": [
    "apt-get install gtk4-launch 2>/dev/null || which gtk4-launch",
    "Verify: gtk4-launch --version",
    "supercli plugins install ./plugins/gtk4-launch --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}