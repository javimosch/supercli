{
  "plugin": "npm-check-updates",
  "binary": "ncu",
  "check": "which ncu",
  "install_steps": [
    "npm install -g npm-check-updates",
    "Verify: ncu --version",
    "supercli plugins install ./plugins/npm-check-updates --on-conflict replace --json"
  ],
  "note": "Npm-check-updates checks for newer dependency versions. Use -u flag to upgrade package.json directly. Supports --json output for automation. Compatible with npm, yarn, pnpm, deno, and bun."
}
