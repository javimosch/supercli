{
  "plugin": "pnmfile",
  "binary": "pnmfile",
  "check": "which pnmfile",
  "install_steps": [
    "apt-get install pnmfile 2>/dev/null || which pnmfile",
    "Verify: pnmfile --version",
    "supercli plugins install ./plugins/pnmfile --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}