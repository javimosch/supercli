{ "plugin": "br", "binary": "br", "check": "which br", "install_steps": ["apt-get install br 2>/dev/null || which br", "Verify: br --version", "supercli plugins install ./plugins/br --on-conflict replace --json"] }
