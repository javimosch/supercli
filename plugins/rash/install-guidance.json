{
  "plugin": "rash",
  "binary": "rash",
  "check": "which rash",
  "install_steps": [
    "cargo install rash_core",
    "Verify: rash --version",
    "supercli plugins install ./plugins/rash --on-conflict replace --json"
  ],
  "note": "Also available via AUR (yay -S rash), Docker (ghcr.io/rash-sh/rash), or direct binary download from GitHub releases."
}
