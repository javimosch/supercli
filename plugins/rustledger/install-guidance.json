{
  "plugin": "rustledger",
  "binary": "rledger",
  "check": "which rledger",
  "install_steps": [
    "cargo install rustledger",
    "Verify: rledger --version",
    "supercli plugins install ./plugins/rustledger --on-conflict replace --json"
  ],
  "note": "Also available via: brew install rustledger, yay -S rustledger-bin (Arch), scoop install rustledger (Windows), or download from GitHub releases. Binary is 'rledger' not 'rustledger'."
}