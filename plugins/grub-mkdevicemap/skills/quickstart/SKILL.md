---
name: grub-mkdevicemap
description: Use this skill when the user wants to use grub-mkdevicemap, cli tool: grub-mkdevicemap.
---

# grub-mkdevicemap Plugin

CLI tool: grub-mkdevicemap.

## Commands
- `grub-mkdevicemap <resource> <action>` — Execute grub-mkdevicemap commands
- `grub-mkdevicemap self version` — Print grub-mkdevicemap version
- `grub-mkdevicemap _ _` — Passthrough to grub-mkdevicemap CLI

## Usage Examples
- "grub-mkdevicemap --help"
- "grub-mkdevicemap self version"

## Installation
```bash
apt-get install grub-mkdevicemap 2>/dev/null || which grub-mkdevicemap
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
