{
  "plugin": "mmount",
  "binary": "mmount",
  "check": "which mmount",
  "install_steps": [
    "apt-get install mmount 2>/dev/null || which mmount",
    "Verify: mmount --version",
    "supercli plugins install ./plugins/mmount --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}