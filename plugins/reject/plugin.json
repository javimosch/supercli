{
  "name": "reject",
  "version": "0.1.0",
  "description": "Reject print jobs from destinations",
  "source": "https://packages.debian.org/reject",
  "checks": [{ "type": "binary", "name": "reject" }],
  "install_guidance": {
    "plugin": "reject", "binary": "reject", "check": "which reject",
    "install_steps": ["apt-get install cups", "Verify: reject --version", "supercli plugins install ./plugins/reject --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "reject", "resource": "_", "action": "_",
    "description": "Passthrough to reject CLI",
    "adapter": "process", "adapterConfig": { "command": "reject", "passthrough": true, "missingDependencyHelp": "Install reject: apt-get install cups" }, "args": []
  }]
}
