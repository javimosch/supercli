{ "plugin": "reject", "binary": "reject", "check": "which reject", "install_steps": ["apt-get install cups", "Verify: reject --version", "supercli plugins install ./plugins/reject --on-conflict replace --json"] }
