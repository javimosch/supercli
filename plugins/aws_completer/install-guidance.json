{ "plugin": "aws_completer", "binary": "aws_completer", "check": "which aws_completer", "install_steps": ["apt-get install aws_completer 2>/dev/null || which aws_completer", "Verify: aws_completer --version", "supercli plugins install ./plugins/aws_completer --on-conflict replace --json"] }
