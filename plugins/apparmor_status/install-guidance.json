{ "plugin": "apparmor_status", "binary": "apparmor_status", "check": "which apparmor_status", "install_steps": ["apt-get install apparmor_status 2>/dev/null || which apparmor_status", "Verify: apparmor_status --version", "supercli plugins install ./plugins/apparmor_status --on-conflict replace --json"] }
