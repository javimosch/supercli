{
  "plugin": "encfs",
  "binary": "encfs",
  "check": "which encfs",
  "install_steps": [
    "brew install encfs",
    "Verify: encfs --version",
    "supercli plugins install ./plugins/encfs --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}