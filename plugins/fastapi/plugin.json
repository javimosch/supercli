{
  "name": "fastapi",
  "version": "1.0.0",
  "description": "fastapi \u2014 fastapi \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "fastapi"
    }
  ],
  "install_guidance": {
    "plugin": "fastapi",
    "binary": "fastapi",
    "check": "which fastapi",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "fastapi",
      "resource": "run",
      "action": "exec",
      "description": "fastapi \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "fastapi",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}