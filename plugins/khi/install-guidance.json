{
  "plugin": "khi",
  "binary": "khi",
  "check": "which khi",
  "install_steps": [
    "go install github.com/nicholasgasior/khi@latest",
    "Verify: khi --version",
    "supercli plugins install ./plugins/khi --on-conflict replace --json"
  ],
  "note": "Streamlined Kubernetes log viewing and filtering tool."
}
