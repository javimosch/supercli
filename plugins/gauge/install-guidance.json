{
  "plugin": "gauge",
  "binary": "gauge",
  "check": "which gauge",
  "install_steps": [
    "brew install gauge",
    "Verify: gauge --version",
    "supercli plugins install ./plugins/gauge --on-conflict replace --json"
  ]
}
