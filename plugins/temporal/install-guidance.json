{
  "plugin": "temporal",
  "binary": "temporal",
  "check": "which temporal",
  "install_steps": [
    "brew install temporal",
    "Verify: temporal --version",
    "supercli plugins install ./plugins/temporal --on-conflict replace --json"
  ],
  "note": "Also available via direct download from temporal.download or Docker (temporalio/temporal)."
}
