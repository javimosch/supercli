{
  "plugin": "mapscii",
  "binary": "mapscii",
  "check": "which mapscii",
  "install_steps": [
    "brew install mapscii",
    "Verify: mapscii --version",
    "supercli plugins install ./plugins/mapscii --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}