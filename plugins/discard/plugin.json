{
  "name": "discard",
  "version": "0.1.0",
  "description": "CLI tool: discard",
  "source": "https://packages.debian.org/discard",
  "checks": [{ "type": "binary", "name": "discard" }],
  "install_guidance": {
    "plugin": "discard", "binary": "discard", "check": "which discard",
    "install_steps": ["apt-get install discard 2>/dev/null || which discard", "Verify: discard --version", "supercli plugins install ./plugins/discard --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "discard", "resource": "_", "action": "_",
    "description": "Passthrough to discard CLI",
    "adapter": "process", "adapterConfig": { "command": "discard", "passthrough": true, "missingDependencyHelp": "Install discard: apt-get install discard" }, "args": []
  }]
}
