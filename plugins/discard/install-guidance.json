{ "plugin": "discard", "binary": "discard", "check": "which discard", "install_steps": ["apt-get install discard 2>/dev/null || which discard", "Verify: discard --version", "supercli plugins install ./plugins/discard --on-conflict replace --json"] }
