{
  "plugin": "bridle",
  "binary": "bridle",
  "check": "which bridle",
  "install_steps": [
    "brew install neiii/bridle/bridle",
    "Verify: bridle --version",
    "supercli plugins install ./plugins/bridle --on-conflict replace --json"
  ],
  "note": "Also available via cargo (cargo install bridle), npm (npm install -g bridle-ai), or from source."
}
