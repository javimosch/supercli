{
  "plugin": "safety-db",
  "binary": "safety-db",
  "check": "which safety-db",
  "install_steps": ["pip install safety-db", "Verify: safety-db --version", "supercli plugins install ./plugins/safety-db --on-conflict replace --json"]
}
