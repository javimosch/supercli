{
  "plugin": "do",
  "binary": "do",
  "check": "which do",
  "install_steps": ["go install github.com/do@latest", "Verify: do --version", "supercli plugins install ./plugins/do --on-conflict replace --json"]
}
