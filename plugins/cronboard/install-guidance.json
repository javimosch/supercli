{
  "plugin": "cronboard",
  "binary": "cronboard",
  "check": "which cronboard",
  "install_steps": [
    "pip install cronboard",
    "Verify: cronboard --version",
    "supercli plugins install ./plugins/cronboard --on-conflict replace --json"
  ],
  "note": "No auth required. CLI-only tool."
}
