{
  "plugin": "spec-driven-develop",
  "binary": "git",
  "check": "which git",
  "install_steps": [
    "git clone https://github.com/zhu1090093659/spec_driven_develop.git",
    "supercli plugins install ./plugins/spec-driven-develop --on-conflict replace --json",
    "sc skills teach spec-driven-develop:quickstart"
  ],
  "note": "Comes with 2 skills: Spec-Driven Develop (7-phase pipeline for large refactors/migrations) and Deep Discuss (structured 7-phase problem analysis). Uses S.U.P.E.R architecture principles (Single Purpose, Unidirectional Flow, Ports over Implementation, Environment-Agnostic, Replaceable Parts). Also installable via /plugin command in Claude Code."
}
