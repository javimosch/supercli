{
  "plugin": "replibyte",
  "binary": "replibyte",
  "check": "which replibyte",
  "install_steps": [
    "# Install replibyte",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/replibyte-linux-amd64 -o /usr/local/bin/replibyte",
    "chmod +x /usr/local/bin/replibyte",
    "# Or via package manager, see crates.io/crates/replibyte"
  ]
}