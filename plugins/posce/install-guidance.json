{
  "plugin": "posce",
  "binary": "posce",
  "check": "which posce",
  "install_steps": [
    "See https://github.com/vdt/posce",
    "Verify: posce --version",
    "supercli plugins install ./plugins/posce --on-conflict replace --json"
  ],
  "note": "See project repository"
}