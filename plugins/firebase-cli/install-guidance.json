{
  "plugin": "firebase-cli",
  "binary": "firebase-cli",
  "check": "which firebase-cli",
  "install_steps": [
    "brew install firebase-cli",
    "Verify: firebase-cli --version",
    "supercli plugins install ./plugins/firebase-cli --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}