{ "plugin": "adduser", "binary": "adduser", "check": "which adduser", "install_steps": ["apt-get install adduser 2>/dev/null || which adduser", "Verify: adduser --version", "supercli plugins install ./plugins/adduser --on-conflict replace --json"] }
