{
  "name": "adduser",
  "version": "0.1.0",
  "description": "CLI tool: adduser",
  "source": "https://packages.debian.org/adduser",
  "checks": [{ "type": "binary", "name": "adduser" }],
  "install_guidance": {
    "plugin": "adduser", "binary": "adduser", "check": "which adduser",
    "install_steps": ["apt-get install adduser 2>/dev/null || which adduser", "Verify: adduser --version", "supercli plugins install ./plugins/adduser --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "adduser", "resource": "_", "action": "_",
    "description": "Passthrough to adduser CLI",
    "adapter": "process", "adapterConfig": { "command": "adduser", "passthrough": true, "missingDependencyHelp": "Install adduser: apt-get install adduser" }, "args": []
  }]
}
