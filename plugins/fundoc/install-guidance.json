{
  "plugin": "fundoc",
  "binary": "fundoc",
  "check": "which fundoc",
  "install_steps": [
    "cargo install fundoc",
    "Verify: fundoc --help",
    "supercli plugins install ./plugins/fundoc --on-conflict replace --json"
  ],
  "note": "fundoc provides language agnostic documentation generation"
}
