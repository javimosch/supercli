{
  "name": "dh_installinfo",
  "version": "0.1.0",
  "description": "CLI tool: dh_installinfo",
  "source": "https://packages.debian.org/dh_installinfo",
  "checks": [{ "type": "binary", "name": "dh_installinfo" }],
  "install_guidance": {
    "plugin": "dh_installinfo", "binary": "dh_installinfo", "check": "which dh_installinfo",
    "install_steps": ["apt-get install dh_installinfo 2>/dev/null || which dh_installinfo", "Verify: dh_installinfo --version", "supercli plugins install ./plugins/dh_installinfo --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_installinfo", "resource": "_", "action": "_",
    "description": "Passthrough to dh_installinfo CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_installinfo", "passthrough": true, "missingDependencyHelp": "Install dh_installinfo: apt-get install dh_installinfo" }, "args": []
  }]
}
