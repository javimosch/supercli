{ "plugin": "dh_installinfo", "binary": "dh_installinfo", "check": "which dh_installinfo", "install_steps": ["apt-get install dh_installinfo 2>/dev/null || which dh_installinfo", "Verify: dh_installinfo --version", "supercli plugins install ./plugins/dh_installinfo --on-conflict replace --json"] }
