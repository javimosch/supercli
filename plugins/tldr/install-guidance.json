{
  "plugin": "tldr",
  "binary": "tldr",
  "check": "which tldr",
  "install_steps": [
    "npm install -g tldr",
    "Or: brew install tldr",
    "Or: cargo install tealdeer",
    "Verify: tldr --version",
    "supercli plugins install ./plugins/tldr --on-conflict replace --json"
  ],
  "note": "Multiple implementations available: tldr-node (npm), tealdeer (Rust/cargo), tldr-go (Go), tldr-python-client (pip). The npm version is the most widely used. Also available via: pip install tldr. First use will download the pages cache automatically."
}
