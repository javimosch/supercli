{
  "name": "dind",
  "version": "0.1.0",
  "description": "CLI tool: dind",
  "source": "https://packages.debian.org/dind",
  "checks": [{ "type": "binary", "name": "dind" }],
  "install_guidance": {
    "plugin": "dind", "binary": "dind", "check": "which dind",
    "install_steps": ["apt-get install dind 2>/dev/null || which dind", "Verify: dind --version", "supercli plugins install ./plugins/dind --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dind", "resource": "_", "action": "_",
    "description": "Passthrough to dind CLI",
    "adapter": "process", "adapterConfig": { "command": "dind", "passthrough": true, "missingDependencyHelp": "Install dind: apt-get install dind" }, "args": []
  }]
}
