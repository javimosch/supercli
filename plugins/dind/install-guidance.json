{ "plugin": "dind", "binary": "dind", "check": "which dind", "install_steps": ["apt-get install dind 2>/dev/null || which dind", "Verify: dind --version", "supercli plugins install ./plugins/dind --on-conflict replace --json"] }
