{ "plugin": "cd", "binary": "cd", "check": "which cd", "install_steps": ["apt-get install cd 2>/dev/null || which cd", "Verify: cd --version", "supercli plugins install ./plugins/cd --on-conflict replace --json"] }
