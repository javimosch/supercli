---
name: clay-zig-bindings
description: Use this skill when the user wants to use clay-zig-bindings for Zig bindings for the library clay: A high performance UI layout library in C..
---

# clay-zig-bindings Plugin

Zig bindings for the library clay: A high performance UI layout library in C.

## Commands

### General
- `clay-zig-bindings self version` — Print clay-zig-bindings version
- `clay-zig-bindings _ _` — Passthrough to clay-zig-bindings CLI

## Usage Examples
- "Run clay-zig-bindings"
- "Use clay-zig-bindings to Zig bindings for the library clay: A high performance UI lay"

## Installation

```bash
pip install clay-zig-bindings
```

## Key Features
- Zig bindings for the library clay: A high performance UI layout library in C.
- CLI tool, headless and scriptable
