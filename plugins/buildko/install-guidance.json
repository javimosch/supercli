{
  "plugin": "buildko",
  "binary": "buildko",
  "check": "which buildko",
  "install_steps": [
    "go install github.com/buildko/buildko@latest",
    "Verify: buildko --version",
    "supercli plugins install ./plugins/buildko --on-conflict replace --json"
  ],
  "note": "Binary name: buildko"
}
