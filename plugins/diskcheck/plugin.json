{
  "name": "diskcheck",
  "version": "0.1.0",
  "description": "CLI tool: diskcheck",
  "source": "https://packages.debian.org/diskcheck",
  "checks": [{ "type": "binary", "name": "diskcheck" }],
  "install_guidance": {
    "plugin": "diskcheck", "binary": "diskcheck", "check": "which diskcheck",
    "install_steps": ["apt-get install diskcheck 2>/dev/null || which diskcheck", "Verify: diskcheck --version", "supercli plugins install ./plugins/diskcheck --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "diskcheck", "resource": "_", "action": "_",
    "description": "Passthrough to diskcheck CLI",
    "adapter": "process", "adapterConfig": { "command": "diskcheck", "passthrough": true, "missingDependencyHelp": "Install diskcheck: apt-get install diskcheck" }, "args": []
  }]
}
