{ "plugin": "diskcheck", "binary": "diskcheck", "check": "which diskcheck", "install_steps": ["apt-get install diskcheck 2>/dev/null || which diskcheck", "Verify: diskcheck --version", "supercli plugins install ./plugins/diskcheck --on-conflict replace --json"] }
