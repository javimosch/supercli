{ "plugin": "at_log", "binary": "at_log", "check": "which at_log", "install_steps": ["apt-get install at_log 2>/dev/null || which at_log", "Verify: at_log --version", "supercli plugins install ./plugins/at_log --on-conflict replace --json"] }
