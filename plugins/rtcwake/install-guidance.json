{
  "plugin": "rtcwake",
  "binary": "rtcwake",
  "check": "which rtcwake",
  "install_steps": [
    "apt-get install rtcwake 2>/dev/null || which rtcwake",
    "Verify: rtcwake --version",
    "supercli plugins install ./plugins/rtcwake --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}