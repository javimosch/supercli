{
  "plugin": "s3m",
  "binary": "s3m",
  "check": "which s3m",
  "install_steps": [
    "cargo install s3m",
    "Verify: s3m --version",
    "supercli plugins install ./plugins/s3m --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}