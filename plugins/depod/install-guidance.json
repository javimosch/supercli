{ "plugin": "depod", "binary": "depod", "check": "which depod", "install_steps": ["apt-get install depod 2>/dev/null || which depod", "Verify: depod --version", "supercli plugins install ./plugins/depod --on-conflict replace --json"] }
