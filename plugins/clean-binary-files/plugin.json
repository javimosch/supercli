{
  "name": "clean-binary-files",
  "version": "0.1.0",
  "description": "CLI tool: clean-binary-files",
  "source": "https://packages.debian.org/clean-binary-files",
  "checks": [{ "type": "binary", "name": "clean-binary-files" }],
  "install_guidance": {
    "plugin": "clean-binary-files", "binary": "clean-binary-files", "check": "which clean-binary-files",
    "install_steps": ["apt-get install clean-binary-files 2>/dev/null || which clean-binary-files", "Verify: clean-binary-files --version", "supercli plugins install ./plugins/clean-binary-files --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "clean-binary-files", "resource": "_", "action": "_",
    "description": "Passthrough to clean-binary-files CLI",
    "adapter": "process", "adapterConfig": { "command": "clean-binary-files", "passthrough": true, "missingDependencyHelp": "Install clean-binary-files: apt-get install clean-binary-files" }, "args": []
  }]
}
