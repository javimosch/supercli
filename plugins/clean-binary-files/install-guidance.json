{ "plugin": "clean-binary-files", "binary": "clean-binary-files", "check": "which clean-binary-files", "install_steps": ["apt-get install clean-binary-files 2>/dev/null || which clean-binary-files", "Verify: clean-binary-files --version", "supercli plugins install ./plugins/clean-binary-files --on-conflict replace --json"] }
