{
  "name": "autosp",
  "version": "0.1.0",
  "description": "CLI tool: autosp",
  "source": "https://packages.debian.org/autosp",
  "checks": [{ "type": "binary", "name": "autosp" }],
  "install_guidance": {
    "plugin": "autosp", "binary": "autosp", "check": "which autosp",
    "install_steps": ["apt-get install autosp 2>/dev/null || which autosp", "Verify: autosp --version", "supercli plugins install ./plugins/autosp --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "autosp", "resource": "_", "action": "_",
    "description": "Passthrough to autosp CLI",
    "adapter": "process", "adapterConfig": { "command": "autosp", "passthrough": true, "missingDependencyHelp": "Install autosp: apt-get install autosp" }, "args": []
  }]
}
