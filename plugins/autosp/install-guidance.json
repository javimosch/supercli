{ "plugin": "autosp", "binary": "autosp", "check": "which autosp", "install_steps": ["apt-get install autosp 2>/dev/null || which autosp", "Verify: autosp --version", "supercli plugins install ./plugins/autosp --on-conflict replace --json"] }
