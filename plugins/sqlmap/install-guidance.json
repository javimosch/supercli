{
  "plugin": "sqlmap",
  "binary": "sqlmap",
  "check": "which sqlmap",
  "install_steps": [
    "brew install sqlmap",
    "Verify: sqlmap --version",
    "supercli plugins install ./plugins/sqlmap --on-conflict replace --json"
  ]
}
