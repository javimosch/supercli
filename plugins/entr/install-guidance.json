{
  "plugin": "entr",
  "binary": "entr",
  "check": "which entr",
  "install_steps": [
    "brew install entr",
    "Or download from http://eradman.com/entrproject/",
    "Verify: entr -v",
    "supercli plugins install ./plugins/entr --on-conflict replace --json"
  ],
  "note": "Entr receives file paths from stdin and runs a command when any of them change. Use find or ls to pipe file list to entr."
}
