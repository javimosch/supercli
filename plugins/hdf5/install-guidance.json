{
  "plugin": "hdf5",
  "binary": "hdf5",
  "check": "which hdf5",
  "install_steps": [
    "brew install hdf5",
    "Verify: hdf5 --version",
    "supercli plugins install ./plugins/hdf5 --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}