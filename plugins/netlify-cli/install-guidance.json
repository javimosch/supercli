{
  "plugin": "netlify-cli",
  "binary": "netlify",
  "check": "which netlify",
  "install_steps": [
    "npm install -g netlify-cli",
    "Verify: netlify --version",
    "Authenticate: netlify login",
    "Alternative: export NETLIFY_AUTH_TOKEN=...",
    "supercli plugins install ./plugins/netlify-cli --on-conflict replace --json"
  ],
  "note": "Also available as npx: npx netlify-cli. Use --json flag on most commands for structured output. Requires a Netlify account (free tier available)."
}
