{
  "plugin": "ldapurl",
  "binary": "ldapurl",
  "check": "which ldapurl",
  "install_steps": [
    "apt-get install ldapurl 2>/dev/null || which ldapurl",
    "Verify: ldapurl --version",
    "supercli plugins install ./plugins/ldapurl --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}