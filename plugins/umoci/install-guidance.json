{
  "plugin": "umoci",
  "binary": "umoci",
  "check": "which umoci",
  "install_steps": [
    "brew install umoci",
    "Verify: umoci --version",
    "supercli plugins install ./plugins/umoci --on-conflict replace --json"
  ]
}