{
  "plugin": "hetzner-cli",
  "binary": "hcloud",
  "check": "which hcloud",
  "install_steps": [
    "brew install hcloud",
    "Verify: hcloud --version",
    "supercli plugins install ./plugins/hetzner-cli --on-conflict replace --json"
  ]
}
