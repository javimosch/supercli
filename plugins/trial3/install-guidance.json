{
  "plugin": "trial3",
  "binary": "trial3",
  "check": "which trial3",
  "install_steps": [
    "apt-get install trial3 2>/dev/null || which trial3",
    "Verify: trial3 --version",
    "supercli plugins install ./plugins/trial3 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}