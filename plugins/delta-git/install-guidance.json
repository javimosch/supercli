{
  "plugin": "delta-git",
  "binary": "delta",
  "check": "which delta",
  "install_steps": [
    "cargo install git-delta",
    "Verify: delta --version",
    "supercli plugins install ./plugins/delta-git --on-conflict replace --json"
  ],
  "note": "Binary name: delta"
}
