---
name: git-annex-2
description: git-annex — manage files with git without checking them in. Sync large files across repositories and remotes.
---
# git-annex-2
git-annex — manage files with git without checking them in. Sync large files across repositories and remotes.
