{
  "plugin": "shipa-cli",
  "binary": "shipa",
  "check": "which shipa",
  "install_steps": ["brew install shipa", "Verify: shipa --version", "supercli plugins install ./plugins/shipa-cli --on-conflict replace --json"]
}
