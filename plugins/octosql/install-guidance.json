{
  "plugin": "octosql",
  "binary": "octosql",
  "check": "which octosql",
  "install_steps": [
    "brew install octosql",
    "Verify: octosql --version",
    "supercli plugins install ./plugins/octosql --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/cube2222/octosql/cmd/octosql@latest. Supports multiple data sources including CSV, JSON, PostgreSQL, MySQL, Redis, and more. Use SQL to query and join data from different sources."
}
