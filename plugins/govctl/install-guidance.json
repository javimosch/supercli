{
  "plugin": "govctl",
  "binary": "govctl",
  "check": "which govctl",
  "install_steps": [
    "cargo install govctl",
    "Verify: govctl --version",
    "supercli plugins install ./plugins/govctl --on-conflict replace --json"
  ],
  "note": "Also available via cargo binstall govctl"
}
