{
  "plugin": "jmap",
  "binary": "jmap",
  "check": "which jmap",
  "install_steps": [
    "apt-get install jmap 2>/dev/null || which jmap",
    "Verify: jmap --version",
    "supercli plugins install ./plugins/jmap --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}