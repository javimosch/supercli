{
  "plugin": "systemd-stdio-bridge",
  "binary": "systemd-stdio-bridge",
  "check": "which systemd-stdio-bridge",
  "install_steps": [
    "apt-get install systemd-stdio-bridge 2>/dev/null || which systemd-stdio-bridge",
    "Verify: systemd-stdio-bridge --version",
    "supercli plugins install ./plugins/systemd-stdio-bridge --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}