{ "plugin": "crda", "binary": "crda", "check": "which crda", "install_steps": ["apt-get install crda 2>/dev/null || which crda", "Verify: crda --version", "supercli plugins install ./plugins/crda --on-conflict replace --json"] }
