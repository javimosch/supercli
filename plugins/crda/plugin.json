{
  "name": "crda",
  "version": "0.1.0",
  "description": "CLI tool: crda",
  "source": "https://packages.debian.org/crda",
  "checks": [{ "type": "binary", "name": "crda" }],
  "install_guidance": {
    "plugin": "crda", "binary": "crda", "check": "which crda",
    "install_steps": ["apt-get install crda 2>/dev/null || which crda", "Verify: crda --version", "supercli plugins install ./plugins/crda --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "crda", "resource": "_", "action": "_",
    "description": "Passthrough to crda CLI",
    "adapter": "process", "adapterConfig": { "command": "crda", "passthrough": true, "missingDependencyHelp": "Install crda: apt-get install crda" }, "args": []
  }]
}
