{
  "plugin": "git-filter-repo",
  "binary": "git-filter-repo",
  "check": "which git-filter-repo",
  "install_steps": [
    "pip3 install --user git-filter-repo",
    "Verify: git-filter-repo --version",
    "Or: brew install git-filter-repo",
    "supercli plugins install ./plugins/git-filter-repo --on-conflict replace --json"
  ],
  "note": "git-filter-repo requires Python 3. It is the officially recommended tool for rewriting Git history (replaces git filter-branch). Can also be used via 'git filter-repo' if installed as a contrib. For macOS, brew install is easiest. For Linux, pip3 is recommended. See https://github.com/newren/git-filter-repo for full docs."
}
