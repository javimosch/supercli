{
  "plugin": "genrb",
  "binary": "genrb",
  "check": "which genrb",
  "install_steps": [
    "apt-get install genrb 2>/dev/null || which genrb",
    "Verify: genrb --version",
    "supercli plugins install ./plugins/genrb --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}