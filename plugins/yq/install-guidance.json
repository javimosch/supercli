{
  "plugin": "yq",
  "binary": "yq",
  "check": "which yq",
  "install_steps": [
    "brew install yq",
    "Verify: yq --version",
    "supercli plugins install ./plugins/yq --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/mikefarah/yq/v4@latest. Supports YAML, JSON, XML, CSV, TOML, HCL, and properties formats. Can convert between formats. Uses jq-like syntax for querying."
}
