{
  "plugin": "shot-cli",
  "binary": "shot-cli",
  "check": "which shot-cli",
  "install_steps": ["brew install shot-cli", "Verify: shot-cli --version", "supercli plugins install ./plugins/shot-cli --on-conflict replace --json"]
}
