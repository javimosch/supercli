{
  "plugin": "jrnl",
  "binary": "jrnl",
  "check": "which jrnl",
  "install_steps": [
    "pip install jrnl",
    "Verify: jrnl --version",
    "Initialize: jrnl (creates default config and journal)",
    "supercli plugins install ./plugins/jrnl --on-conflict replace --json"
  ],
  "note": "Also available via: brew install jrnl. Journal files stored locally in ~/.config/jrnl/journal.txt by default. Supports encrypted journals (AES-256) and JSON export for agent consumption."
}
