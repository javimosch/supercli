{
  "plugin": "pdfx",
  "binary": "pdfx",
  "check": "which pdfx",
  "install_steps": [
    "npm install -g pdfx",
    "Verify: pdfx --version",
    "supercli plugins install ./plugins/pdfx --on-conflict replace --json"
  ],
  "note": "PDF component library CLI for seamless PDF document creation."
}