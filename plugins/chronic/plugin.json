{
  "name": "chronic",
  "version": "0.1.0",
  "description": "CLI tool: chronic",
  "source": "https://packages.debian.org/chronic",
  "checks": [{ "type": "binary", "name": "chronic" }],
  "install_guidance": {
    "plugin": "chronic", "binary": "chronic", "check": "which chronic",
    "install_steps": ["apt-get install chronic 2>/dev/null || which chronic", "Verify: chronic --version", "supercli plugins install ./plugins/chronic --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "chronic", "resource": "_", "action": "_",
    "description": "Passthrough to chronic CLI",
    "adapter": "process", "adapterConfig": { "command": "chronic", "passthrough": true, "missingDependencyHelp": "Install chronic: apt-get install chronic" }, "args": []
  }]
}
