{ "plugin": "chronic", "binary": "chronic", "check": "which chronic", "install_steps": ["apt-get install chronic 2>/dev/null || which chronic", "Verify: chronic --version", "supercli plugins install ./plugins/chronic --on-conflict replace --json"] }
