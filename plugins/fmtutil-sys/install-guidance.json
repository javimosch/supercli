{
  "plugin": "fmtutil-sys",
  "binary": "fmtutil-sys",
  "check": "which fmtutil-sys",
  "install_steps": [
    "apt-get install fmtutil-sys 2>/dev/null || which fmtutil-sys",
    "Verify: fmtutil-sys --version",
    "supercli plugins install ./plugins/fmtutil-sys --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}