{
  "plugin": "elm",
  "binary": "elm",
  "check": "which elm",
  "install_steps": [
    "# Install elm",
    "# See elm-lang.org",
    "# apt: sudo apt-get install elm",
    "# brew: brew install elm",
    "# cargo: cargo install elm",
    "# npm: npm install -g elm"
  ]
}