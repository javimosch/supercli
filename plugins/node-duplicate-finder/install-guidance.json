{
  "plugin": "node-duplicate-finder",
  "binary": "dedup",
  "check": "which dedup",
  "install_steps": [
    "npm install -g node-duplicate-finder",
    "Verify: dedup --help",
    "supercli plugins install ./plugins/node-duplicate-finder --on-conflict replace --json"
  ],
  "note": "Node-duplicate-finder (dedup) scans directories to find duplicate files. Uses file content hashing for accurate comparison. Supports recursive scanning, ignore patterns, and file type filtering."
}
