{
  "plugin": "cups.lpadmin",
  "binary": "cups.lpadmin",
  "check": "which cups.lpadmin",
  "install_steps": [
    "apt-get install cups.lpadmin 2>/dev/null || which cups.lpadmin",
    "Verify: cups.lpadmin --version",
    "supercli plugins install ./plugins/cups.lpadmin --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}