{
  "plugin": "jjs",
  "binary": "jjs",
  "check": "which jjs",
  "install_steps": [
    "apt-get install jjs 2>/dev/null || which jjs",
    "Verify: jjs --version",
    "supercli plugins install ./plugins/jjs --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}