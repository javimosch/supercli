{
  "plugin": "graphql-cli",
  "binary": "graphql-cli",
  "check": "which graphql-cli",
  "install_steps": ["npm install -g graphql-cli", "Verify: graphql-cli --version", "supercli plugins install ./plugins/graphql-cli --on-conflict replace --json"]
}
