{
  "plugin": "ktor",
  "binary": "ktor",
  "check": "which ktor",
  "install_steps": [
    "brew install ktor",
    "Verify: ktor --version",
    "supercli plugins install ./plugins/ktor --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}