{
  "plugin": "urfave-cli",
  "binary": "urfave-cli",
  "check": "which urfave-cli",
  "install_steps": [
    "brew install urfave-cli",
    "Verify: urfave-cli --version",
    "supercli plugins install ./plugins/urfave-cli --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/urfave/cli/v2/cmd/urfave-cli@latest. A command-line interface framework for Go. Build powerful CLI applications with flags, subcommands, and help text generation. Popular Go CLI framework."
}
