{
  "plugin": "hashcat",
  "binary": "hashcat",
  "check": "which hashcat",
  "install_steps": [
    "brew install hashcat",
    "Verify: hashcat --version",
    "supercli plugins install ./plugins/hashcat --on-conflict replace --json"
  ]
}
