{
  "plugin": "stash",
  "binary": "stash",
  "check": "which stash",
  "install_steps": ["brew install stash", "Verify: stash --version", "supercli plugins install ./plugins/stash --on-conflict replace --json"]
}
