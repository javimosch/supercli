{
  "plugin": "jcmd",
  "binary": "jcmd",
  "check": "which jcmd",
  "install_steps": [
    "apt-get install jcmd 2>/dev/null || which jcmd",
    "Verify: jcmd --version",
    "supercli plugins install ./plugins/jcmd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}