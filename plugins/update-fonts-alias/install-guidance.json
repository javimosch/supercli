{
  "plugin": "update-fonts-alias",
  "binary": "update-fonts-alias",
  "check": "which update-fonts-alias",
  "install_steps": [
    "apt-get install update-fonts-alias 2>/dev/null || which update-fonts-alias",
    "Verify: update-fonts-alias --version",
    "supercli plugins install ./plugins/update-fonts-alias --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}