{
  "plugin": "pdfmom",
  "binary": "pdfmom",
  "check": "which pdfmom",
  "install_steps": [
    "apt-get install pdfmom 2>/dev/null || which pdfmom",
    "Verify: pdfmom --version",
    "supercli plugins install ./plugins/pdfmom --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}