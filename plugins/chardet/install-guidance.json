{ "plugin": "chardet", "binary": "chardet", "check": "which chardet", "install_steps": ["apt-get install chardet 2>/dev/null || which chardet", "Verify: chardet --version", "supercli plugins install ./plugins/chardet --on-conflict replace --json"] }
