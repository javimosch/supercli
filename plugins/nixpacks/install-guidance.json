{
  "plugin": "nixpacks",
  "binary": "nixpacks",
  "check": "which nixpacks",
  "install_steps": [
    "brew install nixpacks",
    "Verify: nixpacks --version",
    "supercli plugins install ./plugins/nixpacks --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install nixpacks. Automatically detects programming languages and builds Docker images. Uses Nix packages for dependencies. Supports many languages including Node.js, Python, Go, Rust, Java, and more."
}
