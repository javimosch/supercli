{
  "plugin": "cargo-llvm-cov",
  "binary": "cargo-llvm-cov",
  "check": "cargo llvm-cov --version",
  "install_steps": [
    "cargo install cargo-llvm-cov",
    "Verify: cargo llvm-cov --version",
    "supercli plugins install ./plugins/cargo-llvm-cov --on-conflict replace --json"
  ],
  "note": "Also available via binary download. Cargo subcommand for LLVM source-based code coverage for Rust. Generates HTML, LCOV, and JSON coverage reports. Requires rustc and cargo. Great for CI/CD coverage reporting."
}
