{
  "plugin": "avra",
  "binary": "avra",
  "check": "which avra",
  "install_steps": [
    "brew install avra",
    "Verify: avra --version",
    "supercli plugins install ./plugins/avra --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}