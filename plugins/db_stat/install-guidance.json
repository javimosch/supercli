{ "plugin": "db_stat", "binary": "db_stat", "check": "which db_stat", "install_steps": ["apt-get install db_stat 2>/dev/null || which db_stat", "Verify: db_stat --version", "supercli plugins install ./plugins/db_stat --on-conflict replace --json"] }
