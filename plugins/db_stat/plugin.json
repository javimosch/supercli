{
  "name": "db_stat",
  "version": "0.1.0",
  "description": "CLI tool: db_stat",
  "source": "https://packages.debian.org/db_stat",
  "checks": [{ "type": "binary", "name": "db_stat" }],
  "install_guidance": {
    "plugin": "db_stat", "binary": "db_stat", "check": "which db_stat",
    "install_steps": ["apt-get install db_stat 2>/dev/null || which db_stat", "Verify: db_stat --version", "supercli plugins install ./plugins/db_stat --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_stat", "resource": "_", "action": "_",
    "description": "Passthrough to db_stat CLI",
    "adapter": "process", "adapterConfig": { "command": "db_stat", "passthrough": true, "missingDependencyHelp": "Install db_stat: apt-get install db_stat" }, "args": []
  }]
}
