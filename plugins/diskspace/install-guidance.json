{ "plugin": "diskspace", "binary": "diskspace", "check": "which diskspace", "install_steps": ["apt-get install diskspace 2>/dev/null || which diskspace", "Verify: diskspace --version", "supercli plugins install ./plugins/diskspace --on-conflict replace --json"] }
