{
  "name": "diskspace",
  "version": "0.1.0",
  "description": "CLI tool: diskspace",
  "source": "https://packages.debian.org/diskspace",
  "checks": [{ "type": "binary", "name": "diskspace" }],
  "install_guidance": {
    "plugin": "diskspace", "binary": "diskspace", "check": "which diskspace",
    "install_steps": ["apt-get install diskspace 2>/dev/null || which diskspace", "Verify: diskspace --version", "supercli plugins install ./plugins/diskspace --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "diskspace", "resource": "_", "action": "_",
    "description": "Passthrough to diskspace CLI",
    "adapter": "process", "adapterConfig": { "command": "diskspace", "passthrough": true, "missingDependencyHelp": "Install diskspace: apt-get install diskspace" }, "args": []
  }]
}
