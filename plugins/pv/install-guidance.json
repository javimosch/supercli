{
  "plugin": "pv",
  "binary": "pv",
  "check": "which pv",
  "install_steps": [
    "brew install pv",
    "Verify: pv --version",
    "supercli plugins install ./plugins/pv --on-conflict replace --json"
  ]
}
