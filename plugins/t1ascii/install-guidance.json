{
  "plugin": "t1ascii",
  "binary": "t1ascii",
  "check": "which t1ascii",
  "install_steps": [
    "apt-get install t1ascii 2>/dev/null || which t1ascii",
    "Verify: t1ascii --version",
    "supercli plugins install ./plugins/t1ascii --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}