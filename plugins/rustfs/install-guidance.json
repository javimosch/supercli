{
  "plugin": "rustfs",
  "binary": "rustfs",
  "check": "which rustfs",
  "install_steps": [
    "pip install rustfs",
    "Verify: rustfs --version",
    "supercli plugins install ./plugins/rustfs --on-conflict replace --json"
  ],
  "note": "Language: Rust"
}