{
  "plugin": "nomnoml-cli",
  "binary": "nomnoml-cli",
  "check": "which nomnoml-cli",
  "install_steps": ["npm install -g nomnoml-cli", "Verify: nomnoml-cli --version", "supercli plugins install ./plugins/nomnoml-cli --on-conflict replace --json"]
}
