{
  "plugin": "rackdiag",
  "binary": "rackdiag",
  "check": "which rackdiag",
  "install_steps": ["pip install rackdiag", "Verify: rackdiag --version", "supercli plugins install ./plugins/rackdiag --on-conflict replace --json"]
}
