{
  "plugin": "ftl",
  "binary": "ftl",
  "check": "which ftl",
  "install_steps": [
    "brew install yarlson/tap/ftl",
    "Verify: ftl --version",
    "supercli plugins install ./plugins/ftl --on-conflict replace --json"
  ]
}