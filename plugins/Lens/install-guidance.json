{
  "plugin": "Lens",
  "binary": "Lens",
  "check": "which Lens",
  "install_steps": [
    "apt-get install Lens 2>/dev/null || which Lens",
    "Verify: Lens --version",
    "supercli plugins install ./plugins/Lens --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}