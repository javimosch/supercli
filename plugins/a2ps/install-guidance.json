{ "plugin": "a2ps", "binary": "a2ps", "check": "which a2ps", "install_steps": ["apt-get install a2ps 2>/dev/null || which a2ps", "Verify: a2ps --version", "supercli plugins install ./plugins/a2ps --on-conflict replace --json"] }
