{
  "name": "deja-dup",
  "version": "0.1.0",
  "description": "CLI tool: deja-dup",
  "source": "https://packages.debian.org/deja-dup",
  "checks": [{ "type": "binary", "name": "deja-dup" }],
  "install_guidance": {
    "plugin": "deja-dup", "binary": "deja-dup", "check": "which deja-dup",
    "install_steps": ["apt-get install deja-dup 2>/dev/null || which deja-dup", "Verify: deja-dup --version", "supercli plugins install ./plugins/deja-dup --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "deja-dup", "resource": "_", "action": "_",
    "description": "Passthrough to deja-dup CLI",
    "adapter": "process", "adapterConfig": { "command": "deja-dup", "passthrough": true, "missingDependencyHelp": "Install deja-dup: apt-get install deja-dup" }, "args": []
  }]
}
