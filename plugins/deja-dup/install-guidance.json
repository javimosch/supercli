{ "plugin": "deja-dup", "binary": "deja-dup", "check": "which deja-dup", "install_steps": ["apt-get install deja-dup 2>/dev/null || which deja-dup", "Verify: deja-dup --version", "supercli plugins install ./plugins/deja-dup --on-conflict replace --json"] }
