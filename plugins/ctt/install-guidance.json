{ "plugin": "ctt", "binary": "ctt", "check": "which ctt", "install_steps": ["apt-get install ctt 2>/dev/null || which ctt", "Verify: ctt --version", "supercli plugins install ./plugins/ctt --on-conflict replace --json"] }
