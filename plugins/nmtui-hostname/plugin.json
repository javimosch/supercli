{
  "name": "nmtui-hostname",
  "version": "0.1.0",
  "description": "CLI tool: nmtui-hostname",
  "source": "https://manpages.debian.org/nmtui-hostname",
  "checks": [
    {
      "type": "binary",
      "name": "nmtui-hostname"
    }
  ],
  "install_guidance": {
    "plugin": "nmtui-hostname",
    "binary": "nmtui-hostname",
    "check": "which nmtui-hostname",
    "install_steps": [
      "apt-get install nmtui-hostname 2>/dev/null || which nmtui-hostname",
      "Verify: nmtui-hostname --version",
      "supercli plugins install ./plugins/nmtui-hostname --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "nmtui-hostname",
      "resource": "self",
      "action": "version",
      "description": "Print nmtui-hostname version",
      "adapter": "process",
      "adapterConfig": {
        "command": "nmtui-hostname",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install nmtui-hostname: apt-get install nmtui-hostname 2>/dev/null || which nmtui-hostname"
      },
      "args": []
    },
    {
      "namespace": "nmtui-hostname",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to nmtui-hostname CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "nmtui-hostname",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install nmtui-hostname: apt-get install nmtui-hostname 2>/dev/null || which nmtui-hostname"
      },
      "args": []
    }
  ]
}