{
  "plugin": "grmon",
  "binary": "grmon",
  "check": "which grmon",
  "install_steps": [
    "go install github.com/bcicen/grmon@latest",
    "Verify: grmon --version",
    "supercli plugins install ./plugins/grmon --on-conflict replace --json"
  ]
}