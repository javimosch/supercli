{
  "name": "dpkg-buildflags",
  "version": "1.0.0",
  "description": "dpkg-buildflags \u2014 dpkg-buildflags \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dpkg-buildflags"
    }
  ],
  "install_guidance": {
    "plugin": "dpkg-buildflags",
    "binary": "dpkg-buildflags",
    "check": "which dpkg-buildflags",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dpkg-buildflags",
      "resource": "run",
      "action": "exec",
      "description": "dpkg-buildflags \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dpkg-buildflags",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}