{
  "plugin": "hp-testpage",
  "binary": "hp-testpage",
  "check": "which hp-testpage",
  "install_steps": [
    "apt-get install hp-testpage 2>/dev/null || which hp-testpage",
    "Verify: hp-testpage --version",
    "supercli plugins install ./plugins/hp-testpage --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}