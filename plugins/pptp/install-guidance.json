{
  "plugin": "pptp",
  "binary": "pptp",
  "check": "which pptp",
  "install_steps": [
    "apt-get install pptp 2>/dev/null || which pptp",
    "Verify: pptp --version",
    "supercli plugins install ./plugins/pptp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}