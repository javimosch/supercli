{ "plugin": "contextfs", "binary": "contextfs", "check": "which contextfs", "install_steps": ["apt-get install contextfs 2>/dev/null || which contextfs", "Verify: contextfs --version", "supercli plugins install ./plugins/contextfs --on-conflict replace --json"] }
