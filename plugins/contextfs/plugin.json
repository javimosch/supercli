{
  "name": "contextfs",
  "version": "0.1.0",
  "description": "CLI tool: contextfs",
  "source": "https://packages.debian.org/contextfs",
  "checks": [{ "type": "binary", "name": "contextfs" }],
  "install_guidance": {
    "plugin": "contextfs", "binary": "contextfs", "check": "which contextfs",
    "install_steps": ["apt-get install contextfs 2>/dev/null || which contextfs", "Verify: contextfs --version", "supercli plugins install ./plugins/contextfs --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "contextfs", "resource": "_", "action": "_",
    "description": "Passthrough to contextfs CLI",
    "adapter": "process", "adapterConfig": { "command": "contextfs", "passthrough": true, "missingDependencyHelp": "Install contextfs: apt-get install contextfs" }, "args": []
  }]
}
