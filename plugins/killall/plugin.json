{
  "name": "killall",
  "version": "0.1.0",
  "description": "killall — Kill processes by name",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "killall"
    }
  ],
  "install_guidance": {
    "plugin": "killall",
    "binary": "killall",
    "check": "which killall",
    "install_steps": [
      "base system",
      "supercli plugins install ./plugins/killall --on-conflict replace --json"
    ],
    "note": "Standard system utility."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "killall",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to killall CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "killall",
        "passthrough": true,
        "missingDependencyHelp": "Install killall: base system"
      },
      "args": []
    }
  ]
}
