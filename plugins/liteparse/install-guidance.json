{
  "plugin": "liteparse",
  "binary": "lit",
  "check": "which lit",
  "install_steps": [
    "cargo install liteparse",
    "Verify: lit --version",
    "supercli plugins install ./plugins/liteparse --on-conflict replace --json"
  ],
  "note": "Also available via npm (@llamaindex/liteparse) and pip (liteparse). Supports PDF, DOCX, XLSX, PPTX, and images."
}