{
  "plugin": "filecheck",
  "binary": "filecheck",
  "check": "which filecheck",
  "install_steps": [
    "cargo install filecheck",
    "Verify: filecheck --version",
    "supercli plugins install ./plugins/filecheck --on-conflict replace --json"
  ]
}
