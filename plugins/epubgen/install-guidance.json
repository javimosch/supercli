{
  "plugin": "epubgen",
  "binary": "epubgen",
  "check": "which epubgen",
  "install_steps": ["swift build -c release", "Verify: epubgen --version", "supercli plugins install ./plugins/epubgen --on-conflict replace --json"]
}
