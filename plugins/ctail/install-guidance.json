{ "plugin": "ctail", "binary": "ctail", "check": "which ctail", "install_steps": ["apt-get install ctail 2>/dev/null || which ctail", "Verify: ctail --version", "supercli plugins install ./plugins/ctail --on-conflict replace --json"] }
