{
  "name": "check_real",
  "version": "0.1.0",
  "description": "CLI tool: check_real",
  "source": "https://packages.debian.org/check_real",
  "checks": [{ "type": "binary", "name": "check_real" }],
  "install_guidance": {
    "plugin": "check_real", "binary": "check_real", "check": "which check_real",
    "install_steps": ["apt-get install check_real 2>/dev/null || which check_real", "Verify: check_real --version", "supercli plugins install ./plugins/check_real --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check_real", "resource": "_", "action": "_",
    "description": "Passthrough to check_real CLI",
    "adapter": "process", "adapterConfig": { "command": "check_real", "passthrough": true, "missingDependencyHelp": "Install check_real: apt-get install check_real" }, "args": []
  }]
}
