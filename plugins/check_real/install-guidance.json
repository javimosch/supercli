{ "plugin": "check_real", "binary": "check_real", "check": "which check_real", "install_steps": ["apt-get install check_real 2>/dev/null || which check_real", "Verify: check_real --version", "supercli plugins install ./plugins/check_real --on-conflict replace --json"] }
