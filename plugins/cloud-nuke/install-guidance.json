{
  "plugin": "cloud-nuke",
  "binary": "cloud-nuke",
  "check": "which cloud-nuke",
  "install_steps": ["brew install cloud-nuke", "Verify: cloud-nuke --version", "supercli plugins install ./plugins/cloud-nuke --on-conflict replace --json"]
}
