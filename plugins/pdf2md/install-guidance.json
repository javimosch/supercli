{
  "plugin": "pdf2md",
  "binary": "pdf2md",
  "check": "which pdf2md",
  "install_steps": [
    "npm install -g @opendocsg/pdf2md",
    "Verify: npx @opendocsg/pdf2md --help",
    "supercli plugins install ./plugins/pdf2md --on-conflict replace --json"
  ],
  "note": "PDF2md converts PDF files to Markdown. For large recursive conversions, use node lib/pdf2md-cli.js with --max-old-space-size flag to avoid memory issues."
}
