{
  "plugin": "conjure",
  "binary": "conjure",
  "check": "which conjure",
  "install_steps": [
    "# Install conjure",
    "# See github.com/conjure-cp/conjure",
    "# apt: sudo apt-get install conjure",
    "# brew: brew install conjure",
    "# cargo: cargo install conjure",
    "# npm: npm install -g conjure"
  ]
}