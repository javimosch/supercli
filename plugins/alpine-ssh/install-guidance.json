{
  "plugin": "alpine-ssh",
  "binary": "alpine-ssh",
  "check": "which alpine-ssh",
  "install_steps": ["docker run --rm -it typezac/alpine-ssh", "Verify: alpine-ssh --version", "supercli plugins install ./plugins/alpine-ssh --on-conflict replace --json"]
}
