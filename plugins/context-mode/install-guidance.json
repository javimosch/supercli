{
  "plugin": "context-mode",
  "binary": "context-mode",
  "check": "which context-mode",
  "install_steps": [
    "npm install -g context-mode",
    "Verify: context-mode --version",
    "supercli plugins install ./plugins/context-mode --on-conflict replace --json"
  ],
  "note": "For Claude Code: /plugin marketplace add mksglu/context-mode && /plugin install context-mode@context-mode. For Gemini CLI: add MCP server to ~/.gemini/settings.json. For other platforms: claude mcp add context-mode -- npx -y context-mode. See https://github.com/mksglu/context-mode for full platform-specific install guides."
}
