{
  "plugin": "grub-syslinux2cfg",
  "binary": "grub-syslinux2cfg",
  "check": "which grub-syslinux2cfg",
  "install_steps": [
    "apt-get install grub-syslinux2cfg 2>/dev/null || which grub-syslinux2cfg",
    "Verify: grub-syslinux2cfg --version",
    "supercli plugins install ./plugins/grub-syslinux2cfg --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}