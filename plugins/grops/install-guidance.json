{
  "plugin": "grops",
  "binary": "grops",
  "check": "which grops",
  "install_steps": [
    "apt-get install grops 2>/dev/null || which grops",
    "Verify: grops --version",
    "supercli plugins install ./plugins/grops --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}