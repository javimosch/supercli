{
  "plugin": "psjoin",
  "binary": "psjoin",
  "check": "which psjoin",
  "install_steps": [
    "apt-get install psjoin 2>/dev/null || which psjoin",
    "Verify: psjoin --version",
    "supercli plugins install ./plugins/psjoin --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}