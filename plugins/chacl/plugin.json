{
  "name": "chacl",
  "version": "0.1.0",
  "description": "CLI tool: chacl",
  "source": "https://packages.debian.org/chacl",
  "checks": [{ "type": "binary", "name": "chacl" }],
  "install_guidance": {
    "plugin": "chacl", "binary": "chacl", "check": "which chacl",
    "install_steps": ["apt-get install chacl 2>/dev/null || which chacl", "Verify: chacl --version", "supercli plugins install ./plugins/chacl --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "chacl", "resource": "_", "action": "_",
    "description": "Passthrough to chacl CLI",
    "adapter": "process", "adapterConfig": { "command": "chacl", "passthrough": true, "missingDependencyHelp": "Install chacl: apt-get install chacl" }, "args": []
  }]
}
