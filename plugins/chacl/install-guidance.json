{ "plugin": "chacl", "binary": "chacl", "check": "which chacl", "install_steps": ["apt-get install chacl 2>/dev/null || which chacl", "Verify: chacl --version", "supercli plugins install ./plugins/chacl --on-conflict replace --json"] }
