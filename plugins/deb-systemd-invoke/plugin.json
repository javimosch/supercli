{
  "name": "deb-systemd-invoke",
  "version": "1.0.0",
  "description": "deb-systemd-invoke \u2014 deb-systemd-invoke \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "deb-systemd-invoke"
    }
  ],
  "install_guidance": {
    "plugin": "deb-systemd-invoke",
    "binary": "deb-systemd-invoke",
    "check": "which deb-systemd-invoke",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "deb-systemd-invoke",
      "resource": "run",
      "action": "exec",
      "description": "deb-systemd-invoke \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "deb-systemd-invoke",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}