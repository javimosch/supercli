{
  "name": "debconf-apt-progress",
  "version": "1.0.0",
  "description": "debconf-apt-progress \u2014 debconf-apt-progress \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "debconf-apt-progress"
    }
  ],
  "install_guidance": {
    "plugin": "debconf-apt-progress",
    "binary": "debconf-apt-progress",
    "check": "which debconf-apt-progress",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "debconf-apt-progress",
      "resource": "run",
      "action": "exec",
      "description": "debconf-apt-progress \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "debconf-apt-progress",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}