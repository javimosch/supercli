{
  "plugin": "ccusage",
  "binary": "ccusage",
  "check": "which ccusage",
  "install_steps": [
    "npm install -g ccusage",
    "Verify: ccusage --version",
    "supercli plugins install ./plugins/ccusage --on-conflict replace --json"
  ],
  "note": "Also works via bunx/npx without global install. Supports --json, --offline flags. Reads local usage data from Claude Code, Codex, OpenCode, Gemini CLI, and 12+ more sources."
}