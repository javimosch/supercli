{
  "plugin": "loffice",
  "binary": "loffice",
  "check": "which loffice",
  "install_steps": [
    "apt-get install loffice 2>/dev/null || which loffice",
    "Verify: loffice --version",
    "supercli plugins install ./plugins/loffice --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}