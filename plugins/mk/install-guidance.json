{
  "plugin": "mk",
  "binary": "mk",
  "check": "which mk",
  "install_steps": [
    "brew install mk",
    "Verify: mk --version",
    "supercli plugins install ./plugins/mk --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}