{
  "plugin": "trippy",
  "binary": "trip",
  "check": "which trip",
  "install_steps": [
    "brew install trippy",
    "Or download from https://github.com/fujiapple852/trippy/releases",
    "Verify: trip --version",
    "supercli plugins install ./plugins/trippy --on-conflict replace --json"
  ],
  "note": "Binary is `trip`. Available via brew, cargo install trippy, apt, dnf, pacman, and GitHub releases (.deb/.rpm). Use --unprivileged to trace without root on supported platforms."
}
