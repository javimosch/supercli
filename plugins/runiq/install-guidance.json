{
    "plugin": "runiq",
    "binary": "runiq",
    "check": "which runiq",
    "install_steps": ["cargo install runiq", "which runiq", "Verify: runiq --version", "supercli plugins install ./plugins/runiq --on-conflict replace --json"]
  }
