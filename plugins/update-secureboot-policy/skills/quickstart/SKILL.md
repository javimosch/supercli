---
name: update-secureboot-policy
description: Use this skill when the user wants to use update-secureboot-policy, cli tool: update-secureboot-policy.
---

# update-secureboot-policy Plugin

CLI tool: update-secureboot-policy.

## Commands
- `update-secureboot-policy <resource> <action>` — Execute update-secureboot-policy commands
- `update-secureboot-policy self version` — Print update-secureboot-policy version
- `update-secureboot-policy _ _` — Passthrough to update-secureboot-policy CLI

## Usage Examples
- "update-secureboot-policy --help"
- "update-secureboot-policy self version"

## Installation
```bash
apt-get install update-secureboot-policy 2>/dev/null || which update-secureboot-policy
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
