{
  "plugin": "svn",
  "binary": "svn",
  "check": "which svn",
  "install_steps": [
    "brew install subversion",
    "Verify: svn --version",
    "supercli plugins install ./plugins/svn --on-conflict replace --json"
  ]
}
