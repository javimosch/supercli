{
  "plugin": "wishlist",
  "binary": "wishlist",
  "check": "which wishlist",
  "install_steps": ["brew install wishlist", "Verify: wishlist --version", "supercli plugins install ./plugins/wishlist --on-conflict replace --json"]
}
