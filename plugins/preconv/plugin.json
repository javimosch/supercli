{
  "name": "preconv",
  "version": "1.0.0",
  "description": "preconv \u2014 preconv \u2014 CLI utility",
  "source": "/usr/bin/preconv",
  "checks": [
    {
      "type": "binary",
      "name": "preconv"
    }
  ],
  "install_guidance": {
    "plugin": "preconv",
    "binary": "preconv",
    "check": "which preconv",
    "install_steps": [
      "# Install preconv:",
      "# apt: sudo apt-get install preconv",
      "# brew: brew install preconv",
      "# cargo: cargo install preconv",
      "# npm: npm install -g preconv",
      "# pip: pip install preconv",
      "# or download from /usr/bin/preconv"
    ]
  },
  "commands": [
    {
      "namespace": "preconv",
      "resource": "preconv",
      "action": "run",
      "description": "Run preconv",
      "adapter": "process",
      "adapterConfig": {
        "command": "preconv",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install preconv from /usr/bin/preconv"
      },
      "args": []
    }
  ]
}