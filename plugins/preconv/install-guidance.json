{
  "plugin": "preconv",
  "binary": "preconv",
  "check": "which preconv",
  "install_steps": [
    "# Install preconv",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/preconv-linux-amd64 -o /usr/local/bin/preconv",
    "chmod +x /usr/local/bin/preconv",
    "# Or via package manager, see /usr/bin/preconv"
  ]
}