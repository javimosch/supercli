{
  "plugin": "hint",
  "binary": "hint",
  "check": "which hint",
  "install_steps": [
    "npm install -g hint",
    "Verify: hint --version",
    "supercli plugins install ./plugins/hint --on-conflict replace --json"
  ]
}
