{
  "plugin": "uuidd",
  "binary": "uuidd",
  "check": "which uuidd",
  "install_steps": [
    "apt-get install uuidd 2>/dev/null || which uuidd",
    "Verify: uuidd --version",
    "supercli plugins install ./plugins/uuidd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}