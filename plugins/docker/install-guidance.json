{
  "plugin": "docker",
  "binary": "docker",
  "check": "which docker",
  "install_steps": [
    "brew install docker",
    "Verify: docker --version",
    "supercli plugins install ./plugins/docker --on-conflict replace --json"
  ]
}