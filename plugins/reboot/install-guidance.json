{
  "plugin": "reboot",
  "binary": "reboot",
  "check": "which reboot",
  "install_steps": [
    "# Install reboot",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/reboot-linux-amd64 -o /usr/local/bin/reboot",
    "chmod +x /usr/local/bin/reboot",
    "# Or via package manager, see /usr/sbin/reboot"
  ]
}