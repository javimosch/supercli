{
  "plugin": "linux32",
  "binary": "linux32",
  "check": "which linux32",
  "install_steps": [
    "apt-get install linux32 2>/dev/null || which linux32",
    "Verify: linux32 --version",
    "supercli plugins install ./plugins/linux32 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}