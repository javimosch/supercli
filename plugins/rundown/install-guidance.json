{
  "plugin": "rundown",
  "binary": "rundown",
  "check": "which rundown",
  "install_steps": ["npm install -g rundown", "Verify: rundown --version", "supercli plugins install ./plugins/rundown --on-conflict replace --json"]
}
