{
  "plugin": "gnome-weather",
  "binary": "gnome-weather",
  "check": "which gnome-weather",
  "install_steps": [
    "apt-get install gnome-weather 2>/dev/null || which gnome-weather",
    "Verify: gnome-weather --version",
    "supercli plugins install ./plugins/gnome-weather --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}