---
name: umount.udisks2
description: Use this skill when the user wants to use umount.udisks2, cli tool: umount.udisks2.
---

# umount.udisks2 Plugin

CLI tool: umount.udisks2.

## Commands
- `umount.udisks2 <resource> <action>` — Execute umount.udisks2 commands
- `umount.udisks2 self version` — Print umount.udisks2 version
- `umount.udisks2 _ _` — Passthrough to umount.udisks2 CLI

## Usage Examples
- "umount.udisks2 --help"
- "umount.udisks2 self version"

## Installation
```bash
apt-get install umount.udisks2 2>/dev/null || which umount.udisks2
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
