{ "plugin": "bluetoothd", "binary": "bluetoothd", "check": "which bluetoothd", "install_steps": ["apt-get install bluetoothd 2>/dev/null || which bluetoothd", "Verify: bluetoothd --version", "supercli plugins install ./plugins/bluetoothd --on-conflict replace --json"] }
