{
  "plugin": "lodraw",
  "binary": "lodraw",
  "check": "which lodraw",
  "install_steps": [
    "apt-get install lodraw 2>/dev/null || which lodraw",
    "Verify: lodraw --version",
    "supercli plugins install ./plugins/lodraw --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}