{
  "plugin": "portfwd",
  "binary": "portfwd",
  "check": "which portfwd",
  "install_steps": [
    "go install github.com/portfwd/portfwd@latest",
    "Verify: portfwd --version",
    "supercli plugins install ./plugins/portfwd --on-conflict replace --json"
  ]
}
