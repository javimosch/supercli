{
  "name": "dcclient",
  "version": "0.1.0",
  "description": "CLI tool: dcclient",
  "source": "https://packages.debian.org/dcclient",
  "checks": [{ "type": "binary", "name": "dcclient" }],
  "install_guidance": {
    "plugin": "dcclient", "binary": "dcclient", "check": "which dcclient",
    "install_steps": ["apt-get install dcclient 2>/dev/null || which dcclient", "Verify: dcclient --version", "supercli plugins install ./plugins/dcclient --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dcclient", "resource": "_", "action": "_",
    "description": "Passthrough to dcclient CLI",
    "adapter": "process", "adapterConfig": { "command": "dcclient", "passthrough": true, "missingDependencyHelp": "Install dcclient: apt-get install dcclient" }, "args": []
  }]
}
