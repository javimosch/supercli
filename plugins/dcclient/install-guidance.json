{ "plugin": "dcclient", "binary": "dcclient", "check": "which dcclient", "install_steps": ["apt-get install dcclient 2>/dev/null || which dcclient", "Verify: dcclient --version", "supercli plugins install ./plugins/dcclient --on-conflict replace --json"] }
