{
  "plugin": "splash-cli",
  "binary": "splash-cli",
  "check": "which splash-cli",
  "install_steps": [
    "See https://github.com/splash-cli/splash-cli",
    "Verify: splash-cli --version",
    "supercli plugins install ./plugins/splash-cli --on-conflict replace --json"
  ],
  "note": "See project repository"
}