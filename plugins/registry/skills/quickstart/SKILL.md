---
name: registry
description: Use this skill when the user wants to use registry for A community driven registry service for Model Context Protocol (MCP) servers..
---

# registry Plugin

A community driven registry service for Model Context Protocol (MCP) servers.

## Commands

### General
- `registry self version` — Print registry version
- `registry _ _` — Passthrough to registry CLI

## Usage Examples
- "Run registry"
- "Use registry to A community driven registry service for Model Context Protoc"

## Installation

```bash
pip install registry
```

## Key Features
- A community driven registry service for Model Context Protocol (MCP) servers.
- CLI tool, headless and scriptable
