{
  "plugin": "pig",
  "binary": "pig",
  "check": "which pig",
  "install_steps": [
    "brew install pgsty/pig/pig",
    "Verify: pig --version",
    "supercli plugins install ./plugins/pig --on-conflict replace --json"
  ]
}