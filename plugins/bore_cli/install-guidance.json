{
    "plugin": "bore-cli",
    "binary": "bore-cli",
    "check": "which bore-cli",
    "install_steps": ["cargo install bore-cli", "which bore-cli", "Verify: bore-cli --version", "supercli plugins install ./plugins/bore_cli --on-conflict replace --json"]
  }
