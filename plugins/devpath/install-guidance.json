{ "plugin": "devpath", "binary": "devpath", "check": "which devpath", "install_steps": ["apt-get install devpath 2>/dev/null || which devpath", "Verify: devpath --version", "supercli plugins install ./plugins/devpath --on-conflict replace --json"] }
