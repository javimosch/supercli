{
  "plugin": "helm",
  "binary": "helm",
  "check": "which helm",
  "install_steps": [
    "brew install helm",
    "Verify: helm --version",
    "supercli plugins install ./plugins/helm --on-conflict replace --json"
  ]
}