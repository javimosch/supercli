{
  "plugin": "clconf",
  "binary": "clconf",
  "check": "which clconf",
  "install_steps": ["go install github.com/clconf@latest", "Verify: clconf --version", "supercli plugins install ./plugins/clconf --on-conflict replace --json"]
}
