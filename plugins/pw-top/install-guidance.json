{
  "plugin": "pw-top",
  "binary": "pw-top",
  "check": "which pw-top",
  "install_steps": [
    "apt-get install pw-top 2>/dev/null || which pw-top",
    "Verify: pw-top --version",
    "supercli plugins install ./plugins/pw-top --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}