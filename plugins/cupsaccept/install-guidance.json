{ "plugin": "cupsaccept", "binary": "cupsaccept", "check": "which cupsaccept", "install_steps": ["apt-get install cups", "Verify: cupsaccept --version", "supercli plugins install ./plugins/cupsaccept --on-conflict replace --json"] }
