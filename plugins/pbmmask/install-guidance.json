{
  "plugin": "pbmmask",
  "binary": "pbmmask",
  "check": "which pbmmask",
  "install_steps": [
    "apt-get install pbmmask 2>/dev/null || which pbmmask",
    "Verify: pbmmask --version",
    "supercli plugins install ./plugins/pbmmask --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}