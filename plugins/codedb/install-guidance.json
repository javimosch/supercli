{
  "plugin": "codedb",
  "binary": "codedb",
  "check": "which codedb",
  "install_steps": [
    "brew install codedb",
    "Verify: codedb --version",
    "supercli plugins install ./plugins/codedb --on-conflict replace --json"
  ]
}