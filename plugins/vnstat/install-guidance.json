{
    "plugin": "vnstat",
    "binary": "vnstat",
    "check": "which vnstat",
    "install_steps": ["apt-get install vnstat", "which vnstat", "Verify: vnstat --version", "supercli plugins install ./plugins/vnstat --on-conflict replace --json"]
  }
