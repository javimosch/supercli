{
  "plugin": "lzsa",
  "binary": "lzsa",
  "check": "which lzsa",
  "install_steps": [
    "brew install lzsa",
    "Verify: lzsa --version",
    "supercli plugins install ./plugins/lzsa --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}