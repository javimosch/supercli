{
  "plugin": "frozen",
  "binary": "frozen",
  "check": "which frozen",
  "install_steps": [
    "brew install frozen",
    "Verify: frozen --version",
    "supercli plugins install ./plugins/frozen --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}