{
  "plugin": "grub-mkrescue",
  "binary": "grub-mkrescue",
  "check": "which grub-mkrescue",
  "install_steps": [
    "apt-get install grub-mkrescue 2>/dev/null || which grub-mkrescue",
    "Verify: grub-mkrescue --version",
    "supercli plugins install ./plugins/grub-mkrescue --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}