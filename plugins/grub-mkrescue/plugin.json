{
  "name": "grub-mkrescue",
  "version": "0.1.0",
  "description": "CLI tool: grub-mkrescue",
  "source": "https://manpages.debian.org/grub-mkrescue",
  "checks": [
    {
      "type": "binary",
      "name": "grub-mkrescue"
    }
  ],
  "install_guidance": {
    "plugin": "grub-mkrescue",
    "binary": "grub-mkrescue",
    "check": "which grub-mkrescue",
    "install_steps": [
      "apt-get install grub-mkrescue 2>/dev/null || which grub-mkrescue",
      "Verify: grub-mkrescue --version",
      "supercli plugins install ./plugins/grub-mkrescue --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-mkrescue",
      "resource": "self",
      "action": "version",
      "description": "Print grub-mkrescue version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkrescue",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-mkrescue: apt-get install grub-mkrescue 2>/dev/null || which grub-mkrescue"
      },
      "args": []
    },
    {
      "namespace": "grub-mkrescue",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-mkrescue CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkrescue",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-mkrescue: apt-get install grub-mkrescue 2>/dev/null || which grub-mkrescue"
      },
      "args": []
    }
  ]
}