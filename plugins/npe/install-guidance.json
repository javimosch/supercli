{
  "plugin": "npe",
  "binary": "npe",
  "check": "which npe",
  "install_steps": [
    "npm install -g npe",
    "Verify: npe --version",
    "supercli plugins install ./plugins/npe --on-conflict replace --json"
  ]
}