{
  "plugin": "skill",
  "binary": "skill",
  "check": "which skill",
  "install_steps": [
    "apt-get install skill 2>/dev/null || which skill",
    "Verify: skill --version",
    "supercli plugins install ./plugins/skill --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}