{
  "plugin": "tangle",
  "binary": "tangle",
  "check": "which tangle",
  "install_steps": [
    "apt-get install tangle 2>/dev/null || which tangle",
    "Verify: tangle --version",
    "supercli plugins install ./plugins/tangle --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}