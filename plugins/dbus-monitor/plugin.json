{
  "name": "dbus-monitor",
  "version": "1.0.0",
  "description": "dbus-monitor \u2014 dbus-monitor \u2014 CLI utility",
  "checks": [
    {
      "type": "binary",
      "name": "dbus-monitor"
    }
  ],
  "install_guidance": {
    "plugin": "dbus-monitor",
    "binary": "dbus-monitor",
    "check": "which dbus-monitor",
    "install_steps": [
      "Install via package manager"
    ]
  },
  "commands": [
    {
      "namespace": "dbus-monitor",
      "resource": "run",
      "action": "exec",
      "description": "dbus-monitor \u2014 CLI utility",
      "adapter": "process",
      "adapterConfig": {
        "command": "dbus-monitor",
        "passthrough": true,
        "timeout_ms": 30000
      }
    }
  ]
}