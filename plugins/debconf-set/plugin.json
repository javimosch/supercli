{
  "name": "debconf-set",
  "version": "0.1.0",
  "description": "CLI tool: debconf-set",
  "source": "https://packages.debian.org/debconf-set",
  "checks": [{ "type": "binary", "name": "debconf-set" }],
  "install_guidance": {
    "plugin": "debconf-set", "binary": "debconf-set", "check": "which debconf-set",
    "install_steps": ["apt-get install debconf-set 2>/dev/null || which debconf-set", "Verify: debconf-set --version", "supercli plugins install ./plugins/debconf-set --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "debconf-set", "resource": "_", "action": "_",
    "description": "Passthrough to debconf-set CLI",
    "adapter": "process", "adapterConfig": { "command": "debconf-set", "passthrough": true, "missingDependencyHelp": "Install debconf-set: apt-get install debconf-set" }, "args": []
  }]
}
