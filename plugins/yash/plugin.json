{
  "name": "yash",
  "version": "0.1.0",
  "description": "POSIX-compliant shell and terminal utility",
  "source": "shell",
  "checks": [{ "type": "binary", "name": "yash" }],
  "install_guidance": {
    "plugin": "yash",
    "binary": "yash",
    "check": "which yash",
    "install_steps": ["sudo apt install yash", "Verify: yash --version", "supercli plugins install ./plugins/yash --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "yash",
    "resource": "_",
    "action": "_",
    "description": "Passthrough to yash CLI",
    "adapter": "process",
    "adapterConfig": {
      "command": "yash",
      "passthrough": true,
      "missingDependencyHelp": "Install yash: sudo apt install yash"
    },
    "args": []
  }]
}
