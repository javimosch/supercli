{
  "plugin": "x11perf",
  "binary": "x11perf",
  "check": "which x11perf",
  "install_steps": [
    "apt-get install x11perf 2>/dev/null || which x11perf",
    "Verify: x11perf --version",
    "supercli plugins install ./plugins/x11perf --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}