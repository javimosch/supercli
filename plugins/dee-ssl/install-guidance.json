{
  "plugin": "dee-ssl",
  "binary": "dee-ssl",
  "check": "which dee-ssl",
  "install_steps": ["cargo install dee-ssl", "Verify: dee-ssl --version", "supercli plugins install ./plugins/dee-ssl --on-conflict replace --json"]
}
