{
  "plugin": "hadolint",
  "binary": "hadolint",
  "check": "which hadolint",
  "install_steps": [
    "brew install hadolint",
    "Verify: hadolint --version",
    "supercli plugins install ./plugins/hadolint --on-conflict replace --json"
  ],
  "note": "Also available via: apt, pacman, dnf, docker (hadolint/hadolint). Pre-built binaries on GitHub releases."
}
