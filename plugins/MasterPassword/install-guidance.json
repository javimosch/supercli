{
  "plugin": "MasterPassword",
  "binary": "mpw",
  "check": "which mpw",
  "install_steps": [
    "brew install masterpassword",
    "Verify: mpw --version",
    "supercli plugins install ./plugins/MasterPassword --on-conflict replace --json"
  ],
  "note": "Install via brew: brew install masterpassword"
}
