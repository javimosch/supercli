{
  "name": "dbmanage",
  "version": "0.1.0",
  "description": "CLI tool: dbmanage",
  "source": "https://packages.debian.org/dbmanage",
  "checks": [{ "type": "binary", "name": "dbmanage" }],
  "install_guidance": {
    "plugin": "dbmanage", "binary": "dbmanage", "check": "which dbmanage",
    "install_steps": ["apt-get install dbmanage 2>/dev/null || which dbmanage", "Verify: dbmanage --version", "supercli plugins install ./plugins/dbmanage --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbmanage", "resource": "_", "action": "_",
    "description": "Passthrough to dbmanage CLI",
    "adapter": "process", "adapterConfig": { "command": "dbmanage", "passthrough": true, "missingDependencyHelp": "Install dbmanage: apt-get install dbmanage" }, "args": []
  }]
}
