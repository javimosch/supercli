{ "plugin": "dbmanage", "binary": "dbmanage", "check": "which dbmanage", "install_steps": ["apt-get install dbmanage 2>/dev/null || which dbmanage", "Verify: dbmanage --version", "supercli plugins install ./plugins/dbmanage --on-conflict replace --json"] }
