{ "plugin": "dconf", "binary": "dconf", "check": "which dconf", "install_steps": ["apt-get install dconf 2>/dev/null || which dconf", "Verify: dconf --version", "supercli plugins install ./plugins/dconf --on-conflict replace --json"] }
