{ "plugin": "cgset", "binary": "cgset", "check": "which cgset", "install_steps": ["apt-get install cgset 2>/dev/null || which cgset", "Verify: cgset --version", "supercli plugins install ./plugins/cgset --on-conflict replace --json"] }
