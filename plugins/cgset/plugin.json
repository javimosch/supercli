{
  "name": "cgset",
  "version": "0.1.0",
  "description": "CLI tool: cgset",
  "source": "https://packages.debian.org/cgset",
  "checks": [{ "type": "binary", "name": "cgset" }],
  "install_guidance": {
    "plugin": "cgset", "binary": "cgset", "check": "which cgset",
    "install_steps": ["apt-get install cgset 2>/dev/null || which cgset", "Verify: cgset --version", "supercli plugins install ./plugins/cgset --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cgset", "resource": "_", "action": "_",
    "description": "Passthrough to cgset CLI",
    "adapter": "process", "adapterConfig": { "command": "cgset", "passthrough": true, "missingDependencyHelp": "Install cgset: apt-get install cgset" }, "args": []
  }]
}
