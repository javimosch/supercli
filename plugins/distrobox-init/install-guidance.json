{ "plugin": "distrobox-init", "binary": "distrobox-init", "check": "which distrobox-init", "install_steps": ["apt-get install distrobox-init 2>/dev/null || which distrobox-init", "Verify: distrobox-init --version", "supercli plugins install ./plugins/distrobox-init --on-conflict replace --json"] }
