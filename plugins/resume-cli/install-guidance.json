{
  "plugin": "resume-cli",
  "binary": "resume",
  "check": "resume --version",
  "install_steps": [
    "npm install -g resume-cli"
  ],
  "note": "Uses JSON Resume schema. Export requires a theme - install with npm install -g jsonresume-theme-<name>"
}
