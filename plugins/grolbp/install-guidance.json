{
  "plugin": "grolbp",
  "binary": "grolbp",
  "check": "which grolbp",
  "install_steps": [
    "apt-get install grolbp 2>/dev/null || which grolbp",
    "Verify: grolbp --version",
    "supercli plugins install ./plugins/grolbp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}