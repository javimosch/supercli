{
  "plugin": "wasmer",
  "binary": "wasmer",
  "check": "which wasmer",
  "install_steps": [
    "curl -fsSL https://get.wasmer.io | sh",
    "Verify: wasmer --version",
    "supercli plugins install ./plugins/wasmer --on-conflict replace --json"
  ],
  "note": "Wasmer is a universal WebAssembly runtime. Supports WASI and Emscripten out of the box. Runs over 1,000+ WASM packages from the Wasmer registry."
}
