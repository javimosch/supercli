{
  "plugin": "gnome-session-quit",
  "binary": "gnome-session-quit",
  "check": "which gnome-session-quit",
  "install_steps": [
    "apt-get install gnome-session-quit 2>/dev/null || which gnome-session-quit",
    "Verify: gnome-session-quit --version",
    "supercli plugins install ./plugins/gnome-session-quit --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}