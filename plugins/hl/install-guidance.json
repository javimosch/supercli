{
  "plugin": "hl",
  "binary": "hl",
  "check": "which hl",
  "install_steps": [
    "brew install pamburus/tap/hl",
    "Verify: hl --version",
    "supercli plugins install ./plugins/hl --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install hl. Fast log viewer with syntax highlighting for JSON and other formats. Supports filtering, live tail, and custom themes. Great for log analysis in terminal and CI/CD."
}
