{
  "plugin": "httpx",
  "binary": "httpx",
  "check": "which httpx",
  "install_steps": [
    "brew install httpx",
    "Verify: httpx -version",
    "supercli plugins install ./plugins/httpx --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/projectdiscovery/httpx/cmd/httpx@latest. Supports multiple output formats. Can detect technologies, SSL certificates, and server headers. Configurable threads and timeouts."
}
