{
  "name": "blinker",
  "version": "0.1.0",
  "description": "CLI tool: blinker",
  "source": "https://packages.debian.org/blinker",
  "checks": [{ "type": "binary", "name": "blinker" }],
  "install_guidance": {
    "plugin": "blinker", "binary": "blinker", "check": "which blinker",
    "install_steps": ["apt-get install blinker 2>/dev/null || which blinker", "Verify: blinker --version", "supercli plugins install ./plugins/blinker --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "blinker", "resource": "_", "action": "_",
    "description": "Passthrough to blinker CLI",
    "adapter": "process", "adapterConfig": { "command": "blinker", "passthrough": true, "missingDependencyHelp": "Install blinker: apt-get install blinker" }, "args": []
  }]
}
