{ "plugin": "blinker", "binary": "blinker", "check": "which blinker", "install_steps": ["apt-get install blinker 2>/dev/null || which blinker", "Verify: blinker --version", "supercli plugins install ./plugins/blinker --on-conflict replace --json"] }
