{
  "plugin": "strace",
  "binary": "strace",
  "check": "which strace",
  "install_steps": [
    "brew install strace",
    "Verify: strace --version",
    "supercli plugins install ./plugins/strace --on-conflict replace --json"
  ]
}
