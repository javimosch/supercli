{
  "plugin": "dep-tree",
  "binary": "dep-tree",
  "check": "which dep-tree",
  "install_steps": [
    "brew install dep-tree",
    "Verify: dep-tree --version",
    "supercli plugins install ./plugins/dep-tree --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/gabotechs/dep-tree@latest. Renders dependency trees in ASCII, tracks cycles, and analyzes exports. Supports JavaScript, TypeScript, Python, and Go. Great for understanding codebase architecture."
}
