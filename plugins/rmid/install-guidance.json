{
  "plugin": "rmid",
  "binary": "rmid",
  "check": "which rmid",
  "install_steps": [
    "apt-get install rmid 2>/dev/null || which rmid",
    "Verify: rmid --version",
    "supercli plugins install ./plugins/rmid --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}