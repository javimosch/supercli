---
name: MeshCore
description: Use this skill when the user wants to use MeshCore for A new lightweight, hybrid routing mesh protocol for packet radios.
---

# MeshCore Plugin

A new lightweight, hybrid routing mesh protocol for packet radios

## Commands

### General
- `MeshCore self version` — Print MeshCore version
- `MeshCore _ _` — Passthrough to meshcore CLI

## Usage Examples
- "Run MeshCore"
- "Use meshcore to A new lightweight, hybrid routing mesh protocol for packet r"

## Installation

```bash
pip install meshcore
```

## Key Features
- A new lightweight, hybrid routing mesh protocol for packet radios
- CLI tool, headless and scriptable
