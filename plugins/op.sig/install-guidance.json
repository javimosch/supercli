{
  "plugin": "op.sig",
  "binary": "op.sig",
  "check": "which op.sig",
  "install_steps": [
    "apt-get install op.sig 2>/dev/null || which op.sig",
    "Verify: op.sig --version",
    "supercli plugins install ./plugins/op.sig --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}