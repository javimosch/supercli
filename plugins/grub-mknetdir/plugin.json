{
  "name": "grub-mknetdir",
  "version": "0.1.0",
  "description": "CLI tool: grub-mknetdir",
  "source": "https://manpages.debian.org/grub-mknetdir",
  "checks": [
    {
      "type": "binary",
      "name": "grub-mknetdir"
    }
  ],
  "install_guidance": {
    "plugin": "grub-mknetdir",
    "binary": "grub-mknetdir",
    "check": "which grub-mknetdir",
    "install_steps": [
      "apt-get install grub-mknetdir 2>/dev/null || which grub-mknetdir",
      "Verify: grub-mknetdir --version",
      "supercli plugins install ./plugins/grub-mknetdir --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-mknetdir",
      "resource": "self",
      "action": "version",
      "description": "Print grub-mknetdir version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mknetdir",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-mknetdir: apt-get install grub-mknetdir 2>/dev/null || which grub-mknetdir"
      },
      "args": []
    },
    {
      "namespace": "grub-mknetdir",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-mknetdir CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mknetdir",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-mknetdir: apt-get install grub-mknetdir 2>/dev/null || which grub-mknetdir"
      },
      "args": []
    }
  ]
}