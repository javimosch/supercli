{
  "plugin": "helib",
  "binary": "helib",
  "check": "which helib",
  "install_steps": [
    "brew install helib",
    "Verify: helib --version",
    "supercli plugins install ./plugins/helib --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}