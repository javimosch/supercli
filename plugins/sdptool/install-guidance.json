{
  "plugin": "sdptool",
  "binary": "sdptool",
  "check": "which sdptool",
  "install_steps": [
    "apt-get install sdptool 2>/dev/null || which sdptool",
    "Verify: sdptool --version",
    "supercli plugins install ./plugins/sdptool --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}