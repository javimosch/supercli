{ "plugin": "ddconv", "binary": "ddconv", "check": "which ddconv", "install_steps": ["apt-get install ddconv 2>/dev/null || which ddconv", "Verify: ddconv --version", "supercli plugins install ./plugins/ddconv --on-conflict replace --json"] }
