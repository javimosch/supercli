{
  "name": "ddconv",
  "version": "0.1.0",
  "description": "CLI tool: ddconv",
  "source": "https://packages.debian.org/ddconv",
  "checks": [{ "type": "binary", "name": "ddconv" }],
  "install_guidance": {
    "plugin": "ddconv", "binary": "ddconv", "check": "which ddconv",
    "install_steps": ["apt-get install ddconv 2>/dev/null || which ddconv", "Verify: ddconv --version", "supercli plugins install ./plugins/ddconv --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ddconv", "resource": "_", "action": "_",
    "description": "Passthrough to ddconv CLI",
    "adapter": "process", "adapterConfig": { "command": "ddconv", "passthrough": true, "missingDependencyHelp": "Install ddconv: apt-get install ddconv" }, "args": []
  }]
}
