{
  "plugin": "getafm",
  "binary": "getafm",
  "check": "which getafm",
  "install_steps": [
    "apt-get install getafm 2>/dev/null || which getafm",
    "Verify: getafm --version",
    "supercli plugins install ./plugins/getafm --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}