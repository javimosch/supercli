{
  "plugin": "runit",
  "binary": "runit",
  "check": "which runit",
  "install_steps": [
    "# Install runit",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/runit-linux-amd64 -o /usr/local/bin/runit",
    "chmod +x /usr/local/bin/runit",
    "# Or via package manager, see smarden.org/runit"
  ]
}