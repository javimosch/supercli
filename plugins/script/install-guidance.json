{
  "plugin": "script",
  "binary": "script",
  "check": "which script",
  "install_steps": [
    "Linux: apt install util-linux (Debian/Ubuntu), dnf install util-linux (Fedora)",
    "macOS: built-in (BSD version at /usr/bin/script)",
    "Verify: script --version"
  ]
}
