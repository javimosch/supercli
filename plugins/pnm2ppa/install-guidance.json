{
  "plugin": "pnm2ppa",
  "binary": "pnm2ppa",
  "check": "which pnm2ppa",
  "install_steps": [
    "apt-get install pnm2ppa 2>/dev/null || which pnm2ppa",
    "Verify: pnm2ppa --version",
    "supercli plugins install ./plugins/pnm2ppa --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}