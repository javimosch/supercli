{
  "plugin": "grim",
  "binary": "grim",
  "check": "which grim",
  "install_steps": [
    "brew install grim",
    "Verify: grim --version",
    "supercli plugins install ./plugins/grim --on-conflict replace --json"
  ]
}
