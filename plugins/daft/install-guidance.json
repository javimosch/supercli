{
  "plugin": "daft",
  "binary": "daft",
  "check": "which daft",
  "install_steps": [
    "pip install daft",
    "Verify: daft --version",
    "supercli plugins install ./plugins/daft --on-conflict replace --json"
  ],
  "note": "Install via pip: pip install daft"
}
