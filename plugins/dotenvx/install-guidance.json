{
  "plugin": "dotenvx",
  "binary": "dotenvx",
  "check": "which dotenvx",
  "install_steps": [
    "npm install -g dotenvx",
    "Verify: dotenvx --version",
    "supercli plugins install ./plugins/dotenvx --on-conflict replace --json"
  ],
  "note": "Dotenvx supports variable interpolation, default values, alternate values, command substitution, and encryption. Use --debug flag to see which env vars are being injected."
}
