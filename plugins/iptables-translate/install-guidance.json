{
  "plugin": "iptables-translate",
  "binary": "iptables-translate",
  "check": "which iptables-translate",
  "install_steps": [
    "apt-get install iptables-translate 2>/dev/null || which iptables-translate",
    "Verify: iptables-translate --version",
    "supercli plugins install ./plugins/iptables-translate --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}