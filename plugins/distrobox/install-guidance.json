{ "plugin": "distrobox", "binary": "distrobox", "check": "which distrobox", "install_steps": ["apt-get install distrobox 2>/dev/null || which distrobox", "Verify: distrobox --version", "supercli plugins install ./plugins/distrobox --on-conflict replace --json"] }
