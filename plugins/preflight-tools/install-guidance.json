{
  "plugin": "preflight-tools",
  "binary": "preflight",
  "check": "which preflight",
  "install_steps": ["brew install preflight", "Verify: preflight --version", "supercli plugins install ./plugins/preflight-tools --on-conflict replace --json"]
}
