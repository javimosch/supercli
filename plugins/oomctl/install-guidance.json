{
  "plugin": "oomctl",
  "binary": "oomctl",
  "check": "which oomctl",
  "install_steps": [
    "apt-get install oomctl 2>/dev/null || which oomctl",
    "Verify: oomctl --version",
    "supercli plugins install ./plugins/oomctl --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}