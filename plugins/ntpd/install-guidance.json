{
  "plugin": "ntpd",
  "binary": "ntpd",
  "check": "which ntpd",
  "install_steps": [
    "apt-get install ntpd 2>/dev/null || which ntpd",
    "Verify: ntpd --version",
    "supercli plugins install ./plugins/ntpd --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}