{
  "plugin": "codefetch",
  "binary": "codefetch",
  "check": "which codefetch",
  "install_steps": [
    "npm install -g codefetch",
    "Verify: codefetch --version",
    "supercli plugins install ./plugins/codefetch --on-conflict replace --json"
  ],
  "note": "Also available via npx: npx codefetch. Works with local codebases, GitHub repos (via API or git clone), and websites. Designed for AI/LLM context windows."
}
