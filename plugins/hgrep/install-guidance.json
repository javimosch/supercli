{
  "plugin": "hgrep",
  "binary": "hgrep",
  "check": "which hgrep",
  "install_steps": [
    "brew install hgrep",
    "Verify: hgrep --version",
    "supercli plugins install ./plugins/hgrep --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}