{
  "plugin": "nali",
  "binary": "nali",
  "check": "which nali",
  "install_steps": [
    "brew install nali",
    "Verify: nali --version",
    "supercli plugins install ./plugins/nali --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/zu1k/nali/cmd/nali@latest. Offline IP geolocation database. Supports IPv4 and IPv6. Can query CDN service providers. Works with dig and nslookup commands."
}
