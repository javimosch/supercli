{
  "plugin": "tokei",
  "binary": "tokei",
  "check": "which tokei",
  "install_steps": [
    "cargo install tokei",
    "Verify: tokei --version",
    "supercli plugins install ./plugins/tokei --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, pacman, scoop, winget, nix. Supports 150+ languages."
}