{
  "plugin": "vile",
  "binary": "vile",
  "check": "which vile",
  "install_steps": ["brew install vile", "Verify: vile --version", "supercli plugins install ./plugins/vile --on-conflict replace --json"]
}
