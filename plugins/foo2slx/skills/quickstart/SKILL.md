---
name: foo2slx
description: Use this skill when the user wants to use foo2slx, cli tool: foo2slx.
---

# foo2slx Plugin

CLI tool: foo2slx.

## Commands
- `foo2slx <resource> <action>` — Execute foo2slx commands
- `foo2slx self version` — Print foo2slx version
- `foo2slx _ _` — Passthrough to foo2slx CLI

## Usage Examples
- "foo2slx --help"
- "foo2slx self version"

## Installation
```bash
apt-get install foo2slx 2>/dev/null || which foo2slx
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
