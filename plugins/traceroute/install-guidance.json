{
  "plugin": "traceroute",
  "binary": "traceroute",
  "check": "which traceroute",
  "install_steps": [
    "brew install traceroute",
    "Verify: traceroute --version",
    "supercli plugins install ./plugins/traceroute --on-conflict replace --json"
  ]
}
