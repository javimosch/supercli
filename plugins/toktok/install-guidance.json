{
  "plugin": "toktok",
  "binary": "toktok",
  "check": "which toktok",
  "install_steps": ["cargo install toktok", "Verify: toktok --version", "supercli plugins install ./plugins/toktok --on-conflict replace --json"]
}
