{
  "plugin": "slugify",
  "binary": "slugify",
  "check": "which slugify",
  "install_steps": [
    "apt-get install slugify 2>/dev/null || which slugify",
    "Verify: slugify --version",
    "supercli plugins install ./plugins/slugify --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}