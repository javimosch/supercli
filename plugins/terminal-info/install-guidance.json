{
  "plugin": "terminal-info",
  "binary": "tinfo",
  "check": "which tinfo",
  "install_steps": [
    "cargo install terminal-info",
    "Verify: tinfo --version",
    "supercli plugins install ./plugins/terminal-info --on-conflict replace --json"
  ],
  "note": "Binary name: tinfo"
}
