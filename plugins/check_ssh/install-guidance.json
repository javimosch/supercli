{ "plugin": "check_ssh", "binary": "check_ssh", "check": "which check_ssh", "install_steps": ["apt-get install check_ssh 2>/dev/null || which check_ssh", "Verify: check_ssh --version", "supercli plugins install ./plugins/check_ssh --on-conflict replace --json"] }
