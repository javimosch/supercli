{
  "name": "check_ssh",
  "version": "0.1.0",
  "description": "CLI tool: check_ssh",
  "source": "https://packages.debian.org/check_ssh",
  "checks": [{ "type": "binary", "name": "check_ssh" }],
  "install_guidance": {
    "plugin": "check_ssh", "binary": "check_ssh", "check": "which check_ssh",
    "install_steps": ["apt-get install check_ssh 2>/dev/null || which check_ssh", "Verify: check_ssh --version", "supercli plugins install ./plugins/check_ssh --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "check_ssh", "resource": "_", "action": "_",
    "description": "Passthrough to check_ssh CLI",
    "adapter": "process", "adapterConfig": { "command": "check_ssh", "passthrough": true, "missingDependencyHelp": "Install check_ssh: apt-get install check_ssh" }, "args": []
  }]
}
