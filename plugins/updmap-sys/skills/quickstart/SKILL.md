---
name: updmap-sys
description: Use this skill when the user wants to use updmap-sys, cli tool: updmap-sys.
---

# updmap-sys Plugin

CLI tool: updmap-sys.

## Commands
- `updmap-sys <resource> <action>` — Execute updmap-sys commands
- `updmap-sys self version` — Print updmap-sys version
- `updmap-sys _ _` — Passthrough to updmap-sys CLI

## Usage Examples
- "updmap-sys --help"
- "updmap-sys self version"

## Installation
```bash
apt-get install updmap-sys 2>/dev/null || which updmap-sys
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
