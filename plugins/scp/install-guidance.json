{
  "plugin": "scp",
  "binary": "scp",
  "check": "which scp",
  "install_steps": [
    "apt-get install openssh-client",
    "Verify: scp -h",
    "supercli plugins install ./plugins/scp --on-conflict replace --json"
  ]
}
