{
  "name": "accept",
  "version": "0.1.0",
  "description": "Accept print jobs to destinations",
  "source": "https://packages.debian.org/accept",
  "checks": [{ "type": "binary", "name": "accept" }],
  "install_guidance": {
    "plugin": "accept", "binary": "accept", "check": "which accept",
    "install_steps": ["apt-get install cups", "Verify: accept --version", "supercli plugins install ./plugins/accept --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "accept", "resource": "_", "action": "_",
    "description": "Passthrough to accept CLI",
    "adapter": "process", "adapterConfig": { "command": "accept", "passthrough": true, "missingDependencyHelp": "Install accept: apt-get install cups" }, "args": []
  }]
}
