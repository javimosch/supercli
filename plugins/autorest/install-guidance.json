{
  "plugin": "autorest",
  "binary": "autorest",
  "check": "which autorest",
  "install_steps": [
    "brew install autorest",
    "Verify: autorest --version",
    "supercli plugins install ./plugins/autorest --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}