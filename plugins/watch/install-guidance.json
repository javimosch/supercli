{
  "plugin": "watch",
  "binary": "watch",
  "check": "which watch",
  "install_steps": [
    "brew install watch",
    "Verify: watch --version",
    "supercli plugins install ./plugins/watch --on-conflict replace --json"
  ]
}
