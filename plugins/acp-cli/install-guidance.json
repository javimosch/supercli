{
  "plugin": "acp-cli",
  "binary": "acp-cli.js",
  "check": "which node",
  "install_steps": [
    "Install Node.js from nodejs.org",
    "acp-cli.js is bundled with the plugin",
    "Verify: node plugins/acp-cli/acp-cli.js help",
    "supercli plugins install ./plugins/acp-cli --on-conflict replace --json"
  ],
  "note": "ACP agents require API keys as env vars (e.g., ANTHROPIC_API_KEY, GEMINI_API_KEY, GITHUB_TOKEN). The acp-cli uses JSON-RPC over stdio — no network daemon needed."
}
