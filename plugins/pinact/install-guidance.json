{
  "plugin": "pinact",
  "binary": "pinact",
  "check": "which pinact",
  "install_steps": [
    "brew install pinact",
    "Verify: pinact --version",
    "supercli plugins install ./plugins/pinact --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/suzuki-shunsuke/pinact/cmd/pinact@latest. Pins versions of GitHub Actions and Reusable Workflows to ensure reproducibility. Updates and verifies version annotations. Great for CI/CD security."
}
