---
name: yaml-validate
description: Basic YAML validation from stdin. Checks for key:value structure.
---
# yaml-validate Plugin
Basic YAML validation from stdin. Checks for key:value structure.
## Usage
- `yaml validate run < config.yaml`
