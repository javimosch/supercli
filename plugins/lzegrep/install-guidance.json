{
  "plugin": "lzegrep",
  "binary": "lzegrep",
  "check": "which lzegrep",
  "install_steps": [
    "apt-get install lzegrep 2>/dev/null || which lzegrep",
    "Verify: lzegrep --version",
    "supercli plugins install ./plugins/lzegrep --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}