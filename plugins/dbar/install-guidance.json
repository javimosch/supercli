{ "plugin": "dbar", "binary": "dbar", "check": "which dbar", "install_steps": ["apt-get install dbar 2>/dev/null || which dbar", "Verify: dbar --version", "supercli plugins install ./plugins/dbar --on-conflict replace --json"] }
