{
  "name": "dbar",
  "version": "0.1.0",
  "description": "CLI tool: dbar",
  "source": "https://packages.debian.org/dbar",
  "checks": [{ "type": "binary", "name": "dbar" }],
  "install_guidance": {
    "plugin": "dbar", "binary": "dbar", "check": "which dbar",
    "install_steps": ["apt-get install dbar 2>/dev/null || which dbar", "Verify: dbar --version", "supercli plugins install ./plugins/dbar --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dbar", "resource": "_", "action": "_",
    "description": "Passthrough to dbar CLI",
    "adapter": "process", "adapterConfig": { "command": "dbar", "passthrough": true, "missingDependencyHelp": "Install dbar: apt-get install dbar" }, "args": []
  }]
}
