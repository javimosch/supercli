{
  "name": "ciptool",
  "version": "0.1.0",
  "description": "CLI tool: ciptool",
  "source": "https://packages.debian.org/ciptool",
  "checks": [{ "type": "binary", "name": "ciptool" }],
  "install_guidance": {
    "plugin": "ciptool", "binary": "ciptool", "check": "which ciptool",
    "install_steps": ["apt-get install ciptool 2>/dev/null || which ciptool", "Verify: ciptool --version", "supercli plugins install ./plugins/ciptool --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "ciptool", "resource": "_", "action": "_",
    "description": "Passthrough to ciptool CLI",
    "adapter": "process", "adapterConfig": { "command": "ciptool", "passthrough": true, "missingDependencyHelp": "Install ciptool: apt-get install ciptool" }, "args": []
  }]
}
