{ "plugin": "atk-registry", "binary": "atk-registry", "check": "which atk-registry", "install_steps": ["apt-get install atk-registry 2>/dev/null || which atk-registry", "Verify: atk-registry --version", "supercli plugins install ./plugins/atk-registry --on-conflict replace --json"] }
