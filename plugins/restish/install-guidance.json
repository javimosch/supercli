{
  "plugin": "restish",
  "binary": "restish",
  "check": "which restish",
  "install_steps": [
    "brew install restish",
    "Verify: restish --version",
    "supercli plugins install ./plugins/restish --on-conflict replace --json"
  ],
  "note": "Also available via go install: go install github.com/rest-sh/restish@latest. User-friendly HTTP client for REST APIs with schema discovery, JSON navigation, and resource management. Supports OpenAPI and auto-completion."
}
