{
  "plugin": "cli-emoji",
  "binary": "cli-emoji",
  "check": "which cli-emoji",
  "install_steps": ["npm install -g cli-emoji", "Verify: cli-emoji --version", "supercli plugins install ./plugins/cli-emoji --on-conflict replace --json"]
}
