{
  "plugin": "mingle",
  "binary": "mingle",
  "check": "which mingle",
  "install_steps": [
    "apt-get install mingle 2>/dev/null || which mingle",
    "Verify: mingle --version",
    "supercli plugins install ./plugins/mingle --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}