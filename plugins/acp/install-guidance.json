{
  "plugin": "acp",
  "binary": "npx",
  "check": "which npx",
  "install_steps": [
    "npx is included with Node.js — install from nodejs.org",
    "Verify: npx --version",
    "supercli plugins install ./plugins/acp --on-conflict replace --json"
  ],
  "note": "ACP is a protocol. Each server is distributed separately via npx/binary. This plugin provides the registry interface and launcher for all 35+ ACP agents. Requires Node.js for npx-based agents."
}
