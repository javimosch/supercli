{
  "name": "grub-mkrelpath",
  "version": "0.1.0",
  "description": "CLI tool: grub-mkrelpath",
  "source": "https://manpages.debian.org/grub-mkrelpath",
  "checks": [
    {
      "type": "binary",
      "name": "grub-mkrelpath"
    }
  ],
  "install_guidance": {
    "plugin": "grub-mkrelpath",
    "binary": "grub-mkrelpath",
    "check": "which grub-mkrelpath",
    "install_steps": [
      "apt-get install grub-mkrelpath 2>/dev/null || which grub-mkrelpath",
      "Verify: grub-mkrelpath --version",
      "supercli plugins install ./plugins/grub-mkrelpath --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-mkrelpath",
      "resource": "self",
      "action": "version",
      "description": "Print grub-mkrelpath version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkrelpath",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-mkrelpath: apt-get install grub-mkrelpath 2>/dev/null || which grub-mkrelpath"
      },
      "args": []
    },
    {
      "namespace": "grub-mkrelpath",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-mkrelpath CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkrelpath",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-mkrelpath: apt-get install grub-mkrelpath 2>/dev/null || which grub-mkrelpath"
      },
      "args": []
    }
  ]
}