{
  "plugin": "gitoxide",
  "binary": "gix",
  "check": "which gix",
  "install_steps": [
    "brew install gitoxide",
    "Verify: gix --version",
    "supercli plugins install ./plugins/gitoxide --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install gitoxide. Pure Rust implementation of Git. Provides gix as a fast alternative for common Git operations. Great for automation and embedding."
}
