{ "plugin": "diffimg", "binary": "diffimg", "check": "which diffimg", "install_steps": ["apt-get install diffimg 2>/dev/null || which diffimg", "Verify: diffimg --version", "supercli plugins install ./plugins/diffimg --on-conflict replace --json"] }
