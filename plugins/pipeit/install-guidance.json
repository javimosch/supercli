{
  "plugin": "pipeit",
  "binary": "pipeit",
  "check": "which pipeit",
  "install_steps": ["brew install pipeit", "Verify: pipeit --version", "supercli plugins install ./plugins/pipeit --on-conflict replace --json"]
}
