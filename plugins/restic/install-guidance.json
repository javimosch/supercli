{
  "plugin": "restic",
  "binary": "restic",
  "check": "which restic",
  "install_steps": [
    "brew install restic",
    "Verify: restic --version",
    "supercli plugins install ./plugins/restic --on-conflict replace --json"
  ]
}