{
  "plugin": "stego",
  "binary": "stego",
  "check": "which stego",
  "install_steps": [
    "cargo install stego",
    "Verify: stego --version",
    "supercli plugins install ./plugins/stego --on-conflict replace --json"
  ]
}