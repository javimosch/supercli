{ "plugin": "acpi_available", "binary": "acpi_available", "check": "which acpi_available", "install_steps": ["apt-get install acpi_available 2>/dev/null || which acpi_available", "Verify: acpi_available --version", "supercli plugins install ./plugins/acpi_available --on-conflict replace --json"] }
