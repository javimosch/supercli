{
  "name": "acpi_available",
  "version": "0.1.0",
  "description": "CLI tool: acpi_available",
  "source": "https://packages.debian.org/acpi_available",
  "checks": [{ "type": "binary", "name": "acpi_available" }],
  "install_guidance": {
    "plugin": "acpi_available", "binary": "acpi_available", "check": "which acpi_available",
    "install_steps": ["apt-get install acpi_available 2>/dev/null || which acpi_available", "Verify: acpi_available --version", "supercli plugins install ./plugins/acpi_available --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "acpi_available", "resource": "_", "action": "_",
    "description": "Passthrough to acpi_available CLI",
    "adapter": "process", "adapterConfig": { "command": "acpi_available", "passthrough": true, "missingDependencyHelp": "Install acpi_available: apt-get install acpi_available" }, "args": []
  }]
}
