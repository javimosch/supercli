{
  "plugin": "miniserve",
  "binary": "miniserve",
  "check": "which miniserve",
  "install_steps": [
    "brew install miniserve",
    "Verify: miniserve --version",
    "supercli plugins install ./plugins/miniserve --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install miniserve. Supports file upload, authentication, HTTPS/TLS, random URL paths, and directory listings. Can serve single files or entire directories."
}
