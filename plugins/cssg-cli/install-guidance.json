{
  "plugin": "cssg-cli",
  "binary": "cssg",
  "check": "which cssg",
  "install_steps": [
    "npm install -g cssg-cli",
    "Verify: cssg --version",
    "supercli plugins install ./plugins/cssg-cli --on-conflict replace --json"
  ]
}