{
  "plugin": "msgconv",
  "binary": "msgconv",
  "check": "which msgconv",
  "install_steps": [
    "apt-get install msgconv 2>/dev/null || which msgconv",
    "Verify: msgconv --version",
    "supercli plugins install ./plugins/msgconv --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}