{
  "plugin": "gnome-keyring-daemon",
  "binary": "gnome-keyring-daemon",
  "check": "which gnome-keyring-daemon",
  "install_steps": [
    "apt-get install gnome-keyring-daemon 2>/dev/null || which gnome-keyring-daemon",
    "Verify: gnome-keyring-daemon --version",
    "supercli plugins install ./plugins/gnome-keyring-daemon --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}