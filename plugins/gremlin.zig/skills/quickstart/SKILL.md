---
name: gremlin.zig
description: Use this skill when the user wants to use gremlin.zig for A zero-dependency Google Protocol Buffers implementation in pure Zig. Single all.
---

# gremlin.zig Plugin

A zero-dependency Google Protocol Buffers implementation in pure Zig. Single allocation encode and lazy decode

## Commands

### General
- `gremlin.zig self version` — Print gremlin.zig version
- `gremlin.zig _ _` — Passthrough to gremlin.zig CLI

## Usage Examples
- "Run gremlin.zig"
- "Use gremlin.zig to A zero-dependency Google Protocol Buffers implementation in "

## Installation

```bash
pip install gremlin.zig
```

## Key Features
- A zero-dependency Google Protocol Buffers implementation in pure Zig. Single allocation encode and l
- CLI tool, headless and scriptable
