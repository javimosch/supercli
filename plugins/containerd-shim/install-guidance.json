{ "plugin": "containerd-shim", "binary": "containerd-shim", "check": "which containerd-shim", "install_steps": ["apt-get install containerd-shim 2>/dev/null || which containerd-shim", "Verify: containerd-shim --version", "supercli plugins install ./plugins/containerd-shim --on-conflict replace --json"] }
