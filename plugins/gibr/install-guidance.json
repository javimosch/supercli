{
  "plugin": "gibr",
  "binary": "gibr",
  "check": "which gibr",
  "install_steps": [
    "pip install gibr",
    "Verify: gibr --version",
    "supercli plugins install ./plugins/gibr --on-conflict replace --json"
  ],
  "note": "Requires Python and pip"
}
