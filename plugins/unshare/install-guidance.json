{
  "plugin": "unshare",
  "binary": "unshare",
  "check": "which unshare",
  "install_steps": [
    "apt-get install util-linux",
    "Verify: unshare --version",
    "supercli plugins install ./plugins/unshare --on-conflict replace --json"
  ]
}
