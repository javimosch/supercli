---
name: poof
description: Use this skill when the user wants to use poof for Ephemeral filesystem isolation via Linux overlayfs (experimental).
---

# poof Plugin

Ephemeral filesystem isolation via Linux overlayfs (experimental)

## Commands

### General
- `poof self version` — Print poof version
- `poof _ _` — Passthrough to poof CLI

## Usage Examples
- "Run poof"
- "Use poof to Ephemeral filesystem isolation via Linux overlayfs (experime"

## Installation

```bash
pip install poof
```

## Key Features
- Ephemeral filesystem isolation via Linux overlayfs (experimental)
- CLI tool, headless and scriptable
