{
  "plugin": "tsv-utils",
  "binary": "tsv-filter",
  "check": "which tsv-filter",
  "install_steps": [
    "brew install tsv-utils",
    "Or build from source: git clone https://github.com/eBay/tsv-utils.git && cd tsv-utils && make",
    "Verify: tsv-filter --help"
  ]
}
