{
  "plugin": "git-delta",
  "binary": "git-delta",
  "check": "which git-delta",
  "install_steps": [
    "brew install git-delta",
    "Verify: git-delta --version",
    "supercli plugins install ./plugins/git-delta --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}