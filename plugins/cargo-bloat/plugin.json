{
  "name": "cargo-bloat",
  "version": "0.1.0",
  "description": "cargo-bloat — analyze Rust binary size to find what takes up space",
  "source": "https",
  "checks": [
    {
      "type": "binary",
      "name": "cargo-bloat"
    }
  ],
  "install_guidance": {
    "plugin": "cargo-bloat",
    "binary": "cargo-bloat",
    "check": "which cargo-bloat",
    "install_steps": [
      "brew install cargo-bloat"
    ],
    "note": "Dev tool."
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "cargo-bloat",
      "resource": "_",
      "action": "_",
      "description": "Passthrough",
      "adapter": "process",
      "adapterConfig": {
        "command": "cargo-bloat",
        "passthrough": true,
        "missingDependencyHelp": "Install cargo-bloat: brew install cargo-bloat"
      },
      "args": []
    }
  ]
}
