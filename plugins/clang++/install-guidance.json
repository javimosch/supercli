{ "plugin": "clang++", "binary": "clang++", "check": "which clang++", "install_steps": ["apt-get install clang++ 2>/dev/null || which clang++", "Verify: clang++ --version", "supercli plugins install ./plugins/clang++ --on-conflict replace --json"] }
