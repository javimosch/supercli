{
  "plugin": "osinfo-query",
  "binary": "osinfo-query",
  "check": "which osinfo-query",
  "install_steps": [
    "apt-get install osinfo-query 2>/dev/null || which osinfo-query",
    "Verify: osinfo-query --version",
    "supercli plugins install ./plugins/osinfo-query --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}