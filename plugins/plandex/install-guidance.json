{
  "plugin": "plandex",
  "binary": "plandex",
  "check": "which plandex",
  "install_steps": [
    "brew install plandex",
    "Verify: plandex --version",
    "supercli plugins install ./plugins/plandex --on-conflict replace --json"
  ]
}