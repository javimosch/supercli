{
  "plugin": "dt2dv",
  "binary": "dt2dv",
  "check": "which dt2dv",
  "install_steps": [
    "apt-get install dt2dv 2>/dev/null || which dt2dv",
    "Verify: dt2dv --version",
    "supercli plugins install ./plugins/dt2dv --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}