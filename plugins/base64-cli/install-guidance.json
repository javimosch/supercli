{
  "plugin": "base64-cli",
  "binary": "base64-cli",
  "check": "which base64-cli",
  "install_steps": ["npm install -g base64-cli", "Verify: base64-cli --version", "supercli plugins install ./plugins/base64-cli --on-conflict replace --json"]
}
