{
  "plugin": "unzip",
  "binary": "unzip",
  "check": "which unzip",
  "install_steps": [
    "brew install unzip",
    "Verify: unzip --version",
    "supercli plugins install ./plugins/unzip --on-conflict replace --json"
  ]
}
