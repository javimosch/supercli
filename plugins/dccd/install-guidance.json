{ "plugin": "dccd", "binary": "dccd", "check": "which dccd", "install_steps": ["apt-get install dccd 2>/dev/null || which dccd", "Verify: dccd --version", "supercli plugins install ./plugins/dccd --on-conflict replace --json"] }
