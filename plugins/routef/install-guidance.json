{
  "plugin": "routef",
  "binary": "routef",
  "check": "which routef",
  "install_steps": [
    "apt-get install routef 2>/dev/null || which routef",
    "Verify: routef --version",
    "supercli plugins install ./plugins/routef --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}