{
  "name": "aleph",
  "version": "0.1.0",
  "description": "CLI tool: aleph",
  "source": "https://packages.debian.org/aleph",
  "checks": [{ "type": "binary", "name": "aleph" }],
  "install_guidance": {
    "plugin": "aleph", "binary": "aleph", "check": "which aleph",
    "install_steps": ["apt-get install aleph 2>/dev/null || which aleph", "Verify: aleph --version", "supercli plugins install ./plugins/aleph --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "aleph", "resource": "_", "action": "_",
    "description": "Passthrough to aleph CLI",
    "adapter": "process", "adapterConfig": { "command": "aleph", "passthrough": true, "missingDependencyHelp": "Install aleph: apt-get install aleph" }, "args": []
  }]
}
