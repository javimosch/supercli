{
  "plugin": "zamba",
  "binary": "zamba",
  "check": "which zamba",
  "install_steps": [
    "pip install zamba",
    "Verify: zamba --version",
    "supercli plugins install ./plugins/zamba --on-conflict replace --json"
  ]
}