{
  "plugin": "e2image",
  "binary": "e2image",
  "check": "which e2image",
  "install_steps": [
    "apt-get install e2image 2>/dev/null || which e2image",
    "Verify: e2image --version",
    "supercli plugins install ./plugins/e2image --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}