{
  "plugin": "hygen",
  "binary": "hygen",
  "check": "which hygen",
  "install_steps": [
    "npm install -g hygen",
    "Verify: hygen --version",
    "supercli plugins install ./plugins/hygen --on-conflict replace --json"
  ]
}