{
  "plugin": "foremost",
  "binary": "foremost",
  "check": "which foremost",
  "install_steps": [
    "brew install foremost",
    "Verify: foremost --version",
    "supercli plugins install ./plugins/foremost --on-conflict replace --json"
  ]
}
