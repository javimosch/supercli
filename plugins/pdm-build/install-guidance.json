{
  "plugin": "pdm-build",
  "binary": "pdm-build",
  "check": "which pdm-build",
  "install_steps": ["pip install pdm-build", "Verify: pdm-build --version", "supercli plugins install ./plugins/pdm-build --on-conflict replace --json"]
}
