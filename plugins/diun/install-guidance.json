{
  "plugin": "diun",
  "binary": "diun",
  "check": "which diun || echo 'diun not installed'",
  "install_steps": [
    "Download from https://github.com/crazy-max/diun/releases/latest",
    "Or run: docker run -d --name diun -v $(pwd)/diun.yml:/diun.yml crazymax/diun serve",
    "supercli plugins install ./plugins/diun --on-conflict replace --json"
  ],
  "note": "See https://crazymax.dev/diun/ for configuration reference. Supports notifications via Slack, Discord, Telegram, Gotify, and more."
}
