{
  "plugin": "gray-matter",
  "binary": "gray-matter",
  "check": "which gray-matter",
  "install_steps": ["npm install -g gray-matter", "Verify: gray-matter --version", "supercli plugins install ./plugins/gray-matter --on-conflict replace --json"]
}
