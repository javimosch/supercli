{
  "plugin": "sha384sum",
  "binary": "sha384sum",
  "check": "which sha384sum",
  "install_steps": [
    "which sha384sum",
    "Verify: sha384sum --version",
    "supercli plugins install ./plugins/sha384sum --on-conflict replace --json"
  ]
}
