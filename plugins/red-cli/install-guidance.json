{
  "plugin": "red-cli",
  "binary": "red-cli",
  "check": "which red-cli",
  "install_steps": ["go install github.com/red-cli@latest", "Verify: red-cli --version", "supercli plugins install ./plugins/red-cli --on-conflict replace --json"]
}
