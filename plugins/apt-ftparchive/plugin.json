{
  "name": "apt-ftparchive",
  "version": "0.1.0",
  "description": "CLI tool: apt-ftparchive",
  "source": "https://packages.debian.org/apt-ftparchive",
  "checks": [{ "type": "binary", "name": "apt-ftparchive" }],
  "install_guidance": {
    "plugin": "apt-ftparchive", "binary": "apt-ftparchive", "check": "which apt-ftparchive",
    "install_steps": ["apt-get install apt-ftparchive 2>/dev/null || which apt-ftparchive", "Verify: apt-ftparchive --version", "supercli plugins install ./plugins/apt-ftparchive --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "apt-ftparchive", "resource": "_", "action": "_",
    "description": "Passthrough to apt-ftparchive CLI",
    "adapter": "process", "adapterConfig": { "command": "apt-ftparchive", "passthrough": true, "missingDependencyHelp": "Install apt-ftparchive: apt-get install apt-ftparchive" }, "args": []
  }]
}
