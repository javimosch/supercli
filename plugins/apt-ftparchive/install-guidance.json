{ "plugin": "apt-ftparchive", "binary": "apt-ftparchive", "check": "which apt-ftparchive", "install_steps": ["apt-get install apt-ftparchive 2>/dev/null || which apt-ftparchive", "Verify: apt-ftparchive --version", "supercli plugins install ./plugins/apt-ftparchive --on-conflict replace --json"] }
