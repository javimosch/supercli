{
  "plugin": "hub",
  "binary": "hub",
  "check": "which hub",
  "install_steps": [
    "# Install hub",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/hub-linux-amd64 -o /usr/local/bin/hub",
    "chmod +x /usr/local/bin/hub",
    "# Or via package manager, see hub.github.com"
  ]
}