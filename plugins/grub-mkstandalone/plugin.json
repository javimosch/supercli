{
  "name": "grub-mkstandalone",
  "version": "0.1.0",
  "description": "CLI tool: grub-mkstandalone",
  "source": "https://manpages.debian.org/grub-mkstandalone",
  "checks": [
    {
      "type": "binary",
      "name": "grub-mkstandalone"
    }
  ],
  "install_guidance": {
    "plugin": "grub-mkstandalone",
    "binary": "grub-mkstandalone",
    "check": "which grub-mkstandalone",
    "install_steps": [
      "apt-get install grub-mkstandalone 2>/dev/null || which grub-mkstandalone",
      "Verify: grub-mkstandalone --version",
      "supercli plugins install ./plugins/grub-mkstandalone --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "grub-mkstandalone",
      "resource": "self",
      "action": "version",
      "description": "Print grub-mkstandalone version",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkstandalone",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install grub-mkstandalone: apt-get install grub-mkstandalone 2>/dev/null || which grub-mkstandalone"
      },
      "args": []
    },
    {
      "namespace": "grub-mkstandalone",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to grub-mkstandalone CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "grub-mkstandalone",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install grub-mkstandalone: apt-get install grub-mkstandalone 2>/dev/null || which grub-mkstandalone"
      },
      "args": []
    }
  ]
}