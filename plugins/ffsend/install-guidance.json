{
  "plugin": "ffsend",
  "binary": "ffsend",
  "check": "which ffsend",
  "install_steps": [
    "cargo install ffsend",
    "Verify: ffsend --version",
    "supercli plugins install ./plugins/ffsend --on-conflict replace --json"
  ],
  "note": "Also available via: brew, snap, pacman, scoop, nix. Files encrypted client-side before upload."
}