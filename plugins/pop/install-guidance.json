{
  "plugin": "pop",
  "binary": "pop",
  "check": "which pop",
  "install_steps": ["brew install pop", "Verify: pop --version", "supercli plugins install ./plugins/pop --on-conflict replace --json"]
}
