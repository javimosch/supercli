{
  "plugin": "amp",
  "binary": "amp",
  "check": "which amp",
  "install_steps": ["cargo install amp", "Verify: amp --version", "supercli plugins install ./plugins/amp --on-conflict replace --json"]
}
