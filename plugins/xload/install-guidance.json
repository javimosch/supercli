{
  "plugin": "xload",
  "binary": "xload",
  "check": "which xload",
  "install_steps": [
    "apt-get install xload 2>/dev/null || which xload",
    "Verify: xload --version",
    "supercli plugins install ./plugins/xload --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}