{
  "plugin": "aniemoji",
  "binary": "aniemoji",
  "check": "which aniemoji",
  "install_steps": ["npm install -g aniemoji", "Verify: aniemoji --version", "supercli plugins install ./plugins/aniemoji --on-conflict replace --json"]
}
