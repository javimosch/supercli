{ "plugin": "addgroup", "binary": "addgroup", "check": "which addgroup", "install_steps": ["apt-get install addgroup 2>/dev/null || which addgroup", "Verify: addgroup --version", "supercli plugins install ./plugins/addgroup --on-conflict replace --json"] }
