{
  "name": "addgroup",
  "version": "0.1.0",
  "description": "CLI tool: addgroup",
  "source": "https://packages.debian.org/addgroup",
  "checks": [{ "type": "binary", "name": "addgroup" }],
  "install_guidance": {
    "plugin": "addgroup", "binary": "addgroup", "check": "which addgroup",
    "install_steps": ["apt-get install addgroup 2>/dev/null || which addgroup", "Verify: addgroup --version", "supercli plugins install ./plugins/addgroup --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "addgroup", "resource": "_", "action": "_",
    "description": "Passthrough to addgroup CLI",
    "adapter": "process", "adapterConfig": { "command": "addgroup", "passthrough": true, "missingDependencyHelp": "Install addgroup: apt-get install addgroup" }, "args": []
  }]
}
