{
  "plugin": "setpci",
  "binary": "setpci",
  "check": "which setpci",
  "install_steps": [
    "apt-get install setpci 2>/dev/null || which setpci",
    "Verify: setpci --version",
    "supercli plugins install ./plugins/setpci --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}