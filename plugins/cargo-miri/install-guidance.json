{ "plugin": "cargo-miri", "binary": "cargo-miri", "check": "which cargo-miri", "install_steps": ["apt-get install cargo-miri 2>/dev/null || which cargo-miri", "Verify: cargo-miri --version", "supercli plugins install ./plugins/cargo-miri --on-conflict replace --json"] }
