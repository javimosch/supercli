{
  "plugin": "dee-feed",
  "binary": "dee-feed",
  "check": "which dee-feed",
  "install_steps": ["cargo install dee-feed", "Verify: dee-feed --version", "supercli plugins install ./plugins/dee-feed --on-conflict replace --json"]
}
