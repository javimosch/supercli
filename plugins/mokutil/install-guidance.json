{
  "plugin": "mokutil",
  "binary": "mokutil",
  "check": "which mokutil",
  "install_steps": [
    "apt-get install mokutil 2>/dev/null || which mokutil",
    "Verify: mokutil --version",
    "supercli plugins install ./plugins/mokutil --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}