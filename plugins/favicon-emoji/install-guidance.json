{
  "plugin": "favicon-emoji",
  "binary": "favicon-emoji",
  "check": "which favicon-emoji",
  "install_steps": [
    "npm install -g favicon-emoji",
    "Verify: favicon-emoji --version",
    "supercli plugins install ./plugins/favicon-emoji --on-conflict replace --json"
  ]
}
