{ "plugin": "api", "binary": "api", "check": "which api", "install_steps": ["apt-get install api 2>/dev/null || which api", "Verify: api --version", "supercli plugins install ./plugins/api --on-conflict replace --json"] }
