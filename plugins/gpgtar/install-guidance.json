{
  "plugin": "gpgtar",
  "binary": "gpgtar",
  "check": "which gpgtar",
  "install_steps": [
    "apt-get install gpgtar 2>/dev/null || which gpgtar",
    "Verify: gpgtar --version",
    "supercli plugins install ./plugins/gpgtar --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}