{
  "name": "pg_basebackup",
  "version": "0.1.0",
  "description": "CLI tool: pg_basebackup",
  "source": "https://manpages.debian.org/pg_basebackup",
  "checks": [
    {
      "type": "binary",
      "name": "pg_basebackup"
    }
  ],
  "install_guidance": {
    "plugin": "pg_basebackup",
    "binary": "pg_basebackup",
    "check": "which pg_basebackup",
    "install_steps": [
      "apt-get install pg_basebackup 2>/dev/null || which pg_basebackup",
      "Verify: pg_basebackup --version",
      "supercli plugins install ./plugins/pg_basebackup --on-conflict replace --json"
    ]
  },
  "learn": {
    "file": "skills/quickstart/SKILL.md"
  },
  "commands": [
    {
      "namespace": "pg_basebackup",
      "resource": "self",
      "action": "version",
      "description": "Print pg_basebackup version",
      "adapter": "process",
      "adapterConfig": {
        "command": "pg_basebackup",
        "baseArgs": [
          "--version"
        ],
        "missingDependencyHelp": "Install pg_basebackup: apt-get install pg_basebackup 2>/dev/null || which pg_basebackup"
      },
      "args": []
    },
    {
      "namespace": "pg_basebackup",
      "resource": "_",
      "action": "_",
      "description": "Passthrough to pg_basebackup CLI",
      "adapter": "process",
      "adapterConfig": {
        "command": "pg_basebackup",
        "passthrough": true,
        "cwd": "invoke_cwd",
        "missingDependencyHelp": "Install pg_basebackup: apt-get install pg_basebackup 2>/dev/null || which pg_basebackup"
      },
      "args": []
    }
  ]
}