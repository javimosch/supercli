{ "plugin": "apache2ctl", "binary": "apache2ctl", "check": "which apache2ctl", "install_steps": ["apt-get install apache2ctl 2>/dev/null || which apache2ctl", "Verify: apache2ctl --version", "supercli plugins install ./plugins/apache2ctl --on-conflict replace --json"] }
