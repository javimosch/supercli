{
  "plugin": "jstack",
  "binary": "jstack",
  "check": "which jstack",
  "install_steps": [
    "apt-get install jstack 2>/dev/null || which jstack",
    "Verify: jstack --version",
    "supercli plugins install ./plugins/jstack --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}