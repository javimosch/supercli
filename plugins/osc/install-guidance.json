{
  "plugin": "osc",
  "binary": "osc",
  "check": "which osc",
  "install_steps": [
    "brew install osc",
    "Verify: osc --version",
    "supercli plugins install ./plugins/osc --on-conflict replace --json"
  ]
}