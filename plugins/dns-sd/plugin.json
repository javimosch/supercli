{
  "name": "dns-sd",
  "version": "0.1.0",
  "description": "DNS service discovery CLI",
  "source": "https://packages.debian.org/dns-sd",
  "checks": [{ "type": "binary", "name": "dns-sd" }],
  "install_guidance": {
    "plugin": "dns-sd", "binary": "dns-sd", "check": "which dns-sd",
    "install_steps": ["apt-get install libnss-mdns", "Verify: dns-sd --version", "supercli plugins install ./plugins/dns-sd --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dns-sd", "resource": "_", "action": "_",
    "description": "Passthrough to dns-sd CLI",
    "adapter": "process", "adapterConfig": { "command": "dns-sd", "passthrough": true, "missingDependencyHelp": "Install dns-sd: apt-get install libnss-mdns" }, "args": []
  }]
}
