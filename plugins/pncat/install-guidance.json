{
  "plugin": "pncat",
  "binary": "pncat",
  "check": "which pncat",
  "install_steps": [
    "npm install -g pncat",
    "Verify: pncat --version",
    "supercli plugins install ./plugins/pncat --on-conflict replace --json"
  ]
}