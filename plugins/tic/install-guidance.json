{
  "plugin": "tic",
  "binary": "tic",
  "check": "which tic",
  "install_steps": [
    "apt-get install tic 2>/dev/null || which tic",
    "Verify: tic --version",
    "supercli plugins install ./plugins/tic --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}