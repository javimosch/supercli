{
  "plugin": "vuln-scanner",
  "binary": "vuln-scanner",
  "check": "which vuln-scanner",
  "install_steps": ["go install github.com/vuln-scanner@latest", "Verify: vuln-scanner --version", "supercli plugins install ./plugins/vuln-scanner --on-conflict replace --json"]
}
