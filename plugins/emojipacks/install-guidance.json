{
  "plugin": "emojipacks",
  "binary": "emojipacks",
  "check": "which emojipacks",
  "install_steps": [
    "npm install -g emojipacks",
    "Verify: emojipacks --version",
    "supercli plugins install ./plugins/emojipacks --on-conflict replace --json"
  ]
}
