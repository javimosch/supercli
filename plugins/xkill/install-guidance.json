{
  "plugin": "xkill",
  "binary": "xkill",
  "check": "which xkill",
  "install_steps": [
    "apt-get install xkill 2>/dev/null || which xkill",
    "Verify: xkill --version",
    "supercli plugins install ./plugins/xkill --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}