{
  "plugin": "jps",
  "binary": "jps",
  "check": "which jps",
  "install_steps": [
    "apt-get install jps 2>/dev/null || which jps",
    "Verify: jps --version",
    "supercli plugins install ./plugins/jps --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}