{
  "name": "getconf",
  "version": "1.0.0",
  "description": "getconf \u2014 CLI getconf \u2014 Command-line utility for system administration",
  "source": "https://github.com/search?q=getconf+cli",
  "checks": [
    {
      "type": "binary",
      "name": "getconf"
    }
  ],
  "install_guidance": {
    "plugin": "getconf",
    "binary": "getconf",
    "check": "which getconf",
    "install_steps": [
      "Install via package manager: apt install getconf or brew install getconf"
    ]
  },
  "commands": [
    {
      "namespace": "getconf",
      "resource": "run",
      "action": "exec",
      "description": "getconf \u2014 Command-line utility for system administration",
      "adapter": "process",
      "adapterConfig": {
        "command": "getconf",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install getconf via package manager"
      },
      "args": []
    }
  ]
}