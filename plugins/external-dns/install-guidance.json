{
  "plugin": "external-dns",
  "binary": "external-dns",
  "check": "which external-dns",
  "install_steps": ["brew install external-dns", "Verify: external-dns --version", "supercli plugins install ./plugins/external-dns --on-conflict replace --json"]
}
