{
  "plugin": "htop",
  "binary": "htop",
  "check": "which htop",
  "install_steps": [
    "brew install htop",
    "Verify: htop --version",
    "supercli plugins install ./plugins/htop --on-conflict replace --json"
  ]
}
