{
  "plugin": "sad",
  "binary": "sad",
  "check": "which sad",
  "install_steps": [
    "# Install sad",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/sad-linux-amd64 -o /usr/local/bin/sad",
    "chmod +x /usr/local/bin/sad",
    "# Or via package manager, see crates.io/crates/sad"
  ]
}