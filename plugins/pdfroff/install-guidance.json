{
  "plugin": "pdfroff",
  "binary": "pdfroff",
  "check": "which pdfroff",
  "install_steps": [
    "apt-get install pdfroff 2>/dev/null || which pdfroff",
    "Verify: pdfroff --version",
    "supercli plugins install ./plugins/pdfroff --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}