{
  "plugin": "grog",
  "binary": "grog",
  "check": "which grog",
  "install_steps": [
    "apt-get install grog 2>/dev/null || which grog",
    "Verify: grog --version",
    "supercli plugins install ./plugins/grog --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}