{
  "plugin": "guard",
  "binary": "guard",
  "check": "which guard",
  "install_steps": ["go install github.com/stellarproject/guard@latest", "Verify: guard --version", "supercli plugins install ./plugins/guard --on-conflict replace --json"]
}
