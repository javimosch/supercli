{
  "plugin": "repo-radar",
  "binary": "repo-radar",
  "check": "which repo-radar",
  "install_steps": [
    "cargo install repo-radar",
    "Verify: repo-radar --version",
    "supercli plugins install ./plugins/repo-radar --on-conflict replace --json"
  ],
  "note": "Binary name: repo-radar"
}
