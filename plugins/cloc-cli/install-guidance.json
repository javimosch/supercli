{
  "plugin": "cloc-cli",
  "binary": "cloc",
  "check": "which cloc",
  "install_steps": [
    "npm install -g cloc",
    "Verify: cloc --version",
    "supercli plugins install ./plugins/cloc-cli --on-conflict replace --json"
  ],
  "note": "Cloc (Count Lines of Code) counts lines in source files. Supports 200+ programming languages. Use --json for machine-readable output. Can exclude directories with --exclude-dir."
}
