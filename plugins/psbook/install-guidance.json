{
  "plugin": "psbook",
  "binary": "psbook",
  "check": "which psbook",
  "install_steps": [
    "apt-get install psbook 2>/dev/null || which psbook",
    "Verify: psbook --version",
    "supercli plugins install ./plugins/psbook --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}