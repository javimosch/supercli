{
  "plugin": "tfmv",
  "binary": "tfmv",
  "check": "which tfmv",
  "install_steps": [
    "brew install suzuki-shunsuke/tap/tfmv",
    "Verify: tfmv --version",
    "supercli plugins install ./plugins/tfmv --on-conflict replace --json"
  ]
}