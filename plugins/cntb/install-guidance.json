{
  "plugin": "cntb",
  "binary": "cntb",
  "check": "which cntb",
  "install_steps": [
    "brew install cntb",
    "Verify: cntb --version",
    "supercli plugins install ./plugins/cntb --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}