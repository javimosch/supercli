{
  "plugin": "pvck",
  "binary": "pvck",
  "check": "which pvck",
  "install_steps": [
    "apt-get install pvck 2>/dev/null || which pvck",
    "Verify: pvck --version",
    "supercli plugins install ./plugins/pvck --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}