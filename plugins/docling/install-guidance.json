{
  "plugin": "docling",
  "binary": "docling",
  "check": "which docling",
  "install_steps": [
    "pip install docling",
    "Verify: docling --version",
    "supercli plugins install ./plugins/docling --on-conflict replace --json"
  ],
  "note": "Install via pip: pip install docling"
}
