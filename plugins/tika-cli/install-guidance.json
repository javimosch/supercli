{
  "plugin": "tika-cli",
  "binary": "tika-cli",
  "check": "which tika-cli",
  "install_steps": ["java -jar tika-app.jar", "Verify: tika-cli --version", "supercli plugins install ./plugins/tika-cli --on-conflict replace --json"]
}
