{
  "plugin": "pytube",
  "binary": "pytube",
  "check": "which pytube",
  "install_steps": [
    "pip install pytube",
    "Verify: pytube --version",
    "supercli plugins install ./plugins/pytube --on-conflict replace --json"
  ]
}
