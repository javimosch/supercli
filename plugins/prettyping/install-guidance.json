{
  "plugin": "prettyping",
  "binary": "prettyping",
  "check": "which prettyping",
  "install_steps": [
    "# Install prettyping",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/prettyping-linux-amd64 -o /usr/local/bin/prettyping",
    "chmod +x /usr/local/bin/prettyping",
    "# Or via package manager, see github.com/denilsonsa/prettyping"
  ]
}