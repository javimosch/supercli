{
  "plugin": "ppmcie",
  "binary": "ppmcie",
  "check": "which ppmcie",
  "install_steps": [
    "apt-get install ppmcie 2>/dev/null || which ppmcie",
    "Verify: ppmcie --version",
    "supercli plugins install ./plugins/ppmcie --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}