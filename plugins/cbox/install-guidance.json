{
  "plugin": "cbox",
  "binary": "cbox",
  "check": "which cbox",
  "install_steps": [
    "pip install cbox",
    "Verify: cbox --version",
    "supercli plugins install ./plugins/cbox --on-conflict replace --json"
  ]
}