{
  "plugin": "mirrord",
  "binary": "mirrord",
  "check": "which mirrord",
  "install_steps": [
    "brew install mirrord",
    "Verify: mirrord --version",
    "supercli plugins install ./plugins/mirrord --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install mirrord. Connects local processes to cloud Kubernetes environments. Enables local debugging with cloud context. Supports network traffic interception, file system access, and environment variable loading."
}
