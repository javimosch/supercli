{
  "plugin": "mirrord",
  "binary": "mirrord",
  "check": "which mirrord",
  "install_steps": ["brew install mirrord", "Verify: mirrord --version", "supercli plugins install ./plugins/mirrord --on-conflict replace --json"]
}
