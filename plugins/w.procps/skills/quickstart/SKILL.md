---
name: w.procps
description: Use this skill when the user wants to use w.procps, cli tool: w.procps.
---

# w.procps Plugin

CLI tool: w.procps.

## Commands
- `w.procps <resource> <action>` — Execute w.procps commands
- `w.procps self version` — Print w.procps version
- `w.procps _ _` — Passthrough to w.procps CLI

## Usage Examples
- "w.procps --help"
- "w.procps self version"

## Installation
```bash
apt-get install w.procps 2>/dev/null || which w.procps
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
