{
  "plugin": "whis",
  "binary": "whis",
  "check": "which whis",
  "install_steps": [
    "cargo install whis-cli",
    "Verify: whis --version",
    "supercli plugins install ./plugins/whis --on-conflict replace --json"
  ],
  "note": "Linux requires Vulkan SDK for local transcription"
}
