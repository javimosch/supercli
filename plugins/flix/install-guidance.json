{
  "plugin": "flix",
  "binary": "flix",
  "check": "which flix",
  "install_steps": [
    "# Install flix",
    "# See flix.dev",
    "# apt: sudo apt-get install flix",
    "# brew: brew install flix",
    "# cargo: cargo install flix",
    "# npm: npm install -g flix"
  ]
}