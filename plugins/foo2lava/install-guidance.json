{
  "plugin": "foo2lava",
  "binary": "foo2lava",
  "check": "which foo2lava",
  "install_steps": [
    "apt-get install foo2lava 2>/dev/null || which foo2lava",
    "Verify: foo2lava --version",
    "supercli plugins install ./plugins/foo2lava --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}