{
  "plugin": "demystify",
  "binary": "demystify",
  "check": "which demystify",
  "install_steps": [
    "npm install -g demystify",
    "Verify: demystify --version",
    "supercli plugins install ./plugins/demystify --on-conflict replace --json"
  ]
}