{
  "plugin": "git-sweep",
  "binary": "git-sweep",
  "check": "which git-sweep",
  "install_steps": ["pip install git-sweep", "Verify: git-sweep --version", "supercli plugins install ./plugins/git-sweep --on-conflict replace --json"]
}
