{
  "plugin": "svgo",
  "binary": "svgo",
  "check": "which svgo",
  "install_steps": [
    "npm install -g svgo",
    "Verify: svgo --version",
    "supercli plugins install ./plugins/svgo --on-conflict replace --json"
  ]
}
