{
  "plugin": "svgo",
  "binary": "svgo",
  "check": "which svgo",
  "install_steps": [
    "npm install -g svgo",
    "Verify: svgo --version",
    "supercli plugins install ./plugins/svgo --on-conflict replace --json"
  ],
  "note": "Also available via yarn: yarn global add svgo, or pnpm: pnpm add -g svgo. Supports plugin architecture for custom optimizations. Can process directories recursively."
}
