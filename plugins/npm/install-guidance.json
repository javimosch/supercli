{
  "plugin": "npm",
  "binary": "npm",
  "check": "which npm",
  "install_steps": [
    "brew install npm",
    "Verify: npm --version",
    "supercli plugins install ./plugins/npm --on-conflict replace --json"
  ]
}