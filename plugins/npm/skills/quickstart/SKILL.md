---
name: npm
description: Use this skill when the user wants to install Node.js packages, manage dependencies, run scripts, or publish packages to the npm registry.
---

# npm Plugin

npm is the standard package manager for Node.js.

## Usage Examples

- "install dependencies from package.json"
- "add a new package to the project"
- "run the test script from package.json"
- "publish a new version to npm"

## Installation

```bash
npm install -g npm
```

## Key Features
- Package installation and publishing
- Dependency version management
- Script execution (npm run)
- Workspace monorepo support
- Lockfile for reproducible builds
- CI-optimized npm ci command
- Audit for security vulnerabilities
- Private registry support
