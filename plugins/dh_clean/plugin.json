{
  "name": "dh_clean",
  "version": "0.1.0",
  "description": "CLI tool: dh_clean",
  "source": "https://packages.debian.org/dh_clean",
  "checks": [{ "type": "binary", "name": "dh_clean" }],
  "install_guidance": {
    "plugin": "dh_clean", "binary": "dh_clean", "check": "which dh_clean",
    "install_steps": ["apt-get install dh_clean 2>/dev/null || which dh_clean", "Verify: dh_clean --version", "supercli plugins install ./plugins/dh_clean --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dh_clean", "resource": "_", "action": "_",
    "description": "Passthrough to dh_clean CLI",
    "adapter": "process", "adapterConfig": { "command": "dh_clean", "passthrough": true, "missingDependencyHelp": "Install dh_clean: apt-get install dh_clean" }, "args": []
  }]
}
