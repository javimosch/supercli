{ "plugin": "dh_clean", "binary": "dh_clean", "check": "which dh_clean", "install_steps": ["apt-get install dh_clean 2>/dev/null || which dh_clean", "Verify: dh_clean --version", "supercli plugins install ./plugins/dh_clean --on-conflict replace --json"] }
