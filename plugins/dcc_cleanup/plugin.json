{
  "name": "dcc_cleanup",
  "version": "0.1.0",
  "description": "CLI tool: dcc_cleanup",
  "source": "https://packages.debian.org/dcc_cleanup",
  "checks": [{ "type": "binary", "name": "dcc_cleanup" }],
  "install_guidance": {
    "plugin": "dcc_cleanup", "binary": "dcc_cleanup", "check": "which dcc_cleanup",
    "install_steps": ["apt-get install dcc_cleanup 2>/dev/null || which dcc_cleanup", "Verify: dcc_cleanup --version", "supercli plugins install ./plugins/dcc_cleanup --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "dcc_cleanup", "resource": "_", "action": "_",
    "description": "Passthrough to dcc_cleanup CLI",
    "adapter": "process", "adapterConfig": { "command": "dcc_cleanup", "passthrough": true, "missingDependencyHelp": "Install dcc_cleanup: apt-get install dcc_cleanup" }, "args": []
  }]
}
