{ "plugin": "dcc_cleanup", "binary": "dcc_cleanup", "check": "which dcc_cleanup", "install_steps": ["apt-get install dcc_cleanup 2>/dev/null || which dcc_cleanup", "Verify: dcc_cleanup --version", "supercli plugins install ./plugins/dcc_cleanup --on-conflict replace --json"] }
