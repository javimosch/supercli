{
  "plugin": "dog",
  "binary": "dog",
  "check": "which dog",
  "install_steps": [
    "brew install dog",
    "Verify: dog --version",
    "supercli plugins install ./plugins/dog --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install dog. A command-line DNS client with colorful, human-readable output. Supports all major record types (A, AAAA, MX, NS, TXT, CNAME, SOA). Uses DNS-over-TLS and DNS-over-HTTPS."
}
