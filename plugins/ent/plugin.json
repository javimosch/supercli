{
  "name": "ent",
  "version": "1.0.0",
  "description": "ent \u2014 Go entity framework",
  "source": "entgo.io",
  "checks": [
    {
      "type": "binary",
      "name": "ent"
    }
  ],
  "install_guidance": {
    "plugin": "ent",
    "binary": "ent",
    "check": "which ent",
    "install_steps": [
      "# Install ent",
      "# See entgo.io",
      "# apt: sudo apt-get install ent",
      "# brew: brew install ent",
      "# cargo: cargo install ent",
      "# npm: npm install -g ent",
      "# pip: pip install ent"
    ]
  },
  "commands": [
    {
      "namespace": "ent",
      "resource": "ent",
      "action": "run",
      "description": "Run ent",
      "adapter": "process",
      "adapterConfig": {
        "command": "ent",
        "passthrough": true,
        "timeout_ms": 30000,
        "missingDependencyHelp": "Install ent from entgo.io"
      },
      "args": []
    }
  ]
}