{
  "plugin": "dlm",
  "binary": "dlm",
  "check": "which dlm",
  "install_steps": [
    "cargo install dlm",
    "Verify: dlm --version",
    "supercli plugins install ./plugins/dlm --on-conflict replace --json"
  ],
  "note": "Also available via direct download from GitHub releases or crates.io."
}
