{ "plugin": "db_recover", "binary": "db_recover", "check": "which db_recover", "install_steps": ["apt-get install db_recover 2>/dev/null || which db_recover", "Verify: db_recover --version", "supercli plugins install ./plugins/db_recover --on-conflict replace --json"] }
