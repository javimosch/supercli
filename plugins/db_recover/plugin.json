{
  "name": "db_recover",
  "version": "0.1.0",
  "description": "CLI tool: db_recover",
  "source": "https://packages.debian.org/db_recover",
  "checks": [{ "type": "binary", "name": "db_recover" }],
  "install_guidance": {
    "plugin": "db_recover", "binary": "db_recover", "check": "which db_recover",
    "install_steps": ["apt-get install db_recover 2>/dev/null || which db_recover", "Verify: db_recover --version", "supercli plugins install ./plugins/db_recover --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "db_recover", "resource": "_", "action": "_",
    "description": "Passthrough to db_recover CLI",
    "adapter": "process", "adapterConfig": { "command": "db_recover", "passthrough": true, "missingDependencyHelp": "Install db_recover: apt-get install db_recover" }, "args": []
  }]
}
