{
  "name": "apt-get",
  "version": "0.1.0",
  "description": "CLI tool: apt-get",
  "source": "https://packages.debian.org/apt-get",
  "checks": [{ "type": "binary", "name": "apt-get" }],
  "install_guidance": {
    "plugin": "apt-get", "binary": "apt-get", "check": "which apt-get",
    "install_steps": ["apt-get install apt-get 2>/dev/null || which apt-get", "Verify: apt-get --version", "supercli plugins install ./plugins/apt-get --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "apt-get", "resource": "_", "action": "_",
    "description": "Passthrough to apt-get CLI",
    "adapter": "process", "adapterConfig": { "command": "apt-get", "passthrough": true, "missingDependencyHelp": "Install apt-get: apt-get install apt-get" }, "args": []
  }]
}
