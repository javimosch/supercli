{
  "plugin": "twarc2",
  "binary": "twarc2",
  "check": "which twarc2",
  "install_steps": [
    "apt-get install twarc2 2>/dev/null || which twarc2",
    "Verify: twarc2 --version",
    "supercli plugins install ./plugins/twarc2 --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}