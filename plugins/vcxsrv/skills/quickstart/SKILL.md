---
name: vcxsrv
description: Use this skill when the user wants to use vcxsrv for Windows X-server based on the xorg git sources (like xming or cygwin's xwin), bu.
---

# vcxsrv Plugin

Windows X-server based on the xorg git sources (like xming or cygwin's xwin), but compiled with Visual Studio 2012 Community Edition.

## Commands

### General
- `vcxsrv self version` — Print vcxsrv version
- `vcxsrv _ _` — Passthrough to vcxsrv CLI

## Usage Examples
- "Run vcxsrv"
- "Use vcxsrv to Windows X-server based on the xorg git sources (like xming o"

## Installation

```bash
pip install vcxsrv
```

## Key Features
- Windows X-server based on the xorg git sources (like xming or cygwin's xwin), but compiled with Visu
- CLI tool, headless and scriptable
