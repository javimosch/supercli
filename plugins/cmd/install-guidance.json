{ "plugin": "cmd", "binary": "cmd", "check": "which cmd", "install_steps": ["apt-get install cmd 2>/dev/null || which cmd", "Verify: cmd --version", "supercli plugins install ./plugins/cmd --on-conflict replace --json"] }
