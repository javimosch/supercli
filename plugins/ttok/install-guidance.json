{
  "plugin": "ttok",
  "binary": "ttok",
  "check": "which ttok",
  "install_steps": ["pip install ttok", "Verify: ttok --version", "supercli plugins install ./plugins/ttok --on-conflict replace --json"]
}
