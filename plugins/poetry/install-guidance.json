{
  "plugin": "poetry",
  "binary": "poetry",
  "check": "which poetry",
  "install_steps": [
    "brew install poetry",
    "Verify: poetry --version",
    "supercli plugins install ./plugins/poetry --on-conflict replace --json"
  ]
}