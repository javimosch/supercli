{
  "name": "disp",
  "version": "0.1.0",
  "description": "CLI tool: disp",
  "source": "https://packages.debian.org/disp",
  "checks": [{ "type": "binary", "name": "disp" }],
  "install_guidance": {
    "plugin": "disp", "binary": "disp", "check": "which disp",
    "install_steps": ["apt-get install disp 2>/dev/null || which disp", "Verify: disp --version", "supercli plugins install ./plugins/disp --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "disp", "resource": "_", "action": "_",
    "description": "Passthrough to disp CLI",
    "adapter": "process", "adapterConfig": { "command": "disp", "passthrough": true, "missingDependencyHelp": "Install disp: apt-get install disp" }, "args": []
  }]
}
