{ "plugin": "disp", "binary": "disp", "check": "which disp", "install_steps": ["apt-get install disp 2>/dev/null || which disp", "Verify: disp --version", "supercli plugins install ./plugins/disp --on-conflict replace --json"] }
