{
  "plugin": "litellm",
  "binary": "litellm",
  "check": "which litellm",
  "install_steps": [
    "pip install litellm",
    "Verify: litellm --help",
    "supercli plugins install ./plugins/litellm --on-conflict replace --json"
  ],
  "note": "AI/LLM tool. Requires Python 3.8+. Also available via: pipx install litellm."
}
