{
  "plugin": "hexd",
  "binary": "hexd",
  "check": "which hexd",
  "install_steps": [
    "brew install hexd",
    "Verify: hexd --version",
    "supercli plugins install ./plugins/hexd --on-conflict replace --json"
  ],
  "note": "Install from package manager"
}