{ "plugin": "arbiter", "binary": "arbiter", "check": "which arbiter", "install_steps": ["apt-get install arbiter 2>/dev/null || which arbiter", "Verify: arbiter --version", "supercli plugins install ./plugins/arbiter --on-conflict replace --json"] }
