{
  "plugin": "sntp",
  "binary": "sntp",
  "check": "which sntp",
  "install_steps": [
    "apt-get install sntp 2>/dev/null || which sntp",
    "Verify: sntp --version",
    "supercli plugins install ./plugins/sntp --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}