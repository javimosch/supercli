{
  "plugin": "frp",
  "binary": "frpc",
  "check": "which frpc",
  "install_steps": [
    "brew install frp",
    "Or download from: https://github.com/fatedier/frp/releases/latest",
    "Verify: frpc --version",
    "supercli plugins install ./plugins/frp --on-conflict replace --json"
  ],
  "note": "frp ships with two binaries: frpc (client) and frps (server). Install via brew, GitHub releases, Docker, or go install. Most usage requires a TOML config file — see https://gofrp.org/docs/ for examples."
}
