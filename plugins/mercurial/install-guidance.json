{
  "plugin": "mercurial",
  "binary": "hg",
  "check": "which hg",
  "install_steps": [
    "brew install mercurial",
    "Verify: hg --version",
    "supercli plugins install ./plugins/mercurial --on-conflict replace --json"
  ]
}
