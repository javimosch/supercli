{
  "description": "GNU parallel — build and execute shell commands in parallel across multiple CPU cores or multiple machines. Supports job queues, remote execution, and pipelining.",
  "tags": ["parallel", "gnu", "shell", "cli", "tool", "parallelism", "concurrency", "jobs"],
  "has_learn": true
}
