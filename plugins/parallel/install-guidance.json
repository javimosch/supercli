{
  "plugin": "parallel",
  "binary": "parallel",
  "check": "which parallel && parallel --version",
  "install_steps": [
    "brew install parallel",
    "Or: apt-get install parallel (Debian/Ubuntu)",
    "Or: dnf install parallel (Fedora)",
    "Verify: parallel --version",
    "supercli plugins install ./plugins/parallel --on-conflict replace --json"
  ],
  "note": "GNU parallel is a shell tool for executing jobs in parallel on a single machine or across multiple machines via SSH. It supports job slots, progress bars, pipelining, and file/argument distribution. Available in most package managers. Source: https://www.gnu.org/software/parallel/."
}
