{
  "plugin": "at",
  "binary": "at",
  "check": "which at",
  "install_steps": [
    "macOS: brew install at",
    "Debian/Ubuntu: apt-get install at",
    "Verify: at --version"
  ],
  "note": "Installed together with atq, atrm, and batch."
}
