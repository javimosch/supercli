{
  "name": "clamdscan",
  "version": "0.1.0",
  "description": "CLI tool: clamdscan",
  "source": "https://packages.debian.org/clamdscan",
  "checks": [{ "type": "binary", "name": "clamdscan" }],
  "install_guidance": {
    "plugin": "clamdscan", "binary": "clamdscan", "check": "which clamdscan",
    "install_steps": ["apt-get install clamdscan 2>/dev/null || which clamdscan", "Verify: clamdscan --version", "supercli plugins install ./plugins/clamdscan --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "clamdscan", "resource": "_", "action": "_",
    "description": "Passthrough to clamdscan CLI",
    "adapter": "process", "adapterConfig": { "command": "clamdscan", "passthrough": true, "missingDependencyHelp": "Install clamdscan: apt-get install clamdscan" }, "args": []
  }]
}
