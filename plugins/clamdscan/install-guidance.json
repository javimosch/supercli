{ "plugin": "clamdscan", "binary": "clamdscan", "check": "which clamdscan", "install_steps": ["apt-get install clamdscan 2>/dev/null || which clamdscan", "Verify: clamdscan --version", "supercli plugins install ./plugins/clamdscan --on-conflict replace --json"] }
