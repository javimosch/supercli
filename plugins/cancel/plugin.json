{
  "name": "cancel",
  "version": "0.1.0",
  "description": "Cancel CUPS print jobs",
  "source": "https://packages.debian.org/cancel",
  "checks": [{ "type": "binary", "name": "cancel" }],
  "install_guidance": {
    "plugin": "cancel", "binary": "cancel", "check": "which cancel",
    "install_steps": ["apt-get install cups", "Verify: cancel --version", "supercli plugins install ./plugins/cancel --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "cancel", "resource": "_", "action": "_",
    "description": "Passthrough to cancel CLI",
    "adapter": "process", "adapterConfig": { "command": "cancel", "passthrough": true, "missingDependencyHelp": "Install cancel: apt-get install cups" }, "args": []
  }]
}
