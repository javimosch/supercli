{ "plugin": "cancel", "binary": "cancel", "check": "which cancel", "install_steps": ["apt-get install cups", "Verify: cancel --version", "supercli plugins install ./plugins/cancel --on-conflict replace --json"] }
