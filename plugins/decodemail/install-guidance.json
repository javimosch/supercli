{ "plugin": "decodemail", "binary": "decodemail", "check": "which decodemail", "install_steps": ["apt-get install decodemail 2>/dev/null || which decodemail", "Verify: decodemail --version", "supercli plugins install ./plugins/decodemail --on-conflict replace --json"] }
