{
  "name": "decodemail",
  "version": "0.1.0",
  "description": "CLI tool: decodemail",
  "source": "https://packages.debian.org/decodemail",
  "checks": [{ "type": "binary", "name": "decodemail" }],
  "install_guidance": {
    "plugin": "decodemail", "binary": "decodemail", "check": "which decodemail",
    "install_steps": ["apt-get install decodemail 2>/dev/null || which decodemail", "Verify: decodemail --version", "supercli plugins install ./plugins/decodemail --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "decodemail", "resource": "_", "action": "_",
    "description": "Passthrough to decodemail CLI",
    "adapter": "process", "adapterConfig": { "command": "decodemail", "passthrough": true, "missingDependencyHelp": "Install decodemail: apt-get install decodemail" }, "args": []
  }]
}
