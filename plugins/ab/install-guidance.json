{
  "plugin": "ab",
  "binary": "ab",
  "check": "which ab",
  "install_steps": ["apt install apache2-utils", "Verify: ab --version", "supercli plugins install ./plugins/ab --on-conflict replace --json"]
}
