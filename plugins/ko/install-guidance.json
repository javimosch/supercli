{
  "plugin": "ko",
  "binary": "ko",
  "check": "which ko",
  "install_steps": [
    "go install github.com/google/ko@latest",
    "Verify: ko --version",
    "supercli plugins install ./plugins/ko --on-conflict replace --json"
  ]
}