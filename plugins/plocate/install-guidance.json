{
  "plugin": "plocate",
  "binary": "plocate",
  "check": "which plocate",
  "install_steps": [
    "See undefined",
    "Verify: plocate --version",
    "supercli plugins install ./plugins/plocate --on-conflict replace --json"
  ],
  "note": "See project repository"
}