{
  "plugin": "ionice",
  "binary": "ionice",
  "check": "which ionice",
  "install_steps": [
    "apt-get install ionice 2>/dev/null || which ionice",
    "Verify: ionice --version",
    "supercli plugins install ./plugins/ionice --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}