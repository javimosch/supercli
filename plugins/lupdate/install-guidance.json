{
  "plugin": "lupdate",
  "binary": "lupdate",
  "check": "which lupdate",
  "install_steps": [
    "apt-get install lupdate 2>/dev/null || which lupdate",
    "Verify: lupdate --version",
    "supercli plugins install ./plugins/lupdate --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}