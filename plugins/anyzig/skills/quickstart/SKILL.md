---
name: anyzig
description: Use this skill when the user wants to use anyzig for One zig to rule them all..
---

# anyzig Plugin

One zig to rule them all.

## Commands

### General
- `anyzig self version` — Print anyzig version
- `anyzig _ _` — Passthrough to anyzig CLI

## Usage Examples
- "Run anyzig"
- "Use anyzig to One zig to rule them all."

## Installation

```bash
pip install anyzig
```

## Key Features
- One zig to rule them all.
- CLI tool, headless and scriptable
