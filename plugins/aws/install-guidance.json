{
  "plugin": "aws",
  "binary": "aws",
  "check": "which aws",
  "install_steps": [
    "brew install aws",
    "Verify: aws --version",
    "supercli plugins install ./plugins/aws --on-conflict replace --json"
  ]
}