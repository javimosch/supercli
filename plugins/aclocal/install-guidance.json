{ "plugin": "aclocal", "binary": "aclocal", "check": "which aclocal", "install_steps": ["apt-get install aclocal 2>/dev/null || which aclocal", "Verify: aclocal --version", "supercli plugins install ./plugins/aclocal --on-conflict replace --json"] }
