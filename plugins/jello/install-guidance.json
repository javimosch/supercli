{ "plugin": "jello", "binary": "jello", "check": "which jello", "install_steps": ["pipx install jello", "Verify: jello --version", "supercli plugins install ./plugins/jello --on-conflict replace --json"], "note": "Requires Python 3.7+. Use pipx for isolated installation: pipx install jello. Also available via pip: pip install jello." }
