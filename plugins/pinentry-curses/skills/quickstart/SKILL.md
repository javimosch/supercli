---
name: pinentry-curses
description: Use this skill when the user wants to use pinentry-curses, cli tool: pinentry-curses.
---

# pinentry-curses Plugin

CLI tool: pinentry-curses.

## Commands
- `pinentry-curses <resource> <action>` — Execute pinentry-curses commands
- `pinentry-curses self version` — Print pinentry-curses version
- `pinentry-curses _ _` — Passthrough to pinentry-curses CLI

## Usage Examples
- "pinentry-curses --help"
- "pinentry-curses self version"

## Installation
```bash
apt-get install pinentry-curses 2>/dev/null || which pinentry-curses
```

## Key Features
- CLI-based tool
- Scriptable and automatable
- Pipeline-ready output
- system based
