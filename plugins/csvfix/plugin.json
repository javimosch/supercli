{
  "name": "csvfix",
  "version": "0.1.0",
  "description": "General-purpose CSV manipulation and repair tool",
  "source": "shell",
  "checks": [{ "type": "binary", "name": "csvfix" }],
  "install_guidance": {
    "plugin": "csvfix",
    "binary": "csvfix",
    "check": "which csvfix",
    "install_steps": ["sudo apt install csvfix", "Verify: csvfix --version", "supercli plugins install ./plugins/csvfix --on-conflict replace --json"]
  },
  "commands": [{
    "namespace": "csvfix",
    "resource": "_",
    "action": "_",
    "description": "Passthrough to csvfix CLI",
    "adapter": "process",
    "adapterConfig": {
      "command": "csvfix",
      "passthrough": true,
      "missingDependencyHelp": "Install csvfix: sudo apt install csvfix"
    },
    "args": []
  }]
}
