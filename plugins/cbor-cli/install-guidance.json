{
  "plugin": "cbor-cli",
  "binary": "cbor-cli",
  "check": "which cbor-cli",
  "install_steps": ["cargo install cbor-cli", "Verify: cbor-cli --version", "supercli plugins install ./plugins/cbor-cli --on-conflict replace --json"]
}
