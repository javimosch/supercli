{
  "plugin": "dotty",
  "binary": "dotty",
  "check": "which dotty",
  "install_steps": [
    "apt-get install dotty 2>/dev/null || which dotty",
    "Verify: dotty --version",
    "supercli plugins install ./plugins/dotty --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}