{
  "plugin": "systemd-sysusers",
  "binary": "systemd-sysusers",
  "check": "which systemd-sysusers",
  "install_steps": [
    "apt-get install systemd-sysusers 2>/dev/null || which systemd-sysusers",
    "Verify: systemd-sysusers --version",
    "supercli plugins install ./plugins/systemd-sysusers --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}