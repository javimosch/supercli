{
  "plugin": "jupytext",
  "binary": "jupytext",
  "check": "which jupytext",
  "install_steps": ["pip install jupytext", "Verify: jupytext --version", "supercli plugins install ./plugins/jupytext --on-conflict replace --json"]
}
