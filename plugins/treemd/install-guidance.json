{
  "plugin": "treemd",
  "binary": "treemd",
  "check": "which treemd",
  "install_steps": [
    "cargo install treemd",
    "Verify: treemd --version",
    "supercli plugins install ./plugins/treemd --on-conflict replace --json"
  ],
  "note": "Also available from source (git clone + cargo build --release) or prebuilt binaries from GitHub Releases. Package manager support may be available via system repositories."
}
