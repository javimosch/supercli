{
  "plugin": "btm",
  "binary": "btm",
  "check": "which btm",
  "install_steps": [
    "brew install bottom",
    "Verify: btm --version",
    "supercli plugins install ./plugins/btm --on-conflict replace --json"
  ],
  "note": "Also available via cargo: cargo install bottom. Pre-built binaries at https://github.com/ClementTsang/bottom/releases. Supports CPU, memory, disk, network, temperature, GPU (NVIDIA), and process monitoring with multiple theming options."
}
