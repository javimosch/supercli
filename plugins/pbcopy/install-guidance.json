{
  "plugin": "pbcopy",
  "binary": "pbcopy",
  "check": "which pbcopy",
  "install_steps": [
    "# Install pbcopy",
    "curl -sS https://github.com/$REPO/$REPO/releases/latest/download/pbcopy-linux-amd64 -o /usr/local/bin/pbcopy",
    "chmod +x /usr/local/bin/pbcopy",
    "# Or via package manager, see github.com/skottok/shortcut"
  ]
}