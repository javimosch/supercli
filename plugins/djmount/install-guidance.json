{ "plugin": "djmount", "binary": "djmount", "check": "which djmount", "install_steps": ["apt-get install djmount 2>/dev/null || which djmount", "Verify: djmount --version", "supercli plugins install ./plugins/djmount --on-conflict replace --json"] }
