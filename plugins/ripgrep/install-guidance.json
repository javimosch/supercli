{
  "plugin": "ripgrep",
  "binary": "rg",
  "check": "which rg",
  "install_steps": [
    "cargo install ripgrep",
    "Verify: rg --version",
    "supercli plugins install ./plugins/ripgrep --on-conflict replace --json"
  ],
  "note": "Also available via: brew, apt, pacman, scoop, winget, nix, and more. Binary name is 'rg'."
}