{
  "plugin": "lefty",
  "binary": "lefty",
  "check": "which lefty",
  "install_steps": [
    "apt-get install lefty 2>/dev/null || which lefty",
    "Verify: lefty --version",
    "supercli plugins install ./plugins/lefty --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}