{
  "plugin": "p-scan",
  "binary": "p-scan",
  "check": "which p-scan",
  "install_steps": ["go install github.com/p-scan/cli@latest", "Verify: p-scan --version", "supercli plugins install ./plugins/p-scan --on-conflict replace --json"]
}
