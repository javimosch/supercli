{
  "plugin": "mdir",
  "binary": "mdir",
  "check": "which mdir",
  "install_steps": [
    "apt-get install mdir 2>/dev/null || which mdir",
    "Verify: mdir --version",
    "supercli plugins install ./plugins/mdir --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}