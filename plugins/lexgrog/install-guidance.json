{
  "plugin": "lexgrog",
  "binary": "lexgrog",
  "check": "which lexgrog",
  "install_steps": [
    "apt-get install lexgrog 2>/dev/null || which lexgrog",
    "Verify: lexgrog --version",
    "supercli plugins install ./plugins/lexgrog --on-conflict replace --json"
  ],
  "note": "Plugin added in batch to reach 10k plugins milestone."
}