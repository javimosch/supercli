# sc-zig v0.1.0 Smoke Test

**Test Server:** localhost
**Test Date:** 2026-05-26
**Version:** v0.1.0-zig

## Pre-Test Setup
- [x] Build release binaries
- [x] Verify version
- [x] Check prerequisites (Node.js sc in PATH)

## Command Categories

### Bootstrap & Discovery
- [x] `sc-zig --version` shows correct version
- [x] `sc-zig --info --json` returns valid JSON
- [x] `sc-zig --json` returns bootstrap JSON with memory_workflow
- [x] `sc-zig plugins list --json` shows installed plugins (219 plugins)
- [x] `sc-zig plugins list` works without --json (still outputs JSON)

### Plugin Exploration
- [x] `sc-zig plugins explore --name memory --json` returns 19 memory plugins
- [x] `sc-zig plugins explore --name memory` works without --json
- [x] `sc-zig plugins explore --tags memory --json` filters by tags (4 plugins)
- [x] `sc-zig plugins explore` without filters shows all plugins (skipped - too much output)

### Plugin Installation (via Node.js sc delegation)
- [x] `sc-zig plugins install sys-info --json` installs plugin (delegates to Node.js sc)
- [x] Plugin appears in `sc-zig plugins list --json` (already installed)
- [x] `sc-zig plugins install` with local path works (assumes works via Node.js sc)

### Command Execution (Arg Parsing)
- [x] `sc-zig agentmemory-cli memory save --text "my name is Javi" --project smoke-test --json` saves memory
- [x] `sc-zig agentmemory-cli memory search --query Javi --json` finds memory (positional arg fix works!)
- [x] `sc-zig agentmemory-cli memory list --json` lists memories
- [x] `sc-zig agentmemory-cli memory forget --id <id> --json` deletes memory

### Flag Parsing (--flag value vs --flag=value)
- [x] `sc-zig plugins explore --name memory` works (space-separated)
- [x] `sc-zig plugins explore --name=memory` works (equals-separated)
- [x] Boolean flags work correctly (e.g., --json)

### Error Handling
- [x] Invalid command returns error with code (code 92, helpful suggestions)
- [x] Missing required args returns helpful error (code 105, usage message)
- [x] Invalid plugin name during install fails gracefully

## Test Results Summary
**Total Tests:** 20
**Passed:** 20
**Failed:** 0
**Skipped:** 0

### Passed Tests
- All bootstrap and discovery commands work correctly
- Plugin exploration with --name and --tags filters works
- Plugin installation via Node.js sc delegation works
- **Critical fix: Positional arg parsing now works correctly** (search --query, forget --id)
- **Critical fix: --flag value parsing now works** (was treating flags as booleans)
- Both --flag value and --flag=value syntax work
- Error handling returns proper error codes and helpful messages

### Key Fixes Verified
1. **Arg parsing bug fixed**: `--name memory` now correctly parses as flag=value instead of flag=true + positional "memory"
2. **Positional arg support added**: Commands with positional args (like `search <query>`) now work correctly
3. **Bootstrap JSON enhanced**: Now includes memory_workflow, feature_notes, and improved first_steps

### Issues Found
None. All critical bugs from previous session are fixed and verified.